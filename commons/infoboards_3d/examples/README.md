# InfoBoard Examples

Example scripts demonstrating how to load and use info boards.

## Key Files
- `load_boards_example.gd` — Shows loading Point, Line, Triangle boards via scene files or programmatically; demonstrates dynamic board switching at runtime
