[gd_scene load_steps=2 format=3 uid="uid://csolewitt0001"]

[ext_resource type="Script" path="res://commons/artifacts/sol_lewitt_wall_drawing/sol_lewitt_wall_drawing.gd" id="1_script"]

; sol_lewitt_wall_drawing.tscn — a plastered panel carrying line geometry
; generated from a written instruction, with that instruction hung beside it.
; All geometry is built procedurally in sol_lewitt_wall_drawing.gd; a map may
; drive "instruction" (corner_lines / not_straight_band / points_connected) and
; "seed" through grid config, so two placements of one rule differ.

[node name="sol_lewitt_wall_drawing" type="Node3D"]
script = ExtResource("1_script")
