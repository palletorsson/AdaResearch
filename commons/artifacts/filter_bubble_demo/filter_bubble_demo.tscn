[gd_scene load_steps=2 format=3 uid="uid://bfiltbubble01"]

[ext_resource type="Script" path="res://commons/artifacts/filter_bubble_demo/filter_bubble_demo.gd" id="1_script"]

; filter_bubble_demo.tscn — a contracting window on a circle that never shrinks,
; backed by filter_bubble_demo.gd. Horizon ring, live population and the ghost
; spiral are all built in _ready() from a fixed seed, so two builds of the same
; placement are pixel-identical and no state is baked into the scene file.

[node name="filter_bubble_demo" type="Node3D"]
script = ExtResource("1_script")
