{
 "artifacts": {
  "csg_union_demo": {
   "dna": {
    "promoted": "2026-08-11",
    "stage": "2 - variation",
    "axes": {
     "fusion": [
      "lobed",
      "distinct",
      "necked",
      "single"
     ],
     "join": [
      "flush",
      "seam",
      "tinted",
      "shared"
     ]
    },
    "default": "fusion=lobed, join=flush - both are short-circuits, not recomputations that happen to land on the same numbers. _fusion_offset() at lobed is a bare `return sphere_offset`, NOT `1.0 * sphere_offset`, so the shipped solid is never rebuilt through a scalar and cannot drift by a float; join=flush takes the path that always shipped - one CSGCombiner3D named UnionRoot at position.y 0.7 holding a CSGBox3D and a CSGSphere3D both at OPERATION_UNION under a single StandardMaterial3D material_override - and builds nothing else. ELEVEN placements corpus-wide, reaching the artifact by three different paths, every one bit-identical. (1) TWO BARE TOKENS, Boolean_Union and Spine_Museum, both the naked string 'csg_union_demo' with no rotation, no y-offset and no config keys: GridInteractablesComponent builds merged_config from default_params + delegate params + token params, this entry declares no default_params, so merged_config is EMPTY and the `if not merged_config.is_empty()` gate means _apply_artifact_config is never called at all - no metadata is stamped and apply_grid_config is never reached. (2) SEVEN exhibit_furniture#kind:plinth#size:m#mount:csg_union_demo plinth mounts, across Trial_castelvecchio / grande / halls / kanazawa / sainsbury / thoronet_circumambulation_void / uffizi: exhibit_furniture._mount_artifact loads the scene, instantiates, positions and adds the child, and never calls apply_grid_config or stamps identity, so those build at pure export defaults too. (3) TWO curation_station reach-paths, Corridor_Boolean_Union and Curation_Bay_boolean_surfaces_0. The station DOES call apply_grid_config, with exactly {'emissive': false} - a key no build path reads - so the new guarded body stamps config_emissive into metadata, recomputes the signature, finds it unchanged and returns before any teardown. It also calls ArtifactIdentity.stamp before add_child, and stamp reads default_params and nothing else from the entry, so a dna block opens no config channel here. A placement naming sphere_offset directly still WINS over fusion: _offset_explicit short-circuits _fusion_offset() ahead of the axis, exactly as the two promoted siblings do. (A twelfth occurrence, Spine_Museum's map_info.spine_trail hero field, is metadata and instantiates nothing.)",
    "was": "one fused blob, rotating, with a Label3D reading 'A u B' and nothing else. sphere_offset is this artifact's own declared critical_parameter and its @identity states it as an AMOUNT - 'far apart = barely fused; overlapping = one blob' - while exposing it only as an unnamed Vector3 that no map token has ever set, so eleven placements are eleven copies of one point on a range the author wrote down. Its own `needs` line names the gap too: 'Missing: a before/after toggle that separates the two inputs back out'. apply_grid_config assigned body_color, box_size and sphere_radius with no rebuild behind it, so all three worked only because config happens to arrive before _ready.",
    "note": "fusion is SHARED, taken character for character from implicit_surface_modeling, metaballs and raymarched_metaballs - same word, same four values. It transplants because fusion's values name STATES OF THE RESULT (how many bodies you see) rather than settings of a knob, which is why it survives crossing from a smoothing parameter to a rigid offset. Resolved here as scalar multiples k of the shipped sphere_offset (0.4, 0.25, 0.0), |d| = 0.4717, so direction never changes and the axis is pure magnitude, against box_size 0.7 (half-extent 0.35) and sphere_radius 0.5: distinct k=2.3 puts the centre at (0.92, 0.575, 0) and the nearest box point (0.35, 0.35, 0) at 0.613 against a radius of 0.5 - a 0.113 GAP, two disjoint solids still under OPERATION_UNION, which is the before/after separation the @identity asks for and argues the uncomfortable thing, a lawful union that looks like a pile; necked k=1.85 leaves 0.094 of penetration across the box's +X/+Y edge, connected through a narrow waist; lobed k=1.0 is SHIPPED, two readable lobes under one skin; single k=0 is concentric, radius 0.5 against a half-extent of 0.35 and a corner distance of 0.606, so the box vanishes inside the sphere except eight corner spikes and the union reads as one primitive with defects. WHAT DIFFERS FROM THE METABALL SIBLINGS, said plainly: there smin(a, b, k) 'only acts where |a - b| < k', so necked is a smooth bridge produced by a blend and the ladder is a continuum; CSG union has no k and fuses absolutely at first contact, so here necked is a CREASE with a discontinuous normal and the ladder is a sequence of topological events. Same four states, two ontologies of joining. And the shipped rung is not the same rung - over there it is distinct and 'the ladder runs upward only', here it is lobed and the ladder runs both ways from it, which is why this derived value list is default-first and therefore in a different ORDER from theirs. The word and the four values are identical; the reading order records where each artifact already stood. join is NEW, and it is the registry's own sentence about this token - 'the gentlest Boolean, and the one that hides the most... the seam between them dissolved into a single skin' - turned into a knob: a ladder of disclosure depth, three of whose four rungs change nothing but the skin. flush is SHIPPED, one root material_override, nothing says where A ends. seam marks the intersection curve as a bright tube built as (sphere shell r +/- 0.015) INTERSECT (box shell size +/- 0.015), each shell an outer primitive minus an inner one - exact for any offset and radius, no curve maths - and deliberately 0.015 proud of BOTH surfaces so the marker never lands coplanar with the skin it marks, which is what this sequence's own coincident_face is about. tinted drops the root override for per-primitive CSGBox3D.material / CSGSphere3D.material, so A's share of the skin stays body_color and B's takes CUT_COLOR: the provenance of every square centimetre of one continuous surface. shared ghosts A INTERSECT B inside the fused solid - the volume both shapes claim and union counts once, |A u B| = |A| + |B| - |A n B| rendered rather than asserted. The corpus's nearest word to join is workings (outcome | trace | operands | expression, 26 artifacts, held in this very sequence by csg_difference_demo) and it asks a different question: how much of the DERIVATION is drawn. join asks nothing about derivation, only what the finished surface confesses about its own origins. seam is unavailable as a NAME - escher_staircase and florensky_sphere hold seam = none|hairline|gap|scaffold|field for the impossible-joint question - so it is used here only as a value.",
    "declined": "strike (centred | raised | corner | grazing) - the family's most-shared word, carried character for character by fontana_puncture, csg_difference_demo and csg_compose_workbench, and driven by the very export this artifact declares as its critical_parameter - is REFUSED, and the refusal is the finding. strike's four values are PLACES and its question is where the void struck; a union has no void, nothing strikes anything, and this artifact's @identity frames sphere_offset as a magnitude ('far apart / overlapping'), not a location. Taking the word would put four location names where four amounts belong, which is the exact fault exhibit_vitrine renamed regard->guard to fix: 'One word cannot mean both across three artifacts standing in the same room' - and these five CSG tokens literally stand in one room, all placed by the single curation_station in Curation_Bay_boolean_surfaces_0. subtraction_suite already set the precedent for refusing strike on argument, and csg_intersection_demo refused it in the same pass for its own reason. Declining leaves the family clean: strike = direction, for artifacts that place a void; fusion / breach / consensus = amount, for artifacts whose question is how much. Also refused: an axis over sphere_radius. It is a size, a size is legible as a number, and fontana_puncture's breach already owns amount-of-radius for a cube and a sphere.",
    "fixture": {
     "rotation_speed": 0.0,
     "capture_anchor": "on"
    },
    "fixture_why": "TWO entries, and only the first is a pin. rotation_speed: UnionRoot turns at 0.35 rad/s from the first frame, so an unpinned sweep photographs each variant at whatever yaw its settle landed on and the diff measures rotation instead of the axis. Pinning to zero holds the shipped look at t=0 rather than inventing a new one. Written as a NUMBER and not the string '0.0': the sweep assigns fixture values straight onto the typed float export before _ready, where a String is rejected. capture_anchor is not a pin but a BENCH SWITCH, and the word, the two values and the gate are csg_intersection_demo's, character for character. This artifact owns no MeshInstance3D at all - the body is CSG and the caption is a Label3D - and capture_config_sweep._subtree_aabb counts MeshInstance3D, falls back to MultiMeshInstance3D and otherwise returns a 1 m box at the origin, so without it every rung of both axes is photographed against a camera placed for a shape that is not there. It defaults to 'off' and apply_grid_config deliberately does not read it, so no map token can switch it on; only direct property assignment, which is what dna.fixture does, reaches it. That gating is load-bearing rather than tidy: GridInteractablesComponent._compute_local_aabb walks CSGShape3D as well as MeshInstance3D and feeds _auto_ground_artifact, and the ladder-sized anchor hangs 0.15 below the box floor (at `single` the sphere is concentric and reaches -0.5 where the box stops at -0.35), so an ungated anchor would lift all eleven placements by exactly that much.",
    "framing": 0.73,
    "framing_why": "DERIVED, not measured, and the first sweep should correct it. The capture anchor is sized over the WHOLE fusion ladder so the camera is identical for all sixteen combinations whether or not the fixed-camera pre-pass runs: local x[-0.5, 1.42], y[-0.5, 1.075], z[-0.5, 0.5], which is 1.92 x 1.575 x 1.0 and a diagonal of 2.677. The shipped solid's own box is 1.25 x 1.1 x 1.0, diagonal 1.942. 1.942 / 2.677 = 0.725, so a rig fitting by the ladder diagonal shrinks the shipped variant by very nearly a quarter and 0.73 hands that back. NOTE the correction to the approved design, which put this at 0.78 on the assumption that `distinct` sets the union: it sets the HIGH corner (x 1.42, y 1.075) but not the low one - at `single` the sphere is concentric and reaches -0.5 on x and y where the box stops at -0.35 - so the true ladder diagonal is 2.677 and not 2.48. PAD is 1.9, so at 0.73 the effective pad is still 1.39 and even the widest rung is nowhere near cropped.",
    "rebuild_guard": "apply_grid_config now stamps config into metadata, re-reads it, and returns early unless a value the build actually reads has changed - and returns immediately if _ready has not built yet. The old body assigned body_color, box_size and sphere_radius with no rebuild at all, which worked only because config always arrives before _ready. Nine of the eleven placements never call it and the two that do pass one key nothing reads, so the guard means not one of them is even torn down. The seam marker and the shared ghost hang off a plain Node3D rig rather than off UnionRoot: a CSGCombiner3D nested inside another combiner is a SHAPE, not a container, and would fuse both of them back into the very solid they exist to comment on. The rig sits at the origin and the solid's offset is purely vertical, so rotating the rig is the identical world transform the old _root_csg.rotation.y produced - Translate(0, 0.7, 0) * Rot_y(t) either way.",
    "still_note": "every rung of both axes is standing geometry and materials resolved during _ready; there is no animation here except the yaw the fixture pins. fusion is legible at any yaw because the count of connected bodies is yaw-independent. The weak rung by frame area is join=seam: a 0.03 tube crossing a 0.7 m solid is roughly 2 percent of the subject, so if the sweep reports it faint the honest remedy is to WIDEN THE TUBE (SEAM_PROUD), not to re-word the axis - written down here rather than discovered in the verdict. Two rungs are legitimately empty at one end of the other axis and both are truthful rather than broken: at fusion=distinct the two shells never meet, so there is no seam because nothing joined, and A INTERSECT B is empty, so `shared` correctly renders nothing where |A u B| = |A| + |B|.",
    "placements": 11
   },
   "name": "CSG Union Demo",
   "lookup_name": "csg_union_demo",
   "description": "Two CSG primitives combined with the union operator. Visualizes A union B as a single composite solid.",
   "scene": "res://algorithms/boolean_surfaces/csg_union_demo.tscn",
   "category": "boolean_surfaces",
   "sequence": "boolean_surfaces",
   "artifact_type": "boolean_surfaces",
   "qfep_term": "F",
   "capacity": "UNION as composition",
   "dev_themes": [
    "boolean_surfaces",
    "sieve_2026_05_13"
   ],
   "dev_category": "boolean_surfaces",
   "complexity": "intermediate",
   "tags": [
    "csg",
    "boolean",
    "union",
    "F_order"
   ],
   "include_in_map_data": true,
   "map_ready": true,
   "map_sequences": [
    "boolean_surfaces"
   ],
   "parameters": {
    "footprint": [
     1,
     1,
     1
    ],
    "size_group": "compact"
   },
   "qfep_connection": "A∪B: two solids declared one. The gentlest Boolean, and the one that hides the most — union looks like putting things next to each other, but it fuses them, the seam between them dissolved into a single skin. The chapter's opening insistence that composition is not a pile: the moment two forms join, their boundary is renegotiated, and the inside of the whole is more than the sum of the insides."
  },
  "csg_intersection_demo": {
   "dna": {
    "promoted": "2026-08-11",
    "stage": "2 - variation",
    "axes": {
     "consensus": [
      "broad",
      "unanimous",
      "narrow",
      "token"
     ],
     "forfeit": [
      "none",
      "numeral",
      "gauge",
      "losses"
     ]
    },
    "default": "consensus=broad, forfeit=none - both defaults are short-circuits rather than recomputations. _cut_offset() returns sphere_offset (0.3, 0.2, 0.1) by a BARE RETURN at broad, not k=1.0 multiplied through; the two are bit-identical for these floats, so the reason is not arithmetic but that the shipped vector must never travel the same code path as the derived ones, and a later edit to the scale table therefore cannot reach it. forfeit=none builds nothing beyond the single CSGCombiner3D named IntersectionRoot at y 0.7 the artifact has always built, and never runs one iteration of the volume lattice. TEN placements, every one bit-identical: ONE bare token (Boolean_Intersection r5 c5, the naked string - no rotation, no y-offset, no config - so merged_config is empty and apply_grid_config is never called at all); SEVEN exhibit_furniture#kind:plinth#size:m#mount:csg_intersection_demo mounts across the Trial_* museum maps (castelvecchio, grande, halls, sainsbury, kanazawa, thoronet, uffizi), where _mount_artifact instantiates, positions and never calls apply_grid_config; and TWO curation_station reach-paths (Corridor_Boolean_Intersection, Curation_Bay_boolean_surfaces_0) which forward exactly {\"emissive\": false}, a key no build path reads, so the guarded body stamps it as metadata, recomputes the signature, finds no change and returns before teardown. The entry declares no default_params, so ArtifactIdentity.stamp adds no config either. A placement that names sphere_offset directly still WINS over consensus, via _offset_explicit.",
    "was": "one lens, rotating, with a Label3D reading 'A n B' and nothing else. The truth line is a QUANTITY claim - 'what both shapes agree on is always less than either alone' - and it had shipped at exactly ONE overlap, ten times, with no way to see it hold anywhere else. The artifact's own @identity already spelled out the ladder it never got ('small overlap = a sliver; full overlap = nearly a whole primitive; no overlap = nothing at all') against an export reachable only as an unnamed Vector3, and ended by naming what it lacked: 'a readout of the surviving volume as the overlap shrinks toward zero'. apply_grid_config assigned body_color and had no rebuild behind it, so the one knob it did expose could not take effect after _ready.",
    "note": "consensus is a NEW word and it asks HOW MUCH OF WHAT WAS BROUGHT SURVIVES. Nothing in the corpus asks how much of its inputs an operation returns: fontana_puncture's breach is an amount but an amount of REMOVAL (how far the void has eaten, driven by radius against a fixed cube), and this is an amount of SURVIVAL - the two run opposite. The word comes from the artifact's own vocabulary, whose @identity says 'agreement', 'consensus', 'both parties', so the rungs read as degrees of agreement rather than as four positions of a sphere. Resolved as scalar multiples k of the SHIPPED sphere_offset, so |d| = 0.374 stays fixed in direction and the axis is pure magnitude, against a box half-extent of 0.4 and a sphere radius of 0.55. unanimous k=0, concentric: face distance 0.4 < r 0.55 < edge midpoint 0.566 < corner 0.693, so the survivor is A ITSELF with its twelve edges shaved and its eight corners cut, roughly 95 percent of the box - agreement costing almost nothing. broad k=1, SHIPPED, the lens. narrow k=1.9: centre (0.57, 0.38, 0.19), nearest box point (0.4, 0.38, 0.19) at 0.17, penetration 0.38 - a cap on one face. token k=2.6: centre (0.78, 0.52, 0.26), nearest box point (0.4, 0.4, 0.26) at 0.398, penetration 0.152 - a sliver at the box's edge, agreement in name only. THE LADDER STOPS SHORT OF THE VANISHING POINT on purpose: solving (0.3k - 0.4)^2 + (0.2k - 0.4)^2 = 0.55^2 puts the last surviving overlap at k = 3.03, and past it the frame measures NO RENDER so the verdict becomes a fact about the capture. The registry order is the sweep's tile order, default leftmost, which is why 'broad' is listed first; the ladder in the code runs unanimous, broad, narrow, token. forfeit is also NEW and asks WHETHER THE OBJECT WILL SAY WHAT AGREEMENT COST - the artifact's own written gap, turned into a ladder from silence to the discarded material itself. none SHIPPED: the lens alone, you are handed the consensus and never told its price. numeral: a dark plate 0.9 x 0.22 hung at y 0.25 carrying |A|, |B|, |A n B| and the two percentages, all three integrated on a deterministic 48-cubed lattice of cell centres over the union AABB - no RNG anywhere, so no seed and no reproducibility question, and all three measured by the SAME instrument on purpose, since an analytic |A| against a sampled |A n B| would make the percentages disagree with the bars beside them. gauge: three bars on a small base at x=+0.75, heights proportional to those same three volumes with the tallest normalised to 0.6 m, so the truth line stops being a sentence - the third bar is ALWAYS the shortest, because |A n B| <= min(|A|, |B|) is a fact about the operator and not about this placement. losses: the two discarded regions A - B and B - A put back as translucent ghosts exactly where they were taken from, so the frame reconstitutes A u B around the solid survivor and you see the whole of what both parties brought. losses is NOT csg_compose_workbench's atoms: atoms puts the three disjoint regions on three SEPARATE benches to argue the regions are primitive and the operators are not, while this reassembles two of them IN PLACE around the survivor to argue what THIS one operation threw away. The two axes compose with no null cell - forfeit=gauge photographed at consensus=token shows the third bar collapsed, which is the 'shrinks toward zero' sentence in a single frame.",
    "declined": "strike (centred | raised | corner | grazing), the shared family word, is REFUSED here even though it is reused character for character across fontana_puncture, csg_difference_demo and csg_compose_workbench for this same export. Its four values are LOCATIONS and its question is where the void struck; this artifact has no void, it has a survivor, and its own @identity converts the identical export into a MAGNITUDE ('small overlap = a sliver ... no overlap = nothing at all'). strike here would be four place-names standing in for four amounts. csg_compose_workbench is not a counter-example: the bench needs one placement of B to drive five operators at once, which is a question about the SET, and a single-operator demo has no set. Also declined: THE EMPTY RUNG. 'No overlap = nothing at all' is the truest thing this operator can do and it is refused because an empty frame measures NO RENDER - fontana_puncture stopped breach short of 0.866 for exactly this and said so. The ladder ends at token, k=2.6, with the vanishing point at k=3.03 named in the note so the omission is visible rather than tidy. Also declined: assay (none | gauge | control | chart | vitrine, 19 artifacts), the near miss for forfeit. It is the softbody bench family's word for apparatus placed AROUND A SPECIMEN - petri_dish_worms defines control as 'a second, identical, EMPTY dish' - and a Boolean demonstration has no specimen; transplanting it would force vitrine to mean something here or force it to be dropped, and dropping a rung from a nineteen-artifact word is exactly the drift the corpus keeps catching. readout (18 artifacts) is refused for a mechanical reason: its members forward through XYZSliderPlate.readout_name() so their lists cannot drift, and this artifact has no such alias path.",
    "fixture": {
     "rotation_speed": 0.0,
     "capture_anchor": "on"
    },
    "fixture_why": "TWO entries, and only the first is a pin. rotation_speed: IntersectionRoot spins at 0.35 rad/s from frame one, so without this the sweep photographs each variant at whatever yaw its settle happened to land on and the diff measures rotation instead of the axis. Numeric and not the string '0.0': the sweep assigns fixture values straight onto the property before _ready, where a String would be rejected by the typed float. capture_anchor is NOT a pin, it is a repair to the bench, and it is the finding of this promotion. capture_config_sweep fits its camera to a union AABB built from MeshInstance3D nodes only; a CSG solid is a CSGShape3D and a Label3D is not a mesh either, so at forfeit=none the ONLY measurable body this artifact has is the plate LabelFramer builds behind the 'A n B' caption at y 1.7 - every rung of consensus would be photographed with the camera fitted to the caption and the survivor below the frame, four tiles measuring a fact about the bench. capture_anchor=on builds A itself, the primitive every survivor is a subset of, as an invisible box (layers = 0 is per-instance: drawn by no camera, casting no shadow, keeping its AABB), sized to A exactly and not one centimetre more. It is reachable ONLY by direct property assignment; apply_grid_config deliberately does not read it, so no map token and no default_params can turn it on. That gate is load-bearing rather than tidy: auto-grounding measures CSGShape3D as well as MeshInstance3D and snaps the lowest point to the cell surface, so an anchor reaching 0.05 m below the survivor's own base would silently drop all ten placements by that much.",
    "framing": 0.73,
    "framing_why": "DERIVED, not measured, and the first sweep should correct it. With the capture anchor in place the union across the consensus rungs is the anchor (0.8 box at y 0.3-1.1) merged with the caption plate at y 1.7, about 0.8 x 1.52 x 0.9, diagonal 1.94; the largest rung, unanimous, is that 0.8 box at diagonal 1.386, and 1.386/1.94 = 0.715 keeps the biggest survivor filling the frame instead of floating in it. Rounded to 0.73 because the caption plate's width is estimated from the font metrics rather than measured. Expect token, the smallest rung, at roughly half a percent of frame: that is the axis working - the bite is carried by what DISAPPEARS between unanimous and token, order eight percent of frame - and it must not be read as NO RENDER.",
    "rebuild_guard": "apply_grid_config now stamps config into metadata, re-reads it, and returns early unless a value the build actually reads has changed - and returns immediately if _ready has not built yet. The old body assigned body_color and had no rebuild at all, which worked only because config always arrives before _ready. Eight of the ten placements pass no keys and the other two pass a key no build path reads, so the guard means none of them is even torn down. The loss ghosts hang off a plain Node3D rig rather than the combiner: a CSGCombiner3D nested inside another combiner is a shape, not a container, and would have unioned the ghosts back into the solid they comment on. The rig rotates about the same axis the combiner did - _root_csg's offset is purely vertical, x = z = 0, and translation along Y commutes with rotation about Y - so the world transform is unchanged, and _compute_local_aabb recurses through Node3D children applying their transforms, so auto-grounding's min_y is unchanged too.",
    "still_note": "every rung of both axes is standing geometry or text resolved during _ready. The survivor's SIZE is the whole reading of consensus, and size is the most still-legible property there is: a near-whole box, a lens, a cap, a sliver. gauge and losses each add roughly as much frame area as the survivor itself; numeral is the weakest by area and is kept because it is literally the readout the author asked for and because a text readout is established practice on eighteen artifacts. Nothing animates once the yaw is pinned."
   },
   "name": "CSG Intersection Demo",
   "lookup_name": "csg_intersection_demo",
   "description": "Two CSG primitives with intersection operator. Only the overlap survives.",
   "scene": "res://algorithms/boolean_surfaces/csg_intersection_demo.tscn",
   "category": "boolean_surfaces",
   "sequence": "boolean_surfaces",
   "artifact_type": "boolean_surfaces",
   "qfep_term": "F",
   "capacity": "INTERSECT as overlap",
   "dev_themes": [
    "boolean_surfaces",
    "sieve_2026_05_13"
   ],
   "dev_category": "boolean_surfaces",
   "complexity": "intermediate",
   "tags": [
    "csg",
    "boolean",
    "intersection",
    "F_order"
   ],
   "include_in_map_data": true,
   "map_ready": true,
   "map_sequences": [
    "boolean_surfaces"
   ],
   "parameters": {
    "footprint": [
     1,
     1,
     1
    ],
    "size_group": "compact"
   },
   "qfep_connection": "A∩B: only the overlap survives. The strictest operator — intersection keeps solely the space both solids agreed to occupy and discards everything else, so two large forms can yield a tiny sliver or nothing at all. The primitive of the joint: where two things meet is not where they touch but where they *coincide*, and this operator is the chapter's tool for finding that shared interior."
  },
  "csg_difference_demo": {
   "name": "CSG Difference Demo",
   "lookup_name": "csg_difference_demo",
   "description": "A box with a sphere subtracted. The operator that makes cavities and openings.",
   "scene": "res://algorithms/boolean_surfaces/csg_difference_demo.tscn",
   "dna": {
    "promoted": "2026-08-03",
    "stage": "2 - variation",
    "axes": {
     "workings": [
      "outcome",
      "trace",
      "operands",
      "expression"
     ],
     "strike": [
      "centred",
      "raised",
      "corner",
      "grazing"
     ]
    },
    "default": "workings=outcome, strike=corner - both defaults are short-circuits, not recomputations. outcome builds nothing beyond the single CSGCombiner the artifact always built, and corner returns the raw sphere_offset (0.3, 0.1, 0.3) untouched instead of resolving a fraction of box_size, so all 27 placements are bit-identical. None of the 27 passes a config key at all, and a placement that names sphere_offset directly still overrides strike.",
    "was": "one carved box, rotating, with a Label3D reading 'A - B' and nothing else. sphere_offset is the artifact's own declared critical_parameter ('where the bite lands') and was reachable only as an unnamed Vector3; apply_grid_config assigned body_color and had no rebuild behind it, so the knob it did expose could not take effect after _ready.",
    "note": "This is fontana_puncture's direct sibling - the same cube-minus-sphere, one as art and one as demonstration - so strike is reused from it word for word and value for value. What differs, said plainly: the NUMBERS are not fontana's and cannot be. Its sphere is 1.36 half-extents, this one's is 1.10, so the same fraction cuts a different topology; and its 'corner' is a body diagonal while the value shipped here is an XZ diagonal sitting low. The two agree on the word and the list and each resolves it against its own geometry, which is what makes them comparable rather than merely identical. workings is the axis a demonstration wants and an artwork does not: how much of the operation it is willing to show. outcome = only the answer; trace = the removed material (A intersect B) put back as a translucent ghost in the cavity it left, which is 'absence is constructive' rendered rather than asserted; operands = the uncut box and the subtracting sphere faint and in place; expression = the operands plus sphere_offset drawn as a rod between the two centres, with both terms labelled. Word and value list taken character for character from geometric_transformations, bounce_well, transform_composition_workbench and the vector_op console.",
    "declined": "breach, fontana's amount axis, is NOT promoted here: the radius is fixed at 0.55 against a 1.0 box and this artifact's declared critical parameter is the offset, so a second amount axis would repeat the family's existing claim and add nothing a demo argues. The non-commutative B - A that the @identity asks for is a real third demonstration and belongs beside this object, not inside workings - B - A is not a different amount of showing, it is a different thing shown.",
    "fixture": {
     "rotation_speed": "0.0"
    },
    "fixture_why": "The rig spins at 0.35 rad/s, so the sweep photographs each variant at whatever yaw the settle happened to land on. Pinning it to zero costs about seven degrees against the shipped look and makes strike measurable instead of arbitrary; centred, raised and grazing move on Y and are legible from any yaw regardless.",
    "rebuild_guard": "apply_grid_config now compares a signature of every value the build reads and returns early when nothing changed, so a placement re-announcing its config no longer rebuilds. The ghosts hang off a plain Node3D rig rather than the combiner: a CSGCombiner3D nested inside another combiner is a shape, not a container, and would have unioned the trace ghost back into the solid it comments on. The rig rotates about the same axis the combiner did (the offset is purely vertical), so the world transform is unchanged."
   },
   "category": "boolean_surfaces",
   "sequence": "boolean_surfaces",
   "artifact_type": "boolean_surfaces",
   "qfep_term": "F",
   "capacity": "SUBTRACT as cavity",
   "dev_themes": [
    "boolean_surfaces",
    "sieve_2026_05_13"
   ],
   "dev_category": "boolean_surfaces",
   "complexity": "intermediate",
   "tags": [
    "csg",
    "boolean",
    "difference",
    "F_order"
   ],
   "include_in_map_data": true,
   "map_ready": true,
   "map_sequences": [
    "boolean_surfaces"
   ],
   "parameters": {
    "footprint": [
     1,
     1,
     1
    ],
    "size_group": "compact"
   },
   "qfep_connection": "A−B: the box remembers the sphere by its absence. Difference is the operator of cavities and openings — subtract one solid from another and what remains is shaped by a thing no longer there, the hollow a negative portrait of what was removed. The walk's central power: every window, every socket, every carved bowl is a subtraction, form defined by a precise act of taking-away.",
   "measurements": {
    "aabb_size": [
     1.428,
     1.79,
     1.392
    ],
    "aabb_center": [
     0.185,
     1.095,
     0.167
    ],
    "grid_cells": [
     2,
     2
    ],
    "max_dimension_m": 1.79,
    "measured_at": "2026-07-23T13:18:22Z",
    "source": "probe_artifact_elements"
   },
   "footprint_measured": [
    2,
    1,
    2
   ],
   "spatial_needs": {
    "footprint_cells": 4,
    "footprint_source": "probe_artifact_elements"
   }
  },
  "csg_compose_workbench": {
   "dna": {
    "promoted": "2026-08-03",
    "stage": "2 - variation",
    "axes": {
     "algebra": [
      "triad",
      "ordered",
      "complete",
      "atoms"
     ],
     "strike": [
      "centred",
      "raised",
      "corner",
      "grazing"
     ]
    },
    "default": "algebra=triad, strike=corner - PRESERVES all seven placements exactly, and both defaults are short-circuits rather than recomputations. _recipe() at triad returns the same three dictionaries _build_presets used to hold inline, in the same order, with the same colours and the same labels; the bench width formula preset_spacing * (count + 0.2) evaluates at count=3 to preset_spacing * 3.2, the literal that shipped, and the layout formula (i - (count-1)/2) * preset_spacing evaluates to (i - 1) * preset_spacing, likewise the literal. strike=corner returns preset_offset (0.2, 0.1, 0.1) untouched instead of resolving a fraction of box_size. All seven placements are bare tokens passing no config keys at all, plus two reach-paths through curation_station whose #with_wall / #with_pillars / #with_barrier flags belong to the station and are never forwarded here. A placement that names preset_offset directly still wins over strike.",
    "was": "no declared axis - the runner refused this token for NO TURNABLE KNOBS. Three exports existed and none of them was an argument: bench_color is palette, rotation_speed is a rate and invisible to a still, preset_spacing is the declared critical_parameter and a distance. The operator SET - the thing the artifact exists to hold - was a three-element literal array inside _build_presets, and the operand pair (box 0.45, sphere r 0.3 at (0.2, 0.1, 0.1)) was three more literals in the loop below it. apply_grid_config assigned preset_spacing with no rebuild behind it, so the one knob it exposed could not take effect after _ready.",
    "note": "algebra is a NEW word. Nothing in the corpus asks what a closed operator set consists of; the nearest neighbours ask about one operation's depth (fontana_puncture's breach) or its exposition (workings). triad is Godot's three CSG primitives and the three the bracelet offers. ordered adds B - A beside A - B, and that pair is the entire proof that difference is the only non-commutative operator here - which is exactly the demonstration csg_difference_demo declined to swallow into its own workings and deferred to 'a second object beside this one'. This is that object. complete adds the symmetric difference, built as a nested (A union B) combiner minus a nested (A intersect B) combiner, and labelled longhand rather than as a symbol because the label IS the claim: XOR is derivable and is not a primitive of the tool. atoms drops the operators entirely for the three disjoint regions A-B, A intersect B, B-A that every one of them is a union of - three benches again, and the identical count is the point, since the room looks like the shipped one while arguing the opposite thing. strike is reused CHARACTER FOR CHARACTER from fontana_puncture and csg_difference_demo: same word, same four values, same question. The numbers are not theirs and cannot be - fontana's sphere is 1.36 half-extents, the demo's 1.10, this bench's 1.33 (r 0.3 against a 0.225 half-extent) - so the same fraction of box_size cuts a different topology in each, which is what makes the three comparable rather than merely identical. On this artifact it does something neither sibling can: one offset drives every operator on the bench at once, so the axis puts the bench's own claim (that these are one family) under load. At centred the union is a ball with eight box corners through it, the intersection is that ball flattened on six faces, and the difference breaches all six faces so the box survives as its twelve edges - three unmistakably different solids from a single placement of B.",
    "declined": "workings (outcome | trace | operands | expression), the twelve-artifact family word, genuinely FITS this artifact and is still not shipped. The sibling already holds it: csg_difference_demo's promotion is precisely how much of ONE operation is drawn, and repeating it here across up to five presets would fill the frame with translucent ghosts and say the family's existing thing five times over. The hub's question is the set, the demo's question is the derivation; kept apart the two artifacts are comparable, merged one of them is redundant. Also declined: an axis over preset_spacing, the declared critical_parameter. It is a distance, and a distance is legible as a number - it does not need a vocabulary laid over it.",
    "fixture": {
     "rotation_speed": 0.0
    },
    "fixture_why": "Every preset root spins at 0.25 rad/s from the first frame, so without this the sweep photographs each variant at whatever yaw its settle happened to land on and the diff measures rotation instead of the axis. rotation_speed is a plain float export read only by _process, so pinning it to zero holds the presets at the yaw they have at spawn - the shipped look at t=0, not a new one. Numeric and not the string '0.0': the sweep assigns fixture values straight onto the property before _ready, where a String would be rejected by the typed float.",
    "rebuild_guard": "apply_grid_config now stamps config into metadata, re-reads it, and returns early unless a value the build actually reads has changed - and returns immediately if _ready has not built yet. The old body assigned preset_spacing and had no rebuild at all, which worked only because config always arrives before _ready. All seven placements pass no keys, so the guard means none of them is even torn down.",
    "still_note": "everything both axes build is standing CSG geometry resolved during _ready. There is no animation on this bench except the yaw the fixture pins, so a single frame is a complete account of both values."
   },
   "name": "CSG Compose Workbench",
   "lookup_name": "csg_compose_workbench",
   "description": "Workbench holding all three CSG operators side by side. The introduction artifact for the csg-compose catalyst affordance.",
   "scene": "res://algorithms/boolean_surfaces/csg_compose_workbench.tscn",
   "category": "boolean_surfaces",
   "sequence": "boolean_surfaces",
   "artifact_type": "boolean_surfaces",
   "qfep_term": "F",
   "capacity": "COMPOSE with CSG ops",
   "dev_themes": [
    "boolean_surfaces",
    "sieve_2026_05_13"
   ],
   "dev_category": "boolean_surfaces",
   "complexity": "intermediate",
   "tags": [
    "csg",
    "catalyst_affordance",
    "workbench"
   ],
   "include_in_map_data": true,
   "map_ready": true,
   "map_sequences": [
    "boolean_surfaces"
   ],
   "parameters": {
    "footprint": [
     1,
     1,
     1
    ],
    "size_group": "mixed"
   },
   "qfep_connection": "All three operators on one bench — union, intersection, difference, side by side, the chapter's grammar laid out as a palette. The workbench where you learn that solids have an algebra: any complex form is a sentence in these three verbs, and constructive solid geometry is the claim that you can say any shape by combining primitives lawfully enough."
  },
  "csg_architecture_cavity": {
   "dna": {
    "promoted": "2026-08-11",
    "stage": "2 - variation",
    "axes": {
     "openwork": [
      "sampler",
      "door",
      "arcade",
      "screen"
     ],
     "interior": [
      "none",
      "corner",
      "court",
      "room"
     ]
    },
    "default": "openwork=sampler, interior=none - PRESERVES all eleven placements exactly, and both defaults are short-circuits rather than recomputations. _build_openings matches on openwork and sampler falls to the `_:` branch's `front` arm, which is the shipped body verbatim: _sub_door, _sub_porthole(porthole_offset), _sub_arch(arch_offset_x), in that order, from those literals, with no scaling, no counting and no layout arithmetic anywhere in the path. The wall box short-circuits too - at run_len == wall_size.x it assigns wall_size itself rather than rebuilding a Vector3 that happens to equal it. interior=none returns from _build_interior_runs before a second run exists and never touches _build_ground, so the slab stays 5.4 x 0.04 x 4.4 at y -0.02 and the child order is WallRoot, Label3D, Ground exactly as it shipped. THE ELEVEN, counted: 2 bare tokens (Boolean_Architecture_Cavity and Color_Context_Placed, both the naked string with no rotation, no y-offset and no config keys); 7 exhibit_furniture#kind:plinth#size:m#mount:csg_architecture_cavity mounts across Trial_castelvecchio, Trial_grande, Trial_halls, Trial_kanazawa, Trial_sainsbury, Trial_thoronet_circumambulation_void and Trial_uffizi - pure export defaults, because _mount_artifact instantiates and seats the scene and never calls apply_grid_config at all; and 2 curation_station reach-paths (Corridor_Boolean_Architecture_Cavity, Curation_Bay_boolean_surfaces_0) which forward exactly {\"emissive\": false}, a key no build path reads. The entry declares no default_params, so ArtifactIdentity.stamp adds only artifact_lookup_name. A placement that names door_size, door_offset_x, porthole_radius, porthole_offset, arch_size, arch_top_radius or arch_offset_x still WINS: openwork chooses WHICH subtractions exist, never how big they are, so there is no fraction-of-the-export path for it to lose to and no explicit-override flag is needed.",
    "was": "one wall run, one ratio, one label. The declared critical_parameter is 'wall_size against the opening sizes - the ratio of solid to void', and it had shipped exactly one value of that ratio across eleven placements; the three subtractions were literals inline in _build_wall_with_openings and the only way to move any of them was to name a raw Vector3. The @identity then said in so many words what was missing - 'a second wall to make an enclosed room the player can stand inside' - and that sentence had been sitting there unbuilt. apply_grid_config assigned wall_color and had no rebuild behind it, and since the grid delivers config through call_deferred (after _ready), the one knob it exposed could never take effect at all.",
    "note": "openwork asks WHAT A WALL'S HOLES ARE, and at what density mass stops being mass. A ladder in count and kind against a 3.4 x 2.6 m face of 8.84 sq m, read as solid fraction of the front run: door = the rectangular doorway alone, 1.44 sq m void, 84 percent solid - the minimum claim, one difference and it is already architecture. sampler SHIPPED = doorway + porthole + arched window, about 75 percent solid, three unlike opening types arguing that all three are one operator. arcade = the same arch repeated on even piers and nothing else, 5 bays, 3.37 sq m, 62 percent - repetition of a single difference, and the wall becomes rhythm. screen = a 7 x 5 lattice of 0.36 m square subtractions on a 0.486 x 0.52 pitch leaving 0.126 and 0.16 m mullions, 4.54 sq m, 49 percent - past the tipping point, where the wall reads as a filter rather than as mass with holes. It stops there deliberately: thinner mullions read as NO WALL and the verdict would become a fact about the capture. The Label3D follows the rung ('wall - door = room', 'wall - 5 arches = room', 'wall - 35 cuts = room') because on this artifact the label is part of the claim. interior asks WHETHER THE SUBTRACTION HAS PRODUCED AN INSIDE - the thing one wall can demonstrate and never deliver. none SHIPPED, the single 3.4 m run. corner = two runs meeting at a right angle, shelter and a claim on space, still no inside. court = three runs open at the back, enclosure felt before it is closed. room = four runs, the first rung on which the player is INSIDE and the void is bounded, which is the sequence's payoff standing up rather than being asserted. The two axes COMPOSE - every added run carries the active openwork, so interior=room at openwork=screen is a lantern - and one rule governs the composition, the rule that makes 'room' honest: only the FRONT run is entered, so the ground-level doorway appears there and nowhere else. Windows repeat around the room; doors do not. Bay and column counts scale with run length rather than staying fixed, so what wraps the enclosure is the PITCH, and the fabric reads as one fabric. Both words are NEW and both were checked against the corpus before use. openwork is the craft term for exactly this - work defined by its openings - and the plainer names are taken: `opening` is held by agreement_gauge and five siblings for the pose two vectors are found in, `aperture` by breathing_room for how far a door stands open, and one letter between opening and openings across artifacts that share the Trial_* museum maps is the collision exhibit_vitrine's regard->guard rename exists to prevent. `interior` likewise: `plan` is room_shape_demonstrator's (pair|head|circle|panopticon|vacant) and `enclosure` is exhibit_vitrine's (box|bell|cradle|niche|open) for the case around an exhibit - and exhibit_vitrine stands in the same Trial_* maps as this wall, which is precisely where one word cannot mean two things. Nothing in the corpus asks what a wall's holes ARE, or whether an operation has produced an inside; the nearest neighbour, fontana_puncture's breach, asks how deep ONE hole has eaten.",
    "declined": "strike, the family word shared character for character by fontana_puncture, csg_difference_demo and csg_compose_workbench, is NOT promoted here, and the reason is specific to this artifact rather than borrowed from its siblings. The word is technically reachable - this wall does have a sphere subtraction with an offset - and it would have to pick a favourite. Those three each have ONE B to place. This wall's B is three things at once, a box, a sphere and a box-union-cylinder, and moving any one of them across four named positions changes about 2 percent of the face while announcing itself as the artifact's structural axis. That is subtraction_suite's refusal in another key ('the tray has no B to place'); here there are three Bs and no single one of them is the strike. Also declined: an axis directly over the solid-to-void RATIO, which is this artifact's own declared critical_parameter. It is a fraction, a fraction is already legible as a number, and csg_compose_workbench declined an axis over preset_spacing for exactly that reason - openwork moves the same ratio as a CONSEQUENCE of changing what the holes ARE, which is the thing a vocabulary is actually needed for.",
    "fixture": {},
    "framing": 0.62,
    "fixture_why": "NOTHING IS PINNED, and this is the only artifact of the four that needs nothing pinned: no _process, no rotation, no RNG, no deferred build. Every value of both axes is standing CSG resolved synchronously in _ready, so one frame is a complete account. What IS required is framing, and it is derived rather than measured. capture_config_sweep._subtree_aabb counts MeshInstance3D only (MultiMesh as a fallback) and every part of this wall is CSG, so the measured box is the GROUND SLAB ALONE: 5.4 x 0.04 x 4.4, diagonal 6.97, against the wall's own 3.4 x 2.6 x 0.4, diagonal 4.30. 4.30 / 6.97 = 0.617, hence 0.62. Note that this lands on the same number by different arithmetic than a CSG-counting merge would give (5.4 x 2.64 x 4.4, diagonal 7.45, ratio 0.577) - the coincidence is worth recording so the next reader does not 'correct' it back. Do NOT reach for a layers=0 anchor here: the ground is walkable and part of the shipped look. THE KNOWN RISK, to correct from the first sweep: because the AABB is the flat slab, the camera aims at y -0.02 and the wall rises 2.6 m above the aim point, so at 0.62 the wall sits high in frame at roughly an eighth of its area - legible and uncropped by about six degrees of margin, but not centred. If the first sheet shows it small, the fix is a smaller framing (0.50-0.55 still clears the label at y 2.95), not a change to the geometry.",
    "rebuild_guard": "apply_grid_config now stamps config into metadata, re-reads it, and returns early unless a value the build actually reads has changed - and returns immediately if _ready has not built yet. The signature covers all eleven: openwork, interior, wall_color, wall_size, door_size, door_offset_x, porthole_radius, porthole_offset, arch_size, arch_top_radius, arch_offset_x. Nine placements pass no keys and two pass {\"emissive\": false}, which is stamped as metadata nobody reads and leaves the signature identical, so not one of the eleven is torn down. A rebuild frees only the nodes this script added, tracked in _owned, rather than every child - the grid and the curation station both attach things of their own to an artifact root after _ready. The four wall runs are SEPARATE top-level CSGCombiner3D nodes under the Node3D root, never nested: a CSGCombiner3D inside another combiner is a shape, not a container, and nesting them would union the returning runs into the front wall instead of standing them beside it.",
    "still_note": "Everything both axes build is standing CSG resolved during _ready, and this artifact has no _process at all, so a single frame is a complete account of every one of the sixteen combinations. The returning runs are sized FROM the shipped ground slab - depth is (wall_size.z + 4.0) / 2 minus half the wall thickness minus a 0.1 m margin - so all four interior rungs stand wholly on it, the measured AABB is identical at every value, and one framing number serves the whole sweep. spatial_needs is deliberately left alone: corner, court and room want roughly four clear floor cells and the default does not, and the default is what every existing map places.",
    "hazard_noted": "ONE PRE-EXISTING ODDITY, reported and NOT fixed here, because defaults are sacred. porthole_offset.y is 1.4 against a wall half-height of 1.3, so the sphere centre sits 0.1 m ABOVE the wall top and the shipped 'porthole' is really a scallop bitten out of the top edge - a circular segment of about 0.098 sq m, not a 0.32 m disc through the wall. This is why the sampler rung reads about 75 percent solid rather than the 74 a full disc would give. A SECOND, also untouched: the 7 plinth mounts seat a 5.4 m ground slab on a size:m plinth."
   },
   "name": "CSG Architecture Cavity",
   "lookup_name": "csg_architecture_cavity",
   "description": "A solid wall with three openings cut through it. Architecture as Boolean composition.",
   "scene": "res://algorithms/boolean_surfaces/csg_architecture_cavity.tscn",
   "category": "boolean_surfaces",
   "sequence": "boolean_surfaces",
   "artifact_type": "boolean_surfaces",
   "qfep_term": "F",
   "capacity": "WALL minus openings",
   "dev_themes": [
    "boolean_surfaces",
    "sieve_2026_05_13"
   ],
   "dev_category": "boolean_surfaces",
   "complexity": "intermediate",
   "tags": [
    "csg",
    "architecture",
    "boolean",
    "wall"
   ],
   "include_in_map_data": true,
   "map_ready": true,
   "map_sequences": [
    "boolean_surfaces"
   ],
   "parameters": {
    "footprint": [
     1,
     1,
     1
    ],
    "size_group": "environment"
   },
   "qfep_connection": "A wall with three openings cut clean through it — architecture reduced to its Boolean truth. The turn's demonstration that building is subtraction: a doorway is a difference, a room is a void bounded by solid, and the mass of a wall exists to be selectively removed. Standing before it you read architecture the way this chapter teaches — not as material placed, but as space composed, the cavity the real subject and the wall merely its frame."
  },
  "coincident_face": {
   "dna": {
    "promoted": "2026-08-11",
    "stage": "2 - variation",
    "framing": 0.55,
    "axes": {
     "separation": [
      "coincident",
      "clear",
      "hairline",
      "inverted"
     ]
    },
    "default": "separation=coincident is a SHORT-CIRCUIT, not a recomputation. _faces() returns the face_x export UNTOUCHED at that value, and face_x's own default is the shipped pair (0.02, -0.02), so A's right face and B's left face are the literals that shipped rather than numbers a formula lands back on. Everything downstream follows from them unchanged: the band still runs b_min.x to a_max.x at 26 slivers of sw*0.9 alternating amber then blue, _b_depth() returns 0.0 so every z is the old z plus zero, and the verdict tag and the plaque body are the fallthrough arms of their match blocks, character for character. inverted takes that same fallthrough on purpose - it keeps the shipped bounds exactly and settles the contest in z instead. ZERO PLACEMENTS, and that is the honest headline: this token appears in no map_data.json anywhere in the corpus - not bare, not through exhibit_furniture, not through curation_station. Every reference to it is a reader, not a placer: tools/build_seams_gallery.py (the boolean_surfaces row of the seam table), commons/data/artifact_relations.json (three times, each 'same category and sequence' with placements_together 0), commons/artifacts/dressing_rooms/coincident_face.json (rotations, approach, footprint, clearance, footing, artifact_offset - not one key this build reads), doc/auto_research_inventory and doc/book/text_mentions. So there is nothing to preserve except the seams gallery's rendering, and the short-circuit exists for that and for honesty rather than for a placement count. A placement that names face_x directly still wins over separation.",
    "was": "no declared axis, and one asserted state. The artifact's headline is that the exact boundary is a coin toss per pixel, and it had only ever drawn the coin toss - no reading of the same diagram with the boundary uncontested, so nothing in the picture could say whether the flicker is a property of Booleans or of one particular pair of numbers. Only color_a and color_b were exports. The rect bounds, the band bounds and the 26-sliver count were literals inside _ready, so there was nothing for an axis to resolve against and the promotion had to LIFT BEFORE IT COULD DERIVE - the same first move csg_compose_workbench made when its operand pair turned out to be three literals inside _build_presets. And apply_grid_config stamped set_meta('config_<k>') for every key it was handed and had NO READER ANYWHERE: the metadata was written and never consulted. That is one step worse than the sibling's knob-without-a-rebuild - it was a knob without a wire - so this promotion needed _read_metadata_overrides, the signature compare and the early return, all three.",
    "note": "separation is THE GAP BETWEEN THE TWO FACES, IN UNITS OF THE FLOAT GRID, and what it argues is THAT EXACTNESS HAS A GRAIN SIZE AND THAT CROSSING IT IS TOTAL. clear: a_max.x -0.04 against b_min.x +0.04, a visible 0.08 gap, no shared face, no contest - the exact boundary CSG actually promises, and the control this diagram has never had. hairline: an 0.008 gap, the two solids all but touching and the depth order still decided cleanly, a thin dark seam and no band - the rung that turns the claim into a claim about SIZE. coincident: SHIPPED, a_max.x 0.02 and b_min.x -0.02 with the 0.04 band of 26 alternating slivers standing in the overlap. inverted: the shipped bounds with B advanced 0.004 toward the viewer, so the band goes solid blue - one step of the grid, and the answer does not degrade, it flips entirely. The ladder is deliberately NOT monotone: the failure sits in the middle of it, with order on both sides. ON THE DECLARED ORDER, since the two disagree and the disagreement is not a mistake: the @export_enum is written clear, hairline, coincident, inverted, which is the ladder as argued; the declaration above puts coincident first because apply_dna_block.py moves the export default to the leftmost tile so a sweep reads the shipped lineage first. Same four values, and the code is the thing that renders. The verdict line under the diagram and the plaque body follow the rung, because this artifact's text is half its ink and a still that kept 'z-fight: the shared face flickers' over a clean cut would be lying. THE WORD IS NEW. Nothing in the corpus asks what distance an exact answer needs in order to stay exact. The two nearest words are taken and mean something else: seam on escher_staircase and florensky_sphere (none|hairline|gap|scaffold|field) names the joint of an impossible object, and grain on six artifacts names how a solid is divided. This is the boolean_surfaces station of the bias landscape and its charge is 'bias is conserved; show the seam' - separation shows the bias is not in the flicker, it is in the threshold that decides whether there is one.",
    "declined": "tiebreak (stripes | blotch | split | toss) - HOW the contest is decided, pixel by pixel - is this artifact's own headline turned into an axis, is deterministic once seeded, and its four rungs are four real policies: the shipped alternation; the seeded blotching a real coincident face actually shows; a clean rule awarding left to A and right to B, which abolishes the flicker and relocates the arbitrariness into the rule, the seam family's whole thesis; and the literal per-sliver coin toss. It is REFUSED ON ARITHMETIC. Every rung changes only the band - 0.04 x 0.40 m of an 0.86 x 0.66 m plate, 2.8 per cent of the subject face and under 2 per cent of frame even at framing 0.55 - so the critic's hottest-5-per-cent window would be more than half unchanged pixels and the verdict would be a fact about the band's width. It becomes shippable the moment the plate is redrawn at a wider band, and redrawing the plate changes the shipped picture: a decision to take on its own, not to smuggle in under a promotion. Also declined: evidence (result|trace|longhand|axiom, 45 artifacts). There is no derivation here to show more or less of - this artifact does not compute an answer, it exhibits a place where the answer does not exist.",
    "fixture_why": "NO FIXTURE, and the absence is checkable rather than claimed: there is not one call to randf, randi or randomize in the file, no _process, no _physics_process and nothing that reads time. Everything the axis and the default build is static emissive geometry resolved synchronously in _ready, so one frame is the whole account and a capture's settle cannot photograph this artifact half-way through anything. The seeded rungs that would have needed a seed export live in the declined tiebreak axis, not in what is shipped. _ready is ungated and builds standalone, so no dna.fixture is needed to make it render either. Label3D text is non-billboard throughout, so no plates are added and the capture AABB is the mesh extent. What IS required, and is not a pin, is dna.framing - see framing_note.",
    "framing_note": "ARITHMETIC, NOT TASTE, and it is the documented wide-flat-subject trap in its purest form. The MeshInstance3D AABB is 0.86 x 0.798 x 0.031: x from the backing slab, y from the backing's bottom edge at -0.31 up to the plaque's accent strip at 0.488, z only the three centimetres of stacked slabs. Diagonal 1.174, radius 0.587, and that diagonal is set almost entirely by a subject which is effectively 2D. capture_config_sweep fits by the diagonal (radius / tan(FOV/2) * PAD * framing, FOV 34, PAD 1.9, RES 760), which at framing 1.0 puts the camera 3.65 m out where the visible frame is 2.23 m across; at the sweep's yaw 0.62 and pitch -0.26 the plate face foreshortens to 0.787 of its area and projects to about 11 per cent of frame. 0.55 brings the camera to 2.01 m and the frame to 1.23 m, and the plate face to about 36 per cent of frame - clear of the ~6 per cent dilution floor, with the whole 0.86 x 0.80 body still inside the frame. 0.45 was considered and refused: it would leave under 0.2 m of margin on a 0.80 m subject whose plaque sits at the very top of its own AABB. The AABB does not move between rungs (both rectangles stay inside the backing at every value, and inverted's z step is 4 mm), so all four variants are framed identically and no part of the measured difference is a camera moving.",
    "rebuild_guard": "apply_grid_config still stamps every config key into metadata exactly as before, then re-reads the four keys it understands (separation, face_x, color_a, color_b), and returns early unless a value the build actually reads has changed - and returns immediately if _ready has not built yet. The old body had no rebuild AND no reader, so this is the first version in which that metadata means anything. The teardown frees get_children() and nothing else, which is safe here because every child of this node is made by this script: the .tscn is a bare Node3D root carrying the script, with no children of its own. Zero placements pass any key, so the guard means none of them is even torn down; and because the sweep sets @export properties directly rather than calling apply_grid_config, the sweep path never enters the guard at all. ONE HONEST CONSEQUENCE of wiring the metadata up: color_a and color_b were always exports and were always stamped, and were always ignored - a placement passing either of them would now get the colour it asked for where before it got the default. That is a behaviour change, and it is safe here only because the placement count is zero and the dressing room passes rotations, approach, exit, footprint, clearance, extras, posture, footing and artifact_offset, not one of which this build reads.",
    "still_note": "BITE PREDICTION, stated up front so a thin number is read correctly rather than filed as inert. Between rungs both rectangles move, all four outlines redraw, the band appears, vanishes or goes solid, and two text elements change (the verdict line under the diagram, and two of the plaque's three body lines). The pairs that set clear or hairline against coincident should bite hard: the band is 0.04 x 0.40 m of an 0.86 x 0.66 m face going from bright emissive to bare dark backing, and two vertical outlines move 0.06 m. THE PAIR AT RISK IS coincident AGAINST inverted, and it is named here rather than discovered in the verdict. That pair changes only the band's colour and the text, and the two colours are near-isoluminant: amber (1.0, 0.62, 0.18) has a Rec.709 luma of 0.669 and blue (0.3, 0.7, 1.0) of 0.637, so an alternating band and a solid blue band differ by roughly four grey levels in the luminance the verdict is measured in. Per channel the same change is enormous - red goes from a blended ~166 to 76 across the band - which is exactly the case focus_rgb exists to veto, so if that pair falls under TWIN_FOCUS it should surface in the critic's COLOUR-ONLY list and not as a twin. Read it there before concluding the rung is decoration. ONE THING THAT WILL NOT PHOTOGRAPH AS DRAWN, said so it is not mistaken for a fault: at RES 760 and framing 0.55 the frame is 1.6 mm per pixel and a sliver is 1.4 mm wide before foreshortening, so the 26 slivers are sub-pixel and the band renders as a blended amber-blue stripe rather than 26 countable bars. That is what z-fighting looks like, but nobody should expect to count slivers in the tile. If the axis comes back faint, crop-recheck before redesigning: this is random_walk_collection's pathology, a large change over a small area, not an inert axis."
   },
   "category": "boolean_surfaces",
   "complexity": "intermediate",
   "description": "The boolean-surfaces seam. CSG promises exact cuts; subtract one solid from another and the boundary is a clean surface. But when the cut plane lands on a coincident face, the two surfaces claim the same depth and the float grid cannot say which is in front - so the renderer flickers, awarding each pixel to whichever number rounded higher this frame. The clean boolean is exact everywhere except the one place you asked it to be.",
   "dev_themes": [
    "digital_bias",
    "seam"
   ],
   "include_in_map_data": true,
   "interaction": "ambient",
   "lookup_name": "coincident_face",
   "map_ready": true,
   "map_sequences": [
    "boolean_surfaces"
   ],
   "name": "Coincident Face",
   "qfep_connection": "A station in the bias landscape (doc/SEAM_PER_SEQUENCE.md); the digital-bias seam for boolean_surfaces. Bias is conserved; show the seam.",
   "scene": "res://algorithms/booleansurfaces/coincident_face/coincident_face.tscn",
   "sequence": "boolean_surfaces",
   "tags": [
    "boolean",
    "csg",
    "z_fighting",
    "coincident",
    "float_precision",
    "seam",
    "digital_bias"
   ]
  }
 }
}
