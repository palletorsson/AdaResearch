# Pipe Layouts

JSON definitions for pipe network topologies.

## Files

- `drain_network.json`: drain pipe configuration
- `example_network.json`: example/demo pipe layout

## Usage

Loaded by `pipe_layout.gd` to define pipe segment positions and connections.
