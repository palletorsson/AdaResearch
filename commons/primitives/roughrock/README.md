# Rough Rock

Irregular polyhedron with rough, noisy surface variation.

## Files

- `roughrock.gd`: rock with deterministic vertex roughness
- `roughrock.tscn`: scene wrapper

## Behavior

- Applies procedural roughness to base vertex positions.
- Deterministic variation (same seed produces same shape).
