# Prismes

Rotating prism animation system.

## Files

- `rotation.gd`: configurable rotation animation applied to parent node
- `rotating_prisme.tscn`: rotating prism scene
- `prism_block.tscn`: static prism block

## Behavior

- Configurable rotation speed.
- Rotation applied continuously in `_process()`.
