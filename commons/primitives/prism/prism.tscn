[gd_scene load_steps=2 format=3 uid="uid://7cmeoec6zza1u"]

[ext_resource type="Script" path="res://commons/primitives/prism/prism.gd" id="1_script"]

; prism.tscn — configurable triangular-prism primitive backed by prism.gd.
; Replaces a previously-hardcoded scene (PrismMesh sub-resource + SimpleGrid
; shader sub-resource) with a script-driven version that exposes
; left_to_right / size / colour exports + apply_grid_config.
; UID preserved so existing registry / map / shapes_test.gd references resolve.
; NOT the same shape as prismblock — see prism.gd @identity for the distinction.

[node name="prism" type="Node3D"]
script = ExtResource("1_script")
