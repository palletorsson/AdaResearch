# Position Label

Displays world position as 3D text that updates every frame.

## Files

- `positionlabel.gd`: real-time position display
- `positionlabel.tscn`: scene wrapper

## Behavior

- Shows current world coordinates as Label3D text.
- Updates position readout each frame in `_process()`.
