[gd_scene load_steps=2 format=3]

;; LegDemo — shows one-leg, two-leg critter, and scaled beast_demo side by side.
;; Run with F5/F6 to compare IK creatures.

[ext_resource type="Script" path="res://commons/hazards/octapod_crawler/leg_demo.gd" id="1_script"]

[node name="LegDemo" type="Node3D"]
script = ExtResource("1_script")
