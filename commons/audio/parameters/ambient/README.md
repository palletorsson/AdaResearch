# Ambient Parameters

JSON synthesis parameters for ambient sound generation.

## Files

- `dark_808_sub_bass.json` — Deep 808-style sub bass drone
- `melodic_drone.json` — Melodic ambient drone

## Usage

Loaded by `AudioSynthesizer` or `EnhancedParameterLoader` to configure ambient sound generation. Referenced by ambient presets in `../../presets/`.
