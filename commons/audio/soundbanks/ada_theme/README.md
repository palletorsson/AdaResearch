# Ada Theme Soundbank

Procedural sound scripts for the Ada Research project theme music.

## Sounds

`kick.gd`, `snare.gd`, `hihat.gd`, `bass.gd`, `pad.gd`, `sequence.gd`

## Usage

Loaded by `SoundbankLoader.load_genre("ada_theme")`. Configuration in `brief.json`.
