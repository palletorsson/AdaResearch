# Rave Soundbank

Procedural sound scripts for rave music — hoover bass, stabs, hard kicks, breakbeat elements.

## Sounds

`kick.gd`, `snare.gd`, `hihat.gd`, `pad.gd`, `stab.gd`, `hoover.gd`

## Usage

Loaded by `SoundbankLoader.load_genre("rave")`. Configuration in `brief.json`.
