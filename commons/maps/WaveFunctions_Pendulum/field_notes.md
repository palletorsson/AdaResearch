## 2026-09-10 — batch W1, hall 1 (doc/research/waves-chance-noise/NEXT_BATCH.md)

Ruling followed: the map is the placement authority; the recovered structure (museum.wall_height 3, gate_depth_rows 0, interior 2 = platform, perimeter w, from doc/space/wave-platform-recovery-2026-09-10) is kept cell for cell. No structure or utility cell moved. `museum.artifact_placement: "map"` added, Astra's opt-in from the pilot review; the bench stamp under `wavefunctions|pendulum` is keyed to an old pearl name (the live pearl is `wavefunctions pendulum`) and claims nothing either way.

Placement: `PendulumWave:90` at (1,7) stood against the west wall with its record receding INTO that wall after half a metre (the pre-museum map, c47be1717, had it at x=0 with nothing west of it; the museum's `w` ring took that space). Moved to (6,11), rotation 180, so the record recedes south down the open half toward the exit, six metres clear. GlassRack (7,15) moved out of the record's lane to (10,12); seismograph (5,11) moved to (2,13), the west walkway beside the trail, as the room's second recorder. Nothing removed.

Artifact (PendulumWave.gd, full rewrite; before-copy in doc/space/waves-chance-noise-w1-2026-09-10/before/): motion moved from `_process` to `_physics_process` (fixed step; the shipped frame-based integration and frame-based sampling would have given a 6.7 m trail at 90 fps and 10 m at 60); the sampler is fixed-time with two limits, `max_trail_length` 300 (kept) and `max_history_seconds` 10 (new); depth = age × `time_speed` (0.6 m/s, was 2.0); the marks are a MultiMesh with `visible_instance_count`; the period is measured from successive upward centre crossings; policies FINE 25 ms / COARSE 200 ms / STROBE = measured period; RESET resets the experiment only. Housing: gallows frame (posts at ±1.7 m, pivot 2.9 m, bob at 0.9 m), RackTemplates panel on the −x post (`em_local_instrument` meta so its button areas are not the footprint), cased readout on a stem at (−1.7, 1.45, −3.0). The scene's camera/light/environment/floor/label are freed when embedded. `apply_grid_config` reads `sampler` (word-valued; no numeric keys, so the grid's shorthand rule cannot misread it).

Design decisions: (1) two limits rather than one, so FINE and COARSE run into DIFFERENT rules and the readout shows which — a finer record keeps less time under the same memory. (2) the strobe as the third policy: the invariant the visitor can check is period / interval = marks per swing, and at interval = period the record of a moving bob stands still. (3) marks, not only a line, so the count on the plate is a thing one can count. (4) the record recedes toward the exit so the visitor walks its length and meets the eviction at the far end.

Rejected: a grab on this bob (the Intro's pendulum has one; this room is about the record, and a hand would change the motion mid-comparison); a distance-gated sampler like Point_Trace's (the contrast with it is the point); a fade on old marks (the brief asks for the mark to disappear, not to dim).

Exactness: the final quotes the append/evict block (9 lines) and the depth line, verbatim (checked by stripping indentation against the script). "Three hundred marks at 25 ms are 7.5 s" and "fifty marks at 200 ms are 10 s" are arithmetic on the exported values. The period at 45° is stated as measured, not computed; the probe asserts 2.7–3.2 s.

Evidence: gdparse passes (script, probe); pathfinder OK 209/273 (unchanged); check_map_tokens OK; check_wave_platform_recovery persistent contract passes (12 maps, 193 platforms, 137 walls). Runtime: `commons/testing/probe_wcn_pendulum.gd` written (real hand file, counts/spans under all three policies, direction of recession, readout text, walkway casts, capture) and NOT RUN — Godot PID 2780 held the user:// lock. Headset: not walked.

Supporting texts: intent, blurb, summary, tutorial rewritten (they described a row of graduated pendulums, a seconds-pendulum room and sliders that do not exist); technical.md's invented `simple_pendulum.gd` and `pendulum_wave.gd` replaced with the real script; critical.md kept and given a closing section on the two limits and the strobe. Book line for PendulumWave written; hero moved from WavePaintings to PendulumWave to match the primary.


## Astra source review — 10 September 2026

The display now distinguishes the target interval from actual timestamp gaps on the physics clock. Fine sampling alternates about 16.7/33.3 ms at 60 Hz. The strobe nearly aligns; it is not perfectly motionless. The probe now counts centre crossings in artifact-local coordinates. Both technical and critical texts are reconciled with the configured primary; book lines and citations follow the correction. These notes supersede conflicting exactness claims above. Original texts are preserved in `doc/space/wcn-w1-review-2026-09-10/before/`. Four scripts parse and 26 code excerpts match source. No Godot run or headset walk: PID 2780 remained active. Full review: `doc/book/handoffs/astra-wcn-w1-review-2026-09-10.md`.


## 2026-09-10 — first museum runs (Fable, after Astra's W1 review)

Runs: `probe_wcn_pendulum.gd`, rendered (gl_compatibility, window parked at 4000,4000) under the watchdog, four times; a final headless run with `--log-file` for the engine's own errors (none beyond a harmless viewport-texture warning). Godot 4.6 stable, physics 60 Hz. PID 2780 (a hung headless probe from 2026-09-09 noon, `probe_zero_controllers.gd`, 5 MB, no window; not a session) holds no lock and was left running.

Observed and fixed:
- The museum reported the hall SEVERED by two older bodies. `foucault_pendulum` at (8,1) is a 7 × 7 m body in the museum; its collider seal took x 5..11 by rows −2..4, the door row included. `draw_dot_time_domain`'s 2 m body at (5,4) sealed (3..4, 2..4), the wedge onto the stage among them. Moved: `foucault_pendulum:0:0:0.5` to (10,3), `draw_dot_time_domain` to (4,17) beside the end of the record. The museum now reports nothing severed; its walk grid shows the west passage (x 1..2) and the wedge open.
- The museum deals sculpture plinths onto free walk cells. A per-map key, `museum.sculpture_clear_rects` (read for map-authored halls since today; the museum edit sits beside the `artifact_placement` opt-in), keeps them off [[3,10,9,19]], the record's lane and the walkway beside it. The lane is clear of every collider (checked, not only the hall's).
- The probe's own faults: it subtracted the vestibule from a map cell (the records keep map cells), and the lane check counted only the hall's collision.

Observed and passing (50 checks, 0 failures): built at (6,11) rotation 180; frame, readout at 1.45 m three metres down the record on the east side, panel buttons at 1.19 m; FINE 119 marks at 3 s, 300 marks / 7.48 s / 4.49 m at 9 s, actual gaps 16.7–33.3 ms; COARSE 50 marks / 9.88 s / 5.93 m; period 2.950 s; marks per swing ≈ period / target; STROBE 3 marks, x spread 2.6 cm; RESET zeroes the experiment clock and list and keeps the sampler; three capsule walks 1.0; the readout text exactly as designed. Captures: `probe_pendulum.png` (from the north-east; the GlassRack at (10,12) stands in that viewpoint's foreground) and `probe_pendulum_readout.png` (legible at 1.35 m).

Not observed: a tracked hand at the panel; the seismograph's facing; the Foucault rig at half scale, in a headset.


## 2026-09-10, evening — second runtime pass, under project startup (Fable, after Astra's runtime review)

Runs: `bash tools/run_wcn_probe.sh pendulum live` — the probe ported to a Node (`probe_wcn_pendulum_live.gd`, generated by `tools/port_wcn_probes_live.py`) and run as the project's main scene through `commons/testing/probe_live.tscn`, autoloads present. The run of 20:37 completed: 56 checks, 0 failures, no script errors — every earlier measurement again (FINE / COARSE / STROBE / RESET, period 2.950 s, the three capsule walks, nothing severed) and two new lanes:

- **Actual desktop input**, recorded apart from the emitted signals: the project's desktop rig stood on the east walkway at segment-local (8.3, 0, 10.3), looked at COARSE and left-clicked through the input pipeline; its pointer reported `PendulumWave/Panel/Btn_1/InteractableAreaButton` under the crosshair and the sampler switched to `coarse`. It then walked the walkway south on `ui_up` from (9.5, 0.05, 10.5): 5.0 m in 60 physics frames, no obstruction (the walkway is clear floor to row 19; the south door is at x 5–7).
- **Museum streaming**, through the museum's own streamer rather than the bare root: `em._stream_free(seg_i, "south")` freed the hall and the pendulum with it (`freed: true`); the streamer's record carried a cursor snapshot (`rebuildable: true`); `_stream_rebuild` built the hall again with a pendulum recording anew — 63 marks when checked — with its panel, case and readout.

The pictures of that run were not the rig's: the museum walker's one-second camera guard (`endless_museum.gd`, `cam_guard`) had reclaimed the view, so `probe_pendulum_desktop_walk_live.png` and `probe_pendulum_readout_live.png` show the walker's view from the doorway; the primary `probe_pendulum_live.png` had won by calling `make_current` thirty frames running. The driver now stops that guard and each capture records which camera drew it (`captures.<name>.is_ours`, `desktop_input.walk_capture_pose`). A rerun at 20:53 got through the walk check and stalled at the capture: at 20:54:36 the Godot editor was opened on the project (PID 40320), and the runner refuses to start beside a live instance since. That launch had deleted the 20:37 JSON; the runner's printed summary of it is kept as `probe_pendulum_live_2037_runner_output.txt`. The readout plate and its facing stand as inspected at 17:32 (`probe_pendulum_readout.png`, read from the walkway) with the same numbers in the 20:37 run (readout 1.45 m, panel 1.185 m).

**Pending, when Godot is free:** `bash tools/run_wcn_probe.sh pendulum live` — writes the rig's own walk view, the readout capture with `is_ours`, and `desktop_input.south_walkway_meshes`. Headset: pending.


## 2026-09-11, 07:36 — the missing view, completed (Fable, Astra's W2 completion brief, item 1)

`bash tools/run_wcn_probe.sh pendulum live` on the current probe sources, Godot free (no editor, no play instance): 56 checks, 0 failures, no script errors (`probe_pendulum_live.json`, `_engine.log`). This time every capture records which camera drew it: `captures.primary.is_ours` and `captures.readout.is_ours` are true, and the walk view's pose is the rig's own camera at (9.5, 1.6, 15.5) looking south (`desktop_input.walk_capture_pose`). Inspected: `probe_pendulum_desktop_walk_live.png` is the east walkway from the rig after its 5.0 m walk — the walkway running on to the south wall, the wall card on it, the south door's opening to the right and the map's own void cell at (7,18) beside the way to the door; `probe_pendulum_readout_live.png` is the cased plate from the walkway, three metres down the record, reading `t 11.8 s · 300 marks kept 7.5 s / target 25 ms · 0.6 m per s / actual gaps 16.7–33.3 ms / period 2.95 s measured · step 16.7 ms`. The readout (1.45 m) and the panel (1.185 m) measure as before.

The "block in front of the door" seen in the first live chain's picture was the museum walker's doorway view, not the walkway: `desktop_input.south_walkway_meshes` lists only `ShowingCard14` (a wall card on the south wall at z 19.99) and the segment's `DetailExtentAnchor` in the box x 8.5–10.5, rows 15–20. Nothing stands on the walkway. Desktop input as before: COARSE through the rig's pointer (`Panel/Btn_1/InteractableAreaButton`, sampler `coarse`), the walk 5.0 m in 60 physics frames; museum streaming freed and rebuilt the hall with a pendulum recording again (63 marks). Headset: pending.

**2026-09-12 — the visual pass (Astra: "the primary view is obstructed by the blank back of a foreground panel… investigate the pictured floor hole").** The obstruction was the capture, not a panel: the primary camera stood at (9.8, 9.6), inside the lab table's footprint, and photographed its back; the frame is now taken from the east wall (11.5, 12.2) with an 80° lens, and a second frame shows the controls from the walkway. The sampler panel is turned to face the east walkway (world +x) on the outer side of its post, so a standing visitor sees the bob, the near record and the buttons together; the marks and the line are deep teal (they were pale cyan over a white floor) on a dark matte runner (1.9 × 4.8 m) under the record. The two holes were the map's `0` cells at (2,8), the west aisle's tc cell, and (7,18), the teleporter's; both floored in the museum by `museum.floor_cells`. The lab table stays at (9,9): moved to (2,10) it sealed the west aisle with the seismograph. The walkway rect widened to x 11 (`[3,10,12,19]`): a dealt plinth had landed on the walkway once the deal changed, and a map edit made while a run was in progress rebuilt the hall under the probe (exit 124). Live: 61 checks / 0 failures.
