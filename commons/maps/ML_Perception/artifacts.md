# ML Perception — Artifacts
*Machine Learning: Algorithms That Learn · integration · 2 artifacts*

> A pixel is a number. A grid of pixels is a matrix. Vision starts there — raw arithmetic on light. Convolution slides a small window across the image, multiplying and summing, extracting edges, textures, shapes. Each layer sees more abstraction. Edges become contours. Contours become faces. The network learns its own features — no one tells it what an eye is.

The map, read through what it holds — its artifacts in the order you meet them:

## ComputerVision_VR
![ComputerVision_VR](/scene-catalog/computer_vision_vr.png)

Interactive image processing with spatial convolution filters

`computer_vision_vr`

## ConvolutionalNeuralNetworksCNNs_VR
![ConvolutionalNeuralNetworksCNNs_VR](/scene-catalog/convolutional_neural_networks_cnns_vr.png)

Walkable CNN architecture with interactive kernels

`convolutional_neural_networks_cnns_vr`
