# Eye shot — ML_Synthesis

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **0 overlaps, 0 tight** — the law wants ≥1.2m to walk between.

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 1/1  mean Δ=+0.076
      walkability improved: 0/1  mean Δ=-0.045

no sibling kept — the move did not beat the ride (overlaps 0→0, tight 0→0). Note-only.

## The voice (qfep)
2 of 3 cast members carry a theory-claim; 1 mute.
- **anomaly_detection** — Anomalies are high-F surprises — data points that the F-model cannot predict. Detection algorithms learn the b
- **joint_learn_walk** — Reinforcement learning as embodied F-minimization — the agent builds an internal model through interaction, re
- mute: explainable_ai_xai_vr

## The text vs the space
**no walked.md** — the space stands unwritten; this note is the first text this map has.

## The heuristic understanding
The space already walks: the bodies keep the law without being told. What carries this map is its voice, not its floor.
