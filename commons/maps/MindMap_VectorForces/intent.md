Concept: The vectors-and-forces concept map made walkable. `tools/mindmap_to_map.py` projects the 2D mind-map of 25 vector/force concepts (Coordinate system through General force / pad) onto a floor — diagram layout becomes level layout, diagram order becomes the spawn-to-teleporter walk. The arc moves from the descriptive vector grammar (how we name and combine arrows) into applied dynamics (what those arrows do when they become forces in the world).
Sequence role: A meta-instrument beside the Vectors & Forces material, not a curriculum step. Undeclared by design — the projector's output, the comprehensive concept map (all examples, grouped by the problem each solves) compressed into one walkable view. Spawn at `Coordinate system` (the measuring frame), teleporter at `General force / pad` (the abstraction that subsumes the specific forces). One representative artifact per concept.
Technical angle: Generated map_data.json (3 layers) from the concept map via mindmap_to_map.py with weighting w=0.00 (pure concept-graph layout). 869 artifacts — the largest of the three projected maps — all reachable, single teleporter, pathfinder-clean. The placed-channel loop (placed_channel.py) measures the built level back against the source diagram, scoring how faithfully the walk reproduces the concept arrangement.
Critical angle: A physics syllabus is usually a sequence — chapter follows chapter. Laying it out as a neighbourhood reveals the actual structure: the vector operations form a tight descriptive core, and the forces radiate from it as applications. Walking from `Coordinate system` to `General force / pad` enacts the curriculum's deepest move — that "describing space" and "explaining motion" are the same machinery seen from two distances. The duplicates (several artifacts per concept) are kept on purpose: the map shows abundance, not a single canonical answer.
Key artifacts: One representative artifact per concept node — `Coordinate system` anchors the spawn; the vector-grammar cluster (Addition, Dot product, Cross product / torque) forms the descriptive core; the applied forces (Gravity / orbit, Spring, Pendulum, Momentum, Lever) radiate outward; `General force / pad` holds the teleporter and the unifying abstraction.

Gap: Generated meta-map — one heuristic-picked artifact per concept, so representative quality depends on the concept-map scorer. No subtitle truth-beats embedded; the walk teaches by adjacency and order alone.
