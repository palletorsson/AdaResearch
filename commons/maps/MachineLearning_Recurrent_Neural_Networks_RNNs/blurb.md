# Recurrent Neural Networks RNNs

Dice roll as architecture. This is a laboratory of stochastic processes — random walks that carve terrain, Gaussian distributions that sculpt peaks, height fields that refuse to repeat. Every algorithm here takes chance as input and produces structure as output.

The grid is 8×8. Cubes rise by coin flip. Walkers stumble through cells leaving trails of raised geometry. Profiles generate from noise — no two landscapes identical. Remove functions dissolve structure back into possibility. The arena cycles between construction and erasure, each pass producing forms that no designer intended.

Randomness is not absence of pattern. It is pattern without author. The same distribution, run twice, gives different cities. What persists is not the shape but the rule that shapes. Order emerges from disorder — not despite it, but through it. The algorithm doesn't decide. It allows.