# Eye shot — GT_Flow

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **0 overlaps, 1 tight** — the law wants ≥1.2m to walk between.
- `[tight  ] karger_algorithm:0:0:0.6   <-> push_relabel_algorithm:0:0:0.6 gap +0.41m (centers 1.41m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 0/1  mean Δ=+0.000
      walkability improved: 1/1  mean Δ=+0.067

sibling **Trial_eye_GT_Flow** kept: overlaps 0→0, tight 1→0, pathfinder OK.

## The voice (qfep)
2 of 2 cast members carry a theory-claim; 0 mute.
- **karger_algorithm** — initial_vertex_count (n) — P(success per run) = 2/(n*(n-1)); for n=8 that is ~3.6% per run, but parallel repet
- **push_relabel_algorithm** — At the F to E boundary: the network's fixed topology constrains how excess can discharge. Height labels turn s

## The text vs the space
walked.md exists — the writing names 2/2 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: karger_algorithm, push_relabel_algorithm sit in clearance violations — the text promises what the floor obstructs.

## The heuristic understanding
The space already walks: the bodies keep the law without being told. What carries this map is its voice, not its floor.
