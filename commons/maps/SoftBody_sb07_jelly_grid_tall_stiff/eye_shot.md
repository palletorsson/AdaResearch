# Eye shot — SoftBody_sb07_jelly_grid_tall_stiff

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **0 overlaps, 0 tight** — the law wants ≥1.2m to walk between.

## The move
    placing 1 map(s)  ·  IN-PLACE mode
      SKIP Trial_eye_SoftBody_sb07_jelly_grid_tall_stiff: no placeable artifacts found in Trial_eye_SoftBody_sb07_jelly_grid_tall_stiff
    logged 0 entries to ada_run\placement_log.json

no sibling kept — the move did not beat the ride (overlaps 0→0, tight 0→0). Note-only.

## The voice (qfep)
0 of 1 cast members carry a theory-claim; 1 mute.
- mute: gallery_marker_soft-body

## The text vs the space
**no walked.md** — the space stands unwritten; this note is the first text this map has.

## The heuristic understanding
The space already walks: the bodies keep the law without being told. What carries this map is its voice, not its floor.
