Eight cells wide. One bit to start. Two rules unfold across the grid — same mechanism, radically different worlds.

Rule 30 takes a single black cell and detonates it into apparent randomness. Deterministic input, chaotic output. No compression, no shortcut — the only way to know row 500 is to compute every row before it. Wolfram used it to generate random numbers. A machine that produces what it cannot predict about itself.

Rule 110 runs the same logic at a different address. Same one-dimensional tape, same binary neighborhood lookup. But here, structure survives. Gliders emerge. Collisions compute. Turing-complete — meaning anything computable lives inside this single-digit rule, given enough tape and time.

One rule dissolves pattern into noise. The other holds the exact threshold where pattern becomes computation. The difference is three bits in an eight-bit table.