# Random_Entropy — field notes

## 2026-09-11 — R1: the ledger under the gauge (Fable, on Palle's word to continue with the next hall on the route after the Synthesis Lab, in the same way as W2–W4)

**The ruling.** The card asks for three views of one sample — the order drawn, the sorted order and the histogram — under one entropy reading; a contrasting concentrated distribution with the same alphabet and the same N; the meter's disclosure modes reused to reveal counts and the calculation after the observation; and a final that reads the strip and the bars before the number, sorts the same sample and watches what refuses to change, reads the real loop with its omitted zero terms and its ceiling, and lets the unchanged number expose what it did not measure. The gauge computed the right thing already — a seeded local stream of 200 draws over ten symbols, counted, and H = −Σ p log₂ p over the counts — and it offered nothing to do: a wall panel 0.7 × 0.5 m built at its origin in `_ready`, which the museum lane stands half in the floor (no auto-grounding of `_ready`-built bodies), showing forty of its two hundred draws on a strip and no way to sort, contrast or step its own disclosure; the present final's sort is imagined ("Imagine gathering all occurrences…").

**What changed, and why.**

1. *An opt-in `stand` axis on `shannon_entropy_meter` (`ledger`).* The shipped `none` is byte-for-byte the previous behaviour in what it builds and shows; the artifact's other placements are untouched. Under `ledger` every script-built child is lifted 1.40 m onto a post, and a 2.30 × 0.45 m desk at 0.92 m with a collider stands in front of the panel.
2. *The ribbon.* All 200 draws as tiles (0.8 × 3 × 2 cm, a MultiMesh) in a 2 m row on the desk, in the order drawn, coloured as the histogram colours their symbols; a pale mark under the first forty says which the panel's strip shows.
3. *SORT.* A stable sort of a COPY: each tile's place in the sorted order is computed (symbol by symbol, draw order within a symbol) and the tiles glide there over 1.2 s (a small hop on the way, so the motion reads). The sample array, the counts, the histogram bars and H are not touched; the readout prints the sorted copy's H beside the reading, `equal`, and how many of the 200 tiles changed place; the mark under the first forty goes, because those forty scatter. SORT again lays the tiles out as drawn.
4. *CONTRAST.* A second sample of the same alphabet and the same N, drawn once at build from its own generator seeded with the first seed + 1, under a concentrated law p(k) ∝ 2⁻ᵏ (symbol 0 half the draws, symbol 1 a quarter…). CONTRAST measures it through the same `_measure` — the histogram, H, the strip and the ribbon all change together — and names the source and its seed on the readout (and on the `origin` rung's strip). CONTRAST again measures the first sample again; nothing is resampled.
5. *DISCLOSE.* Steps the meter's own rung (`apply_grid_config({"disclosure": next})`, wrapping origin → oracle): the meter tears its panel down and builds it again — the same seeded draws — and the staging survives the rebuild because `_rebuild_now` now skips it; the rebuilt boards are lifted again. The map places the gauge at `ledger` (bars, the number, the first forty draws; no formula): DISCLOSE reveals the formula (works), then the source and its seed with the ghost bars of the declared law (origin).
6. *A housed readout* (five lines on a dark plate on the desk's front-left): `N 200 · 10 symbols · uniform · seed …`; `counts … · sum 200`; `H … bits · max log2(10) = 3.322`; `as drawn · the strip shows the first 40` or `sorted copy: H … · equal · n of 200 tiles moved`; `disclosure ledger · the first forty draws shown`.
7. *The measurement path.* `_run_measurement` (the draw) now hands its sequence to `_measure` (the counts, H, the boards) — the shipped build walks both in the same order with the same lines; `measure(sequence)` lets a probe put a sample of its own on the wall (the contract's ends: one symbol → 0, an even population → log₂ 10). `entropy_of`, `counts_of`, `sorted_copy`, `get_ledger_state` for probes and the readout.
8. *Map.* `shannon_entropy_meter` at (5,6) → `shannon_entropy_meter:90#stand:ledger#disclosure:ledger`: turned to face east, the corridor (the door route, columns 7–9), so a visitor walking south from the north door sees the panel and the desk on their right and reads from the corridor's west edge; `museum.artifact_placement: "map"`; clear rects over the desk and its approach [4,4,7,9] and the door route [7,0,9,11] (W4's lessons: the dealer puts bodies on door cells and corridors, and a plinth centred on a rect's far edge is outside it — the first rect ended at row 8 and a plinth stood on (4,8)). No structure or utility cell moved; the map has no interior one-cell gaps, so no bench denial.
9. *A repair in the shipped script, for Astra.* The museum lane hands a map token's config to `apply_grid_config` BEFORE `_ready`; the shipped meter rebuilt on it (`_rebuild_now` → `_build_all`) and then built again in `_ready` — twenty frequency bars and two of every board, stacked in place (measured 13:24: `_freq_bars` 20 long, the first ten in proportion, the rest behind them). A `_built` flag now stores the values before `_ready` and lets `_ready` build once; after `_ready` a change rebuilds inline as before. Every placement of the meter that carries a token (`#disclosure:`) under the museum was built twice until now; the standalone grid, which configures after `add_child`, was not affected.

**Rejected.** A new sampling control (RESAMPLE): the card allows one only if it exposes the source and resets locally, and the ledger's comparison needs the same 200 draws to stay on the wall — CONTRAST is a second fixed sample, not a resample. Sorting the sample in place (the card: "do not resample while claiming only order changed" — the copy is sorted, the sample is not). Two ribbons (drawn and sorted side by side): the motion of one ribbon is what makes the invariance visible. Changing the alphabet or N for the contrast. Moving any secondary: the desk and its approach are clear; the corridor is the route.

**Runtime, 2026-09-11, 13:19–13:30, both lanes (Godot free only in the gaps between another session's museum autopilot runs; the runner refused beside each and a poll launched in the gaps; nothing terminated).** Three pairs of runs led to **bare 89 checks / 0 failures, live 96 / 0, no script errors**, each a runtime finding:

1. *13:20 — the staging hid its own subject.* The readout plate and the LEDGER panel stood on the desk top in front of the ribbon and hid its two ends from a standing eye (the first sorted capture showed the blocks from symbol 2 to 8); the mark under the first forty was hidden by the tiles. Both plates moved onto the desk's front face at hand height (the desk top is the ribbon's alone); the mark became a pale bar lying in front of the first forty. The probe's colour count read the MultiMesh's quantised instance colours back and matched none exactly — the nearest symbol colour now.
2. *13:24 — the panel built twice.* `_freq_bars` was twenty long: the museum lane hands the token's config to `apply_grid_config` before `_ready`, the shipped meter rebuilt on it and then built again in `_ready` — two of everything, stacked in place. The `_built` guard (item 9 above). The same run: a plinth dealt on (4,8), the first rect's far edge (the rect now ends at row 9); the corridor's column 8 stopped at row 4 by a dealt body in that run (the route rect was in place; the dealer's cells vary between runs) — the walk now tries all three columns and records what stops a body.
3. *13:28 — green.* Ten bars; the rebuilt hall staged with one panel; columns 8 and 9 of the door route run from row 1 to row 10; column 7 is stopped at row 3 by the entropy jar's own pedestal (`EntropyJar/Pedestal`, a map body on the corridor's west lane — the museum's route is the three-wide corridor and its verdict is not severed); into the desk, the body is stopped by `Staging/Desk` at 0.50 of 1.2 m.

**Measured (both lanes agree):** the gauge at (5,6) on the deck (y 0.00), its front east; ten symbols, two hundred draws; desk top 0.92 m; the panel's centre 1.40 m; the ribbon's 200 tiles on the desk top; the arrival counts 17 11 21 31 17 18 19 24 13 29 (sum 200), seed 1342042698, H 3.256 bits (3.25564732026922) against log₂(10) = 3.322, recomputed by the probe's loop to 10⁻⁶; the ten bars in proportion to the counts (diffs 0.0); the strip, the ribbon's first forty tiles and the sample's first forty the same forty; the tiles by colour the counts. SORT: the same counts, the sorted copy's H equal to the reading to the last digit, the bars unmoved, the tiles running 0…9 in blocks, 198 of 200 moved, the bar gone; SORT again restores every tile's place (and the bar). CONTRAST: counts 109 46 20 17 6 2 0 0 0 0 (sum 200), H 1.818 bits, seed 1342042699; the strip, the ribbon and the bars follow; CONTRAST again brings the arrival draws back, draw for draw. `measure()`: 200 of one symbol → 0.000000; i mod 10 → 3.321928. DISCLOSE: ledger → works (the formula) → origin (the source strip, ten ghost bars) → oracle → tally → ledger, the same draws and H at every rung, the staging and its ribbon kept. The museum: no seals, no slide, the walk not severed; plinths off the rects. The ribbon's far ends 1.52 m from an eye before the desk; the SORT button 0.93 m from an eye before the panel. The streamer frees the hall with the gauge and its panel and rebuilds it staged, reading the same draws and H. Live (autoloads present, the rig 1.5 m before the desk): the pointer had SORT, CONTRAST and DISCLOSE under its crosshair and pressed each (the tiles regrouped; the concentrated sample took the wall; the rung stepped, and four more presses brought it round); the rig walked the door route south 2.5 m in half a second and was stopped by the desk at 0.92 m walking west from the corridor; the walker's camera guard stopped; the front view by the rig's own camera. Captures: `probe_entropy{,_sorted,_contrast,_origin,_readout,_ribbon,_overview,_desktop_front}_live.png`. Observed in the first runs: the panel's own baked labels (the title, the ceiling, the forty-symbol strip) washed out under the museum's light and needed closeness; the glowing H read from the corridor (addressed in the follow-up below). **Runtime status, corrected after Astra's review (item 4):** both R1 runner logs record exit 0 (13:28:41, 13:29:22); my "exits 127 in every run" was W3/W4's fact carried over without reading these logs. Two engine-log facts, kept apart from the assertions: `ERROR: Unrecognized UID: "uid://rwex60pqapc"` at startup — the project's `buses/default_bus_layout` reference (project.godot line 24), present in all 13 saved runs of every hall since the pilots; and `WARNING: ObjectDB instances leaked at exit` — present in the three saved runs of the randomness chapter (Random_Definition live 08:08:18, both R1 runs) and absent from the ten Waves runs, including a hall where the meter is not placed, so not the meter's; the standalone build-once probe (below) records whether the meter alone leaks.

**Pending:** a person at the desk; a headset walk; Astra's acceptance. The meter's other placements: 94 tokens in 47 maps, none carrying a `#disclosure:` or other config (84 bare, 8 `:0:0`, 2 `:0:-0.5`), so no placement but this hall's ever took the pre-`_ready` rebuild; the `_built` guard changes what they build in no case (an empty config never rebuilt).

## 2026-09-11, afternoon — R1 follow-up (Astra's review: partial acceptance, three bounded corrections; `doc/book/handoffs/fable-r1-entropy-followup.md`)

**1. The origin ghosts follow the active law.** `_measure` drew every ghost at `sequence_length / num_symbols` — the uniform expectation — while CONTRAST had the concentrated law on the wall and the `origin` strip named it. Now `active_probabilities()` (1/N each for the shipped source; 2⁻ᵏ / Σ 2⁻ᵏ under CONTRAST — the same table `_draw_contrast_sample` draws from, `_contrast_weights()`) and `expected_counts()` (p(k) · N) give each ghost its own height, `clamp(expected / max count, 0, 1) · 0.1`: one flat line for the shipped source (byte-identical heights to before), a descending staircase under CONTRAST (100.1, 50.0, 25.0, 12.5, 6.3, 3.1, 1.6, 0.8, 0.4, 0.2 expected of 200). The readout gains a sixth line at origin naming the law and its expected counts; `get_ledger_state` carries `probabilities`, `expected_counts`, `ghost_heights`. The probe (section 7b) checks both orders — CONTRAST then DISCLOSE to origin, and DISCLOSE to origin then CONTRAST — sorted and unsorted, with the probabilities summing to 1, the expected counts to N, every drawn ghost height against its expected count after the display scaling, the staircase or the flat line, and the return to the arrival (ledger, the first sample, as drawn, no ghosts). Rerun 15:11–15:17, both lanes: **bare 132 / 0, live 139 / 0, exit 0, no script errors**; the expected counts under CONTRAST 100.1 50.0 25.0 12.5 6.3 3.1 1.6 0.8 0.4 0.2, the ghost heights 0.0918 0.0459 0.0230 0.0115 0.0057 0.0029 then the 0.002 floor (largest count 109); uniform 20.0 each → 0.0645 flat (largest count 31); the same in both orders and with SORT on.

**2. Readable from standing.** The baked tags (title, formula, H, ceiling, ticks, symbol labels, strip, source rows) are dark glass plates with a lit bezel and a textured face, all shaded: under the museum's light their specular turned the plates pale and the text faint in the standing view. Under the ledger `_print_tags()` draws every tag's meshes unshaded (the plates stay dark, the text keeps its colour); the shipped strip — one row of forty at 2 cm, 1.11 m wide, wider than the panel — is laid in two rows of twenty at 2.6 cm inside the panel's width (`_lay_strip_rows`, the shipped board hidden, not freed); the LEDGER panel's cream face and the push buttons' bright caps are toned to a matte mid grey (`_tone_panel`, labels' plates untouched). Nothing in the shipped look changes. Judged from three new captures — the standing approach from the corridor's edge (2.2 m), the panel at 0.75 m, the buttons from a standing eye — not by a headset: at 0.75 m every label on the panel reads (title, H, ticks, ceiling, symbols, both strip rows); from 2.2 m the H, the bars, the strip bands, the desk's plate and the button panel read, the title and small labels not yet; the buttons are three matte caps with their names under them.

**3. The build-once repair, verified alone.** `commons/testing/probe_wcn_entropy_buildonce.gd` (`bash tools/run_wcn_probe.sh entropy_once`; headless-capable; the meter instantiated outside the museum), 15 checks / 0 failures, exit 0, no script errors: (a) the shipped default builds ten bars, ten labels, no ghosts, the formula, 42 children, H 3.256; (b) configured to `origin` BEFORE `_ready` (the museum's order): one panel — ten bars, ten ghosts, the source strip, 55 children, the same draws and H; (g) configured to `origin` AFTER `_ready` (the standalone grid's order): the same 55-child shape; (c) `tally` after `_ready`: ten bars, no ghosts, no formula, 40 children; (d) four `step_disclosure` calls walk oracle → tally → ledger → works and leave the default's 42 children with the same draws and H; (e) `seed 7` before or after `_ready`: the same draws either way, different from the default's, one panel each; (f) the ledger before `_ready`: one staging, ten bars, 200 tiles, the same draws; a disclosure rebuild keeps one staging. The meter alone: the project's UID error, **no** leak warning at exit.

**5. The bars fell off the panel after a measure — found in the follow-up's captures.** `_measure` writes every histogram bar's and ghost's y in the shipped frame (`FREQ_REGION_BOTTOM + h/2`, about −0.13 m). The first measure precedes the lift, so the arrival looked right; any later measure — CONTRAST, a probe's `measure()` — dropped the bars 1.4 m into the desk, and the origin + contrast capture (15:12) showed an empty histogram region under the symbol labels while every height check passed (heights, never places). `_after_measure` now lifts the bars and the ghosts again, and the probe checks their places on the panel after every measure and in every ghost state — verified in the 15:16 rerun: twenty bars and ghosts between 1.22 and 1.27 m in every state.

**4. The record.** See the corrected runtime paragraph above (exit 0; the UID error project-wide; the leak warning in the randomness chapter's runs only, including a hall without the meter, and absent when the meter runs alone). Historical logs of the accepted-partial run retained in `ada_run/waves_chance_noise/Random_Entropy/history/2026-09-11-1328-r1-accepted-partial/` before any rerun.

**Evidence plan.** `commons/testing/probe_wcn_entropy.gd` (`bash tools/run_wcn_probe.sh entropy [live]`): placement, facing and the arrival rung; the post, the desk top, the panel's height, the ribbon's 200 tiles on the desk, the mark, the panel's three buttons, the readout, the lifted boards, the formula withheld at ledger; the contract (counts sum to 200, H recomputed from the counts by the probe's own loop, H below log₂ 10, the bars in proportion to the counts, the strip = the ribbon's first forty = the sample's first forty, the ribbon's tiles by colour = the counts, the readout's lines); SORT through the button's signal (the sorted layout, the same counts, the sorted copy's H equal to the reading exactly, the bars unchanged, the tiles running 0…9 left to right, the moved tiles counted, the mark gone) and back; CONTRAST (same alphabet and N, symbol 0 near half, H far lower by the same loop, the seed named, the strip and ribbon and bars following) and back to the arrival draws; measure() at the ends (0 and log₂ 10) and the arrival restored; DISCLOSE ledger → works (formula) → origin (source strip, ghost bars) → … → ledger with the same draws and the staging kept; the museum's door-to-door verdict, seals, plinths against the rects; walks (the door route along column 8, the approach, along the desk's front, into the desk); reading distance and reach; the live lane's rig pressing SORT, CONTRAST and DISCLOSE through its pointer from 1.5 m, walking the door route and into the desk; captures (primary, sorted, contrast, origin, readout, ribbon, overview, the rig's front view); the streamer freeing and rebuilding the hall with the gauge staged, reading the same draws.


## Astra review, 11 September — after the handback

The preserved runner logs record exit 0 in both R1 lanes. Both engine logs include an unrecognised default-audio-bus UID and an ObjectDB shutdown leak warning; no SCRIPT ERROR was found. The earlier exit-127 statement above does not describe these runs. Source review finds uniform ghost bars still used under the concentrated source; that combined mode needs correction and a targeted check. Standing panel/button readability and a representative legacy placement after the shared build-once change remain open. Astra corrected final.md, supporting prose and primary book metadata; no artifact/map/probe change or new runtime run was made. See `doc/book/handoffs/astra-r1-entropy-review-2026-09-11.md`.

**2026-09-12 — the visual pass (Astra: "200 tiny tiles become a thin strip at approach distance").** The first forty draws stand again on the desk's FRONT face at four times the tile (0.026 × 0.10 m, pitch 0.032, from x −1.08), in the ribbon's own colours draw for draw, as drawn and staying as drawn while the ribbon sorts, under a caption that says so; the ribbon's pale bar still marks which forty. The desk's approach rect was written for an inclusive edge: `[7,0,10,11]` now (the plinth checks in the probes read the far edge as the museum does, exclusive). The shipped meter's own labels are as R1's follow-up left them. Live: 143 checks / 0 failures.

**2026-09-12, later — after Astra's review of the pass ("the readout partly covers the enlarged samples; the caption is too small").** The magnified row moved up the desk's front: tiles 0.026 × 0.08 m at y 0.872, so the row's bottom (0.832) clears the cased readout's plate top (0.825) and all forty stand whole from the approach; the caption reads `the first forty · ×4 · as drawn` at font 18 (pixel 0.0014) beside the row's right end; the row's colours are refreshed with the ribbon's so a regrow redraws it. Probe: every tile ≥ 0.075 m tall, the row's bottom above the plate top, the caption at a readable size. Live: 144 checks / 0 failures. Astra's acceptance, the headset: pending.

## 2026-09-12 — Astra: neighbours, counts and the changing ruler

Preserved the existing ledger, enlarged fixed first-forty excerpt, SORT/CONTRAST/DISCLOSE and prior build/lift/ghost-law repairs. Improved local panel contrast, enlarged its controls, made the readout backing unshaded and changed the excerpt caption to 22 px “first forty · as drawn”; the false uniform ×4 magnification claim is removed. All map layers and nine declared placements remain. Eight instantiate; replay_casino still has no living scene. Primary, final anchor and book hero remain shannon_entropy_meter.

The text starts with neighbouring colours, then watches sorting leave counts and marginal H unchanged. Four absent symbols still receive 2 mm marks; the tallest bar stays 10 cm as the largest count changes from 31 to 109. These display choices now belong to the discovery. Final and supporting texts are aligned, and the missing critical.md is written. Twelve source excerpts were checked.

Independent final desktop run, 21:07:48–21:08:38 +02:00, PID 14372: 149 checks, no assertion failures, exit 0; real pointer/button paths, both source/disclosure orders, excerpt identities, endpoints, approach and unload/rebuild. Both complete 200-symbol samples independently counted; H 3.25564732026922 and 1.81760045686154. Three matched desktop captures. Earlier colour-precision failures and a stopped stale-probe run are archived separately and are not acceptance evidence. The final colour check distinguishes stored-buffer precision from symbol identity.

No script errors; startup/logger/renderer diagnostics, a 156 ms generated mosaic build and an ObjectDB shutdown warning remain recorded. Headset reach, close lettering and Quest performance remain unverified. Source/runtime receipts and limitations: doc/space/entropy-review-2026-09-12/README.md. Review: http://localhost:3003/research/possible-bodies/random-entropy.html. All 55 published files match over HTTP; browser sorting, contrast, expectations and the one written primary card verified. Next: Random_Remove, where the eligible set becomes spatial.

## 2026-09-16 — central hero and supporting studies

Measure symbol shares at the central ledger; compare the measurement with visible disorder in the side exhibits. Current placements and sizes are listed in artifacts.md. The staging manifest and desktop runtime evidence are in doc/space/randomness-staging-2026-09-16/. This pass supersedes older placement/count descriptions in these notes; tracked-hand reach, headset comfort and performance remain to be checked.
