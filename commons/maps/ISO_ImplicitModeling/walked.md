# ISO_ImplicitModeling — walked

> ghost-drafted from the working map (close slot, isosurfaces thread); Palle rules the voice.

## The cast

implicit_surface_modeling

## The walk

One artifact closes the chapter with its craft matured. The implicit_surface_modeling station composes fields the way the gallery only displayed them: union as min, intersection as max, blending as soft algebra between functions. Sphere plus box minus cylinder, smoothed — each operation a sentence extended, and the surface re-extracted after every word.

## The close

Shape as sentence: say the field precisely enough and the surface is the proof. The close gathers the thread's whole walk — fog of values, fifteen tiles, described-not-modeled sculpture, authored fields, leaning cliffs, political thresholds, worlds drafted by distributions — into a single working method. Modeling implicitly means committing to the chapter's truth all the way down: geometry is never the primary object, only the visible verdict of an invisible field. Change the sentence, and the world re-decides itself.

## The seed (missing)

No chamber plants isosurfaces forward — the seed slot stands open, third of the open gaps (with foundationscrisis and graphtheory). The chapter that taught inside/outside as a movable line has no room where that verb is handed on.
