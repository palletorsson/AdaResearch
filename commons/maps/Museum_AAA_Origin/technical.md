# Museum AAA Origin — Technical

The dressing-room compiler emits one configured artifact token:

```text
origin:90:0#beam_height:0.6#octahedron_size:0.08#rotation_speed:0.18
```

`DressingRoomBuilder` applies the same `artifact_config` before `_ready()` during staging capture. The browser proof and playable map therefore request the same artifact variant.

The 5 × 5 footing reserves a full walking orbit. The measured body stays inside the central region; the larger contract describes interaction and viewing space, not an inflated mesh estimate.
