# History as museum material — 13 September 2026

The original ca_bridge scene grows 120 rows at 0.5 m spacing and was placed at (5,16) with y offset -5 in a 13x18 room. Its 15.5 m width and 60 m record did not fit that staging. A before snapshot of map, source, text and plans is under doc/space/ca-history-review-2026-09-13/before.

The opt-in `#study:history` uses the parent's actual `_calculate_next_row` and `_apply_rule`, with 15 sites, 24 retained rows and Rule 30. It creates three mappings of one stored history. The desk is the record, occupied deck tiles have matching collision, and the ornamental lattice turns site address upwards and generation along the room. Its bronze frame has collision; its coloured infill is visual. Emission makes the material luminous; projected coloured lighting is not implemented.

The study arrives held with generations 0–11. REPLAY returns to generation 0. STEP and RUN append to a bounded 24-row record. SEED cycles single / pair / locally seeded row, and FLIP X9 changes that one initial bit and replays. ROW moves the gold witness backwards through the retained rows. A changed beginning resets all three outputs; it does not edit an old generation while pretending its descendants still belong to it. Controls should be used from the rim, because replay removes support tiles. A 0.6 m lower basin and end ramps collect the gaps. The surrounding museum passage is independent of the generated tiles.

Nineteen new rows stage the study before the retained gallery, with a two-column extension to the east (15×37 overall). The old structure and all four other artifacts are shifted by +19 in z; two front-partition wall cells open the east bypass and one old east-wall cell joins the added aisle to the rear terrace. The first capture triggered automatic shrinking of the lab to 65%; the added aisle lets it remain at its authored full size. Original primary ca_bridge moves into the study bay; no artifacts or roles are removed. The retained lab/columns/mirror still need their own detailed interaction review. Current active order remains Introduction → ElementaryRules → this hall → BeyondBinary. The broader proposed ladder has not been applied in this pass.

Museum applications to develop through the chapter: stored CA history as screens, perforated cladding, railings and woven surfaces; moving Life patterns as agents that light neighbouring objects; proximity entering a surface's update in ExpandingSpace; coupled fields as changing wall or garment material in SoftRules after its live simulation exists. Each application must expose its actual mapping, inputs and physical consequences. A common appearance alone does not establish shared state or interaction.

Runtime evidence and remaining limits are recorded separately in doc/space/ca-history-review-2026-09-13. Headset comfort and performance remain unverified.
