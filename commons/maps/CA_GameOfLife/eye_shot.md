# Eye shot — CA_GameOfLife

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **0 overlaps, 1 tight** — the law wants ≥1.2m to walk between.
- `[tight  ] dark_sphere                <-> ca_columns:0:-0.5:0.2      gap +0.08m (centers 1.00m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 1/1  mean Δ=+0.172
      walkability improved: 0/1  mean Δ=-0.268

no sibling kept — the move did not beat the ride (overlaps 0→0, tight 1→1). Note-only.

## The voice (qfep)
5 of 5 cast members carry a theory-claim; 0 mute.
- **ca_bridge** — rule (0-255) — each Wolfram rule number produces a completely different bridge topology A bridge built by a ru
- **ca_columns** — rule_born/rule_survive — the Life variant determines whether columns are sparse towers or dense walls Stack ti
- **dark_sphere** — Transformation often needs an invariant reference to be legible. The sphere stays simple so surrounding change
- **lab_room** — accent_color — drives the strip, lights, signage tint, and sub-line color. Matches the QFEP phase of the workb

## The text vs the space
**no walked.md** — the space stands unwritten; this note is the first text this map has.

## The heuristic understanding
The space already walks: the bodies keep the law without being told. What carries this map is its voice, not its floor.
