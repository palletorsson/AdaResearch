# CA Introduction — the next row and the blob-culture transition

13 September 2026. The user asked to continue into Cellular Automata after clarifying that the easy noise filter can become a mental algorithmic wall. Its organic or queer gesture remains valuable; learning further capabilities keeps other forms possible.

## Prepared result

Three primary works have aligned final.md anchors, roles/order and book lines: ca_rule_explorer, persian_rug and line_network_ca. All nine authored placements remain. Original structure and utilities are byte-equivalent as parsed arrays. The optional explorer study is moved from (10,12) to (10,4), turned towards entry and supported at y=1.04 m. Its 32-cell, 24-row, 1.6 m board starts held at Rule 90. STEP, RUN, SEED and CELL give access to the existing elementary mechanism; the side plate reads the actual three inputs and predicts their next state. The original rule/speed sliders and five preset/reset buttons remain. Shared reset now clears unused display rows and the fractional interval; unconfigured dimensions, auto-run defaults and keyboard controls remain.

The rug plane stays at (4,4) and rises from -.49 to .025 m. Both placements explicitly suppress automatic plinths, since the new explorer contains its own desk and the rug belongs on the deck. The museum placement row is regenerated from the current map. Its converter still maps authored numeric tiers 3/4/5 to tile 4; distinct-height recovery remains a spatial review item. No height recovery is claimed in this pass. All other rows in both museum plans remain unchanged.

The original final was grounded in the explorer, but supporting prose described the rug incorrectly as a one-dimensional automaton, claimed uniformity where frame/interior rules differ, and treated its imposed mirror as unplanned emergence. The revised three-artifact account follows actual source. The line network is stochastic frontier growth with occupancy writes during the scan; its renderer's later parent choice is not a causal birth record. All original texts are in before/.

Noise_Perlin_Simplex's ending and critical text now retain Palle's precise blob argument, with the further clarification stored in doc/book/RULINGS.md. The critique addresses a possible aesthetic habit, not a proven prevalence claim about all visual culture. Noise can be volumetric and can remain part of later bodies. An isosurface alone does not implement interior material. The prior hall review links into the new CA review.

## Verification and limits

22 grammar, source-excerpt and alignment checks pass via ada_run/verify_ca_intro.py. These include complete placement preservation, unrelated plan/role/book preservation, no extra automatic plinths, and matching primary anchors/order. GDScript grammar checking uses gdtoolkit with a workspace cache; it is not Godot's type checker.

Godot PID 40112 was already open. The guarded runner declined to launch another process. No current in-engine capture, input, collision, timing or headset result is claimed. No existing process was stopped. The new study's placement, labels and pointer targets remain pending in-engine verification.

The web exercise independently implements the same elementary rule and proposed dimensions in JavaScript. 17 browser checks pass, including explicit expected generations, wrapping, bounded history, reset, loaded images, no horizontal overflow and all three primary book cards. Details are recorded separately in browser-verification.json. This is a remote learning exercise and reviewable proposal, not a Godot recording.

The prepared real-scene probe is ada_run/review_ca_intro.py. Run when Godot is free. It captures the actual museum and checks 2048 rule/input cases, seeded updates, wrapping, history clearing, witness prediction, unconfigured defaults, all nine pointer buttons and scoped ownership. Further manual/automated checks should inspect both original sliders, actual approach/crossing collision, rug floor clearance, the line network's bounds and showcase performance. Headset checks remain deferred by the user's remote setup.

## Next

1. Complete the prepared Godot review and correct any observed furniture, pointer or collision issue before reporting runtime acceptance.
2. Inspect the retained grid_substrate_runner's connection to the actual museum floor; do not state that the whole floor is an operating CA without evidence.
3. Continue with CA_ElementaryRules / structure_growth: its 26-neighbour volume and multistate decay extend the toolbox. Distinguish its programmed generation stop from equilibrium. CA_ElementaryRules and CA_AgentsCircuits are missing from the six-hall active plan. The current physical successor is CA_GameOfLife. Review and restore the missing elementary hall before claiming that insertion is complete.
4. The optional Lab_Path connection remains a separate backlog item; it does not block the main continuation into Cellular Automata.

---

## Earlier field notes (historical)

