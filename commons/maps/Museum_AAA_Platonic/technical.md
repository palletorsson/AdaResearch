# Museum AAA Platonic — Technical

The map token is:

```text
platonicsolids:180:0#palette:trans#staging:bare
```

`staging=bare` makes ownership explicit: internal floor, banners, scripted lights, packed preview lights, and camera do not define the museum room. The 7 × 3 m geometry sits on a contract-generated low platform inside an 11 × 7 m clearance envelope.

A `wp` extra is part of the dressing-room recipe, so the raised artifact anchor passes the same reachability rules as the surrounding route.
