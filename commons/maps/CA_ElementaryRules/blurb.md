Elementary cellular automata: one dimension, three-cell neighborhoods, eight possible patterns. 256 possible rules, each producing different behavior. Rule 30 generates apparent chaos. Rule 110 is Turing complete. Wolfram's classification: from death to complexity in a single number.

The `structure_growth` artifact evolves an elementary CA forward in time, letting you watch the pattern emerge generation by generation. Rule 110 runs in the same format as Rule 30; the only difference is eight bits of output table. Wolfram's Class I–IV parallels Langton's lambda and Kauffman's NK model — all converge on the same insight: complexity lives at the boundary between order and chaos. Rule 110's Turing completeness is the sequence's deepest result. A one-dimensional, two-state, nearest-neighbor automaton can simulate any computation. Simplicity is not a limitation; it is sufficient.
