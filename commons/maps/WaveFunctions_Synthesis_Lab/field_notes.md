# WaveFunctions_Synthesis_Lab — field notes

## 2026-09-11 — W4: the sum on the bench (Fable, on Palle's word to continue the Synthesis Lab in the same way as W2 and W3)

**The ruling.** The card asks for the existing five-slider instrument on a reachable bench with its component ladder unobstructed, a route from the assembled shape to one component and back with a baseline wired to the actual coefficients, and a final that meets the sum, removes contributions, predicts a region, shows the real accumulation loop and lets five harmonics fail to make a corner. The instrument computed the right thing already — one loop summing five sines, redrawn every frame from 256 samples — and three things stood between a visitor and it, none visible from the map: the component ladder hangs from −0.35 to −1.15 m below the demo's origin, so at the placed 0.5 m offset harmonics 2–5 were drawn under the floor; the five sliders stand in front of the sum's right third; and the placed cell (2,6) puts a 2.2 m display between the west wall (column 1, height 4) and a one-metre platform at (3,6), so its ends were inside both. A fourth is a property of the design, not a fault: the display scrolls at one cycle a second, so "a place on the sum" moves half a metre every second and cannot be compared with the same place on a row.

**What changed, and why.**

1. *An opt-in `stand` axis on `additive_wave_demo` (`bench`).* The shipped `none` is byte-for-byte the previous behaviour; the artifact's ten other placements are untouched. Under `bench` the display (sum, ladder, formula, preset and title labels) is lifted 1.75 m onto a dark backboard on two posts; the rows' axes stand at 1.40, 1.25, 1.10, 0.95 and 0.80 m, which a standing eye 0.55 m before the bench sees over the bench's back edge (0.92 m) and the readout's top edge (about 1.03 m). The shipped `Base` plate becomes the foot on the deck; the shipped caption moves onto the bench's front face.
2. *A bench.* 2.90 × 0.50 m at 0.92 m with a collider, in front of the display — a support the walker meets and a hand rests on. The shipped `ControlPanel` (five horizontal sliders, their labels, "HARMONICS") becomes a console standing at the bench's right end, tilted 20° back, with a dark back plate — clear of the display, at hand height (handles between about 0.98 and 1.37 m).
3. *A SUM panel* (RackTemplates, three push buttons on the bench's front-left, tilted 60° up): **BASELINE** restores the five arrival coefficients (`_baseline`, recorded after `set_preset(waveform)` at `_ready` and whenever the grid config changes the preset) through the same `_set_amplitudes` the presets use, so every slider moves back too; **H1 ALONE** keeps the first coefficient and zeroes the other four (the route "to one component"); **HOLD** stops `_time`, so the display stands still and a place on it can be read; pressed again it runs.
4. *A housed readout* (a dark plate on the bench's front, five lines): the five coefficients; the arrival five; each drawn row's value at a marker; the sum of the drawn rows against the total the sum mesh was drawn from, with `=` or `≠` and `held`/`running`; and `differs: a2 0.50→0.00, …` naming the coefficients that differ from the arrival. The values are the same arithmetic the meshes are built from (`display_values(i)`), at sample `mark_index()` = round(0.625 · 255) = 159, x = +0.247 m.
5. *A marker.* A thin amber line across the sum and the ladder at that sample, with a "mark" tag, so "here" is one place on every row and on the total.
6. *Ribbons.* Under `bench` the sum and the rows are flat triangle ribbons (half-thickness 7 and 4.5 mm; the rows at alpha 0.85 on the dark board) instead of one-pixel line strips, which vanish in a capture; the sample points are recoverable from the vertices (the point at sample k is the mean of vertices 6k and 6k+5).
7. *Map.* The primary moves from (2,6) to (4,2): `additive_wave_demo:0#stand:bench#waveform:sawtooth`, the display against the north wall (row 1, columns 3–6 are height-4 walls), its front to the room, the bench on row 3, the console's standing spot at about (5.9, 3.85). `lissajous_curves:0:1:0.1` moves from (5,2) to the vacated (2,6); `seismograph` from (6,2) to (2,8); `record_room:0:0` from (6,3), where its body stood in the console's standing spot (the first captures showed its wall beside the sliders), to the east strip's top at (12,2) as `record_room:-90:0`, its back to the east wall. `museum.artifact_placement: "map"`; `sculpture_clear_rects` over the bench and its approach [2,2,6,4], the north door [6,0,8,3], the one-cell corridor column [7,3,7,11] and the south door approach [5,10,8,13] — the museum's dealer put dream bodies on (5,11), (6,11), then (6,13) (the door cell itself) and a plinth on (7,4), the only cell of the corridor between the wall column 6 and the platforms of column 8, and each severed the walk; `props_deny: ["bench"]` — the museum widens the one-cell gap in the south wall (6,12) by converting its flank (7,12) to floor, the bench placer then reads the gap as a wall-side cell with a free far side and puts a bench in it, and the far side carries the microscope (`[em-walk] z=12 6:bench, 7:seal:microscope`); Point_One denies benches the same way. No structure or utility cell moved; the recovery contract (interior `2` = one-metre platform, perimeter `w`, `wall_height` 3, `gate_depth_rows` 0) is kept. Three instruments still stand on the south wall's cells (microscope (7,12), chemicalapparatus (9,12), multimeter (10,12) — the pathfinder marks them unreachable at h = 4); left as the map has them, for Palle.

**Rejected.** Moving `coloredlines` (5,4), whose nine-cell venue stands red and green beside the console's spot (no collider; the sliders stay reachable and readable — an arrangement call for Palle); bringing the three wall-top instruments down to floor cells (the same); a sixteen-slider bank or audio from the sum (the supporting texts describe both; neither exists and the card prefers five clear contributions); overlay mode as the staged default (the revised final reads rows; the `components` axis still offers overlay by token); a second marker (one place, read on every row, is the comparison — the visitor picks others by eye while held); a "vowel a" preset or biology stations decomposing heartbeats (fiction in summary.md and critical.md, not in the source); the optional application of the sum to a surface or garment (kept for later — the card allows keeping the instrument alone, and the sum is the subject here).

**Evidence plan.** `commons/testing/probe_wcn_synthesis_lab.gd` (`bash tools/run_wcn_probe.sh synth [live]`): placement and facing at (4,2); the arrival coefficients and each slider reading them back; the bench top, the sum's axis, the five row axes and the sight line from a standing eye over the bench's back edge and the readout's edge; the handles' heights and reach; the panel's three buttons, the readout's first lines, the marker at the readout's sample; under HOLD, the DRAWN total against the sum of the DRAWN rows read from the meshes' own vertices at six samples in one frame, and the readout's printed total against the marker sample; each of the five sliders moved to 0.70 through `move_slider` (the end of both the handle's and the desktop pointer's drag path) changing its own coefficient alone, its row redrawn at that amplitude, the difference named; the formula label; BASELINE and H1 ALONE through the push button's own signal, with all five coefficients and sliders restored and the preset label recognising the sawtooth; four rows leaving the ladder and one sine of amplitude 1 remaining; one harmonic raised again on its slider; the finite corner (local maxima over four cycles, the trough-to-crest rise in samples and metres, the error against the ideal sawtooth at the corner and on the flank); what else sounds; the museum's door-to-door verdict, local seals, plinths against the clear rect; walks (the door route, row 3 to the console, into the bench); the live lane's desktop rig dragging H2's handle through its pointer, pressing BASELINE and HOLD through it, walking the door route and into the bench; captures (primary, H1 alone, ladder, readout, console, overview, the rig's front view); the streamer freeing and rebuilding the hall with the instrument staged and at the sawtooth again.

**Runtime, 2026-09-11, 09:20–09:37, the bare lane (SceneTree probe, Godot free only between another session's museum autopilot runs — the runner refused beside each and a poll launched in the gaps; nothing terminated).** Five attempts led to **84 checks / 0 failures, no script errors**, each a runtime finding:

1. *09:20 — the mesh reader.* `ImmediateMesh` exposes no `surface_get_primitive_type`; the probe now tells a ribbon (6 (N − 1) vertices) from a line strip (N) by the count. Everything drawn was right in the captures already.
2. *09:26 — a row read one frame early.* After the first `move_slider` the row still showed the old amplitude: the probe had resumed from a timer, which fires after that frame's draw, so one `process_frame` later the meshes were the previous frame's. Two frames after every change now; the four later sliders had passed because they resumed from `process_frame` itself.
3. *09:26 and 09:29 — dealt bodies on the route.* The museum's dealer put dream bodies on (5,11) and (6,11), then on (6,13) — the south door cell — and a plinth on (7,4), the one-cell corridor; each severed the walk. `sculpture_clear_rects` now cover the north door, the corridor column and the south door approach (the first rect's far edge had excluded (6,11): a plinth centred on the edge is outside a `Rect2`).
4. *09:31 — the bench in the gap.* With the dealer off the route the walk still died on `z=12 6:bench, 7:seal:microscope`: the museum's door widener converts a flank of the one-cell gap (6,12) to floor, the bench placer then reads the gap as a wall-side cell whose far side is free, and the far side (7,12) carries the microscope. `props_deny: ["bench"]` (Point_One's way); no bench stands in the hall.
5. *09:36 — green.* The museum's own verdict: "the walk was severed — reopened it (slid GlassRack by (−1,−1))" — its repair, not mine, recorded here because the map is meant to be the authority: GlassRack (8,2) stood in the two-cell route by the north door.

**Measured (bare lane):** the instrument at (4,2) on the deck (y 0.00), its front south; arrival coefficients 1.000, 0.500, 0.333, 0.250, 0.200, each slider reading its own back; bench top 0.92 m; the sum's axis 1.75 m; the rows' axes 1.40, 1.25, 1.10, 0.95, 0.80 m; from an eye at (4.5, 1.55, 4.0) the sight line to the lowest row's axis passes 0.99 m over the bench's back edge (0.92) and 1.14 m over the readout's top edge (1.03); the five handles between 0.99 and 1.36 m, the farthest 0.86 m from the console's eye; the marker at x +0.247 (sample 159). Held, the drawn total read from the sum ribbon's own vertices equals the sum of the drawn rows at six samples to 0.0000 (−0.458, 0.823, −1.582, −0.988, 0.079, 1.407) and the readout's printed total is the marker sample's. Each slider moved to 0.70 through `move_slider` changed its own coefficient alone, its row redrawn at 0.70, the difference named; the formula label listed the five. BASELINE restored 1.000/0.500/0.333/0.250/0.200 on the coefficients and on all five sliders; H1 ALONE left [1, 0, 0, 0, 0] with four rows gone and one sine of peak 1.000; the second harmonic raised again on its slider gave [1, 0.5, 0, 0, 0]. The finite corner: 19 local maxima over four cycles (a sawtooth has four), the rise from trough to crest 11 samples = 0.086 m of a 0.5 m cycle, peak 1.583, the ideal missed by 1.54 at the corner and 0.29 on the flank. HOLD held `_time` still for 0.3 s and released. Walks: from the corridor west along row 3 to the console's spot 1.00; along the bench's front to the display's centre 1.00; south from the display's centre 1.00; row 3 west past the platform at (3,4) 1.00 (a 0.55 m gap between the bench's front and the platform); into the bench from the room, stopped at 0.35 of 1.5 m; the corridor south along column 7 from row 3, stopped at 0.79 — row 9, under the SoundscapeRadioRack lifted 1.4 m by its token (a body detours by column 8; measured in the live lane). Plinths dealt at (9,6), (8,6), (12,4), (9,3), none on the route; local seals air_music_display_case, record_room, spring_demo. The SoundscapeRadioRack's player is the only other `AudioStreamPlayer3D` playing, 7.7 m away. The streamer freed the hall with its instrument and panel and rebuilt it staged, at the sawtooth again. Captures by the intended camera: `probe_synthesis_lab.png` (the bench from the visitor's spot), `_h1_alone`, `_ladder`, `_readout`, `_console`, `_overview` (from the west room), `_plan` (straight down from 2.9 m; the wall tops read as a corridor — informational). Godot exits 127 at application quit after the JSON is written, the class of crash AirMusic left open.

**Runtime, 09:37–09:52, the live lane (project startup, autoloads present, the project's desktop rig; the bare lane rerun at 09:42 after the GlassRack move: 84 / 0, the museum's walk not severed and nothing slid).** Five live runs led to **93 checks / 0 failures, no script errors**:

6. *09:38 — the pointer drags.* The rig at the console's spot, aimed at H2's handle, held the left button and swung its view 5 cm along the track: the pointer reported the slider under the crosshair and a drag midway, and a2 went 0.500 → 0.851 through `pointer_event` → `move_slider` — the same path a mouse takes. Its two panel presses found nothing under the crosshair, and its door walk met the GlassRack the museum had slid into (7,1).
7. *09:43, 09:45, 09:49 — the presses.* With the rig 0.4 m before the bench and 0.15 m from the platform at (3,4), the aim was exact at the moment of aiming (the driver now iterates on the camera's own forward; miss 0.0°) and the ray still passed 8 cm over the button: the physics pushed the rig out of the platform's overlap after the aim, the eye moved 0.11 m, and at 0.65 m that is the whole button. The button's area was inspected in place — layer 21, monitorable, a cylinder 2 cm wide at (3.628, 0.995, 3.336) — and a sphere query and three controlled rays (along the cap's normal, from the camera to the centre, to the outer cap) all met it. The fix was the stand.
8. *09:51 — green.* From a stand 1.0 m before the bench (4.4, 4.3), a 27° look-down: BASELINE under the crosshair and pressed through the pointer (the five restored), HOLD pressed (held), pressed again (running). The rig walks column 8 south from the north door 2.5 m in half a second and, walking north from the room, is stopped by the bench at 0.55 m. The front view is drawn by the rig's own camera (`probe_synthesis_lab_desktop_front_live.png`); the walker's camera guard was stopped.

**Offline arithmetic (the numbers the corner checks expect, Python on the same five coefficients, 256 samples, four cycles):** 18 local maxima over four cycles (a sawtooth has four); the rise from the trough to the crest 11 samples = 0.086 m of a 0.5 m cycle; peak 1.58; the ideal Σ sin(nφ)/n = (π − φ)/2 is missed by π/2 at the corner and by up to 0.29 on the flank.

**2026-09-12 — the visual pass (Astra: "large red geometry fills the desktop operating view and covers most of the waveform and controls"; and the abnormal shutdown).** The geometry was `algorithms/wavefunctions/sine_hallway/hallway_scene.gd` — a sixty-metre tunnel of eighty-metre, 0.4 m tubes swinging 2.5 m — built at its own size as the synthesis stand's hero at (10,4); it fits the slab now when its parent is a synthesis stand (a three-metre miniature: the same waves per metre, the sway in proportion; measured 0.52 × 0.16 × 3.05 m at x 10.3–10.8). The `coloredlines:0:1:0.05` ornament at (5,4) is gone from this map: its `setup_scene()` dresses whatever camera is current in a dark sky and fog — in the museum, the visitor's — and the file is another hand's mid-edit (forum 260912-uf0uo). The corridor rect `[7,3,7,11]` had protected nothing (the far edge is exclusive) and the bench rect stopped a row short of the operator's spot: `[7,3,8,12]` and `[2,2,6,7]` now; a dealt plinth had landed in the corridor and the aisle before the bench once the deal changed. The shutdown crash was the same `FMPianoSynth` release fault as AirMusic's (via `air_music_display_case` at (1,2)), fixed at its source. Live: 98 checks / 0 failures, exit 0, no backtrace; the operating view shows the display, the ribbons, the plate and the console with nothing in front of them.
