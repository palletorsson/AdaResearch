# Eye shot — ISO_Caves

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **2 overlaps, 0 tight** — the law wants ≥1.2m to walk between.
- `[OVERLAP] mc_portal_landscape        <-> queer_marching_cave        gap -6.01m (centers 4.24m)`
- `[OVERLAP] mc_inside_cave             <-> queer_marching_cave        gap -5.25m (centers 5.00m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 0/1  mean Δ=-0.027
      walkability improved: 0/1  mean Δ=-0.150

no sibling kept — the move did not beat the ride (overlaps 2→2, tight 0→0). Note-only.

## The voice (qfep)
3 of 3 cast members carry a theory-claim; 0 mute.
- **mc_inside_cave** — Standing inside the isosurface: the cave is where the field says *in*, and you are in it. The boundary wraps a
- **mc_portal_landscape** — Two regimes of the same field joined by a portal — thresholds as doorways, the level-set constant as the archi
- **queer_marching_cave** — The cave that refuses a single threshold: iso-values shifted, blended, contested. If inside/outside is just a 

## The text vs the space
walked.md exists — the writing names 3/3 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: mc_inside_cave, mc_portal_landscape, queer_marching_cave sit in clearance violations — the text promises what the floor obstructs.

## The heuristic understanding
The violations are real but mechanical moving does not fix them — they are placement DECISIONS (which body yields?), not placement errors. This is verdict material, not tooling material.
