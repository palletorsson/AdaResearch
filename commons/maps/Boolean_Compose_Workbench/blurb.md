AND, OR, NOT — stack them. Boolean operations compose. A union of two intersections. A difference between two unions. Every complex shape is a sentence in this grammar.

Three workstations: at one, union. At the second, intersection. At the third, difference. Walk between them composing — the shape at the centre changes as the player chooses operands. Logic as sculpture, sculpture as proposition.
