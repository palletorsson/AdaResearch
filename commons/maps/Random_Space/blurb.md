The sequence finale — randomness fills space itself. Gaussian distributions, fluttering butterflies, Pollock paintings converge in a contained arena. All the threads of the sequence woven into a final meditation on chaos.

The `random_space` artifact synthesizes the sequence's techniques into a single volumetric experience: 3D noise fields, Poisson-disk distributions, stochastic density, all navigable. `dark_sphere` persists as the constant, the only unrandomized object in twelve maps. Randomness began as a number (map 1), became a process (random walk), acquired structure through distribution (Gaussian), produced emergence through collective action (pheromones), and arrives here as the fabric of space. Entropy is not disorder but the cloth from which order gets cut. The learner who cranked the PRNG handle in map one now stands inside a world that a single integer seed built.
