# Museum AAA Phi — Technical

Runtime `GeometryInstance3D` measurement replaces the old 8 × 8 m placeholder with a 0.594 × 0.211 × 0.243 m body. Body size alone is not enough to place it: the placement contract requires `mode=host_mounted`, `support=table`, and a support surface between 0.9 and 1.1 m.

The dressing-room footing raises its central 1 × 1 m cell to two half-metre units. In staged mode this resolves to `station_bench` with a 1.0 m top. The composer carries that support dependency into negotiation evidence before it writes or changes a map.

Only rotation `0` is permitted so the rail, labels, and southern interaction face remain aligned.
