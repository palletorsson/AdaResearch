A 12×12 grid. An 8×8 arena at its center. Three processes compete for the same space: an array visualizer lays bare the data structure underneath, an organic space grows without blueprint, a recursive tree branches by rule. Same grid, different logics. Order, growth, recursion — running side by side where their outputs can be compared directly.

Randomness is not one thing. A shuffled array is flat chaos. An organic form uses noise to simulate life. A recursive tree follows strict self-similarity with stochastic variation at each fork. Three algorithms, three relationships to chance. The arena makes the difference visible — not in theory, but in cells lighting up, patterns emerging or refusing to emerge.

The grid doesn't care which algorithm fills it. Structure is downstream of process.