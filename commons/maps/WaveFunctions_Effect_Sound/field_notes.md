# WaveFunctions_Effect_Sound — field notes

## 2026-09-10 — W2: the comparison becomes an instrument (Fable, from Astra's runtime follow-up)

**The ruling.** The card asks for one controlled sound comparison a visitor can discover, on a primary within reach, in a zone the racks cannot mask, with the mechanism named only after the ear has met it. The primary already computed the right thing — a decaying sinusoid inside the carrier's phase, 44,100 samples a second — and showed almost none of it: two cages at the floor, a colour cube and captions hung a metre below the origin, notes retriggered by any movement, the scope reachable only through a config word. What was missing was not synthesis but staging and a way to hold a reference.

**What changed.**

- `DualBallFMController.gd`: a staging axis `stand` (`none` = the shipped rig, untouched, in its six other placements; `desks` = the deck at the origin: two 0.92 m desks under the cages, a 0.70 m shelf for the cube between them, colliders on all three, the shipped caption on the shelf's front, an AUDITION panel at 1.02 m and a cased readout on the visitor's side (1.46 m at first, 1.30 m after the first captures), the scope lifted above the cages, the player's reach shortened from 40 m to 18 m). The audition: BASELINE (fc 440, A 0.01, D 1.5, R 2, I 0, τm 0.5 — unmodulated, index 0, the envelope and the tanh still applied, 1.8 s), HOLD (keeps the six values AND the synthesised note as A), PLAY (the current note without a move), COMPARE (A, a quarter second, then B, through a one-shot Timer that a second COMPARE restarts). The readout: the six values, the held six, `differs: …` naming exactly what moved, and what is sounding with the note's length, the synthesis milliseconds and the note count. `_on_*_changed` now ignores an unchanged triple (the mapper emits every frame; under `longhand` every emission rebuilt 4 × 1200 samples of scope). `drag_note_cap` (token `#drag_cap:`) can shorten movement-triggered notes; it ships at 0, the shipped behaviour, until the probe's synthesis timing says otherwise. `render_samples()` and `get_audition_state()` let a probe read the note as numbers.
- The map: `DualBallFMController` from (4,5) — a 1 m platform in the bench row, the 303 rack in the next cell — to (8,11) on the open floor of the south-east quarter, `DualBallFMController:180#stand:desks#evidence:longhand` (rotation 180 turns the panel to face north, the way a visitor arrives from the bench row). `museum.sculpture_clear_rects` [[6,9,11,13]] and `artifact_placement: "map"` (the recovered halls' opt-in). Structure and utilities untouched; the racks, monitor, sculptor, spring, table, chaser, pipes and cables stay where they were.
- `MelodyChaser3D.apply_grid_config` and `UniversalVRAudioController.apply_grid_config`: configuration accepted before `_ready` (the museum's map-authored lane calls it while the root is outside the tree; the grid component defers it) and applied once the `@onready` dependencies exist. The chaser keeps the words and builds in `_ready`; the audio controller keeps the whole config and applies it first thing in `_ready`, loading the control scenes for a JSON rack whichever path brought it.

**Why the quiet quarter.** The bench row at row 5 is a platform row of racks with their own players; a comparison held there is a comparison against the 303's squelch. Rows 9–12 east of the melody chaser carry nothing but the timbre sculptor at (9,8) three metres north; the floor is level, the door row is two cells south. The probe counts every `AudioStreamPlayer3D` playing within 5 m of the desks at rest; a person has not listened yet.

**Rejected.** Moving the racks (the bench row is the hall's other argument and Palle's arrangement). Muting the racks while the audition plays (a global mute is what the brief forbids; the racks are the room's density on purpose). A preset hunt (the four classic patches remain reachable through `#regime:`, none is required). Rendering the comparison on the monitor (the readout's `differs:` line is the comparison; the scope is the mechanism).

**Evidence plan.** `commons/testing/probe_wcn_effect_sound.gd` (`bash tools/run_wcn_probe.sh effect [live]`): placement, staging heights, the panel and readout, the four lanes and their captions, the note's reach; BASELINE's samples against the bare carrier under its envelope (max error), 1.8 s, no clipping; the orange ball lifted through the mapper → index 4 and nothing else, a retrigger, synthesis time, the head of the note differing from the baseline and the tail not, then a slower modulator decay carrying the difference into the tail, then the blue ball's x → 880 Hz; twenty moves in a second → one note per 0.3 s; the panel through its signals (BASELINE, HOLD, the readout's `differs: I 0.00→4.00`, COMPARE's A then B, PLAY); the quiet zone; the four AudioContr racks built from their tokens and the chaser's circle of fifths at tempo 60; five walks and the desks stopping a body; nothing severed; captures; the museum's streamer freeing the hall (rig, player, buses) and rebuilding it with a rig that sounds. The live port adds the desktop rig clicking BASELINE, HOLD and COMPARE through its pointer and walking into the desks.

**Runtime.** Pending at the time of writing: a Godot editor (PID 40320, opened 20:54) and its play instance of `endless_museum.tscn` hold the project, and the runner refuses to start beside a live instance. Results go below when they exist.


## 2026-09-11, 07:35–07:52 — the room runs (Fable, Astra's W2 completion brief, item 2)

Godot free (no editor, no play instance; the runner now refuses beside ANY Godot process). `bash tools/run_wcn_probe.sh effect` (a SceneTree probe, `--script`) and `bash tools/run_wcn_probe.sh effect live` (the same probe as a Node under project startup, autoloads present, the desktop rig pressing buttons through its pointer). Final runs at 07:50–07:52: **bare 89 checks / 0 failures, live 94 / 0, no script errors in either engine log**, every capture drawn by its intended camera (`captures.*.is_ours` true; the desktop front by the rig's camera). Files: `probe_effect_sound{,_live}.json`, `_engine.log`, the five views `probe_effect_sound_{,readout_,scope_,overview_}live.png` and `probe_effect_sound_desktop_front_live.png`, and three notes for a person to hear: `audio_A_baseline_unmodulated.wav`, `audio_B_index4_moddecay0.5.wav`, `audio_B_index4_moddecay1.5.wav`.

**What the first attempt (07:39) showed, and what changed for it** — each a runtime finding, none visible in source:

1. *The readout hid the scope.* The plate at 1.46 m stood in front of the scope's rule and output lanes for a standing eye at 1.6 m, and the CARRIER / MODULATOR captions were cut by the scope plate. Now: the readout at 1.30 m (1.15–1.45, above the panel at 1.02, below the eye), the scope lifted a further 0.30 m (bottom 1.78), two short arms from the shelf to the panel and the plate. The captures show all four lanes above the captions, the readout below them, the panel under it.
2. *The museum's walk was severed.* With `artifact_placement: map` the hall is built as the map says, and the museum's walk map found the door pocket closed — `[em-walk] SEVERED and nothing reopens it`: from the north door only (6,1)–(6,2)–(7..9,2)–(7,3), then a decoration `pick_up_cube` at (7,4), the RackSineBasic's two-cell seal at (5..6,3) and the cable builder's at (8..10,3). Moved that one cube (7,4) → (11,7); the museum's dealt dream-body sculpture then took (6,4), so `sculpture_clear_rects` now also keeps the door corridor [[4,1,7,5]], the west aisle [[1,3,4,7]] and row 7 [[2,7,11,8]] free of plinths (a plinth had landed in the west aisle on the next run). The museum now walks the hall door to door (`_walk_severed` empty); it still reports three local seals — two cubes and the slider rack — as closing pockets behind the row-1 partition and in the east column, which are not on the route. One plinth is dealt, at (9,2).
3. *The quiet quarter was not quiet.* At rest one player sounded within 5 m of the desks: the `timbre_sculptor` at (9,8), 3.3 m off, whose tone runs continuously from `_ready` at −6 dB, unit size 2 m. Its placement now carries `#sound:near` (a new opt-in word on the artifact, default off, its other placements untouched): the tone sounds only while the listener — the viewport's camera — is within 3 m. Verified both ways: silent at rest with the listener at the doorway; a camera 2 m from it wakes it; a camera at the desks (3.32 m) leaves it silent. Beyond 5 m two players of the `harmonic_distance_table` (5.9 m) run continuously; noted, not touched. This is a count and a distance rule, not a listening.
4. *Bus bookkeeping.* 1 bus before the build, 11 after, 3 after the museum's streamer freed the hall — the three are `Master`, `EmTone`, `EmSpace`, the museum's own; every `RackBus_*` of the hall's four audio controllers was removed with them.
5. *The bare lane had died.* Every SceneTree probe called `add_child(drv)` in its desktop section; a SceneTree has no `add_child`, so the six `--script` probes failed to parse. `root.add_child(drv)` in all six (the port maps it). And the bare lane's capture cameras were being reclaimed by the walker's guard; the capture block stops the guard in both lanes now.

**Measured, both lanes:** the rig at (8,11) on the floor (y 0.00), facing north; cages 0.92 m, balls 1.17 m, a metre apart; panel 1.02 m at z 10.84 (the north side); readout 1.30 m; scope lanes modulator / carrier / rule / output with the output captioned `out(t) = sin(2π · fc · t + m(t)) · env(t)`; the player's reach 18 m. BASELINE deals 440 Hz, attack 0.01, decay 1.5, ratio 2, index 0, modulator decay 0.5 — a 1.8 s note of 79,380 samples, peak 0.332, and at index 0 the samples equal the bare carrier under its envelope and the tanh to 1e-14. Lifting the orange ball through the mapper to 40 % of its cage sets index 4.00 and nothing else; the move retriggers a note (`drag`); synthesis 45–78 ms per 1.8 s note on the main thread. Index 4 against the baseline: mean |Δ| 0.321 over the first 0.1 s, 0.0088 over 1.45–1.75 s; with modulator decay 1.5 s the tail difference is 0.030. The blue ball's x sets 880 Hz and leaves the modulator alone. Twenty moves in 1.4–1.6 s trigger 4–5 notes: the shipped one-per-0.3 s limit holds, and each drag note is cut by the next, so a drag is a run of beginnings. BASELINE, HOLD, PLAY, COMPARE through their signals: HOLD keeps index 0 as A and plays it; one lift → `differs: I 0.00→4.00` on the readout's third line; COMPARE plays A then, after 1.8 + 0.25 s, B; PLAY replays B. The four AudioContr racks build from their tokens before entry (1, 33, 33 and 17 children; no pending config); the melody chaser builds the circle of fifths (13 markers) at tempo 60 (beat 1.0 s). Five capsule walks 1.0 (the west aisle past the bench row, row 7 east, down to the desks, the audition floor west of the desks, to the south door); a body walking into the desks is stopped at 0.63 of 1.6 m. The streamer frees the hall with its rig and player and rebuilds it with a rig that sounds.

**Actual desktop input (live lane):** the project's rig at (8.25, 0, 9.94), 0.9 m north of the panel, left-clicked BASELINE, HOLD and COMPARE through the input pipeline — its pointer reported `Staging/Panel/Btn_0/1/3/InteractableAreaButton` — and the rig heard `baseline`, then A, then A followed by B; walking south on `ui_up` it covered 3.03 m of a possible 3.75 and was stopped by the desks. The balls cannot be grabbed on the desktop lane (an XR Tools pickable needs a hand); the probe moves them through the mapper's own path, which is kept apart from a player's grab in every record.

**`drag_note_cap` decision:** stays 0 (the shipped behaviour). A drag note is cut by the next trigger after 0.3 s whatever its length, so a cap would not change what is heard; synthesis costs 45–78 ms a note, which the frame absorbs. The token remains for a placement that needs it.

**Pending:** a person listening (the three WAV files are the notes the probe rendered); a headset walk; Astra's acceptance.

**2026-09-12 — the visual pass (Astra: "the large central readout obscures the carrier/modulator interaction area; the scope is cropped from the operating view").** The cased readout and the AUDITION panel lean low (40°) in front of the carrier and modulator desks at 0.66 m, below the hand space and off the line between the balls, each on a short arm from its desk's front; the scope comes down to 0.12 m over the cages (its bottom at 1.50 m) so the balls and all four lanes share the operating view. A first cut had put them outboard on floor stems and blocked the audition floor beside the desks (0.32 of the way): withdrawn. The sculptor's proximity check now stops the walker's camera guard before it moves the listener and gives the wake a few frames — the one failure in Astra's isolated rerun was the guard reclaiming the current camera ("the view drifted to another camera — reclaimed for the walker" a line before it). Live: 97 checks / 0 failures.
