# Life addresses — 13 September 2026

The new application takes the same present into the floor and into three room lamps. Its three witnesses are fixed; its output connection can be removed while the cells continue. This makes “the museum answers the simulation” an inspectable operation.

First desktop findings: the glider reaches A at generation 8; the prepared glider/block meeting is empty by generation 24. The blinker keeps A occupied through both phases. A lamp is therefore not a generic oscillator display or glider detector.

The first screenshots showed the retained hexagonal work entering the new bay. Six additional floor rows separate the study from that gallery, preserving every existing artifact. Lamp labels gained cases. The first colour assertion was too strict for reduced-precision GPU instance colours; the final check records maximum channel error and requires it below 0.002, far below the difference between occupied and empty colours.

The tested scope is the primary and a portion of its surrounding route. The retained rear gallery is not fully reviewed for reach or traversal. Existing startup UID diagnostics are recorded in the engine log; runtime script errors must be absent before publication. Headset testing remains deferred.

Source backups and evidence: `doc/space/ca-life-review-2026-09-13/`. The active CA order and all artifact roles remain unchanged. The book hero now agrees with the existing primary role. Next: CA_ExpandingSpace, proximity, surface state and support.
