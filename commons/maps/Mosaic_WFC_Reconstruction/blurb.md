Wave Function Collapse takes adjacency rules extracted from documented mosaics and generates new tilings that satisfy the same local constraints. This map lets the player feed historical patterns into the WFC solver and compare its output to the originals. The algorithm gets the neighborhood logic right but often misses the larger compositional structure — constraint satisfaction is not the same as design.
