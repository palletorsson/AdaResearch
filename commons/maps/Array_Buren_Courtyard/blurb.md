# Array_Buren_Courtyard

Walk through Buren. Forty-nine striped columns set in a 14×14 plaza, every third cell. Same composition repeated across the grid; height shifts with `(row + col) % 5`. The variation is the lesson — an array becomes legible by what changes across its indices.
