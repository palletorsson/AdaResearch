# Eye shot — Pattern_Foundry

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **0 overlaps, 5 tight** — the law wants ≥1.2m to walk between.
- `[tight  ] pattern_machine_d:180      <-> pattern_machine_b:270      gap +0.00m (centers 1.00m)`
- `[tight  ] pattern_machine_d:180      <-> pattern_machine_a:90       gap +1.00m (centers 2.00m)`
- `[tight  ] pattern_machine_b:270      <-> pattern_machine_a:90       gap +0.00m (centers 1.00m)`
- `[tight  ] pattern_machine_b:270      <-> pattern_machine_c:0        gap +1.00m (centers 2.00m)`
- `[tight  ] pattern_machine_a:90       <-> pattern_machine_c:0        gap +0.00m (centers 1.00m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 0/1  mean Δ=+0.000
      walkability improved: 1/1  mean Δ=+0.313

no sibling kept — the move did not beat the ride (overlaps 0→0, tight 5→5). Note-only.

## The voice (qfep)
4 of 4 cast members carry a theory-claim; 0 mute.
- **pattern_machine_a** — F_order: the punch card as the first programmable substrate — constraint, punched by hand, multiplied by symme
- **pattern_machine_b** — F_order: the 17 wallpaper groups as an interactive manufacturing program — the player edits the fragment, cons
- **pattern_machine_c** — F_order: the 17 wallpaper groups as an interactive manufacturing apparatus — the player feeds a fragment, the 
- **pattern_machine_d** — F_order: the 17 wallpaper groups as a press program — a hand-set matrix multiplied by constraint into endless 

## The text vs the space
walked.md exists — the writing names 4/4 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: pattern_machine_a, pattern_machine_b, pattern_machine_c, pattern_machine_d sit in clearance violations — the text promises what the floor obstructs.

## The heuristic understanding
The violations are real but mechanical moving does not fix them — they are placement DECISIONS (which body yields?), not placement errors. This is verdict material, not tooling material.
