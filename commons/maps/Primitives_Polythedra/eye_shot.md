# Eye shot — Primitives_Polythedra

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **0 overlaps, 22 tight** — the law wants ≥1.2m to walk between.
- `[tight  ] path_watchdog              <-> path_game_controller       gap +0.00m (centers 1.00m)`
- `[tight  ] path_watchdog              <-> interactive_point_origin_force:0:1#mode:transformation gap +1.00m (centers 2.00m)`
- `[tight  ] path_game_controller       <-> interactive_point_origin_force:0:1#mode:transformation gap +0.00m (centers 1.00m)`
- `[tight  ] path_game_controller       <-> becoming_catalyst          gap +0.87m (centers 1.41m)`
- `[tight  ] interactive_point_origin_force:0:1#mode:transformation <-> becoming_catalyst          gap +0.45m (centers 1.00m)`
- `[tight  ] interactive_point_origin_force:0:1#mode:transformation <-> snap_tetrahedron_puzzle:0:0.5:0#fillhole:reveal gap +0.41m (centers 1.41m)`
- `[tight  ] becoming_catalyst          <-> snap_tetrahedron_puzzle:0:0.5:0#fillhole:reveal gap +0.45m (centers 1.00m)`
- `[tight  ] becoming_catalyst          <-> floating_sphere_field#bounds:4,3,6 gap +0.87m (centers 1.41m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 1/1  mean Δ=+0.292
      walkability improved: 1/1  mean Δ=+0.016

sibling **Trial_eye_Primitives_Polythedra** kept: overlaps 0→0, tight 22→11, pathfinder OK.

## The voice (qfep)
7 of 11 cast members carry a theory-claim; 4 mute.
- **becoming_catalyst** — Embodies the full QFEP trajectory: from order (primitives) through entropy (randomness) to the edge of chaos (
- **cube_scene** — Transformation changes S while the cube serves as the invariant reference body. Because the base form is so le
- **floating_sphere_field** — The void was never empty, only under-rendered. Replacing the lattice with a drift trades the comfort of fixed 
- **lab_room** — accent_color — drives the strip, lights, signage tint, and sub-line color. Matches the QFEP phase of the workb
- mute: grab_trihedron, interactive_point_origin_force, pyramid_edit, snap_tetrahedron_puzzle

## The text vs the space
walked.md exists — the writing names 8/11 of the cast; dwells declared for 2.
- **the writing's subjects are blocked in space**: becoming_catalyst, interactive_point_origin_force, path_game_controller, snap_tetrahedron_puzzle sit in clearance violations — the text promises what the floor obstructs.
- space without text: floating_sphere_field, lab_room, path_watchdog — standing in the room, absent from the walk.

## The heuristic understanding
The floor was fighting the walk — bodies inside each other's clearance. The mover found a better seating; the ride confirms it in text. The voice column above says what the room is FOR; the next writing pass should say it in the walked page.
