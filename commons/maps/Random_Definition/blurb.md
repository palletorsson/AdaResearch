Order flickers, entropy whispers. This is where the algorithm cannot predict, cannot index, cannot control — the computational equivalent of wilderness, where randomness resists the cadastral grid.

The `prng_crank_machine` makes the pseudo-random generator physically operable: you turn the crank, the algorithm advances its internal state, the output appears. The `entropy_jar` collects entropy as substance. `trng_vs_prng` stages the central distinction — deterministic algorithm versus physical noise source. The `random_number_book_page_1955` reproduces the RAND Corporation's famous million-digit table, the moment randomness became a publishable commodity. Kolmogorov called randomness incompressibility: a sequence is random when no shorter description generates it. The map begins by naming the tension — grid versus noise — that the next twelve maps explore.
