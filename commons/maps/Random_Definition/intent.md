Concept: A surprise that returns exactly. Two grids of coloured cubes built from the same seed and the same draw order match cell for cell; RANDOM picks another seed and REPLAY brings it back; one deliberate extra draw before the colouring makes the same seed paint a different grid. Irregular appearance, reproducible construction and the procedure that links them are kept as three separate things.
Sequence role: Opens the Randomness sequence on the current spine, after Waves closed on construction (Synthesis Lab: a complex-looking result from a small definite procedure). Establishes seed, draw order and generator as the parts of reproducibility that later rooms rely on; leads to Random_Entropy, where a number is asked what it can retain from a sequence.
Technical angle: A RandomNumberGenerator seeded per column, three randf() draws per cell row by row (192 per grid), the `offset` comparison that consumes extra_draws values before colouring, and a RANDOM button that uses an artifact-local generator so the game's global RNG state is untouched. Placed as `seed_replay_demo:0:-0.5#comparison:replicas`; the panel carries the seed slider, REPLAY, RANDOM and +1 DRAW, and prints the draw count under the headline.
Critical angle: Reproducibility belongs to the whole procedure, not to the seed. The same integer under a changed draw order names a different picture, so the seed is not a hidden face behind the mask; it is one input among several, and the room shows the others by changing exactly one of them.
Key artifacts: seed_replay_demo is the primary encounter; prng_crank_machine turns a generator one state at a time as the secondary; trng_vs_prng asks where a value comes from when there is no seed to return to; entropy_axiom, entropy_jar and random_butterflies carry the sequence's entropy theme forward; random_number_book_page_1955 anchors randomness as published labour; the catalyst_pickup on the platform is labelled RANDOMNESS CATALYST, not chaos; dark_sphere anchors.

Gap: The +1 DRAW comparison and the local RANDOM generator are new (2026-09-10) and checked from source and a museum probe, not yet in a headset. The room's raised platforms are restored by museum.wall_height 3; the standalone grid builds them as two-cube stacks.
