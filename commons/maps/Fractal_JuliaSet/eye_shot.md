# Eye shot — Fractal_JuliaSet

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **0 overlaps, 3 tight** — the law wants ≥1.2m to walk between.
- `[tight  ] julia_set                  <-> dark_sphere                gap +0.66m (centers 4.00m)`
- `[tight  ] julia_set                  <-> julia_set_explorer:0:0.5:1 gap +0.19m (centers 3.61m)`
- `[tight  ] dark_sphere                <-> lyapunov_fractal           gap +0.18m (centers 1.00m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 0/1  mean Δ=+0.000
      walkability improved: 0/1  mean Δ=+0.000

no sibling kept — the move did not beat the ride (overlaps 0→0, tight 3→3). Note-only.

## The voice (qfep)
4 of 4 cast members carry a theory-claim; 0 mute.
- **dark_sphere** — Transformation often needs an invariant reference to be legible. The sphere stays simple so surrounding change
- **julia_set** — One c, one temperament: where Mandelbrot maps the whole family, a Julia set is a single member's portrait — co
- **julia_set_explorer** — Julia sets from c inside Mandelbrot are connected (ordered). Outside = disconnected dust (chaos). The c parame
- **lyapunov_fractal** — Maps regions of parameter space by stability — blue = ordered (λ→0), warm = chaotic (λ→1). The fractal boundar

## The text vs the space
walked.md exists — the writing names 4/4 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: dark_sphere, julia_set, julia_set_explorer, lyapunov_fractal sit in clearance violations — the text promises what the floor obstructs.

## The heuristic understanding
The violations are real but mechanical moving does not fix them — they are placement DECISIONS (which body yields?), not placement errors. This is verdict material, not tooling material.
