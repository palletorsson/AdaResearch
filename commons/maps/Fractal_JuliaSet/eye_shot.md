# Eye shot — Fractal_JuliaSet

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **1 overlaps, 2 tight** — the law wants ≥1.2m to walk between.
- `[OVERLAP] julia_set                  <-> dark_sphere                gap -1.10m (centers 2.24m)`
- `[tight  ] julia_set                  <-> julia_set_explorer:0:0.5:1 gap +0.19m (centers 3.61m)`
- `[tight  ] dark_sphere                <-> julia_set_explorer:0:0.5:1 gap +1.08m (centers 2.00m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 1/1  mean Δ=+0.028
      walkability improved: 1/1  mean Δ=+0.139

sibling **Trial_eye_Fractal_JuliaSet** kept: overlaps 1→0, tight 2→3, pathfinder OK.

## The voice (qfep)
3 of 4 cast members carry a theory-claim; 1 mute.
- **dark_sphere** — Transformation often needs an invariant reference to be legible. The sphere stays simple so surrounding change
- **julia_set_explorer** — Julia sets from c inside Mandelbrot are connected (ordered). Outside = disconnected dust (chaos). The c parame
- **lyapunov_fractal** — Maps regions of parameter space by stability — blue = ordered (λ→0), warm = chaotic (λ→1). The fractal boundar
- mute: julia_set

## The text vs the space
walked.md exists — the writing names 4/4 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: dark_sphere, julia_set, julia_set_explorer sit in clearance violations — the text promises what the floor obstructs.

## The heuristic understanding
The floor was fighting the walk — bodies inside each other's clearance. The mover found a better seating; the ride confirms it in text. The voice column above says what the room is FOR; the next writing pass should say it in the walked page.
