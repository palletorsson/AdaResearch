A production rule: `A → -BF+AFA+FB-`. A handful of symbols, a single substitution. Apply it once — a simple path. Apply it again — the path folds. Again — it begins to fill space.

Walk a checkerboard of platforms that mirrors the Hilbert curve's recursive structure. Overhead, a 3D curve traces the same grammar into volume — one unbroken line visiting every point in three dimensions. On the floor, three curves side by side: Hilbert, Peano, Moore. Different grammars, different paths, same result. Total coverage. Every point touched exactly once.

The rule fits in a sentence. The space it fills is boundless. A 1D line that saturates 2D. A grammar that exhausts 3D. Dimension is not a container — it's a dare.