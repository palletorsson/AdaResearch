# Eye shot — LSystems_Grammars_And_Curves

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **1 overlaps, 0 tight** — the law wants ≥1.2m to walk between.
- `[OVERLAP] space_filling_curve_gallery <-> ContextFreeGrammars        gap -0.00m (centers 2.24m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 1/1  mean Δ=+0.149
      walkability improved: 0/1  mean Δ=-0.093

sibling **Trial_eye_LSystems_Grammars_And_Curves** kept: overlaps 1→0, tight 0→1, pathfinder OK.

## The voice (qfep)
3 of 3 cast members carry a theory-claim; 0 mute.
- **ContextFreeGrammars** — The formal hierarchy of grammars — L-systems are a subset of parallel grammars; this artifact locates where re
- **Hilbert3D** — A 1D path that visits every point in 3D space — maximum E(S) coverage from minimum F-description. The curve's 
- **space_filling_curve_gallery** — Space-filling curves achieve maximum E(S) coverage — a 1D line that touches every point in 2D space. The gramm

## The text vs the space
**no walked.md** — the space stands unwritten; this note is the first text this map has.

## The heuristic understanding
The floor was fighting the walk — bodies inside each other's clearance. The mover found a better seating; the ride confirms it in text. The voice column above says what the room is FOR; the next writing pass should say it in the walked page.
