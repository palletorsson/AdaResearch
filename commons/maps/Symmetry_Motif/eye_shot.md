# Eye shot — Symmetry_Motif

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **2 overlaps, 4 tight** — the law wants ≥1.2m to walk between.
- `[tight  ] pattern_tile_4x4           <-> pattern_tile_cube          gap +0.13m (centers 1.05m)`
- `[tight  ] pattern_tile_4x4           <-> tile_meander_floor         gap +1.18m (centers 2.10m)`
- `[OVERLAP] pattern_tile_4x4           <-> science_screen             gap -0.94m (centers 1.05m)`
- `[tight  ] pattern_tile_cube          <-> tile_meander_floor         gap +0.05m (centers 1.05m)`
- `[OVERLAP] pattern_tile_cube          <-> science_screen             gap -0.59m (centers 1.48m)`
- `[tight  ] tile_meander_floor         <-> science_screen             gap +0.28m (centers 2.35m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 0/1  mean Δ=+0.000
      walkability improved: 0/1  mean Δ=+0.000

no sibling kept — the move did not beat the ride (overlaps 2→2, tight 4→4). Note-only.

## The voice (qfep)
4 of 4 cast members carry a theory-claim; 0 mute.
- **pattern_tile_4x4** — The atom of pattern: a 4×4 grid you paint, set to repeat. The smallest complete lesson in the chapter — a tile
- **pattern_tile_cube** — The unit you place — a grabbable color cube dragged into a cell of the pattern grid. The atom of the whole cha
- **science_screen** — Transformation here is projection: a 3D state is re-expressed in 2D while preserving selected structural relat
- **tile_meander_floor** — grid_cells — resolution of the tile grid the Greek key is a tiling problem — corners and straights compose the

## The text vs the space
walked.md exists — the writing names 3/4 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: pattern_tile_4x4, pattern_tile_cube, tile_meander_floor sit in clearance violations — the text promises what the floor obstructs.
- space without text: science_screen — standing in the room, absent from the walk.

## The heuristic understanding
The violations are real but mechanical moving does not fix them — they are placement DECISIONS (which body yields?), not placement errors. This is verdict material, not tooling material.
