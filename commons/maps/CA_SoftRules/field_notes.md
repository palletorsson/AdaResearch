# CA_SoftRules field notes · 14 September 2026

The first museum capture contained only ca_sphere and rule_30_110_gravity. The current authored JSON held six works. Regenerating only this hall's two plan rows recovered the placements; runtime exposed a second obstacle: the_clockmaker_of_rules had no map_ready registry flag. Its scene exists, so it is now registered for placement. Labels and source comments distinguish static illustrations from computed histories.

The original rd_artifact had no config_path in this map. Its batch mode cannot teach live exchange. An optional #study:exchange installation now uses the existing Gray–Scott stencil incrementally, with a held common seed and a single comparison variable. The legacy batch path is preserved.

First inspection moved the control/readout case to the side so it does not obscure the two walls. RUN work is bounded to four updates per opportunity. A failed probe used a nonexistent PackedFloat32Array.min method; it was corrected to explicit finite/range checks. That probe error was not an artifact failure.

Evidence and source backups: doc/space/ca-soft-rules-review-2026-09-14. Actual desktop controls, state comparisons, GPU texture values and walking paths are reviewed there. Headset checks and retained-secondary encounters remain pending. The active route is unchanged. Next: CA_EdgeOfChaos, inspect its complete collection before deciding how a matched intervention can make sensitivity observable.
