# Random_Walk — field notes

## 2026-09-11 — R2: the trail and the step (Fable, the next hall on the route after Random_Remove; Astra's card: "keep one walker easy to follow… stage its case and controls at human reach; show accepted steps, elapsed simulation time and retained trail length… keep the seed and boundary treatment explicit… verify RESET's seed policy instead of promising replay from its label")

**The ruling.** The card asks for one walker easy to follow before the population, the case and its controls at human reach, a readout of accepted steps, simulation time and retained trail, the seed and boundary treatment made explicit, the step rule compared apart from dimensionality, autonomous creatures kept away or made deliberate, the lab room's Gaussian annotation corrected, and a final that predicts from a suggestive trail and watches the prediction fail, separates a step from the accumulated position and the stored drawing, reads the increment update and the reflection rule and names LEVY's finite cap, and lets a projected crossing separate in depth. The primary already had most of its body: `random_walk_terrarium.gd` builds a cabinet (the terrarium case of the cabinet grammar, member 20), a glass tank at 0.92 m, five beads, fading trails of 200 positions, a keypad 2D · 3D · LEVY / RESET on a wedge at 0.78 m, and a seated stats screen at 0.57 m reading mode, steps and MSD. What was missing: the readout the card names (steps against time, the trail kept, the seed, the glass), a way to follow one walker, a seed a visitor can read and repeat (the shipped `walk_seed` is −1: the global stream, and RESET simply continued it), and — measured, not read — a keypad RESET that did anything at all.

**What the museum built before anything was touched (the survey run, 16:21).** The map's arena is `0`, which the grid walks as ground and the museum reads as holes (Palle, 2026-08-23: "value 0 for floating objects"): a starry void with a one-cell strip of floor down column 7, a ring of `2` platforms a metre up with nothing leading onto it, and the terrarium's own cell a `3` post — a wall. Without map placement authority the museum dealt the hall's bodies where it could: the terrarium into the raised ring at (2,1) with its keypad under the ring's floor, the grab-paper rack onto the north door strip sealing eight cells including the door, the leash, the sphere, the 128 walk and the pickup into the vestibule rows. Verdict: `SEVERED and nothing reopens it`. No probe of this programme had walked the hall before; the card's "implementation pending" was the honest state.

**What changed, and why.**

1. *The floor (R5).* Seventy-two arena cells `0` → `1`, (4,1) `3` → `1`. The museum lays floor only on `1`; the recovery of 10 September (another hand's, uncommitted, kept) had spelled the walls `w` and the ring `2` and left the arena as it found it. In the grid lane the change raises the arena from the ground plane to the strip's level, half a metre; the ring keeps its metre above the floor. `wall_height 3` and `gate_depth_rows 0` kept; the two `0` at (0,4) and (10,12) keep the grid lane's teleporters.
2. *The primary, on the floor, facing the visitor.* `random_walk_terrarium:90#stand:logbook` at (4,1): the front (+z, keypad and stats) toward the door strip, where a visitor stands at (5.5, 1.5) facing west. It had been `:180:-0.3` on the post — 2.2 m up, its face to the north wall, for a deck the museum never reaches.
3. *The logbook (opt-in, R2 · R3 · R6).* `stand:logbook` bolts a wing to the cabinet's south flank: a body with a collider (a hand rests on it, a walker meets it), a plate at 1.03 m leaning back 28° with six lines refreshed at 10 Hz — the rule and its dimension; steps and simulation time at thirty a second; the frame clock, frames and the catch-up cap of five; the trail kept of two hundred and who is followed; the seed and RESET's policy; the glass rule — and ONE · ALL / NEW SEED at 0.76 m. Under the logbook the tank runs a named five-digit seed drawn from a private generator, so RESET replays the same walk; NEW SEED draws another; `#seed:N` pins one. A body round the case, because the shipped cabinet is meshes only and a walker put a head through the glass. `stand:none` builds nothing of this.
4. *ONE.* The red bead followed, the four others dimmed to grey with faint trails — one trail to read before the population. ALL restores five. Implemented as instance colours and one colour helper the shipped path passes through unchanged.
5. *RESET, fixed.* `_reset_area.button_pressed.connect(_reset_walkers)` bound a zero-argument method to a one-argument signal; Godot refused it at every emit (`Method expected 0 argument(s), but called with 1`, two per press in the first full run). The keypad's RESET had never restarted the walk; KEY_R had. Relayed through `_on_reset_pressed(_button)`. The one change to shipped behaviour, and it makes a dead button do what its label says.
6. *The arena's other bodies.* `random_walk_128` is a 32 × 32 lattice over ten metres of 31 cm cubes with a collider each, not the "tiny cubes" its description promises: unscaled it carpeted the arena to the cabinet's foot and blocked every walk (0.01–0.14 of the way). At a fifth (`:0:0:0.2`, the token's fourth field, which both lanes read) it is a two-metre floor drawing of six-centimetre cubes at (3,4), west of the route. The rack to (3,7). The lab room to (9,4), off the door approach, its `dx_i ~ Normal(0, sigma)` annotation — Gaussian steps, describing neither the mounted Monte Carlo workshop nor the fixed-step tank — replaced by `each step is independent of the last — the position is their sum` and `fixed 0.015 m steps in a random direction, LEVY capped at ten — not Gaussian`; its sub-line `sampling pavilion — Monte Carlo workshop`. The three pixel clouds onto the ring cells the museum was already standing them on, at half scale and seeded (`#walk_seed:101/102/103`): unseeded, their cube shapes decided from boot to boot which cells were sealed, and one run had the museum scale a cloud to 80% to reopen the walk. `walk_seed` joins the grid's config-key whitelist (`GridInteractablesComponent.gd`), or the grid reads `#walk_seed:N` as a rotation shorthand. The catalysts stay: the vent requires the bracelet (`require_catalyst_armed` true), no brood in the hall, and the museum stands both on the ring's row 1.
7. *Map authority.* `artifact_placement: "map"`, `sculpture_clear_rects [[3,1,7,5]]` (cells 3–6 × 1–4; the far edge is exclusive).

**Rejected.** Rebuilding the arena's pit for the museum (a wedge up to the ring, a deck the visitor stands on): the card's encounter is at the glass, not above it, and the ring's forty-one cells are the recovery's to keep. A start/stop on the keypad: the cadence is the artifact's, and a local pause would not stop the museum's clock (R3). A population slider: a rebuild of the walkers for one press; ONE / ALL gives the single trail and the five without it. Fixing the lab room's `bool(String)` (`lab_room.gd:725`): another's file, not in the heads-up, two maps carry the key — reported. Leaving the 128 carpet as it was: the primary was unreachable behind it.

**Runtime, 2026-09-11, 16:21–16:57, both lanes (Godot free between another session's runs; the runner refused none this evening; nothing terminated).** A survey, six bare runs and two live runs led to **bare 106 checks / 0 failures, live 112 / 0, both exit 0 at quit, no shutdown leak warning; the engine log's only script errors are the lab room's**, each a runtime finding:

1. *16:21 — the survey.* The void, the ring, the slid bodies, the sealed door (above).
2. *16:38 — the first full run, 97 checks, two failures, one false pass.* The mechanics held at once: 2D pins y, the glass folds, LEVY's law and cap, the trail cap, the catch-up cap, the seed's replay, the stream's non-replay, the same first draw as heading and azimuth, NEW SEED, ONE / ALL, the six lines, the reach, the streamer. What failed: the wing's collider looked for by an auto-generated name; every walk south blocked by the 128 carpet's cubes. What passed falsely: "RESET restarted the walk" — the 3D press before it had already reset; the engine log carried two `Method expected 0 argument(s)` errors per RESET.
3. *16:45 — a parse error*: one variable declared twice; `gdparse` checks syntax, not scope. The project's `check_compile.gd` does.
4. *16:48 — 100 checks, one failure.* The carpet at a fifth, RESET relayed (no signal errors), the clouds still unseeded (the museum scaled one to 80% to reopen the walk); the one failure my cast into the ring's raised row at column 6.
5. *16:52 — 102 checks, one failure.* Clouds seeded at half scale; the route cast along column 7 into the south door frame's edge (0.77): the door's centre is column 6. Split into the ring's gap and the door's centre.
6. *16:54 — green in both lanes* (bare 103 / 0, live 109 / 0), and one section still owed: the shipped default beside the staged one.
7. *16:56 and 16:57 — final, green in both lanes*: bare 106 / 0, live 112 / 0. A bare instance without `#stand` has `Cabinet` and `Tank` and nothing else, the global stream, nobody dimmed, five own colours, and its keypad's RESET reaches the walk too.

**Measured (both lanes agree unless noted):** the terrarium at (4.5, 0.00, 1.5), front east; `stand` logbook; the tank at 0.92 m; the keypad at 0.775 m, 0.96 m from a standing eye at the visitor's spot; the plate at 1.025 m, 1.23 m from the eye; NEW SEED 1.20 m; the tank's middle 1.09 m. Six lines, the longest 42 characters. On arrival one bead in its own colour, four dimmed. The stream: 30 steps in a measured 0.85–0.89 s; simulation time steps/30; the stats screen and the logbook count the same steps. 2D: 300 driven steps, every y 0.20, every x and z inside ±0.25, the trail 200 of 300. The rule on given points: 0.30 → 0.20, −0.27 → −0.23, 0.26 → 0.24; 2D pins 0.35 → 0.20; 3D folds −0.05 → 0.05 and 0.45 → 0.35. LEVY over 2000 draws: max 0.1461–0.1494 m (cap 0.15), mean 0.0263–0.0279 (the integral 0.0272), 22–30 draws over 0.10 m; 300 LEVY steps all inside. A two-second frame: five steps. The screen's MSD equals the recomputed mean squared displacement to 4 decimals. RESET under the seed: sixty steps land the five walkers on the same spots bit for bit; under the shipped −1 they do not; the same seed's first draw is walker 0's heading in 2D and its azimuth in 3D to 1e-6 rad (0.3702 / 1.1495 / 3.0780 in three runs). NEW SEED: another five-digit seed, sixty steps elsewhere. The museum: no walk severed; seals lab_room (30 cells) and one pixel cloud (6); plinths at (4,11), (8,9), (8,12) across runs, none in the rect; walks: down the door strip past the case 1.00, column 6 to row 10 1.00, row 7 to the leash 1.00, into the case stopped at 0.27 by RESET's button body, into the wing at 0.35 by the wing's buttons. No brood; the vent unarmed. The streamer frees the hall with the terrarium and its wing and rebuilds it with the wing and five walkers under a new seed. Captures by the probe's camera: primary, running, tank, logbook, keypad, north door, arena, plan. Live (autoloads present, the rig 0.4 m behind the spot): 2D under the crosshair and pressed through the pointer — the mode switched; NEW SEED — 84744 → 74567; RESET — 23 → 0; the rig walked 2.5 m south down column 6 in half a second and was stopped by the case at 0.55 m of 1.1 walking west; the walker's camera guard stopped; the front view by the rig's own camera. The engine logs: the project's UID error, no leak warning, and four `SCRIPT ERROR`s per run, all `lab_room.gd:725`.

**Pending:** a person at the keypad and the plate; a headset walk; Astra's acceptance; Palle's calls on the pixel-cloud towers, the lab room's fault and the grid lane's raised arena.

**2026-09-12 — the visual pass (Astra: "the tank nearly vanishes into the bright room; its small point is faint").** Under the logbook stand a dark matte backboard (the tank plus 0.30 m each way) stands behind the glass, and the followed bead is twice the size of the four dimmed ones (instance scale 2.0 against 1.0), so the present point is told from its trail by size as well as colour. The bare-instance RESET check asserts what it claims (steps > 0 → 0) instead of an exact 20: a freshly built instance can tick once before `set_process(false)` lands (Astra's rerun counted 21). Live: 114 checks / 0 failures.
