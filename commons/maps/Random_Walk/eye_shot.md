# Eye shot — Random_Walk

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **1 overlaps, 1 tight** — the law wants ≥1.2m to walk between.
- `[OVERLAP] random_walk_terrarium:180:-0.3 <-> random_walk_collection     gap -1.12m (centers 1.00m)`
- `[tight  ] random_walk_128            <-> dark_sphere                gap +0.05m (centers 1.41m)`

## The move
        w, d = self.artifact.footprint_dim()
      File "C:\Users\palle\Documents\GitHub\AdaResearch_46\tools\placement_research.py", line 62, in footprint_dim
        s = max(1, int(round(math.sqrt(self.footprint_cells))))
    TypeError: must be real number, not list

no sibling kept — the move did not beat the ride (overlaps 1→1, tight 1→1). Note-only.

## The voice (qfep)
8 of 8 cast members carry a theory-claim; 0 mute.
- **catalyst_pickup** — sequence_name — IS the catalyst-bracelet mode this the chamber teaches the algorithm; the catalyst is the a pe
- **dark_sphere** — Transformation often needs an invariant reference to be legible. The sphere stays simple so surrounding change
- **lab_room** — accent_color — drives the strip, lights, signage tint, and sub-line color. Matches the QFEP phase of the workb
- **pixel_cloud** — upward_bias — weight multiplier for +Y steps; higher values produce taller, thinner clouds Self-avoidance is m

## The text vs the space
walked.md exists — the writing names 8/8 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: dark_sphere, random_walk_128, random_walk_collection, random_walk_terrarium sit in clearance violations — the text promises what the floor obstructs.

## The heuristic understanding
The violations are real but mechanical moving does not fix them — they are placement DECISIONS (which body yields?), not placement errors. This is verdict material, not tooling material.
