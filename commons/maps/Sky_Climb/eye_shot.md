# Eye shot — Sky_Climb

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **0 overlaps, 1 tight** — the law wants ≥1.2m to walk between.
- `[tight  ] force_pad                  <-> calder_mobile_primaries    gap +0.00m (centers 1.00m)`

## The move
        r = apply_to_map(m, engine=args.engine, in_place=args.in_place,
      File "C:\Users\palle\Documents\GitHub\AdaResearch_46\tools\place.py", line 222, in apply_to_map
        new_inter[r][c] = name_to_token.get(p.artifact.lookup_name,
    TypeError: list indices must be integers or slices, not float

no sibling kept — the move did not beat the ride (overlaps 0→0, tight 1→1). Note-only.

## The voice (qfep)
4 of 5 cast members carry a theory-claim; 1 mute.
- **calder_mobile** — a mobile is the lever law made into art — Calder's genius was that the weights and a binary tree of steel arms
- **calder_object_mobile** — F_force: torque made curatorial — the Calder mobile balanced at tau = w*d on every arm, hung with the project'
- **force_pad** — F_force: a velocity handed to the body (up + forward); gravity returns it to ground. up_force + forward_force 
- **return_launcher** — a launch is an initial condition; pick the landing and the velocity is forced. The stand on the glowing launch
- mute: calder_mobile_primaries

## The text vs the space
walked.md exists — the writing names 5/5 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: calder_mobile_primaries, force_pad sit in clearance violations — the text promises what the floor obstructs.

## The heuristic understanding
The space already walks: the bodies keep the law without being told. What carries this map is its voice, not its floor.
