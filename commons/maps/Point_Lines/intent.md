## Current encounter — 16 September 2026

Lesson: Two distinct points determine a straight line; the part between them is a segment. Move the endpoints and observe how distance and direction change.

The required book path has three encounters: make a connection and see the barrier open (`line_demo`, `do_not_cross_barrier`); vary two endpoints (`line`); compare a straight instruction with a recorded walk (`walk_this_line_marking`, `player_trace`). These five tokens are primary. The barrier supplies one concrete example of a further consequence attached in code. The room does not need to explain every use a line can acquire.

Abundance belongs in the narration. The reader briefly notices other experiments, then chooses to continue. Beginning in Point_One required leaving code unread; continuing here requires leaving worthwhile associations open. This is a decision about time and attention, not a claim that those associations lack value.

The paired-plus bench, red division, workshop, ruler, held rod, bead, laser, parallels and line collection are secondary encounters. They remain in the museum. Their longer book passages are retained in `doc/space/point-lines-proof-2026-09-16/chapter-before-focused-path.md`, with implementation details in encounter-reference.md. The paired bench is a useful return visit for comparing acceptance rules; four endpoints and two predicates need not become prerequisites for understanding a segment.

The exit asks what endpoint subtraction cannot recover: the route taken between those positions. Trace becomes necessary because we want to keep something of that journey; Grid follows with addresses and resolution. All 52 physical placements remain. This pass changes the reading and its primary roles, without rearranging the floor.

The historical notes below preserve earlier ambitions and placements; consult map_data.json and encounter-reference.md for the current setup.

## Prior layout notes — retained research material

Concept: Multiplication of lines into relational systems — parallels produce direction, crossings produce intersection, grids produce metric frameworks. Discrete relations begin behaving as networks.
Sequence role: Second map. Extends Point_One's single mark into connection and multiplicity. The point gains companions; relation replaces isolation. Prepares Point_Trace by establishing the static scaffolding that trace will temporalize.
Technical angle: Line drawing between two Vector3 positions, parametric line equations, grid construction from parallel/perpendicular sets, perspective projection and vanishing points, scale and measurement via laser_measure.
Critical angle: The politics of the grid — who decides which lines are parallel, which directions are privileged. Le Corbusier's Modulor (modulor_man_demo) as ideological measurement system. Perspective as Renaissance power geometry. And the blurb's closing charge — "efficient, relentless, forgetting everything but endpoints" — is put on the floor as an object rather than as prose: the two klee_walking_point units in the entry are the same artifact at walk_style=meander and walk_style=straight, the walk that wandered beside the walk that gave up.
Key artifacts: line_demo for the sentence the blurb opens with — carry one point to the other and the line springs into being; two_points_line for the same sentence stated as Euclid's postulate, and line for it already drawn, so the entry says it three ways (made, stated, inherited); plus_line_puzzle and parallel_line_puzzle for interactive constraint discovery; perspective_lines, scale_lines and dgrid for the three measurement systems; line_builder_3d for constructing lines in space; modulor_man_demo for the human body as metric unit; klee_walking_point x2 for the line as biography.

Walk: the hall is a nave with three east chapels and a west aisle, and it now has a real spawn (utility `s` at r1,c2 — the `sp` in the corner is a score cube, not a spawn, and the engine was falling back to the floor centroid mid-nave). Row 2 of the entry corridor is deliberately kept clear as the walking spine; everything hangs off rows 1 and 3. Five phases, north to south: ENTRY (corridor, the opening sentence three ways, under a 10 m ladder of reference lengths, past the Modulor and the two walks) — CONSTRAINT (upper nave: plus_line_puzzle then parallel_line_puzzle on the centre line, in blurb order, with line_builder_3d between them) — PROJECTION (the east chapels: perspective_lines alone in chapel A where its isolation:2 can be honoured; chapel B is a Dürer apparatus on the column-12 sightline, viewer at r13 → dgrid at r12 → gap → fontana_puncture at r10, which answers dgrid's registry `needs` line, "missing a subject seen THROUGH the grid to project") — REFLECTION (the quiet lower nave, grabbable_line and two lightrods) — EXIT (do_not_cross_barrier stands directly north of the teleporter and has no collider, so you leave by walking through the prohibition). The laser range was pulled out of the nave into chapel C so the room ends on its argument instead of on target practice; the five-unit laser_measure fan is kept, each still pointing its own way.

Voice: the hall has a `subtitles` block for the first time. Its two subtitle triggers used to fall through to their own key names and said "laser distance" and "line measures"; there are now four, keyed to the blurb's four movements and placed at the thresholds — opening (beside the spawn), crossing (the nave door), projection (the east door), violence (on the barrier cell before the exit).

Gap: dgrid at (12,12) now carries `dgrid:90`, which the registry's own description prescribes: durer_grid.tscn bakes a 90° yaw into its root and builds the frame in the local XY plane, so at token rotation 0 the frame's normal ran along world X while the designed sightline runs north-south along column 12 — the Dürer apparatus was being seen edge-on, as a 0.04 m strip. scale_lines' vertical seat wants the same look (its rung ladder is authored at absolute heights and the artifact auto-grounds). science_screen at (11,2) declares `#mode:line`: `apply_grid_config` handles a `mode` key (science_screen.gd:1582 → `_set_explicit_mode`, which skips all scanning), so the declaration is real and outranks the 8 m proximity scan that the `line` at (11,3) would otherwise have to win. An earlier pass called `#mode:line` fabricated after grepping only for `@export var mode`; that was wrong, and the corrected fact is recorded here so it is not re-derived. summary.md still describes the 7x27 corridor this hall was before the 2026-08-22 rebuild and should be regenerated by whoever owns it; walked.md's cast is now accurate again for the first time since that rebuild.
