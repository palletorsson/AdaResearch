# Artifacts in Random_Game

The instrument and the spatial encounter share a hall. Placements are source-grid metres; the entrance is at -Z.

| Artifact | Role | Position (x, z) |
|---|---|---|
| `r_c` | primary | (9, 6) |
| `random_removal_arena` | primary | (9, 17) |
| `random_doors` | primary | (9, 29) |
| `cube_projectile_spawner` | primary | (9, 37) |

The book markers match the primary roles. Glass boundaries are staging, with open entrances and exits. Controls marked `compact` retain their original callbacks on a common console.

`random_removal_arena` deliberately returns from Random_Remove as an application in a game; this is not an accidental duplicate. The smaller table remains in its teaching hall.

Supporting artifact: `waiting_profile`, at (9,47): restored wooden pickup cubes and random-height profile.
