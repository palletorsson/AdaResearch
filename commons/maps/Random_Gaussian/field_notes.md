# Random_Gaussian — field notes

## 2026-09-11 — R3: many draws, one law (Fable, the next hall on the route after Random_Walk; Astra's card: "focus the entrance comparison on GAUSS versus UNIFORM using the same display range, bins and number of draws… expose total samples and counts at range boundaries; make clipping visible… a fixed-count batch or pause/read facility… verify sample_seed and mode-specific formulas")

**The ruling.** The card asks for GAUSS against UNIFORM at the same range, bins and N; total samples and the counts at the range edges exposed, clipping made visible; a batch or a pause for a fair comparison, a bin count selectable only after the first lesson works; the Galton board kept if it helps; and a final that begins with a sparse, lopsided histogram and refuses to complete the bell in words, gathers more draws and switches law at equal N, reads the draw, transform and binning code after the difference is seen, and looks at the tail that hits the display's edge without letting a mean or a model define the value of a person. The primary had the draws and the laws and CLEAR: four transforms behind a keypad, a falling-bead histogram normalised to its tallest bar, a density curve at its own scale, a side plate with n, mean and deviation from bin centres. What was missing: everything the card names. Total samples was on the side plate in small type; the edge counts, the clipped draws and the bins were nowhere; there was no batch, no pause, no seed a visitor could read; and the keypad stood eight centimetres under the floor.

**What the museum built before anything was touched (the survey run, 22:32).** The south half of the map is `0`, which the museum reads as holes: a void with a one-cell strip of floor down column 7. The twenty-metre, five-metre-tall bell terrain (`random_bell_curve`, a heightmap you can walk) covered the whole hall from wherever the dealt lane put it; the dealt lane had also moved every body (the sampler to (10,15), the comparator into the vestibule, the paint splatter onto the north door strip, sealing it). The sampler stood at y 0 with its display at the knees and its keypad below the deck; 218 beads had landed in five seconds. Fifty-one rigid bodies in the hall: the Galton board's beads. Verdict: the walk not severed, seals from the splatter and the terrain (57 cells).

**What changed, and why.**

1. *The floor (R5).* Rows 14–20 `0` → `1` (73 cells): the museum lays floor only on `1`. In the grid lane the south half rises half a metre from the ground plane to the north hall's level, so the `wp` wedge at (10,12) that bridged the step goes with it. The teleporter's cell (10,3) stays `0` because the grid's rules want a teleporter on a void (the pathfinder warns otherwise); the museum lays floor over it through `museum.floor_cells: [[10,3]]`, a data-gated key added to both hall derivers on Astra's word (12 September) — the first and only map that uses it. `wall_height 3`, `gate_depth_rows 0` and the recovery's nineteen platforms kept.
2. *The primary, on the floor, in a cabinet.* `distribution_sampler:180#stand:cabinet` at (11,19): the front north, where a visitor stands at (11.5, 18.6), 0.7 m before the face, facing south. It had been `:180:0.5:1`, half a metre up.
3. *The cabinet (opt-in; R2 · R7 · R1).* `stand:cabinet` builds a body under the sampler, lifts the histogram to 0.95 m, carries the keypad and a second panel — BATCH · PAUSE / NEW SEED · BINS — outboard on a shoulder at 0.755 m (the cabinet is 1.6 m wide so nothing stands in front of the plate), houses a six-line readout under the histogram at 0.90 m (the law and its transform; landed, in flight and the cap; clipped draws and the counts in the two edge bins; bins, mean and deviation; the seed and what CLEAR does with it; the cadence and the batch), and draws behind every bar a ghost frame at the count the law expects for that bin at the present N, with the mass the clamp folds onto the edges counted in the edge bins — the bars and the ghosts are the same kind of thing, counts at this N, which is what makes equal-N comparison honest and keeps the bounded display from impersonating an unbounded law. Under the cabinet the sampler runs a named five-digit seed from a private generator, so CLEAR replays and a change of law sends the same stream through another transform. `stand:none` builds none of it.
4. *Bookkeeping on every path.* The landed values are kept (so BINS re-bins the same draws), the draws are counted, and `_fit` — the clamp the display always applied — counts what it folds. The returned values are unchanged.
5. *The arena's other bodies.* `random_bell_curve` to (3,17) at a third of its size (`:0:0:0.3`): a 6 m mound 1.5 m high in the south-west instead of a 20 m, 5 m mountain over the primary. `galton_board:180:0:1.5` at (9,20): it had been `:180:1:4` at (8,20), four times bench scale and a metre up, through the roof; one cell east because at (8,20) its seal took the door strip's column. `gaussian_random` from (10,18) to (10,15), out of the visitor's spot. The comparator, the sphere and the three north-hall applications as they were.
6. *Map authority.* `artifact_placement: "map"`, `sculpture_clear_rects [[9,16,13,21]]` (cells 9–12 × 16–20; the far edge is exclusive).

**Rejected.** A μ / σ control on the cabinet (the card keeps expansions for later; EXPON shows the fold without it). Rescaling the density curve to the bars (the card: "a theoretical curve and histogram are different evidence" — the ghosts carry the expectation at N; the curve keeps its scale and the final says so). A bins slider (BINS cycles three counts and re-bins; a slider would tempt continuous fiddling). Removing the Galton board or the comparator (honest company). Filling the teleporter's void in the structure (the grid wants it; the museum floors it by key instead).

**Runtime, 2026-09-11, 22:32–22:55, both lanes (Godot free; the runner refused none; nothing terminated).** A survey, four bare runs and two live runs led to **bare 100 checks / 0 failures, live 106 / 0, both exit 0 at quit, no script error, no signal error and no shutdown leak warning in either lane**, each a runtime finding:

1. *22:32 — the survey.* The void, the terrain over everything, the sunken keypad (above).
2. *22:45 — the first full run, six failures and a crash.* The mechanics held: BATCH, the laws at equal N, the ghosts summing to N, the clamp's count, CLEAR's replay, NEW SEED, PAUSE. What failed: the probe's in-flight count included the frame's landings (a landed bead stays in the list until the next sweep); the middle-six-bins threshold was the law's own expectation (150 of 300, half by the arithmetic); after BINS the rebuilt bars and ghosts kept their names for a frame beside the queued-free nodes, so `get_node` answered the old ones; the route cast down column 7 ran into the dark sphere at (7,10); the Galton board at (8,20) sealed the door strip's column and the museum slid it east to reopen its walk; the cabinet check assumed a visitor's spot nearer than 1.6 m. Then a stale reference to the rebuilt bars crashed the probe and the watchdog closed the run (exit 124). Fixes: the rebuilt nodes leave the tree synchronously, in flight counts unlanded beads, the middle eight bins against 188, the cast down column 6, the board to (9,20), the spot 0.7 m before the face.
3. *22:49 — 99 / 0, and two things the checks could not see.* The readout's first line hid under the cabinet cap's lip; the keypad's buttons went dark under `HangarKit.harmonize`. The plate lowered and taller, the keypad left its cream plate.
4. *22:52 — bare 100 / 0, live 106 / 0, and the eye's view.* From the visitor's eye the two panels on the shoulder stood in front of the plate's lower three lines, and the stand panel's labels were blank under harmonize. The cabinet widened to 1.6 m, the panels outboard at ±0.55, the plate 0.62 m centred, no harmonize (memory: [[readout-behind-the-panels]]).
5. *22:55 — final, green in both lanes*: bare 100 / 0, live 106 / 0, six lines and two labelled panels in the eye's capture.

**Measured (both lanes agree unless noted):** the sampler at (11.5, 0.00, 19.5), front north; the histogram on the cabinet top at 0.95 m; the keypad and the stand panel on the shoulder at 0.755 m; the readout at 0.90 m, its plate's top at 0.905 under the cap's lip at 0.914; reach from the eye at (11.5, 1.55, 18.6): GAUSS 1.17 m, BATCH 1.14, the readout 1.02, the histogram's middle 0.98. Seeds 81011 (bare) and 40483 (live). The rain: 49 / 54 landed in 1.10 / 1.06 s, 41 / 39 in flight, 278 / 280 drawn. GAUSS at 300: the tallest bar 30 / 33, the edge bins 0–1, nothing clipped, 0.28 expected per edge; UNIFORM: the tallest 19 / 18, ten expected everywhere; EXPON: the first bin 49 / 68, one clipped, 0.91 expected in the last bin; POISSON: even bins only, the tallest 55 / 61. CLEAR replays two hundred bin for bin; the stream does not; BINS round the cycle returns the same histogram and values. The museum: no walk severed, the Galton board's three cells sealed at (9,20), the plinth at (8,11), the mound's body stopping a capsule at 0.38–0.46 of 4 m across its cells, the cabinet's body stopping one at 0.33 of 1.5 m; the route 1.00 on every leg; no floor under (10,3). The Galton board's top 1.43 m at scale 1.5; the mound's 1.25 / 1.55 m at scale 0.3 (unseeded surface noise); gaussian_random at (10.5, 15.5). The shipped default: no cabinet, no ghosts, the stream, the keypad at −0.08 m, fifty draws binning as before. The streamer rebuilds the hall with the cabinet, thirty bins and a new seed. Live: UNIFORM, BATCH (0 → 100) and CLEAR (→ 0) through the pointer; the rig 2.5 m south in half a second, stopped by the cabinet at 0.72 m; the camera guard stopped; the front view by the rig's camera. Captures: `probe_gaussian{,_uniform,_poisson,_running,_display,_readout,_keypad,_north_door,_south_door,_arena,_plan}` in both lanes and `_desktop_front_live`. Engine 4.6-stable, exit 0 both, no errors, no leak.

**Verification, 2026-09-12, after Astra's word.** The museum lays floor over the teleporter's void through a new data-gated key, `museum.floor_cells: [[10,3]]`, read by both hall derivers (the museum's live re-read and `tools/em_map_halls.py`); the grid keeps its `0`. The book's `random gaussian` pearl names the sampler as its hero. Both lanes rerun on the result: bare 101 checks / 0 failures (exit 0 at 08:59:58, seed 90292), live 107 / 0 (exit 0 at 09:06:18, seed 72131); the floor check at (10,3) is a hard check now and passes.

**Pending:** a person at the keypad and the plate; a headset walk; Astra's review of the built room; Palle's calls on the mound's and the board's scales.

**2026-09-12 — the visual pass (Astra: "its six-line readout is small from the operating view and the separate summary board is hard to decipher").** A headline (font 21) over the six lines (font 11) on the same plate names the law, the landed N, the bins, the clipped count and the two edge bins — `UNIFORM · N 231 · 30 bins · clipped 0 · edges 6|11` — and the shipped side plate on its bracket (n, μ, σ from bin centres at font 12) is switched off under the cabinet, its numbers being the readout's fourth line. Live: 109 checks / 0 failures.
