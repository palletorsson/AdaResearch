# ML Classification — Artifacts
*Machine Learning: Algorithms That Learn · integration · 3 artifacts*

> Three algorithms. Three ways to carve space into regions where things belong. K-means drops centroids and iterates — points migrate toward their nearest center until the system settles, variance minimized, clusters stable. SVM finds the widest gap between classes, a hyperplane balanced on the few points that matter most. Random forest grows a hundred crooked trees, each one wrong in its own way, and lets them vote.

The map, read through what it holds — its artifacts in the order you meet them:

## Enhanced K-Means Clustering
![Enhanced K-Means Clustering](/scene-catalog/enhanced_kmeans.png)

Interactive K-means clustering visualization with adjustable k, colored cluster assignments, and centroid movement animation.

`enhanced_kmeans`

## Support Vector Machine Visualization
![Support Vector Machine Visualization](/scene-catalog/svm_visualization.png)

SVM hyperplane separator with support vectors highlighted. Shows maximum margin classification and kernel trick for non-linear boundaries.

`svm_visualization`

## Random Forest Visualization
![Random Forest Visualization](/scene-catalog/random_forest_visualization.png)

Visual representation of random forest ensemble — multiple decision trees voting on classification, showing how diversity improves accuracy.

`random_forest_visualization`
