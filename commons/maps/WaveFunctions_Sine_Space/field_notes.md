## 2026-09-10 — batch W1, hall 2 (doc/research/waves-chance-noise/NEXT_BATCH.md)

Ruling followed: the map is the placement authority; the recovered structure (museum.wall_height 3, gate_depth_rows 0, interior 2 = platform, perimeter w) kept cell for cell. No structure or utility cell moved; the basin and the one-cell bridge at x=10 (8d49b4d37, "five rooms made walkable") are the museum route and were left alone. `museum.artifact_placement: "map"` added; the stamp under `wavefunctions|sine space` is keyed to an old pearl name (live: `wavefunctions sine space`) and claims nothing either way.

Placement: the corridor stood at (7,3):90, spanning rows 2–4 across the doorway's cell, so a visitor entering at (7,1) met its north wall face-on and walked through it. Moved to (6,4):90: 8 m along the antechamber from x 2.5 to 10.5, rows 3–5. The west mouth is reached from the door along row 2 and the corner (1..2, 3..5); the east mouth opens at (10..11, 4..5), the head of the bridge. The direct museum route (door → row 2 east → x=10 → bridge) is untouched, and the corridor is a passage alongside it. sine_wall_explanation (2,5) → (1,2):90 and sine_space_explanation (9,5) → (11,2):-90, the antechamber's corners; their facing (rotation) is a guess at the cases' front and unverified. The basin bodies (sine_space, colorballs, dark_sphere) untouched.

Physical contract: "animated visible surfaces around a stable walk". The walls have no collider (the placed scene already disabled it); the museum's `_occupied_cells` treats a wide body with no collider as a venue and seals nothing, so the walk map is unchanged. With collision the whole 9×3-cell footprint would be sealed and the corridor could not stand across the antechamber without severing. The final says plainly that the wall does not collide, and the probe walks a capsule through both walls to prove it.

Artifact (SineWallCorridor.gd, additive; before-copy in doc/space/waves-chance-noise-w1-2026-09-10/before/): `panel` export (none|entrance, default none — every other placement and the DNA sweep unchanged), `amplitude_max` 0.45; RackTemplates panel at the west mouth's south corner (AMP, PHASE sliders; FREEZE, RESET buttons; `em_local_instrument` meta), a readout printing amplitude, offset, cycles per length, gap range and running/frozen; `set_amplitude`, `set_phase_offset`, `toggle_animation`, `reset_declared` (the placement's values and the handles); accessors `wall_displacements`, `gap_at`, `gap_range`, `contract`; the floor mesh lifted 12 mm above the deck; the scene's camera, sun and environment freed when embedded (the blue fill light stays). The displacement, the colour ramp, the fabric axis and the mesh builders are untouched.

Scene (sine_wall_corridor.tscn): rows 64 → 8 (the displacement does not vary with height, so the per-frame rebuild drops from ~51k to ~5.6k triangles), left wall frequency and amplitude multipliers 1.6/0.5 → 1.0/1.0 so PHASE compares two equal walls (R1: change one thing), animation_speed 0.05 → 0.35 rad/s so the phase advance is visible while standing (a crest every ~18 s), `panel = "entrance"`. Length 8, width 2, height 4, frequency 5.2, amplitude 0.2, collision off, animation on: as shipped.

Design decisions: (1) the passage runs along the antechamber and is entered from a mouth, not crossed through a wall; (2) equal walls, so the phase offset is a clean comparison with an exact reading at both ends of the slider: offset 0 → the walls agree, the gap breathes (1.43–2.57 m at amp 0.2 and phase 0; the extremes drift by ±0.02 m as the phase runs, because the three layers have different spatial frequencies); offset π → every sine flips sign, the right wall copies the left, the gap is 2.00 m everywhere (a snake); (3) the readout prints the gap range so "narrow" has a number; (4) no collider, said out loud, and made discoverable by walking into a crest.

Rejected: colliding walls (would seal the antechamber's route; a cheap 2-row trimesh was designed and set aside until a placement off the route exists); putting the corridor on the bridge as parapets (its collider would seal the bridge, and without a collider the visible walls would invite a step into the basin); `cut: ribs` for the placement (the plush skin is the bodily encounter; ribs is the comparison view from outside and stays a config option).

Exactness: the final quotes the layer line of `_wave_displacement` and the placement line of `_create_wall_mesh`, verbatim. "5.2 cycles over 8 m", "1.54 m", "0.35 rad/s", "~18 s" are arithmetic on the placed values; "1.4–2.6 m" and "2.00 m" are `gap_range()` at the two slider ends (the probe asserts them). The gap at the AMP maximum (0.73 m at phase 0; 0.72–0.78 m over a phase cycle, modelled in Python from the same three layers) is arithmetic and asserted (> 0.55 m).

Evidence: gdparse passes (script, probe); pathfinder OK 116/258 (unchanged); check_map_tokens OK. Runtime: `commons/testing/probe_wcn_sine_space.gd` written (real hand; placement, mouths, panel reach, PHASE/AMP one-variable checks, half-turn identity, FREEZE/RESET, five capsule routes and a walk through both walls, two captures) and NOT RUN — Godot PID 2780 held the user:// lock. Headset: not walked.

Supporting texts: intent, blurb, summary, tutorial rewritten (they described a walkable floor-wave corridor with levers, markers and presets that do not exist, and the final ended at a unit-circle room that is not on the route); technical.md corrected where it claimed touchable walls; critical.md given a closing section on visible versus colliding surfaces. Book line for sine_wall_corridor written; hero moved from sine_space_explanation to sine_wall_corridor to match the primary.


## Astra source review — 10 September 2026

Removed the duplicated running phase and the zero-amplitude colour division. The readout names local-X gap; constant x-gap is not shortest clearance. Running multilayer phase and walking a frozen wall are distinct transformations. Collision remains off. Both technical and critical texts are reconciled with the configured primary; book lines and citations follow the correction. These notes supersede conflicting exactness claims above. Original texts are preserved in `doc/space/wcn-w1-review-2026-09-10/before/`. Four scripts parse and 26 code excerpts match source. No Godot run or headset walk: PID 2780 remained active. Full review: `doc/book/handoffs/astra-wcn-w1-review-2026-09-10.md`.


## 2026-09-10 — first museum runs (Fable, after Astra's W1 review)

Runs: `probe_wcn_sine_space.gd`, rendered under the watchdog, seven times as faults were found and fixed. Godot 4.6 stable.

Observed and fixed:
- The corridor stood two metres INTO the floor: the museum's map-authored lane does not ground a body whose geometry is built in `_ready` (the corridor centred its meshes on its origin; the grid lane's auto-ground had hidden this). Fixed in the artifact: meshes and colliders are lifted by half the height, so the floor sits at the origin whatever lane placed it. The panel was at −0.93 m; it is at 1.07 m.
- Three `colorballs` (RigidBody3D balls) rolled out of the basin into the passage and onto the bridge and blocked four capsule walks. Removed from the hall.
- `sine_wall_explanation` at (1,2) sealed the corner cells and cut the west approach into a pocket (the museum's severance rule). Moved to the row-1 nook (1,1). `sine_space_explanation`'s display stands 4 m from its origin and 27 cm tall as placed (extent x 6.95–8.05 for an origin at 11.5, top at 0.12 m); it left the hall.
- A museum sculpture plinth stood inside the passage (first run) and at its east mouth (second). The passage floor now carries a thin collider slab (24 mm), so the walk map treats the passage as this body's ground and nothing is dealt into it; `museum.sculpture_clear_rects` [[1,1,11,6]] keeps plinths out of the antechamber. The walls stay collision-free.
- The 8 m corridor's east mouth stood in the bridge head's cell; 7 m and 4.5 cycles now (wavelength 1.56 m, still a step and a half). With the panel on the +X (north) side of the mouth, one row nearer the door, the museum scaled the whole corridor to 0.65 and turned it; on the −X side, in line with the south wall's end, it stands at scale 1. Recorded; the cause was not chased.
- The readout was pale text over a grey wall; it is on a dark plate now (legible at 1.6 m in the capture). Panel and plate sit above the 1 m platform cell (2,5), which reads as a table.

Observed and passing (57 checks, 0 failures): built at (6,4) rotation 90, mouths at x 3.0 and 10.0, walls 4 m, floor on the deck; the declared values reached the script; PHASE at half a turn gives x-gap 2.00 m everywhere and d_right = −d_left; at zero 1.45–2.56 m; AMP max leaves 0.75 m; AMP zero gives flat walls and finite colours; phase applied once (0.73 rad test); FREEZE holds, RESET restores values and handles; six capsule routes 1.0, including through both walls; the museum sealed the passage's cells and severed nothing. Captures: approach, inside (from the west mouth down the passage), panel (plate legible), bridge (the basin as a star void with the sine_space spheres rising through the bridge), overview from above the door.

Not observed: a tracked hand at the panel; the plush walls at 8 rows in a headset; the case's facing.


## 2026-09-10, evening — second runtime pass, under project startup (Fable, after Astra's runtime review)

Run: `bash tools/run_wcn_probe.sh sine live`. The same probe, ported to a Node by `tools/port_wcn_probes_live.py` (`probe_wcn_sine_space_live.gd`) and run as the project's main scene through `commons/testing/probe_live.tscn`, so TextManager, GameManager, XRToolsUserSettings and MapProgressionManager exist. 61 checks, 0 failures (`probe_sine_space_live.json`), no script errors in the engine log.

Actual desktop input, recorded apart from the emitted signals: the project's desktop rig (`commons/scenes/desktop_player.tscn`, driven by `commons/testing/wcn_desktop_driver.gd`) stood at segment-local (2.0, 0, 4.6), 0.9 m from the panel above the platform cell, looked at FREEZE and left-clicked through `Input.parse_input_event`. The rig's own pointer reported `Panel/Btn_0/InteractableAreaButton` under the crosshair and the walls stopped (`is_animating` true → false); a second click from the same spot set them running again. The rig then walked the passage on `ui_up` from x 3.3 to x 9.88 — 6.58 m in 80 physics frames, on the slab, through no wall, eye at 1.62 m. Captures drawn by the rig's camera: `probe_sine_space_desktop_mid_live.png` (mid-passage, looking east: the walls either side, the east mouth ahead) and `probe_sine_space_desktop_exit_live.png` (at the east mouth, turned south toward the bridge head: the wall column at x 12 on the left, the basin's void ahead). The corridor's readout plate reads `amp 0.20 m · phase offset 0.60 rad · 4.5 cycles … x-gap 1.46–2.48 m · running` from the visitor's standing distance (`probe_sine_space_panel_live.png`). The corner case at (1,1) keeps its checks (in the nook, top above 0.6 m).

Two harness faults found on the way, both of which had made the first live chain's pictures lie, both fixed in the driver: (1) the desktop rig, added under the tree root, took itself for a standalone launch and changed the scene to the Lab (`DesktopPlayer._ready`), freeing the museum under the probe — it is a child of the driver now; (2) the museum walker's camera carries a one-second guard that reclaims the view from any other camera (`endless_museum.gd`, `cam_guard`), so every capture "from the rig" had been the walker's view from the doorway — the driver stops that Timer when it spawns, and every capture now records which camera drew it (`current_camera` in the JSON). Headset: still pending.


## 2026-09-11, 07:37 — refreshed under the current probe sources (Fable, Astra's W2 completion brief, item 1)

`bash tools/run_wcn_probe.sh sine live`, Godot free: 61 checks, 0 failures. Why the rerun: after this room's 20:50 run the shared probe sources changed (the capture settle moved from the render server's `frame_post_draw` to a timer; the driver's pose log and the guard flag were added), so the evidence is regenerated from the sources that now stand; the results are the same as the second pass. Captures refreshed in place. Headset: pending.

**2026-09-12 — the visual pass (Astra: "the long readout line escapes its black plate at the approach… make the entrance/exit floor boundary clear").** The readout is four lines (amp and phase; cycles over length; the x-gap; running or frozen), none over 34 characters, on a 0.95 × 0.36 m plate at +0.64 m over the panel; the passage's two mouths carry a dark threshold strip across the floor. The panel keeps its place at the west mouth. Live: 64 checks / 0 failures.
