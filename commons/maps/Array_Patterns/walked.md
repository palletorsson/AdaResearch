# Array_Patterns — walked

> R-021/R-028: considered critical tutorial, ghost-drafted from the working map;
> Palle rules the voice. The walk (tutorial) woven with the turn (critical).

## The cast

pattern tiles · wallpaper-group analyzer · loom / punch-card reference

## The walk

You paint one small tile and the room does the rest: the tile **repeats**, reflected and rotated across a floor that builds itself from that single cell. This is the sequence's first claim, made before a single word about "data structures" — that an array is not a box you put things in, it is a machine for turning *one* into *many by rule*. Change the tile and the whole carpet re-derives; the wallpaper-group analyzer names what your hand discovered — p1, pm, p4m — and tells you there are exactly seventeen ways to tile a plane, no more, a completeness you can *feel* by trying to invent an eighteenth and failing.

## The turn (critical)

The chapter opens here, not with a spreadsheet, because arrays need rescuing from the idea that they are neutral storage — and the fastest way to do it is to point at a carpet and say: **that is an algorithm, and it is a thousand years older than the computer.** Every textile tradition on Earth found the seventeen symmetry groups by hand before mathematicians proved they were all of them; Jacquard's loom encoded pattern on punch cards a century before punch cards encoded programs. The array's real politics surface in the same breath: it does not store *anything* — it imposes regular spacing, integer indexing, discrete sampling, and calls that "just addressing." The handmade-to-computational bridge is the chapter's whole thesis, and it starts by refusing to let the grid pretend it is innocent.

## Room for improvement

*(Palle: this is the chapter's opening claim — carpet = algorithm. Note whether
the felt "one tile becomes a floor" moment carries it, or reads as decoration.)*
