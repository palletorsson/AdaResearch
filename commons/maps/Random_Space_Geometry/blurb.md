Geometry meets entropy. Two chambers — north and south — connected by a narrow spine. Random geometry sculpts space itself, turning the grid into an arena of unpredictable forms.

The `random_transformations_geometric` drives translation, rotation, and scale with stochastic parameters applied to mesh vertices and spatial regions. `sculpt_one` gives you a sculptural interface — randomness as creative tool rather than degenerative force. `env_one` frames the resulting geometry as environment rather than object. The sequence's pivot: randomness was property (a cube has random edges) and becomes medium (space itself is stochastic). When randomness shapes space rather than objects in space, the figure/ground distinction dissolves. Quantum field theory's vacuum is not empty but seething with random energy. This map makes that insight walkable.
