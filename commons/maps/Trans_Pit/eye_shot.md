# Eye shot — Trans_Pit

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **0 overlaps, 1 tight** — the law wants ≥1.2m to walk between.
- `[tight  ] pusher_block#axis:z#distance:3#speed:2#pause:1 <-> pusher_block#axis:-z#distance:3#speed:1.8#pause:1.2 gap +1.00m (centers 2.00m)`

## The move
        r, c = best_pos(a)
      File "C:\Users\palle\Documents\GitHub\AdaResearch_46\tools\placement_research.py", line 798, in best_pos
        for r in range(room.depth - d + 1):
    TypeError: 'float' object cannot be interpreted as an integer

no sibling kept — the move did not beat the ride (overlaps 0→0, tight 1→1). Note-only.

## The voice (qfep)
2 of 2 cast members carry a theory-claim; 0 mute.
- **grower_block** — Scale as pressure — the uncanny transformation that changes everything while appearing to change nothing. You 
- **pusher_block** — Translation as force — the simplest transformation becomes the most direct threat. The block does to the playe

## The text vs the space
walked.md exists — the writing names 2/2 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: pusher_block sit in clearance violations — the text promises what the floor obstructs.

## The heuristic understanding
The space already walks: the bodies keep the law without being told. What carries this map is its voice, not its floor.
