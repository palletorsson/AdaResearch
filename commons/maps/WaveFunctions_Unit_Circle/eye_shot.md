# Eye shot — WaveFunctions_Unit_Circle

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **0 overlaps, 17 tight** — the law wants ≥1.2m to walk between.
- `[tight  ] cube_scene:0:0             <-> cube_scene:0:3             gap +0.00m (centers 1.00m)`
- `[tight  ] cube_scene:0:0             <-> cube_scene:0:3             gap +1.00m (centers 2.00m)`
- `[tight  ] cube_scene:0:3             <-> cube_scene:0:3             gap +0.00m (centers 1.00m)`
- `[tight  ] cube_scene:0:3             <-> cube_scene:0:3             gap +1.00m (centers 2.00m)`
- `[tight  ] cube_scene:0:3             <-> cube_scene:0:3             gap +0.00m (centers 1.00m)`
- `[tight  ] cube_scene:0:3             <-> cube_scene:0:3             gap +1.00m (centers 2.00m)`
- `[tight  ] cube_scene:0:3             <-> cube_scene:0:3             gap +0.00m (centers 1.00m)`
- `[tight  ] cube_scene:0:3             <-> cube_scene:0:3             gap +1.00m (centers 2.00m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 1/1  mean Δ=+0.177
      walkability improved: 0/1  mean Δ=-0.140

sibling **Trial_eye_WaveFunctions_Unit_Circle** kept: overlaps 0→0, tight 17→2, pathfinder OK.

## The voice (qfep)
5 of 6 cast members carry a theory-claim; 1 mute.
- **OscillationCurve** — frequency — controls how many oscillations per unit length A sine wave is the shadow of uniform circular motio
- **SimpleOscillatingBridge** — oscillation_speed — controls how fast the wave propagates through platforms A traveling wave is identical osci
- **colorballs** — current_palette_index — switches the entire chromatic vocabulary; same physics, different visual language a co
- **cube_scene** — Transformation changes S while the cube serves as the invariant reference body. Because the base form is so le
- mute: unit_circle_advanced

## The text vs the space
**no walked.md** — the space stands unwritten; this note is the first text this map has.

## The heuristic understanding
The floor was fighting the walk — bodies inside each other's clearance. The mover found a better seating; the ride confirms it in text. The voice column above says what the room is FOR; the next writing pass should say it in the walked page.
