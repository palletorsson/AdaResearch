# Handoff — the endless museum pipeline, 2026-08-15

*Written for the next agent. Read `doc/spatial/CURRENT_STATE.md` for the
full session-by-session record and `doc/SPATIAL_PIPELINE.md` for doctrine;
this page is the ten-minute version of where things stand and what not to
touch. Branch `palm-scanner-door-entry`, pushed at `a0e8de807`.*

## The chain, and who owns what

```
spatial_contract.resolve(token)  →  body_m, posture, venue          [python]
spatial_floorplan.from_museum()  →  the tile as a plan (WALL_H from em_detail)
spatial_negotiation.negotiate()  →  Placement (slot / court / bay / REJECT)
spine_run.py spine --write-plan  →  ada_run/em_plan.json  (v2: plans[])
                                    ~10 MIN, isosurfaces alone 257 s — never
                                    wrap it in a short timeout; CHECK THE MTIME
export_museum_plan.py            →  rows in TILE coords (apron subtracted HERE)
endless_museum.gd                →  builds from the plan; TAB = editor      [gdscript]
  em/em_detail.gd    WALL_H 4.5 — THE ONE OWNER of height; everything derives
  em/em_lighting.gd  reads em_detail
  em/em_props.gd     reads em_detail; mount_defaults() + prop_wall_rules.json
  em/em_plinths.gd   viewing band; band() reads standing_rules.json
  em/em_editor.gd    stateless helpers; rulings → ada_run/em_overrides.json
```

**Authorship rule (non-negotiable):** the plan is the ONE author of placement.
Everything the hand does is a RULING recorded in a file and re-applied at
build where the thing is created — never a scene transform the builder does
not know about. Every fault this week was two authors holding one number.

## The three rulings files (all hand-authored, all gated)

| file | written by | read by | scope |
|---|---|---|---|
| `ada_run/em_overrides.json` | museum editor (TAB, F5) | `_deal_from_plan`, `_dress_props`, plinth stamp, `em_detail.dress_segment` | per-placement: move/turn/offset/scale/remove/add for artifacts; offset(+turn) for furniture, plinths, showings — `kind` field |
| `commons/data/prop_wall_rules.json` | prop corridor + in-situ editor | `em_props._ruled_y` | per-TOKEN wall mounting height |
| `commons/data/standing_rules.json` | prop corridor band zone | `em_plinths.band()` AND `spatial_contract.plinth_band()` | the viewing band (both languages) |

Delete any of them and the code convention returns. `em_overrides.json` was
deleted 08-15 by Palle's ruling (ten rulings idle after regen) — the next F5
mints it fresh. Not git-tracked.

## Scenes

- `commons/scenes/endless_museum.tscn` — desktop museum. `--em-plan` to walk
  the negotiated plan; TAB toggles the editor (or `--em-edit`).
- `commons/scenes/endless_museum_staged.tscn` — the same, wrapped for the
  shipped loop (menu → ∞ Endless Museum). VR builds building-only.
- `commons/scenes/prop_reference_wall.tscn` — the measurement corridor: 14
  ruled wall props, the 5-handle viewing band, 63 specimens in situ. Menu →
  ✎ Prop Corridor (desktop only), F10 back.
- `commons/scenes/vr_staging.tscn` — the shipped entry. `movement_only_hands`
  is DEFAULT ON (Palle's ruling): every loaded rig is locomotion-only.

## Gates — run before believing anything

```
godot --headless --path . --xr-mode off --script res://commons/testing/check_compile.gd -- --files=<a.gd,b.gd>
godot --headless --path . --xr-mode off --script res://commons/testing/test_em_editor.gd      # rulings round trip
godot --headless --path . --xr-mode off --script res://commons/testing/test_em_furniture.gd   # furniture/plinth/showing
godot --headless --path . --xr-mode off --script res://commons/testing/test_em_prop_insitu.gd
godot --headless --path . --xr-mode off --script res://commons/testing/test_prop_wall_rules.gd
godot --headless --path . --xr-mode off --script res://commons/testing/test_em_bays.gd
godot --headless --path . --xr-mode off --script res://commons/testing/test_em_vr_floor.gd
python tools/test_spatial_contract.py ; python tools/test_em_plan_chapters.py
python tools/godot_watchdog.py --expect=ada_run/em_autopilot.json -- godot --path . --xr-mode off --no-window res://commons/scenes/endless_museum.tscn -- --em-plan --em-autopilot=6
```
ONE Godot at a time (the second dies silently on the user:// lock). Editor
trials WRITE `em_overrides.json` — back it up first if a hand file exists.

## Where the corpus stands (regen 08-15 08:46)

placed 943 · interior 448 · courts 164 · rejected 213 · escalations **95**.
Rung 1 (bay) ships, gated, inert on this corpus (bodies too TALL, not too
wide). Rung 2 (turnable courts + joint budget per length) took escalations
114 → 95. The remaining 95 = the **32 worlds** (>40 m: env_one, AntColonyV2,
marchingcave…) + square bodies >12 m no turn helps — named by the refusal
text "a WORLD, not an exhibit — Palle's ruling". **That ruling is rung 3 and
it is Palle's, not an agent's.**

## Traps this week paid for (do not pay again)

1. `artifact_sizes.json` is a STALE MIRROR. Never cite it. Resolve the contract.
2. A regen cut by a timeout leaves the OLD plan; check `_spine_run.at`/mtime.
3. JSON hands ints back as floats — compare typed (`int(a) == int(b)`).
4. `seg.set_meta("em_chapter")` is set BEFORE the deal now; do not move it.
5. Heredoc-authored GDScript loses backslashes and `\n` — patch via a .py
   file on disk, or the Edit tool.
6. `git checkout -- base.tscn` restores a CLOBBERED rig if the clobber was
   committed — diff against the last known-good commit, not HEAD.
7. Another agent was live in this tree on 08-15 (em_autopilot.py 09:14,
   probe_interaction.gd 06:38). `git status --short <file>` before editing.
