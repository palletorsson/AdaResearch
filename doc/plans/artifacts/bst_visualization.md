# Artifact: Binary Search Tree

> Binary search tree with dynamic insertion maintaining the BST invariant: left child < parent < right child. Watch how ordered insertion creates degenerate trees while balanced insertion creates efficient ones.

## Context

**Category:** datastructures | **Complexity:** beginner
**Tags:** bst, binary_search_tree, search, ordered, tree, data_structure | **Themes:** algorithms, data_structures

Binary search tree with dynamic insertion maintaining the BST invariant: left child < parent < right child. Watch how ordered insertion creates degenerate trees while balanced insertion creates efficient ones.

## Design

- **Visual:** material: standard, animation: transform

## Architecture

| | |
|---|---|
| **File** | `algorithms/datastructures/binarysearchtree/bst_visualization.gd` (376 lines) |
| **Scene** | `res://algorithms/datastructures/binarysearchtree/bst_visualization.tscn` |
| **Registry** | `datastructures.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `auto_insert_values` | bool | true |
| `initial_values` | Array[int] | [50, 30, 70, 20, 40, 60, 80] |
| `insertion_speed` | float | 1.0 |
| `show_traversal` | bool | true |
| `node_radius` | float | 0.5 |
| `level_spacing` | float | 3.0 |
| `horizontal_spacing` | float | 2.0 |
| `show_connections` | bool | true |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `setup_environment()` -- Lighting
- `queue_insert(value: int)`
- `queue_delete(value: int)`
- `queue_traversal(type: String)`
- `execute_current_operation()`
- `insert_value(value: int)`
- `insert_recursive(node: TreeNode, new_node: TreeNode)`
- `delete_value(value: int)`
- `delete_recursive(node: TreeNode, value: int)`
- `find_min(node: TreeNode)`
- `create_node_visual(node: TreeNode)` -- Create sphere mesh
- `update_node_visual(node: TreeNode)`
- `calculate_positions()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- datastructures

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
