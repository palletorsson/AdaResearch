# Artifact: L-System Tree

> F → F[+F]F[-F]F at 25.7° — the classic Lindenmayer plant in one rule

## Context

**Category:** procedural | **Complexity:** advanced
**Tags:** l_system, procedural, simulation, tree | **Themes:** education, grammar, l_systems, procedural

Classic deterministic L-system tree — the foundational teaching example showing how a simple grammar rule produces organic branching structure.

*One rule, one angle, one axiom — a tree grows.*

## Design

- **Visual:** material: vertex_color, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To stand as the canonical L-system tree — the simplest grammar that produces a convincing plant
- **Critical Parameter:** base_angle (25.7°) — Lindenmayer's original angle, balancing spread and density perfectly
- **What Emerges:** Branching depth creates a brown-to-green gradient — trunk becomes canopy through recursion alone
- **Triggers:** Changing iterations → sapling to forest giant; adjusting angle → palm to pine to bush

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/lsystem_tree/lsystem_tree.gd` (241 lines) |
| **Scene** | `res://commons/artifacts/lsystem_tree/lsystem_tree.tscn` |
| **Registry** | `lsystems.json` |
| **Class** | `LSystemTree` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `display_size` | float | 0.6 |
| `trunk_color` | Color | Color(0.45, 0.28, 0.12) |
| `tip_color` | Color | Color(0.2, 0.65, 0.15) |
| `iterations` | int | 4 |
| `base_angle` | float | 25.7 |
| `base_length` | float | 0.1 |

### Key Methods

- `_ready()`
- `_create_display()`
- `_create_base()`
- `_create_label()`
- `_generate()` -- Rewrite axiom through iterations
- `_draw_tree()`
- `_update_label()`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `base_angle`, `base_length`, `display_size`, `iterations`

## Curriculum Position

### Sequences

- **L-Systems: Rules Grow Structure** (lsystems) -- map: LSystems_Grammar_Lab
- **L-Systems: Rules Grow Structure** (lsystems) -- map: LSystems_Growth
- **L-Systems: Rules Grow Structure** (lsystems) -- map: LSystems_Living
- **Procedural Generation (All Maps)** (proceduralgeneration_all) -- map: ProceduralGeneration_Five

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| LSystems_Grammar_Lab | [5,2] | `-` |
| LSystems_Growth | [5,4] | `-` |
| LSystems_Living | [2,5] | `-` |
| LSystems_Tree_L_Systems | [8,3] | `-` |
| ProceduralGeneration_Five | [6,5] | `-` |

### Relationships

- Appears in LSystems_Grammar_Lab, LSystems_Growth, and LSystems_Living. The reference tree for the whole sequence.
- Needs: VR iteration/angle controls [missing], Label3D [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
