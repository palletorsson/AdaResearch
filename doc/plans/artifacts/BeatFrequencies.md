# Artifact: Beat Frequencies

> When two waves of slightly different frequency combine, they create a pulsing envelope—the 'beat'. This is how piano tuners work, how interference patterns form, how waves negotiate difference.

## Context

**Category:** waves | **Complexity:** advanced
**Tags:** sound, waves | **Themes:** oscillation, physics, waves

When two waves of slightly different frequency combine, they create a pulsing envelope—the 'beat'. This is how piano tuners work, how interference patterns form, how waves negotiate difference.

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle
- **Scale:** [3.0, 1.0, 1.0] (room_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/beat_frequencies/BeatFrequencies.gd` (428 lines) |
| **Scene** | `res://algorithms/wavefunctions/beat_frequencies/BeatFrequencies.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural_mesh |

### Key Methods

- `_ready()`
- `_setup_audio()`
- `_setup_controls()`
- `_create_visualizations()` -- Create container nodes
- `_create_wave_multimesh()`
- `_init_wave_positions(mm: MultiMesh, y_base: float)`
- `_create_beat_envelope_multimesh()` -- Create bar mesh for envelope visualization
- `_create_labels()`
- `_update_frequency_labels()`
- `_on_slider1_changed(value: float)`
- `_on_slider2_changed(value: float)`
- `_on_toggle_pressed()`
- `_process(delta: float)`
- `_generate_audio_samples()`
- `_update_waveform_visualization()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Beat_Frequencies | [2,2] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
