# Artifact: CA Chair Test

> strategy.step(grid) where strategy in {rule_switching, memory, gradient, pure_ca, downward_columns}

## Context

**Category:** emergence | **Complexity:** beginner
**Tags:** cellular_automata, emergence | **Themes:** biology, cellular_automata, computation, emergence

Cellular Automata Chair Test.

*A chair is a convergent form — different rules find it because sitting demands it.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [11.0, 1.0, 31.0] (world_scale)
- **Interaction:** To grow furniture from rules — watch a chair emerge from a seed through five different CA strategies
- **Critical Parameter:** strategy_type — each of five strategies produces fundamentally different growth morphologies
- **What Emerges:** Chair-like forms from volumetric CA — no furniture geometry was specified, only growth rules
- **Triggers:** Switching strategy → same seed, completely different chair; 30 generations → growth halts, form is final

## Architecture

| | |
|---|---|
| **File** | `algorithms/cellularautomata/CAchairtests/Main.gd` (170 lines) |
| **Scene** | `res://algorithms/cellularautomata/CAchairtests/Main.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | procedural |

### Dependencies

- `res://algorithms/cellularautomata/CAchairtests/CAStrategy.gd`

### Key Methods

- `_ready()`
- `setup_camera()`
- `setup_ui()`
- `reset_simulation()`
- `_process(delta: float)`
- `step_simulation()`
- `update_ui()`
- `handle_camera(delta)`
- `update_camera_position()`
- `_unhandled_input(event: InputEvent)`
- `_on_reset_pressed()`
- `_on_step_pressed()`
- `_on_play_pressed()`
- `_on_strategy_pressed()`
- `_on_speed_changed(value: float)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Cellular Automata: Local Rules, Global Patterns** (cellularautomata) -- map: CA_ExpandingSpace

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CA_ExpandingSpace | [1,3] | `-` |

### Relationships

- Feeds into CA_ExpandingSpace. Demonstrates that CA rules can be design tools, not just simulations.
- Needs: VR buttons [has via UI], speed slider [has], strategy switcher [has], Label3D [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
