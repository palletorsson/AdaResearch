# Artifact: Transforming Pattern

> wave_superposition(sin(t+x*a+z*b), cos(t*c+x*d-z*e)) -> height, scale, hue per cell

## Context

**Category:** qfep | **Complexity:** intermediate
**Tags:** pattern, qfep, wave | **Themes:** philosophy, qfep, theory

Constantly evolving wave pattern. Queer φ>0 signature.

*positive phi means the pattern itself is in motion — transformation is not corruption but the signature of life*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** watch a grid that never settles — every cell dancing to its own superposition of waves
- **Critical Parameter:** transform_speed — how fast the waves evolve, how quickly the pattern forgets itself
- **What Emerges:** traveling wave fronts and interference patterns that no single wave produces alone
- **Triggers:** _process continuously updates height, scale, color, and rotation of every cell from wave functions

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/transforming_pattern.gd` (107 lines) |
| **Scene** | `res://commons/interfaces/qfep/transforming_pattern.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `QFEPTransformingPattern` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | int | 6 |
| `cell_size` | float | 0.06 |
| `transform_speed` | float | 0.8 |
| `pattern_color` | Color | Color(1.0, 0.8, 0.2, 1.0) |

### Key Methods

- `_ready()`
- `_build_pattern()`
- `_process(delta: float)`

## Curriculum Position

### Sequences

- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Phi_Term

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| QFEP_Phi_Term | [6,7] | `:0:0.5` |

### Relationships

- contrasts preserved_pattern (phi<0 frozen vs phi>0 flowing); paired with fluid_form; represents positive phi
- Needs: VR speed control [missing], wave parameter adjustment [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
