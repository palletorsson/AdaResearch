# Artifact: ComputerVision_VR

> output(x,y) = Σ kernel(i,j) · image(x+i, y+j) — convolution as local pattern matching

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** machine_learning | **Themes:** ai, machine_learning

Interactive image processing with spatial convolution filters

*every act of perception is a convolution — a commitment to which local patterns matter*

## Design

- **Visual:** material: generated_texture, animation: physics
- **Scale:** [4, 5, 2] (large)
- **Interaction:** slide a kernel across an image with your hands, watch edges and features materialize
- **Critical Parameter:** kernel weights — Sobel detects edges, Gaussian blurs, Sharpen enhances
- **What Emerges:** the realization that "seeing" is not passive — it requires a choice of what to look for
- **Triggers:** painting pixels changes the input image; applying different filters reveals different truths about the same data

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/computer_vision/ComputerVision_VR.gd` (631 lines) |
| **Scene** | `res://algorithms/machinelearning/computer_vision/ComputerVision_VR.tscn` |
| **Registry** | `machinelearning.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `image_canvas_size` | float | 4.0 |
| `pixel_size` | float | 0.2 |
| `filter_size` | float | 0.3 |
| `feature_map_spacing` | float | 1.5 |
| `image_resolution` | int | 16 |
| `num_feature_maps` | int | 4 |
| `edge_threshold` | float | 0.5 |
| `show_sobel_filter` | bool | true |
| `show_gaussian_blur` | bool | true |
| `show_sharpen_filter` | bool | true |
| `animate_convolution` | bool | true |
| `enable_pixel_painting` | bool | true |
| `enable_filter_grabbing` | bool | true |
| `enable_object_detection` | bool | true |
| `show_sliding_kernel` | bool | true |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_initialize_image_data()`
- `_create_input_canvas()`
- `_create_pixel(x: int, y: int, value: float)`
- `_create_filter_toolkit()`
- `_create_filter_object(filter_data: Dictionary, pos: Vector3)`
- `_create_edge_detection_area()`
- `_create_feature_extraction_area()`
- `_create_object_detection_area()`
- `_create_bounding_box(data: Dictionary)`
- `_create_convolution_visualizer()`
- `_create_control_panel()`
- `_create_info_panels()` -- Convolution explanation
- `_create_info_panel(pos: Vector3, text: String, color: Color)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| MachineLearning_Explainable_AI_XAI | [11,6] | `-` |
| ML_Perception | [1,5] | `:0:0:0.6` |

### Relationships

- unlocks convolutional_neural_networks_cnns_vr (learned kernels vs hand-designed); depends on neural_networks_vr (feature hierarchy)
- Needs: VR pixel painting [has conceptual], grabbable filter kernels [has], sliding kernel animation [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
