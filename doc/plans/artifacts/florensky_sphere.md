# Artifact: Florensky Sphere

> A ∧ ¬A — paraconsistent logic; truth that holds contradiction without triviality

## Context

**Category:** philosophy | **Complexity:** expert
**Tags:** florensky, paraconsistent_logic, superposition, quantum, interactive | **Themes:** philosophy, qfep

Paraconsistent logic sphere - holds A and not-A simultaneously without collapse

*A and not-A can both be true — this is not a failure of logic but a description of reality that classical logic cannot contain*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [2, 2, 2] (compact)
- **Interaction:** observe the sphere oscillating between blue (A) and red (¬A), collapsing on interaction then returning to superposition
- **Critical Parameter:** current_state — BOTH is the natural state; observation collapses it temporarily
- **What Emerges:** the experience that contradiction is not destruction — it is a mode of being
- **Triggers:** click/interact collapses superposition to A or ¬A; timer returns to BOTH after 2 seconds; right-click cycles all four states

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/foundations/florensky_sphere.gd` (299 lines) |
| **Scene** | `res://commons/interfaces/foundations/florensky_sphere.tscn` |
| **Registry** | `foundations.json` |
| **Class** | `FlorenskySphere` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `radius` | float | 0.3 |
| `detail` | int | 32 |
| `color_A` | Color | Color(0.2, 0.4, 1.0, 0.8) |
| `color_not_A` | Color | Color(1.0, 0.3, 0.2, 0.8) |
| `color_both` | Color | Color(0.8, 0.3, 1.0, 0.8) |
| `current_state` | LogicState | LogicState.BOTH |
| `allow_observation` | bool | true |

### Key Methods

- `_ready()`
- `_create_sphere()`
- `_create_inner_sphere()`
- `_create_glow()`
- `_create_labels()` -- Title
- `_create_interactable()`
- `_on_input_event(_camera: Node, event: InputEvent, _position: Vector3, _normal: Vector3, _shape_idx: int)`
- `_on_hover_start()`
- `_on_hover_end()`
- `_process(delta: float)`
- `_animate_superposition()` -- Oscillate between A and not-A colors
- `_animate_collapsed()`
- `_animate_void()`
- `_update_visual_state()`
- `observe()`

### Signals

- `state_observed`
- `superposition_entered`
- `superposition_collapsed`

## Curriculum Position

### Sequences

- **Foundations Crisis: The Limits of Formalization** (foundationscrisis) -- map: Florensky_Paraconsistent
- **Foundations Crisis: The Limits of Formalization** (foundationscrisis) -- map: Crisis_Synthesis

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Crisis_Synthesis | [13,12] | `:0:0.8` |
| Florensky_Paraconsistent | [5,6] | `:0:1:1` |

### Relationships

- contrasts schrodinger_box (quantum vs logical superposition); contrasts russell_set_box (contradiction as paradox vs contradiction as truth); unlocks qfep_formula_3d (the QFEP oscillation IS this logic)
- Needs: VR area interaction [has], mouse click [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
