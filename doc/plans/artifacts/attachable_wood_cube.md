# Artifact: Attachable Wood Cube

> Scalable wood cube with attachment points on all faces. Pieces snap together when attachment points align and face each other.

## Context

**Category:** primitives | **Complexity:** beginner
**Tags:** primitives | **Themes:** decoration, environment

Scalable wood cube with attachment points on all faces. Pieces snap together when attachment points align and face each other.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/cubes/grab_cube_attachable.gd` (158 lines) |
| **Scene** | `res://commons/primitives/cubes/grab_cube_attachable.tscn` |
| **Registry** | `furniture.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `attachment_faces` | int | 63 |

### Dependencies

- `res://commons/primitives/furniture/attachment_point.tscn`

### Key Methods

- `_ready()`
- `_create_attachment_points()`
- `_get_face_name(face_dir: int)`
- `_update_attachment_position(point: Node)`
- `_update_all_attachment_positions()`
- `_on_scale_changed(_new_scale: float, _scale_index: int)`
- `_on_axis_scale_changed(_axis: String, _new_scale: float, _scale_index: int)`
- `_on_attachment_connected(other_point: Node, face_dir: int)`
- `_on_attachment_disconnected(other_point: Node, face_dir: int)`
- `get_attachment_point(face_dir: int)`
- `is_face_connected(face_dir: int)`
- `get_connected_faces()`
- `get_attached_objects()`

### Signals

- `attachment_connected`
- `attachment_disconnected`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
