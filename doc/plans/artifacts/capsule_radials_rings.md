# Artifact: Capsule Radials Rings

> CapsuleMesh(radial_segments, rings, height, radius) — cylinder with hemispherical caps

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

Configurable capsule with radial segments and rings. Usage: capsule_radials_rings:90:1:#radial:6#rings:3 for 6 radial segments and 3 rings.

*the capsule is the physicist's preferred solid — its hemispherical ends are differentiable at all points*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** learner understands the capsule as a cylinder that has been made "safe" at its ends
- **Critical Parameter:** radial_segments — controls how round the cylinder cross-section appears
- **What Emerges:** the capsule as the collision primitive of choice — smooth ends prevent snagging in physics
- **Triggers:** grid config syntax (capsule_radials_rings#config:8:4) or apply_grid_config() call at runtime

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/capsule/capsule_radials_rings.gd` (117 lines) |
| **Scene** | `res://commons/primitives/capsule/capsule_radials_rings.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `base_color` | Color | Color(0.4, 0.8, 0.6) |
| `radial_segments` | int | 8 |
| `rings` | int | 4 |
| `height` | float | 1.0 |
| `radius` | float | 0.25 |

### Dependencies

- `res://commons/primitives/shared/grid_material_factory.gd`

### Key Methods

- `_ready()` -- Check for config metadata set by grid system
- `_parse_config_string(config_str: String)`
- `apply_grid_config(config_data: Dictionary)`
- `_build_capsule()` -- Clean up existing mesh
- `_create_collision()` -- Remove existing collision
- `set_base_color(color: Color)`
- `set_capsule_params(new_radial: int = -1, new_rings: int = -1, new_height: float = -1, new_radius: float = -1)`

### Grid Config

Accepts: `config`, `height`, `radial`, `radius`, `rings`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Primitives_Ignorance

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Primitives_Ignorance | [23,2] | `:90:1:#radial:3#rings:3` |
| Primitives_Ignorance | [23,3] | `:90:1:#radial:7#rings:3` |
| Primitives_Ignorance | [23,5] | `:90:1:#radial:7#rings:3` |
| Primitives_Ignorance | [23,6] | `:90:1:#radial:3#rings:3` |

### Relationships

- sibling to torus_radials_rings in the configurable mesh family; both support #config syntax
- Needs: [missing VR controls — configured via map data; no live slider]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
