# Artifact: Vector Subtraction

> A - B = A + (-B). Subtraction is addition of the opposite. The difference vector points from B's tip to A's tip.

## Context

**Category:** mathematics | **Complexity:** beginner
**Tags:** vectors, subtraction, direction, interactive | **Themes:** vectors

Interactive vector subtraction: A - B = C. Shows how subtraction finds the difference vector between two points.

*The difference between two positions is a direction. Subtraction creates the arrow that connects them.*

## Design

- **Visual:** material: standard, animation: particle_lifecycle
- **Scale:** [3, 3, 4] (xlarge)
- **Interaction:** To show that subtraction is not removal — it is the directed gap between two positions.
- **Critical Parameter:** The relationship between vector_a and vector_b. When equal, the result is zero — the gap closes.
- **What Emerges:** The ghost arrow -B appearing at A's tip makes the tip-to-tail construction visible. Subtracting identical vectors yields zero — visually collapsing to a point.
- **Triggers:** Handle drag → all five arrows update (A, B, -B, -B from tip, result), preset buttons → orthogonal/same/opposing configurations

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/vector_subtraction_demo/vector_subtraction_demo.gd` (509 lines) |
| **Scene** | `res://commons/artifacts/vector_subtraction_demo/vector_subtraction_demo.tscn` |
| **Registry** | `vectors.json` |
| **Class** | `VectorSubtractionDemo` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_vector_length` | float | 1.2 |
| `arrow_thickness` | float | 0.006 |
| `vector_a` | Vector3 | Vector3(0.8, 0.4, -0.1): |
| `vector_b` | Vector3 | Vector3(0.2, 0.7, 0.3): |
| `color_a` | Color | Color(1.0, 0.3, 0.3) |
| `color_b` | Color | Color(0.3, 0.5, 1.0) |
| `color_neg_b` | Color | Color(0.5, 0.7, 1.0, 0.6) |
| `color_result` | Color | Color(0.3, 1.0, 0.4) |
| `panel_color` | Color | Color(0.06, 0.06, 0.08, 0.9) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_add_tracked_child(node: Node)`
- `_create_text_panel(panel_name: String, text: String, pos: Vector3, 
		size: Vector2 = Vector2(0.3, 0.08)`
- `_add_panel_frame(panel: Node3D, size: Vector2)`
- `_get_panel_label(panel: Node3D)`
- `_ready()`
- `_init_shared_materials()`
- `_create_base()` -- Ground plane
- `_create_coordinate_axes()`
- `_create_axis_line(direction: Vector3, color: Color, label_text: String)`
- `_create_vector_labels()`
- `_create_vector_label(text: String, color: Color)`
- `_create_arrows()`
- `_create_arrow(arrow_name: String, color: Color, ghost: bool = false)`
- `_create_handles()` -- Grabbable handle for vector A tip
- `_create_handle(handle_name: String, color: Color)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: VectorFoundations

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| VectorFoundations | [11,18] | `-` |
| Vectors_Intro | [1,3] | `-` |
| VectorSubtraction | [3,4] | `-` |

### Relationships

- Inverse of vector_addition_demo. Prerequisite for hl_turret_vectors (direction = target - turret is subtraction).
- Needs: VR grabbable handles [has], preset buttons [has]. Missing: animation showing the construction step by step.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
