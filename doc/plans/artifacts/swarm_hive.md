# Artifact: Swarm Hive

> hive.spawn(boid) -> flock(separation, alignment, cohesion) -- stationary hive releasing swarms

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** swarm, boids, flocking, emergence | **Themes:** gameplay, hazards

Stationary hive spawning boid particles that follow separation/alignment/cohesion rules plus player-seeking. Emergent collective behavior from simple local rules. Destroy the hive to disperse the swarm.

*separation, alignment, cohesion -- three rules, and the swarm organizes itself.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** a hive spawning boid particles following three flocking rules -- the swarm IS the weapon
- **Critical Parameter:** boid rules (separation, alignment, cohesion) / spawn_timer -- three rules generate all behavior
- **What Emerges:** three rules produce flocking -- separation prevents collision, alignment creates direction, cohesion mass
- **Triggers:** spawn_timer releases boids; boids flock using three rules; player proximity attracts swarm

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/swarm_hive/swarm_hive.gd` (298 lines) |
| **Scene** | `res://commons/hazards/swarm_hive/swarm_hive.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `SwarmHive` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_boids` | int | 20 |
| `spawn_interval` | float | 0.3 |
| `boid_speed` | float | 3.0 |
| `boid_damage` | float | 5.0 |
| `separation_radius` | float | 0.5 |
| `alignment_radius` | float | 1.5 |
| `cohesion_radius` | float | 2.5 |
| `separation_weight` | float | 2.0 |
| `alignment_weight` | float | 1.0 |
| `cohesion_weight` | float | 1.0 |
| `player_seek_weight` | float | 1.5 |
| `detection_radius` | float | 8.0 |
| `hive_color` | Color | Color(0.6, 0.45, 0.15) |
| `boid_color` | Color | Color(0.9, 0.7, 0.1) |
| `emission_color` | Color | Color(1.0, 0.8, 0.2) |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_create_materials()`
- `_build_hive()` -- Hexagonal prism hive body
- `_spawn_boid()`
- `_update_boids(delta: float)`
- `take_damage(amount: float)`
- `apply_damage(amount: float)`
- `damage(amount: float)`
- `_apply_damage(amount: float)`
- `_find_player()`
- `configure(config: Dictionary)`
- `apply_grid_config(config: Dictionary)`

### Signals

- `enemy_destroyed`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_5

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_5 | [7,9] | `-` |

### Relationships

- embodies swarmintelligence sequence; three rules that generate coordinated group behavior
- Needs: player tracking [has]; boid array [has]; flocking rules [has]; hive health [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
