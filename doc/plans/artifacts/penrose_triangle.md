# Artifact: penrose_triangle

> 3 bars, 3 right angles, each corner coherent, the whole impossible -- visual Goedel in 3D

## Context

**Category:** philosophy | **Complexity:** intermediate
**Tags:** penrose, impossible, illusion, escher, interactive | **Themes:** philosophy, qfep

The impossible triangle - locally coherent, globally impossible. Visual Gödel.

*each corner is valid, the object is not -- incompleteness you can hold in your hands.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** a triangle that looks correct from one viewpoint but is geometrically impossible in 3-space
- **Critical Parameter:** _is_at_sweet_spot -- whether the viewer stands at the one angle where illusion coheres
- **What Emerges:** local validity does not guarantee global consistency -- the triangle knows what formal systems learned
- **Triggers:** player position relative to sweet spot; indicator shows when the impossible view aligns

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/penrose_triangle/penrose_triangle.gd` (150 lines) |
| **Scene** | `res://commons/artifacts/penrose_triangle/penrose_triangle.tscn` |
| **Registry** | `foundations.json` |
| **Class** | `PenroseTriangle` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `size` | float | 0.5 |
| `bar_thickness` | float | 0.08 |
| `auto_rotate` | bool | false |
| `rotation_speed` | float | 0.2 |
| `show_sweet_spot` | bool | true |

### Key Methods

- `_ready()`
- `_create_impossible_triangle()` -- Penrose triangle: three beams along the edges of an equilateral triangle.
- `_add_beam(from: Vector3, to: Vector3, mat: StandardMaterial3D)`
- `_create_info_label()`
- `_create_sweet_spot_indicator()`
- `_process(delta)`
- `check_viewer_position(viewer_pos: Vector3)` -- Check if viewer is near the sweet spot where illusion works
- `apply_grid_config(config_data: Dictionary)`

### Signals

- `viewpoint_locked`
- `illusion_revealed`

### Grid Config


## Curriculum Position

### Sequences

- **Art & Mathematics: Visual Philosophy** (artmathematics) -- map: Escher_Impossible

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Escher_Impossible | [7,10] | `:0:0.8` |

### Relationships

- paired with escher_staircase (impossible objects family); feeds Dark_Room_Paradox
- Needs: bar meshes [has]; sweet spot detection [has]; info label [has]; VR viewpoint tracking [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
