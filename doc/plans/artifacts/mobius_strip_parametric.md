# Artifact: Möbius Strip

> The iconic one-sided surface: a strip with a half-twist. Walk along it and you return to where you started but on the 'other side' — except there is no other side.

## Context

**Category:** geometry | **Complexity:** beginner
**Tags:** parametric, topology, non_orientable, mobius, one_sided, pickable | **Themes:** curves, math, parametric

The iconic one-sided surface: a strip with a half-twist. Walk along it and you return to where you started but on the 'other side' — except there is no other side.

## Design

- **Visual:** material: shader, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/parametric/mobius_strip.gd` (128 lines) |
| **Scene** | `res://commons/primitives/parametric/mobius_strip.tscn` |
| **Registry** | `parametric.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `u_min` | float | 0.0 |
| `u_max` | float | 6.28 |
| `u_steps` | int | 64 |
| `v_min` | float | -1.0 |
| `v_max` | float | 1.0 |
| `v_steps` | int | 16 |
| `radius` | float | 1.0 |
| `width` | float | 0.5 |
| `base_color` | Color | Color(0.9, 0.3, 0.7) |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `create_parametric_surface()`
- `apply_material()`
- `create_collision()` -- Add collision shape for XRToolsPickable

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
