# Artifact: WFC Tile Mosaic

> Floor panel showing a pre-generated Wave Function Collapse output — a grid of tiles where each cell respects adjacency constraints. Constraint propagation made visible.

## Context

**Category:** showcase | **Complexity:** advanced
**Tags:** wfc, wave_function_collapse, constraint, tiling, mosaic, procedural | **Themes:** arrays, biology, data_structures

Floor panel showing a pre-generated Wave Function Collapse output — a grid of tiles where each cell respects adjacency constraints. Constraint propagation made visible.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/wfc_tile_mosaic/wfc_tile_mosaic.gd` (356 lines) |
| **Scene** | `res://commons/artifacts/wfc_tile_mosaic/wfc_tile_mosaic.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `WfcTileMosaic` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `mosaic_world_size` | Vector2 | Vector2(0.8, 0.8) |
| `grid_size` | int | 16 |
| `pixel_scale` | int | 4 |

### Key Methods

- `_ready()`
- `_create_mosaic_mesh()`
- `_generate_wfc()`
- `_solve_wfc()` -- Each cell holds an array of possible tile types (superposition).
- `_attempt_solve()`
- `_propagate(possibilities: Array, collapsed: Array)` -- Iteratively remove impossible tiles based on neighbor constraints
- `_is_compatible(tile: int, x: int, y: int, possibilities: Array, collapsed: Array)` -- up
- `_backtrack(history: Array, possibilities: Array, collapsed: Array)`
- `_count_remaining(collapsed: Array)`
- `_deep_copy_possibilities(p: Array)`
- `_deep_copy_collapsed(c: Array)`
- `_fallback_grid()`
- `_render_grid(grid: Array)`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `grid_size`, `pixel_scale`

## Curriculum Position

### Sequences

- **Mosaic Analysis: Grid Logic in Stone and Glass** (mosaicanalysis) -- map: Mosaic_WFC_Reconstruction

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Mosaic_WFC_Reconstruction | [1,1] | `-` |
| Pattern_Maker_Showcase | [2,0] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
