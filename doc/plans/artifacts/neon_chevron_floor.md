# Artifact: Neon Chevron Floor

> a floor encoding directional energy through repeating V-shapes

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pixel_art, chevron, neon | **Themes:** arrays, art, cultural_computation

Procedural pixel art floor with bold V-shaped chevron stripes radiating from center. Hot pink, electric blue, yellow, and purple bands alternate with black gaps for a rainbow V pattern.

*the chevron is a distance function visualized — math made visible as pattern*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see bold chevrons pulsing outward from the center
- **Critical Parameter:** tiles_short — pixel resolution on the short axis
- **What Emerges:** the insight that a V-shape is just abs(x - center) mapped to color bands
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/neon_chevron_floor.gd` (172 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/neon_chevron_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `NeonChevronFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.2, 0.9) |
| `tiles_short` | int | 32 |
| `border_width` | int | 1 |

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_create_neon_material(color: Color, emission: bool = false)`
- `_build()`

### Grid Config

Accepts: `floor_size`, `tiles_short`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- pixel_invader_floor (aesthetic sibling), rainbow_checker_floor (color cousin)
- Needs: [implemented] procedural mesh, chevron field, color cycling

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
