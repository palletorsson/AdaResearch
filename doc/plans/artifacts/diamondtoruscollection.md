# Artifact: diamondtoruscollection

> diamonds[i].position = (cos(2πi/n)*r, -cylinder_length, sin(2πi/n)*r) — radial distribution on a torus

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** geometry | **Themes:** algorithms, misc

Torus reference ring with hanging cylinders and colored diamonds.

*a torus is a circle of circles — its symmetry group allows equal radial distribution of any count*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [5.0, 1.0, 5.0] (compact)
- **Interaction:** learner feels the torus as a path — walking around it reveals equally-spaced hanging elements
- **Critical Parameter:** diamond_count — how many hanging diamonds; more creates denser radial symmetry
- **What Emerges:** the rainbow color array as a pedagogical signal — hue encodes position in the radial sequence
- **Triggers:** nothing at runtime — static display; editor-tool flag allows live-editing in Godot editor

## Architecture

| | |
|---|---|
| **File** | `algorithms/primitives/diamondtoruscollection/diamondtoruscollection.gd` (233 lines) |
| **Scene** | `res://algorithms/primitives/diamondtoruscollection/diamondtoruscollection.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `torus_radius` | float | 2.0 |
| `cylinder_length` | float | 1.0 |
| `cylinder_radius` | float | 0.01 |
| `diamond_count` | int | 7 |
| `diamond_scale` | float | 0.5 |
| `show_torus_wireframe` | bool | true |

### Key Methods

- `_ready()`
- `create_torus_reference()`
- `create_hanging_arrangement()`
- `create_hanging_cylinder(torus_position: Vector3)`
- `set_diamond_count(count: int)`
- `set_torus_radius(radius: float)`
- `set_cylinder_length(length: float)`
- `clear_arrangement()`
- `toggle_torus_wireframe()`
- `set_diamond_colors_custom(colors: Array[Color])`
- `set_cylinder_color(color: Color)`
- `get_arrangement_info()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Primitives_Melencolia

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Primitives_Melencolia | [10,3] | `:0:4` |

### Relationships

- uses octahedron as diamond unit; depends on torus geometry from torus_radials_rings
- Needs: [missing VR controls — static display; no live diamond_count slider]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
