# Artifact: Three-Body Problem

> F_ij = G*m_i*m_j/r^2. Three bodies, three mutual attractions, no closed-form solution. Deterministic rules, unpredictable trajectories.

## Context

**Category:** physics | **Complexity:** advanced
**Tags:** three_body, chaos, gravitational, sensitivity, deterministic, unpredictable, lagrange_points, figure_eight, initial_conditions | **Themes:** chaos, gravity, three_body, sensitivity

The infamous three-body problem: three gravitationally interacting masses with no closed-form solution. Tiny changes in initial conditions lead to wildly divergent trajectories — the original exemplar of deterministic chaos. Explore stable figure-eight orbits, Lagrange points, and the chaotic regime where prediction becomes impossible.

*Three bodies under gravity have no formula. The future is computable but not predictable. Determinism and predictability are not the same thing.*

## Design

- **Visual:** material: unlit, animation: physics
- **Scale:** [184, 182, 195] (xlarge)
- **Interaction:** To watch three glowing bodies dance — orbiting, swapping partners, ejecting one, recapturing — Poincare's impossibility made visible.
- **Critical Parameter:** gravitational_constant (0.1) and initial conditions. Tiny changes in starting positions produce wildly different long-term behavior. This IS chaos.
- **What Emerges:** Figure-eight orbits (rare, unstable). Hierarchical pairs (two orbit closely, third orbits the pair). Ejection events (one body flung away). Sensitivity to initial conditions.
- **Triggers:** Auto-start — bodies attract pairwise, trails trace history. Reset button → return to initial positions. Pause → freeze the dance. Mass slider → all masses change.

## Architecture

| | |
|---|---|
| **File** | `algorithms/physicssimulation/threebodyproblem/threebodyproblem.gd` (215 lines) |
| **Scene** | `res://algorithms/physicssimulation/threebodyproblem/threebodyproblem.tscn` |
| **Registry** | `physics_simulation.json` |
| **Class** | `ThreeBodyProblem` extends `Node3D` |
| **Pattern** | procedural |

### Key Methods

- `_ready()` -- Scale for VR reachability
- `_create_star_field()` -- Create a background star field using a single MultiMeshInstance3D
- `_initialize_bodies()`
- `_physics_process(delta: float)`
- `_apply_gravitational_forces(_delta)` -- Calculate gravitational forces between all pairs of bodies
- `_update_bodies(delta)` -- Update each body's physics
- `_update_trails()` -- Update trails for each body
- `_connect_ui()`
- `_on_reset_pressed()` -- Reset all bodies to initial positions (CelestialBody.reset_to_initial
- `_on_pause_pressed()`
- `_on_trail_toggle_pressed()`
- `_on_mass_changed(value: float)` -- Update mass of all bodies
- `_apply_vibrant_colors()` -- Apply vibrant queer colors to celestial bodies.
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: ForcesChaos
- **Physics Simulation: The Engine Behind Reality** (physicssimulation) -- map: PhysicsSim_Fields
- **Physics Simulation: The Engine Behind Reality** (physicssimulation) -- map: PhysicsSimulation_Three_Body_Problem

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Forces_8 | [1,2] | `-` |
| ForcesChaos | [1,2] | `-` |
| PhysicsSim_Fields | [5,4] | `:0:1` |
| PhysicsSimulation_Three_Body_Problem | [4,5] | `-` |

### Relationships

- Extends exercise_1_8 (two-body attraction → three-body chaos). Pairs with nbody_simulation (3 → N). Gateway to chaos_attractor (strange attractors from deterministic ODEs).
- Needs: VR UI buttons [has], trail visualization [has], auto-rotate for 3D perspective [has]. Missing: VR grabbable bodies to set initial conditions, Lyapunov exponent display.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
