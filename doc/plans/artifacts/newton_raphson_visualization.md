# Artifact: Newton-Raphson Method

> Root-finding by tangent line iteration — each step uses the derivative to leap closer to where the function crosses zero. Quadratic convergence when it works, spectacular chaos when it doesn't.

## Context

**Category:** algorithms | **Complexity:** intermediate
**Tags:** newton_raphson, root_finding, tangent, iteration, convergence, numerical_methods | **Themes:** algorithms, misc

Root-finding by tangent line iteration — each step uses the derivative to leap closer to where the function crosses zero. Quadratic convergence when it works, spectacular chaos when it doesn't.

## Design

- **Visual:** material: standard, animation: transform

## Architecture

| | |
|---|---|
| **File** | `algorithms/numericalmethods/newtonraphson/newton_raphson_visualization.gd` (479 lines) |
| **Scene** | `res://algorithms/numericalmethods/newtonraphson/newton_raphson_visualization.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `target_function` | String | "x^3 - 2*x - 5" |
| `initial_guess` | float | 2.0 |
| `tolerance` | float | 0.0001 |
| `max_iterations` | int | 20 |
| `iteration_speed` | float | 1.0 |
| `function_resolution` | int | 200 |
| `x_range` | Vector2 | Vector2(-5, 5) |
| `y_range` | Vector2 | Vector2(-10, 10) |
| `show_tangent_lines` | bool | true |
| `show_convergence_path` | bool | true |
| `animate_convergence` | bool | true |
| `show_formula` | bool | true |
| `show_steps` | bool | true |
| `function_preset` | String | "Cubic" |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_input(event: InputEvent)`
- `setup_environment()` -- Lighting
- `setup_camera()`
- `load_function_preset(preset: String = "")`
- `evaluate_function(x: float)`
- `evaluate_derivative(x: float)`
- `create_function_visualization()`
- `create_curve_mesh(points: PackedVector3Array, color: Color, thickness: float)`
- `create_axis_line(start: Vector3, end: Vector3, color: Color)`
- `start_newton_raphson()`
- `perform_iteration_step()`
- `create_tangent_line(x: float, fx: float, fpx: float)`
- `create_iteration_point(position: Vector3, color: Color)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
