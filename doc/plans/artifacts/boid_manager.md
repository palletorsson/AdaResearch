# Artifact: boid_manager

> f_i = Σ(neighbor_j)[separate(i,j) + align(vel_j) + cohere(pos_j)] — three weighted averages over a local neighborhood

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** boid | **Themes:** algorithms, misc

boid_manager

*coordination is not agreement — it is geometry; close enough means you are already the same*

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [20.0, 10.0, 20.0] (world_scale)
- **Interaction:** to reach out a VR hand and feel the flock split and reform around your arm — to be a boulder in a river of agency
- **Critical Parameter:** interaction_radius — at zero, it's a particle cloud; past a threshold, a coherent organism crystallizes from the randomness
- **What Emerges:** coordinated lane reversals, vortex rings, and split-rejoin events that no rule specifies — the flock invents maneuvers
- **Triggers:** left trigger attracts boids toward controller; right trigger repels — learner's body becomes a temporary leader with no followers bound by loyalty

## Architecture

| | |
|---|---|
| **File** | `algorithms/emergentsystems/boidflocking/boid_manager.gd` (167 lines) |
| **Scene** | `res://algorithms/emergentsystems/boidflocking/boid_manager.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Class** | `BoidManager` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `boid_scene` | PackedScene | - |
| `num_boids` | - | 50 |
| `spawn_area_size` | - | Vector3(20, 10, 20) |
| `vr_player_path` | NodePath | - |
| `left_controller_path` | NodePath | - |
| `right_controller_path` | NodePath | - |
| `interaction_radius` | - | 3.0 |
| `attraction_strength` | - | 5.0 |
| `repulsion_strength` | - | 8.0 |

### Key Methods

- `_ready()` -- Get VR references
- `spawn_boids()`
- `_connect_controller_signals()` -- Connect controller input signals if using OpenXR
- `_physics_process(_delta)` -- Handle controller interaction with boids
- `_process_controller_interaction()` -- Process left controller
- `_interact_with_nearby_boids(controller_pos, _direction, attract)`
- `_on_left_controller_button_pressed(button_name)`
- `_on_left_controller_button_released(button_name)`
- `_on_right_controller_button_pressed(button_name)`
- `_on_right_controller_button_released(button_name)`
- `_input(event: InputEvent)` -- For testing without VR hardware
- `get_boids()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Swarm Intelligence: No Leader, Yet Coordinated** (swarmintelligence) -- map: SwarmIntelligence_Boids_Algorithm

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| SwarmIntelligence_Boids_Algorithm | [7,3] | `-` |

### Relationships

- boids_aquarium wraps the same physics in a compact glass tank with sliders; boid_flocking adds the documentation UI; contrasts with FlowFieldMain (rules vs. field)
- Needs: [has] VR controller attract/repel via trigger; [missing] no sliders for separation/alignment/cohesion weights; no on-screen parameter display

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
