# Artifact: recursive_tree

> Procedural binary tree — a trunk forks into branches, each branch forks again, recursion depth 3. Box meshes with emission materials create a geometric canopy. Self-similar at every level.

## Context

**Category:** unknown | **Complexity:** advanced
**Tags:** tree | **Themes:** generation, procedural

recursive_tree

*A tree is a list that learned to decide — left or right, less or greater. Hierarchy emerges not from authority but from the constraint of binary choice repeated.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 6.0, 13.0] (world_scale)
- **Interaction:** To grow by halving — generate_branches calls itself with decremented depth, producing sub-branches from each endpoint. The tree is its own blueprint.
- **Critical Parameter:** num_main_branches / max_sub_branches — control branching factor; random_seed — deterministic chaos, same seed same tree
- **What Emerges:** A geometric sculpture where every subtree is a complete tree — hierarchy from constraint, not command. Each node knows only its children yet the whole structure self-organizes.
- **Triggers:** _ready → seed + material creation + full tree generation; rebuild_tree → clear all children, regenerate from parameters

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/recursive_tree/recursive_tree.gd` (496 lines) |
| **Scene** | `res://algorithms/fractals/recursive_tree/recursive_tree.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_main_branches` | - | - |
| `max_sub_branches` | - | - |
| `branch_length_min` | - | - |
| `branch_length_max` | - | - |
| `branch_width_min` | - | - |
| `branch_width_max` | - | - |
| `branch_height_min` | - | - |
| `branch_height_max` | - | - |
| `trunk_height` | - | - |
| `trunk_width` | - | - |
| `random_seed` | - | - |
| `primary_color` | - | - |
| `secondary_color` | - | - |
| `trunk_color` | - | - |
| `metallic` | - | - |
| `roughness` | - | - |
| `emission_strength` | - | - |

### Key Methods

- `_ready()` -- Set the random seed
- `create_materials()` -- Primary material (most blocks)
- `generate_tree()` -- Create root node for the tree
- `create_trunk()`
- `generate_branches(parent, origin_point, num_branches, current_depth, max_depth)`
- `add_detail_blocks(parent_node, branch_instance, branch_size)`
- `generate_complex_geometric_tree()` -- Create root node for the tree
- `create_base()`
- `create_block_cluster(parent, origin_point)` -- Create a node for the cluster
- `create_main_blocks(parent, origin_point)` -- In the reference image, there are about 3-4 large block clusters
- `create_connecting_structures(parent, origin_point)` -- Create a few connector pieces
- `add_cluster_details(parent, origin_point)`
- `rebuild_tree()` -- Remove existing tree
- `generate_image_style_tree()` -- Create base materials
- `_exit_tree()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Data Structures** (datastructures) -- map: DataStructures_Trees
- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_RecursiveTrees
- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_Synthesis
- **Procedural Generation (All Maps)** (proceduralgeneration_all) -- map: ProceduralGeneration_Seven
- **Recursive Emergence** (recursiveemergence) -- map: RecursiveEmergence_Tail_Recursion_Memoization

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| DataStructures_Trees | [4,6] | `-` |
| Fractal_RecursiveTrees | [4,12] | `:0:0:0.7` |
| Fractal_Synthesis | [8,5] | `-` |
| ProceduralGeneration_Seven | [10,2] | `-` |
| RecursiveEmergence_Tail_Recursion_Memoization | [4,3] | `-` |

### Relationships

- Appears in DataStructures_Trees (binary recursion) and RecursiveEmergence_Tail_Recursion_Memoization (call stack visualization). The tree that teaches both data structure and recursion.
- Needs: VR branch manipulation [missing], growth animation [missing], parameter sliders [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
