# Artifact: Catalyst Pedestal

> wireframe_cube(1m) -> display_case(catalyst) -- a glowing cage that presents the crystal

## Context

**Category:** tools | **Complexity:** simple
**Tags:** tool, catalyst, pedestal, display, wireframe, pickable, spawn | **Themes:** gameplay, hazards

A one-meter wireframe cube display case with the Becoming Catalyst crystal floating and rotating inside. Place this in lab spawn areas. The player walks up, sees the glowing crystal, and grabs it — the cage fades away as the crystal absorbs into their hand.

*the cage doesn't protect the crystal — it frames the invitation.*

## Design

- **Visual:** material: unlit, animation: shader_time
- **Interaction:** to be noticed, approached, and opened — the first thing the player reaches for
- **Critical Parameter:** cage_color / crystal_hover_height -- the wireframe must say "pick me up"
- **What Emerges:** the moment before transformation — the tool exists, waiting to be claimed
- **Triggers:** player enters lab; catalyst not yet absorbed; visual beacon draws the eye

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/becoming_catalyst/catalyst_pedestal.gd` (214 lines) |
| **Scene** | `res://commons/hazards/becoming_catalyst/catalyst_pedestal.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `CatalystPedestal` extends `Node3D` |
| **Pattern** | procedural |

### Dependencies

- `res://commons/hazards/becoming_catalyst/becoming_catalyst.tscn`

### Key Methods

- `_ready()`
- `_process(delta: float)` -- Crystal hover and spin
- `_build_cage()`
- `_build_edge(start: Vector3, end_pos: Vector3, idx: int)`
- `_build_light()`
- `_spawn_crystal()`
- `_on_crystal_taken(_pickable)` -- Crystal has been grabbed — fade the cage away
- `apply_grid_config(config_data: Dictionary)`

### Signals

- `catalyst_taken`

### Grid Config

Accepts: `cage_color`

## Curriculum Position

### Sequences

- hazards
- lab
- testmaps

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Lab | [2,3] | `-` |

### Relationships

- contains becoming_catalyst; inspired by cube_lines aesthetic; lives in lab spawn area
- Needs: BecomingCatalyst scene [spawns]; wireframe rendering [has]; slow rotation [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
