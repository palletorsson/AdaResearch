# Artifact: CA Bridge

> next_cell = wolfram_rule(left, center, right) extruded as walkable bridge

## Context

**Category:** emergence | **Complexity:** intermediate
**Tags:** cellular_automata, emergence | **Themes:** biology, cellular_automata, computation, emergence

Cellular Automata Bridge.

*A bridge built by a rule — you walk on computation.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** To be crossed — a 1D automaton made physical, each row of cubes a generation you walk through
- **Critical Parameter:** rule (0-255) — each Wolfram rule number produces a completely different bridge topology
- **What Emerges:** Walkable architecture from an 8-bit number — bridges, walls, gaps appear from three-neighbor logic
- **Triggers:** Rule 30 → chaotic, broken path; Rule 110 → structured walkway; random initial row → organic variation

## Architecture

| | |
|---|---|
| **File** | `algorithms/cellularautomata/ca_bridge/ca_bridge.gd` (161 lines) |
| **Scene** | `res://algorithms/cellularautomata/ca_bridge/ca_bridge.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `width` | int | 21 |
| `length` | int | 100 |
| `cell_size` | float | 0.5 |
| `generation_speed` | float | 0.1 |
| `auto_generate` | bool | true |
| `rule` | int | 30 |
| `random_initial_row` | bool | false |
| `initial_density` | float | 0.5 |

### Dependencies

- `res://commons/primitives/cubes/cube_scene.tscn`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_setup_collision()` -- Create a single StaticBody for the bridge to hold all shapes
- `start_generation()`
- `stop_generation()`
- `_clear_bridge()`
- `_initialize_row()`
- `step()`
- `_spawn_row(row: Array, z_index: int)`
- `_calculate_next_row(row: Array)`
- `_apply_rule(a, b, c)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Cellular Automata: Local Rules, Global Patterns** (cellularautomata) -- map: CA_GameOfLife

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CA_GameOfLife | [15,4] | `:0:-5` |
| Disposition_Test_Maze | [3,9] | `-` |

### Relationships

- Feeds into CA_GameOfLife. Contrasts with ca_columns (1D→bridge vs 2D→towers). Precedes ca_rule_explorer.
- Needs: VR rule selector [missing], speed control [missing], collision [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
