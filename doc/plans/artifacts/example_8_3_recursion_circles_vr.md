# Artifact: example_8_3_recursion_circles_vr

> circle(center, r, d) = torus(r) + 4 * circle(center +/- offset, r * 0.5, d-1)

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

this is the test

*Recursion is not repetition — it is self-reference, and the circle that contains circles is the first fractal you can hold.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 3.0, 3.0] (room_scale)
- **Interaction:** To hypnotize — nested tori spinning at depth-dependent speeds, each ring a portal into the next scale
- **Critical Parameter:** max_depth — at 3 it's a simple pattern, at 5 the circles overlap and interfere, creating moire-like density
- **What Emerges:** Interference patterns between spinning tori at different depths — visual beats from simple rotation ratios
- **Triggers:** Full rotation of outermost ring → pause; depth-varying rotation_per_depth creates visual decoherence

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/example_8_3_recursion_circles_vr.gd` (134 lines) |
| **Scene** | `res://algorithms/fractals/example_8_3_recursion_circles_vr.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_depth` | int | 4 |
| `radius_reduction` | float | 0.5 |
| `rotation_speed` | float | 0.3 |
| `rotation_per_depth` | float | 0.5 |
| `pause_duration` | float | 10.0 |

### Dependencies

- `res://commons/resourses/materials/noc_vr/noc_vr_pink_primary.tres`
- `res://algorithms/shaders/queer_materials/mat_pop_plastic.tres`

### Key Methods

- `_ready()`
- `_setup_environment()`
- `_draw_circles(center: Vector3, radius: float, depth: int)`
- `_create_circle(center: Vector3, radius: float, depth: int)`
- `_process(delta: float)` -- Handle pause timer
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_Recursion

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_Recursion | [4,4] | `-` |

### Relationships

- Simplest fractal recursion — precedes Koch and Sierpinski; introduces the 4-fold branching pattern
- Needs: VR depth slider [missing], rotation speed control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
