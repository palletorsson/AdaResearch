# Artifact: Voxel Noise

> voxel[x,y,z] = 1 if FastNoiseLite.get_noise_3d(x,y,z) > iso_level else 0 — continuous noise field discretized into a binary 3D grid

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** 3d, procedural | **Themes:** probability, procedural, randomness

3D noise visualized as voxels — cubes appear or disappear based on noise threshold, revealing the underlying scalar field as solid geometry.

*matter is a threshold — the voxel world reveals that solid and void are not intrinsic qualities but the result of where you draw the line through a continuous field*

## Design

- **Visual:** material: shader, animation: static
- **Interaction:** to touch the threshold — to slide iso_level and watch a landscape of solid cubes dissolve or materialize, understanding that the voxel world is just a snapshot of a continuous noise field
- **Critical Parameter:** iso_level — the decision boundary between solid and void; small changes cause avalanches of appearing/disappearing cubes
- **What Emerges:** the iso-surface concept — that topology of solid matter is defined by a threshold value, not by explicit geometry; also the glass/pink shader makes solid cubes feel crystalline
- **Triggers:** iso_level crosses a noise peak → a cave collapses; noise_scale changes → the spatial frequency of the underlying field reshapes what's solid

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/voxelnoise/voxelnoise.gd` (215 lines) |
| **Scene** | `res://algorithms/randomness/voxelnoise/voxelnoise.tscn` |
| **Registry** | `randomness.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `debug` | bool | false |
| `chunk_size` | int | 32 |
| `world_height` | int | 64 |
| `voxel_scale` | float | 1.0 |
| `iso_level` | float | 0.0 |
| `generation_offset` | Vector3i | Vector3i(3, 0, 3) |
| `noise_seed` | int | 1337 |
| `noise_scale` | float | 0.05 |
| `noise_octaves` | int | 4 |
| `noise_persistence` | float | 0.5 |
| `noise_lacunarity` | float | 2.0 |
| `link_to_perlin_sculptor` | bool | true |
| `linked_frequency_min` | float | 0.005 |
| `linked_frequency_max` | float | 0.18 |
| `linked_iso_bias` | float | 0.0 |

### Key Methods

- `_ready()`
- `_generate_chunk(chunk_pos: Vector3i)`
- `apply_perlin_terrain_controls(payload: Dictionary)`
- `_ensure_generated_root()`
- `_configure_noise()`
- `_clear_generated()`
- `_queue_rebuild()`
- `_rebuild_now()`
- `_add_cube(st: SurfaceTool, pos: Vector3)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Grammar Systems: Rules Generate Structure** (grammar_systems) -- map: Voxel_Grammar
- **Noise: Entropy with Memory** (noise) -- map: Noise_Voxel

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Noise_Voxel | [2,1] | `-` |
| TestMap_Unused_28 | [1,3] | `:0:2` |
| Voxel_Grammar | [8,2] | `:0:1:0.45` |

### Relationships

- depends on perlin_terrain_sculptor for interactive control; discretizes what noiselayers shows continuously; connects to marching cubes (smoother iso-surface)
- Needs: links to perlin_terrain_sculptor via apply_perlin_terrain_controls signal bus [has]; no standalone VR sliders [missing]; responds to group "voxelnoise_receivers" [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
