# Artifact: Bifurcation Diagram

> x(n+1) = r·x(n)·(1 - x(n)); period doubling cascade → chaos at r ≈ 3.57

## Context

**Category:** philosophy | **Complexity:** advanced
**Tags:** chaos, bifurcation, edge_of_chaos, lambda, interactive | **Themes:** qfep, philosophy

Interactive 3D bifurcation diagram from logistic map - walk through the edge of chaos

*the edge of chaos is not a metaphor — it is a precise mathematical boundary where computation is maximized*

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle
- **Scale:** [5, 5, 5] (environment)
- **Interaction:** walk through the phase transition from order to chaos as r sweeps right
- **Critical Parameter:** r (mapped to current_r) — the ONE number that controls the entire regime
- **What Emerges:** Feigenbaum universality — the same constants appear in ALL period-doubling systems
- **Triggers:** r crossing 3.0 → bifurcation; r crossing 3.57 → chaos onset; r ≈ 3.83 → window of order inside chaos

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/foundations/bifurcation_diagram.gd` (299 lines) |
| **Scene** | `res://commons/interfaces/foundations/bifurcation_diagram.tscn` |
| **Registry** | `foundations.json` |
| **Class** | `BifurcationDiagram` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `width` | float | 4.0 |
| `height` | float | 2.0 |
| `depth` | float | 0.5 |
| `r_min` | float | 2.5 |
| `r_max` | float | 4.0 |
| `r_steps` | int | 200 |
| `iterations` | int | 100 |
| `skip_transient` | int | 50 |
| `point_size` | float | 0.015 |
| `order_color` | Color | Color(0.2, 0.5, 1.0) |
| `chaos_color` | Color | Color(1.0, 0.3, 0.2) |
| `edge_color` | Color | Color(0.2, 1.0, 0.5) |
| `current_r` | float | 3.0: |

### Key Methods

- `_ready()`
- `_generate_bifurcation_data()` -- Collect all points first
- `_get_color_for_r(r: float)`
- `_create_axes()` -- X axis (r parameter)
- `_create_highlight()` -- Vertical plane showing current r value
- `_create_labels()` -- R value label
- `_update_highlight()`
- `_get_regime_name(r: float)`
- `_process(_delta: float)` -- Could add interactive r adjustment here
- `set_r_value(r: float)`
- `get_regime()`
- `animate_r(from_r: float, to_r: float, duration: float)`

### Signals

- `r_changed`
- `regime_changed`

## Curriculum Position

### Sequences

- **Foundations Crisis: The Limits of Formalization** (foundationscrisis) -- map: Crisis_Synthesis

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Crisis_Synthesis | [13,8] | `:0:1` |

### Relationships

- unlocks lambda_slider (r IS the QFEP lambda); contrasts florensky_sphere (holding contradiction vs cascading into it)
- Needs: VR slider for r [missing] — highlight plane tracks current_r but no physical controller

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
