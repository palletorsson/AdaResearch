# Artifact: Entropy Meter

> Visual gauge showing current E(S) entropy level. A filling tube that responds to lambda. Low=blue, medium=green, high=red. Classic lab instrument aesthetic.

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** compression, qfep, visualization | **Themes:** art, music, philosophy, qfep

Visual gauge showing current E(S) entropy level. A filling tube that responds to lambda. Low=blue, medium=green, high=red. Classic lab instrument aesthetic.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1, 1, 1] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/entropy_meter.gd` (186 lines) |
| **Scene** | `res://commons/interfaces/qfep/entropy_meter.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `EntropyMeter` extends `Node3D` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_create_tube()`
- `_create_fill()`
- `_create_scale()` -- Create tick marks on the side
- `_create_labels()` -- Bottom label: 0
- `_process(delta)` -- Smooth approach to target
- `_update_fill()`
- `_update_fill_position()`
- `_update_color()`
- `set_entropy(value: float)`
- `on_lambda_changed(lambda: float)`
- `_connect_to_lambda()`

### Signals

- `entropy_changed`

## Curriculum Position

### Sequences

- qfeplaboratory

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
