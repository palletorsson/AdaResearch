# Artifact: QFEP Oscilloscope

> Real-time waveform display showing F (blue) and E(S) (red) over time. Watch the oscillation between order and entropy. Classic lab monitor aesthetic.

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** animation, compression, qfep, visualization | **Themes:** art, philosophy, qfep, theory

Real-time waveform display showing F (blue) and E(S) (red) over time. Watch the oscillation between order and entropy. Classic lab monitor aesthetic.

## Design

- **Visual:** material: unlit, animation: vertex
- **Scale:** [2, 2, 1] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/qfep_oscilloscope.gd` (242 lines) |
| **Scene** | `res://commons/interfaces/qfep/qfep_oscilloscope.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `QFEPOscilloscope` extends `Node3D` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_create_frame()`
- `_create_screen()`
- `_create_lines()` -- F line (blue)
- `_create_labels()` -- Title
- `_initialize_history()`
- `_process(delta)`
- `_sample_values()` -- Calculate current F and E based on lambda and phi
- `_update_lines()` -- Clear and redraw F line
- `_update_labels()`
- `on_lambda_changed(lambda: float)`
- `on_phi_changed(phi: float)`
- `_connect_to_sliders()`

## Curriculum Position

### Sequences

- qfeplaboratory

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
