# Artifact: Noise Mixer

> Fractal Brownian Motion noise mixer — layers multiple sin-based noise octaves with controllable lacunarity and persistence. 128x128 floor texture with earth-tone color ramp. VR sliders adjust octaves (1–8), lacunarity (1.5–3.0), and persistence (0.3–0.7) to explore how complexity emerges from layered simple functions.

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** noise, fbm, fractal, octaves, lacunarity, persistence, procedural, floor_display | **Themes:** music, probability, procedural, randomness

Fractal Brownian Motion noise mixer — layers multiple sin-based noise octaves with controllable lacunarity and persistence. 128x128 floor texture with earth-tone color ramp. VR sliders adjust octaves (1–8), lacunarity (1.5–3.0), and persistence (0.3–0.7) to explore how complexity emerges from layered simple functions.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/noise_mixer/noise_mixer.gd` (321 lines) |
| **Scene** | `res://commons/artifacts/noise_mixer/noise_mixer.tscn` |
| **Registry** | `randomness.json` |
| **Class** | `NoiseMixer` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `quad_size` | Vector2 | Vector2(0.8, 0.8) |
| `octaves` | int | 4 |
| `lacunarity` | float | 2.0 |
| `persistence` | float | 0.5 |
| `base_frequency` | float | 4.0 |
| `seed_value` | int | 42 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_generate_octave_offsets()` -- Max 8 octaves
- `_build_floor_quad()`
- `_generate_texture()`
- `_fbm(nx: float, ny: float)` -- Fractal Brownian Motion: sum of octaves with increasing frequency
- `_sin_noise(x: float, y: float)` -- Sin-based pseudo-noise: combines multiple sin waves at irrational angles
- `_sample_color_ramp(t: float)` -- Sample the earth-tone color ramp at position t in [0, 1].
- `_create_labels()`
- `_update_info()`
- `_create_vr_controls()`
- `_sync_sliders()` -- Octaves: 1–8 → normalised (octaves-1)/7
- `_on_octaves_changed(_pos)`
- `_on_lacunarity_changed(_pos)`
- `_on_persistence_changed(_pos)`
- `_on_randomise(_b = null)`

### Grid Config

Accepts: `base_frequency`, `lacunarity`, `octaves`, `persistence`, `seed`

## Curriculum Position

### Sequences

- noise
- randomness

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
