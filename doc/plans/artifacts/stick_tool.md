# Artifact: Stick Tool

> mediator(player, plasma) -> transmute(form) -- stick bridging player and plasma critter

## Context

**Category:** tools | **Complexity:** intermediate
**Tags:** interactive, tools, vr | **Themes:** gameplay, hazards

A simple wooden stick the player can grab. Hold near a Plasma Critter to transform it into a torch. The mediator between player and plasma — the relational bridge.

*the stick is the oldest mediator -- extending reach and transforming relationship with the world.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** a simple stick that when dipped into plasma transforms raw energy into directed fire
- **Critical Parameter:** attached_plasma -- which plasma critter the stick mediates; tip contact triggers change
- **What Emerges:** the simplest tool becomes the key to transmuting hazard into benefit
- **Triggers:** XRToolsPickable grab; tip contacts plasma -> attachment; plasma transmutes via stick mediation

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/plasma/stick_tool.gd` (203 lines) |
| **Scene** | `res://commons/hazards/plasma/stick_tool.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `StickTool` extends `XRToolsPickable` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `stick_length` | float | 0.8 |
| `stick_radius` | float | 0.02 |
| `stick_color` | Color | Color(0.35, 0.22, 0.1) |
| `tip_glow_when_ready` | bool | true |

### Key Methods

- `_ready()`
- `_process(_delta: float)` -- Subtle glow at tip when no plasma is attached (inviting interaction)
- `is_stick()`
- `receive_plasma(plasma: PlasmaCritter)` -- Already carrying plasma
- `release_plasma()`
- `_on_plasma_depleted()`
- `_build_stick()` -- Stick mesh — wooden cylinder
- `_setup_physics()` -- Physics properties
- `_on_picked_up(_pickable)`
- `_on_dropped(_pickable)`
- `apply_grid_config(config_data: Dictionary)`
- `configure(config_data: Dictionary)`
- `_cfg_float(value: Variant, fallback: float)`

### Signals

- `plasma_attached`
- `plasma_detached`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_4

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_4 | [3,3] | `-` |

### Relationships

- paired with plasma_critter; mediator in Q-FEP dual-nature system; the ur-tool
- Needs: XRToolsPickable [has]; tip marker [has]; plasma attachment [has]; VR grab interaction [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
