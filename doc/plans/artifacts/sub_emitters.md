# Artifact: sub_emitters

> On death, spawn children. Recursion in particle systems. One generation seeds the next.

## Context

**Category:** unknown
**Tags:** particles, sub-emitters, fireworks

Firework mortar launching projectiles that spawn burst particles on death

*Particles spawning particles is recursion made physical. Death becomes birth.*

## Design

- **Interaction:** To explode beautifully. To show that death is not ending but transformation.
- **Critical Parameter:** Burst count — 10 children = sparkler, 40 = firework, 100 = supernova. Also: primary lifetime (height of burst).
- **What Emerges:** Fireworks from mortar + burst. Recursive spawning as a pattern. The dramatic arc of launch-peak-explosion-rain.
- **Triggers:** Primary death → burst spawns at death position, gravity on children → parabolic arcs, color per burst → visual identity

## Architecture

| | |
|---|---|
| **File** | `algorithms/particles/sub_emitters/sub_emitters.gd` (182 lines) |
| **Registry** | `physics_simulation.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_setup_multimesh()`
- `_setup_labels()`
- `_process(delta: float)`
- `_launch_mortar()`
- `_spawn_burst(origin: Vector3)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Particle Systems** (particles) -- map: Particles_Emitters

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Particles_Emitters | [5,5] | `-` |

### Relationships

- Extends Particles_Emitters concept (emitters that ARE particles). Feeds into particle_campfire (embers are soft sub-emitters).
- Needs: VR burst count slider [missing]. Primary launch angle control [missing]. Gravity slider [missing].

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
