# Artifact: Running Text Display

> Scrolling text display for critical theory — words flowing through space, text as material, reading as embodied spatial practice.

## Context

**Category:** critical | **Complexity:** beginner
**Tags:** text, display, critical_theory, spatial_text, reading | **Themes:** algorithms, misc

Scrolling text display for critical theory — words flowing through space, text as material, reading as embodied spatial practice.

## Design

- **Visual:** material: standard, animation: transform

## Architecture

| | |
|---|---|
| **File** | `algorithms/criticaltheory/runningtextdisplay/running_text_display.gd` (136 lines) |
| **Scene** | `res://algorithms/criticaltheory/runningtextdisplay/RunningTextDisplay.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Class** | `RunningTextDisplay` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `text` | String | "YOUR TEXT GOES HERE... POWER IS THE MOST OBVIOUS APHRODISIAC..." |
| `font_size` | int | 24 |
| `text_color` | Color | Color(0, 1, 1, 1) |
| `background_color` | Color | Color(0, 0, 0, 1) |
| `display_width` | float | 2.0 |
| `display_height` | float | 0.2 |
| `scroll_speed` | float | 0.5 |
| `direction` | Vector2 | Vector2(-1, 0) |
| `scroll_direction` | int | 0: |

### Key Methods

- `_ready()` -- Create the display mesh
- `create_led_display()` -- Create a simple flat box to represent the LED display
- `setup_viewport_text()` -- Create viewport for the text
- `_process(delta: float)` -- Calculate scrolling based on direction and speed
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
