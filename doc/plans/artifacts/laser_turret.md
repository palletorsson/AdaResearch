# Artifact: Laser Turret

> Standalone laser turret that auto-targets and destroys balls. Place separately from ball_dropper to customize layout. Demonstrates vector subtraction (direction), magnitude (range), normalization (aim), dot product (lock).

## Context

**Category:** mathematics | **Complexity:** beginner
**Tags:** vectors, subtraction, magnitude, normalize, dot_product, turret, laser, standalone | **Themes:** vectors, game_mechanics, applied_math

Standalone laser turret that auto-targets and destroys balls. Place separately from ball_dropper to customize layout. Demonstrates vector subtraction (direction), magnitude (range), normalization (aim), dot product (lock).

## Design

- **Visual:** material: unlit, animation: physics

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/turret_targeting/laser_turret.gd` (606 lines) |
| **Scene** | `res://commons/artifacts/turret_targeting/laser_turret.tscn` |
| **Registry** | `vectors.json` |
| **Class** | `LaserTurret` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `rotation_speed` | float | 8.0 |
| `detection_range` | float | 30.0 |
| `fire_delay` | float | 0.1 |
| `damage_per_second` | float | 80.0 |
| `laser_color` | Color | Color(1.0, 0.05, 0.0) |
| `laser_width` | float | 0.012 |
| `laser_glow_width` | float | 0.05 |
| `laser_intensity` | float | 5.0 |
| `auto_target` | bool | true |
| `target_closest` | bool | true |
| `target_player` | bool | true |
| `idle_reset_time` | float | 10.0 |

### Key Methods

- `_ready()`
- `_build_turret()`
- `_build_laser()`
- `_build_particles()`
- `_build_lighting()`
- `_process(delta: float)`
- `_reset_hunt()`
- `_find_target()`
- `_find_player()`
- `_get_all_balls()`
- `_scan_for_balls(node: Node, results: Array)`
- `_acquire_target(ball: Node3D)`
- `_lose_target()`
- `_track_target(delta: float)`
- `_get_target_position()`

### Signals

- `ball_destroyed`
- `target_acquired`
- `target_lost`

## Curriculum Position

### Sequences

- color

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Context | [31,2] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
