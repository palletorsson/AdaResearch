# Artifact: 1.2: A zigzagprofile

> Interactive zigzag strip featuring top and base grab points plus presets for zigzag, sine, and sawtooth patterns.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** interactive, vr | **Themes:** core, primitives

Interactive zigzag strip featuring top and base grab points plus presets for zigzag, sine, and sawtooth patterns.

## Design

- **Visual:** material: shader, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/zigzagprofile/zigzagprofile.gd` (302 lines) |
| **Scene** | `res://commons/primitives/zigzagprofile/zigzagprofile.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `sphere_size_multiplier` | float | 0.3 |
| `sphere_y_offset` | float | 0.0 |
| `segment_width` | float | 0.5 |
| `segment_height` | float | 0.8 |
| `segment_count` | int | 5 |
| `double_sided` | bool | true |
| `alter_freeze` | bool | false |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `create_profile_mesh()` -- Create profile mesh
- `create_zigzag_vertices()` -- Create zigzag vertices with alternating peaks and valleys (70% folded)
- `_setup_drag_points()`
- `_collect_point_positions()`
- `update_profile_mesh()` -- Update the profile mesh from current vertices
- `add_triangle_with_normal(st: SurfaceTool, vertices: Array)`
- `_on_point_picked_up(_index: int, _pickable, _meta: Dictionary)`
- `_on_point_dropped(index: int, _pickable, meta: Dictionary)`
- `_on_point_moved(index: int, position: Vector3, meta: Dictionary)`
- `apply_profile_material(mesh_instance: MeshInstance3D, color: Color)`
- `reset_to_zigzag()` -- Reset to perfect zigzag pattern (70% folded)
- `reset_to_sine_wave()` -- Reset to sine wave pattern
- `reset_to_sawtooth()` -- Reset to sawtooth pattern
- `update_sphere_positions()`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
