# Artifact: Chromatic Field

> damage(t) = f(hue_phase, player_position) -- RGB zone shifting hue with color-coded effects

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** color, rgb, perception, zone | **Themes:** education, gameplay, hazards

RGB zone hazard with three overlapping translucent spheres cycling through hue. Damage type depends on the dominant color channel — red heat, green poison, blue cold. Teaches color mixing and perception.

*color is not decoration -- it is a signal channel carrying damage semantics in the visible spectrum.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** three overlapping colored spheres cycling through RGB, each hue carrying different damage
- **Critical Parameter:** _hue_phase -- continuous rotation through color space determines active damage type
- **What Emerges:** color as information -- the visible spectrum becomes a hazard language where hue means harm-type
- **Triggers:** player enters area; hue_phase cycles continuously; damage accumulates based on current color

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/chromatic_field/chromatic_field.gd` (252 lines) |
| **Scene** | `res://commons/hazards/chromatic_field/chromatic_field.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `ChromaticField` extends `Area3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `field_radius` | float | 2.0 |
| `inner_radius` | float | 1.5 |
| `hue_cycle_speed` | float | 0.15 |
| `damage_per_second` | float | 8.0 |
| `rotation_speed` | float | 0.5 |
| `red_color` | Color | Color(1.0, 0.15, 0.1, 0.3) |
| `green_color` | Color | Color(0.1, 1.0, 0.2, 0.3) |
| `blue_color` | Color | Color(0.1, 0.2, 1.0, 0.3) |

### Key Methods

- `_ready()` -- Collision setup
- `_process(delta: float)`
- `_create_materials()`
- `_build_mesh()` -- Three offset spheres for R, G, B
- `_on_body_entered(body: Node3D)`
- `_on_body_exited(body: Node3D)`
- `_find_player()`
- `take_damage(amount: float)` -- Zone hazards are indestructible
- `apply_damage(amount: float)`
- `damage(amount: float)`
- `configure(config: Dictionary)`
- `apply_grid_config(config: Dictionary)`

### Signals

- `enemy_destroyed`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_6

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_6 | [7,3] | `-` |

### Relationships

- embodies color sequence concepts as spatial hazard; pairs with noise_field
- Needs: Area3D detection [has]; RGB sphere meshes [has]; hue-based damage [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
