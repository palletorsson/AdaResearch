# Artifact: Gradient Hunter

> x_next = x - lr * grad(loss(x)) -- gradient descent as locomotion, loss as distance-to-player

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** gradient-descent, optimization, machine-learning, loss-function | **Themes:** education, gameplay, hazards

Sleek predator with orbiting sample-point particles. Gradient descent pathfinding toward player — gets stuck in local minima, anneals out with random teleport. Machine learning as locomotion strategy.

*gradient descent finds A minimum but not THE minimum -- the hunter gets stuck because optimization does.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a sleek predator sampling 6 points, moving toward lowest loss, stuck in local minima
- **Critical Parameter:** _sample_positions / _gradient_dir -- 6-point gradient estimate determines movement
- **What Emerges:** machine learning as hunting -- the loss function IS the space between predator and prey
- **Triggers:** continuous gradient sampling; steepest descent movement; local minima -> random annealing jump

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/gradient_hunter/gradient_hunter.gd` (318 lines) |
| **Scene** | `res://commons/hazards/gradient_hunter/gradient_hunter.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `GradientHunter` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `learning_rate` | float | 3.0 |
| `sample_radius` | float | 0.8 |
| `anneal_timeout` | float | 3.0 |
| `anneal_jump_radius` | float | 2.5 |
| `sample_count` | int | 6 |
| `body_inner_radius` | float | 0.08 |
| `body_outer_radius` | float | 0.22 |
| `sample_sphere_radius` | float | 0.04 |
| `leg_length` | float | 0.25 |
| `leg_radius` | float | 0.02 |

### Key Methods

- `_on_ready()`
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()`
- `_build_leg(index: int)`
- `_process_visual(delta: float)` -- Walk animation
- `_process_chase(delta: float)`
- `_compute_gradient()`
- `_anneal()` -- Simulated annealing: jump to random offset to escape local minimum
- `_on_damaged(_amount: float)` -- Getting hit increases learning rate temporarily
- `_on_state_changed(new_state: BaseState)`

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_6

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_6 | [3,1] | `-` |

### Relationships

- embodies machinelearning sequence; pairs with grammar_markov (optimization vs stochastic)
- Needs: HazardCreatureBase [has]; gradient sampling [has]; minima detection [has]; annealing [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
