# Artifact: binary_space_partitioning

> Recursive hyperplane cuts partitioning an AABB into a binary tree of convex cells — division as architecture

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** procedural | **Themes:** generation, procedural

binary_space_partitioning

*Architecture begins with division — every room is what remains after the cut.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [11.0, 1.0, 11.0] (world_scale)
- **Interaction:** To show that dungeon rooms, office layouts, and game levels all begin with the same act: splitting space in two
- **Critical Parameter:** max_depth — how many recursive cuts; each level doubles the cell count and halves the room scale
- **What Emerges:** Room layouts, density heatmaps, and hierarchical spatial structure from a single recursive rule
- **Triggers:** Depth 3 produces a few large rooms; depth 7 produces dense labyrinth; gradient bias clusters small cells at edges

## Architecture

| | |
|---|---|
| **File** | `algorithms/proceduralgeneration/spatial_partitioning/binary_space_partitioning/BinarySpacePartitioning.gd` (341 lines) |
| **Scene** | `res://algorithms/proceduralgeneration/spatial_partitioning/binary_space_partitioning/BinarySpacePartitioning.tscn` |
| **Registry** | `procgen_extra.json` |
| **Class** | `BinarySpacePartitioning` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_depth` | int | 7 |
| `min_cell_size` | float | 0.5 |
| `space_size` | Vector3 | Vector3(10, 10, 10) |
| `gradient_type` | int | 0 |
| `gradient_falloff` | float | 1.5 |
| `center_bias` | float | 0.8 |
| `show_partitions` | bool | true |
| `show_heatmap` | bool | true |
| `show_stage` | bool | true |
| `stage_color` | Color | Color(0.95, 0.93, 0.88) |
| `regenerate` | bool | false: |

### Key Methods

- `_ready()`
- `generate_bsp()`
- `partition_node(node: BSPNode)`
- `calculate_partition_probability(position: Vector3)`
- `get_best_split_axis(size: Vector3)` -- Weight by size
- `get_split_ratio(position: Vector3, axis: int)` -- Use gradient to influence split position
- `collect_leaves(node: BSPNode)`
- `visualize_partitions()` -- Note: clear_children() is called in generate_bsp() before this
- `_create_box_geo_nodes(bounds: AABB, color: Color)`
- `create_heatmap_mesh()` -- Create a plane mesh showing the partition probability heatmap
- `create_stage()` -- Create an off-white platform/stage beneath the BSP sculpture
- `clear_children()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Procedural Generation (All Maps)** (proceduralgeneration_all) -- map: ProceduralGeneration_Space_Partitioning
- **Spatial Partitioning: Dividing Space** (spatial_partitioning) -- map: ProceduralGeneration_Space_Partitioning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ProceduralGeneration_Space_Partitioning | [6,5] | `:0:2.2:0.5` |

### Relationships

- Foundation for dungeon generators in constraint_solvers. Contrasts with Voronoi (proximity vs cuts).
- Needs: @tool editor preview [has], gradient bias [has], stage visualization [has], VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
