# Artifact: gridcolorizer

> color_pattern(i) -> MultiMesh.set_instance_color(i) — palettes, gradients, and sphere reflection mapped to grid indices

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** 2d, color | **Themes:** color, perception

Finds cube meshes in the scene and recolors an 11x11 grid to match the rose vase palette.

*color is not a property of the cube — it is a function of the cube's position in the pattern*

## Design

- **Visual:** material: shader, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** to watch the entire grid floor transform as named color patterns sweep across hundreds of cubes simultaneously
- **Critical Parameter:** pattern_names[current_pattern_index] — each pattern is a different mapping from position to color (diagonal stripes, radial gradient, sphere reflection)
- **What Emerges:** the sphere_reflection pattern creates an illusion of a reflective ball on a flat grid — pure math, no actual sphere
- **Triggers:** auto_cycle timer rotates through all patterns; NextCube signal advances manually; palette resource provides the color vocabulary

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/gridcolorizer/gridcolorizer.gd` (476 lines) |
| **Scene** | `res://algorithms/color/gridcolorizer/gridcolorizer.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `color_palette_resource` | Resource | preload("res://algorithms/color/color_palettes.tres") |
| `multimesh_path` | NodePath | "" |
| `debug_logs` | bool | false |
| `auto_cycle_enabled` | bool | true |
| `cycle_interval_seconds` | float | 10.0 |

### Dependencies

- `res://algorithms/color/color_palettes.tres`

### Key Methods

- `_log(message: String)`
- `_initialize_pattern_names()`
- `_get_palette_colors(palette_name: String)`
- `_ready()`
- `find_multimesh()`
- `_find_multimesh_recursive(node: Node)`
- `_find_grid_structure(node: Node)`
- `_apply_named_pattern(pattern_name: String)`
- `start_pattern_cycling()`
- `apply_pattern_to_multimesh(palette_colors: Array, pattern_name: String)`
- `_adjust_material_for_colors()`
- `apply_gradient_pattern(gradient_name: String)`
- `get_gradient_colors(gradient_name: String)`
- `calculate_gradient_color(row: int, col: int, grid_size: int, gradient_colors: Array, gradient_name: String)`
- `interpolate_gradient(progress: float, gradient_colors: Array)`

## Curriculum Position

### Sequences

- **Color: Perception, Not Physics** (color) -- map: Color_Grid_Pallet

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Grid_Pallet | [0,0] | `-` |

### Relationships

- depends on GridMultiMesh and color_palettes.tres; contrasts with pillarcolorcollection (flat grid vs vertical pillars); GameManager provides gradient palettes
- Needs: MultiMeshInstance3D [has]; NextCube integration [has]; auto_cycle [has]; VR pattern selector [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
