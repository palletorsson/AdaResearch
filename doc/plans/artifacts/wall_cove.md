# Artifact: Wall Cove

> Half-cove slope artifact — curved floor-to-wall transition built with SurfaceTool. High-res quarter-circle curve with continuous UVs for shader tiling. Place at wall base for smooth architectural transition.

## Context

**Category:** shaders | **Complexity:** advanced
**Tags:** cove, slope, surface_tool, shader, configurable, wall, transition, architectural | **Themes:** art, shaders, visual

Half-cove slope artifact — curved floor-to-wall transition built with SurfaceTool. High-res quarter-circle curve with continuous UVs for shader tiling. Place at wall base for smooth architectural transition.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/wall_cove/wall_cove.gd` (318 lines) |
| **Scene** | `res://commons/artifacts/wall_cove/wall_cove.tscn` |
| **Registry** | `shaders.json` |
| **Class** | `WallCove` extends `Node3D` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cove_width` | float | 1.0 |
| `cove_radius` | float | 0.3 |
| `curve_segments` | int | 24 |
| `wall_extend` | float | 0.0 |

### Key Methods

- `_ready()`
- `_build()`
- `apply_grid_config(config_data: Dictionary)` -- ── 1. Geometry ──
- `_apply_fallback(config_data: Dictionary)`
- `_set_shader_param(mat: ShaderMaterial, key: String, s: String)` -- Int first (shader int uniforms need int, not float)
- `_add_display_light()`
- `_build_cove_mesh()`
- `_generate_domain_texture(seed_val: int)`
- `_make_domain_pattern(rng: RandomNumberGenerator, pattern_type: int)`
- `_grid8(fill: int)`

### Grid Config

Accepts: `cove_radius`, `cove_width`, `curve_segments`, `wall_extend`

## Curriculum Position

### Sequences

- patterngeneration
- array_tutorial

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
