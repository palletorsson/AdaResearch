# Artifact: Bifurcation Walkway

> x_n+1 = r * x_n * (1 - x_n), iterated across r -> period-doubling cascade

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** walkable, chaos, phase_transition, embodied_learning | **Themes:** qfep, emergence

A corridor where your position = r parameter in the logistic map. Walk through the phase transition: stable → period doubling → chaos → windows of order. The bifurcation diagram made walkable.

*deterministic equations produce unpredictable behavior — chaos is not randomness but sensitive dependence*

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle
- **Scale:** [2, 3, 10] (environment)
- **Interaction:** walk along the r axis and feel order shatter into chaos
- **Critical Parameter:** r — the growth rate that drives the system through bifurcations
- **What Emerges:** the Feigenbaum constant hiding in the self-similar spacing of bifurcation points
- **Triggers:** r_min/r_max slider adjustment recomputes entire diagram; preset buttons jump to chaos/bifurcation regions

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/bifurcation_walkway/bifurcation_walkway.gd` (332 lines) |
| **Scene** | `res://commons/interfaces/qfep/bifurcation_walkway.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `BifurcationWalkway` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `display_width` | float | 2.0 |
| `display_height` | float | 1.5 |
| `r_min` | float | 2.5: |
| `r_max` | float | 4.0: |
| `samples_per_r` | int | 100 |
| `r_steps` | int | 400 |
| `warmup_iterations` | int | 200 |
| `show_iterations` | int | 50 |
| `point_color` | Color | Color(0.2, 0.8, 1.0) |
| `background_color` | Color | Color(0.02, 0.02, 0.04) |
| `frame_color` | Color | Color(0.15, 0.15, 0.2) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_frame()` -- Background panel
- `_create_multimesh()`
- `_create_labels()`
- `_create_vr_controls()`
- `_exit_tree()`
- `_sync_sliders_deferred()`
- `_sync_r_min_slider()`
- `_sync_r_max_slider()`
- `_on_r_min_slider_moved(_position)`
- `_on_r_max_slider_moved(_position)`
- `_set_r_range(new_min: float, new_max: float)`
- `_generate_diagram()`
- `_smoothstep(edge0: float, edge1: float, x: float)`
- `_input(event)`

### Grid Config


## Curriculum Position

### Sequences

- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Lambda_Spectrum

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| QFEP_Lambda_Spectrum | [6,0] | `:90:0:0.8` |

### Relationships

- unlocks understanding of lambda spectrum; depends on iteration concept; contrasts ordered_grid (fixed point vs period doubling)
- Needs: VR sliders [has], preset buttons [has], zoom via keyboard [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
