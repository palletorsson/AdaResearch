# Artifact: Rainbow Checkerboard Floor

> a floor encoding the full visible spectrum as a discrete checkerboard

## Context

**Category:** primitives | **Complexity:** beginner
**Tags:** 2d, pattern, mosaic, floor, pixel_art, rainbow, checkerboard | **Themes:** arrays, art, cultural_computation

Procedural pixel art floor with rainbow checkerboard pattern. Each row uses a different spectrum color paired with black: red, orange, yellow, green, blue, purple — then repeats.

*a checkerboard is the simplest 2-coloring of a grid — rainbow makes it six*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see bold rainbow rows of alternating color and black
- **Critical Parameter:** tiles_short — pixel resolution on the short axis
- **What Emerges:** the insight that modular arithmetic on row index maps to a color cycle
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/rainbow_checker_floor.gd` (168 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/rainbow_checker_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `RainbowCheckerFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.2, 0.9) |
| `tiles_short` | int | 30 |
| `border_width` | int | 1 |

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_create_neon_material(color: Color, emission: bool = false)`
- `_build()`

### Grid Config

Accepts: `floor_size`, `tiles_short`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- neon_chevron_floor (color cycling sibling), pixel_cross_floor (bold pattern cousin)
- Needs: [implemented] procedural mesh, rainbow palette, checkerboard logic

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
