# Artifact: example_8_4_cantor_pagoda_vr

> level(d) = {block(segment) for segment in cantor_segments(d)}, stacked with collision — walkable Cantor architecture

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** 3d | **Themes:** generation, procedural

Cantor Pagoda - 3D extension of Cantor set principle

*The Cantor set becomes architecture when you give its gaps floors and its segments walls.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [9.0, 3.0, 13.0] (world_scale)
- **Interaction:** To be walked through — each level is a Cantor iteration with StaticBody3D collision, ascending from 2^d blocks to 1
- **Critical Parameter:** max_depth — each additional level doubles the block count and shrinks them by 1/3, creating denser platforms above
- **What Emerges:** Walkable gaps between blocks at each level — the removed middle thirds become corridors and windows
- **Triggers:** The pagoda builds bottom-up; depth-based color gradient from purple (bottom/dense) to bright pink (top/sparse)

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/example_8_4_cantor_pagoda_vr.gd` (180 lines) |
| **Scene** | `res://algorithms/fractals/example_8_4_cantor_pagoda_vr.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_depth` | int | 5 |
| `base_width` | float | 8.0 |
| `base_depth` | float | 8.0 |
| `block_height` | float | 1.0 |
| `block_thickness` | float | 1.5 |
| `vertical_offset` | float | 0.0 |

### Dependencies

- `res://commons/resourses/materials/noc_vr/noc_vr_pink_primary.tres`

### Key Methods

- `_ready()`
- `_setup_environment()`
- `_build_pagoda()`
- `_draw_cantor_level(depth: int, y_pos: float)`
- `_get_cantor_segments(depth: int)`
- `_create_block(x_start: float, width: float, y_pos: float, depth: int)` -- Create StaticBody3D for collision
- `get_block_count()`
- `get_level_info(level: int)`
- `reset_pagoda()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_CantorSet

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_CantorSet | [11,8] | `:0:-1:0.5` |

### Relationships

- Architectural interpretation of cantor_set; pairs with fibonacci_pagoda (golden vs Cantor scaling)
- Needs: VR walking [has via collision], level info display [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
