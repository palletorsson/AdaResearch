# Artifact: Stepped Zigzag Floor

> a floor where flat stone becomes an impossible staircase

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pompeii, optical_illusion, zigzag | **Themes:** arrays, art, cultural_computation

Pompeii mosaic floor with stepped zigzag pattern — interlocking L-shaped steps in three tones create a 3D staircase illusion reminiscent of Escher's impossible staircases.

*stepped zigzag patterns appear in Roman, Greek, and pre-Columbian floors alike*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see Escher-like depth from three shades of marble
- **Critical Parameter:** cells_short — number of zigzag cells on the short axis
- **What Emerges:** the realization that luminance alone can fabricate volume
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/stepped_zigzag_floor.gd` (317 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/stepped_zigzag_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `SteppedZigzagFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.0, 1.0) |
| `cells_short` | int | 4 |
| `border_widths` | Array[int] | [2, 1, 1] |
| `border_motif` | int | BorderMotifs.Motif.SOLID |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_medium` | Color | MosaicPalette.MEDIUM |
| `color_light` | Color | MosaicPalette.LIGHT |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.02 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`
- `res://commons/artifacts/pompeii_mosaic_floor/border_motifs.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`

### Grid Config

Accepts: `border_widths`, `cells_short`, `floor_size`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- tumbling_blocks_floor (sibling 3D illusion), swastika_meander_floor (sibling meander)
- Needs: [implemented] procedural mesh, border bands, stepped L-shape pixel grid

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
