# Artifact: Vertical Wave Lines

> Array of vertical lines displaced by wave functions — interference, standing waves, and superposition made visible as a wall of oscillating rods.

## Context

**Category:** wavefunctions | **Complexity:** beginner
**Tags:** wave, vertical, lines, interference, standing_wave, superposition | **Themes:** oscillation, physics, waves

Array of vertical lines displaced by wave functions — interference, standing waves, and superposition made visible as a wall of oscillating rods.

## Design

- **Visual:** material: shader, animation: particle_lifecycle

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/vertical_wave_lines/vertical_wave_lines.gd` (151 lines) |
| **Scene** | `res://algorithms/wavefunctions/vertical_wave_lines/vertical_wave_lines.tscn` |
| **Registry** | `wavefunctions_extra.json` |
| **Class** | `VerticalWaveLines` extends `Node3D` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | Vector2i | Vector2i(32, 32): set = set_grid_size |
| `spacing` | float | 0.5: set = set_spacing |
| `line_thickness` | float | 0.05: set = set_line_thickness |
| `line_height` | float | 3.0: set = set_line_height |
| `base_color` | Color | Color(0.1, 0.1, 0.8): set = set_base_color |
| `wave_color` | Color | Color(0.4, 0.8, 1.0): set = set_wave_color |
| `speed` | float | 2.0: set = set_speed |
| `frequency` | float | 0.5: set = set_frequency |
| `amplitude` | float | 3.0: set = set_amplitude |

### Dependencies

- `res://algorithms/wavefunctions/vertical_wave_lines/wave_lines.gdshader`

### Key Methods

- `set_line_height(val: float)`
- `_ready()`
- `_process(_delta: float)` -- Shader handles animation via TIME, but we can pass other uniforms if needed manually
- `_setup_system()`
- `_update_grid()`
- `_update_uniforms()`
- `set_grid_size(val: Vector2i)`
- `set_spacing(val: float)`
- `set_line_thickness(val: float)`
- `set_base_color(val: Color)`
- `set_wave_color(val: Color)`
- `set_speed(val: float)`
- `set_frequency(val: float)`
- `set_amplitude(val: float)`
- `_exit_tree()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- wavefunctions

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
