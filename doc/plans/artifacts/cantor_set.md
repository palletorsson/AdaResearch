# Artifact: cantor_set

> cantor(bar) = [bar.left_third, bar.right_third], remove middle third, repeat. Measure → 0.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

cantor_set

*The Cantor set removes everything and keeps everything — zero length, uncountable points, the paradox made physical.*

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [13.0, 4.0, 14.0] (world_scale)
- **Interaction:** To watch something disappear — each iteration removes a third, and the bars physically fall into place with RigidBody3D
- **Critical Parameter:** max_iterations — at 5 iterations, each bar is 1/243 of the original, yet the set is still uncountably infinite
- **What Emerges:** The physical stacking of bars — gravity gives the abstract Cantor set a visceral presence as bars clatter into position
- **Triggers:** iteration_interval tick → split all current bars → new bars fall with physics → hue shifts per generation

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/cantorset/cantor_set.gd` (194 lines) |
| **Scene** | `res://algorithms/fractals/cantorset/cantor_set.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `iteration_interval` | float | 2.0 |
| `max_iterations` | int | 5 |
| `auto_start` | bool | true |
| `initial_bar_length` | float | 9.0 |
| `bar_thickness` | float | 0.3 |
| `vertical_spacing` | float | 1.5 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `create_initial_bar()`
- `create_bar(position: Vector3, length: float)` -- Create RigidBody3D
- `perform_iteration()` -- Check if we've reached the maximum
- `get_bar_length(bar: RigidBody3D)`
- `start_generation()`
- `stop_generation()`
- `reset()` -- Clear all bars
- `step()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_CantorSet
- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_Synthesis

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_CantorSet | [3,5] | `-` |
| Fractal_Synthesis | [2,6] | `-` |

### Relationships

- Foundation for sierpinski_triangle (2D Cantor) and menger_sponge (3D Cantor); demonstrates measure-zero sets
- Needs: VR iteration trigger [missing], physics interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
