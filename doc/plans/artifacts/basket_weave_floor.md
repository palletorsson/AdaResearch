# Artifact: Basket Weave Floor

> a floor encoding the Roman textile-to-stone translation

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pompeii, weave, textile | **Themes:** arrays, art, cultural_computation

Procedural Pompeii mosaic floor with basket weave (opus spicatum) pattern. Alternating horizontal and vertical pairs of 2:1 rectangular tiles in a checkerboard arrangement create a woven textile appearance. Dark and light pairs interlock like fabric.

*a pair of rectangles rotated 90 degrees becomes a completely different surface*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see woven fabric frozen in tesserae
- **Critical Parameter:** tiles_short — number of weave cells on the short axis
- **What Emerges:** the insight that rotation alone generates pattern from uniformity
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/basket_weave_floor.gd` (266 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/basket_weave_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `BasketWeaveFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.0, 0.75) |
| `tiles_short` | int | 8 |
| `border_widths` | Array[int] | [2, 1, 1] |
| `border_motif` | int | BorderMotifs.Motif.SAWTOOTH |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.06 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`
- `res://commons/artifacts/pompeii_mosaic_floor/border_motifs.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`

### Grid Config

Accepts: `border_widths`, `floor_size`, `tiles_short`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- pompeii_mosaic_floor (sibling pattern), pattern_maker_station (web editor)
- Needs: [implemented] procedural mesh, border bands, basket weave field, grout

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
