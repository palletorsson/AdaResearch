# Artifact: Atmospheric Monitoring Station

> reading(t) = base + variation * sin(frequency * t * TAU) + noise

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** probability, waves | **Themes:** oscillation, physics, waves

Three analog gauges at different frequencies: pressure (0.05 Hz slow drift), temperature (0.02 Hz), humidity (0.03 Hz). Demonstrates how multiple independent oscillations create complex patterns. Status lights indicate normal ranges. Real atmospheric data oscillates similarly.

*The atmosphere is a superposition of slow oscillations that instruments make legible.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** Watch laboratory instruments slowly drift through atmospheric measurements
- **Critical Parameter:** pressure_frequency — controls the speed of atmospheric drift cycles
- **What Emerges:** the feeling of a living laboratory where instruments never fully settle
- **Triggers:** time drives sinusoidal variation in pressure, temperature, humidity needles

## Architecture

| | |
|---|---|
| **File** | `commons/lab/atmosphericmonitoring/atmosphericmonitoring.gd` (272 lines) |
| **Scene** | `res://commons/lab/atmosphericmonitoring/atmosphericmonitoring.tscn` |
| **Registry** | `wavefunctions.json` |
| **Class** | `LabAtmosphericMonitoring` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `pressure_base` | float | 1013.25 |
| `pressure_variation` | float | 5.0 |
| `pressure_frequency` | float | 0.05 |
| `temp_base` | float | 22.0 |
| `temp_variation` | float | 0.5 |
| `temp_frequency` | float | 0.02 |
| `humidity_base` | float | 45.0 |
| `humidity_variation` | float | 5.0 |
| `humidity_frequency` | float | 0.03 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_update_status_lights(pressure: float, temp: float, humidity: float)`
- `_build_station()`
- `_create_gauge(pos: Vector3, accent_color: Color, gauge_name: String)`
- `get_pressure()`
- `get_temperature()`
- `get_humidity()`
- `set_alert_conditions(pressure_alert: float, temp_alert: float, humidity_alert: float)`

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Synthesis_Lab

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Synthesis_Lab | [8,5] | `:180:1` |

### Relationships

- depends on sine-driven gauge animation; contrasts with multimeter (AC measurement vs environmental monitoring); unlocks lab atmosphere
- Needs: VR needle observation [has], alert threshold adjustment [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
