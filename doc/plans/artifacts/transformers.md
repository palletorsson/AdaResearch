# Artifact: Transformers

> Transformer architecture visualization — self-attention, multi-head attention, positional encoding. The architecture behind GPT, BERT, and the current AI revolution.

## Context

**Category:** machinelearning | **Complexity:** advanced
**Tags:** transformer, self_attention, multi_head, positional_encoding, deep_learning | **Themes:** ai, biology, machine_learning

Transformer architecture visualization — self-attention, multi-head attention, positional encoding. The architecture behind GPT, BERT, and the current AI revolution.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [24.0, 26.0, 14.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/transformers/transformers.gd` (353 lines) |
| **Scene** | `res://algorithms/machinelearning/transformers/transformers.tscn` |
| **Registry** | `machinelearning.json` |
| **Class** | `TransformersShowcase` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `sequence_length` | int | 8 : set = _set_sequence_length |
| `attention_heads` | int | 4 |
| `animation_speed` | float | 1.0 |
| `pulse_intensity` | float | 1.0 |
| `training_rate` | float | 0.1 |
| `auto_train` | bool | true |
| `color_input_token` | - | - |
| `color_output_token` | - | - |
| `color_encoder` | - | - |
| `color_decoder` | - | - |
| `color_flow` | - | - |

### Key Methods

- `_set_sequence_length(v: int)`
- `_rebuild_tokens()` -- Clear and recreate token visuals when sequence length changes
- `_ready()` -- Initialize Transformers visualization
- `_process(delta: float)`
- `create_input_tokens()` -- Create input sequence tokens with gold metallic materials
- `create_output_tokens()` -- Create output sequence tokens with blue metallic materials
- `create_flow_particles()` -- Create glowing data flow particles that travel through the transformer pipeline
- `setup_training_metrics()` -- Initialize loss and accuracy meters
- `animate_transformer_layers(delta)` -- --- Encoder Layers: green glow intensifies with training ---
- `animate_attention_heads(delta)` -- Animate multi-head attention core
- `animate_positional_encoding(delta)` -- Animate positional encoding core
- `animate_data_flow(delta)` -- Animate flow particles
- `update_training_metrics(delta)` -- Update loss meter
- `set_training_progress(progress: float)`
- `set_loss_value(loss: float)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
