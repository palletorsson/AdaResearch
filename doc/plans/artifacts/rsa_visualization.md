# Artifact: RSA Encryption

> RSA public-key cryptography visualization — prime factorization, modular exponentiation, and the one-way trapdoor that makes secure communication possible. Easy to multiply primes, nearly impossible to factor back.

## Context

**Category:** algorithms | **Complexity:** advanced
**Tags:** rsa, cryptography, encryption, prime, modular_arithmetic, public_key | **Themes:** algorithms, misc

RSA public-key cryptography visualization — prime factorization, modular exponentiation, and the one-way trapdoor that makes secure communication possible. Easy to multiply primes, nearly impossible to factor back.

## Design

- **Visual:** material: vertex_color, animation: vertex
- **Scale:** [1.0, 12.0, 13.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/cryptography/rsa/rsa_visualization.gd` (792 lines) |
| **Scene** | `res://algorithms/cryptography/rsa/rsa_visualization.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Class** | `RSAVisualization` extends `Node3D` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_build_known_primes()` -- Sieve of Eratosthenes up to 1200
- `_is_known_prime(val: int)`
- `_create_material()`
- `_create_meshes()` -- Prime search grid
- `_create_labels()`
- `_make_label(pos: Vector3, size: int)`
- `_create_controllers()`
- `_init_prime_grid()`
- `_pick_message()`
- `_pick_message_by_index(idx: int)`
- `_set_prime_range(idx: int)`
- `_restart()`
- `_process(delta: float)`
- `_update_prime_search(dt: float)`

### Grid Config

Accepts: `animation_speed`, `message_index`, `prime_range`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
