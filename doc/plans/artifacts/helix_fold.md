# Artifact: Helix Fold

> a triangle strip coils from flat ribbon into a helix via sin/cos — DNA, springs, spiral shells

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** fold_system, helix, spiral, sin, triangle_strip, stages | **Themes:** fold_system

A triangle strip coils from flat zigzag ribbon into a helix via sin/cos. Five stages showing the fold progression. DNA springs spiral shells.

*sin(t) and cos(t) are enough to fold a flat ribbon into a living spiral.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a living coil that winds tight when folded and unfurls into a flat banner when open
- **Critical Parameter:** fold_amount — 0.0 is flat zigzag strip, 1.0 is tight helix coil
- **What Emerges:** the helix pitch, radius, and sine modulation create organic coiling motion
- **Triggers:** fold_amount interpolates every vertex between flat position and helical position

## Architecture

| | |
|---|---|
| **File** | `commons/fold_system/helix_fold.gd` (135 lines) |
| **Scene** | `res://commons/fold_system/helix_fold.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `HelixFold` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `segment_count` | int | 24 |
| `strip_width` | float | 0.03 |
| `strip_length` | float | 0.6 |
| `helix_radius` | float | 0.06 |
| `helix_turns` | float | 3.0 |
| `helix_height` | float | 0.25 |
| `sine_modulation` | float | 0.3 |

### Dependencies

- `res://commons/primitives/shared/primitive_mesh_builder.gd`

### Key Methods

- `_ready()`
- `_generate_strip()`
- `_build_stages()`
- `apply_grid_config(_config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Folding: Same Faces, Only Positions Change** (fold_system) -- map: Folding_Zoo

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Folding_Zoo | [4,2] | `:0:0.5` |

### Relationships

- Needs: PrimitiveMeshBuilder [reuse]; triangle strip generation [built here]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
