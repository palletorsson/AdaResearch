# Artifact: shelf

> shelf

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** color | **Themes:** color, perception

shelf

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 2.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/grabcolor/shelf.gd` (382 lines) |
| **Scene** | `res://algorithms/color/grabcolor/shelf.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Dependencies

- `res://commons/primitives/panels/DigitalPaper/grab_paper_text.tscn`

### Key Methods

- `_ready()` -- Offset the entire shelf
- `create_shelf_with_csg()` -- Create wood material
- `create_compartment_cutouts(parent_combiner: CSGCombiner3D)` -- Calculate starting position for compartments
- `create_compartment_colliders()` -- Create thin floor colliders for each compartment so items can rest inside the shelf.
- `create_compartment_markers()` -- Create invisible markers and snap points at the center of each compartment
- `clear_snap_assets()`
- `create_extra_snap_points()`
- `populate_compartment_texts()`
- `do_notify_missing_texts(filled: int)`
- `get_compartment_floor(row: int, col: int)`
- `get_compartment_text(row: int, col: int)`
- `get_compartment_snap_point(row: int, col: int)`
- `get_compartment_marker(row: int, col: int)`
- `get_compartment_center(row: int, col: int, global: bool = true)`
- `get_compartment_floor_height(row: int, col: int)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- color

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Context | [27,2] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
