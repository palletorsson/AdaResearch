# Artifact: Reaction Diffusion

> Turing pattern generation using reaction-diffusion systems â€” the Gray-Scott model on a 2D grid producing spots, stripes, and labyrinthine patterns from two interacting chemicals.

## Context

**Category:** proceduralgeneration | **Complexity:** intermediate
**Tags:** reaction_diffusion, turing_pattern, gray_scott, morphogenesis, growth | **Themes:** generation, procedural

Turing pattern generation using reaction-diffusion systems â€” the Gray-Scott model on a 2D grid producing spots, stripes, and labyrinthine patterns from two interacting chemicals.

## Design

- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/patterngeneration/reactiondiffusion/reaction_diffusion.gd` (282 lines) |
| **Scene** | `res://algorithms/proceduralgeneration/growth_systems/reactiondiffusion/2d_in_3d_turing_pattern_reaction_diffusion.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | rack_templates_panel |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`
- `res://algorithms/patterngeneration/reactiondiffusion/reactiondiffusion.gdshader`

### Key Methods

- `_ready()`
- `_create_subviewports()` -- Create SubViewports as children for ping-pong computation
- `_create_display_surface()` -- Create a 0.5m x 0.5m QuadMesh to show the simulation output
- `_create_vr_controls()`
- `_setup_simulation()` -- 1. Create ShaderMaterial and load the reaction-diffusion shader
- `add_initial_seed()`
- `_process(_delta: float)`
- `_on_add_noise()`
- `_on_reset()`
- `_on_add_seeds()`
- `_on_big_blast()`
- `add_random_noise()`
- `add_seed_points()`
- `reset_simulation()`
- `big_seed_blast()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Procedural Generation (All Maps)** (proceduralgeneration_all) -- map: ProceduralGeneration_Reaction_Diffusion_Systems
- **Soft Bodies & Morphogenesis: Matter That Finds Its Shape** (softbodies) -- map: ProceduralGeneration_Reaction_Diffusion_Systems

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| AutoGenTest2 | [4,12] | `-` |
| Morphogenesis_Intro | [1,3] | `-` |
| ProceduralGeneration_Reaction_Diffusion_Systems | [6,5] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
