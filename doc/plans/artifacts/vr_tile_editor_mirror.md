# Artifact: VR Tile Editor Mirror

> VR workspace = PatternTilePuzzle (tilted editing board at desk height) + live-updating floor carpet (MultiMesh tile texture regenerated on every pattern_changed signal) + optional wall preview panel

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** vr, tiling, mirror, kaleidoscope, patterns, carpet, interactive | **Themes:** arrays, data_structures

VR tile editor starting in kaleidoscope mirror mode. Design symmetric patterns and see them tile across a floor carpet.

*the tile editor is a live proof that a 4×4 array, held in hand, can generate an infinite floor — the editing board is the source code and the carpet is the running program*

## Design

- **Visual:** material: generated_texture, animation: static
- **Interaction:** to design a floor while standing on it — to grab a colored cube, place it on the tilted editing board, and see the tile pattern propagate immediately across the floor beneath your feet, closing the loop between authorship and inhabitation
- **Critical Parameter:** carpet_repeats — controls how many times the source tile repeats across the floor; at Vector2i(8,8) the floor becomes a demonstration of infinite repetition; at (2,2) the relationship between source and repeat is obvious
- **What Emerges:** the live-update loop creates an authorship experience that traditional pattern editors lack — you feel yourself as both designer and inhabitant simultaneously, which embodies the array's core claim about index-to-value mapping
- **Triggers:** PatternTilePuzzle emits cell_changed → _update_carpet_texture regenerates the carpet ImageTexture; VR BY buttons cycle wallpaper lattice; VR AX buttons cycle group within lattice; trigger toggles symmetry mode

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/arrays/vr_tile_editor.gd` (370 lines) |
| **Scene** | `res://commons/primitives/arrays/vr_tile_editor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `VRTileEditor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `tile_size` | int | 4 |
| `repeat_mode` | int | 3 |
| `carpet_size` | Vector2 | Vector2(3.0, 3.0) |
| `carpet_repeats` | Vector2i | Vector2i(8, 8) |
| `carpet_offset` | Vector3 | Vector3(0.0, 0.01, -1.5) |
| `grid_height` | float | 0.9 |
| `grid_tilt` | float | -30.0 |
| `show_wall_preview` | bool | true |
| `wall_preview_size` | Vector2 | Vector2(2.0, 2.0) |

### Dependencies

- `res://commons/primitives/arrays/pattern_tile_puzzle.tscn`

### Key Methods

- `_ready()`
- `_create_puzzle()`
- `_apply_initial_config()`
- `_create_floor_carpet()`
- `_update_carpet_texture()`
- `_create_wall_preview()`
- `_create_labels()` -- Title label
- `_update_mode_label()`
- `_find_controllers()`
- `_find_node_recursive(node: Node, name_contains: String)`
- `_on_button_pressed(button: String, controller_name: String)`
- `_on_cell_changed(_x: int, _y: int, _color_index: int)`
- `_on_mode_changed(_mode: int)`
- `_on_group_changed(_group: int)`
- `_unhandled_input(event: InputEvent)`

### Grid Config

Accepts: `carpet_repeats_x`, `carpet_repeats_y`, `carpet_size_x`, `carpet_size_y`, `grid_height`, `grid_tilt`, `show_wall_preview`

## Curriculum Position

### Sequences

- **Array Tutorial Sequence** (array_tutorial) -- map: Array_Patterns

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Array_Patterns | [1,5] | `:0:-0.5` |
| Pattern_Maker_Showcase | [2,6] | `-` |

### Relationships

- wraps PatternTilePuzzle as the editing logic; vr_tile_editor_mirror token uses MIRROR_XY repeat mode by default; connects to array_carpet for carpet-only display without editing
- Needs: floor carpet MultiMesh [has]; wall preview panel [has]; label for current mode [has]; VR controller button routing [has via handle_vr_button forwarded to puzzle]; grab cubes for editing [has via PatternTilePuzzle]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
