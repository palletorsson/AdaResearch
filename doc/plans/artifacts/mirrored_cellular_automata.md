# Artifact: Mirrored Cellular Automata

> Life-like CA with forced quad/octant/rotational symmetry — pattern = rule + mirror

## Context

**Category:** emergence | **Complexity:** intermediate
**Tags:** cellular_automata, emergence, pattern | **Themes:** biology, cellular_automata, computation, emergence

Symmetric cellular automata pattern generator.

*Symmetry is not decoration — it is a second rule system layered on the first.*

## Design

- **Scale:** [4, 1, 4] (xlarge)
- **Interaction:** To tile the floor with living kaleidoscope patterns that breathe between order and dissolution
- **Critical Parameter:** symmetry_type — quad, eight-way, or rotational mirror fundamentally changes the visual grammar
- **What Emerges:** Mandala-like forms from stochastic birth/death plus symmetry enforcement — no mandala was coded
- **Triggers:** Switching symmetry mode transforms the same rules into radically different visual languages

## Architecture

| | |
|---|---|
| **File** | `algorithms/proceduralgeneration/growth_systems/mirroredcellularautomata/mirrored_cellular_automata.gd` (300 lines) |
| **Scene** | `res://algorithms/proceduralgeneration/growth_systems/mirroredcellularautomata/mirror_cellular_texture_for_3d.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | int | 21 |
| `update_interval` | float | 0.2 |
| `auto_evolve` | bool | true |
| `symmetry_type` | int | 0 |
| `random_fill_percent` | float | 0.3 |
| `birth_probability` | float | 0.2 |
| `death_probability` | float | 0.1 |
| `fixed_border` | bool | true |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_rebuild_mesh()`
- `initialize_grid()`
- `generate_initial_pattern()`
- `generate_quadrant_pattern()`
- `generate_octant_pattern()`
- `generate_rotational_pattern()`
- `set_cell_with_symmetry(x: int, y: int, value: int)`
- `update_grid()`
- `count_live_neighbors(x: int, y: int)`
- `apply_quad_symmetry(new_grid: Array)`
- `apply_eight_way_symmetry(new_grid: Array)`
- `apply_rotational_symmetry(new_grid: Array)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Cellular Automata: Local Rules, Global Patterns** (cellularautomata) -- map: CA_GameOfLife

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CA_GameOfLife | [12,5] | `:0:-0.5` |

### Relationships

- Contrasts with persian_rug (deterministic symmetry vs stochastic). Feeds into CA_GameOfLife.
- Needs: VR symmetry selector [missing], birth/death probability sliders [missing], Label3D [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
