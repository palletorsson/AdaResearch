# Artifact: Rorschach Brain Scan

> Animated mirrored inkblot pattern like a brain CT scan

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** pattern | **Themes:** algorithms, misc

Animated mirrored inkblot pattern like a brain CT scan

## Design

- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/datastructures/rorschach_scan/rorschach_scan.gd` (204 lines) |
| **Scene** | `res://algorithms/datastructures/rorschach_scan/rorschach_scan.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `scan_speed` | float | 0.5 |
| `layer_count` | int | 64 |
| `resolution` | int | 256 |
| `ink_color` | Color | Color(0.9, 0.95, 1.0) |
| `background_color` | Color | Color(0.02, 0.02, 0.05) |
| `noise_scale` | float | 3.0 |
| `threshold` | float | 0.45 |
| `edge_glow` | Color | Color(0.3, 0.8, 1.0, 0.5) |
| `layers_per_frame` | int | 2 |

### Key Methods

- `_ready()`
- `_setup_noise()`
- `_setup_display()`
- `_start_incremental_generation()`
- `_process(delta: float)` -- Incremental generation - generate a few layers per frame
- `_generate_layer(layer_idx: int)`
- `_calculate_pixel(x: int, y: int, z_offset: float, half_width: int, center_y: int)`
- `_apply_brain_mask(img: Image, layer_idx: int)`
- `_add_scan_artifacts(img: Image, layer_idx: int)` -- Sparse scan lines (every 8 pixels instead of 4)
- `_update_display()`
- `set_scan_speed(speed: float)`
- `set_layer(layer_idx: int)`
- `regenerate()`
- `is_ready()`
- `get_generation_progress()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
