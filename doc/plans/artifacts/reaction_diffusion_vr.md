# Artifact: Reaction Diffusion (VR)

> Gray-Scott reaction-diffusion on GPU via ping-pong SubViewport rendering — feed_rate and kill_rate parameters select pattern regimes (coral, mitosis, spots, maze, waves)

## Context

**Category:** proceduralgeneration | **Complexity:** intermediate
**Tags:** reaction_diffusion, turing_pattern, morphogenesis, vr, growth | **Themes:** generation, procedural

Turing reaction-diffusion patterns in VR â€” two chemicals activating and inhibiting each other, producing spots, stripes, and labyrinthine patterns. The math behind animal markings.

*Turing's last paper proved that two diffusing chemicals can create pattern from homogeneity — no blueprint needed, only differential diffusion*

## Design

- **Interaction:** to watch Turing patterns bloom on a quad in front of you — spots splitting, stripes forming, mazes growing — all from two chemicals diffusing at different speeds
- **Critical Parameter:** feed_rate / kill_rate pair — together they select the pattern regime; tiny changes (0.001) cross phase boundaries between spots and stripes
- **What Emerges:** seed placement geometry determines the initial symmetry which the reaction-diffusion dynamics then break or amplify — same parameters, different seeds, different patterns
- **Triggers:** preset cycling (KEY_LEFT/RIGHT) loads predefined feed/kill pairs; KEY_SPACE drops a new seed point; KEY_R resets the simulation to uniform state

## Architecture

| | |
|---|---|
| **File** | `algorithms/proceduralgeneration/growth_systems/reactiondiffusion/reaction_diffusion_vr.gd` (277 lines) |
| **Scene** | `res://algorithms/proceduralgeneration/growth_systems/reactiondiffusion/reaction_diffusion_vr.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `feed_rate` | float | 0.037 |
| `kill_rate` | float | 0.06 |
| `resolution` | int | 256 |

### Dependencies

- `res://algorithms/patterngeneration/reactiondiffusion/reactiondiffusion.gdshader`

### Key Methods

- `_ready()`
- `_setup_viewports()` -- Create SubViewport A
- `_setup_display()` -- Create a 3D quad to display the pattern
- `_setup_shader()` -- Load the GPU shader
- `_setup_ui()` -- Create 3D label for current preset
- `_add_initial_seeds()` -- Add seed pattern after setup
- `_process(_delta)`
- `next_preset()`
- `previous_preset()`
- `_apply_preset()`
- `reset_simulation()` -- Reset to initial state
- `_input(event: InputEvent)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Relationships

- GPU shader complement to turing_pattern_generator's CPU simulation; absorbed into softbodies sequence from morphogenesis
- Needs: slider_horizontal [missing]; push_button [missing]; Label3D [has] (preset label)

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
