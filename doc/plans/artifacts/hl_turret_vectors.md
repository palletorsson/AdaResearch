# Artifact: Half-Life Turret - Vector Compendium

> D = T - P, cos(theta) = F.D/|F||D|, V = D-hat * speed, P' = P + V*dt. Nine vector operations in one machine.

## Context

**Category:** mathematics | **Complexity:** intermediate
**Tags:** vectors, all_operations, turret, game, interactive, subtraction, magnitude, normalize, dot_product, cross_product, projection, addition, scalar_multiplication, velocity, physics, shooting | **Themes:** vectors, game_mechanics, applied_math, physics

Half-Life inspired sentry turret demonstrating ALL vector operations in one coherent system: subtraction (direction to target), magnitude (distance/range check), normalization (unit direction), dot product (FOV check), cross product (rotation axis), projection (ground plane), scalar multiplication (bullet velocity), and velocity integration (bullet physics). Fires tracer rounds with visible velocity vectors!

*A turret does not think. It subtracts, normalizes, dots, crosses, and integrates. Intelligence is arithmetic applied with purpose.*

## Design

- **Visual:** material: unlit, animation: physics
- **Scale:** [11, 5, 20] (xlarge)
- **Interaction:** To make every vector operation serve a purpose — subtraction finds the target, dot product checks the FOV, cross product aims the barrel. Nothing is abstract here.
- **Critical Parameter:** field_of_view_degrees — the cone of awareness. Narrower FOV = more precise but more blind spots. The dot product threshold that separates "acquired" from "searching."
- **What Emerges:** Leading the target — bullets aim at predicted position using ball velocity. Hit accuracy emerging from the interplay of fire rate, bullet speed, and ball chaos.
- **Triggers:** Ball bounces → turret tracks, FOV check passes → laser activates and bullets fire, bullet hits ball → impulse + sparks + score, sliders → adjust FOV/rate/speed

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/hl_turret_vectors/hl_turret_vectors.gd` (1483 lines) |
| **Scene** | `res://commons/artifacts/hl_turret_vectors/hl_turret_vectors.tscn` |
| **Registry** | `vectors.json` |
| **Class** | `HLTurretVectors` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `turret_height` | float | 0.4 |
| `barrel_length` | float | 0.25 |
| `field_of_view_degrees` | float | 90.0: |
| `max_range` | float | 2.5 |
| `tracking_speed` | float | 2.0 |
| `shooting_enabled` | bool | true |
| `fire_rate` | float | 3.0: |
| `bullet_speed` | float | 4.0: |
| `bullet_lifetime` | float | 2.0 |
| `show_bullet_vectors` | bool | true |
| `ball_color` | Color | Color(0.2, 0.8, 1.0): |
| `ball_bounce` | float | 0.8 |
| `ball_radius` | float | 0.08 |
| `color_turret` | Color | Color(0.4, 0.42, 0.45) |
| `color_barrel` | Color | Color(0.25, 0.25, 0.28) |
| `color_laser` | Color | Color(1.0, 0.1, 0.1) |
| `color_fov_in` | Color | Color(0.2, 1.0, 0.3, 0.15) |
| `color_fov_out` | Color | Color(1.0, 0.3, 0.2, 0.15) |
| `color_direction` | Color | Color(1.0, 0.9, 0.3) |
| `color_projection` | Color | Color(0.3, 0.8, 1.0, 0.6) |
| `color_cross` | Color | Color(0.8, 0.4, 1.0) |
| `panel_color` | Color | Color(0.04, 0.04, 0.06, 0.92) |

### Dependencies

- `res://addons/godot-xr-tools/objects/pickable.gd`
- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config_data: Dictionary)`
- `_create_shared_materials()`
- `_create_environment()`
- `_create_ground()`
- `_create_arena_walls()`
- `_create_grid()`
- `_create_line_mesh(start: Vector3, end: Vector3, mat: Material)`
- `_create_range_indicator()`
- `_create_turret()`
- `_create_turret_base(base_mat: StandardMaterial3D)`
- `_create_turret_body(base_mat: StandardMaterial3D)`
- `_create_turret_barrel_and_laser()`
- `_create_ball_target()`
- `_create_ball_visual()`

### Grid Config

Accepts: `bullet_speed`, `field_of_view_degrees`, `fire_rate`, `max_range`, `shooting_enabled`, `turret_height`

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: VectorApplied

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| VectorApplied | [0,4] | `-` |

### Relationships

- Culmination of VectorApplied. Uses every operation taught in VectorBasics through VectorOperations. Contrasts with weather_vector_field (passive observation vs active targeting).
- Needs: VR grabbable ball [has], FOV/fire-rate/speed sliders [has], spawn button [has]. Missing: multiple turrets with crossfire.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
