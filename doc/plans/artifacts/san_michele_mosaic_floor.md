# Artifact: San Michele Mosaic Floor

> a floor that encodes the layered geometry of Roman villa luxury

## Context

**Category:** primitives | **Complexity:** advanced
**Tags:** 2d, pattern, mosaic, floor, roman, labyrinth, circular, meander, rosette | **Themes:** arrays, art, cultural_computation

Circular Roman mosaic floor inspired by Villa San Michele, Capri. Concentric zones read from outside in: circular vine scroll border, square Greek key meander frame, dark solid band, classical 7-circuit labyrinth, and a central terracotta rosette.

*every Roman mosaic floor is a map of the universe read from outside in*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and trace the path from vine border through labyrinth to rosette
- **Critical Parameter:** floor_radius — controls the overall size of the circular mosaic
- **What Emerges:** the understanding that Roman floors were concentric cosmograms — border, order, maze, center
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/san_michele_mosaic_floor.gd` (482 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/san_michele_mosaic_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `SanMicheleMosaicFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_radius` | float | 0.5 |
| `circle_segments` | int | 48 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`
- `_draw_filled_circle(verts: PackedVector3Array, cx: float, cz: float, radius: float, segs: int, y: float = 0.0)`
- `_draw_ring(verts: PackedVector3Array, cx: float, cz: float, r_outer: float, r_inner: float, segs: int, y: float = 0.0)`
- `_draw_rect_frame(verts: PackedVector3Array, x: float, z: float, w: float, h: float, band: float, y: float = 0.0)` -- Top band
- `_add_rect_verts(verts: PackedVector3Array, x: float, z: float, w: float, h: float, y: float = 0.0)`
- `_draw_meander_key(verts: PackedVector3Array, side: int, t: float, inner: float, outer: float, unit: float, y: float)` -- A meander key is a series of right-angle hooks.

### Grid Config

Accepts: `circle_segments`, `floor_radius`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- labyrinth_floor (labyrinth logic), star_rosette_floor (rosette geometry), wave_scale_floor (arc drawing)
- Needs: [implemented] procedural mesh, circular outline, vine border, meander frame, labyrinth, rosette

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
