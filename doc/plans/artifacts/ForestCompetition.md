# Artifact: Forest Ecosystem

> tree.dbh += nutrients * season * strategy * health; nutrients diffuse and deplete — competitive growth on shared soil

## Context

**Category:** procedural | **Complexity:** advanced
**Tags:** l_system, procedural, simulation | **Themes:** grammar, l_systems, procedural

Multiple L-System trees with competing growth strategies and health mechanics

*Competition emerges when grammars share resources — ecology is grammar plus scarcity.*

## Design

- **Visual:** material: vertex_color, animation: vertex
- **Scale:** [4.0, 6.0, 6.0] (environment)
- **Interaction:** To grow a forest where trees compete — watch aggressive species starve neighbors, conservative ones endure winters
- **Critical Parameter:** nutrient_supply vs competition_radius — together they determine whether the forest is cooperative or cutthroat
- **What Emerges:** Ecosystem dynamics from individual L-system trees sharing a diffusing nutrient grid — ecology from grammar
- **Triggers:** Spring → growth burst; Winter → dormancy; aggressive strategy → fast growth but resource depletion; crown overlap → health decline

## Architecture

| | |
|---|---|
| **File** | `algorithms/lsystems/Ecosystem/ForestCompetition.gd` (750 lines) |
| **Scene** | `res://algorithms/lsystems/Ecosystem/ForestCompetition.tscn` |
| **Registry** | `lsystems.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_trees` | int | 5 |
| `tree_generations` | int | 5 |
| `forest_radius` | float | 2.5 |
| `growth_delay` | float | 1.5 |
| `diffusion_rate` | float | 0.15 |
| `nutrient_supply` | float | 0.6 |
| `season_speed` | float | 0.3 |
| `competition_radius` | float | 1.2 |

### Dependencies

- `res://spatial_ui/parameter_controller_3d.tscn`
- `res://utils/lsystem.gd`

### Key Methods

- `_ready()`
- `_init_nutrient_grid()`
- `_diffuse_nutrients(delta: float)` -- Simple discrete diffusion: each cell averages with neighbors
- `_replenish_nutrients(delta: float)` -- Slow replenishment toward nutrient_supply, faster in spring/summer
- `_world_to_grid(world_pos: Vector3)`
- `_consume_nutrients(tree: EcoTree)`
- `_update_allometry(tree: EcoTree)` -- Power-law scaling: height ~ dbh^0.67, crown ~ dbh^0.5
- `_grow_dbh(tree: EcoTree, nutrient_available: float)` -- Growth rate depends on nutrients, season, strategy
- `_advance_season(delta: float)`
- `_setup_forest()`
- `_strategy_color(strategy: int)`
- `_create_lsystem(strategy: int, cls)`
- `_interpret_tree(tree: EcoTree)`
- `_apply_competition()`
- `_grow_step()`

### Grid Config

Accepts: `competition_radius`, `diffusion_rate`, `growth_delay`, `num_trees`, `nutrient_supply`, `season_speed`, `tree_generations`

## Curriculum Position

### Sequences

- **L-Systems: Rules Grow Structure** (lsystems) -- map: LSystems_Competition

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| LSystems_Competition | [2,3] | `-` |
| LSystems_ForestCompetition | [5,4] | `-` |

### Relationships

- Central to LSystems_Competition. Extends lsystem_tree (single tree → competing forest). Uses allometric power laws.
- Needs: VR diffusion/nutrients/speed/trees controllers [has], Label3D [has], health graph [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
