# Artifact: nakama_metaballs

> Kouhei Nakama-inspired organic metaballs — 60 animated blobs with subsurface scattering shader. Approximated mesh via sphere deformation.

## Context

**Category:** proceduralgeneration | **Complexity:** advanced
**Tags:** metaballs, organic, animated, cpu, working | **Themes:** 3d, isosurfaces, math

Kouhei Nakama-inspired organic metaballs — 60 animated blobs with subsurface scattering shader. Approximated mesh via sphere deformation.

## Design

- **Scale:** [1, 3, 6] (environment)

## Architecture

| | |
|---|---|
| **File** | `algorithms/proceduralgeneration/isosurfaces/metaballs/nakama_metaballs.gd` (355 lines) |
| **Scene** | `res://algorithms/proceduralgeneration/isosurfaces/metaballs/nakama_metaballs.tscn` |
| **Registry** | `isosurfaces.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_metaballs` | - | 60 |
| `container_size` | - | Vector3(5.0, 5.0, 5.0) |
| `surface_threshold` | - | 1.0 |
| `base_radius` | - | 0.4 |
| `radius_variation` | - | 0.3 |
| `movement_speed` | - | 0.5 |
| `material_color` | Color | Color(0.95, 0.85, 0.85) |

### Key Methods

- `_ready()` -- Initialize noise for organic movement
- `create_metaballs()` -- Create the metaballs with random positions and radii
- `create_shader_material()` -- Create a shader material for the metaball surface
- `_process(delta: float)`
- `update_metaballs(delta)` -- Move metaballs in organic patterns, but skip every other frame for performance
- `generate_mesh()` -- Generate a mesh based on metaballs using the marching cubes algorithm
- `create_metaball_proxy_mesh(st)` -- Create a surface that approximates what metaballs would look like
- `calculate_metaball_field(point)` -- Calculate the metaball field value at a given point
- `setup_lighting()` -- Set up lighting to highlight the organic forms
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Isosurfaces: Implicit to Explicit** (isosurfaces) -- map: ISO_Metaballs

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ISO_Metaballs | [9,7] | `:0:0:0.2` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
