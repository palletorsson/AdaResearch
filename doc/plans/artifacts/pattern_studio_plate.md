# Artifact: Pattern Studio Plate

> Full pattern design studio ported from Three.js PatternMaker3D. Cream plate with 4x4 paint grid, 3x3 palette, 6 sliders (motif, group, period, size, zone, site), control buttons, and real-time carpet output using all 17 wallpaper symmetry groups. 10 Italian motifs and 5 historical period palettes.

## Context

**Category:** primitives | **Complexity:** advanced
**Tags:** 2d, pattern, primitives, interactive, dieter_rams, wallpaper_groups, sliders | **Themes:** arrays, art, data_structures, symmetry

Full pattern design studio ported from Three.js PatternMaker3D. Cream plate with 4x4 paint grid, 3x3 palette, 6 sliders (motif, group, period, size, zone, site), control buttons, and real-time carpet output using all 17 wallpaper symmetry groups. 10 Italian motifs and 5 historical period palettes.

## Design


## Architecture

| | |
|---|---|
| **File** | `commons/primitives/arrays/pattern_studio_plate.gd` (1127 lines) |
| **Scene** | `res://commons/primitives/arrays/pattern_studio_plate.tscn` |
| **Registry** | `arrays.json` |
| **Pattern** | vr_interactable |

### Dependencies

- `res://commons/primitives/arrays/wallpaper_groups.gd`
- `res://commons/interactables/push_button.tscn`
- `res://commons/interactables/slider_horizontal.tscn`

### Key Methods

- `_ready()` -- Load checkerboard as default
- `_initialize_grid_data()`
- `set_cell(x: int, y: int, color_idx: int)`
- `get_cell(x: int, y: int)`
- `_build_plate()` -- Dark frame
- `_build_paint_grid()`
- `_build_palette()`
- `_build_sliders()`
- `_build_slider(label_text: String, values: Array, default_idx: int, y_pos: float)`
- `_set_slider_initial(slider_node: Node, norm: float)`
- `_build_copper_divider(width: float, pos: Vector3)`
- `_build_controls()`
- `_build_carpet()`
- `_update_carpet()`
- `_get_wallpaper_enum()` -- Map wallpaper_group index (into GROUPS) to WallpaperGroups.Group enum

### Signals

- `cell_changed`
- `group_changed`

### Grid Config

Accepts: `grid_size`, `motif`, `period`, `wallpaper_group`

## Curriculum Position

### Sequences

- mosaicanalysis
- array_tutorial

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
