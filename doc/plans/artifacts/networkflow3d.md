# Artifact: networkflow3d

> Edmonds-Karp max flow via BFS augmenting paths on a 3D spatial network — residual_capacity tracks remaining edge capacity, particles visualize flow along saturated edges

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** graph_theory, max_flow, edmonds_karp, bottleneck, residual_graph, network_flow, algorithm | **Themes:** algorithms, misc

Send flow through a spatial network of pipes. Edmonds-Karp repeatedly finds augmenting paths, particles reveal current flow, and pressure coloring exposes the nodes where capacity is constrained. Elevation penalties make the geometry matter without changing the underlying theorem: max flow is limited by the narrowest cut. It sets up the flow sequence by making bottlenecks visible.

*maximum flow through a network equals the minimum cut that separates source from sink — every bottleneck is simultaneously a capacity and a vulnerability*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [32.0, 17.0, 32.0] (xlarge)
- **Interaction:** to see flow move through a 3D pipe network — particles streaming from source to sink, faster through wide pipes, bottlenecked at narrow ones
- **Critical Parameter:** max_capacity — sets the upper bound on edge capacity; combined with gravity_effect (which reduces uphill capacity by 30%), determines the maximum flow achievable
- **What Emerges:** pressure visualization colors non-source/sink nodes by net incoming flow, revealing bottleneck nodes that accumulate pressure — network congestion made visible
- **Triggers:** calculate_max_flow runs Edmonds-Karp to saturation, then spawns flow particles proportional to each edge's current_flow; gravity_effect toggle rebuilds the network with elevation-penalized capacities

## Architecture

| | |
|---|---|
| **File** | `algorithms/graphtheory/networkflow3d/networkflow3d.gd` (545 lines) |
| **Scene** | `res://algorithms/graphtheory/networkflow3d/networkflow3d.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Class** | `NetworkFlow3D` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `network_size` | int | 12 |
| `connectivity_density` | float | 0.4 |
| `elevation_levels` | int | 4 |
| `spatial_bounds` | Vector3 | Vector3(40, 20, 40) |
| `gravity_effect` | bool | true |
| `max_capacity` | float | 50.0 |
| `min_capacity` | float | 5.0 |
| `flow_algorithm` | FlowAlgorithm | FlowAlgorithm.EDMONDS_KARP_3D |
| `pressure_simulation` | bool | true |
| `elevation_cost_factor` | float | 1.3 |
| `particles_per_unit_flow` | int | 8 |
| `particle_speed` | float | 2.0 |
| `particle_lifetime` | float | 4.0 |
| `show_flow_particles` | bool | true |
| `particle_size` | float | 0.1 |
| `show_pressure_colors` | bool | true |
| `animate_flow_pulses` | bool | true |
| `pipe_base_radius` | float | 0.15 |
| `flow_transparency` | float | 0.8 |
| `show_capacity_labels` | bool | true |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `setup_environment()`
- `setup_containers()`
- `setup_ui()`
- `create_3d_network()`
- `generate_3d_node_position(node_id: int)` -- Source at one corner
- `create_3d_edges()`
- `create_visualization()`
- `create_node_visuals()`
- `create_edge_visuals()`
- `create_flow_pipe(from_pos: Vector3, to_pos: Vector3, capacity: float)`
- `calculate_max_flow()`
- `run_edmonds_karp_3d()`
- `find_augmenting_path_bfs(residual_capacity: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Graph Theory: Connections Define Structure** (graphtheory) -- map: GT_Network_Analysis

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| GraphTheory_Network_Analysis | [6,5] | `-` |
| GT_Network_Analysis | [4,5] | `:0:0:0.6` |

### Relationships

- appears in GT_Network_Analysis alongside network_analysis; demonstrates max-flow min-cut duality; precedes push_relabel which solves the same problem differently
- Needs: slider_horizontal [missing]; push_button [missing]; Label3D [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
