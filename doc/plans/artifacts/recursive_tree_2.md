# Artifact: recursive_tree_2

> branch(pos, dir, len, depth) = cylinder(tapered) + branch_count * branch(end, rotated_dir, len * 0.7, depth+1)

## Context

**Category:** unknown | **Complexity:** advanced
**Tags:** tree | **Themes:** generation, procedural

recursive_tree_2

*A tree is not designed — it is a recursive function that ran until it ran out of energy.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To grow before your eyes — trunk first, then branches unfurl in real-time, leaf clusters blooming at the tips
- **Critical Parameter:** randomness_amount (0.15) — the variance injected into every angle and length, making each tree unique
- **What Emerges:** Natural-looking asymmetry from uniform random perturbation — no two trees are alike, yet all are recognizably trees
- **Triggers:** growth_interval tick → pop next branch from queue → create tapered cylinder → enqueue children

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/recursivetree/recursive_tree.gd` (343 lines) |
| **Scene** | `res://algorithms/fractals/recursivetree/recursive_tree.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `growth_interval` | float | 0.3 |
| `max_depth` | int | 10 |
| `auto_start` | bool | true |
| `initial_branch_length` | float | 2.0 |
| `initial_branch_thickness` | float | 0.2 |
| `length_reduction` | float | 0.7 |
| `thickness_reduction` | float | 0.65 |
| `branch_count` | int | 3 |
| `branch_angle_min` | float | 20.0 |
| `branch_angle_max` | float | 35.0 |
| `rotation_variation` | float | 360.0 |
| `add_randomness` | bool | true |
| `randomness_amount` | float | 0.15 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `create_materials()` -- Bark material - brown with subtle texture
- `create_trunk()`
- `perform_growth_step()`
- `create_branching_node(parent_data: Dictionary, branch_index: int)`
- `create_branch(data: Dictionary)`
- `create_tapered_cylinder(height: float, bottom_radius: float, top_radius: float)`
- `add_leaf_cluster(data: Dictionary)`
- `start_growth()`
- `stop_growth()`
- `reset()` -- Clear all children
- `step()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_RecursiveTrees

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_RecursiveTrees | [1,8] | `-` |

### Relationships

- Deterministic sibling to fractal_stochastic_tree; feeds into inverted_tree_cloud (same algorithm, flipped)
- Needs: VR growth speed slider [missing], prune-by-grab [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
