# Artifact: menu

> Stacked slanted-cube VR menu with animated hover, keyboard navigation, and theme switching.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** vr | **Themes:** core, primitives

Stacked slanted-cube VR menu with animated hover, keyboard navigation, and theme switching.

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/menu/menu.gd` (217 lines) |
| **Scene** | `res://commons/primitives/menu/menu.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `menu_radius` | float | 3.0 |
| `menu_items` | Array[String] | ["PLAY", "OPTIONS", "ABOUT", "QUIT"] |
| `base_colors` | Array[Color] | [ |

### Dependencies

- `res://commons/primitives/slantedcube/slantedcube.tscn`

### Key Methods

- `_ready()`
- `create_game_menu()` -- Create menu items using the slanted cube primitive
- `create_menu_item(index: int)` -- Load the slanted cube scene
- `position_menu_item(item: Node3D, index: int, total: int)` -- Stack buttons vertically instead of in a circle
- `setup_menu_navigation()` -- Add keyboard/gamepad navigation
- `_input(event)`
- `navigate_menu(direction: int)` -- Update selection
- `update_menu_selection()`
- `activate_selected_item()`
- `_on_menu_item_selected(item_text: String)`
- `start_game()` -- Load main game scene
- `open_options()` -- Load options scene
- `open_about()` -- Load about scene
- `quit_game()` -- Confirmation dialog could go here
- `close_menu()` -- Could return to previous scene or show pause menu

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
