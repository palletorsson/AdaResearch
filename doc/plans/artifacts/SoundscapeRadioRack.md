# Artifact: SoundscapeRadioRack

> station = floor(angle / station_width), clarity = 1 - |offset| / threshold

## Context

**Category:** waves | **Complexity:** beginner
**Tags:** audio, radio, tuning, soundscape, vr, interactive, frequency, analog | **Themes:** music, oscillation, physics, waves

Turn a physical wheel in VR to tune between soundscape radio stations. Station index maps from wheel angle, clarity from proximity to center — analog radio metaphor. Static noise between stations, clean signal at center. Every frequency band is a station; tuning is selecting which resonance to amplify.

*Every frequency band is a station; tuning is selecting which resonance to amplify.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** Turn a physical wheel in VR to tune between soundscape radio stations
- **Critical Parameter:** station_width (degrees per station) — determines tuning precision
- **What Emerges:** static noise between stations, clean signal at center — analog radio metaphor
- **Triggers:** wheel rotation angle maps to station index; proximity to center determines signal clarity

## Architecture

| | |
|---|---|
| **File** | `commons/audio/rack_configs/SoundscapeRadioRack.gd` (228 lines) |
| **Scene** | `res://commons/audio/rack_configs/SoundscapeRadioRack.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `station_presets` | Array[String] | [ |
| `noise_preset` | String | "minimal_drone.json" |

### Dependencies

- `res://commons/audio/systems/SciFiLoFiSoundscape.gd`

### Key Methods

- `_ready()`
- `_setup_soundscape_player()`
- `_connect_wheel_signals()`
- `_on_tuning_wheel_moved(angle: float)`
- `_on_volume_wheel_moved(angle: float)` -- Map 0-360 degrees to volume range
- `_update_radio_station()` -- Calculate station position from angle
- `_tune_to_station(station_index: int)`
- `_play_noise()`
- `_apply_volume()`
- `_update_display()`
- `_update_volume_display()`
- `_update_indicator(station_position: float)`
- `next_station()`
- `previous_station()`
- `set_station(index: int)`

### Signals

- `station_changed`
- `volume_changed`

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Synthesis_Lab

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Synthesis_Lab | [11,2] | `:90:1.4` |

### Relationships

- depends on SciFiLoFiSoundscape presets; contrasts with audio_catalog_tablet (browsing vs tuning); unlocks ambient audio curation
- Needs: VR hinge wheel [has], volume wheel [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
