# Artifact: Grid2D — Seeds

> Seeds automaton — explosive, ephemeral. Every cell lives one generation then dies. Electric cyan explosions from minimal seeds.

## Context

**Category:** substrate | **Complexity:** beginner
**Tags:** substrate, grid2d, seeds, cellular_automata | **Themes:** biology, grid, spatial

Seeds automaton — explosive, ephemeral. Every cell lives one generation then dies. Electric cyan explosions from minimal seeds.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1, 1, 2] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/substrates/grid2d/grid2d_substrate.gd` (345 lines) |
| **Scene** | `res://commons/substrates/grid2d/grid2d_substrate.tscn` |
| **Registry** | `grid2d.json` |
| **Class** | `Grid2DSubstrate` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_width` | int | 48 |
| `grid_height` | int | 48 |
| `step_interval` | float | 0.12 |
| `auto_play` | bool | true |
| `label_text` | String | "" |
| `algorithm` | Algorithm | Algorithm.GAME_OF_LIFE: |

### Dependencies

- `res://commons/substrates/grid2d/cartridges/cartridge_game_of_life.gd`
- `res://commons/substrates/grid2d/cartridges/cartridge_seeds.gd`
- `res://commons/substrates/grid2d/cartridges/cartridge_brians_brain.gd`
- `res://commons/substrates/grid2d/cartridges/cartridge_rule_1d.gd`
- `res://commons/substrates/grid2d/cartridges/cartridge_bfs.gd`
- `res://commons/substrates/grid2d/cartridges/cartridge_dfs_maze.gd`
- `res://commons/substrates/grid2d/cartridges/cartridge_wireworld.gd`
- `res://commons/substrates/grid2d/cartridges/cartridge_langtons_ant.gd`

### Key Methods

- `_ready()`
- `_load_cartridge()`
- `_process(delta: float)`
- `_advance_step()`
- `play()`
- `pause()`
- `toggle_play()`
- `single_step()`
- `reset()`
- `touch_cell(x: int, y: int)`
- `get_step_count()`
- `is_playing()`
- `_update_base_plate()`
- `_update_touch_area()`
- `_update_label()`

### Signals

- `generation_advanced`
- `cell_touched`
- `playback_changed`

### Grid Config


## Curriculum Position

### Sequences

- **Unused Artifacts Test** (testmaps) -- map: Test_Substrates

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Test_Substrates | [1,7] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
