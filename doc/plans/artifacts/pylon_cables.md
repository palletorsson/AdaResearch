# Artifact: Electric Pylon Cables

> Four electric pylons with swinging cables between them. Cables sway in the wind. Final cable drops to a transformer box on the ground. Lattice tower construction with insulators.

## Context

**Category:** waves | **Complexity:** advanced
**Tags:** waves | **Themes:** oscillation, physics, waves

Four electric pylons with swinging cables between them. Cables sway in the wind. Final cable drops to a transformer box on the ground. Lattice tower construction with insulators.

## Design

- **Visual:** material: standard, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/line_builder_3d/pylon_cables.gd` (323 lines) |
| **Scene** | `res://algorithms/wavefunctions/line_builder_3d/pylon_cables.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_pylons` | int | 4 |
| `pylon_spacing` | float | 8.0 |
| `pylon_height` | float | 6.0 |
| `pylon_base_width` | float | 1.5 |
| `pylon_top_width` | float | 0.5 |
| `pylon_color` | Color | Color(0.4, 0.4, 0.45) |
| `cables_per_span` | int | 3 |
| `cable_thickness` | float | 0.03 |
| `cable_sag` | float | 0.8 |
| `cable_color` | Color | Color(0.1, 0.1, 0.12) |
| `cable_segments` | int | 20 |
| `box_size` | Vector3 | Vector3(0.8, 0.6, 0.5) |
| `box_color` | Color | Color(0.3, 0.5, 0.3) |
| `wind_enabled` | bool | true |
| `wind_strength` | float | 0.3 |
| `wind_speed` | float | 1.5 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_create_pylons()`
- `_create_single_pylon()`
- `_add_cross_beams(pylon: Node3D, height: float, width: float, mat: Material)`
- `_create_cables()` -- Cables between each pylon pair
- `_create_cable(start: Vector3, end: Vector3)`
- `_generate_cable_mesh(start: Vector3, end: Vector3, wind_offset: float)`
- `_create_transformer_box()`
- `_update_cable_sway()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Wavefunctions_Cables | [3,4] | `:0:0` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
