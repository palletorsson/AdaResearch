# Artifact: Random Walker Machine

> Random walk simulation — agents taking stochastic steps, demonstrating how randomness underlies exploration in ML. From Brownian motion to Monte Carlo methods.

## Context

**Category:** machinelearning | **Complexity:** beginner
**Tags:** random_walk, stochastic, exploration, monte_carlo | **Themes:** ai, machine_learning

Random walk simulation — agents taking stochastic steps, demonstrating how randomness underlies exploration in ML. From Brownian motion to Monte Carlo methods.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [25.0, 7.0, 25.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/randomwalkermachine/randomwalkermachine.gd` (354 lines) |
| **Scene** | `res://algorithms/machinelearning/randomwalkermachine/randomwalkermachine.tscn` |
| **Registry** | `machinelearning.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_walkers` | int | 300 |
| `learning_rate` | float | 0.1 |
| `temperature` | float | 2.0: |
| `exploration_rate` | float | 0.3 |
| `circle_radius` | float | 10.0 |
| `evolution_interval` | int | 200 |
| `show_top_n_walkers` | int | 20 |

### Key Methods

- `_ready()`
- `setup_noise()`
- `generate_target_points()` -- Sample 100 points on a noise-disturbed circle
- `create_walker_agents()` -- Create 100 random walkers starting from center
- `setup_materials()` -- Enhanced materials with metallic sheen and stronger emission
- `visualize_targets()` -- Create target spheres
- `_process(_delta)` -- ── Update all walkers ───────────────────────────────────────────────
- `update_walkers()`
- `evolve_walkers()` -- Evolutionary step: keep top 30%, mutate middle 40%, replace bottom 30% with offspring
- `visualize_walkers()` -- Remove old walker visualizations and recreate with fitness-based coloring
- `_input(event: InputEvent)` -- Space key — randomness burst
- `experiment_with_randomness_injection()` -- Method to test different randomness injection strategies
- `log_performance_metrics()` -- Print current generation metrics to console
- `_create_stats_label()` -- Floating 3D label showing generation, fitness, and temperature in real-time
- `_update_stats_label_now()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| MachineLearning_Random_Walker_Machine | [6,5] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
