# Artifact: cube_subdivision

> cube -> 8 sub-cubes -> pick one -> repeat. Scale reduction 0.5 per level.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

cube_subdivision

*To subdivide is to choose — and the fractal is the history of every choice made at every scale.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 3.0, 6.0] (environment)
- **Interaction:** To be watched as it eats itself — one cube chosen, split open, its children exposed, then one of them chosen next
- **Critical Parameter:** random_corner — when true, every subdivision picks a different child, creating unpredictable growth; when false, it always drills the same corner
- **What Emerges:** Multi-scale color clouds from depth-indexed palette — no cube knows its neighbors but the ensemble reads as a fractal
- **Triggers:** Each subdivision_interval tick selects and detonates a cube; color shifts per depth generation

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/cubesubdivision/cube_subdivision.gd` (283 lines) |
| **Scene** | `res://algorithms/fractals/cubesubdivision/cube_subdivision.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `subdivision_interval` | float | 0.8 |
| `max_subdivisions` | int | 10 |
| `auto_start` | bool | true |
| `random_corner` | bool | true |
| `fixed_corner_index` | int | 7 |

### Dependencies

- `res://commons/primitives/cubes/cube_scene.tscn`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_find_initial_cube()` -- Look for the InitialCube node or any cube_scene instance
- `perform_single_subdivision()` -- Check if we've reached the maximum
- `subdivide_cube_return_children(cube: Node3D)`
- `_apply_color_to_cube(cube: Node3D, color: Color)` -- Find the mesh instance inside the cube
- `_highlight_target_cube(cube: Node3D)` -- Make the target cube glow brighter to show it's next
- `start_subdivision()`
- `stop_subdivision()`
- `reset()` -- Remove all cubes except initial
- `step()`
- `set_corner(index: int)`
- `set_random(enabled: bool)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_Recursion

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_Recursion | [5,3] | `-` |
| Fractal_Recursion | [5,4] | `-` |
| Fractal_Recursion | [5,5] | `-` |

### Relationships

- Foundation for menger_sponge (structured removal) and recursive_boolean_cube (Sierpinski pattern)
- Needs: VR grab-to-subdivide [missing], depth slider [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
