# Artifact: Waterbomb Enemy

> waterbomb_tessellation.roll() -> deploy(rigid_foldable_flower) -- compressed sphere blooming to attack

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** hazard, origami, physics | **Themes:** gameplay, hazards

True rigid-foldable origami enemy using waterbomb tessellation. Faces remain rigid during folding; geometry is computed from kinematic constraints. Rolls collapsed, unfolds to flower shape when attacking.

*every triangle face holds shape through unfolding -- rigidity in motion, origami made lethal.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a waterbomb rolling as compressed sphere, then blooming into rigid-foldable flower formation
- **Critical Parameter:** deployment state -- ROLLED (sphere) vs DEPLOYED (open waterbomb flower pattern)
- **What Emerges:** the crease pattern IS the creature -- waterbomb mathematics determine both forms
- **Triggers:** player proximity triggers bloom; rigid-foldable faces maintain shape through unfolding

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/armadillo_droideka/waterbomb_enemy.gd` (584 lines) |
| **Scene** | `res://commons/hazards/armadillo_droideka/waterbomb_enemy.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `WaterbombEnemy` extends `CharacterBody3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `radial_segments` | int | 12: |
| `ring_count` | int | 2: |
| `shell_radius` | float | 0.5: |
| `max_health` | float | 80.0 |
| `roll_speed` | float | 2.8 |
| `turn_speed` | float | 5.0 |
| `detection_radius` | float | 10.0 |
| `disengage_radius` | float | 16.0 |
| `shots_per_burst` | int | 3 |
| `fire_interval` | float | 0.35 |
| `fire_speed` | float | 14.0 |
| `fire_damage` | float | 14.0 |
| `contact_damage` | float | 10.0 |
| `detect_hold_time` | float | 0.35 |
| `deploy_duration` | float | 0.8 |
| `aim_duration` | float | 0.4 |
| `retract_duration` | float | 0.6 |
| `shell_color` | Color | Color(0.55, 0.85, 0.75, 1.0) |
| `crease_color` | Color | Color(0.2, 0.25, 0.22, 1.0) |
| `core_color` | Color | Color(0.15, 0.15, 0.18, 1.0) |
| `emission_color` | Color | Color(1.0, 0.4, 0.15, 1.0) |

### Dependencies

- `res://commons/hazards/armadillo_droideka/fire_bolt.tscn`

### Key Methods

- `_ready()`
- `_physics_process(delta: float)`
- `_process_roll(delta: float)`
- `_process_detect(delta: float)`
- `_process_deploy(_delta: float)`
- `_process_aim(delta: float)`
- `_process_fire(delta: float)`
- `_process_retract(_delta: float)`
- `_process_dead(delta: float)`
- `_set_state(new_state: State)`
- `_create_materials()`
- `_build_collision()`
- `_rebuild_shell()` -- Clear existing
- `_build_core()`
- `_create_crease_line(v0: Vector3, v1: Vector3, fold_sign: float)`

### Signals

- `fired_projectile`
- `enemy_destroyed`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo | [7,7] | `-` |

### Relationships

- origami family with miura_crawler, kresling_spire; sphere roller family
- Needs: CharacterBody3D [has]; waterbomb geometry [has]; rigid-foldable deployment [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
