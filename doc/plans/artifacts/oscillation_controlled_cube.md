# Artifact: Oscillation Controlled Cube

> cube.y = A*sin(t), cube.rotation = R*sin(t), cube.scale = lerp(min,max,sin(t))

## Context

**Category:** waves | **Complexity:** advanced
**Tags:** animation, qfep, visualization, wave | **Themes:** oscillation, physics, qfep, waves

Stage 4 of oscillation progression: The 'mario cube' responding to pendulum control. Three simultaneous mappings: y_offset → Y position, angular_velocity → rotation speed, amplitude → scale pulse. Visual guides show vertical rail, rotation arc, and real-time parameter breakdown. QFEP: One input, many outputs—oscillation as control signal.

*Translation, rotation, and scaling are three faces of the same oscillation projected onto different axes.*

## Design

- **Visual:** material: shader, animation: vertex
- **Scale:** [1, 2, 1] (compact)
- **Interaction:** Watch a cube translate, rotate, and scale simultaneously driven by a single oscillation source
- **Critical Parameter:** the connected oscillation signal — one driver controls all three transformations
- **What Emerges:** the unity of transformation types — all are manifestations of the same underlying oscillation
- **Triggers:** external oscillation_updated signal maps y_offset, angular_velocity, amplitude to transform channels

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/oscillation_controlled_cube/oscillation_controlled_cube.gd` (294 lines) |
| **Scene** | `res://commons/artifacts/oscillation_controlled_cube/oscillation_controlled_cube.tscn` |
| **Registry** | `wavefunctions.json` |
| **Class** | `OscillationControlledCube` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cube_size` | float | 0.3 |
| `translation_scale` | float | 0.2 |
| `rotation_scale` | float | 2.0 |
| `scale_range` | Vector2 | Vector2(0.8, 1.2) |
| `show_guides` | bool | true |
| `cube_color` | Color | Color(0.2, 0.6, 0.9) |
| `pendulum_path` | NodePath | - |

### Dependencies

- `res://commons/primitives/cubes/cube_scene.tscn`

### Key Methods

- `_ready()`
- `_connect_pendulum()`
- `_create_cube()`
- `_apply_cube_color()`
- `_create_label()`
- `_create_guides()`
- `_create_vertical_rail()`
- `_create_guide_marker(color: Color)`
- `_create_rotation_arc()`
- `_update_rotation_arc()`
- `_create_mapping_panel()`
- `_on_oscillation_updated(y_offset: float, angular_velocity: float, amplitude: float)` -- Translation: pendulum Y â†’ cube Y
- `_update_color_feedback(intensity: float)`
- `_process(_delta)` -- Fallback: if no pendulum connected, do default oscillation
- `apply_grid_config(config_data: Dictionary)`

### Grid Config


## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Intro

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Intro | [13,2] | `:180:0.5:1` |

### Relationships

- depends on control_pendulum oscillation signal; contrasts with rotating_cube_demo (multi-transform vs single rotation); unlocks transformation-as-oscillation
- Needs: VR observation [has], guide rails [has], connection to control_pendulum [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
