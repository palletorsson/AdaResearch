# Artifact: WFCCav

> WFCCav

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

WFCCav

## Design

- **Scale:** [16.0, 4.0, 16.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/proceduralgeneration/constraint_solvers/wave_function_collapse/WFCCave.gd` (660 lines) |
| **Scene** | `res://algorithms/proceduralgeneration/constraint_solvers/wave_function_collapse/WFCCave.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cave_size` | Vector3i | Vector3i(20, 8, 20) |
| `cell_size` | float | 4.0 |
| `generation_speed` | float | 0.02 |
| `auto_generate` | bool | true |
| `cave_density` | float | 0.3 |
| `entrance_probability` | float | 0.1 |
| `chamber_probability` | float | 0.05 |
| `water_probability` | float | 0.02 |
| `use_noise_seeding` | bool | true |
| `noise_scale` | float | 0.1 |
| `connectivity_bias` | float | 0.7 |

### Key Methods

- `_ready()`
- `setup_noise()`
- `setup_cave_tiles()`
- `initialize_cave_grid()`
- `start_generation()`
- `_process(delta: float)`
- `cave_wfc_step()` -- Get uncollapsed cells
- `_pick_weighted_cell(cells: Array[CaveCell])` -- For now, just pick randomly - could add location-based weighting
- `_pick_weighted_tile(cell: CaveCell)`
- `_update_cell_visual(cell: CaveCell)`
- `_propagate_cave_constraints(changed_cell: CaveCell)`
- `_post_process_cave()` -- Add additional cave features after main generation
- `_add_cave_lighting()` -- Add torches or bioluminescent lighting in chambers and tunnels
- `_add_cave_atmosphere()` -- Add fog or particle effects
- `_is_valid_cave_position(pos: Vector3i)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
