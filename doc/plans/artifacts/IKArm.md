# Artifact: IKArm

> FABRIK — reach backward from tip to target, then forward from base to tip. Iterate until convergence.

## Context

**Category:** unknown
**Tags:** physics, joints, inverse-kinematics

FABRIK inverse kinematics on a 4-segment arm — target orbits, arm solves backward and forward

*Specify where the hand should be. The math bends the arm. Animation becomes positioning endpoints.*

## Design

- **Interaction:** To be reached for. To solve the arm when the learner moves the target. To show that endpoints can constrain chains.
- **Critical Parameter:** Target position — reachable vs unreachable changes behavior. Number of iterations vs convergence speed.
- **What Emerges:** Natural-looking arm motion from simple backward-forward passes. Joint angles from endpoint constraint.
- **Triggers:** Target in range → arm solves smoothly, target out of range → arm stretches toward (graceful degradation)

## Architecture

| | |
|---|---|
| **File** | `algorithms/joint/11_ik_arm/IKArm.gd` (214 lines) |
| **Registry** | `physics_simulation.json` |
| **Pattern** | procedural |

### Key Methods

- `_build_demo()` -- Build arm segments as visual-only nodes (no physics — pure IK)
- `_process(_delta: float)`
- `_solve_fabrik(positions: Array[Vector3], target: Vector3)`
- `_up_for_direction(dir: Vector3)`
- `_draw_chain(positions: Array[Vector3], target: Vector3)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Joints** (joints) -- map: Joints_Kinematics

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Joints_Kinematics | [5,4] | `-` |

### Relationships

- Completes Joints_Kinematics. Contrasts with GimbalStabilizer (forward kinematics). Depends on chain concept from ChainSwing.
- Needs: VR grabbable target [missing — orbits automatically]. Segment count slider [missing]. Iteration count display [missing].

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
