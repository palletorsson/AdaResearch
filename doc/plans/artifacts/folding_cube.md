# Artifact: folding_cube

> Animated cube net folding into 3D cube. Shows how 6 connected squares become a volume. Cross net with hinge animation.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** 3d, topology | **Themes:** core, primitives

Animated cube net folding into 3D cube. Shows how 6 connected squares become a volume. Cross net with hinge animation.

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/cubes/folding_cube.gd` (343 lines) |
| **Scene** | `res://commons/primitives/cubes/folding_cube.tscn` |
| **Registry** | `primitives.json` |
| **Class** | `FoldingCube` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `face_size` | float | 0.35: |
| `face_thickness` | float | 0.02: |
| `face_gap` | float | 0.01: |
| `face_color` | Color | Color(0.9, 0.85, 0.8, 1.0): |
| `hinge_color` | Color | Color(0.3, 0.3, 0.35, 1.0): |
| `fold_duration` | float | 2.0 |
| `fold_progress` | float | 0.0: |
| `auto_fold` | bool | false: |
| `convert_to_rigidbody` | bool | true |
| `spawn_new_on_complete` | bool | true |
| `drop_height` | float | 0.3 |

### Key Methods

- `_ready()`
- `_create_material()`
- `_update_materials()`
- `_rebuild()` -- Clear existing
- `_create_net()`
- `_create_face(face_name: String, local_pos: Vector3)`
- `_apply_fold()`
- `animate_fold(duration: float = -1.0)`
- `animate_unfold(duration: float = -1.0)`
- `toggle_fold()`
- `set_fold_instant(folded: bool)`
- `_on_fold_complete()`
- `_on_unfold_complete()`
- `_convert_to_physics_cube()` -- Create a rigid body cube to replace the folded net
- `get_state()`

### Signals

- `fold_started`
- `fold_completed`
- `fold_progress_changed`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
