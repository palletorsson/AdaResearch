# Artifact: menger_sponge

> menger(cube) = 20 * menger(sub-cubes at kept positions), remove 7 cross-cubes per iteration. D = log(20)/log(3) ~ 2.727.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

menger_sponge

*The Menger sponge has infinite surface area and zero volume — it is a solid made entirely of holes.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [9.0, 3.0, 10.0] (environment)
- **Interaction:** To be walked inside — at 9m initial size and 2 iterations, the sponge has human-scale corridors through its cross-holes
- **Critical Parameter:** max_iterations — at 1 the cross-holes appear; at 2 the sub-holes within holes create a labyrinth; at 3 it would be 8000 cubes
- **What Emerges:** Corridors from three orthogonal axes — the removed crosses create walkable passages that penetrate the entire structure
- **Triggers:** subdivision_interval tick → find all cube_scene instances in scene tree → subdivide each into 20 kept positions → remove original

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/mengersponge/menger_sponge.gd` (199 lines) |
| **Scene** | `res://algorithms/fractals/mengersponge/menger_sponge.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `subdivision_interval` | float | 1.0 |
| `max_iterations` | int | 2 |
| `auto_start` | bool | true |

### Dependencies

- `res://commons/primitives/cubes/cube_scene.tscn`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `perform_iteration()` -- Check if we've reached the maximum
- `find_all_cube_scenes()`
- `_find_cubes_recursive(node: Node, cubes: Array)` -- Check if this node is a cube_scene (including "InitialCube")
- `subdivide_cube_menger(cube: Node3D)`
- `start_subdivision()`
- `stop_subdivision()`
- `reset()`
- `step()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_MengerSponge

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_MengerSponge | [5,6] | `-` |

### Relationships

- The 3D culmination of the removal fractals: Cantor (1D) → Sierpinski (2D) → Menger (3D); D ~ 2.727, more than a plane
- Needs: VR walkability [has at 9m scale], iteration control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
