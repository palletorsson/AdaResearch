# Artifact: Non-Teleological Evolution

> evolution without purpose — bodies persisting through drift alone

## Context

**Category:** machinelearning | **Complexity:** intermediate
**Tags:** evolution, non_teleological, drift, no_fitness, emergence, critical_theory | **Themes:** ai, biology, machine_learning

Bodies exist in a space with no fitness function. They reproduce based on proximity and energy (gained by exploring, lost by existing). Forms diversify without goal or optimization — just drift, variation, and persistence. No best creature. No progress narrative.

*there is no ladder of progress — only a bush of persistence*

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle
- **Scale:** [2, 1, 2] (standard)
- **Interaction:** players witness diversification without optimization or progress narratives
- **Critical Parameter:** energy_decay — how fast creatures lose energy by merely existing
- **What Emerges:** the realization that evolution needs no goal to produce complexity
- **Triggers:** instantiation, runs continuously

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/non_teleological_evolution/non_teleological_evolution.gd` (317 lines) |
| **Scene** | `res://algorithms/machinelearning/non_teleological_evolution/non_teleological_evolution.tscn` |
| **Registry** | `machinelearning.json` |
| **Class** | `NonTeleologicalEvolution` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `world_size` | float | 2.0 |
| `initial_population` | int | 30 |
| `max_population` | int | 120 |
| `energy_decay` | float | 0.15 |
| `energy_from_exploration` | float | 0.8 |
| `reproduction_threshold` | float | 3.0 |
| `mutation_strength` | float | 0.15 |
| `grid_resolution` | int | 20 |

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_init_visited_grid()`
- `_create_creature_mesh()`
- `_create_multimesh()`
- `_spawn_initial_population()`
- `_spawn_creature(pos: Vector3, col: Color, creature_size: float, speed: float, energy: float)`
- `_create_labels()`
- `_create_ground_plane()`
- `_process(delta: float)`
- `_update_multimesh()`
- `_update_stats()` -- Count unique "species" by color clustering (rough hue binning)

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ML_Evolution | [5,5] | `:0:2:0.6` |

### Relationships

- evolved_creatures (fitness-driven counterpart), 9_5_evolving_bloops_vr (similar)
- Needs: [implemented] MultiMesh creatures, energy model, reproduction, death, Label3D stats

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
