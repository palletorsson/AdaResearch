# Artifact: Joint Articulator

> joint_type(limb_i) = {hinge, ball, slider, revolute, prismatic, fixed} -- 6 limbs, 6 constraints

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** joints, constraints, articulation, physics | **Themes:** art, gameplay, hazards

Central body with 6 limbs, each using a different joint type — hinge, ball, slider, revolute, prismatic, fixed. Each limb moves per its constraint. Joint physics made visible.

*six limbs, six joint types -- the constraint vocabulary of all articulated motion.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** central body with 6 limbs, each demonstrating a different joint constraint as locomotion
- **Critical Parameter:** joint type per limb -- hinge restricts to 1 axis, ball allows 3, slider translates
- **What Emerges:** physics constraints as morphology -- the joint type IS the movement vocabulary
- **Triggers:** continuous locomotion cycle; each limb moves per its joint constraint; proximity triggers attack

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/joint_articulator/joint_articulator.gd` (287 lines) |
| **Scene** | `res://commons/hazards/joint_articulator/joint_articulator.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `JointArticulator` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `arm_length` | float | 0.3 |
| `arm_radius` | float | 0.02 |
| `joint_marker_radius` | float | 0.04 |
| `body_size` | float | 0.2 |
| `slider_extend_speed` | float | 1.5 |
| `revolute_spin_speed` | float | 5.0 |

### Key Methods

- `_on_ready()`
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()` -- Raise mesh root so creature walks on its lower limbs
- `_build_limb(index: int)`
- `_process_visual(delta: float)`
- `_animate_walk()` -- Hinge leg (index 0) - swing in Z axis
- `_animate_hinge(delta: float)` -- Hinge: swing in one axis only
- `_animate_ball(delta: float)` -- Ball: free rotation on multiple axes
- `_animate_slider(delta: float)` -- Slider: extend/retract along its axis
- `_animate_revolute(delta: float)` -- Revolute: continuous spin around the arm axis
- `_animate_prismatic(delta: float)` -- Prismatic: telescoping (scale the arm length)
- `_process_chase(delta: float)`
- `_attack_with_joints(delta: float)` -- Hinge: fast swipe
- `_on_damaged(_amount: float)` -- Briefly freeze all joints

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_5

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_5 | [3,7] | `-` |

### Relationships

- embodies joints sequence; pairs with spring_mass_bouncer (constraints vs elasticity)
- Needs: HazardCreatureBase [has]; 6 joint type implementations [has]; limb mesh [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
