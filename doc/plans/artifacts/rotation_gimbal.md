# Artifact: Rotation Gimbal

> Gimbal lock demonstrator with 3 nested rotation rings (X=red, Y=green, Z=blue). VR sliders control each Euler angle (0-360 degrees). When Y approaches 90 degrees, X and Z rings align and 'GIMBAL LOCK!' flashes — showing why Euler angles lose a degree of freedom and why quaternions matter. Displays live rotation matrix decomposition.

## Context

**Category:** transforms | **Complexity:** advanced
**Tags:** linear_algebra, topology, transforms, vr | **Themes:** geometry, math, transforms

Gimbal lock demonstrator with 3 nested rotation rings (X=red, Y=green, Z=blue). VR sliders control each Euler angle (0-360 degrees). When Y approaches 90 degrees, X and Z rings align and 'GIMBAL LOCK!' flashes — showing why Euler angles lose a degree of freedom and why quaternions matter. Displays live rotation matrix decomposition.

## Design

- **Visual:** material: vertex_color, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/rotation_gimbal/rotation_gimbal.gd` (528 lines) |
| **Scene** | `res://commons/artifacts/rotation_gimbal/rotation_gimbal.tscn` |
| **Registry** | `transforms.json` |
| **Class** | `RotationGimbal` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `ring_radius_outer` | float | 0.28 |
| `ring_radius_mid` | float | 0.22 |
| `ring_radius_inner` | float | 0.16 |
| `ring_tube_radius` | float | 0.008 |
| `gimbal_lock_threshold` | float | 5.0 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_build_gimbal()`
- `_create_ring(radius: float, tube_radius: float, color: Color, ring_name: String)`
- `_add_axis_tick(pivot: Node3D, radius: float, color: Color, direction: Vector3)` -- Small line segments at the ring's cardinal points to show axis orientation
- `_build_inner_box()`
- `_build_box_face_markers()` -- Small colored quads on +X, +Y, +Z faces of the box
- `_build_labels()` -- Title
- `_build_vr_controls()`
- `_sync_slider(slider: Node, normalized: float)`
- `_sync_all_sliders()`
- `_on_x_slider(_pos)`
- `_on_y_slider(_pos)`
- `_on_z_slider(_pos)`
- `_reset_angles()`
- `_update_gimbal()` -- Euler rotation order: X (outer) -> Y (middle) -> Z (inner)

### Grid Config

Accepts: `angle_x`, `angle_y`, `angle_z`, `gimbal_lock_threshold`

## Curriculum Position

### Sequences

- transformation

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
