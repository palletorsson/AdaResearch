# Artifact: Noise Terrain

> height(x,z) = (primary_simplex + detail_perlin × 0.2 + cellular_warp × bulginess × 0.3) × height_multiplier — three noise layers where cellular noise warps the sample coordinates of the primary noise (domain warping)

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** noise, procedural, terrain | **Themes:** probability, procedural, randomness

Interactive procedural terrain generation using various noise algorithms and height mapping

*domain warping is the act of sampling a function at a displaced location — when you warp the coordinate system with noise, you are not changing what the function computes but where it is asked*

## Design

- **Visual:** material: vertex_color, animation: transform
- **Scale:** [100, 10, 100] (xlarge)
- **Interaction:** to sculpt with sliders in VR and feel the terrain respond — to discover that bulginess does not add hills but distorts the coordinate system, creating a qualitatively different kind of organic shape
- **Critical Parameter:** bulginess — controls the strength of domain warping; at 0 the terrain is standard fBm, at high values it develops bulging protrusions and hollow valleys that feel biological rather than geological
- **What Emerges:** the queer color palette (pink→purple→cyan by height) encodes elevation as emotion rather than information — the terrain becomes a mood landscape; the NoiseBlobSpawner adds animated spheres that navigate the terrain surface
- **Triggers:** randomize_button generates entirely new seeds for all three noise generators; octaves slider changes primary fractal detail in real-time; contour toggle overlays topographic lines that reveal the height function's level sets

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/noiseterrain/noiseterrain.gd` (575 lines) |
| **Scene** | `res://algorithms/randomness/noiseterrain/noiseterrain.tscn` |
| **Registry** | `randomness.json` |
| **Class** | `QueerNoiseTerrain` extends `Node3D` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `terrain_size` | int | 100 |
| `terrain_resolution` | int | 200 |
| `noise_scale` | float | 1.0 |
| `height_multiplier` | float | 5.0 |
| `bulginess` | float | 1.0 |
| `octaves` | int | 4 |
| `color_shift` | float | 0.5 |

### Dependencies

- `res://algorithms/randomness/noiseterrain/heightmap_shader.gdshader`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `setup_noise_generators()`
- `setup_materials()` -- Create heightmap material
- `update_ui_button_states()` -- Set button states based on current material mode
- `setup_ui_connections()`
- `generate_terrain()`
- `clear_terrain()`
- `create_terrain_mesh()`
- `generate_height_at_position(x: float, z: float)` -- Safety check: ensure noise generators are initialized
- `generate_color_at_position(x: float, z: float, height: float)` -- Base queer color palette
- `calculate_normals()`
- `create_terrain_collision()`
- `apply_terrain_material()`
- `animate_terrain_colors(_delta)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Noise: Entropy with Memory** (noise) -- map: Noise_Perlin_Simplex

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Noise_Perlin_Simplex | [6,5] | `-` |

### Relationships

- precedes noise_terrain (terrain_generator.gd, time-animated); both are displayed in Noise_Perlin_Simplex map; domain warping connects to noisesphere concept; queer color scheme connects to QFEP
- Needs: NoiseScaleSlider [has]; HeightMultiplierSlider [has]; BulginessSlider [has]; OctavesSlider [has]; ColorShiftSlider [has]; ContourFrequencySlider [has]; BlobSpawnButton [has]; no VR slider_horizontal controls [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
