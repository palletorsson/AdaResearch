# Artifact: Evolved Creatures (Karl Sims)

> Physics-based creature evolution inspired by Karl Sims — bipedal bodies evolve locomotion through genetic algorithms. Morphology and behavior co-evolve, producing creatures that learn to walk through selection pressure.

## Context

**Category:** machinelearning | **Complexity:** advanced
**Tags:** evolution, karl_sims, creature, locomotion, genetic_algorithm, physics | **Themes:** ai, biology, education, machine_learning

Physics-based creature evolution inspired by Karl Sims — bipedal bodies evolve locomotion through genetic algorithms. Morphology and behavior co-evolve, producing creatures that learn to walk through selection pressure.

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [40, 1, 40] (xlarge)

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/carlsims/evolvedcreatures.gd` (317 lines) |
| **Scene** | `res://algorithms/machinelearning/carlsims/evolvedcreatures.tscn` |
| **Registry** | `machinelearning.json` |
| **Class** | `CarlsimsEvolution` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `random_seed` | int | 20241103 |
| `enable_random_restart` | bool | false |
| `enable_debug_prints` | bool | true |

### Key Methods

- `_ready()`
- `_create_status_label()`
- `_build_ground()`
- `spawn_initial_population()`
- `_grid_position(index: int)`
- `_spawn_creature(genome: CreatureGenome, start_pos: Vector3)`
- `_create_body(size: Vector3, mass: float, colour: Color)` -- Creates a physics body with emissive, slightly metallic material
- `_create_hip_joint(parent: RigidBody3D, child: RigidBody3D, offset: Vector3, leg_idx: int)`
- `_physics_process(delta: float)`
- `_evaluate_population()`
- `_evolve_generation()`
- `_crossover(a: CreatureGenome, b: CreatureGenome)`
- `_mutate(genome: CreatureGenome)`
- `_reset_population_with_genomes(genomes: Array[CreatureGenome])`
- `_clear_population()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| MachineLearning_Evolving_Creatures | [6,5] | `:0:-1.5` |
| ML_Evolution | [1,3] | `:0:2:0.6` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
