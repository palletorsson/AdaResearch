# Artifact: Bias Visualizer

> embedding_space(words, gender_axis) -> proximity_reveals_prejudice

## Context

**Category:** emergence | **Complexity:** intermediate
**Tags:** bias, statistics, critical_theory, visualization, fairness | **Themes:** art, commons, emergence, mixed

Interactive visualization of statistical and algorithmic bias — demonstrates how initial conditions, training data, or parameter choices create systematic skew in outputs. A critical theory artifact making invisible biases tangible.

*classification systems do not describe the world — they encode whose categories get to count*

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle
- **Scale:** [2.0, 1.0, 1.0] (compact)
- **Interaction:** see whose edge cases the training data forgot
- **Critical Parameter:** analogy_type — switches between gender-profession, gender-trait, and algorithmic redlining
- **What Emerges:** the uncomfortable recognition that "neutral" embeddings reproduce structural inequality
- **Triggers:** push-button selection cycles analogy modes; word proximity to gendered anchors shifts meaning

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/bias_visualizer/bias_visualizer.gd` (359 lines) |
| **Scene** | `res://commons/artifacts/bias_visualizer/bias_visualizer.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `BiasVisualizer` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `display_size` | float | 1.0 |
| `word_scale` | float | 0.05 |
| `male_color` | Color | Color(0.3, 0.5, 1.0) |
| `female_color` | Color | Color(1.0, 0.4, 0.6) |
| `neutral_color` | Color | Color(0.5, 0.9, 0.4) |
| `profession_color` | Color | Color(1.0, 0.8, 0.2) |
| `analogy_type` | int | 0: |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_base()`
- `_create_labels()`
- `_create_word_cloud()`
- `_create_connections()`
- `_create_vr_controls()`
- `_show_analogy()` -- Hide all: zero-scale transform and hide labels
- `_draw_connections(words: Array)`
- `_process(delta)`
- `_input(event)`
- `_exit_tree()`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Post-Crisis: Applied Limits** (postfoundationscrisis) -- map: CriticalAlgorithms_Algorithmic_Bias_Visualization

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CriticalAlgorithms_Algorithmic_Bias_Visualization | [4,2] | `-` |

### Relationships

- unlocks critical algorithmic thinking; depends on foundations crisis (incompleteness); contrasts ordered_grid (perfect pattern vs biased pattern)
- Needs: VR push buttons [has], rotation toggle [has], word grab interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
