# Artifact: Graphics Slideshow

> Non-interactive visualization display that cycles through frames every 5 seconds. Use #topic:vectors, forces, arrays, waves, randomness, or procedural

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** procedural, visualization | **Themes:** commons, mixed

Non-interactive visualization display that cycles through frames every 5 seconds. Use #topic:vectors, forces, arrays, waves, randomness, or procedural

## Design

- **Visual:** material: unlit, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/infoboards_3d/graphics_slideshow.gd` (230 lines) |
| **Scene** | `res://commons/infoboards_3d/graphics_slideshow.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Pattern** | infoboard |

### Exports

| Name | Type | Default |
|------|------|---------|
| `frame_duration` | float | 5.0 |
| `visualization_topic` | String | "vectors" |
| `auto_advance` | bool | true |

### Key Methods

- `_ready()`
- `_setup_display()` -- Create SubViewport for 2D rendering
- `_process(delta: float)`
- `_advance_frame()`
- `_get_root_control()` -- Get the root control we created in _setup_display
- `_load_visualization()` -- Remove existing visualization
- `_create_fallback_visualization(vis_type: String)`
- `set_topic(topic: String)`
- `next_frame()`
- `prev_frame()`
- `get_current_frame()`
- `get_total_frames()`
- `apply_grid_config(config_data: Dictionary)`
- `_initialize_visualization_data(vis_ctrl: Control)` -- Initialize particles and forces for visualizations that need them

### Grid Config


## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
