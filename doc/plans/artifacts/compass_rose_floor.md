# Artifact: Compass Rose Floor

> a floor that orients the player within the geometry of navigation

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, compass, radial, renaissance | **Themes:** arrays, art, cultural_computation

Procedural compass rose mosaic floor � 16-pointed mariner's star with alternating dark/light triangular wedges radiating from a terracotta center disc. Inner ring of 8 narrow intercardinal points, outer ring of 8 cardinal points, surrounded by concentric border bands.

*every compass rose admits that the world has no inherent up — only agreements*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see the mariner's compass that guided Renaissance voyages
- **Critical Parameter:** floor_radius — radius of the compass rose disc
- **What Emerges:** the understanding that direction is a human overlay on continuous space
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/compass_rose_floor.gd` (422 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/compass_rose_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `CompassRoseFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_radius` | float | 0.55 |
| `center_circle_fraction` | float | 0.08 |
| `cardinal_tip_fraction` | float | 0.92 |
| `intercardinal_tip_fraction` | float | 0.55 |
| `border_bands` | Array[int] | [2, 1, 1] |
| `border_tile_size` | float | 0.025 |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `color_accent` | Color | MosaicPalette.TERRACOTTA |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.04 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`
- `_add_ring_grout(verts: PackedVector3Array, radius: float, half_w: float, segments: int)`
- `_add_radial_grout(verts: PackedVector3Array, angle: float, r_inner: float, r_outer: float, half_w: float)`

### Grid Config

Accepts: `border_bands`, `cardinal_tip_fraction`, `center_circle_fraction`, `floor_radius`, `intercardinal_tip_fraction`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- radial_mandala_floor (sibling radial pattern), mosaic_palette (shared colors)
- Needs: [implemented] procedural mesh, border bands, 16-point star, center disc

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
