# Artifact: Plane Random

> Generates a planar surface with random vertex displacement, creating organic undulating terrain from flat geometry.

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** 3d, graph, probability, procedural, terrain | **Themes:** probability, procedural, randomness

Generates a planar surface with random vertex displacement, creating organic undulating terrain from flat geometry.

## Design

- **Visual:** material: shader, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/PlaneRandom/plane_random.gd` (170 lines) |
| **Scene** | `res://algorithms/randomness/PlaneRandom/plane_random.tscn` |
| **Registry** | `randomness.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `quads_x` | int | 20 |
| `quads_z` | int | 20 |
| `cell_size` | float | 1.0 |
| `lift_range` | float | 0.5 |
| `do_lift_on_ready` | bool | true |
| `initial_rounds` | int | 100 |
| `update_interval` | float | 1.0 |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `lift_random_vertex()` -- Randomly lift one vertex up/down within ±lift_range
- `_run_initial_rounds()` -- Run initial rounds of random vertex lifting
- `_process(delta: float)`
- `_build_nodes()`
- `_build_flat_grid()`
- `_commit_mesh()` -- Build mesh using SurfaceTool for normals
- `_update_collider()` -- Build smooth physics shape from vertex heights (HeightMapShape3D)
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- randomness

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Random_Plane | [1,0] | `-` |
| TestMap_Unused_15 | [3,3] | `:0:2` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
