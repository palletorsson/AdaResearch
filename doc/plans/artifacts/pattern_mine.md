# Artifact: Pattern Mine

> SDF(x) = contour_rings(mines) -- minefield where crossing zero-boundary detonates

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** sdf, pattern, mines, contour, detonation | **Themes:** gameplay, hazards

Mines with SDF contour rings marking danger zones. Cross the zero-contour and they detonate. Different ring shapes, pulsing indicators, and respawning after cooldown. Signed distance as survival instinct.

*the pattern is the weapon -- cross the zero-boundary and mathematics detonates.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** pulsing SDF contour rings from buried mines -- cross the zero-boundary and they explode
- **Critical Parameter:** mine positions / SDF threshold -- signed distance field defines safe vs lethal
- **What Emerges:** signed distance fields as danger topology -- contour lines border safe and lethal
- **Triggers:** player enters Area3D; SDF evaluated at position; zero-crossing triggers detonation

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/pattern_mine/pattern_mine.gd` (260 lines) |
| **Scene** | `res://commons/hazards/pattern_mine/pattern_mine.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `PatternMine` extends `Area3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `mine_count` | int | 8 |
| `field_radius` | float | 4.0 |
| `mine_radius` | float | 0.08 |
| `ring_radii` | Array[float] | [0.3, 0.6, 0.9] |
| `ring_tube_radius` | float | 0.01 |
| `detonation_radius` | float | 0.3 |
| `detonation_damage` | float | 25.0 |
| `respawn_time` | float | 5.0 |
| `mine_color` | Color | Color(1.0, 0.15, 0.1) |
| `inner_ring_color` | Color | Color(1.0, 0.9, 0.1) |
| `outer_ring_color` | Color | Color(0.2, 0.9, 0.3) |

### Key Methods

- `_ready()` -- Detection area covering the whole field
- `_process(delta: float)`
- `_build_mines()` -- Random position in a circle
- `_detonate_mine(mine: Dictionary)`
- `_activate_mine(mine: Dictionary)`
- `_on_body_entered(body: Node3D)`
- `_on_body_exited(body: Node3D)`
- `_find_player()`
- `take_damage(amount: float)` -- Zone hazard — indestructible
- `apply_damage(amount: float)`
- `damage(amount: float)`
- `configure(config: Dictionary)`
- `apply_grid_config(config: Dictionary)`

### Signals

- `enemy_destroyed`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_7

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_7 | [3,1] | `-` |

### Relationships

- embodies patterngeneration; SDF connects to isosurface_trap (implicit surfaces)
- Needs: Area3D [has]; mine array [has]; SDF contour viz [has]; detonation logic [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
