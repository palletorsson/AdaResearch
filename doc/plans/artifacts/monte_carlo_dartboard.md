# Artifact: Monte Carlo Dart Board

> π ≈ 4 · (points inside circle / total points) — Monte Carlo integration

## Context

**Category:** randomness | **Complexity:** intermediate
**Tags:** monte_carlo, pi, estimation, sampling, convergence, interactive | **Themes:** probability, randomness

Throw darts at a square with an inscribed circle. The ratio of inside/outside hits converges to pi/4. Live counter shows estimate of pi improving with each dart. Green dots inside circle, red dots outside.

*Randomness is not the enemy of precision — given enough samples, it converges to any truth.*

## Design

- **Visual:** material: unlit, animation: vertex
- **Interaction:** throw darts at a board and watch π emerge from chaos — green inside, red outside, gold answer
- **Critical Parameter:** max_darts — more darts means tighter convergence; error scales as 1/sqrt(n)
- **What Emerges:** the π estimate converges — randomness computes a transcendental number without algebra
- **Triggers:** _throw_dart() samples uniform (rx, ry) in [0,1]², tests dx²+dy² <= 0.25

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/monte_carlo_dartboard/monte_carlo_dartboard.gd` (417 lines) |
| **Scene** | `res://algorithms/randomness/monte_carlo_dartboard/monte_carlo_dartboard.tscn` |
| **Registry** | `randomness.json` |
| **Class** | `MonteCarloDartboard` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `board_size` | float | 0.6 |
| `board_thickness` | float | 0.03 |
| `board_height` | float | 1.3 |
| `dart_radius` | float | 0.005 |
| `dart_length` | float | 0.08 |
| `max_darts` | int | 500 |
| `auto_throw` | bool | true |
| `darts_per_second` | float | 3.0 |
| `color_board` | Color | Color(0.15, 0.15, 0.18) |
| `color_circle` | Color | Color(0.1, 0.15, 0.35) |
| `color_inside` | Color | Color(0.2, 0.9, 0.3) |
| `color_outside` | Color | Color(0.9, 0.25, 0.2) |
| `color_pi` | Color | Color(1.0, 0.85, 0.2) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_input(event: InputEvent)`
- `_create_board()` -- Board backing — a square
- `_create_circle_overlay()` -- Draw the inscribed circle using ImmediateMesh (line loop)
- `_throw_dart()`
- `_create_labels()`
- `_update_display()`
- `_create_vr_controls()`
- `_reset()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Randomness: Freedom from Pattern** (randomness) -- map: Randomness_Examples_of_Randomness

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Randomness_Examples_of_Randomness | [7,5] | `-` |

### Relationships

- contrasts with galton_board (integration vs distribution); feeds understanding of random sampling
- Needs: VR push buttons for THROW/AUTO/RESET [has]; dart visual markers [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
