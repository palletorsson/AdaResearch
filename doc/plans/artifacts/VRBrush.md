# Artifact: VRBrush

> VRBrush

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** commons | **Themes:** commons, mixed

VRBrush

## Design

- **Visual:** material: standard, animation: vertex
- **Scale:** [1.0, 2.0, 3.0] (room_scale)

## Architecture

| | |
|---|---|
| **File** | `tools/VRBrush.gd` (476 lines) |
| **Scene** | `res://tools/VRBrush.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `VRBrush` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `ray_length` | float | 5.0 |
| `brush_radius` | float | 1.0 |
| `max_edge_length` | float | 0.6 |
| `smooth_strength` | float | 0.3 |
| `subdivide_interval` | float | 1.0 |

### Key Methods

- `_ready()`
- `_setup_scene()` -- Get the EditableMesh from scene (should already exist in VRBrush.tscn)
- `_setup_grab_stick_collision()`
- `_on_grab_stick_touch_start(body: Node3D)` -- Check if it's touching the EditableMesh's StaticBody
- `_on_grab_stick_touch_end(body: Node3D)`
- `_on_grab_stick_area_entered(area: Area3D)`
- `_update_collision_shape()`
- `_create_brush_indicator()`
- `_process(delta: float)`
- `_perform_brush_action()` -- Update ray
- `_perform_grab_stick_brush_action()`
- `_subdivide_region(center: Vector3, radius: float, max_len: float)`
- `_round_region(center: Vector3, radius: float, strength: float)`
- `_commit_mesh()`
- `_ensure_indices_from_vertices()` -- SurfaceTool commits can produce non-indexed triangle arrays. Build a fallback.

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
