# Artifact: Cave Generator

> Cellular automaton cave generation with flood-fill connectivity â€” classic roguelike technique. 64x64 grid initialized with random walls, smoothed by CA rules (5+ neighbors = wall), then flood-filled to keep only the largest connected region. Entrance and exit markers placed at region extremes.

## Context

**Category:** proceduralgeneration | **Complexity:** intermediate
**Tags:** cave, cellular_automaton, flood_fill, roguelike, procedural, generation, connectivity | **Themes:** biology, generation, procedural

Cellular automaton cave generation with flood-fill connectivity â€” classic roguelike technique. 64x64 grid initialized with random walls, smoothed by CA rules (5+ neighbors = wall), then flood-filled to keep only the largest connected region. Entrance and exit markers placed at region extremes.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/cave_generator/cave_generator.gd` (280 lines) |
| **Scene** | `res://commons/artifacts/cave_generator/cave_generator.tscn` |
| **Registry** | `procgen_extra.json` |
| **Class** | `CaveGeneratorCA` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `display_size` | float | 0.8 |
| `grid_size` | int | 64 |
| `seed_value` | int | 42 |
| `wall_chance` | float | 0.45 |
| `smooth_passes` | int | 5 |
| `wall_threshold` | int | 5 |

### Key Methods

- `_ready()`
- `_build_floor_quad()`
- `_generate_cave()` -- Step 1: Initialize grid with random walls
- `_add_title_label()`
- `_initialize_random()`
- `_ca_smooth_step()`
- `_count_wall_neighbors(cx: int, cy: int)`
- `_find_all_regions()`
- `_flood_fill(start_x: int, start_y: int, visited: PackedByteArray)`
- `_find_extreme(region: Array, top_left: bool)`
- `_render_to_texture(entrance_idx: int, exit_idx: int)`
- `_draw_marker(image: Image, cell_idx: int, color: Color)`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `seed`, `smooth_passes`, `wall_chance`, `wall_threshold`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
