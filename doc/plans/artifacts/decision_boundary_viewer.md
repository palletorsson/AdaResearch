# Artifact: Decision Boundary Viewer

> 2D classification with live decision boundary — two clusters of points separated by a linear classifier (w1*x + w2*y + bias > 0). Background colored by classification region, data points as bright dots, yellow boundary line. VR sliders for w1, w2, bias with live accuracy display.

## Context

**Category:** machinelearning | **Complexity:** beginner
**Tags:** decision_boundary, classification, linear_classifier, supervised_learning, machine_learning | **Themes:** ai, machine_learning

2D classification with live decision boundary — two clusters of points separated by a linear classifier (w1*x + w2*y + bias > 0). Background colored by classification region, data points as bright dots, yellow boundary line. VR sliders for w1, w2, bias with live accuracy display.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/decision_boundary_viewer/decision_boundary_viewer.gd` (276 lines) |
| **Scene** | `res://commons/artifacts/decision_boundary_viewer/decision_boundary_viewer.tscn` |
| **Registry** | `machinelearning.json` |
| **Class** | `DecisionBoundaryViewer` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `plot_size` | float | 0.7 |
| `plot_resolution` | int | 128 |
| `num_points_per_class` | int | 30 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_generate_data_points()` -- Cluster A: centered around (-0.45, -0.35)
- `_build_plot()`
- `_update_plot()`
- `_build_labels()`
- `_setup_sliders()`
- `_on_w1_changed(_value)`
- `_on_w2_changed(_value)`
- `_on_bias_changed(_value)`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `bias`, `num_points_per_class`, `w1`, `w2`

## Curriculum Position

### Sequences

- machinelearning

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
