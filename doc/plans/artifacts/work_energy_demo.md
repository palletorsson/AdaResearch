# Artifact: Work-Energy Demo

> W = F . d = |F||d|cos(theta). Work is the dot product of force and displacement. Perpendicular force does zero work.

## Context

**Category:** vectors | **Complexity:** intermediate
**Tags:** vector, dot_product, work, energy, projection, physics | **Themes:** linear_algebra, math, vectors

Dot product as physical work — force dotted with displacement gives energy transfer. See why pushing perpendicular to motion does zero work.

*Pushing sideways does nothing. Work is the component of force that agrees with motion. Everything else is wasted.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** To let the learner push a ball and see a work meter tick up — then push perpendicular and watch it stay at zero. The dot product made visceral.
- **Critical Parameter:** The angle between force and displacement. At 0 degrees all force does work; at 90 degrees none does; at 180 degrees work is negative (energy extracted).
- **What Emerges:** Total work accumulating as an integral over the path. Negative work when force opposes motion. The work meter as a running dot-product integrator.
- **Triggers:** Drag force vector → ball moves, work accumulates frame by frame as F.d, meter turns green (positive) / red (negative) / yellow (zero), R → reset total

## Architecture

| | |
|---|---|
| **File** | `algorithms/vectors/03_dot_product/WorkEnergyDemo.gd` (177 lines) |
| **Scene** | `res://algorithms/vectors/03_dot_product/WorkEnergyDemo.tscn` |
| **Registry** | `vectors_demos.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_setup_demo()` -- Applied force (user can drag) - Red
- `_physics_process(delta: float)`
- `_update_info(force: Vector3, displacement: Vector3, work: float)`
- `_input(event: InputEvent)`
- `_reset_demo()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: ForcesComposition

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ForcesComposition | [4,16] | `-` |

### Relationships

- Physical application of dot_product_projector. Lives in ForcesComposition. Contrasts with momentum_collision (energy conservation vs work done).
- Needs: VR draggable force [has], work meter display [has], angle readout [has]. Missing: kinetic energy comparison (W = delta KE).

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
