# Artifact: Ordered Grid

> grid(x,y,z) * spacing + synchronized_breathe(sin(t)) -> perfect lattice

## Context

**Category:** qfep | **Complexity:** beginner
**Tags:** 2d, grid, qfep | **Themes:** philosophy, qfep, theory

Perfectly aligned cube grid representing low λ. Order, predictability, rigidity.

*at lambda=0, prediction error is zero — the crystal has no surprises, no freedom, and no life*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** stand inside pure order — every cube in its place, breathing as one
- **Critical Parameter:** spacing — the regularity that makes the grid legible as a crystal
- **What Emerges:** the uncanny valley of perfect order — too regular to feel alive, yet it breathes
- **Triggers:** breathe_enabled creates synchronized vertical oscillation; add_disorder() breaks the pattern

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/ordered_grid.gd` (122 lines) |
| **Scene** | `res://commons/interfaces/qfep/ordered_grid.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `QFEPOrderedGrid` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_x` | int | 4 |
| `grid_y` | int | 2 |
| `grid_z` | int | 4 |
| `cube_size` | float | 0.08 |
| `spacing` | float | 0.12 |
| `order_color` | Color | Color(0.2, 0.4, 0.9, 0.9) |
| `breathe_enabled` | bool | true |
| `breathe_speed` | float | 0.5 |
| `breathe_amount` | float | 0.02 |

### Key Methods

- `_ready()`
- `_build_grid()`
- `_process(delta: float)`
- `add_disorder(amount: float)`
- `reset_order()`

## Curriculum Position

### Sequences

- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Lambda_Spectrum

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| QFEP_Lambda_Spectrum | [4,2] | `:0:0.5` |

### Relationships

- represents F term (free energy = pure order); contrasts random_cubes (E term) and complexity_pattern (edge); depends on grid concept
- Needs: VR disorder control [missing], touch-to-displace [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
