# Artifact: Non-Euclidean Space

> A space where Euclid's parallel postulate fails — rooms that are bigger inside than outside, corridors that loop impossibly. Hyperbolic and elliptic geometry made navigable.

## Context

**Category:** geometry | **Complexity:** advanced
**Tags:** non_euclidean, hyperbolic, elliptic, impossible_space, alternative_geometry | **Themes:** geometry, non_euclidean

A space where Euclid's parallel postulate fails — rooms that are bigger inside than outside, corridors that loop impossibly. Hyperbolic and elliptic geometry made navigable.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [7, 1, 8] (environment)

## Architecture

| | |
|---|---|
| **File** | `algorithms/alternativegeometries/noneuclideanspace/non_euclidean_space.gd` (73 lines) |
| **Scene** | `res://algorithms/alternativegeometries/noneuclideanspace/non_euclidean_space.tscn` |
| **Registry** | `alternative_geometries.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `enable_debug_visuals` | bool | false |
| `portal_transition_speed` | float | 0.2 |

### Key Methods

- `_ready()` -- Set up portal manager
- `_setup_debug_overlay()`
- `_on_portal_entered(portal: Portal, body: Node3D)`
- `_on_portal_exited(portal: Portal, body: Node3D)`
- `_process(_delta)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
