# Artifact: Clipping Planes

> Interactive clipping plane algorithms for geometric visualization and spatial cutting

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** shader | **Themes:** shaders, visual

Interactive clipping plane algorithms for geometric visualization and spatial cutting

## Design

- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/postprocessing/clipping/clippingplanes.gd` (229 lines) |
| **Scene** | `res://algorithms/effects/clipping/clippingplanes.tscn` |
| **Registry** | `shaders.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_objects` | int | 8 |
| `clipping_speed` | float | 1.0 |
| `object_scale` | float | 2.0 |

### Key Methods

- `_ready()`
- `setup_scene()` -- Add ambient lighting
- `create_clipping_objects()` -- Create various geometric objects to be clipped
- `create_clipping_planes()` -- Create invisible clipping plane controllers
- `start_animation()` -- Animate the clipping planes
- `animate_planes()` -- Create tweens for each clipping plane
- `_process(_delta)` -- Update shader uniforms based on plane positions
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Unused** (unused) -- map: One_Adapt_2

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| One_Adapt_2 | [3,2] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
