# Artifact: Revolving Joy Ride

> RigidBody3D hub rotating at ride_speed, with PinJoint3D chains suspending SoftBody3D spheres — centripetal force stretches the soft bodies outward while gravity pulls them down

## Context

**Category:** physics | **Complexity:** intermediate
**Tags:** physics | **Themes:** physics, soft_body

A rotating ride with hanging soft bodies colliding with obstacles.

*a carousel is a centrifuge for compliance — the soft bodies do not choose their shape, the rotation imposes it*

## Design

- **Visual:** material: shader, animation: physics
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** to spin you inside a carousel where every hanging blob deforms differently depending on its chain length and the ride's angular velocity
- **Critical Parameter:** ride_speed — slow rotation lets soft bodies dangle and sway, fast rotation stretches them into elongated teardrops that barely hold together
- **What Emerges:** at certain speeds the chain links begin to oscillate in phase, creating synchronized pendulum waves across the four arms
- **Triggers:** rotation.y increment in _physics_process drives the entire chain system; obstacle ring at the perimeter causes collision-deformation events

## Architecture

| | |
|---|---|
| **File** | `algorithms/softbodies/joyride/revolving_joy_ride.gd` (275 lines) |
| **Scene** | `res://algorithms/softbodies/joyride/revolving_joy_ride.tscn` |
| **Registry** | `soft_bodies.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `ride_radius` | float | 3.0 |
| `ride_speed` | float | 1.0 |
| `arm_length` | float | 0.75 |
| `soft_body_radius` | float | 0.25 |
| `obstacle_radius_offset` | float | 2.0 |
| `obstacle_height` | float | 3.0 |
| `obstacle_y_pos` | float | 1.5 |

### Dependencies

- `res://commons/resourses/shaders/pinktartan.gdshader`
- `res://commons/resourses/shaders/pearlescent.gdshader`
- `res://commons/resourses/shaders/frosted_glass.gdshader`
- `res://commons/resourses/shaders/discoLights.gdshader`

### Key Methods

- `_ready()`
- `create_shader_material(shader: Shader, params: Dictionary = {})`
- `setup_camera()`
- `setup_lighting()`
- `setup_ride()` -- Create the central hub that rotates
- `create_arm(angle: float)`
- `create_link(pos: Vector3, color: Color)` -- Convert local offset to global
- `create_hanging_soft_body(parent_body: RigidBody3D)`
- `_pin_soft_body(sb: SoftBody3D, parent_body: RigidBody3D)`
- `setup_obstacles()`
- `_physics_process(delta: float)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Soft Bodies & Morphogenesis: Matter That Finds Its Shape** (softbodies) -- map: SoftBodies_Carusell

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| SoftBodies_Carusell | [6,8] | `:0:-0.5` |

### Relationships

- builds on jelly_cube's spring-mass intro by adding chain dynamics and centripetal force; contrasts with breathing_room's static pressure oscillation
- Needs: slider_horizontal [missing]; push_button [missing]; Label3D [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
