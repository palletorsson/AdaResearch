# Artifact: SierpinskiPyramid

> sierpinski_3d(pos, level, size) = 4 * sierpinski_3d(tetrahedral_vertices * size/2, level-1, size/2)

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

SierpinskiPyramid

*The Sierpinski pyramid is a solid that, given time and gravity, will prove it was always an arrangement of gaps.*

## Design

- **Visual:** material: shader, animation: static
- **Scale:** [5.0, 5.0, 5.0] (environment)
- **Interaction:** To collapse — frozen RigidBody3D tetrahedra unfreeze after 2 seconds and tumble apart, the fractal disassembling itself
- **Critical Parameter:** max_level — at level 3 there are 64 cubes arranged in a Sierpinski tetrahedron; each level multiplies by 4
- **What Emerges:** The destruction reveals the structure — only when the pieces fall do you see how they were nested
- **Triggers:** 2-second timer → unfreeze all bodies → gravity pulls the fractal apart → cubes scatter and collide

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/sierpinski_pyramid/SierpinskiPyramid.gd` (81 lines) |
| **Scene** | `res://algorithms/fractals/sierpinski_pyramid/SierpinskiPyramid.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | shader_demo |

### Dependencies

- `res://algorithms/fractals/sierpinski_pyramid/pyramid_shader.gdshader`

### Key Methods

- `_ready()`
- `_setup_material()`
- `generate_sierpinski(position, level, current_size)`
- `_on_Timer_timeout()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Relationships

- 3D extension of sierpinski_triangle; uses tetrahedral (not cubic) subdivision, producing a different D ~ 2.0
- Needs: VR re-freeze button [missing], level control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
