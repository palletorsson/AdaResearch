# Artifact: Zelda Tilemap

> Zelda-style tilemap system for game-like environments

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** simulation | **Themes:** commons, mixed

Zelda-style tilemap system for game-like environments

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/context/zeldatilemap/zelda_tilemap.gd` (273 lines) |
| **Scene** | `res://commons/context/zeldatilemap/zelda_tilemap.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `map_width` | int | 60 |
| `map_height` | int | 60 |
| `tile_size` | float | 1.0 |
| `map_scale` | float | 0.2 |
| `initial_position` | Vector3 | Vector3(5.0, -1.5, 15.0) |
| `auto_position_on_ready` | bool | true |

### Key Methods

- `_ready()` -- Create a parent node to hold all tile instances
- `generate_map()`
- `generate_features()` -- Add various terrain features to make the map interesting
- `add_water_bodies(count: int)` -- Choose between lake and river
- `add_terrain_patches(terrain_type: String, count: int, max_size: int)`
- `add_mountain_ranges(count: int)` -- Start point for the mountain range
- `add_buildings(count: int)`
- `create_visual_map()` -- Create the base mesh for a single tile
- `merge_meshes(region_start: Vector2i)`
- `regenerate()` -- Clear existing tiles
- `set_my_position(position: Vector3)`
- `reset_position()`
- `offset_position(offset: Vector3)`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
