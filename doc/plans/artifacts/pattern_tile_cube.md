# Artifact: Pattern Tile Cube

> Small grabbable color cube for pattern tile puzzle. Drag to grid cells to place colors.

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, color, pattern, primitives | **Themes:** arrays, biology, data_structures

Small grabbable color cube for pattern tile puzzle. Drag to grid cells to place colors.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/arrays/pattern_tile_cube.gd` (150 lines) |
| **Scene** | `res://commons/primitives/arrays/pattern_tile_cube.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `PatternTileCube` extends `XRToolsPickable` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `color_index` | int | 0: |
| `cube_size` | float | 0.04 |

### Key Methods

- `_ready()`
- `setup(p_puzzle: PatternTilePuzzle, p_color_index: int, p_size: float = 0.04)`
- `_create_visual()` -- Try to use existing mesh first
- `_update_color()`
- `_on_picked_up(_pickable)` -- If was placed, clear the grid cell
- `_on_dropped(_pickable)`
- `_snap_to_cell(x: int, y: int, target_pos: Vector3)` -- Move to cell position

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Pattern_Maker_Showcase | [1,4] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
