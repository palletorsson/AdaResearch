# Artifact: Pelta Shield Floor

> a floor carrying the defensive geometry of interlocking shields

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pompeii, pelta, shield, crescent | **Themes:** arrays, art, cultural_computation

Roman pelta (Amazon shield) mosaic floor — interlocking crescent shapes with concave bites arranged in offset rows. A classic Roman border and field pattern where paired crescents face opposite directions, creating a rhythmic shield motif.

*the Amazon shield motif spread across the Roman world as border and field pattern*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see the rhythm of paired crescents guarding space
- **Critical Parameter:** pelta_rows — number of pelta rows on the short axis
- **What Emerges:** understanding that subtraction (concave bite) creates interlocking form
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/pelta_shield_floor.gd` (368 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/pelta_shield_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `PeltaShieldFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.2, 0.9) |
| `pelta_rows` | int | 6 |
| `segments_per_arc` | int | 12 |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `color_terra` | Color | MosaicPalette.TERRACOTTA |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.04 |
| `wear_level` | float | 0.3 |
| `border_widths` | Array[int] | [2, 1, 1] |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`

### Grid Config

Accepts: `border_widths`, `floor_size`, `pelta_rows`, `segments_per_arc`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis
- testmaps

### Relationships

- wave_scale_floor (sibling), pattern_maker_station (web editor)
- Needs: [implemented] procedural mesh, border bands, pelta semicircles with concave bites

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
