# Artifact: Queer Morphology Specimen

> soft_body(stiffness(lambda), damping(phi)) + fluid_shader(turbulence) -> specimen_in_jar

## Context

**Category:** emergence | **Complexity:** advanced
**Tags:** queer_theory, morphology, specimen, critical_theory, qfep, taxonomy | **Themes:** art, biology, commons, emergence

A procedural specimen that defies binary classification — its form shifts between categories, refusing stable taxonomy. A critical theory artifact embodying the QFEP framework's challenge to normative boundaries in biological and computational classification.

*queer morphology is not a fixed form but a process of becoming — the specimen is the theory made flesh*

## Design

- **Visual:** material: shader, animation: shader_time
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** hold a creature that refuses to be one thing, watch it breathe between crystal and dissolution
- **Critical Parameter:** lambda — edge of chaos dial that transitions specimen from crystalline to dissolving
- **What Emerges:** the breathing motion at edge of chaos where pressure oscillates — the specimen appears alive
- **Triggers:** lambda shifts stiffness/pressure/color; phi shifts damping/drag/subsurface scatter; presets jump to named states

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/queer_morphology_specimen/queer_morphology_specimen.gd` (499 lines) |
| **Scene** | `res://commons/artifacts/queer_morphology_specimen/queer_morphology_specimen.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `QueerMorphologySpecimen` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `jar_height` | float | 0.4 |
| `jar_radius` | float | 0.15 |
| `lambda` | float | 0.5: |
| `phi` | float | 0.0: |
| `base_stiffness` | float | 0.5 |
| `base_pressure` | float | 1.0 |
| `base_damping` | float | 0.05 |
| `specimen_color` | Color | Color(0.4, 0.2, 0.6, 0.85) |
| `fluid_color` | Color | Color(0.6, 0.8, 0.7, 0.3) |
| `fluid_turbulence` | float | 0.5 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_jar()` -- Glass jar (cylinder with transparent material)
- `_create_fluid()` -- Inner fluid volume (slightly smaller than jar)
- `_create_specimen()` -- The morphing creature — a soft body sphere that deforms
- `_create_jar_collision()` -- Cylinder collision to contain soft body
- `_create_labels()` -- Title
- `_create_vr_controls()`
- `_sync_sliders_deferred()`
- `_sync_lambda_slider()`
- `_sync_phi_slider()`
- `_on_lambda_slider_moved(_position)`
- `_on_phi_slider_moved(_position)`
- `_apply_preset(l: float, p: float)`
- `_apply_lambda()`
- `_apply_phi()`

### Grid Config


## Curriculum Position

### Sequences

- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Synthesis

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Morphogenesis_Intro | [3,1] | `-` |
| QFEP_Synthesis | [9,5] | `:0:0.5:1` |

### Relationships

- synthesizes entire QFEP sequence; depends on lambda_slider, phi_slider; contrasts rigid_sculpture (frozen) vs fluid_form (flowing)
- Needs: VR sliders for lambda and phi [has], preset buttons [has], soft body physics [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
