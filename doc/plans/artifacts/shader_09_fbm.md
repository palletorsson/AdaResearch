# Artifact: Shader 09 — Fractal Brownian Motion

> sum(gain^i * noise(p * lacunarity^i)) — FBM layers noise at multiple frequencies; ridged folds it with abs(); turbulence removes direction; domain warp feeds output back as input

## Context

**Category:** shaders | **Complexity:** advanced
**Tags:** book_of_shaders, fbm, fractal, noise, shaders | **Themes:** shaders, visual

Book of Shaders chapter on fractal Brownian motion and layered noise.

*scale is a parameter, not a property — the same operation applied again produces something irreducibly new*

## Design

- **Visual:** material: shader, animation: transform
- **Interaction:** to slide octaves and watch flat noise become mountain ranges — to push warp_amount and see the procedural engine eat its own tail
- **Critical Parameter:** lacunarity — the frequency multiplier between octaves; 2.0 is standard, lower blurs, higher sharpens
- **What Emerges:** domain warping produces forms that look geological — marble, smoke, lava — from nothing but noise feeding into itself; complexity from repetition at different resolutions
- **Triggers:** gain slider controls amplitude decay per octave; sharpness on ridged mode creates mountain ridge profiles; warp_layers stacks self-referential distortion

## Architecture

| | |
|---|---|
| **File** | `algorithms/shaders/book_of_shaders/shader_09_fbm.gd` (111 lines) |
| **Scene** | `res://algorithms/shaders/book_of_shaders/shader_09_fbm.tscn` |
| **Registry** | `shaders.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_process(_delta: float)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Shaders & Patterns: The Book of Shaders** (patterngeneration) -- map: Shader_09_FBM

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Shader_09_FBM | [5,6] | `-` |

### Relationships

- depends on shader_07_noise; powers every procedural landscape and organic surface; connects to shader_10_reactiondiffusion (simulation as pattern)
- Needs: [has] ShaderRackPanel sliders per display; [missing] no VR push buttons

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
