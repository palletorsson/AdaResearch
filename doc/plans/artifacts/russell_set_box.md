# Artifact: Russell's Set Box

> S = { x | x ∉ x }; S ∈ S ↔ S ∉ S — Russell's paradox as infinite regress

## Context

**Category:** philosophy | **Complexity:** advanced
**Tags:** russell, paradox, self_reference, set_theory, interactive | **Themes:** philosophy, qfep

Nested boxes visualizing Russell's Paradox - the set of all sets that don't contain themselves

*naive set theory is inconsistent — the set of all sets that do not contain themselves destroyed the foundations of mathematics in 1901*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [2, 2, 2] (compact)
- **Interaction:** open the box and find another box inside, and another, forever — feel the paradox as infinite nesting
- **Critical Parameter:** max_visible_depth — how many nested boxes before the infinite regress indicator
- **What Emerges:** the visceral experience that self-reference creates bottomless contradiction
- **Triggers:** click/interact opens next layer; each opening reveals a smaller box; at max depth the ∞ symbol appears; paradox text escalates

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/foundations/russell_set_box.gd` (255 lines) |
| **Scene** | `res://commons/interfaces/foundations/russell_set_box.tscn` |
| **Registry** | `foundations.json` |
| **Class** | `RussellSetBox` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `size` | float | 0.3 |
| `wall_thickness` | float | 0.02 |
| `max_visible_depth` | int | 5 |
| `outer_color` | Color | Color(0.6, 0.3, 0.2) |
| `inner_color` | Color | Color(0.2, 0.1, 0.4) |
| `show_label` | bool | true |

### Key Methods

- `_ready()`
- `_create_nested_boxes()`
- `_create_open_box_mesh(box_size: float, thickness: float)` -- Simplified: just use a box mesh, we'll handle "open" via lid
- `_create_lid(index: int, box_size: float, scale_factor: float)`
- `_create_labels()`
- `_create_interactable()`
- `_on_input_event(_camera: Node, event: InputEvent, _position: Vector3, _normal: Vector3, _shape_idx: int)`
- `_process(delta: float)`
- `open_next_layer()` -- Already at max depth - show infinite regress
- `_update_paradox_text()`
- `reset()`

### Signals

- `paradox_observed`
- `box_opened`
- `infinite_regress_detected`

## Curriculum Position

### Sequences

- **Foundations Crisis: The Limits of Formalization** (foundationscrisis) -- map: Russell_Paradox
- **Foundations Crisis: The Limits of Formalization** (foundationscrisis) -- map: Crisis_Synthesis

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Crisis_Synthesis | [3,12] | `:0:0.8` |
| Russell_Paradox | [5,5] | `:0:1:1` |

### Relationships

- contrasts godel_statement_plaque (arithmetic vs set-theoretic self-reference); contrasts florensky_sphere (contradiction as paradox vs contradiction as truth); unlocks escher_staircase (spatial self-reference)
- Needs: VR click interaction [has], XR ray pickable [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
