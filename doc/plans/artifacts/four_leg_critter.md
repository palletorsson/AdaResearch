# Artifact: Four Leg Critter

> quadruped_gait(t) = diagonal_pairs(FL+BR, FR+BL) -- legs pair diagonally into trot and gallop

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** procedural, ik, legs, locomotion, quadruped | **Themes:** gameplay, hazards

Four-legged procedural walker. Quadruped gait with diagonal pairing — walk, trot, gallop emerge from phase relationships between legs.

*four legs pair diagonals because geometry demands it -- trot and gallop are necessities, not choices.*

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [2.0, 1.0, 2.0] (compact)
- **Interaction:** four legs discovering diagonal pairing -- opposite corners move together
- **Critical Parameter:** diagonal pair timing -- FL+BR alternate with FR+BL; speed determines gait type
- **What Emerges:** diagonal pairing discovered not designed -- four legs naturally fall into this pattern
- **Triggers:** patrol_timer drives movement; gait alternates diagonal pairs; speed modulates walk/trot/gallop

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/octapod_crawler/four_leg_critter.gd` (344 lines) |
| **Scene** | `res://commons/hazards/octapod_crawler/four_leg_critter.tscn` |
| **Registry** | `hazards.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `move_speed` | float | 2.0 |
| `turn_speed` | float | 40.0 |
| `patrol_speed` | float | 1.0 |
| `step_threshold` | float | 1.5 |
| `step_height` | float | 1.0 |
| `step_duration` | float | 0.25 |
| `step_overshoot` | float | 0.5 |

### Key Methods

- `_ready()`
- `_process(delta: float)` -- ── Movement ──────────────────────────────────────────────────────────
- `_update_gait(delta: float)` -- Compute where each foot's "home" position is (in world space)
- `_begin_step(leg_index: int, home: Vector3)`
- `_position_foot(leg_index: int, foot_target: Marker3D, _home: Vector3)`
- `_add_skinned_mesh(skeleton: Skeleton3D, mesh_name: String)`
- `_make_tapered_tube(bone_count: int, spacing: float, base_r: float, tip_r: float, segs: int)`
- `_add_foot_vis(foot: Marker3D)`
- `_build_scene()`

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_3

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_3 | [3,7] | `-` |

### Relationships

- fourth in progression; introduces diagonal symmetry; dominant vertebrate pattern
- Needs: MeshInstance3D [has]; quad IK chains [has]; diagonal gait [has]; patrol [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
