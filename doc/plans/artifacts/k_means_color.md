# Artifact: K-Means Color Clustering

> Extract dominant colors from images using k-means clustering in color space. Machine learning meets aesthetics — the algorithm behind every 'color palette from image' tool.

## Context

**Category:** color | **Complexity:** intermediate
**Tags:** color, k_means, clustering, palette_extraction, machine_learning | **Themes:** art, color, education, perception

Extract dominant colors from images using k-means clustering in color space. Machine learning meets aesthetics — the algorithm behind every 'color palette from image' tool.

## Design

- **Visual:** material: vertex_color, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/k_means_color/k_means_color.gd` (350 lines) |
| **Scene** | `res://algorithms/color/k_means_color/k_means_color.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `setup_layout()` -- Title
- `create_sample_image_data()` -- Create simplified Ada Lovelace portrait data (32x32 for demo)
- `generate_portrait_pixel(x: int, y: int, width: int, height: int)` -- Generate realistic portrait colors based on position
- `populate_row1_quantization_comparison()` -- Row 1: Original image vs different k-means quantization levels
- `populate_row2_pixel_resolution()` -- Row 2: Pixel resolution effects with quantization
- `populate_row3_color_palettes()` -- Row 3: Color palette extraction visualization
- `add_image_panel_3d(parent: Node3D, label_text: String, image_data: Array, palette: Array, panel_index: int)`
- `add_palette_panel_3d(parent: Node3D, label_text: String, palette: Array, panel_index: int)`
- `apply_kmeans_quantization(image_data: Array, k: int)`
- `color_distance(color1: Color, color2: Color)`
- `calculate_color_mean(colors: Array)`
- `extract_color_palette(image_data: Array, max_colors: int)`
- `scale_image_data(original_data: Array, original_size: int, new_size: int)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- color

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Context | [9,2] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
