# Artifact: Hazards Demo

> F_transmute(agent, field) → new_state — same potential, different restraint, different manifestation

## Context

**Category:** demos | **Complexity:** beginner
**Tags:** demos, hazard | **Themes:** gameplay, hazards

Test arena for all hazard creatures. Spawns plasma critters, stick tools, dormant egg-plant octapods, and an active crawler in a dark atmospheric arena with moody lighting and fog.

*A hazard is not an obstacle — it is a relationship waiting for the right mediator.*

## Design

- **Visual:** material: standard, animation: physics
- **Interaction:** walk into danger, grab tools, discover that hazards transmute when you bring the right catalyst
- **Critical Parameter:** force_intensity — governs damage-per-tick in each force field type
- **What Emerges:** players discover the mushroom-to-force-field transmutation loop without explicit instruction
- **Triggers:** body_entered on ForceField Area3D; mushroom proximity triggers transmutation

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/hazards_demo/hazards_demo.gd` (607 lines) |
| **Scene** | `res://commons/hazards/hazards_demo/hazards_demo.tscn` |
| **Registry** | `hazards.json` |
| **Pattern** | procedural |

### Dependencies

- `res://commons/hazards/plasma/plasma_critter.tscn`
- `res://commons/hazards/plasma/stick_tool.tscn`
- `res://commons/hazards/octapod_crawler/octapod_crawler.tscn`
- `res://commons/hazards/octapod_crawler/beast_demo.tscn`
- `res://commons/hazards/octapod_crawler/three_leg_critter.tscn`
- `res://commons/hazards/octapod_crawler/four_leg_critter.tscn`
- `res://commons/hazards/octapod_crawler/six_leg_critter.tscn`
- `res://commons/hazards/mushroom/edible_mushroom.tscn`

### Key Methods

- `_ready()`
- `_load_scenes()`
- `_build_environment()` -- World environment — well-lit test arena
- `_build_ground()`
- `_build_walls()` -- Simple low walls to give spatial reference
- `_build_info_labels()` -- Title label
- `_spawn_hazards()` -- --- Plasma Critters ---
- `_spawn_force_fields()` -- Four force fields along the back-left area of the arena.
- `_add_glowing_pillar(pos: Vector3, color: Color)`
- `_setup_camera()` -- Only add camera if no XROrigin3D is in the scene tree
- `_input(event: InputEvent)`
- `_process(delta: float)`
- `_update_camera_transform()`
- `_update_health_display()`

## Curriculum Position

### Sequences

- **Randomness: Freedom from Pattern** (randomness) -- map: Random_Remove

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Random_Remove | [9,5] | `-` |

### Relationships

- depends on plasma_critter, octapod_crawler, edible_mushroom, force_field; unlocks random_game hazard gauntlet
- Needs: VR locomotion [has], grab [has], force field shaders [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
