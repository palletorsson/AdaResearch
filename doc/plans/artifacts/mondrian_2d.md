# Artifact: Mondrian 2D Canvas

> A classic Mondrian composition rendered in 2D with geometric precision and De Stijl color palette

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** 2d, color, visualization | **Themes:** art, commons, mixed

A classic Mondrian composition rendered in 2D with geometric precision and De Stijl color palette

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 7.0, 10.0] (environment)

## Architecture

| | |
|---|---|
| **File** | `commons/context/mondrian2d/mondrian_2d.gd` (504 lines) |
| **Scene** | `res://commons/context/mondrian2d/mondrian_2d.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `auto_change_pattern` | bool | true |
| `pattern_change_interval` | float | 10.0 |

### Key Methods

- `_ready()` -- Get the actual viewport size with error handling
- `setup_pattern_timer()`
- `_on_pattern_timer_timeout()`
- `_on_countdown_update()`
- `start_auto_change()`
- `stop_auto_change()`
- `set_change_interval(new_interval: float)`
- `get_timer_status()`
- `change_pattern_now()`
- `define_grid_lines()` -- Define the horizontal grid lines based on the image
- `define_colored_sections()` -- Get grid positions for easy reference
- `_draw()` -- Draw white background for the entire canvas
- `animate_construction()` -- Temporarily store original data
- `generate_new_pattern()` -- Clear existing data
- `_force_viewport_update()` -- Find parent SubViewport if we're in a 3D context

### Signals

- `pattern_changed`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
