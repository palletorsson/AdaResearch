# Artifact: QFEP Reactor

> QFE = (1-lambda) - lambda*0.8 + phi*0.2; visualized as pulsing orb with color(lambda) and jitter(chaos)

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** chaos, graph, qfep | **Themes:** philosophy, qfep, theory

Central orb that visualizes the current QFEP state. Pulses and glows based on lambda and phi. Blue=order, green=edge of chaos, red=dissolution. The heart of the laboratory.

*the reactor is the QFEP formula made tangible — not a metaphor for the equation but the equation itself, running*

## Design

- **Visual:** material: shader, animation: particle_lifecycle
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** stand before the reactor and watch theory become light — adjust sliders and see the formula respond
- **Critical Parameter:** lambda — shifts reactor from crystalline blue through living green to chaotic red with position jitter
- **What Emerges:** the reactor appears to think at the edge — fast pulsing, maximum particles, bright green glow
- **Triggers:** set_lambda/set_phi from connected sliders; pulse_speed triples at edge of chaos; jitter activates above 0.6

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/qfep_reactor.gd` (252 lines) |
| **Scene** | `res://commons/interfaces/qfep/qfep_reactor.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `QFEPReactor` extends `Node3D` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_create_core()`
- `_create_particles()`
- `_create_light()`
- `_create_label()`
- `_process(delta)`
- `_update_animation(delta)` -- Pulse speed depends on lambda (faster at edge of chaos)
- `_update_visuals()` -- Calculate color based on lambda
- `set_lambda(value: float)`
- `set_phi(value: float)`
- `_calculate_qfe()` -- Simplified QFE calculation for visualization
- `_connect_to_sliders()` -- Find lambda and phi sliders in scene and connect
- `is_at_edge()`
- `get_state_name()`

### Signals

- `state_changed`

## Curriculum Position

### Sequences

- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Sandbox

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| QFEP_Sandbox | [5,6] | `:0:1.5` |

### Relationships

- central to QFEP_Sandbox; depends on lambda_slider and phi_slider; connects to reactive_particles; computes QFE in real-time
- Needs: VR slider connections via group system [has], label display [has], particle system [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
