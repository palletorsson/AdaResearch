# Artifact: inverted_tree_cloud

> tree(depth) = trunk_down + branches_down(random_angle, shrink) + cube_cloud(orbit, fade_timeline)

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** tree | **Themes:** generation, procedural

inverted_tree_cloud

*An inverted tree is not wrong — it is a root system, and roots are where the real computation happens.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [9.0, 5.0, 13.0] (world_scale)
- **Interaction:** To unsettle — roots reaching down from the sky while a cloud of cubes orbits and pulses above
- **Critical Parameter:** vertical_tilt range (-0.5 to -0.2) — controls how steeply branches plunge downward
- **What Emerges:** The cube cloud feels alive — independent orbit speeds and fade offsets create the illusion of breathing
- **Triggers:** Recursive branch generation with random angles; cube cloud timeline loops with fade-in/out per cube

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/recursive_tree/inverted_tree_cloud.gd` (410 lines) |
| **Scene** | `res://algorithms/fractals/recursive_tree/inverted_tree_cloud.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_main_branches` | int | 5 |
| `max_sub_branches` | int | 4 |
| `branch_length_min` | float | 1.5 |
| `branch_length_max` | float | 4.0 |
| `branch_width_min` | float | 0.8 |
| `branch_width_max` | float | 2.5 |
| `branch_height_min` | float | 0.8 |
| `branch_height_max` | float | 2.5 |
| `trunk_height` | float | 5.0 |
| `trunk_width` | float | 1.5 |
| `primary_color` | Color | Color(0.95, 0.3, 0.3) |
| `secondary_color` | Color | Color(0.85, 0.2, 0.2) |
| `trunk_color` | Color | Color(0.8, 0.3, 0.3) |
| `metallic` | float | 0.1 |
| `roughness` | float | 0.7 |
| `emission_strength` | float | 0.2 |
| `cloud_start_height` | float | 8.0 |
| `cloud_center_cube_size` | float | 2.0 |
| `cloud_sub_cubes` | int | 12 |
| `cloud_sub_cube_size_min` | float | 0.5 |
| `cloud_sub_cube_size_max` | float | 1.5 |
| `cloud_orbit_radius` | float | 4.0 |
| `cloud_animation_duration` | float | 8.0 |
| `cloud_color` | Color | Color(0.7, 0.85, 1.0, 0.8) |
| `random_seed` | int | 42 |

### Key Methods

- `_ready()`
- `create_materials()` -- Primary material
- `build_inverted_tree()`
- `create_inverted_trunk()`
- `generate_inverted_branches(parent: Node, origin_point: Vector3, num_branches: int, current_depth: int, max_depth: int)`
- `add_inverted_detail_blocks(parent_node: Node, branch_instance: MeshInstance3D, branch_size: Vector3)`
- `create_cube_cloud()`
- `create_cloud_cube(size: float)`
- `_process(delta: float)`
- `rebuild()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_RecursiveTrees

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_RecursiveTrees | [3,10] | `-` |
| Fractal_RecursiveTrees | [7,10] | `-` |

### Relationships

- Inverts recursive_tree; the cloud above mirrors what roots extract below — visual metaphor for data flow
- Needs: VR gravity toggle [missing], cloud interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
