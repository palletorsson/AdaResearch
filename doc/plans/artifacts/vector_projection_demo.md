# Artifact: Vector Projection

> reflect(A) = A - 2(A.n)n. Projection strips a component; reflection mirrors through a plane. Both are dot-product machines.

## Context

**Category:** mathematics | **Complexity:** intermediate
**Tags:** vectors, projection, shadow, component, interactive | **Themes:** vectors

Visualizes vector projection onto another vector or plane. Shows shadow/component decomposition.

*A mirror does not reverse left and right. It reverses the component perpendicular to its surface. Reflection is subtraction, twice.*

## Design

- **Visual:** material: standard, animation: particle_lifecycle
- **Scale:** [2, 3, 3] (large)
- **Interaction:** To let the learner tilt a plane and watch a vector bounce — making the law of reflection a spatial experience, not a memorized rule.
- **Critical Parameter:** The normal vector direction. It defines the plane. Tilting it changes what "perpendicular" means and rewrites both projection and reflection.
- **What Emerges:** Angle of incidence equaling angle of reflection — visible in the symmetry of A and its reflection about the translucent plane. Glancing angles produce near-tangent reflections.
- **Triggers:** Handle drag (A or n) → five arrows update simultaneously (A, n, projection, reflection, normal component), preset buttons → floor/wall/45deg/glance

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/vector_projection_demo/vector_projection_demo.gd` (603 lines) |
| **Scene** | `res://commons/artifacts/vector_projection_demo/vector_projection_demo.tscn` |
| **Registry** | `vectors.json` |
| **Class** | `VectorProjectionDemo` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_vector_length` | float | 1.2 |
| `arrow_thickness` | float | 0.025 |
| `vector_a` | Vector3 | Vector3(0.6, 0.7, 0.2): |
| `normal` | Vector3 | Vector3(0.0, 1.0, 0.0): |
| `color_a` | Color | Color(1.0, 0.3, 0.3) |
| `color_normal` | Color | Color(0.3, 0.8, 1.0) |
| `color_projection` | Color | Color(0.3, 1.0, 0.4) |
| `color_reflection` | Color | Color(1.0, 0.6, 0.8) |
| `color_proj_normal` | Color | Color(1.0, 0.8, 0.3, 0.6) |
| `panel_color` | Color | Color(0.06, 0.06, 0.08, 0.9) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_create_text_panel(panel_name: String, text: String, pos: Vector3,
		size: Vector2 = Vector2(0.3, 0.08)`
- `_add_panel_frame(panel: Node3D, size: Vector2)`
- `_get_panel_label(panel: Node3D)`
- `_ready()`
- `_init_shared_materials()`
- `_create_base()`
- `_create_coordinate_axes()`
- `_create_axis_line(direction: Vector3, color: Color, label_text: String)`
- `_create_vector_labels()`
- `_create_vector_label(text: String, color: Color)`
- `_create_plane()`
- `_create_arrows()`
- `_create_arrow(arrow_name: String, color: Color, ghost: bool = false)`
- `_create_drop_line()`
- `_create_handles()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: VectorOperations

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| VectorOperations | [8,15] | `-` |

### Relationships

- Extends dot_product_projector (projection uses dot product). Feeds into normal_force_demo (force decomposition onto surface normal). Used by every billiard/bounce in the engine.
- Needs: VR grabbable handles [has], preset buttons [has], translucent plane mesh [has]. Missing: bouncing ball animation following the reflection path.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
