# Artifact: Necklace Anchor

> Vertical stand with torus ring and glowing pendant — a display piece showing the first body anchor point at neck

## Context

**Category:** body | **Complexity:** beginner
**Tags:** necklace, pendant, anchor, identity, body, jewelry | **Themes:** generation, procedural

Pendant necklace display on a stand showing the concept of an identity anchor point. A vertical stand with torus ring, glowing gem pendant that slowly rotates.

*The body needs a center before it can have edges — the necklace is the seed, not the ornament.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** To introduce the concept of body anchoring: a pendant at the neck as the seed from which embodiment grows outward
- **Critical Parameter:** pendant emission_energy — the glow intensity that draws attention to the anchor point as identity seed
- **What Emerges:** The idea that identity accretes around physical anchors — the pendant is the first fixed point in floating fragments
- **Triggers:** The pendant slowly rotates, catching light; the torus ring marks where the necklace would sit on a body not yet assembled

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/necklace_anchor/necklace_anchor.gd` (111 lines) |
| **Scene** | `res://commons/artifacts/necklace_anchor/necklace_anchor.tscn` |
| **Registry** | `procgen_extra.json` |
| **Class** | `NecklaceAnchor` extends `Node3D` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_build_display()` -- Vertical stand — thin cylinder
- `_process(delta: float)`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `scale`

## Curriculum Position

### Sequences

- **Body Progression: Assembling the Self** (bodyprogression) -- map: BodyProg_Necklace

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| BodyProg_Necklace | [5,4] | `-` |

### Relationships

- Second artifact in bodyprogression. Follows mirror_artifact (hands), precedes ik_arm_demo (arms).
- Needs: Animated pendant rotation [has], emissive material [has], VR interaction [missing — display only]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
