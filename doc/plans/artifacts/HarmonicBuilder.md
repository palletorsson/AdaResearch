# Artifact: Harmonic Builder

> Fourier synthesis in action—stack sine waves of different frequencies to build complex shapes. Any waveform is a sum of simple oscillations; this tool lets you construct them one harmonic at a time.

## Context

**Category:** waves | **Complexity:** advanced
**Tags:** wave | **Themes:** oscillation, physics, waves

Fourier synthesis in action—stack sine waves of different frequencies to build complex shapes. Any waveform is a sum of simple oscillations; this tool lets you construct them one harmonic at a time.

## Design

- **Visual:** material: unlit, animation: transform
- **Scale:** [6.0, 2.0, 1.0] (environment)

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/harmonic_builder/HarmonicBuilder.gd` (404 lines) |
| **Scene** | `res://algorithms/wavefunctions/harmonic_builder/HarmonicBuilder.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural_mesh |

### Key Methods

- `_ready()`
- `_initialize_arrays()`
- `_setup_audio()`
- `_setup_controls()` -- Find sliders (assuming named Slider1, Slider2, etc.)
- `_setup_preset_buttons()` -- Find preset buttons (assuming named PresetSine, PresetSquare, etc.)
- `_create_visualizations()` -- Create harmonic amplitude bars
- `_create_waveform_display()`
- `_create_labels()` -- Title
- `_on_harmonic_slider_changed(value: float, harmonic_index: int)`
- `_on_frequency_changed(value: float)`
- `_update_frequency_label()`
- `_frequency_to_note_name(freq: float)` -- Simple frequency to note conversion (A440 reference)
- `_apply_preset(preset_name: String)`
- `_update_harmonic_bars()`
- `_process(delta: float)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
