# Artifact: Lyapunov Exponents

> Visualization of Lyapunov exponents — the rate at which nearby trajectories diverge in phase space. Positive exponents mean chaos: small differences amplify exponentially, making long-term prediction impossible.

## Context

**Category:** chaos | **Complexity:** advanced
**Tags:** lyapunov, chaos, divergence, sensitivity, dynamical_systems, exponent | **Themes:** chaos, dynamical_systems

Visualization of Lyapunov exponents — the rate at which nearby trajectories diverge in phase space. Positive exponents mean chaos: small differences amplify exponentially, making long-term prediction impossible.

## Design

- **Visual:** material: vertex_color, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `algorithms/chaos/lyapunovexponents/lyapunov_exponents.gd` (788 lines) |
| **Scene** | `res://algorithms/chaos/lyapunovexponents/lyapunov_exponents.tscn` |
| **Registry** | `chaos.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_resolution` | int | 48 |
| `bounding_size` | float | 0.8 |
| `iterations` | int | 80 |
| `transient_skip` | int | 20 |

### Key Methods

- `_ready()`
- `_setup_heightmap_mesh()`
- `_setup_probe_mesh()`
- `_setup_bifurcation_mesh()`
- `_setup_probe_marker()`
- `_setup_labels()`
- `_setup_controllers()`
- `_on_a_center_changed(val: float)`
- `_on_b_center_changed(val: float)`
- `_on_zoom_changed(val: float)`
- `_on_iter_changed(val: float)`
- `_init_lambda_grid()`
- `_compute_lyapunov(a: float, b: float)` -- Compute Lyapunov exponent for logistic map with alternating parameters.
- `_compute_grid_incremental()` -- Compute a few rows per frame to avoid stalling. Returns true when done.
- `_find_grid_bounds()`

### Grid Config

Accepts: `a_max`, `a_min`, `b_max`, `b_min`, `iterations`, `probe_u`, `probe_v`, `resolution`, `sequence`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
