# Artifact: particle_campfire

> Three systems, one scene. Fire rises, embers arc, smoke expands. Forces + lifetime + collision = recognizable effect.

## Context

**Category:** unknown
**Tags:** particles, synthesis, campfire, fire

Synthesis campfire combining fire, embers, and smoke particle systems

*Emitters plus forces plus lifetime curves plus spatial behavior. This is what the sequence was building toward.*

## Design

- **Interaction:** To be warm. To synthesize everything the particle sequence taught into one thing you recognize from the real world.
- **Critical Parameter:** Wind — changes the fire's lean, the ember trajectories, the smoke drift. The campfire's character.
- **What Emerges:** A campfire from three independent particle types. Recognition from parameter curves. Atmosphere from math.
- **Triggers:** Wind change → fire leans and flickers, gravity on embers → ballistic arcs, smoke expansion → volumetric sense

## Architecture

| | |
|---|---|
| **File** | `algorithms/particles/particle_campfire/particle_campfire.gd` (256 lines) |
| **Registry** | `physics_simulation.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_setup_multimesh()`
- `_setup_logs()` -- Simple log arrangement
- `_setup_labels()`
- `_process(delta: float)`
- `_spawn(type: int)`
- `_get_color(type: int, t: float)`
- `_get_size(type: int, t: float)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Particle Systems** (particles) -- map: Particles_Collision

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Particles_Collision | [5,5] | `-` |

### Relationships

- Capstone of particles sequence. Uses lifetime_curves patterns. Uses sub_emitter logic (embers are soft bursts).
- Needs: VR wind slider [missing]. Temperature/intensity control [missing]. Log arrangement could be grabbable.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
