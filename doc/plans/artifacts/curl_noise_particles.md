# Artifact: Curl Noise Particles

> Divergence-free curl noise driving 300 particles in smooth swirling ribbons. Cross product of two noise gradients guarantees incompressible flow. Particles color-shift from deep blue (slow) through cyan to white (fast), with trailing flow lines for visualization.

## Context

**Category:** procedural | **Complexity:** advanced
**Tags:** curl_noise, divergence_free, particles, flow, noise, incompressible, multimesh, 3d_display | **Themes:** probability, procedural, randomness

Divergence-free curl noise driving 300 particles in smooth swirling ribbons. Cross product of two noise gradients guarantees incompressible flow. Particles color-shift from deep blue (slow) through cyan to white (fast), with trailing flow lines for visualization.

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/curl_noise_particles/curl_noise_particles.gd` (364 lines) |
| **Scene** | `res://commons/artifacts/curl_noise_particles/curl_noise_particles.tscn` |
| **Registry** | `randomness.json` |
| **Class** | `CurlNoiseParticles` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `particle_count` | int | 300 |
| `bounds` | Vector3 | Vector3(0.7, 0.5, 0.7) |
| `noise_scale` | float | 2.5 |
| `flow_speed` | float | 0.3 |
| `time_scale` | float | 0.15 |

### Key Methods

- `_ready()`
- `_create_bounds_wireframe()`
- `_create_multimesh()`
- `_create_trail_mesh()`
- `_create_labels()`
- `_spawn_particles()`
- `_noise_3d(p: Vector3, offset: Vector3)` -- 3-octave sin-based noise for smooth turbulence
- `_curl_noise(p: Vector3)` -- Curl noise = cross product of gradient of potential fields
- `_process(delta: float)`
- `_update_particles(delta: float)`
- `_update_multimesh()`
- `_update_trails()` -- Draw short trail lines behind a subset of particles for flow visualization
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `flow_speed`, `noise_scale`, `particle_count`, `time_scale`

## Curriculum Position

### Sequences

- noise
- randomness

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
