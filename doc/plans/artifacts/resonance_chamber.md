# Artifact: Resonance Chamber

> standing_wave(x,t) = cos(n*pi*x/L)*cos(omega*t) -- chamber with standing wave bars

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** resonance, standing-wave, harmonics, audio, nodes | **Themes:** gameplay, hazards, music

Transparent box with standing wave bars at node positions. Damage at wave nodes, safe at anti-nodes; harmonic frequency shifts every 8 seconds. Sound physics as spatial puzzle.

*safe at anti-nodes, lethal at nodes -- the wave divides space into life and death.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** standing waves pinning nodes and anti-nodes to space -- safe at anti-nodes, lethal at nodes
- **Critical Parameter:** frequency / harmonic number -- determines node spacing; shifts every 8 seconds
- **What Emerges:** wave physics as spatial puzzle -- find anti-nodes to survive, but they shift
- **Triggers:** player enters chamber; wave evaluated at position; node = damage, anti-node = safe

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/resonance_chamber/resonance_chamber.gd` (253 lines) |
| **Scene** | `res://commons/hazards/resonance_chamber/resonance_chamber.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `ResonanceChamber` extends `Area3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `chamber_width` | float | 3.0 |
| `chamber_height` | float | 2.0 |
| `chamber_depth` | float | 3.0 |
| `bar_count` | int | 8 |
| `frequency_change_interval` | float | 8.0 |
| `min_harmonic` | int | 1 |
| `max_harmonic` | int | 5 |
| `damage_per_second` | float | 12.0 |
| `bar_thickness` | float | 0.1 |
| `frame_color` | Color | Color(0.4, 0.6, 0.9, 0.15) |
| `bar_color` | Color | Color(0.2, 0.5, 1.0) |
| `bar_emission` | Color | Color(0.3, 0.6, 1.0) |

### Key Methods

- `_ready()` -- Collision for the whole chamber
- `_process(delta: float)`
- `_calculate_node_positions()` -- Standing wave nodes: positions where sin(n * PI * x / width) = 0
- `_build_frame()` -- Transparent box for the chamber walls
- `_build_bars()`
- `_build_label()`
- `_on_body_entered(body: Node3D)`
- `_on_body_exited(body: Node3D)`
- `_find_player()`
- `take_damage(amount: float)` -- Zone hazard — indestructible
- `apply_damage(amount: float)`
- `damage(amount: float)`
- `configure(config: Dictionary)`
- `apply_grid_config(config: Dictionary)`

### Signals

- `enemy_destroyed`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_7

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_7 | [3,3] | `-` |

### Relationships

- embodies proceduralaudio sequence; pairs with wave_rider (traveling vs standing waves)
- Needs: Area3D [has]; standing wave viz [has]; harmonic shifting [has]; position damage [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
