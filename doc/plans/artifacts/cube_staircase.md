# Artifact: Cube Staircase

> staircase(n) = tread(n) + riser(n) + staircase(n-1), diagonal subdivision of a cube

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

Staircase created through diagonal cube subdivision with treads, risers, stringers and handrail

*A staircase is a cube that learned to subdivide itself along its own diagonal.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To be climbed in VR — each step materializing from nothing as the cube reveals its inner staircase
- **Critical Parameter:** step_count — determines granularity of the subdivision, how many slices the cube yields
- **What Emerges:** Handrails and stringers arise as structural necessity from the step geometry, not designed separately
- **Triggers:** show_construction toggle — animated step-by-step reveals the recursive logic; step_delay controls rhythm

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/cubesubdivision/cube_staircase.gd` (230 lines) |
| **Scene** | `res://algorithms/fractals/cubesubdivision/cube_staircase.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `stair_size` | float | 2.0 |
| `step_count` | int | 6 |
| `show_construction` | bool | true |
| `step_delay` | float | 0.3 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_execute_build()` -- Building steps
- `_build_instant()`
- `_build_single_step(index: int)`
- `_build_sides()` -- Create side walls (stringers) that follow the stair profile
- `_build_handrail()`
- `_create_part(pos: Vector3, size: Vector3, color: Color, part_name: String)`
- `reset()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_Recursion

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_Recursion | [7,3] | `-` |

### Relationships

- Sibling to recursive_chair and recursive_table; all share the subdivision-to-furniture pattern
- Needs: VR step-on detection [missing], construction speed slider [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
