# Artifact: fibonacci_pagoda

> tier_scale(n) = base_scale / phi^(n*0.5), stacking Fibonacci-ratioed tiers with golden finial

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** procedural | **Themes:** generation, procedural

fibonacci_pagoda

*The golden ratio is not imposed on the pagoda — the pagoda is what the golden ratio looks like when it tries to be architecture.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [5.0, 8.0, 5.0] (environment)
- **Interaction:** To be looked up at — each tier smaller by the golden ratio, converging to a point that never quite arrives
- **Critical Parameter:** use_golden_ratio — toggles between pure Fibonacci integer ratios and continuous phi scaling, producing subtly different tapering
- **What Emerges:** The visual impression of weightlessness at the top — pure mathematics making stone look like it floats
- **Triggers:** num_tiers increase → taller, more converging structure; roof_overhang → how far the eaves extend beyond the body

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/pillar/fibonacci_pagoda.gd` (308 lines) |
| **Scene** | `res://algorithms/fractals/pillar/fibonacci_pagoda.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_tiers` | int | 8 |
| `base_scale` | float | 3.0 |
| `tier_height` | float | 1.2 |
| `roof_overhang` | float | 1.3 |
| `use_golden_ratio` | bool | true |

### Dependencies

- `res://algorithms/fractals/pillar/pillarpart.tscn`

### Key Methods

- `_ready()`
- `_generate_fibonacci_sequence(count: int)`
- `_setup_environment()`
- `_build_pagoda()`
- `_get_tier_scale(tier_index: int)`
- `_create_tier(tier_index: int, y_position: float)`
- `_create_roof_layer(parent: Node3D, scale_factor: float, tier_index: int)`
- `_create_corner_pillars(parent: Node3D, scale_factor: float, tier_index: int)`
- `_create_finial(y_position: float)`
- `_apply_tier_material(node: Node3D, tier_index: int)`
- `_apply_roof_material(node: Node3D, tier_index: int)`
- `_apply_pillar_material(node: Node3D, tier_index: int)`
- `_apply_finial_material(node: Node3D, level: int)`
- `_find_mesh_instances(node: Node)`
- `get_tier_count()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_Recursion

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_Recursion | [1,2] | `-` |

### Relationships

- Bridges fibonacci_sequences (abstract math) to architecture; contrasts with romanesco (organic Fibonacci)
- Needs: VR tier-count slider [missing], walk-inside collision [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
