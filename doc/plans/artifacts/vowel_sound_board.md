# Artifact: vowel_sound_board

> vowel(formant1, formant2) -> pulse_train filtered through formant resonances

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** speech | **Themes:** audio, language, music, speech

vowel_sound_board

*Every vowel is a pair of resonant frequencies — the mouth is a tunable resonator.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** Press buttons in VR to hear synthesized vowels and words assembled from phoneme chains
- **Critical Parameter:** formant frequencies (F1, F2) — the two resonant peaks that define each vowel
- **What Emerges:** recognizable speech from pure synthesis — words without a voice, language from mathematics
- **Triggers:** phrase selection triggers sequential phoneme playback with pitch interpolation

## Architecture

| | |
|---|---|
| **File** | `algorithms/speech/phoneme_cloud/vowel_sound_board.gd` (417 lines) |
| **Scene** | `res://algorithms/speech/phoneme_cloud/vowel_sound_board.tscn` |
| **Registry** | `speech.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `phrase_selection` | String | "Ada" |
| `is_ambient_enabled` | bool | true |
| `repeat_interval` | float | 3.0 |

### Key Methods

- `_ready()` -- Configure Mapper Range
- `apply_grid_config(data: Dictionary)` -- Support for grid-level overrides
- `_start_ambient_loop()`
- `play_word(word: String, pitch: float = 130.0)`
- `_play_ada()`
- `_play_research()`
- `_play_iloveyou()`
- `_play_right_here_right_now()`
- `_play_right()`
- `_play_here()`
- `_play_now()`
- `_play_funk()`
- `_play_hello_world()`
- `_play_right_about_now()`
- `_play_about()`

### Grid Config

Accepts: `repeat`, `say`

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: Wavefunctions_Bernini

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Wavefunctions_Bernini | [12,7] | `:180:3#say:right here, right now#repeat:true` |

### Relationships

- depends on formant synthesis engine; contrasts with timbre_sculptor (speech vs musical timbre); unlocks the physics of voice
- Needs: VR button press [has], phrase selection [has], ambient mode [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
