# Artifact: Edge of Chaos Unlocked

> Gray-Scott reaction-diffusion on a 64x64 grid with an UNLOCKED lambda slider — the control the map withheld, now given

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** reaction_diffusion, turing_pattern, lambda, edge_of_chaos, critical_theory, qfep | **Themes:** philosophy, qfep, theory, emergence

Reaction-diffusion system with the UNLOCKED lambda slider that the QFEP_Edge_Of_Chaos map withheld. Drag lambda from 0 (frozen crystal) through 0.4 (Turing patterns emerge) to 1.0 (pure noise). The artifact answers the critical text's question: adjusting IS knowledge. Witnessing IS knowledge. Different kinds.

*when you control lambda, you understand the parameter space; when you witness it locked, you understand what it feels like to be at the edge; both are knowledge*

## Design

- **Visual:** material: generated_texture, animation: transform
- **Scale:** [2, 2, 2] (room_scale)
- **Interaction:** to answer the critical.md question by letting the learner discover that adjusting lambda IS knowledge and witnessing lambda locked IS knowledge — different kinds
- **Critical Parameter:** lambda (0.0-1.0) — maps to feed/kill rate balance; 0=frozen crystal, 0.4=Turing patterns emerge, 1.0=pure noise
- **What Emerges:** Turing spots, stripes, labyrinthine patterns at the edge; the learner discovers the parameter space has texture, not just a gradient
- **Triggers:** dragging lambda slowly from 0 toward 0.4 produces the moment of emergence — spots appear from nothing; pushing past 0.7 dissolves everything into static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/edge_of_chaos_unlocked/edge_of_chaos_unlocked.gd` (305 lines) |
| **Scene** | `res://commons/artifacts/edge_of_chaos_unlocked/edge_of_chaos_unlocked.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `EdgeOfChaosUnlocked` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | int | 64 |
| `lambda` | float | 0.4: |
| `display_rate` | int | 2 |
| `sim_steps` | int | 4 |

### Key Methods

- `_ready()`
- `apply_grid_config(config_data: Dictionary)`
- `_process(delta: float)`
- `_init_grid()`
- `_seed_pattern()` -- Seed a few spots of chemical B in the center region
- `_update_reaction_params()` -- Map lambda to Gray-Scott feed/kill parameter space
- `_simulate_step()`
- `_build_display()`
- `_update_texture()`
- `_build_controls()`
- `_build_labels()`
- `_update_regime_label()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- qfeplaboratory

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| QFEP_Edge_Of_Chaos | [8,3] | `-` |

### Relationships

- companion to QFEP_Edge_Of_Chaos map where lambda_slider is locked at 0.4; the map withholds what this artifact gives
- Needs: [has] lambda slider via ArtifactControls; [has] regime label (FROZEN/EDGE/CHAOS); [has] display rate control; [missing] no pattern save; no comparison view

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
