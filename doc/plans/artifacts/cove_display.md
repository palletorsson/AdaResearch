# Artifact: Cove Display

> Cyclorama/infinity cove surface — curved floor-to-wall form built with SurfaceTool. Accepts any shader via grid config. Continuous UV mapping for seamless tiling.

## Context

**Category:** shaders | **Complexity:** advanced
**Tags:** cove, cyclorama, surface_tool, display, shader, configurable, pattern | **Themes:** shaders, visual

Cyclorama/infinity cove surface — curved floor-to-wall form built with SurfaceTool. Accepts any shader via grid config. Continuous UV mapping for seamless tiling.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/cove_display/cove_display.gd` (367 lines) |
| **Scene** | `res://commons/artifacts/cove_display/cove_display.tscn` |
| **Registry** | `shaders.json` |
| **Class** | `CoveDisplay` extends `Node3D` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cove_width` | float | 2.0 |
| `floor_depth` | float | 1.5 |
| `wall_height` | float | 2.0 |
| `cove_radius` | float | 0.4 |
| `curve_segments` | int | 12 |

### Key Methods

- `_ready()`
- `_build()`
- `apply_shader_material(mat: ShaderMaterial)`
- `apply_standard_material(mat: StandardMaterial3D)`
- `get_mesh_instance()`
- `apply_grid_config(config_data: Dictionary)` -- ── 1. Geometry parameters ──
- `_apply_fallback_material(config_data: Dictionary)`
- `_set_shader_param(mat: ShaderMaterial, key: String, s: String)` -- Try int first (important: shader int uniforms need int, not float)
- `_add_display_light()`
- `_build_cove_mesh()`
- `_generate_domain_texture(seed_val: int)`
- `_make_domain_pattern(rng: RandomNumberGenerator, pattern_type: int)`
- `_grid8(fill: int)`

### Grid Config

Accepts: `cove_radius`, `cove_width`, `curve_segments`, `floor_depth`, `wall_height`

## Curriculum Position

### Sequences

- patterngeneration
- mosaicanalysis

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
