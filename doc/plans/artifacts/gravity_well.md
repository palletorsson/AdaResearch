# Artifact: Gravity Well

> The rubber-sheet metaphor made real: a deformable grid mesh warps around a movable mass, creating a visible gravitational well. Colored test particles roll along the curved surface, orbiting, spiraling inward, or escaping depending on their velocity. Adjustable mass strength deepens or flattens the well in real time.

## Context

**Category:** physics | **Complexity:** intermediate
**Tags:** gravity, well, rubber_sheet, curvature, attraction, orbit, field, general_relativity | **Themes:** gravity, general_relativity_metaphor, attraction, fields

The rubber-sheet metaphor made real: a deformable grid mesh warps around a movable mass, creating a visible gravitational well. Colored test particles roll along the curved surface, orbiting, spiraling inward, or escaping depending on their velocity. Adjustable mass strength deepens or flattens the well in real time.

## Design

- **Visual:** material: vertex_color, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/physicssimulation/gravitywell/gravitywell.gd` (294 lines) |
| **Scene** | `res://algorithms/physicssimulation/gravitywell/gravitywell.tscn` |
| **Registry** | `physics_simulation.json` |
| **Class** | `GravityWell` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | float | 1.0 |
| `grid_resolution` | int | 24 |
| `well_depth` | float | 0.3 |
| `well_radius` | float | 0.25 |
| `mass_strength` | float | 1.0: |
| `particle_count` | int | 6 |
| `sheet_color` | Color | Color(0.3, 0.5, 1.0, 0.6) |
| `mass_color` | Color | Color(1.0, 0.8, 0.2) |
| `particle_color` | Color | Color(1.0, 0.3, 0.4) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_sheet()`
- `_create_mass_marker()`
- `_create_particles()`
- `_update_sheet()` -- Build a grid mesh that deforms around the mass position
- `_compute_well_depth(x: float, z: float)`
- `_compute_gradient(x: float, z: float)` -- Numerical gradient of the well surface (slope direction)
- `_physics_process(delta: float)`
- `_move_mass(new_pos: Vector3)`
- `_scatter_particles()`
- `_create_labels()`
- `_create_vr_controls()`
- `_input(event: InputEvent)`
- `reset()`
- `_exit_tree()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- forces

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
