# Artifact: Branching Vine

> F -> F[+F]F[-F]F -- L-system grammar drives vine growth toward the player

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** l-system, growth, vine, turtle-graphics, pruning | **Themes:** gameplay, hazards

L-system vine growing from a root point. Turtle graphics interpretation builds cylinder branches that reach toward the player. Damage prunes random branches. Formal grammar becomes organic growth.

*the simplest grammar generates the branching complexity of all plant life.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a vine growing from axiom F through production rules, branching procedurally toward its target
- **Critical Parameter:** _rules dictionary / _iteration_count -- grammar rules determine branching, iterations control depth
- **What Emerges:** biological growth pattern as hunting strategy -- the vine does not chase, it grows toward you
- **Triggers:** iteration_timer drives growth; player position attracts branch direction; damage on contact

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/branching_vine/branching_vine.gd` (326 lines) |
| **Scene** | `res://commons/hazards/branching_vine/branching_vine.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `BranchingVine` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `iteration_interval` | float | 3.0 |
| `max_iterations` | int | 4 |
| `segment_length` | float | 0.15 |
| `branch_angle` | float | 25.0 |
| `segment_radius` | float | 0.015 |
| `leaf_radius` | float | 0.035 |
| `player_bias` | float | 0.3 |
| `leaf_damage` | float | 8.0 |

### Key Methods

- `_on_ready()`
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()`
- `_build_l_system()` -- Clear existing segments
- `_create_segment(start: Vector3, end: Vector3, depth: int, is_leaf: bool)` -- Leaf sphere
- `_iterate_l_system()`
- `_process_visual(delta: float)` -- Iteration timer
- `_on_damaged(_amount: float)` -- Prune a random branch segment (and its children at higher depth)
- `_on_state_changed(new_state: BaseState)` -- Iterate sooner when chasing

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_5

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_5 | [7,3] | `-` |

### Relationships

- embodies L-systems sequence; contrasts with fractal_hydra (growth vs splitting)
- Needs: HazardCreatureBase [has]; L-system string rewriting [has]; procedural mesh generation [has]; player targeting [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
