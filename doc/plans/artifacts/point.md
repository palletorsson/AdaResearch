# Artifact: 1.0 A Point

> Pickable point sphere that continuously displays its world coordinates with a floating label.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** geometry | **Themes:** core, primitives

Pickable point sphere that continuously displays its world coordinates with a floating label.

## Design

- **Visual:** material: unlit, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/point/point.gd` (101 lines) |
| **Scene** | `res://commons/primitives/point/point.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `setup_point_scene()` -- Get references to the pickable sphere from scene
- `get_position_text()` -- Use the grab sphere's global position since the label is now a child of it
- `_process(_delta)` -- Update position text continuously
- `set_point_position(new_position: Vector3)`
- `set_point_color(color: Color)` -- Find or create point_color node
- `get_pickable_sphere()`
- `is_grabbed()`
- `set_label_visible(visible: bool)`
- `set_label_offset(offset: Vector3)`
- `set_label_color(color: Color)`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
