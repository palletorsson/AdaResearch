# Artifact: QFEP Calibrator

> E(S) = f(phi, delta_e, constraint) -- morphing dodecahedron with 3 Q-FEP parameter beams

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** qfep, parameters, calibration, oscillation, emergence | **Themes:** gameplay, hazards, qfep

Morphing dodecahedron with 3 parameter beams — phi (rotation speed), delta_e (damage), constraint (detection). Parameters oscillate, creating emergent behavior from their interaction. The QFEP laboratory's creature.

*the Q-FEP calibrator does not demonstrate the theory -- it IS the theory, parameters made flesh.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a dodecahedron whose shape responds to three Q-FEP parameters, beams probing the landscape
- **Critical Parameter:** _phi / _delta_e / _constraint -- three Q-FEP dimensions deform body and aim beams
- **What Emerges:** Q-FEP parameter space made physical -- the creature IS the theory, body IS the equation
- **Triggers:** parameter oscillation drives deformation; beams sweep based on Q-FEP state; proximity attacks

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/qfep_calibrator/qfep_calibrator.gd` (370 lines) |
| **Scene** | `res://commons/hazards/qfep_calibrator/qfep_calibrator.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `QFEPCalibrator` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `phi_frequency` | float | 0.7 |
| `delta_e_frequency` | float | 0.5 |
| `constraint_frequency` | float | 0.3 |
| `phi_amplitude` | float | 1.0 |
| `delta_e_amplitude` | float | 1.0 |
| `constraint_amplitude` | float | 1.0 |
| `beam_length` | float | 0.6 |
| `beam_radius` | float | 0.02 |
| `base_damage` | float | 10.0 |
| `base_detection` | float | 7.0 |
| `base_attack_range` | float | 1.5 |

### Key Methods

- `_on_ready()`
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()`
- `_build_leg(index: int)`
- `_get_param_text()`
- `_process_visual(delta: float)`
- `_update_beam(beam: MeshInstance3D, mat: StandardMaterial3D, param_val: float, base_color: Color)`
- `_apply_parameter_effects()` -- Phi: high = fast spin, harder to hit (already applied via spin_speed)
- `_process_chase(delta: float)`
- `_perform_attack()`
- `_flash_beam(beam: MeshInstance3D, mat: StandardMaterial3D, base_color: Color)`
- `_on_damaged(_amount: float)` -- Damage disrupts parameter oscillation phases
- `_on_state_changed(new_state: BaseState)` -- Death: beams collapse inward, body shrinks

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_7

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_7 | [7,9] | `-` |

### Relationships

- embodies qfeplaboratory sequence; theoretical capstone creature of the bestiary
- Needs: HazardCreatureBase [has]; dodecahedron mesh [has]; 3 parameter beams [has]; Q-FEP mapping [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
