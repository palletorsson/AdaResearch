# Artifact: Gradient Descent

> θ(t+1) = θ(t) - η∇f(θ) — follow the negative gradient downhill; four variants racing on the same surface

## Context

**Category:** algorithms | **Complexity:** intermediate
**Tags:** gradient_descent, optimization, loss_landscape, learning_rate, numerical_methods | **Themes:** algorithms, education, misc

Gradient descent optimization on a loss landscape — watch the optimizer slide downhill following the steepest direction. The engine behind all neural network training, with learning rate and momentum visible.

*learning is descending a landscape you cannot fully see — the gradient is the only local truth available*

## Design

- **Visual:** material: vertex_color, animation: vertex
- **Scale:** [8, 5, 8] (xlarge)
- **Interaction:** watch SGD, Momentum, Nesterov, and Adam race down a loss landscape, each with its own strategy
- **Critical Parameter:** learning_rate (η) — too small creeps, too large oscillates, just right converges
- **What Emerges:** Adam's adaptive superiority on most surfaces; saddle points that trap SGD; Nesterov's look-ahead advantage
- **Triggers:** mode button switches between optimizer race, convergence analysis, and Hessian eigenvalue display; function button cycles loss surfaces

## Architecture

| | |
|---|---|
| **File** | `algorithms/numericalmethods/gradientdescent/gradient_descent_visualization.gd` (939 lines) |
| **Scene** | `res://algorithms/numericalmethods/gradientdescent/gradient_descent_visualization.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `function_preset` | int | 0 |
| `learning_rate` | float | 0.05 |
| `momentum_beta` | float | 0.9 |
| `adam_beta1` | float | 0.9 |
| `adam_beta2` | float | 0.999 |
| `step_speed` | float | 2.0 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_create_materials()`
- `_create_mesh_instances()`
- `_create_labels()`
- `_create_controls()`
- `_cycle_mode()`
- `_cycle_function()`
- `_on_lr_changed(_v: float)`
- `_on_beta_changed(_v: float)`
- `_on_speed_changed(_v: float)`
- `_fn_name()`
- `_eval(p: Vector2)`
- `_gradient(p: Vector2)`
- `_hessian(p: Vector2)` -- Returns [fxx, fxy, fyx, fyy] via finite differences

### Grid Config

Accepts: `function_preset`

## Curriculum Position

### Sequences

- machinelearning
- testmaps

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Disposition_Test_Rooms | [7,1] | `-` |
| MachineLearning_Evolving_Creatures | [12,11] | `:0:0:0.1` |
| ML_Evolution | [7,3] | `:0:2:0.6` |
| ML_Gradient_Landscape | [2,2] | `:0:0:0.6` |

### Relationships

- unlocks neural_network_visualization (gradient descent IS the training loop); contrasts 9_3_smart_rockets_vr (gradient-free vs gradient-based optimization)
- Needs: VR push button for mode [has], sliders for learning rate, momentum, speed [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
