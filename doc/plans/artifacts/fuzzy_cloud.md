# Artifact: fuzzy_cloud

> fuzzy_cloud

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** qfep | **Themes:** philosophy, qfep, theory

fuzzy_cloud

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [4, 1, 4] (room_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/criticaltheory/fuzzycloud/fuzzy_cloud.gd` (623 lines) |
| **Scene** | `res://algorithms/criticaltheory/fuzzycloud/fuzzy_cloud.tscn` |
| **Registry** | `qfep.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_blobs` | int | 15 |
| `blob_size_min` | float | 1.0 |
| `blob_size_max` | float | 2.5 |
| `hair_density` | int | 3000 |
| `hair_length_min` | float | 0.3 |
| `hair_length_max` | float | 0.5 |
| `generate_on_ready` | bool | true |
| `blob_color` | Color | Color(0.95, 0.93, 0.88) |
| `hair_color` | Color | Color(0.98, 0.96, 0.92) |
| `randomize_colors` | bool | true |
| `color_variation` | float | 0.05 |

### Key Methods

- `_ready()`
- `create_sculpture()`
- `create_blob_cluster(parent)` -- Create the cluster of hairy blobs
- `generate_cluster_positions(count)`
- `create_hairy_blob(size)`
- `create_blob_core(size)`
- `create_hair_for_blob(parent_node, core_mesh, size)`
- `setup_vr_camera()` -- Remove existing camera if present
- `setup_environment()` -- Create VR-compatible camera setup
- `setup_lighting()` -- Create a world environment
- `add_gallery_lights()` -- Main directional light
- `create_gallery_room()`
- `create_optimized_hair_for_blob(parent_node, core_mesh, size)`
- `create_particle_hair_for_blob(parent_node, core_mesh, _size)`
- `optimize_for_vr(sculpture: Node3D)` -- Ensure all meshes are visible in VR

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
