# Artifact: Strange Attractors

> Gallery of strange attractors — Lorenz, Rössler, and others. Deterministic systems that never repeat yet stay bounded, tracing fractal paths through phase space forever.

## Context

**Category:** chaos | **Complexity:** intermediate
**Tags:** strange_attractor, lorenz, rossler, chaos, fractal, phase_space | **Themes:** art, chaos, dynamical_systems

Gallery of strange attractors — Lorenz, Rössler, and others. Deterministic systems that never repeat yet stay bounded, tracing fractal paths through phase space forever.

## Design

- **Visual:** material: vertex_color, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `algorithms/chaos/strangeattractors/strange_attractors.gd` (597 lines) |
| **Scene** | `res://algorithms/chaos/strangeattractors/strange_attractors.tscn` |
| **Registry** | `chaos.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `attractor_type` | AttractorType | AttractorType.LORENZ |
| `max_points` | int | 8000 |
| `iterations_per_frame` | int | 10 |
| `bounding_size` | float | 0.8 |

### Key Methods

- `_ready()`
- `_setup_trail_mesh()`
- `_setup_glow_mesh()`
- `_setup_point_mesh()`
- `_setup_labels()`
- `_setup_controllers()`
- `_on_param_a_changed(val: float)`
- `_on_param_b_changed(val: float)`
- `_on_speed_changed(val: float)`
- `_on_type_changed(val: float)`
- `_load_attractor_params(atype: AttractorType)`
- `_sync_controllers()`
- `_get_param_range(atype: AttractorType, param: String)`
- `_get_param_label(atype: AttractorType, param: String)`
- `_interpolate_params(delta: float)`

### Grid Config

Accepts: `attractor`

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_Synthesis | [4,10] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
