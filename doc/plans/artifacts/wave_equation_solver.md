# Artifact: Wave Equation Solver

> 2D wave equation on a membrane — finite difference PDE solver simulating a struck drumhead. Gaussian pulse propagates and reflects off clamped boundaries with viscous damping. ImmediateMesh surface colored by displacement: blue positive, red negative, dark zero.

## Context

**Category:** waves | **Complexity:** advanced
**Tags:** wave_equation, pde, finite_difference, membrane, drum, vibration | **Themes:** commons, mixed, waves

2D wave equation on a membrane — finite difference PDE solver simulating a struck drumhead. Gaussian pulse propagates and reflects off clamped boundaries with viscous damping. ImmediateMesh surface colored by displacement: blue positive, red negative, dark zero.

## Design

- **Visual:** material: vertex_color, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/wave_equation_solver/wave_equation_solver.gd` (370 lines) |
| **Scene** | `res://commons/artifacts/wave_equation_solver/wave_equation_solver.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `WaveEquationSolver` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `membrane_size` | float | 0.8 |
| `grid_res` | int | 48 |
| `height_scale` | float | 0.15 |
| `wave_speed` | float | 2.0 |
| `damping` | float | 0.002 |
| `initial_amplitude` | float | 1.0 |
| `initial_sigma` | float | 0.08 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_init_grids()`
- `_apply_gaussian_pulse(cx: float, cy: float, amp: float, sigma: float)`
- `_step_wave()`
- `_update_courant()`
- `_create_frame()`
- `_create_surface()`
- `_build_surface()`
- `_displacement_color(h: float)` -- Normalise to roughly [-1, 1] using initial amplitude
- `_create_labels()`
- `_create_vr_controls()`
- `_sync_sliders()` -- Damping range: 0.0 — 0.02
- `_on_damping_changed(_pos)`
- `_on_speed_changed(_pos)`
- `_on_strike(_b = null)` -- Apply a new Gaussian pulse at a slightly offset position

### Grid Config

Accepts: `damping`, `height_scale`, `initial_amplitude`, `wave_speed`

## Curriculum Position

### Sequences

- wavefunctions

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
