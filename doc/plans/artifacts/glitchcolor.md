# Artifact: Glitch Color

> Color glitch effects — channel shifting, bit corruption, and chromatic aberration. What happens when color data breaks in beautiful ways.

## Context

**Category:** color | **Complexity:** intermediate
**Tags:** color, glitch, chromatic_aberration, corruption, aesthetics | **Themes:** color, perception

Color glitch effects — channel shifting, bit corruption, and chromatic aberration. What happens when color data breaks in beautiful ways.

## Design

- **Visual:** material: unlit, animation: transform

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/glitchcolor/glitchcolor.gd` (321 lines) |
| **Scene** | `res://algorithms/color/glitchcolor/glitchcolor.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `setup_color_cubes()` -- Create a grid of cubes to demonstrate different bit effects
- `create_bit_shift_examples()` -- Each cube demonstrates a different bit manipulation technique
- `animate_bit_effects()`
- `rgb_channel_shift(t: float)` -- Shift RGB channels using bit operations
- `bit_rotation_color(t: float)` -- Rotate bits within color channels
- `binary_noise_color(t: float)` -- Generate color using binary noise patterns
- `xor_pattern_color(t: float, pos: Vector3)` -- Create XOR patterns based on position and time
- `bit_mask_color(t: float)` -- Use bit masks to create color patterns
- `digital_corruption(t: float)` -- Simulate digital corruption
- `chromatic_bit_shift(t: float)` -- Chromatic aberration using bit shifts
- `quantization_glitch(t: float)` -- Simulate color quantization errors
- `binary_fade(t: float)` -- Binary-style fade effect
- `bit_crush_rgb(t: float)` -- Bit-crushing effect on RGB channels

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- color

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Context | [13,12] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
