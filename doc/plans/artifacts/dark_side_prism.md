# Artifact: dark_side_prism

> Snell's law visualized — white beam enters prism, refracts into 7-color fan by wavelength-dependent angle

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** color | **Themes:** color, perception

dark_side_prism

*white light is not the absence of color — it is every color compressed into one, waiting for a boundary to reveal the multiplicity*

## Design

- **Visual:** material: vertex_color, animation: static
- **Scale:** [2.0, 2.0, 1.0] (compact)
- **Interaction:** to stand inside the album cover and watch white light split into a rainbow you can almost touch
- **Critical Parameter:** fan_length — controls how far the spectrum spreads, making refraction feel gentle or dramatic
- **What Emerges:** the glow/bloom system makes the beams feel volumetric even though they are 1-pixel ImmediateMesh lines
- **Triggers:** none — static sculpture, always present, always splitting

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/dark_side_prism.gd` (170 lines) |
| **Scene** | `res://algorithms/color/dark_side_prism.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_create_environment()`
- `_create_camera()`
- `_create_prism()`
- `_add_prism_edges(parent, size)` -- Create a simple wireframe triangle using ImmediateMesh for crisp lines
- `_create_trace_beams()`
- `_draw_line(im: ImmediateMesh, from: Vector3, to: Vector3, color: Color)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Color: Perception, Not Physics** (color) -- map: Color_Nails

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Nails | [7,3] | `:0:1.5` |

### Relationships

- contrasts with rainbow (full arc vs linear split); precedes spectrum_forest (spatial color mapping)
- Needs: VR interaction [missing]; adjustable prism angle [missing]; Label3D [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
