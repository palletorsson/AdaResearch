# Artifact: Turret Targeting

> Applied vector math for targeting — calculate direction, lead the target, account for projectile speed. Vectors solving the ancient problem of aiming at a moving target.

## Context

**Category:** vectors | **Complexity:** intermediate
**Tags:** vector, targeting, turret, prediction, direction, game_math | **Themes:** linear_algebra, math, vectors

Applied vector math for targeting — calculate direction, lead the target, account for projectile speed. Vectors solving the ancient problem of aiming at a moving target.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [2, 3, 3] (room_scale)

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/turret_targeting/turret_targeting.gd` (173 lines) |
| **Scene** | `res://algorithms/vectors/11_turret_targeting/TurretTargeting.tscn` |
| **Registry** | `vectors_demos.json` |
| **Class** | `TurretTargeting` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `turret_position` | Vector3 | Vector3(0, 0, 0) |
| `dropper_offset` | Vector3 | Vector3(0, 3, 2) |
| `drop_interval` | float | 2.5 |
| `max_balls` | int | 4 |
| `auto_drop` | bool | true |
| `detection_range` | float | 8.0 |
| `burn_time` | float | 0.3 |
| `laser_color` | Color | Color(1.0, 0.05, 0.0) |
| `show_stats` | bool | true |
| `show_controls` | bool | true |

### Dependencies

- `res://commons/artifacts/turret_targeting/laser_turret.tscn`
- `res://commons/artifacts/turret_targeting/ball_dropper.tscn`
- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_setup_turret()`
- `_setup_dropper()`
- `_create_stats_display()`
- `_create_vr_controls()`
- `_toggle_auto_drop()`
- `_process(_delta: float)`
- `_update_stats()`
- `_on_ball_destroyed(_ball: Node3D, _pos: Vector3)`
- `reset()`
- `set_auto_drop(enabled: bool)`
- `drop_ball_now()`
- `get_destroyed_count()`
- `apply_grid_config(config_data: Dictionary)`

### Signals

- `ball_destroyed`
- `target_acquired`
- `target_lost`

### Grid Config


## Curriculum Position

### Sequences

- forces

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
