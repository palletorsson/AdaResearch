# Artifact: ConeTwistBag

> cone-twist joint — ball-and-socket with angular limits. Swing span 45°, twist span 15°.

## Context

**Category:** forces | **Complexity:** intermediate
**Tags:** physics, joints, cone-twist-joint, angular-constraint, swing, pendulum, biomechanics, damping | **Themes:** physics, simulation

A punching bag hanging from a fixed anchor via a cone-twist joint. The cone-twist constraint limits the bag's swing to a 45-degree cone while allowing only 15 degrees of twist — like a ball-and-socket joint with angular limits. Nudge the bag and watch it swing within its invisible cone boundary, then slowly settle as damping absorbs the energy. The joint's bias and softness parameters control how sharply the limits are enforced.

*A cone-twist joint draws an invisible boundary in angle-space. The bag knows where it cannot go.*

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [6.0, 4.0, 1.0] (large)
- **Interaction:** To swing within a cone and twist within a narrower one — angular freedom with boundaries.
- **Critical Parameter:** swing_span (45°) and twist_span (15°) — the cone's opening angle and axial rotation limit.
- **What Emerges:** Pendular motion confined to an invisible cone. The bag never escapes its angular prison.
- **Triggers:** Space key applies impulse; bag swings within cone boundary.

## Architecture

| | |
|---|---|
| **File** | `algorithms/joint/06_cone_twist_bag/ConeTwistBag.gd` (194 lines) |
| **Scene** | `res://algorithms/joint/06_cone_twist_bag/ConeTwistBag.tscn` |
| **Registry** | `physics_simulation.json` |
| **Class** | `ConeTwistBagDemo` extends `Node3D` |
| **Pattern** | procedural |

### Key Methods

- `_make_emissive(color: Color, energy: float = 1.5)`
- `_make_emissive_transparent(color: Color, alpha: float = 0.6, energy: float = 1.2)`
- `_build_demo()` -- 1. Beam (static overhead support)
- `_process(_delta: float)`
- `_update_trail()` -- Sample the bottom-center of the bag
- `_update_joint_marker()` -- Keep the joint marker floating at the anchor position
- `_update_info_label()`
- `_nudge()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Joints** (joints) -- map: Joints_BallSocket

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Joints_BallSocket | [1,2] | `-` |

### Relationships

- Same joint type as CharacterRagdoll (hip). Contrasts with PendulumPin (unconstrained swing). Pairs with GimbalStabilizer (angular control).
- Needs: Impulse interaction [has]. Missing: cone visualization, span sliders.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
