# Artifact: Three Leg Critter

> tripod_stable(t) = triangle(foot_A, foot_B, foot_C) -- the first statically stable polygon

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** procedural, ik, legs, locomotion, tripod | **Themes:** gameplay, hazards

Three-legged procedural walker. Tripod gait — the minimum legs for static stability. Always has a support triangle.

*three points define a plane -- the first polygon that does not need to negotiate with falling.*

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [2.0, 1.0, 2.0] (compact)
- **Interaction:** three legs forming a triangle of support -- minimum number for static stability
- **Critical Parameter:** tripod foot positions -- three contacts define support triangle; mass must stay inside
- **What Emerges:** the triangle IS stability -- three legs is where gait becomes geometrically solvable
- **Triggers:** patrol_timer drives movement; tripod gait lifts one leg while two maintain stability

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/octapod_crawler/three_leg_critter.gd` (327 lines) |
| **Scene** | `res://commons/hazards/octapod_crawler/three_leg_critter.tscn` |
| **Registry** | `hazards.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `move_speed` | float | 2.0 |
| `turn_speed` | float | 40.0 |
| `patrol_speed` | float | 1.0 |
| `step_threshold` | float | 1.5 |
| `step_height` | float | 1.0 |
| `step_duration` | float | 0.25 |
| `step_overshoot` | float | 0.5 |

### Key Methods

- `_ready()`
- `_process(delta: float)` -- ── Movement ──────────────────────────────────────────────────────────
- `_update_gait(delta: float)` -- Compute where each foot's "home" position is (in world space)
- `_begin_step(leg_index: int, home: Vector3)`
- `_position_foot(leg_index: int, foot_target: Marker3D, _home: Vector3)`
- `_add_skinned_mesh(skeleton: Skeleton3D, mesh_name: String)`
- `_make_tapered_tube(bone_count: int, spacing: float, base_r: float, tip_r: float, segs: int)`
- `_add_foot_vis(foot: Marker3D)`
- `_build_scene()`

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_3

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_3 | [3,5] | `-` |

### Relationships

- third in leg progression; first stable polygon -- two legs fall, three stand
- Needs: MeshInstance3D [has]; triple IK chains [has]; tripod gait [has]; patrol [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
