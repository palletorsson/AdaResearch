# Artifact: 1.2: A triangleprofilese

> zigzag(n_triangles, height_high, height_low) — a strip of alternating triangles forming a pleat profile

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** 3d | **Themes:** core, primitives

Folded pink zigzag profile built from alternating peaks with drag points to edit the pleated surface.

*a complex surface profile is built from the simplest unit — a triangle — repeated with shared edges*

## Design

- **Visual:** material: vertex_color, animation: static
- **Scale:** [5.0, 1.0, 5.0] (compact)
- **Interaction:** learner feels how a complex perimeter shape is just many small triangles sharing edges
- **Critical Parameter:** grab_point_count — how many vertex handles exist along the zigzag strip perimeter
- **What Emerges:** the strip as a minimal surface description — pleats and folds arise from alternating vertex heights
- **Triggers:** dragging any grab handle — the triangle strip deforms, showing how local edits ripple through profile

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/triangleprofiles/triangleprofiles.gd` (244 lines) |
| **Scene** | `res://commons/primitives/triangleprofiles/triangleprofiles.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `base_y` | float | 0.5 |
| `segment_width` | float | 0.5 |
| `height` | float | 0.2 |
| `segment_count` | int | 32 |
| `double_sided` | bool | true |
| `sphere_scale` | float | 0.3 |
| `color_profile` | - | - |
| `color_alt` | - | - |

### Key Methods

- `_ready()`
- `_create_profile_folded_narrow(z_offset: float)`
- `_get_point_on_square_path(t: float, corners: Array)`
- `_update_profile_mesh_from_verts()`
- `_on_drag_point_moved(index: int, position: Vector3, meta: Dictionary)`
- `_add_face(st: SurfaceTool, a: Vector3, b: Vector3, c: Vector3, color: Color)`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Point_Triangle

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_Triangle | [7,3] | `:0:2` |

### Relationships

- sibling to triangle; demonstrates that profiles and extrusions are just strips of triangles
- Needs: [has many grabbable vertex handles [has], missing global height/frequency sliders]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
