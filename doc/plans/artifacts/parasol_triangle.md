# Artifact: Parasol Triangle

> triangle(elevated) + cylinder(stick) — a triangle raised on a pole like a parasol

## Context

**Category:** primitives | **Complexity:** simple
**Tags:** 3d, triangle, parasol, landmark | **Themes:** core, primitives

A triangle mounted on a thin stick like a parasol. Double-sided (pink front, blue back) to show orientation. Place multiples for a forest of triangle flags.

*elevate a shape and it becomes a symbol.*

## Design

- **Visual:** material: vertex_color, animation: static
- **Interaction:** to make triangles visible from across the room — a flag, a marker, an invitation
- **Critical Parameter:** stick_height / triangle_scale — the parasol must read as "triangle on a stick" instantly
- **What Emerges:** the triangle becomes architectural — not just a shape but a structure, a shelter, a sign
- **Triggers:** placed in map, visible from spawn — draws the eye upward to the triangle surface

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/triangle/parasol_triangle.gd` (111 lines) |
| **Scene** | `res://commons/primitives/triangle/parasol_triangle.tscn` |
| **Registry** | `primitives.json` |
| **Class** | `ParasolTriangle` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `stick_height` | float | 1.5 |
| `stick_radius` | float | 0.015 |
| `triangle_size` | float | 0.4 |
| `triangle_tilt` | float | 10.0 |
| `stick_color` | Color | Color(0.5, 0.5, 0.55) |
| `triangle_color_front` | Color | Color(1.0, 0.2, 0.5) |
| `triangle_color_back` | Color | Color(0.2, 0.5, 1.0) |

### Key Methods

- `_ready()`
- `_build_stick()`
- `_build_triangle()` -- Build a double-sided triangle mesh at the top of the stick
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `color`, `height`, `size`, `tilt`

## Curriculum Position

### Sequences

- primitives

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_Triangle | [4,5] | `-` |

### Relationships

- child of triangle.gd concept; placed in Point_Triangle map; one per map as landmark
- Needs: triangle mesh [has]; stick cylinder [has]; double-sided material [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
