# Artifact: Oscillating Wave

> The fundamental wave made visible—a 3D sine wave oscillating in real-time. Adjust amplitude, frequency, and phase to feel how these parameters shape the perpetual motion between poles.

## Context

**Category:** waves | **Complexity:** advanced
**Tags:** 3d, animation, sound, wave | **Themes:** oscillation, physics, waves

The fundamental wave made visible—a 3D sine wave oscillating in real-time. Adjust amplitude, frequency, and phase to feel how these parameters shape the perpetual motion between poles.

## Design

- **Visual:** material: standard, animation: particle_lifecycle
- **Scale:** [7.0, 8.0, 9.0] (environment)

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/oscillating_wave/OscillatingWave.gd` (358 lines) |
| **Scene** | `res://algorithms/wavefunctions/oscillating_wave/OscillatingWave.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural_mesh |

### Key Methods

- `_ready()`
- `_create_sphere()` -- Oscillating sphere
- `_create_labels()`
- `_create_wave_trail()` -- OPTIMIZED: Use MultiMesh instead of 200 individual MeshInstance3D nodes
- `_create_reference_sine_curve()` -- OPTIMIZED: Use MultiMesh instead of 100 individual MeshInstance3D nodes
- `_setup_audio()`
- `_setup_controls()` -- TODO: Connect to interactables if available
- `_update_sphere_position()` -- Calculate vertical position (y) using sine function
- `_update_sound_from_position()` -- Map horizontal position to frequency
- `_process(delta: float)`
- `_generate_audio_samples()`
- `_update_visualizations()` -- Pulse sphere based on sound intensity
- `set_oscillation_frequency(freq: float)`
- `set_oscillation_amplitude(amp: float)` -- Update reference curve when amplitude changes
- `_update_reference_curve()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
