# Artifact: ConvolutionalNeuralNetworksCNNs_VR

> feature_map = ReLU(input ⊛ kernel + bias); pool → repeat → dense → classify

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** machine_learning | **Themes:** ai, machine_learning

Walkable CNN architecture with interactive kernels

*the convolution is a bet that the same pattern can appear anywhere — translation invariance IS the inductive bias*

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [50, 54, 3] (xlarge)
- **Interaction:** walk through CNN layers as rooms, see feature maps shrink and deepen, feel hierarchy emerge
- **Critical Parameter:** kernel_weights — each 3x3 grid decides what pattern a filter detects
- **What Emerges:** hierarchical feature extraction — edges → textures → parts → objects, without anyone prescribing it
- **Triggers:** training_progress drives accuracy and activation pulsing; kernel editing changes learned features

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/convolutional_neural_networks_CNNs/ConvolutionalNeuralNetworksCNNs_VR.gd` (572 lines) |
| **Scene** | `res://algorithms/machinelearning/convolutional_neural_networks_CNNs/ConvolutionalNeuralNetworksCNNs_VR.tscn` |
| **Registry** | `machinelearning.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `layer_spacing` | float | 8.0 |
| `feature_map_size` | float | 3.0 |
| `kernel_size` | float | 0.4 |
| `input_size` | int | 28 |
| `num_conv_layers` | int | 3 |
| `num_filters_per_layer` | Array[int] | [16, 32, 64] |
| `kernel_dimensions` | int | 3 |
| `num_dense_layers` | int | 2 |
| `auto_train` | bool | true |
| `training_speed` | float | 0.1 |
| `show_activations` | bool | true |
| `show_backprop` | bool | true |
| `enable_kernel_editing` | bool | true |
| `enable_feature_inspection` | bool | true |
| `show_receptive_fields` | bool | true |
| `animate_forward_pass` | bool | true |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_initialize_kernels()`
- `_create_input_layer()`
- `_create_convolutional_layers()`
- `_create_feature_map(layer_idx: int, filter_idx: int)`
- `_create_kernel_viz(layer_idx: int, filter_idx: int)`
- `_create_pooling_layers()`
- `_create_dense_layers()`
- `_create_output_layer()`
- `_create_activation_flow()`
- `_create_control_panel()`
- `_create_info_panels()` -- Convolution explanation
- `_create_info_panel(pos: Vector3, text: String, color: Color)`
- `_animate_feature_maps(_delta)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| MachineLearning_Explainable_AI_XAI | [6,5] | `-` |
| ML_Perception | [3,1] | `:180:0:0.6` |

### Relationships

- depends on computer_vision_vr (hand-designed → learned kernels); contrasts neural_networks_vr (dense vs spatial connectivity)
- Needs: grabbable kernel editors [has], feature map inspection [has], forward pass particle animation [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
