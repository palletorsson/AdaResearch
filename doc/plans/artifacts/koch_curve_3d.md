# Artifact: 3D Koch Curve

> koch_arch = map_to_arch(koch_points(iterations)) + koch_legs, fractal curve bent into a portal

## Context

**Category:** unknown | **Complexity:** advanced
**Tags:** 3d | **Themes:** generation, procedural

Three-dimensional recursive Koch snowflake curve generation

*A doorway made of fractals is a threshold with infinite detail — you can never fully see what you are walking through.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 1.0, 6.0] (environment)
- **Interaction:** To be walked through — a Koch-curve arch with fractal legs, the boundary between order and chaos made into a doorway
- **Critical Parameter:** iterations — each level adds finer bumps to the arch and legs; at 4 the portal shimmers with fractal detail
- **What Emerges:** The arch shape from mapping a linear fractal onto a semicircle — the Koch bumps become architectural ornament
- **Triggers:** depth_layers create Z-depth volume; rotation_speed enables slow rotation for display

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/koshcurve/KochCurve3D.gd` (215 lines) |
| **Scene** | `res://algorithms/fractals/koshcurve/KochCurve3D.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `iterations` | int | 4 |
| `line_width` | float | 0.03 |
| `portal_width` | float | 2.0 |
| `portal_height` | float | 2.5 |
| `leg_height` | float | 1.5 |
| `depth_layers` | int | 3 |
| `layer_spacing` | float | 0.15 |
| `rotation_speed` | float | 0.0 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `setup_material()`
- `generate_portal()` -- Create multiple depth layers for volume
- `create_portal_layer(z_offset: float, layer_index: int)`
- `create_koch_arch(parent: Node3D)` -- Generate Koch points for a straight line
- `map_to_arch(points: Array[Vector3])`
- `create_legs(parent: Node3D)`
- `get_vertical_koch_points(x_pos: float, y_start: float, height: float)` -- Generate Koch curve points and rotate them to be vertical
- `get_koch_points(iter: int)` -- Arc length
- `koch_iteration(points: Array[Vector3])`
- `create_line_segment(start: Vector3, end: Vector3)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_KochSierpinski
- **Recursive Emergence** (recursiveemergence) -- map: RecursiveEmergence_Iterated_Function_Systems_IFS

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_KochSierpinski | [4,5] | `:0:-0.5` |
| RecursiveEmergence_Iterated_Function_Systems_IFS | [4,3] | `-` |

### Relationships

- 3D architectural interpretation of the Koch curve; contrasts flat fractal_koch_curve with volumetric space
- Needs: VR walk-through [has via geometry], rotation control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
