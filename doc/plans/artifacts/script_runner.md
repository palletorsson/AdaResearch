# Artifact: Script Runner

> a 3D SubViewport-rendered code panel that steps through an array of {text, duration, expression} line dictionaries one at a time, evaluating each via GDScript Expression and displaying the result beside the highlighted line

## Context

**Category:** tools | **Complexity:** intermediate
**Tags:** animation, pattern, tools, visualization | **Themes:** scripting, tools

Live code display with real Expression evaluation! Shows code executing line-by-line with actual results. Scripts: #point, #vector_math, #array, #pattern, #loop. Example: script_runner#point:90:1

*a running program is a trajectory through state space — the script runner makes this trajectory visible by showing each state transition as a physical moment in time*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** to watch code execute as a physical event — to stand beside a panel where each line of a for-loop lights up in sequence, with the array it's building appearing in 3D space as the loop runs
- **Critical Parameter:** script_name — determines which pre-configured script content is loaded (e.g., "array" loads an array construction demo); the script content is the entire lesson
- **What Emerges:** the temporal experience of code execution — seeing line 3 highlighted while lines 1-2 are dimmed above makes algorithms feel like processes unfolding in time rather than static text
- **Triggers:** the play button (Area3D) starts execution; auto_loop replays the script after completion; each line highlights for its configured duration; Expression.execute() evaluates GDScript expressions and returns results that appear on result_label

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/script_runner/script_runner.gd` (1098 lines) |
| **Scene** | `res://commons/primitives/script_runner/script_runner.tscn` |
| **Registry** | `script_runner.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `script_name` | String | "point" |
| `script_lines` | Array[Dictionary] | [] |
| `auto_loop` | bool | true |
| `start_delay` | float | 1.0 |
| `default_line_duration` | float | 1.0 |
| `highlight_transition_time` | float | 0.15 |
| `button_press_cooldown` | float | 0.25 |
| `panel_width` | float | 0.6 |
| `panel_height` | float | 0.4 |
| `font_size` | int | 18 |
| `line_height` | float | 24.0 |
| `show_line_numbers` | bool | true |
| `bg_color` | Color | Color(0.08, 0.08, 0.12, 0.95) |
| `text_color` | Color | Color(0.9, 0.9, 0.9, 1.0) |
| `keyword_color` | Color | Color(0.6, 0.8, 1.0, 1.0) |
| `string_color` | Color | Color(0.6, 1.0, 0.6, 1.0) |
| `number_color` | Color | Color(1.0, 0.8, 0.5, 1.0) |
| `comment_color` | Color | Color(0.5, 0.5, 0.5, 1.0) |
| `highlight_color` | Color | Color(1.0, 0.8, 0.0, 0.85) |

### Key Methods

- `_ready()`
- `_check_config_metadata()` -- Check for config metadata set by GridInteractablesComponent
- `_setup_display()` -- Create SubViewport for 2D UI
- `_setup_result_display()` -- Line indicator - shows "Line X:" above result
- `_setup_play_button()` -- Create clickable play button area - larger for VR touch
- `_load_script_config()` -- Load script lines from JSON based on script_name
- `_load_script_from_json(name: String)`
- `_convert_params(params: Dictionary)`
- `_update_code_display()`
- `_syntax_highlight(text: String)`
- `_highlight_code_part(text: String)`
- `_process(delta)`
- `_advance_line()`
- `_update_highlight(line_index: int)`
- `_evaluate_expression(expr_text: String)` -- Replace variable references with their values

### Signals

- `script_started`
- `script_stopped`
- `line_executed`
- `script_completed`

## Curriculum Position

### Sequences

- **Array Tutorial Sequence** (array_tutorial) -- map: Tutorial_Pattern
- **Primitives: Points Build Worlds** (primitives) -- map: Point_One

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_One | [9,4] | `#point:90:1` |

### Relationships

- appears in Tutorial_Pattern with script_name="array"; connects to column_3_z and grid_2d_4x4 (the physical arrays the script constructs in 3D); bridges code literacy with spatial array understanding
- Needs: SubViewport panel [has]; RichTextLabel for syntax-highlighted code [has]; play button Area3D [has]; result Label3D [has]; line indicator Label3D [has]; VR button interaction [has]; target_node for spawning objects [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
