# Artifact: Grid Editor Board (3D)

> 3D interactive version of the grid editor board. Same layout data as the shadow board but with real 3D elements — CRT monitors with phosphor flicker, VU meters with bouncing needles, interactive push buttons, slider faders, rotary knobs, toggle switches, speakers, and blinking LEDs.

## Context

**Category:** interactive | **Complexity:** beginner
**Tags:** grid_editor, control_board, 3d, interactive, black_mesa | **Themes:** arrays, data_structures

3D interactive version of the grid editor board. Same layout data as the shadow board but with real 3D elements — CRT monitors with phosphor flicker, VU meters with bouncing needles, interactive push buttons, slider faders, rotary knobs, toggle switches, speakers, and blinking LEDs.

## Design

- **Visual:** material: unlit, animation: transform

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/grid_editor_board/grid_editor_board_3d.gd` (1083 lines) |
| **Scene** | `res://commons/artifacts/grid_editor_board/grid_editor_board_3d.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `GridEditorBoard3D` extends `Node3D` |
| **Pattern** | vr_interactable |

### Exports

| Name | Type | Default |
|------|------|---------|
| `panel_width` | float | 2.8 |
| `panel_height` | float | 1.4 |
| `cell_size` | float | 0.06 |
| `grid_margin_left` | float | 0.08 |
| `default_grid_w` | int | 24 |
| `default_grid_h` | int | 14 |

### Dependencies

- `res://commons/artifacts/grid_editor_board/control_board_subset.gd`
- `res://commons/artifacts/grid_editor_board/control_board_element_factory.gd`
- `res://commons/interactables/push_button.tscn`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `apply_grid_config(config_data: Dictionary)`
- `_init_occupancy(w: int, h: int)`
- `_clear_board()`
- `_rebuild_for_grid(w: int, h: int)` -- Remove all visual children and rebuild for the new grid size
- `_build_back_panel()`
- `_build_grid()`
- `_build_status_label()`
- `_build_capture_camera()`
- `_load_preset(preset: Dictionary)`
- `_place_element(el_id: String, gx: int, gy: int)`
- `_build_inline_element(el_id: String, w: int, h: int, color_hex: String)`
- `_inline_flask(sx: float, sy: float, col: Color)` -- Cylindrical body
- `_inline_tube(sx: float, sy: float, col: Color, horizontal: bool)`

### Grid Config

Accepts: `element`, `elements`, `grid_size`, `id`, `placements`, `position`, `preset`

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Grid_Editor_Gallery | [1,4] | `#preset:mission_control_desk` |
| Grid_Editor_Gallery | [4,4] | `#preset:server_rack_panel` |
| Grid_Editor_Gallery | [7,4] | `#preset:comm_station` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
