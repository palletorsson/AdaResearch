# Artifact: LSTMs_VR

> c(t) = f·c(t-1) + i·tanh(W·[h,x]); h(t) = o·tanh(c(t)) — three gates control memory flow

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** machine_learning | **Themes:** ai, biology, machine_learning

Interactive LSTM cell with walkable memory flow

*memory is not storage — it is the continuous decision of what to keep and what to let go*

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [11, 4, 11] (xlarge)
- **Interaction:** walk inside a room-scale LSTM cell, grab the forget/input/output gates, watch memory particles flow
- **Critical Parameter:** forget_gate_activation — controls how much the cell remembers vs forgets at each step
- **What Emerges:** selective memory — the cell learns what to remember and what to discard without being told
- **Triggers:** gate activations modulate memory particle flow; training_progress drives loss and accuracy displays

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/LSTMs/LSTMs_VR.gd` (517 lines) |
| **Scene** | `res://algorithms/machinelearning/LSTMs/LSTMs_VR.tscn` |
| **Registry** | `machinelearning.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cell_size` | float | 3.0 |
| `gate_size` | float | 0.4 |
| `token_size` | float | 0.15 |
| `auto_train` | bool | true |
| `training_speed` | float | 0.1 |
| `sequence_length` | int | 8 |
| `enable_gate_grabbing` | bool | true |
| `enable_token_throwing` | bool | true |
| `show_memory_particles` | bool | true |
| `particle_count` | int | 20 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_build_lstm_cell()`
- `_create_floor_grid()`
- `_create_interactive_gates()`
- `_create_gate_sphere(gate_name: String, pos: Vector3, color: Color, label: String)`
- `_create_input_zone()`
- `_create_output_zone()`
- `_create_throwable_token(pos: Vector3, color: Color)`
- `_create_memory_flow()`
- `_create_training_controls()`
- `_create_info_panels()` -- Cell State info
- `_create_info_panel(pos: Vector3, text: String, color: Color)`
- `_update_gate_activations(_delta)`
- `_update_gate_visual(gate: RigidBody3D, activation: float, base_color: Color)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| MachineLearning_LSTMs | [6,5] | `-` |
| ML_Sequence_Memory | [2,1] | `:0:0:0.6` |

### Relationships

- unlocks transformers_vr (attention replaces recurrence); contrasts neural_networks_vr (feedforward has no memory)
- Needs: grabbable gate spheres [has], throwable input tokens [has], memory flow particles [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
