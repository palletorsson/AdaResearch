# Artifact: Julia Set Explorer

> z = z^2 + c per pixel (GPU shader), c controlled by VR sliders. 6 famous presets: Dendrite, San Marco, Rabbit, Spiral, Dragon, Douady.

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** julia_set, fractal, complex_numbers, exploration, mandelbrot | **Themes:** biology, commons, mixed, procedural

Navigate the infinite complexity of Julia sets — zoom into fractal boundaries where order and chaos interweave at every scale. Adjust the complex parameter c to morph between connected and disconnected Julia sets, watching the Mandelbrot set's DNA unfold.

*The Julia set explorer is a microscope for the Mandelbrot set — each c value reveals one of infinitely many fractal universes.*

## Design

- **Visual:** material: shader, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To be tuned — two sliders map to the real and imaginary parts of c, and every tiny adjustment reshapes the entire fractal
- **Critical Parameter:** c (complex) — moving c by 0.01 can change a connected dendrite into disconnected dust; the boundary is the Mandelbrot set
- **What Emerges:** The relationship between c and topology — learners discover that c values inside the Mandelbrot give connected Julia sets
- **Triggers:** VR sliders → c_real/c_imag update → shader recomputes instantly; preset buttons → jump to famous Julia sets

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/julia_set_explorer/julia_set_explorer.gd` (318 lines) |
| **Scene** | `res://commons/artifacts/julia_set_explorer/julia_set_explorer.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `JuliaSetExplorer` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `table_size` | float | 1.0 |
| `max_iterations` | int | 100: |
| `c_real` | float | -0.7: |
| `c_imag` | float | 0.27015: |
| `zoom` | float | 1.0: |
| `color_scheme` | int | 0: |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_table()`
- `_create_display()`
- `_create_info_label()`
- `_create_vr_controls()`
- `_sync_c_real_slider()`
- `_sync_c_imag_slider()`
- `_on_c_real_slider_moved(_position)`
- `_on_c_imag_slider_moved(_position)`
- `_apply_preset(idx: int)`
- `_update_shader_params()`
- `_update_info_label()`
- `_input(event)`
- `set_c(real: float, imag: float)`
- `get_c()`

### Grid Config


## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_JuliaSet

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_JuliaSet | [5,4] | `:0:0.5:1` |

### Relationships

- GPU sibling to julia_set (MultiMesh CPU); paired with mandelbrot_dive for the full Mandelbrot-Julia duality
- Needs: VR sliders [has], preset buttons [has], zoom [has via keyboard]. Mandelbrot overlay showing c position [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
