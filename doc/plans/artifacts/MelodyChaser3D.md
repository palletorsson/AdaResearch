# Artifact: MelodyChaser3D

> scale[i] -> position[i] = f(layout_type), proximity(hand, note) -> trigger

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** pattern, sound, vr, waves | **Themes:** music, oscillation, physics, waves

Follow-the-leader musical instrument - a glowing point traces a helix pattern through pitch space, play notes by following it with your hand

*A melody is a path through pitch-space traversed in time.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** Chase notes through 3D space as a glowing point traverses a musical scale
- **Critical Parameter:** beat_interval — sets the tempo that drives the melodic sequence
- **What Emerges:** spatial music — melody becomes a path you physically follow through space
- **Triggers:** proximity_threshold crossing between hand and note marker fires sound

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/mariocontrol/MelodyChaser3D.gd` (477 lines) |
| **Scene** | `res://algorithms/wavefunctions/mariocontrol/MelodyChaser3D.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `scale_type` | String | "major" |
| `beat_interval` | float | 0.6 |
| `proximity_threshold` | float | 0.12 |
| `base_octave` | int | 4 |
| `hardness` | float | 0.3 |
| `ring_time` | float | 0.5 |
| `leader_ahead_beats` | float | 1.5 |
| `layout_radius` | float | 0.25 |
| `helix_height` | float | 0.4 |

### Key Methods

- `_ready()`
- `apply_grid_config(data: Dictionary)`
- `_setup_note_positions()`
- `_setup_radial_layout(pattern: Array, space: Vector3, center: Vector3, num_notes: int)` -- Position notes radially from center - like clock positions
- `_setup_circle_of_fifths(pattern: Array, space: Vector3, center: Vector3, num_notes: int)` -- Classic circle of fifths - flat circle where position = fifth order
- `_setup_fifths_helix(pattern: Array, space: Vector3, center: Vector3, num_notes: int)` -- 3D Helix of Fifths - spiral upward through octaves
- `_process(delta: float)` -- Update cooldowns
- `_advance_leader()` -- Move leader to the note that's ahead of current
- `_check_note_proximity(user_pos: Vector3)`
- `_trigger_note(semitone: int, octave: int, note_index: int)`
- `_semitone_to_freq(semitone: int, octave: int)`
- `_generate_sine_bell(freq: float)`
- `_create_leader_point()`
- `_create_note_markers()` -- Clear existing
- `_create_fifth_connections()` -- Draw lines connecting consecutive notes to show the path

### Grid Config

Accepts: `scale`

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Effect_Sound

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Effect_Sound | [10,4] | `:90:0.4#scale:fifths_circle#tempo:60` |

### Relationships

- depends on pentatonic/chromatic scale definitions; contrasts with chord_tension_spring (melody vs harmony); unlocks embodied music theory
- Needs: VR hand tracking for proximity [has], scale selection [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
