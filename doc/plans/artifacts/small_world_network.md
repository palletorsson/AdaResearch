# Artifact: Small World Network

> Watts-Strogatz small-world model — 20 nodes in a ring lattice with k=4 neighbors. VR slider controls rewiring probability p: regular lattice at p=0, random graph at p=1. Watch how random shortcuts collapse average path length while clustering stays high. Gray edges are original, yellow edges are rewired shortcuts.

## Context

**Category:** algorithms | **Complexity:** intermediate
**Tags:** small_world, watts_strogatz, graph_theory, network, rewiring, clustering, path_length | **Themes:** algorithms, misc

Watts-Strogatz small-world model — 20 nodes in a ring lattice with k=4 neighbors. VR slider controls rewiring probability p: regular lattice at p=0, random graph at p=1. Watch how random shortcuts collapse average path length while clustering stays high. Gray edges are original, yellow edges are rewired shortcuts.

## Design

- **Visual:** material: vertex_color, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/small_world_network/small_world_network.gd` (380 lines) |
| **Scene** | `res://commons/artifacts/small_world_network/small_world_network.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Class** | `SmallWorldNetwork` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `node_count` | int | 20 |
| `k_neighbors` | int | 4 |
| `graph_radius` | float | 0.30 |
| `node_radius` | float | 0.015 |
| `edge_width` | float | 0.003 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_compute_node_positions()`
- `_build_ring_lattice()`
- `_add_edge(a: int, b: int, rewired: bool)`
- `_remove_edge_at(idx: int)`
- `_rewire_network()` -- Rebuild from scratch — ring lattice then rewire
- `_compute_avg_path_length()`
- `_bfs_distances(source: int)`
- `_compute_clustering_coefficient()`
- `_create_node_visuals()`
- `_update_node_colors()` -- Color by degree — min degree is k_neighbors (ring), max could be higher
- `_create_edge_visual()`
- `_rebuild_edges()`
- `_create_labels()`
- `_update_stats()`

### Grid Config

Accepts: `k_neighbors`, `node_count`, `rewire_probability`

## Curriculum Position

### Sequences

- testmaps

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Disposition_Test_Rooms | [4,9] | `:180` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
