# Artifact: FlowFieldMain

> v_i(t+1) = field[grid(pos_i)] — each agent reads one arrow and moves; the entire "decision" is a lookup

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** algorithm | **Themes:** algorithms, misc

FlowFieldMain

*purpose is not inside the agent — it lives in the field the agent inhabits*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [11.0, 36.0, 16.0] (world_scale)
- **Interaction:** to stand inside an invisible force that bends your trajectory before you notice — to feel steered without a steerer
- **Critical Parameter:** sequence_mode — switching between open field, U-wall, zig-zag, linear wind, and noise turbulence reveals how field topology determines reachability
- **What Emerges:** traffic-like lane formation where many agents converge on the same narrow corridor without coordination or negotiation
- **Triggers:** placing an obstacle changes the entire downstream vector field instantly via Dijkstra propagation from the target cell

## Architecture

| | |
|---|---|
| **File** | `algorithms/pathfinding/flow_field/FlowFieldMain.gd` (147 lines) |
| **Scene** | `res://algorithms/pathfinding/flow_field/FlowFieldMain.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `agent_scene` | PackedScene | - |

### Key Methods

- `_ready()`
- `_process(delta: float)` -- Animate Noise (if in final mode)
- `set_scenario(mode: int)`
- `spawn_agents(count: int)`
- `update_field(target_x: int, target_y: int)` -- Recalculate Logic
- `_unhandled_input(event: InputEvent)` -- VR: interaction handled by XR controller raycasts
- `raycast_from_mouse(mouse_pos)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Swarm Intelligence: No Leader, Yet Coordinated** (swarmintelligence) -- map: SwarmIntelligence_FlowFields

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| SwarmIntelligence_FlowFields | [3,5] | `-` |

### Relationships

- foundation for ant pheromone fields; contrasts with boid_flocking (field is external vs. social); shares grid_resolution with PhysarumColony approach
- Needs: [missing] no VR controls — mouse-click target and right-click walls are desktop-only; VR needs controller-raycast placement of target and obstacles

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
