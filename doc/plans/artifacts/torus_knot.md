# Artifact: Torus Knot

> A knot that wraps around a torus surface — p times around the meridian, q times around the longitude. The family includes the trefoil (2,3) and all torus links.

## Context

**Category:** geometry | **Complexity:** intermediate
**Tags:** parametric, knot, torus, topology, knot_theory, pickable | **Themes:** curves, math, parametric

A knot that wraps around a torus surface — p times around the meridian, q times around the longitude. The family includes the trefoil (2,3) and all torus links.

## Design

- **Visual:** material: shader, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/parametric/torus_knot.gd` (149 lines) |
| **Scene** | `res://commons/primitives/parametric/torus_knot.tscn` |
| **Registry** | `parametric.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `t_min` | float | 0.0 |
| `t_max` | float | 6.28 |
| `t_steps` | int | 256 |
| `tube_steps` | int | 16 |
| `tube_radius` | float | 0.08 |
| `p` | int | 3 |
| `q` | int | 7 |
| `major_radius` | float | 1.0 |
| `minor_radius` | float | 0.4 |
| `knot_scale` | float | 0.12 |
| `base_color` | Color | Color(0.9, 0.7, 0.2) |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `knot_point(t: float)` -- The curve winds p times around the torus and q times through the hole
- `knot_tangent(t: float)`
- `create_parametric_surface()`
- `apply_material()`
- `create_collision()`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
