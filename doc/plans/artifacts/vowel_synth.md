# Artifact: Vowel Synthesizer

> Real-time vowel synthesis from formant parameters — adjust F1 and F2 frequencies to morph between vowel sounds. Hear the geometry of the vowel triangle.

## Context

**Category:** speech | **Complexity:** intermediate
**Tags:** vowel, synthesizer, formant, f1, f2, speech, synthesis | **Themes:** audio, language, music, speech

Real-time vowel synthesis from formant parameters — adjust F1 and F2 frequencies to morph between vowel sounds. Hear the geometry of the vowel triangle.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/speech/phoneme_cloud/vowel_synth.gd` (363 lines) |
| **Scene** | `res://algorithms/speech/phoneme_cloud/vowel_synth.tscn` |
| **Registry** | `speech.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `attack_ms` | float | 20.0 |
| `release_ms` | float | 40.0 |
| `master_consonant_volume` | float | 0.3 |
| `vowel_volume` | float | 0.15 |

### Key Methods

- `_ready()`
- `_process(_delta_time)` -- Push target values from logic thread to audio thread
- `trigger_fricative(type: String, duration_ms: float = 150.0)`
- `trigger_plosive(type: String)` -- 1. Closure (Silence)
- `trigger_nasal(type: String, duration_ms: float = 100.0)`
- `trigger_affricate(_type: String)` -- ch = t + sh
- `stop()`
- `_fill_until(frames_requested: int)`
- `_process_audio_chunk(frames: int)` -- Parameter smoothing (Block Rate for F1/Delta)
- `_apply_event(e: AudioEvent)`
- `_get_filter_coeff(freq: float, q: float)`
- `_get_bp_coeff(freq: float, q: float)`
- `_process_filter(x: float, s: Array, c: Array)`
- `_poly_blep(t: float, dt: float)`
- `_exit_tree()`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
