# Artifact: 1.2: folded_strip

> triangle_strip(24) — alternating-height vertices forming a pleated ribbon of connected faces

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

pfolded_strip

*topology (which vertices are connected) is more fundamental than geometry (where they sit)*

## Design

- **Visual:** material: vertex_color, animation: static
- **Scale:** [3.0, 1.0, 1.0] (compact)
- **Interaction:** learner feels how topology (which vertices connect) differs from geometry (where vertices are)
- **Critical Parameter:** alternating high/low vertex heights — remove alternation and the pleat disappears
- **What Emerges:** mesh topology as a constraint — shared edges mean vertices drag each other's neighbours
- **Triggers:** dragging any of the 24 vertex grab spheres — adjacent triangles deform because they share edges

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/folded_strip/folded_strip.gd` (198 lines) |
| **Scene** | `res://commons/primitives/folded_strip/folded_strip.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `color_main` | Color | Color(0.2, 0.8, 0.3, 1.0) |
| `color_alt` | Color | Color(0.8, 0.2, 0.3, 1.0) |
| `sphere_scale` | float | 0.3 |
| `strip_y_offset` | float | 0.25 |
| `num_triangles` | int | 24 |
| `strip_length` | float | 4.0 |
| `base_y` | float | 0.0 |
| `height` | float | 0.5 |
| `alter_freeze` | bool | false |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `_initialize_straight_strip()` -- Create a straight strip along the X axis with alternating heights
- `_setup_drag_points()`
- `update_mesh()`
- `add_double_sided_triangle(st: SurfaceTool, v1, v2, v3, color: Color)`
- `apply_paper_material(mesh_instance: MeshInstance3D, color: Color)` -- Paper white
- `reset_strip()`
- `_on_point_moved(index: int, position: Vector3, meta: Dictionary)`
- `_on_point_picked_up(_index: int, _pickable, _meta: Dictionary)`
- `_on_point_dropped(_index: int, _pickable, _meta: Dictionary)`
- `print_help()`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Point_Triangle_Context

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_Triangle_Context | [1,6] | `:-90:-0.3` |

### Relationships

- extends triangleprofiles into 3D; shows how strip topology enables organic forms
- Needs: [has 24 grabbable vertex handles [has], missing global fold-height or frequency slider]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
