# Artifact: Shader 01 — Shaping Functions

> y = f(x) — step, smoothstep, sin, pow redistribute intensity across a gradient; each function is a policy for interpolation

## Context

**Category:** shaders | **Complexity:** advanced
**Tags:** book_of_shaders, shaping, math, curves, shaders | **Themes:** shaders, visual

Book of Shaders chapter on shaping functions and mathematical curves.

*a gradient is never neutral — every interpolation curve encodes a decision about how value distributes across space*

## Design

- **Visual:** material: shader, animation: shader_time
- **Interaction:** to drag sliders and watch a gradient bend — threshold sharpens, exponent compresses, frequency breathes
- **Critical Parameter:** threshold/exponent — the single number that decides where value concentrates
- **What Emerges:** the combined panel reveals that every smooth surface is a weighted vote among discontinuity, cubic ease, oscillation, and power law
- **Triggers:** slider changes feed directly into shader uniforms; combined mode blends all four shaping policies

## Architecture

| | |
|---|---|
| **File** | `algorithms/shaders/book_of_shaders/shader_01_shaping.gd` (138 lines) |
| **Scene** | `res://algorithms/shaders/book_of_shaders/shader_01_shaping.tscn` |
| **Registry** | `shaders.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_build_gallery()`
- `_process(_delta: float)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Shaders & Patterns: The Book of Shaders** (patterngeneration) -- map: Shader_01_Shaping

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Shader_01_Shaping | [5,7] | `-` |

### Relationships

- foundation for all subsequent shader maps — colors, shapes, patterns all reuse these shaping primitives
- Needs: [has] ShaderRackPanel sliders per display; [missing] no VR push buttons

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
