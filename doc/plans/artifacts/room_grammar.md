# Artifact: Room Grammar

> Shape grammar for architectural floor plans â€” binary space partitioning recursively splits a rectangle into rooms, draws wall outlines, adds door gaps, and colors rooms by area. The algorithmic foundation behind roguelike dungeon generation.

## Context

**Category:** proceduralgeneration | **Complexity:** intermediate
**Tags:** bsp, room, floor_plan, shape_grammar, procedural, architecture, partition | **Themes:** generation, procedural

Shape grammar for architectural floor plans â€” binary space partitioning recursively splits a rectangle into rooms, draws wall outlines, adds door gaps, and colors rooms by area. The algorithmic foundation behind roguelike dungeon generation.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/room_grammar/room_grammar.gd` (267 lines) |
| **Scene** | `res://commons/artifacts/room_grammar/room_grammar.tscn` |
| **Registry** | `procgen_extra.json` |
| **Class** | `RoomGrammar` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `quad_size` | Vector2 | Vector2(0.8, 0.8) |
| `seed_value` | int | 73 |
| `split_depth` | int | 5 |
| `min_room_size` | int | 12 |

### Key Methods

- `_ready()`
- `_build_floor_quad()`
- `_generate_floor_plan()`
- `_bsp_split(rect: Rect2i, depth: int)`
- `_draw_rect_outline(image: Image, rect: Rect2i, color: Color)`
- `_fill_rect(image: Image, x0: int, y0: int, w: int, h: int, color: Color)`
- `_add_doors(image: Image)`
- `_find_shared_wall(a: Rect2i, b: Rect2i)` -- Check if a's right edge == b's left edge (vertical shared wall)
- `_carve_door(image: Image, pos: Vector3i, width: int)`
- `_in_bounds(x: int, y: int)`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `min_room_size`, `seed`, `split_depth`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
