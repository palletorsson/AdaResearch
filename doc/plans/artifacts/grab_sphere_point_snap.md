# Artifact: grab_sphere_point_snap

> snap(position) = round(position / grid_size) * grid_size — discretise continuous space

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

grab_sphere_point_snap.

*quantization is a choice about what differences are worth keeping*

## Design

- **Visual:** material: unlit, animation: vertex
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** learner feels the tension between analog hand movement and digital quantized grid
- **Critical Parameter:** grid_size — the quantization step that snaps position to nearest lattice point
- **What Emerges:** the grid as a constraint that makes space countable; infinite positions become finite choices
- **Triggers:** moving the grabbed sphere — each snap is a click, a discrete decision

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/point/grab_sphere_point_snap.gd` (290 lines) |
| **Scene** | `res://commons/primitives/point/grab_sphere_point_snap.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grab_point_path` | NodePath | NodePath("GrabPoint") |
| `draw_sphere_path` | NodePath | NodePath("GrabPoint/DrawSphere") |
| `snap_to_grid` | bool | true |
| `grid_size` | float | 1.0 |
| `snap_on_drop` | bool | true |
| `trail_color` | Color | Color(0.2, 1.0, 0.6, 1.0) |
| `trail_max_points` | int | 4096 |
| `min_segment_distance` | float | 0.005 |
| `record_only_when_grabbed` | bool | true |
| `auto_clear_on_drop` | bool | false |
| `reference_frame_position` | Vector3 | Vector3(0, 0, 0.2) |
| `reference_frame_size` | float | 0.5 |
| `show_reference_frame` | bool | false |
| `show_data_table` | bool | true |
| `data_table_height_offset` | float | -0.35 |
| `data_table_color` | Color | Color(0.9, 0.95, 1.0, 1.0) |
| `data_table_font_size` | int | 20 |
| `data_table_update_interval` | float | 0.15 |

### Key Methods

- `_ready()` -- Prevent gravity gun from affecting drag points
- `_setup_trail()`
- `_setup_reference_frame()`
- `_process(delta: float)`
- `_rebuild_trail()`
- `snap_position_to_grid(pos: Vector3)`
- `_on_grab_point_dropped(_pickable)`
- `_on_grab_point_picked_up(_pickable)` -- Record starting position when picked up
- `clear_trail()`
- `get_trail_points()`
- `_setup_data_table()`
- `_update_data_table()`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Point_Line_Grid

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_Context | [4,3] | `:180` |
| Point_Line_Grid | [8,2] | `-` |

### Relationships

- sibling to draw_dot (continuous vs discrete); foundation for voxel/pixel thinking
- Needs: [has Label3D [has], grabbable sphere [has], missing grid_size VR slider]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
