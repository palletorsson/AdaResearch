# Artifact: 1.1 A Line

> length = |p2 - p1| — the distance between two points, made visible and tangible

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** interactive, string, vr | **Themes:** core, primitives

Interactive measuring line linking two grab spheres and rendering a cylinder with live distance text.

*a line segment has no intrinsic existence — it is defined entirely by its two endpoints*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** learner feels a line as relationship, not object — it exists because two points exist
- **Critical Parameter:** the glitch/haptic resistance when length is near an integer — whole numbers resist
- **What Emerges:** the privilege of whole numbers; the line as a tension between two positions
- **Triggers:** dragging either end grab sphere — length label updates live, glitch fires at integer thresholds

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/line/line.gd` (288 lines) |
| **Scene** | `res://commons/primitives/line/line.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `line_thickness` | float | 0.005 |
| `line_color` | Color | Color(1.0, 1.0, 1.0, 1.0) |
| `update_frequency` | float | 0.1 |
| `enable_resistance` | bool | true |
| `resistance_threshold` | float | 0.08 |
| `max_jitter` | float | 0.02 |
| `haptic_intensity_max` | float | 0.8 |
| `glitch_sound_volume_db` | float | -10.0 |

### Key Methods

- `_ready()`
- `create_length_label()`
- `_setup_glitch_audio()`
- `_generate_glitch_stream()`
- `update_connections()`
- `_process(_delta)`
- `_process_resistance(distance: float)` -- Calculate proximity to nearest integer (representing "perfect" measure)
- `_trigger_resistance_haptics(intensity: float)` -- Apply haptics to controllers holding the points
- `create_connection_line(start_pos: Vector3, end_pos: Vector3)`
- `update_line_transform(start_pos: Vector3, end_pos: Vector3)`
- `update_length_label(start_pos: Vector3, end_pos: Vector3)`
- `clear_connections()`
- `refresh_connections()`
- `_on_point_dropped(_pickable)` -- Send map-aware educational message via TextManager

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Point_Lines

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| _Example_Island | [4,1] | `-` |
| _Example_Linear | [4,1] | `-` |
| Point_Line | [4,2] | `:0:1` |
| Point_Line_Context | [4,4] | `:180` |
| Point_Lines | [14,1] | `:0:1` |
| Point_Lines | [15,1] | `:0:1` |

### Relationships

- depends on two static_points; unlocks all polygon primitives, perspective_lines, scale_lines
- Needs: [has Label3D [has], both endpoints are grabbable [has], missing color/thickness slider]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
