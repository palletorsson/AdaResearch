# Artifact: graphspace

> force-directed graph layout — repulsion F_rep = k/d^2, spring attraction F_spring = stretch * stiffness, iterated layout_iters times to position node_count rooms connected by weighted edges

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** graph_theory, force_directed_layout, network, spatialization, walkable_graph, topology, algorithm | **Themes:** algorithms, misc

Walk a graph instead of looking at one on paper. Rooms behave as nodes, bridges behave as edges, and a force-directed layout spreads the network until its tensions settle into a navigable form. Dijkstra-based lighting then turns graph distance into atmosphere, so structural centrality is felt as you move. It introduces graph theory as relation made spatial and prepares the sequence's traversal, flow, and connectivity problems.

*a graph is not a diagram — it is a set of relationships, and force-directed layout reveals that structure has a natural shape if you let physics find it*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [85.0, 5.0, 80.0] (xlarge)
- **Interaction:** to walk through a graph — each room is a node, each bridge is an edge, and the spatial layout you traverse was computed by the same physics that arranges molecules
- **Critical Parameter:** repulsion vs edge_stiffness ratio — high repulsion spreads rooms far apart, high stiffness pulls connected rooms close, the equilibrium defines the walkable topology
- **What Emerges:** Dijkstra distances from focal_node drive ambient lighting — rooms far from the focus grow dim and cool, creating atmospheric depth from pure graph distance
- **Triggers:** seed parameter deterministically generates the graph; structure_type enum (RANDOM, BY_DEGREE, BY_DISTANCE) selects which building appears at each node based on graph properties

## Architecture

| | |
|---|---|
| **File** | `algorithms/graphtheory/graphspace/graphspace.gd` (460 lines) |
| **Scene** | `res://algorithms/graphtheory/graphspace/graphspace.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Class** | `GraphSpace` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `room_scene` | PackedScene | - |
| `portal_scene` | PackedScene | - |
| `structure_scenes` | Array[PackedScene] | [] |
| `structure_type` | StructureType | StructureType.BY_DEGREE |
| `place_structures` | bool | true |
| `structure_scale_range` | Vector2 | Vector2(0.8, 1.2) |
| `structure_offset_y` | float | 0.0 |
| `structure_rotation_variation` | float | 45.0 |
| `node_count` | int | 12 |
| `avg_degree` | float | 2.2 |
| `layout_iters` | int | 250 |
| `seed` | int | 20250922 |
| `room_radius` | float | 6.0 |
| `edge_stiffness` | float | 0.08 |
| `repulsion` | float | 180.0 |
| `damping` | float | 0.82 |
| `bounds_radius` | float | 60.0 |
| `show_debug_links` | bool | true |
| `planar_layout` | bool | true |
| `plane_y` | float | 0.0 |
| `plane_snap` | float | 0.25 |
| `make_bridges` | bool | true |
| `bridge_width` | float | 1.2 |
| `bridge_thickness` | float | 0.12 |
| `focal_node` | int | 0 |
| `edge_cost_min` | float | 1.0 |
| `edge_cost_max` | float | 3.0 |

### Key Methods

- `_ready()`
- `_build_graph()`
- `_add_edge(a: int, b: int, w: float)`
- `_has_edge(a: int, b: int)`
- `_calculate_node_degrees()`
- `_rand_cost()`
- `_rand_sphere(r: float)`
- `_layout_graph()` -- repulsion
- `_select_structure_for_node(node_index: int)`
- `_select_by_degree(node_index: int)`
- `_select_by_distance(node_index: int)`
- `_select_by_custom_pattern(node_index: int)` -- Example custom pattern: alternate structures in a pattern
- `_instantiate_world()` -- Rooms
- `_apply_ambience_by_distance()`
- `_dijkstra(src: int)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Graph Theory: Connections Define Structure** (graphtheory) -- map: GT_Foundations
- **Unused Artifacts Test** (testmaps) -- map: Test_9
- **Unused Artifacts Test** (testmaps) -- map: Test_11

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| GraphTheory_Graphspace | [6,5] | `-` |
| GT_Foundations | [1,1] | `:0:0:0.6` |
| Test_11 | [5,3] | `-` |
| Test_9 | [6,5] | `:0:0:0.1` |

### Relationships

- foundation of graph theory sequence — introduces what a graph IS as a walkable space; extends into KonigsbergBridge which constrains the topology to a specific historical problem
- Needs: slider_horizontal [missing]; push_button [missing]; Label3D [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
