# Artifact: Color Space Navigator

> 3D RGB color cube with HSV cylinder — visualize color as a navigable 3D space. Eight corner spheres mark primary and secondary colors, 500 sample points fill the interior, and a companion HSV cylinder shows the same colors in cylindrical coordinates.

## Context

**Category:** color | **Complexity:** intermediate
**Tags:** color, rgb, hsv, color_space, 3d, visualization | **Themes:** color, perception

3D RGB color cube with HSV cylinder — visualize color as a navigable 3D space. Eight corner spheres mark primary and secondary colors, 500 sample points fill the interior, and a companion HSV cylinder shows the same colors in cylindrical coordinates.

## Design

- **Visual:** material: vertex_color, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/color_space_navigator/color_space_navigator.gd` (316 lines) |
| **Scene** | `res://commons/artifacts/color_space_navigator/color_space_navigator.tscn` |
| **Registry** | `color.json` |
| **Class** | `ColorSpaceNavigator` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cube_size` | float | 0.7 |
| `sample_count` | int | 500 |
| `corner_radius` | float | 0.018 |
| `sample_radius` | float | 0.006 |
| `show_hsl_cylinder` | bool | true |

### Key Methods

- `_ready()`
- `_build_rgb_cube()`
- `_create_sample_points_rgb()`
- `_add_axis_label(text: String, pos: Vector3, color: Color)`
- `_build_hsl_cylinder()`
- `_create_sample_points_hsl(offset: Vector3, radius: float, height: float)`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `cube_size`, `sample_count`, `show_hsl`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
