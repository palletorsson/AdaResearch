# Artifact: City Generator

> X → F[+X]F[-X][^X][&X], F → FF at 90° with pitch/yaw branching — orthogonal L-system architecture

## Context

**Category:** procedural | **Complexity:** advanced
**Tags:** graph, l_system, procedural, simulation | **Themes:** grammar, l_systems, procedural

Procedural pipe network using orthogonal L-System rules

*Change the angle from 25° to 90° and the tree becomes a city — architecture is botany with right angles.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 6.0, 11.0] (world_scale)
- **Interaction:** To generate a city from a seed — watch right-angle grammar produce buildings, bridges, and towers
- **Critical Parameter:** iterations — complexity grows exponentially; each level adds branching in all six cardinal directions
- **What Emerges:** Building-like structures from botanical L-system rules reinterpreted with 90-degree angles
- **Triggers:** Iteration 2 → simple cross; iteration 4 → dense brutalist megastructure; slow rotation reveals 3D depth

## Architecture

| | |
|---|---|
| **File** | `algorithms/lsystems/Architecture/CityGenerator.gd` (152 lines) |
| **Scene** | `res://algorithms/lsystems/Architecture/CityGenerator.tscn` |
| **Registry** | `lsystems.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `iterations` | int | 3 |
| `step_length` | float | 0.5 |
| `structure_thickness` | float | 0.06 |
| `angle` | float | 90.0 |
| `show_animation` | bool | false |

### Dependencies

- `res://utils/lsystem.gd`

### Key Methods

- `_ready()` -- Setup materials for architectural look
- `generate_structure()`
- `create_ui()`
- `_on_iterations_changed(new_val: float)`
- `update_ui()`
- `_process(delta: float)` -- Gentle rotation to view the structure
- `set_iterations(val: int)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Grammar Systems: Rules Generate Structure** (grammar_systems) -- map: Voxel_Grammar
- **L-Systems: Rules Grow Structure** (lsystems) -- map: LSystems_Architecture

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| LSystems_Architecture | [3,2] | `-` |
| LSystems_CityGenerator | [4,4] | `-` |
| Voxel_Grammar | [2,2] | `:180:1:0.55` |

### Relationships

- Feeds into LSystems_Architecture. Contrasts with lsystem_dungeon (3D growth vs 2D floor plan). Uses Turtle3D.
- Needs: VR complexity controller [has], Label3D [has], auto-rotation [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
