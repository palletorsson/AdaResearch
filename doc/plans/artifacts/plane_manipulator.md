# Artifact: plane_manipulator

> This script animates a random walk on a 3D plane. It simulates walkers that move across the plane's surface, raising the ground to create trails in real-time.

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** random_walk, terrain, realtime, trails, 3d | **Themes:** procedural, emergence

This script animates a random walk on a 3D plane. It simulates walkers that move across the plane's surface, raising the ground to create trails in real-time.

## Design

- **Visual:** material: standard, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/plane_effects/plane_manipulator.gd` (168 lines) |
| **Scene** | `res://algorithms/randomness/plane_effects/plane_manipulator.tscn` |
| **Registry** | `randomness.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `border_size` | int | 20 |
| `raise_amount` | float | 0.1 |

### Key Methods

- `_ready()` -- Wait a bit longer for the plane to be fully instantiated
- `_initialize_walk(mi: MeshInstance3D)` -- Try to read plane-like segmentation and size properties; otherwise, infer from mesh
- `_process(_delta)`
- `_is_in_border(x: int, y: int)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- randomness

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Random_Plane | [13,10] | `-` |
| TestMap_Unused_15 | [3,1] | `:0:2` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
