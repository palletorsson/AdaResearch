# Artifact: Cellular Automata 3D Stacked

> layer(n+1) = life_rules(layer(n)) with outward→upward phase transition

## Context

**Category:** emergence | **Complexity:** intermediate
**Tags:** 3d, cellular_automata, emergence | **Themes:** biology, cellular_automata, computation, emergence

Stacked 3D Cellular Automata.

*The same rules in two phases — spread then rise — produce architecture from biology.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** To extrude a flat Game of Life into a standing structure — watch 2D rules build 3D form
- **Critical Parameter:** GrowthPhase transition at generation 4 — switches from horizontal expansion to vertical extrusion
- **What Emerges:** Architectural columns and cantilevers from the phase transition between horizontal and vertical growth
- **Triggers:** Phase OUT → spreading footprint; phase UP → columns rising; generation 10 → growth halts

## Architecture

| | |
|---|---|
| **File** | `algorithms/cellularautomata/cellular_automata_3d_stacked/CellularAutomata3DStacked.gd` (123 lines) |
| **Scene** | `res://algorithms/cellularautomata/cellular_automata_3d_stacked/CellularAutomata3DStacked.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | - | 20 |
| `generation_interval` | - | 0.5 |

### Dependencies

- `res://commons/primitives/cubes/cube_scene.tscn`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `initialize_grid()`
- `generate_next_layer()`
- `grow_outward()`
- `grow_upward()`
- `count_neighbors(layer, x, z)`
- `apply_rules(current_state, neighbors)`
- `create_cube(x, y, z)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_CrossSequence
- **Recursive Emergence** (recursiveemergence) -- map: RecursiveEmergence_Cellular_Automata_3D

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_CrossSequence | [6,2] | `-` |
| RecursiveEmergence_Cellular_Automata_3D | [6,5] | `-` |

### Relationships

- Feeds into CA_AgentsCircuits. Builds on ca_columns (implicit stacking vs explicit phase control).
- Needs: VR phase controls [missing], speed control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
