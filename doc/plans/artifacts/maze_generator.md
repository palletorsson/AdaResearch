# Artifact: Maze Generator

> recursive backtracking — push cell, carve to random unvisited neighbor, backtrack when stuck, repeat until all visited

## Context

**Category:** proceduralgeneration | **Complexity:** beginner
**Tags:** maze, generation, dfs, backtracking, spanning_tree, procedural | **Themes:** generation, procedural

Procedural maze generation â€” watch walls carve out paths through depth-first search, recursive backtracking, or randomized algorithms. Every maze is a spanning tree of a grid graph.

*a perfect maze has no loops — every dead end is a branch that reached as far as it could before the algorithm gave up and tried another way*

## Design

- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** to watch a maze carve itself in real time, the red cursor probing forward then retreating, every wall removal a permanent decision
- **Critical Parameter:** generation_speed — controls how fast the animation plays; slow enough to follow the algorithm's logic, fast enough to see the pattern emerge
- **What Emerges:** the algorithm naturally creates a spanning tree — every cell is reachable from every other cell via exactly one path, without coding for it
- **Triggers:** show_generation enables animated step-by-step; each generation_step carves one wall and advances; stack exhaustion signals completion

## Architecture

| | |
|---|---|
| **File** | `algorithms/proceduralgeneration/procedural_logic/mazegeneration/maze_generator.gd` (387 lines) |
| **Scene** | `res://algorithms/proceduralgeneration/procedural_logic/mazegeneration/maze_generator.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `maze_width` | int | 9 |
| `maze_height` | int | 9 |
| `cell_size` | float | 1.0 |
| `wall_height` | float | 1.0 |
| `generation_speed` | float | 0.2 |
| `wall_color` | Color | Color(0.4, 0.4, 0.6) |
| `path_color` | Color | Color(0.8, 0.8, 0.9) |
| `current_color` | Color | Color(0.9, 0.3, 0.3) |
| `visited_color` | Color | Color(0.3, 0.9, 0.3) |
| `show_generation` | bool | true |

### Key Methods

- `_ready()` -- Removed setup_environment() as it's now in the scene
- `_process(delta: float)`
- `initialize_maze()` -- Initialize maze grid - true = wall, false = path
- `create_maze_visuals()` -- Clear existing visuals cleanly
- `create_cell_visual(x: int, y: int)`
- `create_wall_collider(x: int, y: int)` -- Create StaticBody3D for collision
- `create_floor_collider(x: int, y: int)` -- Create StaticBody3D for floor collision
- `start_generation()` -- Start from top-left path cell
- `generation_step()`
- `get_unvisited_neighbors(cell: Vector2i)`
- `update_cell_visual(cell: Vector2i, color: Color)`
- `update_wall_visual(x: int, y: int)` -- Convert wall to path
- `create_entrance_exit()` -- Create entrance at top
- `update_entrance_exit_visual(x: int, y: int)`
- `get_maze_string()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Relationships

- paired with caverandomwalk in PG_Caves_Mazes; contrasts structured corridors with organic tunnels; both create explorable spaces from rules
- Needs: animated generation [has]; wall/floor collision [has]; entrance/exit creation [has]; VR walk-the-maze [has]; maze size slider [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
