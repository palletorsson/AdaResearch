# Artifact: Wall Pattern Colorizer

> Colors wall cubes in the GridSystem MultiMesh using wallpaper group tiling math. Each wall cube becomes a pixel in the pattern. Uses per-instance colors like GridColorizer.

## Context

**Category:** shaders | **Complexity:** intermediate
**Tags:** colorizer, wallpaper_groups, multimesh, wall, pattern, configurable | **Themes:** shaders, visual

Colors wall cubes in the GridSystem MultiMesh using wallpaper group tiling math. Each wall cube becomes a pixel in the pattern. Uses per-instance colors like GridColorizer.

## Design

- **Visual:** material: shader, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/wall_pattern_colorizer/wall_pattern_colorizer.gd` (316 lines) |
| **Scene** | `res://commons/artifacts/wall_pattern_colorizer/wall_pattern_colorizer.tscn` |
| **Registry** | `shaders.json` |
| **Class** | `WallPatternColorizer` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `wallpaper_group` | int | 5 |
| `tile_scale` | float | 2.0 |
| `pattern_seed` | int | 0 |
| `auto_cycle` | bool | false |
| `cycle_interval` | float | 8.0 |

### Key Methods

- `_ready()`
- `apply_grid_config(config_data: Dictionary)`
- `_find_multimesh()`
- `_search_for_multimesh(node: Node)`
- `_search_for_grid_structure(node: Node)`
- `_apply_wall_pattern()`
- `_count_wall_cubes()`
- `_adjust_material()`
- `_start_cycling()`
- `_exit_tree()`
- `_get_symmetric_uv(uv: Vector2)`
- `_sample_domain(uv: Vector2)`
- `_generate_domain()`

### Grid Config

Accepts: `auto_cycle`, `cycle_interval`, `pattern_seed`, `tile_scale`, `wallpaper_group`

## Curriculum Position

### Sequences

- patterngeneration
- array_tutorial

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
