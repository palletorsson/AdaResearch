# Artifact: Complexity Pattern

> Conway_GoL(B3/S23) on toroidal grid, density ~40% -> edge-of-chaos automaton

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** cellular_automata, chaos, graph, pattern, qfep | **Themes:** biology, philosophy, qfep, theory

Game of Life cellular automata at edge of chaos. Self-organizing complexity.

*at the edge of chaos, simple local rules generate unbounded computational complexity*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** watch complexity self-organize — gliders, oscillators, structures nobody designed
- **Critical Parameter:** initial density (~0.4) — the lambda value that places this automaton at the edge of chaos
- **What Emerges:** gliders, blinkers, and long-lived metastable structures from random initial conditions
- **Triggers:** cells die out below threshold -> auto-reseed; each generation applies B3/S23 rules

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/complexity_pattern.gd` (167 lines) |
| **Scene** | `res://commons/interfaces/qfep/complexity_pattern.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `QFEPComplexityPattern` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | int | 16 |
| `cell_size` | float | 0.04 |
| `update_interval` | float | 0.2 |
| `alive_color` | Color | Color(0.2, 0.9, 0.4, 1.0) |
| `dead_color` | Color | Color(0.1, 0.15, 0.1, 0.5) |

### Key Methods

- `_ready()`
- `_init_grid()`
- `_build_visuals()`
- `_randomize_cells()` -- Initialize with ~40% alive - edge of chaos density
- `_process(delta: float)`
- `_step_simulation()` -- Game of Life rules (edge of chaos rules)
- `_count_neighbors(x: int, z: int)`
- `_update_visuals()`
- `reset()`
- `get_generation()`

## Curriculum Position

### Sequences

- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Lambda_Spectrum

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| QFEP_Lambda_Spectrum | [9,2] | `:0:0.8` |

### Relationships

- unlocks edge_core concept; depends on cellular automata foundations; contrasts preserved_pattern (frozen) and chaos_particles (dissolved)
- Needs: VR speed control [missing], touch-to-toggle cells [missing], density slider [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
