# Artifact: Timbre Sculptor

> waveform(t) = sum(amplitude[n] * sin(n * fundamental * t)) — additive synthesis with audio

## Context

**Category:** waves | **Complexity:** expert
**Tags:** animation, interactive, visualization, vr, wave | **Themes:** oscillation, physics, waves

VR rack panel with 8 vertical sliders controlling harmonic amplitudes for real-time additive synthesis. Waveform and spectrum displays show the result. Preset buttons morph to known timbres: sine, square, sawtooth, triangle, and vowel approximations (A, E, I, O, U). Cognitive compression: timbre IS harmonic recipe — the slider heights ARE the spectrum. Build Fourier transforms with your hands.

*Timbre is the harmonic fingerprint of a sound — the same note on different instruments differs only in overtone amplitudes.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** Sculpt timbre by adjusting harmonic sliders and hearing the result immediately in VR
- **Critical Parameter:** fundamental_freq — sets the pitch; all harmonics are integer multiples of this
- **What Emerges:** the sound of mathematics — square waves, saw waves, clarinet, all from the same formula
- **Triggers:** slider movement changes harmonic amplitudes; morph_speed controls preset transitions

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/timbre_sculptor/timbre_sculptor.gd` (466 lines) |
| **Scene** | `res://commons/artifacts/timbre_sculptor/timbre_sculptor.tscn` |
| **Registry** | `wavefunctions.json` |
| **Class** | `TimbreSculptor` extends `Node3D` |
| **Pattern** | vr_interactable |

### Exports

| Name | Type | Default |
|------|------|---------|
| `fundamental_freq` | float | 220.0 |
| `volume_db` | float | -6.0 |
| `morph_speed` | float | 4.0 |

### Dependencies

- `res://commons/ui/ada_palette.gd`
- `res://commons/audio/interfaces/VRAudioControlSliderVertical.tscn`
- `res://commons/audio/interfaces/VRSimpleWaveform.tscn`
- `res://commons/audio/interfaces/VRSpectrumDisplay.tscn`
- `res://commons/interactables/push_button.tscn`

### Key Methods

- `_ready()` -- Initialize amplitudes to sine preset
- `_build_panel()`
- `_add_section_label(text: String, pos: Vector3)`
- `_build_sliders()`
- `_build_preset_buttons()`
- `_build_displays()` -- Waveform display (left side of display row)
- `_setup_audio()`
- `_on_slider_changed(_value, harmonic_idx: int)` -- Read normalized value from slider
- `_on_preset_pressed(preset_name: String)`
- `_sync_sliders_to_amplitudes()`
- `_process(delta: float)` -- Morph amplitudes toward target
- `_generate_audio()`
- `_unhandled_input(event: InputEvent)` -- F1-F9 trigger presets
- `set_preset(preset_name: String)`
- `set_fundamental(freq: float)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Effect_Sound

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Effect_Sound | [7,8] | `-` |

### Relationships

- depends on AudioStreamGenerator; contrasts with additive_wave_demo (audible vs visual synthesis); unlocks timbre-as-spectrum
- Needs: VR sliders for 8 harmonics [has], preset buttons [has], audio output [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
