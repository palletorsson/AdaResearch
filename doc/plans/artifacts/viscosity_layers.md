# Artifact: Viscosity Layers

> Three transparent columns side by side — air, water, and honey. Drop identical balls into all three simultaneously and watch them descend at wildly different rates. Quadratic drag makes the honey ball crawl while the air ball plummets. Real-time speed readouts let you compare terminal velocities directly.

## Context

**Category:** physics | **Complexity:** beginner
**Tags:** viscosity, drag, friction, fluid, terminal_velocity, comparison, air, water, honey | **Themes:** drag, viscosity, fluid_resistance, terminal_velocity

Three transparent columns side by side — air, water, and honey. Drop identical balls into all three simultaneously and watch them descend at wildly different rates. Quadratic drag makes the honey ball crawl while the air ball plummets. Real-time speed readouts let you compare terminal velocities directly.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/physicssimulation/viscositylayers/viscositylayers.gd` (239 lines) |
| **Scene** | `res://algorithms/physicssimulation/viscositylayers/viscositylayers.tscn` |
| **Registry** | `physics_simulation.json` |
| **Class** | `ViscosityLayers` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `column_height` | float | 1.2 |
| `column_width` | float | 0.25 |
| `ball_radius` | float | 0.03 |
| `gravity_strength` | float | 9.8 |
| `ball_mass` | float | 1.0 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_columns()`
- `_create_balls()`
- `_drop_all()`
- `_physics_process(delta: float)`
- `_create_labels()`
- `_create_vr_controls()`
- `_reset()`
- `_input(event: InputEvent)`
- `reset()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- forces

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
