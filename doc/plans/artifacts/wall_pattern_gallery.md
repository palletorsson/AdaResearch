# Artifact: Wall Pattern Gallery

> for each Z-cluster of wall cubes, overlay all 6 faces with wallpaper_tile shader using a unique wallpaper group + neon palette + procedural domain texture

## Context

**Category:** shaders | **Complexity:** advanced
**Tags:** shader, wallpaper, pattern, gallery, grid, catalyst | **Themes:** art, shaders, visual

Overlays wallpaper group shader patterns on grid wall cubes in clusters. Like the catalyst chromatic mode but with all 17 wallpaper groups as GPU shaders.

*symmetry is not decoration — it is the mathematical structure that determines how a small truth propagates through space*

## Design

- **Visual:** material: generated_texture, animation: static
- **Interaction:** to walk through a tunnel where every surface — walls, ceiling, floor — displays a different wallpaper group in neon color, immersing the body in mathematical symmetry
- **Critical Parameter:** cluster_size — cubes per Z-row before advancing to next wallpaper group; controls how much space each symmetry type occupies
- **What Emerges:** the combination of 8 neon palettes, 6 pattern types (blocks/stripes/diagonal/concentric/checker/cross), and 17 wallpaper groups produces a corridor that never visually repeats
- **Triggers:** on _ready, discovers GridSystem wall cubes, groups by Z, assigns wallpaper groups cyclically; builds ceiling and floor pattern cubes to close the tunnel

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/wall_pattern_gallery/wall_pattern_gallery.gd` (335 lines) |
| **Scene** | `res://commons/artifacts/wall_pattern_gallery/wall_pattern_gallery.tscn` |
| **Registry** | `shaders.json` |
| **Class** | `WallPatternGallery` extends `Node3D` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cluster_size` | int | 3 |
| `tile_scale` | float | 4.0 |
| `emission_strength` | float | 0.8 |
| `grout_width` | float | 0.02 |

### Dependencies

- `res://commons/resourses/shaders/wallpaper_tile.gdshader`

### Key Methods

- `_ready()`
- `apply_grid_config(config_data: Dictionary)`
- `_build_gallery()` -- Find GridSystem like the catalyst does
- `_add_overlay(world_pos: Vector3, cube_size: float, material: ShaderMaterial, gp: Vector3i)` -- Thin box slightly larger than cube face, offset outward
- `_create_wallpaper_material(group_id: int, palette_id: int, seed_val: int)`
- `_generate_domain_texture(seed_val: int, palette_id: int)`
- `_build_ceiling(grid_origin: Vector3, positions: Array, z_values: Array, z_groups: Dictionary, cs: float, total_size: float)` -- Find the tunnel dimensions: max Y of wall cubes, and X range of void cells
- `_add_lights(grid_origin: Vector3, z_values: Array, total_size: float)`

### Grid Config

Accepts: `cluster_size`, `emission_strength`, `grout_width`, `tile_scale`

## Curriculum Position

### Sequences

- **Shaders & Patterns: The Book of Shaders** (patterngeneration) -- map: Cove_Display_Gallery

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Cove_Display_Gallery | [1,2] | `#cluster_size:3#tile_scale:4.0#emission_strength:1.0#grout_width:0.02` |

### Relationships

- the architectural showcase complement to pattern_maker_station (interactive editing vs passive immersion); uses same wallpaper_tile.gdshader
- Needs: [missing] no VR sliders or buttons — configuration only through apply_grid_config; the gallery is a walk-through experience, not an interactive station

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
