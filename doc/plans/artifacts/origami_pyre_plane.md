# Artifact: Pyre Plane

> fold(sheet) -> glide(plane) -> ignite(pyre) -- origami paper plane that catches fire in flight

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** origami, foe, floor_hazard, fire, transformation, invariant | **Themes:** gameplay, hazards

A folded-paper rectangle of fire that sits beneath transport cubes. Its creased surface cycles through affine transformations — scale pulses, rotation sweeps, positional drift — while the rectangular shape remains invariant. Demonstrates that identity persists under transformation. Lethal on contact.

*the fold that makes it fly also makes it burn -- structure and destruction share the same geometry.*

## Design

- **Visual:** material: standard, animation: particle_lifecycle
- **Interaction:** a folded paper plane that glides, burns, and crashes as a flaming projectile
- **Critical Parameter:** _current_phase (AffinePhase) -- folding, gliding, igniting, crashing in sequence
- **What Emerges:** paper folding meets thermodynamics -- beauty IS weapon, pyre IS payload
- **Triggers:** phase timer drives state transitions; affine transforms control fold; fire activates mid-flight

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/origami_pyre_plane/origami_pyre_plane.gd` (445 lines) |
| **Scene** | `res://commons/hazards/origami_pyre_plane/origami_pyre_plane.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `OrigamiPyrePlane` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `base_width` | float | 2.0 |
| `base_depth` | float | 3.0 |
| `crease_count` | int | 4 |
| `scale_pulse_range` | Vector2 | Vector2(0.8, 1.2) |
| `rotation_speed` | float | 0.3 |
| `translation_drift` | float | 0.5 |
| `cycle_duration` | float | 4.0 |
| `damage_on_enter` | float | 100.0 |
| `parchment_color` | Color | Color(0.82, 0.72, 0.55, 1.0) |
| `ember_color` | Color | Color(1.0, 0.45, 0.05, 1.0) |
| `crease_glow_color` | Color | Color(1.0, 0.55, 0.1, 1.0) |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_create_materials()`
- `_build_mesh_root()`
- `_build_paper_geometry()`
- `_rebuild_paper()`
- `_fold_height(crease_index: int)` -- Alternating ridge/valley folds — subtle paper lift
- `_build_crease_lines()`
- `_rebuild_creases()`
- `_build_kill_zone()`
- `_on_body_entered(body: Node3D)`
- `_is_player(body: Node3D)`
- `_spawn_flare_burst()`
- `_build_ember_particles()`
- `_build_glow_light()`

### Signals

- `player_killed`

### Grid Config

Accepts: `base_depth`, `base_width`, `crease_count`, `cycle_duration`, `damage_on_enter`, `ember_color`, `rotation_speed`, `scale_pulse_range`, `translation_drift`

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo | [3,9] | `-` |

### Relationships

- origami family with miura_crawler, kresling_spire; unique fire element
- Needs: fold geometry [has]; glide physics [has]; fire effect [has]; phase state machine [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
