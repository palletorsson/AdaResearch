# Artifact: Paradox Stalker

> self_reference(t) -> contradiction(t) -- creature that contradicts itself, two ghosts swapping

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** paradox, duality, zeno, foundations, ghost | **Themes:** gameplay, hazards, philosophy

Two overlapping ghost silhouettes that swap which is real every 3 seconds. 50% chance damage passes through the wrong ghost. Zeno's paradox: sometimes halves distance to player instead of closing it.

*the creature that contradicts itself cannot be defeated by consistency -- act in paradox.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** two ghostly forms alternating reality -- the liar paradox given a body and a lunge
- **Critical Parameter:** _a_is_real -- which ghost is real; swap_timer oscillates; only real ghost can be hit
- **What Emerges:** self-reference as combat -- the paradox is not a puzzle to solve but a state to survive
- **Triggers:** swap_timer alternates real/ghost; lunge_timer drives attacks; wrong ghost is invulnerable

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/paradox_stalker/paradox_stalker.gd` (210 lines) |
| **Scene** | `res://commons/hazards/paradox_stalker/paradox_stalker.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `ParadoxStalker` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `ghost_separation` | float | 0.6 |
| `swap_interval` | float | 3.0 |
| `zeno_chance` | float | 0.15 |
| `lunge_speed` | float | 5.0 |
| `lunge_duration` | float | 0.4 |
| `ghost_a_color` | Color | Color(0.8, 0.2, 0.9, 0.5) |
| `ghost_b_color` | Color | Color(0.2, 0.8, 0.9, 0.5) |
| `real_emission` | Color | Color(1.0, 0.4, 1.0) |

### Key Methods

- `_on_ready()`
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()` -- Ghost A
- `_build_humanoid(parent: Node3D, mat: StandardMaterial3D)` -- Torso
- `_process_visual(delta: float)`
- `_process_attack(delta: float)`
- `_process_chase(delta: float)` -- Periodically lunge
- `_zeno_halve()` -- Halve the distance to the player (teleport closer)
- `_on_damaged(amount: float)` -- Only take damage if hit matches the "real" ghost

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_6

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_6 | [3,7] | `-` |

### Relationships

- embodies foundationscrisis; pairs with escher_stairwalker (logical vs geometric paradox)
- Needs: HazardCreatureBase [has]; dual ghost nodes [has]; swap logic [has]; lunge [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
