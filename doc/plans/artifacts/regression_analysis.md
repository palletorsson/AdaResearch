# Artifact: Regression Analysis

> Linear and polynomial regression fitting with visual residual analysis.

## Context

**Category:** statistics | **Complexity:** intermediate
**Tags:** regression, linear, fitting, statistics | **Themes:** math, probability, statistics

Linear and polynomial regression fitting with visual residual analysis.

## Design

- **Visual:** material: vertex_color, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/statistics/06_regression_analysis/RegressionAnalysisVR.gd` (1422 lines) |
| **Scene** | `res://algorithms/statistics/06_regression_analysis/RegressionAnalysisVR.tscn` |
| **Registry** | `statistics.json` |
| **Class** | `RegressionAnalysisVR` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `enable_vr` | bool | true |
| `regression_type` | RegressionType | RegressionType.LINEAR |
| `num_data_points` | int | 50 |
| `noise_level` | float | 0.3 |
| `true_slope` | float | 2.0 |
| `true_intercept` | float | 1.0 |
| `show_residuals` | bool | true |
| `show_confidence_intervals` | bool | true |
| `auto_fit` | bool | false |
| `regularization_lambda` | float | 0.1 |

### Key Methods

- `_ready()`
- `setup_vr()`
- `setup_visualization()` -- Scatter plot display
- `create_coordinate_axes()`
- `add_axis_labels(parent: Node3D)`
- `create_interactive_cursor()`
- `setup_info_displays()`
- `_on_controller_button(button_name: String)`
- `_input(event: InputEvent)`
- `generate_sample_data()`
- `generate_linear_data(rng: RandomNumberGenerator)`
- `generate_polynomial_data(rng: RandomNumberGenerator)`
- `generate_multiple_regression_data(rng: RandomNumberGenerator)`
- `generate_logistic_data(rng: RandomNumberGenerator)`
- `update_scatter_plot()` -- Clear existing points

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
