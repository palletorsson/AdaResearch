# Artifact: VectorDotProduct

> Push a ball and watch the work meter tick up. Push perpendicular and it stays at zero. W = F . d = |F||d|cos(theta) — the dot product made visceral. The meter turns green for positive work, red for negative, yellow for zero. Total work accumulates as a running dot-product integrator over the path.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** dot_product, work, energy, projection, alignment, interactive, vectors, physics | **Themes:** linear_algebra, math, vectors

Push a ball and watch the work meter tick up. Push perpendicular and it stays at zero. W = F . d = |F||d|cos(theta) — the dot product made visceral. The meter turns green for positive work, red for negative, yellow for zero. Total work accumulates as a running dot-product integrator over the path.

## Design

- **Visual:** material: unlit, animation: particle_lifecycle
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/vectors/03_dot_product/VectorDotProduct.gd` (207 lines) |
| **Scene** | `res://algorithms/vectors/03_dot_product/VectorDotProduct.tscn` |
| **Registry** | `vectors.json` |
| **Pattern** | procedural |

### Dependencies

- `res://algorithms/vectors/shared/gadgets/hinge_panel_gadget.gd`

### Key Methods

- `_ready()` -- Half-size for exhibition display
- `_process(delta: float)`
- `_update_info(a_vec: Vector3, b_vec: Vector3, dot: float, proj: Vector3, rej: Vector3, theta: float, cos_theta: float)`
- `_create_angle_arc()`
- `_update_angle_arc(a: Vector3, b: Vector3)`
- `_create_projection_dot()`
- `_cache_vector_nodes(arrow: Node3D, cache_dict: Dictionary)`
- `_get_vector_fast(arrow: Node3D, cache_dict: Dictionary)`
- `_update_vector_fast(_arrow: Node3D, vector: Vector3, cache_dict: Dictionary)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- forces

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| VectorDotProduct | [1,2] | `-` |
| VectorOperations | [1,2] | `-` |
| Vectors_2 | [0,0] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
