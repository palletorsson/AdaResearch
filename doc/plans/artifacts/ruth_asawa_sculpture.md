# Artifact: Ruth Asawa Sculpture

> surface(u,v) = radius_curve(u) * (cos(v), sin(v)) + audio_reactive_deformation

## Context

**Category:** waves | **Complexity:** expert
**Tags:** wave | **Themes:** art, oscillation, physics, waves

Wire forms inspired by Ruth Asawa's crocheted metal sculptures—nested surfaces that oscillate between inside and outside, solid and void. Topology as frozen wave, material as line drawing in space.

*A wire sculpture is a surface of revolution that has learned to listen.*

## Design

- **Visual:** material: unlit, animation: vertex
- **Scale:** [1.0, 7.0, 7.0] (environment)
- **Interaction:** See a wire-mesh sculpture breathe and deform in response to ambient sound
- **Critical Parameter:** radius_curve — the profile curve that defines the lobed silhouette of the hanging form
- **What Emerges:** sculpture as frozen listening — the wire form records sound as spatial deformation
- **Triggers:** audio spectrum analysis modulates the surface; band_values deform the mesh in real-time

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/ruth_asawa/ruth_asawa_sculpture.gd` (344 lines) |
| **Scene** | `res://algorithms/wavefunctions/ruth_asawa/ruth_asawa_sculpture.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `u_resolution` | int | 60 |
| `v_resolution` | int | 100 |
| `radius_curve` | Curve | - |
| `height` | float | 10.0 |
| `max_radius` | float | 2.0 |
| `wireframe` | bool | true |
| `auto_rotate` | bool | true |
| `rotation_speed` | float | 0.5 |
| `audio_reactive` | bool | false |
| `frequency_influence` | float | 2.0 |
| `volume_pulse` | float | 0.5 |
| `audio_smoothing` | float | 0.2 |
| `spectrum_bands` | int | 16 |
| `debug_audio` | bool | false |

### Key Methods

- `_ready()`
- `_ensure_audio_initialized()`
- `_setup_audio_analysis()`
- `_process(delta: float)`
- `_create_default_curve()` -- Create a lobed shape similar to the reference
- `generate_surface()`
- `_update_audio_data()`
- `_apply_audio_to_mesh()` -- Apply volume pulse to scale
- `generate_audio_reactive_surface()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_John_Cage

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_John_Cage | [6,6] | `-` |

### Relationships

- depends on AudioEffectSpectrumAnalyzer; contrasts with kusama_sine (restrained wire vs excessive dots); unlocks sound-reactive sculpture
- Needs: VR spatial audio input [has], spectrum analyzer [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
