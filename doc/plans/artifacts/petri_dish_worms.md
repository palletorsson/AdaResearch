# Artifact: Petri Dish Worms

> worm_segment[i].offset = amplitude * sin(oscillation_speed * t + i * phase_per_segment)

## Context

**Category:** emergence | **Complexity:** intermediate
**Tags:** worms, petri_dish, steering, biological, procedural_animation | **Themes:** biology, commons, emergence, mixed

Simulated worm-like organisms wriggling in a petri dish — simple steering behaviors and sinusoidal body motion create surprisingly lifelike movement. Emergence of biological-looking behavior from minimal procedural rules.

*A worm moves by passing a sine wave through its body — locomotion is traveling oscillation.*

## Design

- **Visual:** material: vertex_color, animation: vertex
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** Watch tiny worms wriggle in a petri dish, their bodies undulating with traveling sine waves
- **Critical Parameter:** oscillation_speed — controls the frequency of the sinusoidal body wave
- **What Emerges:** lifelike locomotion from pure sine wave propagation through a chain of segments
- **Triggers:** time drives phase-offset sine displacement along each worm body segment

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/petri_dish_worms/petri_dish_worms.gd` (231 lines) |
| **Scene** | `res://commons/artifacts/petri_dish_worms/petri_dish_worms.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `PetriDishWorms` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_worms` | int | 8 |
| `worm_length` | int | 20 |
| `worm_speed` | float | 0.3 |
| `oscillation_speed` | float | 1.5 |
| `oscillation_amplitude` | float | 0.02 |
| `worm_radius` | float | 0.012 |
| `dish_radius` | float | 0.15 |
| `dish_height` | float | 0.02 |
| `medium_color` | Color | Color(0.9, 0.85, 0.7, 0.4) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_setup_dish()`
- `_spawn_worms()`
- `_setup_controls()`
- `_on_speed_changed()`
- `_process(delta: float)`
- `_update_worm(index: int, delta: float)`
- `_draw_worm(index: int)`
- `_exit_tree()`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Synthesis_Lab

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Synthesis_Lab | [7,2] | `:180:0.8` |

### Relationships

- depends on phase-offset sine animation; contrasts with dna_specimen (motile vs static biology); unlocks biological wave locomotion
- Needs: VR sliders for speed/count [has], observation [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
