# Artifact: Constraints & Joints

> Joint constraints that restrict rigid body degrees of freedom: hinges (1 rotational DOF), sliders (1 translational DOF), ball-and-socket (3 rotational DOF), and fixed joints. Watch how iterative constraint solvers project invalid states back onto the constraint manifold, enforcing physical laws as geometric relationships.

## Context

**Category:** physics | **Complexity:** advanced
**Tags:** constraints, joints, hinge, slider, ball_socket, degrees_of_freedom, solver, iterative, manifold | **Themes:** constraints, joints, degrees_of_freedom, solver

Joint constraints that restrict rigid body degrees of freedom: hinges (1 rotational DOF), sliders (1 translational DOF), ball-and-socket (3 rotational DOF), and fixed joints. Watch how iterative constraint solvers project invalid states back onto the constraint manifold, enforcing physical laws as geometric relationships.

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/physicssimulation/constraints/_deprecated/constraints.gd` (141 lines) |
| **Scene** | `res://algorithms/physicssimulation/constraints/constraints.tscn` |
| **Registry** | `physics_simulation.json` |
| **Class** | `Constraints` extends `Node3D` |
| **Pattern** | procedural |

### Key Methods

- `_ready()` -- Scale for VR reachability
- `_initialize_constraints()` -- Initialize all constraint systems
- `_physics_process(delta: float)`
- `_update_hinge_system(delta)` -- Update hinge angle
- `_update_slider_system(delta)` -- Update slider position
- `_update_pendulum_system(delta)` -- Simple pendulum physics
- `_update_constraint_visualization()` -- Update hinge system
- `_connect_ui()`
- `_on_reset_pressed()` -- Reset all constraint systems
- `_on_pause_pressed()`
- `_on_constraint_type_pressed()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Joints** (joints) -- map: Joints_SoftConstraints
- **Physics Simulation: The Engine Behind Reality** (physicssimulation) -- map: PhysicsSim_Bodies
- **Physics Simulation: The Engine Behind Reality** (physicssimulation) -- map: PhysicsSimulation_Constraints

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Joints_SoftConstraints | [4,4] | `-` |
| PhysicsSim_Bodies | [6,2] | `:0:1` |
| PhysicsSimulation_Constraints | [3,2] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
