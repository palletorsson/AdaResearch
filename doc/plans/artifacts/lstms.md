# Artifact: LSTMs

> Long Short-Term Memory network visualization — gates (forget, input, output) controlling information flow through time. The architecture that solved vanishing gradients and enabled sequence modeling.

## Context

**Category:** machinelearning | **Complexity:** advanced
**Tags:** lstm, recurrent, gates, memory, sequence, deep_learning | **Themes:** ai, machine_learning

Long Short-Term Memory network visualization — gates (forget, input, output) controlling information flow through time. The architecture that solved vanishing gradients and enabled sequence modeling.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [24.0, 26.0, 14.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/LSTMs/LSTMs.gd` (420 lines) |
| **Scene** | `res://algorithms/machinelearning/LSTMs/LSTMs.tscn` |
| **Registry** | `machinelearning.json` |
| **Class** | `LSTMShowcase` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `sequence_length` | int | 8 : set = _set_sequence_length |
| `animation_speed` | float | 1.0 |
| `pulse_intensity` | float | 1.0 |
| `gate_responsiveness` | float | 0.5 |
| `training_rate` | float | 0.1 |
| `auto_train` | bool | true |
| `color_input` | - | - |
| `color_output` | - | - |
| `color_cell` | - | - |
| `color_gate` | - | - |
| `color_memory` | - | - |
| `color_activation` | - | - |

### Key Methods

- `_set_sequence_length(v: int)`
- `_ready()`
- `_process(delta: float)`
- `create_input_tokens()` -- Create input sequence tokens with gold metallic materials
- `create_output_tokens()` -- Create output sequence tokens with blue metallic materials
- `create_memory_particles()` -- Create glowing memory flow particles with transparency
- `create_gate_activations()` -- Create gate activation indicators with cyan glow
- `setup_training_metrics()` -- Initialize loss and accuracy meter positions
- `animate_lstm_cell(delta)` -- Animate LSTM cell core — green glow intensifies with training
- `animate_gates(delta)` -- Animate forget gate — controls what to discard from cell state
- `animate_memory_flow(delta)` -- Animate memory particles flowing through the LSTM pipeline
- `animate_gate_activations(_delta)` -- Animate gate activation indicator ring
- `update_training_metrics(delta)` -- Update loss meter position and color
- `set_training_progress(progress: float)`
- `set_loss_value(loss: float)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
