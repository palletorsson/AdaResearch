# Artifact: Sine Wall Explanation

> surface(z, y, t) = base + A * sin(freq * z + speed * t) — animated sine wall cross-section

## Context

**Category:** waves | **Complexity:** beginner
**Tags:** sound, visualization, wave | **Themes:** oscillation, physics, waves

1m display case showing how sine function shapes corridor walls. Animated mini-corridor demonstrates x = A·sin(f·z)—the wave displacement that creates the sine_wall_corridor effect. Shows amplitude, frequency parameters with live wave propagation.

*A sine wall is a one-dimensional wave made two-dimensional — displacement as architecture.*

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle
- **Interaction:** Examine a labeled sine wall model with adjustable parameters in VR
- **Critical Parameter:** frequency — controls the wave density visible on the wall display
- **What Emerges:** understanding what amplitude, frequency, and phase mean on a physical wall surface
- **Triggers:** VR sliders adjust amplitude, frequency, and wave speed; mesh regenerates per frame

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/sine_wall_explanation/sine_wall_explanation.gd` (421 lines) |
| **Scene** | `res://commons/artifacts/sine_wall_explanation/sine_wall_explanation.tscn` |
| **Registry** | `wavefunctions.json` |
| **Class** | `SineWallExplanation` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `display_size` | float | 1.0 |
| `wall_height` | float | 0.5 |
| `wall_length` | float | 0.9 |
| `amplitude` | float | 0.12 |
| `frequency` | float | 2.0 |
| `wave_speed` | float | 0.4 |
| `animate` | bool | false |
| `wall_color` | Color | Color(0.8, 0.2, 0.3) |
| `value_color` | Color | Color(1.0, 1.0, 0.4) |
| `frequency_min` | float | 0.5 |
| `frequency_max` | float | 6.0 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_base_plate()`
- `_create_wall()`
- `_update_wall_mesh(phase: float)`
- `_create_sine_curve()`
- `_update_sine_curve(phase: float)`
- `_create_value_markers()` -- MultiMesh for marker spheres (one draw call instead of VALUE_POINTS)
- `_update_value_markers(phase: float)`
- `_create_reference_plane()`
- `_create_labels()`
- `_create_frequency_slider()`
- `_on_freq_slider_moved(_pos)`
- `_process(delta)`
- `_exit_tree()`
- `set_amplitude(a: float)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Sine_Space

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Sine_Space | [4,1] | `:-90:-.4` |

### Relationships

- depends on SurfaceTool mesh animation; contrasts with sine_wall_corridor (explanation vs immersion); unlocks wave parameter literacy
- Needs: VR sliders [has], mesh display [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
