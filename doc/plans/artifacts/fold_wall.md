# Artifact: Fold Wall / Curtain

> a triangulated curtain lies pooled on the ground and rises into a hanging drape

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** fold_system, wall, curtain, architecture, cascade, drape | **Themes:** fold_system

Triangulated curtain pooled flat on the ground rises into a hanging drape. Columns cascade left to right with sine billow for fabric feel. Same vertices, same faces.

*a curtain is a floor that remembers it can stand. fold_amount reminds it.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** fabric that cascades upward in a wave — each column lifts slightly after the last
- **Critical Parameter:** fold_amount — 0 is pooled flat, 1 is hanging vertical curtain
- **What Emerges:** the curtain billows as it rises — sine wave adds organic drape to the rigid grid
- **Triggers:** columns cascade left to right with a wave ripple through the fabric

## Architecture

| | |
|---|---|
| **File** | `commons/fold_system/fold_wall.gd` (156 lines) |
| **Scene** | `res://commons/fold_system/fold_wall.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `FoldWall` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `columns` | int | 6 |
| `rows` | int | 4 |
| `panel_width` | float | 0.06 |
| `panel_height` | float | 0.05 |
| `cascade_spread` | float | 0.4 |
| `drape_amount` | float | 0.015 |
| `drape_waves` | float | 2.0 |

### Dependencies

- `res://commons/primitives/shared/primitive_mesh_builder.gd`

### Key Methods

- `_ready()`
- `_generate_geometry()`
- `_build_stages()`
- `apply_grid_config(_config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Folding: Same Faces, Only Positions Change** (fold_system) -- map: Folding_Zoo

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Folding_Zoo | [14,2] | `:0:0.5` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
