# Artifact: Particle Emitter

> emit(pattern, t) = spawn(particle, lifetime, velocity) -- nozzle cycling cone/fountain/spiral

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** particles, emission, lifecycle, pool, patterns | **Themes:** gameplay, hazards

Nozzle firing lifecycle-colored spheres with manual particle pool. Cycles between cone, fountain, and spiral emission patterns. Birth-white to active-yellow to dying-red. The particle system as weapon.

*the particle emitter cycles cone, fountain, spiral -- birth-white to dying-red, lifecycle as ammunition.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** a particle emitter cycling emission patterns, birth-white to dying-red, lifecycle as ammunition
- **Critical Parameter:** _current_pattern (EmissionPattern) -- cone, fountain, spiral produce different threats
- **What Emerges:** particle lifecycle as weapon -- birth, travel, death mapped to color
- **Triggers:** emit_timer spawns particles; pattern_timer cycles modes; particles age from white to red

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/particle_emitter_hazard/particle_emitter_hazard.gd` (296 lines) |
| **Scene** | `res://commons/hazards/particle_emitter_hazard/particle_emitter_hazard.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `ParticleEmitterHazard` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `pool_size` | int | 30 |
| `particle_radius` | float | 0.04 |
| `particle_lifetime` | float | 3.0 |
| `emit_rate` | float | 8.0 |
| `pattern_duration` | float | 5.0 |
| `initial_speed` | float | 4.0 |
| `gravity` | Vector3 | Vector3(0.0, -9.8, 0.0) |
| `drag` | float | 0.3 |
| `contact_damage` | float | 8.0 |
| `contact_radius` | float | 0.15 |
| `damage_cooldown` | float | 0.3 |
| `nozzle_color` | Color | Color(0.4, 0.4, 0.5) |
| `birth_color` | Color | Color(1.0, 1.0, 1.0) |
| `active_color` | Color | Color(1.0, 0.9, 0.2) |
| `dying_color` | Color | Color(1.0, 0.2, 0.05) |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_build_nozzle()`
- `_build_particle_pool()`
- `_emit_particle()` -- Find a dead particle to reuse
- `_update_particles(delta: float)`
- `_find_player()`
- `take_damage(amount: float)` -- Spawner — indestructible
- `apply_damage(amount: float)`
- `damage(amount: float)`
- `configure(config: Dictionary)`
- `apply_grid_config(config: Dictionary)`

### Signals

- `enemy_destroyed`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_7

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_7 | [3,5] | `-` |

### Relationships

- embodies particles sequence; manual particles for educational visibility
- Needs: manual particle management [has]; 3 emission patterns [has]; color lifecycle [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
