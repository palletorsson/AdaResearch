# Artifact: grid_3d_4x4x4

> for x,y,z in range(4)³: cube.position = Vector3(x,y,z) with label "[x,y,z]" — 64 pickup cubes forming a solid 4×4×4 volume where every position is labeled with its three-dimensional index

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** grid | **Themes:** arrays, data_structures

Populates a 4x4x4 lattice of pickup cubes for volumetric layouts.

*a three-dimensional array is the minimum structure needed to represent volumetric space — every 3D game world, CT scan, and weather simulation is an indexed cube of values*

## Design

- **Scale:** [4, 4, 4] (room_scale)
- **Interaction:** to climb inside a three-dimensional array — to reach up and grab a cube at [2,3,1] and feel that three numbers describe a unique place in space that only you can occupy at that moment
- **Critical Parameter:** spacing — at 1.0m the cubes form a compact volume you can reach into; increasing spacing makes the array a sparse structure you must move through rather than reach across
- **What Emerges:** students discover that volumetric indexing requires physical intuition that 2D indexing does not — you must think in three directions simultaneously, and your body knows how to do this even when your mind struggles
- **Triggers:** picking up cubes physically empties cells from the volume — the 3D array becomes a sculpture with negative space carved by interaction

## Architecture

| | |
|---|---|
| **File** | `algorithms/arrays/grid_3d_4x4x4.gd` (93 lines) |
| **Scene** | `res://algorithms/arrays/grid_3d_4x4x4.tscn` |
| **Registry** | `arrays.json` |
| **Pattern** | procedural |

### Dependencies

- `res://commons/scenes/mapobjects/pick_up_cube.tscn`

### Key Methods

- `_ready()`
- `create_grid_3d()` -- Load the pickup cube scene
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Accepts: `label_color`, `label_size`, `size_x`, `size_y`, `size_z`, `spacing`

## Curriculum Position

### Sequences

- **Array Tutorial Sequence** (array_tutorial) -- map: Tutorial_3D

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Tutorial_3D | [2,1] | `-` |

### Relationships

- extends grid_2d_4x4 to 3 dimensions; final step in the 1D→2D→3D spatial array progression; connects to voxelnoise (3D grid with noise values) and grid_3d in the grid_editor sequence
- Needs: pickup cubes [has]; Label3D [x,y,z] labels [has]; no-depth-test labels ensure always-readable index [has]; apply_grid_config supports size_x/y/z/spacing overrides [has]; no live data visualization companion [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
