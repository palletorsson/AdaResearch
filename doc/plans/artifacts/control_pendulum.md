# Artifact: Control Pendulum

> alpha = -(g/L)*sin(theta), omega += alpha*dt, theta += omega*dt, damped by 0.995

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** pendulum, control, damping, chaos, nonlinear_dynamics | **Themes:** commons, mixed, waves

A pendulum with configurable control parameters — adjust damping, driving force, and frequency to explore the transition from periodic motion through quasi-periodicity to chaos. The canonical example of nonlinear dynamics in a physical system.

*A pendulum is gravity's metronome — length alone determines the tempo.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** Grab a pendulum bob in VR and feel gravity convert your release into oscillation
- **Critical Parameter:** pendulum_length — determines the natural frequency via sqrt(g/L)
- **What Emerges:** harmonic motion from the simplest possible setup — mass, string, gravity
- **Triggers:** grab() freezes physics; release() converts hand position to initial angle and starts oscillation

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/control_pendulum/control_pendulum.gd` (238 lines) |
| **Scene** | `res://commons/artifacts/control_pendulum/control_pendulum.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `ControlPendulum` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `pendulum_length` | float | 0.6 |
| `bob_radius` | float | 0.06 |
| `gravity` | float | 9.8 |
| `damping` | float | 0.995 |

### Dependencies

- `res://commons/primitives/point/grab_sphere_point.tscn`

### Key Methods

- `_ready()`
- `_create_pivot()`
- `_create_rod()`
- `_create_grabbable_bob()` -- Instance the grabbable sphere
- `_create_label()`
- `_update_bob_position()` -- Calculate bob position from angle
- `_physics_process(delta)` -- When grabbed, track the bob position and calculate angle
- `_update_label(text: String)`
- `_on_bob_picked_up(_pickable)`
- `_on_bob_dropped(_pickable)`
- `grab()` -- Manual grab (for non-VR)
- `release()` -- Manual release (for non-VR)
- `push(impulse: float)` -- Add angular velocity (for VR swing gesture)
- `set_angle_from_position(world_pos: Vector3)`
- `apply_grid_config(config_data: Dictionary)`

### Signals

- `oscillation_updated`
- `grabbed`
- `released`

### Grid Config


## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Intro

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Intro | [11,2] | `:0:1.5:1` |

### Relationships

- depends on gravity simulation; drives oscillation_controlled_cube via signal; contrasts with spring_demo (gravity vs spring restoring force)
- Needs: VR grab on bob [has], oscillation_updated signal output [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
