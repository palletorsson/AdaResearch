# Artifact: psychoacoustics

> perception = transform(signal, cochlea, brain) -- the gap between pressure wave and heard sound

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** algorithm | **Themes:** algorithms, misc

psychoacoustics

*the instrument is the ear -- every synthesis decision is judged by what the listener constructs.*

## Design

- **Visual:** material: standard, animation: shader_time
- **Scale:** [17.0, 24.0, 21.0] (world_scale)
- **Interaction:** masking, beating, binaural offsets -- auditory illusions revealing how hearing constructs reality
- **Critical Parameter:** masker_frequency / probe_offset -- where one tone renders another inaudible
- **What Emerges:** perception is not reception but generation -- the ear performs its own Fourier transform
- **Triggers:** theme profiles reshape masker frequency, probe timing, binaural offset, modulation depth

## Architecture

| | |
|---|---|
| **File** | `algorithms/proceduralaudio/psychoacoustics/psychoacoustics.gd` (593 lines) |
| **Scene** | `res://algorithms/proceduralaudio/psychoacoustics/psychoacoustics.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `update_masking_parameters()` -- Animate psychoacoustic parameters
- `visualize_frequency_masking()`
- `calculate_masking_threshold(probe_freq: float, masker_freq: float, masker_amp: float)` -- Simplified frequency masking calculation
- `frequency_to_bark(frequency: float)` -- Convert frequency to Bark scale
- `visualize_temporal_masking()`
- `show_critical_bands()`
- `demonstrate_loudness_perception()`
- `calculate_equal_loudness_spl(frequency: float, phon_level: float)` -- Simplified equal-loudness calculation based on ISO 226:2003
- `show_a_weighting_filter(container: Node3D)` -- Show A-weighting frequency response
- `create_contour_connection(from: Vector3, to: Vector3)`
- `setup_audio_synthesis()`
- `ensure_playback()`
- `reset_audio_state()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Procedural Audio** (proceduralaudio) -- map: ProceduralAudio_Psychoacoustics

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ProceduralAudio_Psychoacoustics | [6,5] | `-` |

### Relationships

- capstone of proceduralaudio -- all synthesis lands between waveform and listener
- Needs: AudioStreamGenerator [has]; masker/probe envelope system [has]; binaural offset [has]; VR controls [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
