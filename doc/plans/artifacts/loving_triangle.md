# Artifact: Loving Triangle

> triangle.respond(catalyst_mode) -> fold, tile, dance -- atom of 3D worlds responding to its tool

## Context

**Category:** companions | **Complexity:** advanced
**Tags:** companion, catalyst, becoming, qfep, vr, evolution, triangle, folding, tiling | **Themes:** gameplay, hazards

A triangle-shaped companion that dances with the Becoming Catalyst. Triangles are the atom of all 3D worlds — every mesh reduces to triangles. This entity makes that visible. Not an enemy — a relational partner. It responds to each catalyst mode with its own transformation: folding, tiling, compressing, expanding. Evolves through tiers as lab sequences complete.

*three vertices, three edges, infinite possibility when given a relational partner.*

## Design

- **Visual:** material: standard, animation: physics
- **Interaction:** a triangle that folds and tiles in response to the Becoming Catalyst, dancing with its partner
- **Critical Parameter:** catalyst mode / response type -- each curriculum stage evokes different triangle behavior
- **What Emerges:** the simplest polygon becomes expressive through relational context -- same triangle, different dance
- **Triggers:** proximity to becoming_catalyst; mode-dependent responses; folding, tiling, dance behaviors

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/loving_triangle/loving_triangle.gd` (628 lines) |
| **Scene** | `res://commons/hazards/loving_triangle/loving_triangle.tscn` |
| **Registry** | `hazards.json` |
| **Pattern** | procedural |

### Dependencies

- `res://commons/hazards/loving_triangle/triangle_responses.gd`
- `res://commons/hazards/loving_triangle/triangle_visual.gd`

### Key Methods

- `_ready()`
- `_physics_process(delta: float)`
- `_process_dormant(delta: float)` -- Gentle rotation
- `_process_aware(delta: float)`
- `_process_curious(delta: float)`
- `_process_dancing(delta: float)`
- `_process_resonating(_delta: float)` -- Slow spin, elevated
- `_process_harmonized(_delta: float)` -- Slow multi-axis rotation, fully elevated
- `_process_resting(delta: float)` -- Drift away gently
- `_apply_hover(_delta: float)`
- `_on_projectile_entered(body: Node3D)` -- Check if it's a catalyst projectile
- `_identify_mode(projectile: Node3D)`
- `_record_hit(mode_id: String)`
- `_check_resonance_or_harmony()` -- Resonance: same mode N times
- `_prune_old_hits()`

### Signals

- `state_changed`
- `mode_response`
- `tier_changed`
- `harmonized`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_4
- **Lab Evolution States** (lab) -- map: Lab

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_4 | [3,9] | `-` |
| Lab | [1,5] | `-` |

### Relationships

- companion to becoming_catalyst (atom + tool pair); triangle is the irreducible geometric unit
- Needs: CharacterBody3D [has]; catalyst response system [has]; fold/tile behaviors [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
