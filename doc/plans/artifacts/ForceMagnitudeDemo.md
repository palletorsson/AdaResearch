# Artifact: ForceMagnitudeDemo

> F = ma. Drag the force vector longer → ball accelerates harder. Magnitude is felt, not read.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** force, magnitude, acceleration, newton, f_equals_ma, interactive, vectors, physics | **Themes:** linear_algebra, math, vectors

Drag the force arrow longer and the ball accelerates harder. F = ma made visceral — magnitude is felt, not read. The ball's trajectory traces the force's history. Reset with R, freeze with Space. Contrasts with WorkEnergyDemo (same force, different measure); feeds into CombinedForcesDemo (multiple forces).

*You never feel position or velocity. You feel acceleration. Force is the only thing that reaches your body.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To make the learner drag a red arrow and feel the ball respond proportionally — bigger arrow, faster ball.
- **Critical Parameter:** The length of the applied_force_vector. It is the magnitude that drives everything.
- **What Emerges:** The intuition that force and acceleration are the same shape. The ball's trajectory traces the force's history.
- **Triggers:** Force handle drag → acceleration scales linearly, R key → reset ball to origin, Space → freeze motion

## Architecture

| | |
|---|---|
| **File** | `algorithms/vectors/01_vector_basics/ForceMagnitudeDemo.gd` (146 lines) |
| **Scene** | `res://algorithms/vectors/01_vector_basics/ForceMagnitudeDemo.tscn` |
| **Registry** | `vectors.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_setup_demo()` -- Create force vector (user can drag)
- `_physics_process(delta: float)` -- Update force vector positions to follow ball
- `_update_info(force: Vector3, accel: Vector3, velocity: Vector3)`
- `_input(event: InputEvent)`
- `_reset_demo()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: VectorFoundations

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| VectorAddition | [3,7] | `-` |
| VectorFoundations | [3,19] | `-` |

### Relationships

- Entry point for forces sequence. Feeds into combined_forces_demo (multiple forces). Contrasts with work_energy_demo (same force, different measure).
- Needs: VR draggable force arrow [has], info readout [has]. Missing: mass slider to show a = F/m with different m.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
