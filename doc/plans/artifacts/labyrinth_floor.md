# Artifact: Labyrinth Floor

> a floor that traces the oldest algorithmic path in architecture

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pompeii, labyrinth, classical | **Themes:** arrays, art, cultural_computation

Pompeii mosaic floor with classical 7-circuit labyrinth pattern. A single unbroken path spirals inward from the entrance to the center — not a maze but a meditation in stone. Based on Roman house labyrinths found across the Mediterranean.

*the classical labyrinth is a single algorithm that every ancient culture discovered independently*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and follow a single unbroken path to the center
- **Critical Parameter:** grid_cells — resolution of the labyrinth grid
- **What Emerges:** the realization that a labyrinth is not a maze — there are no choices, only the walk
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/labyrinth_floor.gd` (480 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/labyrinth_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `LabyrinthFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.2, 1.2) |
| `grid_cells` | int | 33 |
| `border_widths` | Array[int] | [2, 1, 1] |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.05 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`

### Grid Config

Accepts: `border_widths`, `floor_size`, `grid_cells`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- pompeii_mosaic_floor (parent pattern), swastika_meander_floor (cousin)
- Needs: [implemented] procedural mesh, border bands, classical labyrinth seed

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
