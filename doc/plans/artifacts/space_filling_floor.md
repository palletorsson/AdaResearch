# Artifact: Space-Filling Floor

> a floor that shows space-filling is an ancient idea with modern math

## Context

**Category:** primitives | **Complexity:** advanced
**Tags:** 2d, pattern, mosaic, floor, space_filling, meander, hilbert, peano, penrose, algorithms | **Themes:** arrays, art, cultural_computation, algorithms

Procedural mosaic floor demonstrating the connection between ancient meanders and modern space-filling curves. Four modes — Greek key, Hilbert, Peano, Penrose — each answer: how do you fill a bounded area with a path?

*the Greek key is the first space-filling curve*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players see the same question answered four ways across millennia
- **Critical Parameter:** mode — switches between meander, Hilbert, Peano, Penrose
- **What Emerges:** the insight that filling a border with a path is the origin of algorithms
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/space_filling_floor.gd` (660 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/space_filling_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `SpaceFillingFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `mode` | int | 0 |
| `floor_size` | Vector2 | Vector2(1.2, 0.9) |
| `grid_cells` | int | 120 |
| `border_depth` | int | 22 |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.06 |
| `wear_level` | float | 0.3 |
| `dot_spacing` | int | 4 |
| `dot_size_fraction` | float | 0.15 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`

### Grid Config

Accepts: `border_depth`, `dot_size_fraction`, `dot_spacing`, `floor_size`, `grid_cells`, `mode`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- turtle_meander_floor (sibling — turtle approach), labyrinth_floor (cousin)
- Needs: [implemented] bitmap meander, turtle curves, Penrose quasicrystal

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
