# Artifact: Shannon Entropy Meter

> Wall-mounted Shannon entropy gauge — generates a random sequence, computes symbol frequencies, and displays H = -Σ p(x) log₂ p(x). Bar gauge fills from 0 to max entropy (~3.32 bits for 10 symbols). Frequency histogram shows symbol distribution.

## Context

**Category:** randomness | **Complexity:** beginner
**Tags:** entropy, shannon, information_theory, measurement, probability, wall_display | **Themes:** probability, randomness

Wall-mounted Shannon entropy gauge — generates a random sequence, computes symbol frequencies, and displays H = -Σ p(x) log₂ p(x). Bar gauge fills from 0 to max entropy (~3.32 bits for 10 symbols). Frequency histogram shows symbol distribution.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/shannon_entropy_meter/shannon_entropy_meter.gd` (307 lines) |
| **Scene** | `res://commons/artifacts/shannon_entropy_meter/shannon_entropy_meter.tscn` |
| **Registry** | `randomness.json` |
| **Class** | `ShannonEntropyMeter` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `panel_size` | Vector2 | Vector2(0.7, 0.5) |
| `num_symbols` | int | 10 |
| `sequence_length` | int | 200 |
| `bar_color_low` | Color | Color(0.2, 0.3, 0.9) |
| `bar_color_high` | Color | Color(0.9, 0.2, 0.3) |

### Key Methods

- `_ready()`
- `_build_panel()`
- `_add_frame_edge(pos: Vector3, size: Vector3, color: Color)`
- `_build_gauge()` -- Background bar track
- `_build_frequency_bars()`
- `_build_labels()` -- Title
- `_run_measurement()` -- Generate random sequence
- `_symbol_color(index: int)`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `num_symbols`, `seed`, `sequence_length`

## Curriculum Position

### Sequences

- randomness

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
