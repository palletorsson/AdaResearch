# Artifact: Constraint Web

> CSP(variables, domains, constraints) -> solution | conflict -- graph coloring as minefield

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** constraint, csp, superposition, graph-coloring | **Themes:** gameplay, hazards

5x5 grid of superposition tiles representing a constraint satisfaction problem. Tiles collapse on step; contradictions explode. Graph coloring as minefield — no two adjacent tiles may share a color.

*a constraint web in superposition is possibility -- collapse is commitment, wrong commitment is damage.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** a 5x5 grid of tiles flickering in superposition until stepped on, collapsing to constraint colors
- **Critical Parameter:** _tile_domains -- which colors remain valid; stepping collapses domain to single value
- **What Emerges:** constraint satisfaction as spatial experience -- the web teaches CSP by making violations lethal
- **Triggers:** player steps on tile -> domain collapses; constraints propagate; violation triggers damage

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/constraint_web/constraint_web.gd` (407 lines) |
| **Scene** | `res://commons/hazards/constraint_web/constraint_web.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `ConstraintWeb` extends `Area3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_cols` | int | 5 |
| `grid_rows` | int | 5 |
| `tile_size` | float | 0.45 |
| `tile_gap` | float | 0.05 |
| `tile_height` | float | 0.08 |
| `explosion_damage` | float | 20.0 |
| `explosion_radius` | float | 1.5 |
| `flicker_speed` | float | 6.0 |
| `propagation_delay` | float | 0.3 |

### Key Methods

- `_ready()` -- Collision — covers the full grid
- `_process(delta: float)`
- `_build_base_plane()`
- `_build_grid()`
- `_build_label()`
- `_get_tile_under_player()`
- `_collapse_tile(idx: int)`
- `_propagate_constraints(idx: int)`
- `_explode_tile(idx: int)`
- `_get_neighbors(idx: int)`
- `_count_constraints()`
- `_on_body_entered(body: Node3D)`
- `_on_body_exited(body: Node3D)`
- `_find_player()`
- `take_damage(_amount: float)` -- Zone hazard — indestructible

### Signals

- `enemy_destroyed`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_6

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_6 | [7,9] | `-` |

### Relationships

- embodies constraint_solvers sequence; pairs with maze_spinner (generation vs satisfaction)
- Needs: Area3D [has]; tile domain tracking [has]; constraint propagation [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
