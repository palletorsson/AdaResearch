# Artifact: Perceptron Playground

> Single neuron with adjustable weights — the simplest possible neural network. Drag sliders to change w1, w2, and bias, watch the decision boundary shift on a 2D scatter plot. Misclassified points get white corners so you can see the perceptron learning.

## Context

**Category:** machinelearning | **Complexity:** beginner
**Tags:** perceptron, neuron, weights, bias, decision_boundary, classification, neural_network | **Themes:** ai, education, machine_learning

Single neuron with adjustable weights — the simplest possible neural network. Drag sliders to change w1, w2, and bias, watch the decision boundary shift on a 2D scatter plot. Misclassified points get white corners so you can see the perceptron learning.

## Design

- **Visual:** material: generated_texture, animation: static
- **Scale:** [2.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/perceptron_playground/perceptron_playground.gd` (487 lines) |
| **Scene** | `res://commons/artifacts/perceptron_playground/perceptron_playground.tscn` |
| **Registry** | `machinelearning.json` |
| **Class** | `PerceptronPlayground` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `w1_slider_path` | NodePath | "ControlPanel/W1Slider" |
| `w2_slider_path` | NodePath | "ControlPanel/W2Slider" |
| `bias_slider_path` | NodePath | "ControlPanel/BiasSlider" |
| `plot_size` | float | 0.5 |
| `plot_resolution` | int | 128 |
| `num_points` | int | 60 |

### Key Methods

- `_ready()`
- `_generate_data_points()`
- `_build_scatter_plot()`
- `_update_plot()`
- `_build_boundary()`
- `_update_boundary()`
- `_build_neuron_diagram()`
- `_update_diagram()`
- `_weight_color(w: float)` -- Positive = blue, negative = red, zero = gray
- `_build_labels()` -- Title
- `_update_labels()`
- `_setup_sliders()`
- `_on_w1_changed(_value)`
- `_on_w2_changed(_value)`
- `_on_bias_changed(_value)`

### Grid Config

Accepts: `bias`, `num_points`, `w1`, `w2`

## Curriculum Position

### Sequences

- machinelearning

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
