# Artifact: Random Cubes

> for i in N: cube(random_pos, random_rot, random_size, random_color) + drift(random_velocity)

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** compression, probability, procedural, qfep | **Themes:** philosophy, qfep, theory

Chaotically arranged cubes representing high entropy. Random positions, rotations, scales.

*randomness is not the absence of information but the presence of all possible configurations simultaneously*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** be surrounded by cubes that have forgotten how to form a grid — pure possibility space
- **Critical Parameter:** drift_speed — at zero, chaos is frozen; above zero, entropy is visibly restless
- **What Emerges:** temporary alignments and clusters that the eye reads as structure — meaning projected onto noise
- **Triggers:** regenerate() re-rolls every position, velocity, and color; boundary bouncing prevents escape

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/random_cubes.gd` (131 lines) |
| **Scene** | `res://commons/interfaces/qfep/random_cubes.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `QFEPRandomCubes` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cube_count` | int | 20 |
| `spawn_radius` | float | 1.0 |
| `size_min` | float | 0.05 |
| `size_max` | float | 0.15 |
| `drift_speed` | float | 0.1 |
| `base_color` | Color | Color(0.9, 0.3, 0.3, 0.8) |

### Key Methods

- `_ready()`
- `_spawn_cubes()`
- `_process(delta: float)`
- `regenerate()`

## Curriculum Position

### Sequences

- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_E_Term

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| QFEP_E_Term | [3,4] | `:0:0.5` |
| QFEP_E_Term | [3,7] | `:0:0.5` |
| QFEP_E_Term | [7,4] | `:0:0.5` |
| QFEP_E_Term | [7,7] | `:0:0.5` |

### Relationships

- represents E(S) entropy term; contrasts ordered_grid (F term); paired with particle_chaos; feeds lambda spectrum
- Needs: VR regenerate button [missing], drift speed control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
