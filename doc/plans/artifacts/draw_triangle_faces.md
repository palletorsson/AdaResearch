# Artifact: draw_triangle_faces

> fan_triangulate(loop) → colored mesh face — closed point loops become surfaces

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

draw_triangle_faces.

*a surface is not a thing — it is an agreement among boundary points*

## Design

- **Visual:** material: unlit, animation: physics
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** learner discovers that faces are just organized points — drop points, close the loop, get geometry
- **Critical Parameter:** the loop-closing gesture — dropping the sphere near the first point closes and fills
- **What Emerges:** the mesh as a collection of closed loops; surfaces as a social contract between points
- **Triggers:** dropping the sphere — each drop places a point; proximity to first point triggers triangulation

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/point/draw_triangle_faces.gd` (681 lines) |
| **Scene** | `res://commons/primitives/point/draw_triangle_faces.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grab_point_path` | NodePath | NodePath("GrabPoint") |
| `draw_sphere_path` | NodePath | NodePath("GrabPoint/DrawSphere") |
| `continuous_drawing` | bool | true |
| `snap_to_grid` | bool | true |
| `grid_size` | float | 0.1 |
| `point_snap_distance` | float | 0.15 |
| `haptic_snap_intensity` | float | 0.5 |
| `haptic_snap_duration` | float | 0.1 |
| `haptic_triangle_intensity` | float | 0.8 |
| `haptic_triangle_duration` | float | 0.2 |
| `point_indicator_size` | float | 0.025 |
| `line_color` | Color | Color(0.2, 1.0, 0.6, 1.0) |
| `active_line_color` | Color | Color(1.0, 0.8, 0.2, 1.0) |
| `point_color` | Color | Color(0.2, 0.8, 0.3, 0.7) |
| `snap_indicator_color` | Color | Color(1.0, 0.3, 0.3, 1.0) |
| `editable_points` | bool | true |
| `triangle_colors` | Array[Color] | [ |
| `wireframe_color` | Color | Color(0.9, 0.9, 0.9, 0.8) |

### Dependencies

- `res://commons/primitives/point/grab_sphere_point.tscn`

### Key Methods

- `_ready()`
- `_setup_line_visualization()` -- Completed lines
- `_setup_snap_indicator()`
- `_process(delta: float)` -- Pulse the ring when grabbed
- `_update_active_line_preview(current_pos: Vector3)`
- `_on_grab_point_picked_up(_pickable)`
- `_on_grab_point_dropped(_pickable)`
- `_handle_snap_to_point(point_index: int)`
- `_create_new_point(position: Vector3)`
- `_create_editable_point(point_index: int, position: Vector3)` -- Create grab sphere point (same as animatedcubebuilder)
- `_create_static_point(point_index: int, position: Vector3)` -- Create non-editable visual indicator
- `_on_edit_point_picked_up(_pickable, point_index: int)`
- `_on_edit_point_dropped(_pickable, point_index: int)` -- Update the point position in our array
- `_find_nearby_point(position: Vector3)`
- `_get_last_point_in_path()`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Point_Triangle_Context

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_Triangle_Context | [3,3] | `:180:1` |

### Relationships

- extends draw_dot into 2D; prerequisite understanding for triangle, quad, and all mesh faces
- Needs: [has Label3D [has], grabbable sphere [has], missing undo control]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
