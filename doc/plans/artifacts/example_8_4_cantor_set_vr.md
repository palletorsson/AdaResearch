# Artifact: example_8_4_cantor_set_vr

> cantor(start, width, d) = line(start, width) + cantor(left_third, d+1) + cantor(right_third, d+1)

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

Cantor Set - infinite subdivision creating zero measure infinite set

*The Cantor set teaches subtraction as a creative act — what you remove defines what remains.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 3.0, 3.0] (room_scale)
- **Interaction:** To be read like a poem — each row shorter, the gaps widening, pink lines fading downward into infinity
- **Critical Parameter:** vertical_spacing — the gap between rows makes the fractal readable; too close and it blurs, too far and the pattern disconnects
- **What Emerges:** The visual rhythm of removal — the eye learns to see what is missing, not what remains
- **Triggers:** Recursion descends row by row; emission intensity fades with depth; width shrinks by 1/3 per level

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/example_8_4_cantor_set_vr.gd` (94 lines) |
| **Scene** | `res://algorithms/fractals/example_8_4_cantor_set_vr.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_depth` | int | 6 |
| `initial_width` | float | 0.8 |
| `vertical_spacing` | float | 0.08 |

### Dependencies

- `res://commons/resourses/materials/noc_vr/noc_vr_pink_primary.tres`

### Key Methods

- `_ready()`
- `_setup_environment()`
- `_draw_cantor(start: Vector3, width: float, depth: int)`
- `_create_line(start: Vector3, width: float, depth: int)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_CantorSet

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_CantorSet | [4,6] | `:0:2` |

### Relationships

- Classic NOC translation; the 2D visualization that precedes the physical cantor_set (RigidBody) and cantor_pagoda (walkable)
- Needs: VR depth control [missing], animation mode [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
