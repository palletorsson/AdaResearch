# Artifact: Disease Spread CA

> S → I with P(infection) * infected_neighbors; I → R with P(recovery) — the SIR model on a 3D grid

## Context

**Category:** emergence | **Complexity:** advanced
**Tags:** emergence, simulation | **Themes:** cellular_automata, computation, emergence

SIR model simulation of disease spread.

*Two probabilities and a grid of neighbors — this is how pandemics actually work.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** To spread — watch infection ripple outward from patient zero, then the green wave of recovery follow
- **Critical Parameter:** INFECTION_RATE vs RECOVERY_RATE — their ratio determines whether the epidemic burns out or persists
- **What Emerges:** Epidemic wavefronts, herd immunity boundaries, and spatial clustering from two probabilities
- **Triggers:** High infection rate → explosive spread; high recovery → quick burnout; few initial infected → slow simmer

## Architecture

| | |
|---|---|
| **File** | `algorithms/cellularautomata/ca_showcase/disease_spread_ca.gd` (136 lines) |
| **Scene** | `res://algorithms/cellularautomata/ca_showcase/disease_spread_ca.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | procedural |

### Key Methods

- `initialize_grid()` -- Configure PulsingCA settings
- `update_simulation(_delta)`
- `spread_disease()`
- `count_infected_neighbors(pos: Vector3i)`
- `update_visualization()`
- `get_disease_counts()`
- `reset_simulation()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Cellular Automata: Local Rules, Global Patterns** (cellularautomata) -- map: CA_EdgeOfChaos

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CA_EdgeOfChaos | [2,6] | `-` |

### Relationships

- Feeds into CA_EdgeOfChaos. Extends PulsingCA (pulsing spheres for visual drama).
- Needs: VR rate sliders [missing], reset button [missing], SIR count display [has via get_disease_counts]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
