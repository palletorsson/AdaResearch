# Artifact: Double Snake Meander

> two mirrored self-avoiding walks whose territories interlock into the Greek key

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pompeii, meander, greek_key, self_avoiding_walk | **Themes:** arrays, art, cultural_computation

Greek key meander generated by TWO mirrored self-avoiding walks that dance around each other. Snake A hugs the outer edge, Snake B hugs the inner edge. Their interlocking territories produce the classic meander pattern. Dark field interior with polka dots.

*the meander is a conversation between two walks*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players see that the meander is not one line but the RELATIONSHIP between two
- **Critical Parameter:** band_width (b) controls the line thickness; border_depth = 4*b
- **What Emerges:** understanding that symmetry and opposition create pattern
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/double_snake_meander.gd` (409 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/double_snake_meander.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `DoubleSnakeMeander` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.2, 0.9) |
| `band_width` | int | 4 |
| `grid_cells` | int | 160 |
| `dot_spacing` | int | 4 |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.02 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`
- `_build_key_tile(d: int, b: int)`
- `_tile_bottom(grid: Array, gw: int, gh: int, d: int, tile: Array)` -- Bottom border: rows gh-d..gh-1, cols d..gw-d-1
- `_tile_top(grid: Array, gw: int, gh: int, d: int, tile: Array)` -- Top border: rows 0..d-1, cols d..gw-d-1
- `_tile_left(grid: Array, gw: int, gh: int, d: int, tile: Array)` -- Left border: cols 0..d-1, rows d..gh-d-1
- `_tile_right(grid: Array, gw: int, gh: int, d: int, tile: Array)` -- Right border: cols gw-d..gw-1, rows d..gh-d-1
- `_add_rect(verts: PackedVector3Array, x: float, z: float, w: float, h: float)`
- `_hash_2d(x: int, y: int)`

### Grid Config

Accepts: `band_width`, `dot_spacing`, `floor_size`, `grid_cells`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- exact_meander_floor (single-line approach), meander_threshold_floor (tiled approach)
- Needs: [implemented] dual snake territory bitmap, tiling, ArrayMesh

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
