# Artifact: Mario Cube

> event -> destroy(DarkSphere) + reveal(Rainbow) — one-shot state transition from darkness to color

## Context

**Category:** primitives | **Complexity:** beginner
**Tags:** mario, cube, interactive, signal, game_design | **Themes:** commons, mixed

An interactive surprise cube inspired by classic platformer mechanics — hit it from below to trigger the NextCube signal, remove the DarkSphere, and reveal a rainbow effect. A playful intersection of game design and signal systems.

*joy is not the presence of color — it is the moment darkness becomes color*

## Design

- **Visual:** material: unlit, animation: static
- **Scale:** [1.0, 2.0, 1.0] (compact)
- **Interaction:** to punch a block and break the darkness open, rainbow erupting where void used to be
- **Critical Parameter:** _activated — boolean gate that ensures the transformation happens exactly once, making it feel like a revelation
- **What Emerges:** the elastic tween on the rainbow and the back-ease shrink on the sphere create a visual call-and-response that feels choreographed
- **Triggers:** NextCube.next_requested signal or player body collision — either path leads to the same irreversible moment

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/mario_cube/mario_cube.gd` (234 lines) |
| **Scene** | `res://commons/artifacts/mario_cube/mario_cube.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `MarioCube` extends `Node3D` |
| **Pattern** | procedural |

### Key Methods

- `_ready()` -- Wait a frame so NextCubes are ready
- `_connect_to_next_cubes()`
- `_find_next_cubes_recursive(node: Node)`
- `_on_next_requested(_from_position: Vector3)`
- `_on_body_entered(body: Node3D)`
- `_activate()`
- `_exit_tree()` -- Disconnect signals from external NextCube nodes
- `_cache_dark_sphere()`
- `_find_by_name(root: Node, target: String)`
- `_remove_dark_sphere()`
- `_create_rainbow()`
- `_show_rainbow()`
- `_arc_mesh(r_in: float, r_out: float, segs: int)`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Color: Perception, Not Physics** (color) -- map: Color_Rainbow

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Rainbow | [0,3] | `-` |

### Relationships

- depends on dark_sphere (destroys it); depends on NextCube (trigger); rainbow echoes the rainbow artifact's color bands
- Needs: NextCube signal [has]; DarkSphere reference [has]; player collision [has]; VR grab [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
