# Artifact: Algo-Gun

> Evolved gravity gun that captures and directs Grid Agents. Use GRIP button to capture agents, then aim and GRIP again to direct them to modify grid structures.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** 2d, interactive, physics | **Themes:** physics, simulation

Evolved gravity gun that captures and directs Grid Agents. Use GRIP button to capture agents, then aim and GRIP again to direct them to modify grid structures.

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/forces/algo_gun.gd` (290 lines) |
| **Scene** | `res://algorithms/forces/algo_gun.tscn` |
| **Registry** | `physics_simulation.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `can_capture_grid_agents` | bool | true |
| `agent_detection_radius` | float | 3.0 |
| `agent_orbit_radius` | float | 0.3 |
| `agent_orbit_speed` | float | 2.0 |
| `grid_agent_task_duration` | float | 30.0 |

### Key Methods

- `_ready()`
- `setup_agent_detection()`
- `_on_agent_entered_range(body: Node3D)`
- `_on_agent_exited_range(body: Node3D)`
- `_physics_process(delta: float)`
- `_check_agent_capture_input()`
- `_attempt_agent_capture()`
- `_capture_agent(agent: GridAgent)`
- `_update_captured_agents(delta: float)`
- `_direct_agent_to_target()`
- `_release_agent(agent: GridAgent, target: Vector3)`
- `release_all_agents()`
- `get_captured_agent_count()`
- `get_highest_captured_tier()`
- `_exit_tree()`

### Signals

- `agent_captured`
- `agent_directed`
- `agent_released`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Dev Examples** (devexamples) -- map: gridagent_puzzle01_copy
- **Dev Examples** (devexamples) -- map: gridagent_puzzle02_translate
- **Dev Examples** (devexamples) -- map: gridagent_puzzle03_rotate
- **Unused Artifacts Test** (testmaps) -- map: test_gridagent_algogun

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| gridagent_puzzle01_copy | [1,1] | `-` |
| gridagent_puzzle02_translate | [1,1] | `-` |
| gridagent_puzzle03_rotate | [1,1] | `-` |
| test_gridagent_algogun | [1,1] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
