# Artifact: Grammar Provenance

> Three L-system trees from three traditions — Lindenmayer 1968, Islamic geometric subdivision, Aboriginal kinship recursion — same engine, different grammars, different worlds

## Context

**Category:** procedural | **Complexity:** advanced
**Tags:** l_system, procedural, critical_theory, provenance, comparison | **Themes:** grammar, l_systems, procedural, philosophy

Side-by-side L-system trees from three cultural/historical sources — Lindenmayer 1968 botanical, Islamic geometric subdivision, Aboriginal kinship recursion. Same engine, different axioms, different worlds. Asks: whose grammar?

*the grammar is never just math — it is always also a claim about what counts as growth*

## Design

- **Visual:** material: vertex_color, animation: static
- **Scale:** [4.0, 2.0, 2.0] (room_scale)
- **Interaction:** to make visible that every grammar encodes a worldview, and the question "whose grammar?" is always a political question
- **Critical Parameter:** generation_depth — at depth 1 all three look alike; at depth 5 the cultural divergence becomes unmistakable
- **What Emerges:** the viewer notices that "natural" branching is itself a cultural assumption — the L-system engine is neutral, but the axioms never are
- **Triggers:** increasing depth reveals that Lindenmayer's tree grows UP (botanical hierarchy), the Islamic pattern grows OUT (tiling symmetry), the kinship pattern grows AROUND (reciprocal branching)

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/grammar_provenance/grammar_provenance.gd` (253 lines) |
| **Scene** | `res://commons/artifacts/grammar_provenance/grammar_provenance.tscn` |
| **Registry** | `lsystems.json` |
| **Class** | `GrammarProvenance` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `generation_depth` | int | 4: |
| `tree_spacing` | float | 1.2 |

### Key Methods

- `_ready()`
- `apply_grid_config(config_data: Dictionary)`
- `_build_trees()`
- `_build_controls()`
- `_build_header()`
- `_expand(axiom: String, rules: Dictionary, depth: int)`
- `_render_lsystem(im: ImmediateMesh, grammar: Dictionary, depth: int)`
- `_regenerate_all()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- lsystems

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| LSystems_Grammar_Lab | [4,1] | `-` |

### Relationships

- placed in LSystems_Grammar_Lab alongside lsystem_editor and lsystem_tree; responds to the critical.md question "whose grammar?"
- Needs: [has] depth slider via ArtifactControls; [has] Label3D provenance annotations; [missing] no audio; no axiom editor

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
