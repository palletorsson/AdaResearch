# Artifact: Ant Agent

> Individual ant agent for colony optimization simulations.

## Context

**Category:** swarmintelligence | **Complexity:** advanced
**Tags:** ant, agent, swarm | **Themes:** collective, emergence, swarm

Individual ant agent for colony optimization simulations.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/swarmintelligence/antcolonyoptimization/AntAgent.gd` (492 lines) |
| **Scene** | `res://algorithms/swarmintelligence/antcolonyoptimization/AntAgent.tscn` |
| **Registry** | `swarmintelligence.json` |
| **Class** | `AntAgent` extends `CharacterBody3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `speed` | float | 2.0 |
| `turning_speed` | float | 4.0 |
| `wander_strength` | float | 0.8 |
| `pheromone_strength` | float | 1.2 |
| `pheromone_drop_interval` | float | 0.2 |
| `pheromone_drop_amount` | float | 1.0 |
| `perception_radius` | int | 1 |
| `detection_radius` | float | 2.0 |
| `colony_color` | Color | Color(0, 0, 0) |
| `carrying_capacity` | float | 1.0 |

### Key Methods

- `_ready()` -- Set up visual representation
- `setup_visuals()` -- Create ant body
- `add_antennae()` -- Left antenna
- `create_antenna()`
- `setup_physics()` -- Create collision shape
- `initialize(colony_ref, pheromone_sys_ref, terrain_ref, food_refs = [])`
- `_physics_process(delta: float)` -- Handle ant behavior based on state
- `process_searching_food(delta)` -- Check if we found food
- `process_returning_home(delta)` -- Check if we're back at the colony
- `process_idle(delta)` -- Just wander around the colony
- `process_fighting(delta)` -- Simple fighting behavior - face the enemy and move toward it
- `process_building(delta)` -- Building behavior - go to build site and work
- `move_ant(delta)` -- Calculate velocity
- `drop_pheromone()`
- `update_visuals()` -- Update food indicator

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
