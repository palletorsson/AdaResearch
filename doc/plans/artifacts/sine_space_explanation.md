# Artifact: Sine Space Explanation

> z(x,y,t) = A * sin(freq * x + speed * t) * sin(freq * y + speed * t)

## Context

**Category:** waves | **Complexity:** beginner
**Tags:** 2d, 3d, visualization, wave | **Themes:** oscillation, physics, waves

1m display case showing how sine waves modulate 3D space. An 8×8 grid of points demonstrates y = A·sin(x)·sin(z)—the product of two sine functions creating a wave surface. Points rise and fall showing phase relationships across X and Z axes.

*Understanding a wave means knowing what each parameter does to the shape.*

## Design

- **Visual:** material: vertex_color, animation: vertex
- **Interaction:** Examine a miniature animated sine surface with labeled parameters you can adjust
- **Critical Parameter:** frequency — controls the wave density visible on the explanation display
- **What Emerges:** understanding through control — seeing how each parameter deforms the surface
- **Triggers:** VR sliders adjust amplitude, frequency, and wave speed in real-time

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/sine_space_explanation/sine_space_explanation.gd` (286 lines) |
| **Scene** | `res://commons/artifacts/sine_space_explanation/sine_space_explanation.tscn` |
| **Registry** | `wavefunctions.json` |
| **Class** | `SineSpaceExplanation` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `display_size` | float | 1.0 |
| `grid_resolution` | int | 24 |
| `amplitude` | float | 0.12 |
| `frequency` | float | 1.5 |
| `wave_speed` | float | 0.3 |
| `animate` | bool | false |
| `surface_color` | Color | Color(0.2, 0.5, 0.8) |
| `wireframe_color` | Color | Color(0.4, 0.8, 1.0) |

### Key Methods

- `_ready()`
- `_create_base_plate()`
- `_create_surface()`
- `_create_wireframe()`
- `_update_surface_mesh(phase: float)`
- `_update_wireframe_mesh(phase: float)`
- `_get_height(x: float, z: float, phase: float)` -- The sine space formula: y = A * sin(f*x + φ) * cos(f*z + φ)
- `_height_to_color(y: float)` -- 0 to 1
- `_create_labels()` -- Title
- `_create_axis_markers()` -- X-axis
- `_process(delta)`
- `set_amplitude(a: float)`
- `set_frequency(f: float)`
- `set_wave_speed(s: float)`
- `_refresh_static()`

### Grid Config


## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Sine_Space

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Sine_Space | [4,8] | `:90:-0.2` |

### Relationships

- depends on SurfaceTool mesh regeneration; contrasts with sine_space (explanation vs immersion); unlocks sine surface parameter literacy
- Needs: VR sliders [has], display surface [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
