# Artifact: Kosaraju's Algorithm

> two-pass DFS — first pass on original graph records finish_times; second pass on transpose_adjacency_list processes vertices in reverse finish order, each new DFS tree is an SCC

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** graph_theory, strongly_connected_components, transpose_graph, depth_first_search, directed_graph, connectivity, topology | **Themes:** algorithms, misc

Watch the same directed graph answer twice. The first DFS records finishing order, the transpose flips every edge, and the second DFS reveals the strongly connected components that survive reversal. Phase labels and colored passes make the graph's symmetry under transposition visible rather than abstract. It pairs with Tarjan by solving the same problem through a more overt two-pass narrative.

*Kosaraju's algorithm proves that strongly connected components are the same in a graph and its transpose — mutual reachability is a symmetric property of directed structure*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [5.0, 2.0, 5.0] (xlarge)
- **Interaction:** to see the graph flip — the transpose reversal switches edge directions, and the fact that SCCs survive this reversal is the visual proof that mutual reachability is symmetric
- **Critical Parameter:** edge_density — determines the graph's SCC structure; the transpose graph is computed at initialization and visualized with distinct orange edges in phase 2
- **What Emerges:** watching the same graph from two directions reveals that SCCs are the invariant structure under edge reversal — what you can reach forward, you can also be reached from backward
- **Triggers:** first_dfs_pass colors vertices as they finish (gray); second_dfs_pass processes the transpose in reverse finish order, coloring each SCC distinctly; phase_label tracks which pass is active

## Architecture

| | |
|---|---|
| **File** | `algorithms/graphtheory/kosaraju_algorithm/kosaraju_algorithm.gd` (492 lines) |
| **Scene** | `res://algorithms/graphtheory/kosaraju_algorithm/kosaraju_algorithm.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Class** | `KosarajuAlgorithm` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `graph_size` | int | 10 |
| `edge_density` | float | 0.4 |
| `auto_start` | bool | true |
| `step_by_step` | bool | true |
| `animation_delay` | float | 1.0 |
| `show_finish_times` | bool | true |
| `show_transpose_graph` | bool | true |
| `highlight_current_vertex` | bool | true |
| `show_scc_colors` | bool | true |
| `animate_dfs_traversal` | bool | true |
| `show_stack_order` | bool | true |
| `enable_graph_editing` | bool | true |
| `allow_edge_editing` | bool | true |
| `real_time_scc_update` | bool | true |
| `show_algorithm_state` | bool | true |
| `vertex_color` | Color | Color(0.4, 0.6, 0.9, 1.0) |
| `visited_color` | Color | Color(0.9, 0.6, 0.2, 1.0) |
| `current_color` | Color | Color(0.9, 0.9, 0.2, 1.0) |
| `scc_color_1` | Color | Color(0.9, 0.2, 0.2, 1.0) |
| `scc_color_2` | Color | Color(0.2, 0.9, 0.2, 1.0) |
| `scc_color_3` | Color | Color(0.2, 0.2, 0.9, 1.0) |
| `scc_color_4` | Color | Color(0.9, 0.2, 0.9, 1.0) |
| `scc_color_5` | Color | Color(0.2, 0.9, 0.9, 1.0) |
| `edge_color` | Color | Color(0.6, 0.6, 0.6, 0.8) |
| `transpose_edge_color` | Color | Color(0.9, 0.5, 0.2, 0.8) |
| `finished_color` | Color | Color(0.5, 0.5, 0.5, 1.0) |

### Key Methods

- `_ready()`
- `setup_environment()` -- Add lighting
- `initialize_graph()`
- `generate_random_graph()` -- Create vertices
- `has_edge(from: String, to: String)`
- `create_visual_elements()` -- Clear existing visuals
- `create_edge_visual(edge: Dictionary)`
- `create_arrow_mesh(from: Vector3, to: Vector3)`
- `start_algorithm()`
- `first_dfs_pass()`
- `dfs_first_pass(vertex: String)`
- `second_dfs_pass()`
- `dfs_second_pass(vertex: String)`
- `show_transpose_edges()` -- Create transpose edge visuals
- `get_scc_color(index: int)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Graph Theory: Connections Define Structure** (graphtheory) -- map: GT_Connectivity

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| GraphTheory_Kosaraju_s_Algorithm | [6,5] | `-` |
| GT_Connectivity | [4,5] | `:0:0:0.6` |

### Relationships

- paired with tarjan_algorithm in GT_Connectivity — same problem, different strategy; the two-pass approach makes the transpose relationship explicit where Tarjan hides it in low_link
- Needs: slider_horizontal [missing]; push_button [missing]; Label3D [has] (info_label, phase_label, vertex labels)

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
