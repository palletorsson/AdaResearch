# Artifact: Vector-Joint Playground

> Human-scale VR playground with 6 interactive stations. Each station pairs draggable vectors with a large physics joint mechanism: crane (magnitude), pistons (addition), door (dot product), seesaw (subtraction), waterwheel (cross product), spring tower (forces). Drag a vector, watch the machine respond.

## Context

**Category:** mathematics | **Complexity:** intermediate
**Tags:** vectors, joints, playground, magnitude, addition, dot_product, subtraction, cross_product, forces, interactive, vr, human_scale | **Themes:** vectors, joints, physics, interactive

Human-scale VR playground with 6 interactive stations. Each station pairs draggable vectors with a large physics joint mechanism: crane (magnitude), pistons (addition), door (dot product), seesaw (subtraction), waterwheel (cross product), spring tower (forces). Drag a vector, watch the machine respond.

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [18.0, 2.0, 18.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/vectors/playground/vector_joint_playground.gd` (740 lines) |
| **Scene** | `res://algorithms/vectors/playground/vector_joint_playground.tscn` |
| **Registry** | `vectors.json` |
| **Pattern** | procedural |

### Dependencies

- `res://commons/primitives/line/line.tscn`
- `res://algorithms/vectors/shared/gadgets/gadget_base.gd`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_build_arena()` -- Floor
- `_build_station_crane()` -- GadgetBase instance
- `_update_crane(do_text: bool)`
- `_build_station_pistons()`
- `_update_pistons(do_text: bool)`
- `_build_station_door()`
- `_update_door(do_text: bool)`
- `_build_station_seesaw()`
- `_update_seesaw(do_text: bool)`
- `_build_station_waterwheel()`
- `_update_waterwheel(do_text: bool)`
- `_build_station_spring()`
- `_update_spring(do_text: bool)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
