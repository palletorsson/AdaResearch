# Artifact: Ball Dropper

> Drops colorful physics balls one at a time. Pair with laser_turret for targeting demo. Balls cycle through colors and have configurable physics properties.

## Context

**Category:** mathematics | **Complexity:** beginner
**Tags:** balls, physics, spawner, dropper, colorful, standalone | **Themes:** vectors, physics, spawning

Drops colorful physics balls one at a time. Pair with laser_turret for targeting demo. Balls cycle through colors and have configurable physics properties.

## Design

- **Visual:** material: standard, animation: physics

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/turret_targeting/ball_dropper.gd` (338 lines) |
| **Scene** | `res://commons/artifacts/turret_targeting/ball_dropper.tscn` |
| **Registry** | `vectors.json` |
| **Class** | `BallDropper` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `drop_interval_min` | float | 0.8 |
| `drop_interval_max` | float | 1.5 |
| `auto_drop` | bool | true |
| `total_balls` | int | 30 |
| `ball_radius` | float | 0.1 |
| `ball_mass` | float | 0.5 |
| `ball_bounce` | float | 0.7 |
| `emission_strength` | float | 0.8 |
| `ball_health` | float | 80.0 |
| `drop_height` | float | 0.0 |
| `spawn_spread` | float | 1.5 |
| `initial_velocity` | Vector3 | Vector3(0, 0, 0) |

### Key Methods

- `_ready()`
- `_build_dropper_visual()` -- Dropper housing
- `_create_ball_pool()`
- `_create_ball(index: int)`
- `_hide_ball(ball: Node3D)`
- `_show_ball(ball: Node3D)`
- `_set_next_interval()`
- `_process(delta: float)` -- Auto drop logic
- `get_ball_count()`
- `drop_ball()`
- `_apply_velocity_deferred(ball: Node3D, vel: Vector3)`
- `_set_ball_color(ball: Node3D, color: Color)`
- `remove_ball(ball: Node3D)`
- `clear_all()`
- `set_auto_drop(enabled: bool)`

### Signals

- `ball_spawned`
- `ball_destroyed`

## Curriculum Position

### Sequences

- forces

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
