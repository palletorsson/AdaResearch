# Artifact: Magritte Pipe

> sign != signified -- a procedural pipe mesh with label saying this is not a pipe

## Context

**Category:** philosophy | **Complexity:** intermediate
**Tags:** magritte, representation, meta, art, interactive | **Themes:** philosophy

'Ceci n'est pas une pipe' - The gap between representation and reality

*representation is a gap pretending to be a bridge -- computation runs on signs detached from signified.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** a pipe you can see and inspect but that insists it is not what it appears to be
- **Critical Parameter:** layers array -- maps representation levels (paint, canvas, word, variable) as strata
- **What Emerges:** the gap between name and thing becomes tangible -- every data structure is a Magritte painting
- **Triggers:** static display; player proximity reveals layer explanations; contradiction is always present

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/foundations/magritte_pipe.gd` (247 lines) |
| **Scene** | `res://commons/interfaces/foundations/magritte_pipe.tscn` |
| **Registry** | `foundations.json` |
| **Class** | `MagrittePipe` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `pipe_color` | Color | Color(0.4, 0.25, 0.15) |
| `frame_color` | Color | Color(0.7, 0.6, 0.4) |
| `background_color` | Color | Color(0.9, 0.88, 0.82) |
| `show_text` | bool | true |
| `current_layer` | int | 0 |

### Key Methods

- `_ready()`
- `_create_frame()`
- `_create_canvas()`
- `_create_pipe()`
- `_create_text()`
- `_create_interactable()`
- `_on_input_event(_camera: Node, event: InputEvent, _position: Vector3, _normal: Vector3, _shape_idx: int)`
- `_process(delta: float)`
- `_update_display()`
- `advance_layer()`
- `reset()`

### Signals

- `representation_questioned`
- `layer_revealed`

## Curriculum Position

### Sequences

- **Art & Mathematics: Visual Philosophy** (artmathematics) -- map: Escher_Impossible

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Escher_Impossible | [10,5] | `:0:0.8` |

### Relationships

- foundational for artmathematics sequence; sign/signified gap extends to all formal systems
- Needs: procedural pipe mesh [has]; label system [has]; layer explanation text [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
