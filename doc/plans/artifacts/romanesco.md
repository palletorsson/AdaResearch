# Artifact: romanesco

> romanesco(cone, depth) = 13 * romanesco(cone * 0.382, depth+1) placed at 137.508 degree golden angle intervals

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

romanesco

*Romanesco broccoli is not shaped like a fractal — it is a fractal, grown by cells that only know the golden angle.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [8.0, 4.0, 8.0] (environment)
- **Interaction:** To be stared at — each bud is a smaller copy of the whole, spiraling at the golden angle, nature's most perfect fractal
- **Critical Parameter:** GOLDEN_ANGLE (137.508 degrees) — the irrational rotation that prevents any two buds from aligning, maximizing packing
- **What Emerges:** Visible Fibonacci spiral arms from golden-angle placement — 8 and 13 spirals appear in opposite directions, never planned
- **Triggers:** growth_interval tick → find all cones at current depth → add 13 spiral buds to each → tilt outward for dome shape

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/romanesco/romanesco.gd` (263 lines) |
| **Scene** | `res://algorithms/fractals/romanesco/romanesco.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `growth_interval` | float | 0.5 |
| `max_depth` | int | 4 |
| `auto_start` | bool | true |
| `fibonacci_spiral_count` | int | 13 |
| `base_cone_size` | float | 1.0 |
| `scale_factor` | float | 0.382 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `create_central_cone()`
- `perform_growth_iteration()`
- `find_cones_at_depth(depth: int)`
- `_find_cones_recursive(node: Node, target_depth: int, cones: Array)`
- `add_spiral_buds(parent_cone: Node3D, depth: int)`
- `create_cone(position: Vector3, size: float, depth: int)`
- `create_cone_mesh(size: float)`
- `start_growth()`
- `stop_growth()`
- `reset()` -- Clear all cones
- `step()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_GoldenSpiral
- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_Synthesis

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_GoldenSpiral | [9,11] | `-` |
| Fractal_Synthesis | [14,15] | `-` |

### Relationships

- The natural-world proof of fibonacci_sequences; contrasts with golden_rectangle (geometric) and fibonacci_pagoda (architectural)
- Needs: VR growth speed control [missing], cross-section view [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
