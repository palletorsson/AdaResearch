# Artifact: Plus

> cross = central box + 4 arm boxes rotated at 0°/90°/180°/270° — 4-fold rotational symmetry

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

Plus/cross shape (+) with 4 arms at 90 degree angles. Classic health pickup shape.

*symmetry is a property of the transformation group that leaves a shape invariant — the cross has C4*

## Design

- **Visual:** material: shader, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** learner recognises the plus symbol as a designed artifact with symmetry group C4
- **Critical Parameter:** arm_length — controls the reach of each arm; determines aspect ratio of the cross
- **What Emerges:** that a cross has the same symmetry as a square — 90° rotation leaves it invariant
- **Triggers:** nothing — static display; fan-triangulated for clean geometry without T-junction artifacts

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/plus/plus.gd` (212 lines) |
| **Scene** | `res://commons/primitives/plus/plus.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | shader_demo |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `create_plus()`
- `create_plus_vertices()`
- `create_plus_faces()`
- `create_collision()`
- `add_triangle_with_normal(st: SurfaceTool, vertices: Array, face: Array)`
- `apply_queer_material(mesh_instance: MeshInstance3D, color: Color)`
- `set_base_color(color: Color)`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Primitives_Ignorance

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Primitives_Ignorance | [25,1] | `:0:1` |
| Primitives_Ignorance | [25,7] | `:0:1` |

### Relationships

- sibling to lshape.gd; both are non-convex combined shapes; connects to plus_line_puzzle
- Needs: [missing VR controls — static display only]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
