# Artifact: TrigWalkingPath

> x_sin(z) = A*sin(omega*z), x_cos(z) = A*cos(omega*z) — dual walkable trig paths

## Context

**Category:** waves | **Complexity:** beginner
**Tags:** waves | **Themes:** oscillation, physics, waves

Walkable path generated by trig functions

*Sine and cosine are the same motion, separated by a quarter turn.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [6.0, 11.0, 26.0] (world_scale)
- **Interaction:** Walk along sine and cosine paths simultaneously, feeling their phase relationship with your feet
- **Critical Parameter:** frequency — controls how tightly the paths oscillate in space
- **What Emerges:** the 90-degree phase offset between sin and cos becomes a physical spatial relationship
- **Triggers:** player Z-position generates new steps ahead; escalator-like phase scroll

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/oscillation_driver/TrigWalkingPath.gd` (279 lines) |
| **Scene** | `res://algorithms/wavefunctions/oscillation_driver/TrigWalkingPath.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `step_scene` | PackedScene | - |
| `frequency` | float | 0.5 |
| `amplitude` | float | 2.0 |
| `step_distance` | float | 0.6 |
| `path_length` | int | 40 |
| `generation_speed` | float | 0.0 |
| `lane_offset` | float | 1.6 |
| `step_width` | float | 1.8 |
| `step_height` | float | 0.2 |
| `step_depth` | float | 0.6 |
| `start_height` | float | 0.6 |
| `end_height` | float | -0.6 |
| `escalator_mode` | bool | false |
| `escalator_speed` | float | 0.6 |
| `escalator_direction` | int | 1 |
| `add_end_platforms` | bool | true |
| `platform_size` | Vector3 | Vector3(6.0, 0.25, 2.0) |
| `platform_color` | Color | Color(0.2, 0.2, 0.25) |
| `platform_height_offset` | float | -0.2 |
| `light_interval` | int | 10 |
| `light_color_sine` | Color | Color(1.0, 0.6, 0.5) |
| `light_color_cosine` | Color | Color(0.5, 0.7, 1.0) |
| `light_energy` | float | 1.5 |
| `light_range` | float | 6.0 |
| `light_height_offset` | float | 2.0 |

### Key Methods

- `_ready()` -- If no step scene is provided, we'll create a simple mesh instance programmatically
- `_generate_initial_path()`
- `_add_step_pair(index: int)` -- Initial z position based on index
- `_create_step(color: Color)` -- Create a StaticBody with Mesh and Collision
- `_add_stair_lights(step_index: int)`
- `_physics_process(delta: float)`
- `_update_escalator(delta: float)` -- Advance the phase
- `_create_end_platforms()`
- `_compute_step_y(z_pos: float, wave_value: float)`
- `_platform_y_for_base(base_y: float)`
- `_process(_delta: float)` -- Implement endless runner style generation here if needed
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_TrigWalkingPath

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_TrigWalkingPath | [3,6] | `:0:-0.1` |

### Relationships

- depends on UnitCircleTrig (walking what it plots); contrasts with SimpleOscillatingBridge (dual path vs single bridge); unlocks trig embodiment
- Needs: VR locomotion [has], step collision [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
