# Artifact: Coordinate System Switcher

> One point, three descriptions: (x,y,z), (r,theta,z), (rho,theta,phi). The point does not change; only the language does.

## Context

**Category:** mathematics | **Complexity:** intermediate
**Tags:** vectors, coordinates, cartesian, cylindrical, spherical, polar, conversion, 3d, visualization | **Themes:** vectors, coordinate_systems, visualization

Shows the same 3D point in Cartesian (x,y,z), Cylindrical (r,θ,z), and Spherical (ρ,θ,φ) coordinates simultaneously. ImmediateMesh axes and arcs with projection lines showing how each system maps. Color-coded: Cartesian=RGB, Cylindrical=yellow, Spherical=magenta.

*A coordinate system is a way of speaking, not a way of being. The point exists before any frame names it.*

## Design

- **Visual:** material: vertex_color, animation: static
- **Interaction:** To show that coordinate systems are choices, not facts — the same white dot described three different ways simultaneously.
- **Critical Parameter:** point_cartesian — moving it rewrites all three readouts and redraws all projection lines and arcs.
- **What Emerges:** The yellow cylindrical radial line and the magenta spherical rho line meeting at the same point — different decompositions of the same position.
- **Triggers:** set_point() or config change → full rebuild of axes, projections, arcs, and labels

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/coordinate_system_switcher/coordinate_system_switcher.gd` (434 lines) |
| **Scene** | `res://commons/artifacts/coordinate_system_switcher/coordinate_system_switcher.tscn` |
| **Registry** | `vectors.json` |
| **Class** | `CoordinateSystemSwitcher` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `point_cartesian` | Vector3 | Vector3(0.25, 0.3, 0.2) |
| `axis_length` | float | 0.45 |
| `axis_thickness` | float | 0.003 |
| `arc_segments` | int | 32 |

### Key Methods

- `_ready()`
- `_build_axes()`
- `_draw_axis_line(mesh: ImmediateMesh, from: Vector3, to: Vector3, color: Color)`
- `_draw_arrowhead(mesh: ImmediateMesh, tip: Vector3, dir: Vector3, color: Color)`
- `_add_axis_label(text: String, pos: Vector3, color: Color)`
- `_build_point()`
- `_build_projections()`
- `_draw_dashed_line(mesh: ImmediateMesh, from: Vector3, to: Vector3, color: Color, segments: int)`
- `_build_arcs()`
- `_draw_arc_xz(mesh: ImmediateMesh, radius: float, start_angle: float, end_angle: float, color: Color)` -- Arc in the XZ plane at y=0
- `_draw_arc_polar(mesh: ImmediateMesh, radius: float, azimuth: float, polar_angle: float, color: Color)` -- Arc from +Y axis down toward the XZ plane, in the vertical plane defined by azimuth
- `_build_labels()`
- `set_point(new_point: Vector3)`
- `_rebuild()` -- Remove old children and rebuild
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `axis_length`, `point_x`, `point_y`, `point_z`

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: VectorFoundations

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| VectorFoundations | [13,15] | `-` |

### Relationships

- Extends basis_vectors_rig (both show frames). Prerequisite for VectorFieldFlow (fields use position-dependent coordinates).
- Needs: Static display [has], conversion formulas [has]. Missing: VR grabbable point handle, animated transitions between systems.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
