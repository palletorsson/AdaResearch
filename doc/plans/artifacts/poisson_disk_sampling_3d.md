# Artifact: poisson_disk_sampling_3d

> Bridson's algorithm in 3D — dart-throwing with minimum distance rejection producing blue noise point distributions

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

poisson_disk_sampling_3d

*Blue noise is what remains when every random proposal that crowds its neighbor gets rejected — beauty through exclusion.*

## Design

- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To show distribution between randomness and regularity: every point survives only by respecting its neighbors' boundaries
- **Critical Parameter:** min_distance — the exclusion radius; larger values produce sparser, more crystalline distributions
- **What Emerges:** Even, natural-looking distributions with no clustering and no grid artifacts — blue noise from pure rejection
- **Triggers:** Low min_distance produces dense cloud; high produces sparse constellation; radius spheres reveal the exclusion zones

## Architecture

| | |
|---|---|
| **File** | `algorithms/proceduralgeneration/spatial_partitioning/poisson_disk_sampling_3d/PoissonDiskSampling3D.gd` (433 lines) |
| **Scene** | `res://algorithms/proceduralgeneration/spatial_partitioning/poisson_disk_sampling_3d/PoissonDiskSampling3D.tscn` |
| **Registry** | `procgen_extra.json` |
| **Class** | `PoissonDiskSampling3D` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `sample_region` | Vector3 | Vector3(10, 10, 10) |
| `min_distance` | float | 1.0 |
| `max_attempts` | int | 30 |
| `seed_value` | int | 0 |
| `display_mode` | int | 1 |
| `point_size` | float | 0.2 |
| `point_color` | Color | Color(0.2, 0.8, 1.0) |
| `show_radius` | bool | true |
| `show_grid` | bool | false |
| `show_distance_lines` | bool | true |
| `show_label` | bool | true |
| `use_boundary` | bool | true |
| `distribution_type` | int | 0 |
| `boundary_falloff` | float | 0.5 |
| `regenerate` | bool | false: |
| `export_points` | bool | false: |

### Key Methods

- `_ready()`
- `generate_samples()`
- `get_initial_point()` -- Uniform
- `generate_point_around(center: Vector3)` -- Generate random point in spherical annulus between min_distance and 2*min_distance
- `is_valid_point(point: Vector3)` -- Check boundaries
- `world_to_grid(point: Vector3)`
- `grid_key(grid_pos: Vector3i)`
- `add_to_grid(point: Vector3)`
- `visualize_samples()`
- `create_sample_visual(point: Vector3)`
- `create_radius_visual()` -- Create wireframe spheres showing min_distance radius
- `create_wireframe_sphere(center: Vector3, radius: float)`
- `create_grid_visual()`
- `create_distance_lines_visual()` -- Draw lines between nearby points to show the minimum distance constraint
- `create_explanation_label()` -- Main explanation

## Curriculum Position

### Sequences

- **Procedural Generation (All Maps)** (proceduralgeneration_all) -- map: ProceduralGeneration_Poisson_Disk_Sampling
- **Spatial Partitioning: Dividing Space** (spatial_partitioning) -- map: ProceduralGeneration_Poisson_Disk_Sampling

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ProceduralGeneration_Poisson_Disk_Sampling | [6,5] | `-` |

### Relationships

- Synthesis artifact in spatial_partitioning. Contrasts with Voronoi (partition vs distribution). Used for object placement.
- Needs: @tool editor preview [has], multiple display modes [has], radius visualization [has], distance lines [has], VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
