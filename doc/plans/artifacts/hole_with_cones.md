# Artifact: Hole With Cones

> ground_plane - circular_hole + 3_cones_at_120° — absence as geometry: topology through subtraction

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

Ground plane with circular hole and 3 traffic cones around it. Construction site warning setup.

*a hole is not the absence of matter — it is a boundary condition imposed on surrounding matter*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [3.0, 1.0, 3.0] (compact)
- **Interaction:** learner understands that holes are defined by what surrounds them, not what fills them
- **Critical Parameter:** hole_segments — how many triangles fan-triangulate the circular boundary of the hole
- **What Emerges:** the concept of Boolean subtraction — the hole is real space defined by its edge, not its missing content
- **Triggers:** nothing — static environment piece; experienced by walking around and looking into the hole

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/combines/hole_with_cones.gd` (236 lines) |
| **Scene** | `res://commons/primitives/combines/hole_with_cones.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `ground_size` | float | 4.0 |
| `hole_radius` | float | 0.6 |
| `ground_color` | Color | Color(0.3, 0.3, 0.35) |
| `cone_distance` | float | 0.9 |

### Dependencies

- `res://commons/primitives/trafficcone/trafficcone.tscn`

### Key Methods

- `_ready()`
- `_build_scene()` -- Clean up existing
- `_build_ground_with_hole()`
- `_add_hole_edge(hole_vertices: Array[Vector3])` -- Create a dark ring around the hole edge
- `_add_traffic_cones()` -- Load traffic cone scene
- `_create_ground_collision()`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Primitives_Ignorance

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Primitives_Ignorance | [11,1] | `:30:-0.5` |

### Relationships

- demonstrates Boolean difference concept; precursor to boolean_tunnel in transformation sequence
- Needs: [missing VR controls — static environment object]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
