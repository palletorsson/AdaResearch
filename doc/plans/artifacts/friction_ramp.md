# Artifact: Friction Ramp

> Inclined plane with adjustable friction coefficient and slope angle. A block slides or sticks based on whether mg·sin(θ) exceeds μ·mg·cos(θ). Force arrows show gravity, normal force, and friction in real time. VR sliders control μ (0–1.5) and angle (5°–60°).

## Context

**Category:** physics | **Complexity:** beginner
**Tags:** friction, ramp, inclined_plane, normal_force, gravity, mu, coefficient, sliding, static, force_diagram, beginner | **Themes:** friction, inclined_plane, static_vs_kinetic, force_balance

Inclined plane with adjustable friction coefficient and slope angle. A block slides or sticks based on whether mg·sin(θ) exceeds μ·mg·cos(θ). Force arrows show gravity, normal force, and friction in real time. VR sliders control μ (0–1.5) and angle (5°–60°).

## Design

- **Visual:** material: vertex_color, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/friction_ramp/friction_ramp.gd` (413 lines) |
| **Scene** | `res://commons/artifacts/friction_ramp/friction_ramp.tscn` |
| **Registry** | `physics_simulation.json` |
| **Class** | `FrictionRamp` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `ramp_length` | float | 0.5 |
| `ramp_width` | float | 0.2 |
| `ramp_angle_deg` | float | 30.0 |
| `friction_mu` | float | 0.5: |
| `block_size` | Vector3 | Vector3(0.04, 0.03, 0.04) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_ramp()`
- `_update_ramp_transform()` -- Ramp center positioned so bottom edge sits near y=0
- `_create_block()`
- `_reset_block()` -- Start near top
- `_update_block_visual()` -- Block position along the ramp surface
- `_create_force_mesh()`
- `_update_force_arrows()`
- `_draw_arrow(im: ImmediateMesh, origin: Vector3, vec: Vector3, col: Color)`
- `_create_labels()`
- `_update_labels()`
- `_update_physics_state()` -- Condition: block slides if mg·sin(θ) > μ·mg·cos(θ)
- `_physics_process(delta: float)`
- `_create_vr_controls()`
- `_sync_slider(slider: Node, normalized: float)`

### Grid Config

Accepts: `angle`, `block_size`, `friction_mu`

## Curriculum Position

### Sequences

- forces

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ForcesFoundations | [1,15] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
