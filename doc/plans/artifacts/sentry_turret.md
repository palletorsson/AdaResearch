# Artifact: Sentry Turret

> Half-Life style sentry turret. Targets balls and/or player. Simple reliable targeting with bullet physics. Emits signals for hits.

## Context

**Category:** mathematics | **Complexity:** beginner
**Tags:** turret, sentry, targeting, bullets, halflife, player, balls | **Themes:** vectors, game_mechanics, targeting

Half-Life style sentry turret. Targets balls and/or player. Simple reliable targeting with bullet physics. Emits signals for hits.

## Design

- **Visual:** material: unlit, animation: physics

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/sentry_turret/sentry_turret.gd` (965 lines) |
| **Scene** | `res://commons/artifacts/sentry_turret/sentry_turret.tscn` |
| **Registry** | `vectors.json` |
| **Class** | `SentryTurret` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `rotation_speed` | float | 5.0 |
| `detection_range` | float | 15.0 |
| `fire_rate` | float | 2.0 |
| `bullet_speed` | float | 20.0 |
| `reload_time` | float | 10.0 |
| `burst_size` | int | 3 |
| `target_player` | bool | true |
| `target_balls` | bool | true |
| `use_laser_damage` | bool | false |
| `laser_damage_per_hit` | float | 20.0 |
| `require_line_of_sight` | bool | true |
| `turret_color` | Color | Color(0.5, 0.5, 0.55) |
| `laser_color` | Color | Color(1.0, 0.1, 0.0) |

### Key Methods

- `_ready()`
- `_exit_tree()`
- `_init_shared_resources()` -- Turret body material template (shared by base + column)
- `_parse_config()` -- Check config_target meta (set by map loader via #target:mode syntax)
- `_apply_target_mode(mode: String)`
- `_build_turret()`
- `_build_base()`
- `_build_head()`
- `_build_barrel()`
- `_build_laser_sight()`
- `_process(delta)`
- `_update_hit_pool(delta: float)`
- `_get_target_position(target: Node3D)`
- `_find_target()`
- `_find_player()` -- Try XR camera first

### Signals

- `target_acquired`
- `target_lost`
- `fired`
- `hit_target`
- `laser_hit`

### Grid Config

Accepts: `bullet_speed`, `burst_size`, `detection_range`, `fire_rate`, `laser_damage_per_hit`, `reload_time`, `rotation_speed`, `target`, `use_laser_damage`

## Curriculum Position

### Sequences

- **Unused Artifacts Test** (testmaps) -- map: test_batch_1

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Lab_Death | [5,8] | `:180:1` |
| test_batch_1 | [8,4] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
