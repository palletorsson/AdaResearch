# Artifact: Scalable Wood Cube

> Grabbable wood cube with two-handed scaling support. Scale snaps to discrete values (0.05x to 4.0x) per axis.

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** primitives | **Themes:** decoration, environment

Grabbable wood cube with two-handed scaling support. Scale snaps to discrete values (0.05x to 4.0x) per axis.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/cubes/grab_cube_scalable.gd` (451 lines) |
| **Scene** | `res://commons/primitives/cubes/grab_cube_scalable.tscn` |
| **Registry** | `furniture.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `scale_snap_values` | PackedFloat32Array | [0.05, 0.1, 0.25, 0.5, 0.75, 1.0, 1.25, 1.5, 2.0, 3.0, 4.0] |
| `enable_scaling` | bool | true |
| `scale_mode` | ScaleMode | ScaleMode.PER_AXIS |
| `scale_sensitivity` | float | 10.0 |
| `initial_scale_x_index` | int | 3 |
| `initial_scale_y_index` | int | 3 |
| `initial_scale_z_index` | int | 3 |
| `show_scale_indicator` | bool | true |
| `indicator_offset` | Vector3 | Vector3(0, 0.15, 0) |

### Key Methods

- `_ready()`
- `_physics_process(_delta: float)`
- `_on_grabbed(_pickable, _by: Node3D)` -- Check if this is a second hand grab
- `_on_released(_pickable, _by: Node3D)` -- Check if we lost the secondary grab
- `_start_scaling()`
- `_stop_scaling()`
- `_disconnect_secondary_controller()`
- `_on_secondary_button_pressed(button: String)` -- Use by_button on secondary controller to cycle axes
- `_process_scaling()`
- `_get_active_axis_index()`
- `_set_active_axis_index(index: int)`
- `_apply_current_scale()`
- `cycle_active_axis()`
- `_update_scale_indicator_text()`
- `_play_scale_tick()` -- Simple audio feedback - haptic feedback

### Signals

- `scale_changed`
- `axis_scale_changed`
- `scaling_state_changed`
- `active_axis_changed`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
