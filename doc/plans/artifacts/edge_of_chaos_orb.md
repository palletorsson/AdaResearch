# Artifact: Edge of Chaos Orb

> A sphere that visualizes the current QFEP state. Color and behavior shift based on global λ and φ values. Green and dancing when at the edge of chaos (λ≈0.4, φ>0).

## Context

**Category:** qfep | **Complexity:** intermediate
**Tags:** visualization, realtime, state_indicator, edge_of_chaos | **Themes:** qfep, emergence

A sphere that visualizes the current QFEP state. Color and behavior shift based on global λ and φ values. Green and dancing when at the edge of chaos (λ≈0.4, φ>0).

## Design

- **Visual:** material: standard, animation: particle_lifecycle

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/edge_of_chaos_orb.gd` (326 lines) |
| **Scene** | `res://commons/interfaces/qfep/edge_of_chaos_orb.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `EdgeOfChaosOrb` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `orb_radius` | float | 0.4 |
| `ring_count` | int | 3 |
| `lambda` | float | 0.4: |
| `phi` | float | 0.5: |

### Key Methods

- `_ready()`
- `_build_orb()`
- `_build_rings()`
- `_build_particles()`
- `_build_light()`
- `_build_labels()`
- `_connect_to_controllers()`
- `_update_state()`
- `_process(delta: float)`
- `set_lambda(value: float)`
- `set_phi(value: float)`
- `get_regime()`

### Signals

- `state_changed`

## Curriculum Position

### Sequences

- qfeplaboratory

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
