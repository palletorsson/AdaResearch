# Artifact: Deltahedron Stages

> five octahedra in a row — fold_amount 0.0, 0.25, 0.5, 0.75, 1.0 — the complete fold progression

## Context

**Category:** hazards | **Complexity:** beginner
**Tags:** fold_system, deltahedron, stages, progression | **Themes:** fold_system

Five octahedra in a row at fold_amount 0%, 25%, 50%, 75%, 100%. Shows the complete net-to-solid fold progression in one glance. Blue (flat) to gold (assembled).

*one variable, five snapshots, the whole story.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** see the entire lifecycle at once — flat net to rigid diamond in five frozen steps
- **Critical Parameter:** spacing between stages shows the fold as a continuous transformation

## Architecture

| | |
|---|---|
| **File** | `commons/fold_system/deltahedron_stages.gd` (215 lines) |
| **Scene** | `res://commons/fold_system/deltahedron_stages.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `DeltahedronStages` extends `Node3D` |
| **Pattern** | procedural |

### Dependencies

- `res://commons/primitives/shared/primitive_mesh_builder.gd`

### Key Methods

- `_ready()`
- `_gen_species()`
- `_gen_tetrahedron()`
- `_gen_octahedron()`
- `_gen_icosahedron()`
- `_compute_flat_net()`
- `_build_stages()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Accepts: `species`

## Curriculum Position

### Sequences

- **Folding: Same Faces, Only Positions Change** (fold_system) -- map: Folding_Zoo

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Folding_Zoo | [2,2] | `:0:0.5` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
