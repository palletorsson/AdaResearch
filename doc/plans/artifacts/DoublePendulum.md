# Artifact: Double Pendulum

> Deterministic chaos—two connected pendulums create wildly unpredictable motion from simple physics. This is where periodic oscillation breaks down; tiny differences in starting conditions lead to radically different trajectories.

## Context

**Category:** waves | **Complexity:** advanced
**Tags:** animation, chaos, topology, wave | **Themes:** oscillation, physics, waves

Deterministic chaos—two connected pendulums create wildly unpredictable motion from simple physics. This is where periodic oscillation breaks down; tiny differences in starting conditions lead to radically different trajectories.

## Design

- **Visual:** material: unlit, animation: vertex
- **Scale:** [6.0, 7.0, 16.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/oscillation_driver/DoublePendulum.gd` (213 lines) |
| **Scene** | `res://algorithms/wavefunctions/oscillation_driver/DoublePendulum.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `m1` | float | 10.0 |
| `m2` | float | 10.0 |
| `l1` | float | 1.5 |
| `l2` | float | 1.5 |
| `g` | float | 9.81 |
| `damping` | float | 0.000 |
| `theta1_start` | float | 90.0 |
| `theta2_start` | float | 90.0 |
| `time_speed` | float | 1.0 |
| `max_trail_length` | int | 1000 |
| `color_cycle_speed` | float | 0.01 |

### Key Methods

- `_ready()`
- `_setup_visuals()`
- `_process(delta: float)` -- --- PHYSICS SIMULATION ---
- `_draw_trail()`
- `_update_drawing()`
- `_find_drawable(node: Node)`
- `_update_cube_color(color: Color)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Joints** (joints) -- map: Joints_ConstrainedMotion

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Joints_ConstrainedMotion | [4,4] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
