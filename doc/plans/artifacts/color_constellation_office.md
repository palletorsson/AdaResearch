# Artifact: color_constellation_office

> RGB/CMY glass walls at angles — additive color mixing through transparency and emitted light overlap

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** color | **Themes:** color, perception

Office space with adaptive additive/subtractive constellations

*color mixing is not a formula — it is what happens when light from different sources arrives at the same place at the same time*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** to walk between colored glass partitions and see your shadow split into complementary colors on the floor
- **Critical Parameter:** transparency — controls how much light passes through each wall; at 0.4 the mixing is visible but walls remain distinct
- **What Emerges:** walking through overlapping RGB walls creates white-ish zones where all three primaries combine — additive mixing proven by architecture
- **Triggers:** none — static installation; color mixing happens through player movement and viewing angle

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/color_mixing/color_constellation_office.gd` (105 lines) |
| **Scene** | `res://algorithms/color/color_mixing/color_constellation_office.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `wall_size` | Vector2 | Vector2(2.0, 2.4) |
| `wall_thickness` | float | 0.03 |
| `transparency` | float | 0.4 |
| `emit_strength` | float | 0.6 |
| `light_energy` | float | 3.0 |
| `light_range` | float | 5.0 |
| `spread` | float | 2.5 |
| `room_size` | Vector3 | Vector3(12.0, 3.0, 10.0) |

### Key Methods

- `_ready()`
- `_build_office_walls()` -- RGB Section - Primary additive colors
- `_add_glass_wall(pos: Vector3, angle_deg: float, color: Color, size: Vector2 = Vector2.ZERO)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Color: Perception, Not Physics** (color) -- map: Color_Walls

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Spectrum | [3,3] | `-` |
| Color_Walls | [3,4] | `:0:-1:2` |

### Relationships

- contrasts with albers_wall_gallery (subtractive paint vs additive light); follows dark_side_prism (spectrum split vs color recombination)
- Needs: OmniLight3D per wall [has]; transparency [has]; VR walkthrough [has]; interactive wall rotation [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
