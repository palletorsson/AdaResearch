# Artifact: Static Point

> a location in space — coordinates (x, y, z) with no size, no volume

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** physics | **Themes:** core, primitives

Non-pickable point marker that displays position. Cannot be grabbed by hands or captured by gravity gun.

*position exists independently of any object occupying it*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** learner feels that a point is pure position, nothing more, nothing else
- **Critical Parameter:** show_label — whether coordinate text billboard appears
- **What Emerges:** the realisation that all geometry begins from zero-dimensional position
- **Triggers:** nothing — entirely still, waiting to be observed

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/point/static_point.gd` (78 lines) |
| **Scene** | `res://commons/primitives/point/static_point.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `point_color` | Color | Color(0.0, 1.0, 1.0, 1.0) |
| `point_radius` | float | 0.04 |
| `show_label` | bool | true |
| `label_offset` | Vector3 | Vector3(0, 0.1, 0) |

### Key Methods

- `_ready()` -- Add to no_gravity_gun group so it won't be picked up
- `_setup_mesh()`
- `_setup_label()`
- `_process(_delta)`
- `set_color(color: Color)`
- `set_label_visible(visible: bool)`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Point_One

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_One | [2,4] | `:0:1` |

### Relationships

- prerequisite for line; contrasts with interactive_point_origin (grabbable version)
- Needs: [missing VR controls — observation only, non-grabbable by design]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
