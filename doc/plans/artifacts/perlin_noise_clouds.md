# Artifact: Perlin Noise Clouds

> Interactive 3D volumetric cloud generation using Perlin noise for atmospheric and procedural sky effects

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** 3d, noise, procedural | **Themes:** probability, procedural, randomness

Interactive 3D volumetric cloud generation using Perlin noise for atmospheric and procedural sky effects

## Design

- **Visual:** material: unlit, animation: particle_lifecycle
- **Scale:** [50.0, 21.0, 50.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/perlinnoiseclouds/perlinnoiseclouds.gd` (450 lines) |
| **Scene** | `res://algorithms/randomness/perlinnoiseclouds/perlinnoiseclouds.tscn` |
| **Registry** | `randomness.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `vr_quality` | VRQuality | VRQuality.BALANCED |
| `enable_adaptive_quality` | bool | true |
| `target_fps` | float | 90.0 |
| `max_frame_time` | float | 11.0 |
| `cloud_size` | Vector3 | Vector3(50, 20, 50) |
| `cloud_density` | float | 0.3 |
| `noise_scale` | float | 0.05 |
| `cloud_layers` | int | 2 |
| `animation_speed` | float | 0.5 |
| `fade_distance` | float | 80.0 |
| `frustum_culling` | bool | true |
| `use_instancing` | bool | true |
| `batch_size` | int | 100 |

### Key Methods

- `_ready()` -- Set quality-based parameters
- `setup_vr_quality()`
- `setup_noise()`
- `setup_multimesh_rendering()` -- Use MultiMesh for efficient instanced rendering in VR
- `setup_vr_cloud_material()`
- `setup_vr_cloud_mesh()`
- `generate_cloud_field()` -- Clear existing particles
- `get_3d_noise(pos: Vector3)` -- Optimized noise sampling for VR
- `create_cloud_particle(pos: Vector3, noise_value: float)` -- Reduced opacity for VR
- `_process(delta: float)`
- `update_camera_info()`
- `animate_clouds_batched(delta)` -- Process clouds in batches to spread load across frames
- `animate_single_particle(particle: CloudParticle, delta)` -- Sample noise for movement (cached for performance)
- `is_in_frustum(pos: Vector3)` -- Simple frustum culling check
- `update_multimesh()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- noise
- randomness

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| TestMap_Unused_14 | [1,3] | `:0:2` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
