# Artifact: Glitch Body

> Mesh that refuses stable form — a procedural sphere-like shape rebuilt every frame with time-varying noise perturbation and HSV hue cycling. Vertices oscillate, stretch, and collapse; the mesh is always transforming, never settling. A body in continuous becoming, resisting fixed categorization.

## Context

**Category:** emergence | **Complexity:** intermediate
**Tags:** glitch, deformation, queer_theory, becoming, procedural, animated, speculative_computation | **Themes:** commons, emergence, mixed

Mesh that refuses stable form — a procedural sphere-like shape rebuilt every frame with time-varying noise perturbation and HSV hue cycling. Vertices oscillate, stretch, and collapse; the mesh is always transforming, never settling. A body in continuous becoming, resisting fixed categorization.

## Design

- **Visual:** material: vertex_color, animation: vertex
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/glitch_body/glitch_body.gd` (241 lines) |
| **Scene** | `res://commons/artifacts/glitch_body/glitch_body.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `GlitchBody` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `body_radius` | float | 0.25 |
| `lat_segments` | int | 20 |
| `lon_segments` | int | 24 |
| `noise_amplitude` | float | 0.08 |
| `noise_speed` | float | 1.5 |
| `hue_cycle_speed` | float | 0.12 |
| `deformation_layers` | int | 4 |

### Key Methods

- `_ready()`
- `_compute_base_sphere()`
- `_create_mesh()`
- `_build_surface()`
- `_deform_vertex(idx: int)`
- `_vertex_color(idx: int, base_hue: float)`
- `_create_labels()`
- `_process(delta: float)`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `body_radius`, `deformation_layers`, `hue_cycle_speed`, `noise_amplitude`, `noise_speed`

## Curriculum Position

### Sequences

- speculativecomputation
- criticalalgorithms

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| SpeculativeComputation_Queer_Machines | [1,1] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
