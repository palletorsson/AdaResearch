# Artifact: Limb Growth

> Detailed limb growth and segmentation model — joints, proportions, and symmetry emerging from simple growth parameters. Biology's recipe for building bodies.

## Context

**Category:** biology | **Complexity:** intermediate
**Tags:** limb, growth, segmentation, joints, biology, morphogenesis | **Themes:** biology, simulation

Detailed limb growth and segmentation model — joints, proportions, and symmetry emerging from simple growth parameters. Biology's recipe for building bodies.

## Design

- **Visual:** material: standard, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `algorithms/computationalbiology/limbgrowth/limbgrowth.gd` (450 lines) |
| **Scene** | `res://algorithms/computationalbiology/limbgrowth/limbgrowth.tscn` |
| **Registry** | `computational_biology.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `sphere_radius` | float | 1.0 |
| `initial_subdivisions` | int | 2 |
| `growth_interval` | float | 1.0 |
| `extrusion_amount` | float | 0.15 |
| `gravity_influence` | float | 0.3 |
| `growth_zone_radius` | float | 0.4 |
| `subdivision_iterations` | int | 2 |
| `dyntopo_enabled` | bool | true |
| `max_edge_length` | float | 0.15 |
| `min_edge_length` | float | 0.03 |
| `detail_size` | float | 0.1 |

### Key Methods

- `_ready()`
- `create_icosphere(radius: float, subdivisions: int)`
- `subdivide_all()`
- `get_or_create_midpoint(v1_idx: int, v2_idx: int, cache: Dictionary)`
- `create_face(vert_indices: Array)`
- `update_face_data()`
- `_process(delta: float)`
- `start_new_growth_zone()` -- Pick random point on sphere surface
- `grow_and_extrude()` -- Dynamic topology: subdivide/collapse based on detail needs
- `static_subdivide_growth_zone()` -- Original static subdivision approach
- `dyntopo_remesh()` -- Dyntopo: adaptive subdivision and collapse based on edge length
- `update_edges()`
- `is_vertex_in_growth_zone(v_idx: int)`
- `subdivide_edge(edge: Edge)`
- `collapse_edge(edge: Edge)` -- Collapse edge by merging v2 into v1

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
