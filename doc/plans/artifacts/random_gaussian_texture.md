# Artifact: Random Gaussian Texture

> Interactive Gaussian distribution texture generation for natural random patterns

## Context

**Category:** emergence | **Complexity:** advanced
**Tags:** emergence, probability, procedural | **Themes:** emergence, probability, randomness

Interactive Gaussian distribution texture generation for natural random patterns

## Design

- **Visual:** material: generated_texture, animation: static
- **Scale:** [2.0, 2.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/distributions/gaussian/random_gaussian_texture.gd` (162 lines) |
| **Scene** | `res://algorithms/randomness/distributions/gaussian/RandomGaussianTexture.tscn` |
| **Registry** | `randomness.json` |
| **Class** | `RandomGaussianTexture` extends `MeshInstance3D` |
| **Pattern** | procedural |

### Key Methods

- `_init(p_width: int = DEFAULT_WIDTH, p_height: int = DEFAULT_HEIGHT, p_stddev: float = 60.0)`
- `_ready()` -- Initialize random number generator with different seed each run
- `_initialize_image()`
- `_setup_timer()`
- `_on_timer_timeout()`
- `_add_gaussian_dot()` -- Sample x-position from Gaussian distribution
- `_draw_dot(center_x: int, center_y: int, radius: int, color: Color)`
- `_update_texture()`
- `_update_material(tex: ImageTexture)`
- `_random_gaussian(mean: float, stddev: float)` -- Use Box-Muller transform to generate Gaussian distribution
- `start()`
- `pause()`
- `reset()`
- `set_standard_deviation(new_stddev: float)`
- `set_update_interval(interval: float)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- randomness

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| TestMap_Unused_18 | [1,3] | `:0:2` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
