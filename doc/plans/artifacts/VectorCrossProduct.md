# Artifact: VectorCrossProduct

> Drag the force and its application point. The purple torque vector pops out perpendicular to both, spinning the ball via the right-hand rule. tau = r x F — the cross product. Zero torque when force passes through center (r parallel to F). Maximum torque when perpendicular to the lever arm.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** cross_product, torque, rotation, right_hand_rule, perpendicular, interactive, vectors, physics | **Themes:** linear_algebra, math, vectors

Drag the force and its application point. The purple torque vector pops out perpendicular to both, spinning the ball via the right-hand rule. tau = r x F — the cross product. Zero torque when force passes through center (r parallel to F). Maximum torque when perpendicular to the lever arm.

## Design

- **Visual:** material: unlit, animation: vertex
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/vectors/06_vector_cross_product/VectorCrossProduct.gd` (169 lines) |
| **Scene** | `res://algorithms/vectors/06_vector_cross_product/VectorCrossProduct.tscn` |
| **Registry** | `vectors.json` |
| **Pattern** | procedural |

### Dependencies

- `res://algorithms/vectors/shared/gadgets/paddle_wheel_gadget.gd`

### Key Methods

- `_ready()` -- Half-size for exhibition display
- `_process(delta: float)`
- `_create_parallelogram_mesh_instance()`
- `_update_parallelogram(a: Vector3, b: Vector3)`
- `_update_info(a: Vector3, b: Vector3, cross: Vector3)`
- `_cache_vector_nodes(arrow: Node3D, cache_dict: Dictionary)`
- `_get_vector_fast(arrow: Node3D, cache_dict: Dictionary)`
- `_update_vector_fast(_arrow: Node3D, vector: Vector3, cache_dict: Dictionary)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- forces
- transformation

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ForcesArena | [26,10] | `:0:-0.2` |
| Vector_Exhibition | [1,10] | `:0:-0.2` |
| VectorOperations | [2,3] | `-` |
| Vectors_3 | [1,4] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
