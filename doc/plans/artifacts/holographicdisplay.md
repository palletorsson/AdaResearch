# Artifact: Holographic Display

> hologram(t) = content * (1 + scale_pulse * sin(2*TAU*t)) + bob * sin(1.5*TAU*t)

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** 3d, visualization, waves | **Themes:** oscillation, physics, waves

Holographic projector with sine-based effects. Scan line sweeps vertically using sin(t), content rotates and bobs, flicker effect at 15Hz creates subtle instability. Wireframe cube demonstrates 3D rotation. Projector lights pulse with phase offsets around the ring.

*A hologram is light structured by interference — oscillation made visible as projection.*

## Design

- **Visual:** material: unlit, animation: transform
- **Interaction:** Observe a floating holographic projection that bobs and pulses with subtle oscillation
- **Critical Parameter:** scan_frequency — controls the speed of the scanning line across the hologram
- **What Emerges:** the illusion of a sci-fi hologram from layered sine-driven animation
- **Triggers:** time drives vertical bob, scale pulse, scan line sweep, and flicker effects

## Architecture

| | |
|---|---|
| **File** | `commons/lab/holographicdisplay/holographicdisplay.gd` (255 lines) |
| **Scene** | `res://commons/lab/holographicdisplay/holographicdisplay.tscn` |
| **Registry** | `wavefunctions.json` |
| **Class** | `LabHolographicDisplay` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `scan_frequency` | float | 2.0 |
| `flicker_frequency` | float | 15.0 |
| `flicker_intensity` | float | 0.1 |
| `hologram_color` | Color | Color(0.2, 0.8, 1.0) |
| `hologram_height` | float | 0.2 |
| `rotation_speed` | float | 0.3 |
| `vertical_oscillation` | float | 0.01 |
| `scale_pulse` | float | 0.05 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_set_hologram_opacity(opacity: float)`
- `_build_display()`
- `_create_hologram_cube()`
- `set_hologram_color(color: Color)` -- Would need to rebuild or update materials
- `set_content_rotation_speed(speed: float)`
- `trigger_glitch()` -- Temporary high flicker

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Synthesis_Lab

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Synthesis_Lab | [4,7] | `:0:1` |

### Relationships

- depends on multi-sine animation layering; contrasts with microscope (hologram vs optical instrument); unlocks sci-fi lab aesthetic
- Needs: VR observation [has], content swap [missing], glitch trigger [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
