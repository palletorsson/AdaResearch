# Artifact: Recursion Spiral

> node(n) = node(n-1) * golden_angle * 0.618 -- Fibonacci spiral of shrinking spheres

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** recursion, fibonacci, golden-ratio, spiral, self-similar | **Themes:** gameplay, hazards

Fibonacci spiral of shrinking sphere nodes spawned at golden-angle intervals. Each node is 0.618x parent size up to depth 5. Recursive cleanup on death. The golden ratio as creature architecture.

*the golden angle distributes optimally -- same ratio arranging sunflower seeds arranges projectiles.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** sphere nodes at the golden angle, each 0.618x the last, spawning recursive projectiles
- **Critical Parameter:** golden angle (137.5 deg) / scale ratio (0.618) -- Fibonacci parameters set geometry
- **What Emerges:** Fibonacci sequence as spatial weapon -- golden ratio determines beauty and threat distribution
- **Triggers:** spawn_timer adds nodes at golden angle; damage_timer fires from spiral nodes

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/recursion_spiral/recursion_spiral.gd` (301 lines) |
| **Scene** | `res://commons/hazards/recursion_spiral/recursion_spiral.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `RecursionSpiral` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `root_radius` | float | 0.2 |
| `golden_ratio` | float | 0.618 |
| `golden_angle` | float | 137.5 |
| `max_depth` | int | 5 |
| `max_nodes` | int | 15 |
| `spawn_interval` | float | 0.8 |
| `base_damage` | float | 15.0 |
| `contact_radius_multiplier` | float | 1.5 |
| `damage_cooldown` | float | 0.5 |
| `base_color` | Color | Color(0.6, 0.2, 0.9) |
| `emission_color` | Color | Color(0.7, 0.3, 1.0) |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_build_root_node()`
- `_spawn_next_node()`
- `_get_next_fibonacci()`
- `_destroy_node_recursive(idx: int)`
- `_find_player()`
- `take_damage(amount: float)`
- `apply_damage(amount: float)`
- `damage(amount: float)`
- `_apply_damage(amount: float)`
- `configure(config: Dictionary)`
- `apply_grid_config(config: Dictionary)`

### Signals

- `enemy_destroyed`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_5

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_5 | [7,7] | `-` |

### Relationships

- embodies recursiveemergence; pairs with fractal_hydra (spiral vs branching)
- Needs: player tracking [has]; spiral node generation [has]; golden angle [has]; projectiles [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
