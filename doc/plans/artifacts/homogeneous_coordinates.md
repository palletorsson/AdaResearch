# Artifact: Homogeneous Coordinates

> Wall panel explaining why 4x4 matrices are used in 3D. Shows a color-coded 4x4 homogeneous transformation matrix: rotation/scale (blue), translation (green), projection (red), homogeneous row (gray). Demonstrates [x,y,z,1] → [x',y',z',1] via matrix multiplication with a 3D coordinate frame.

## Context

**Category:** transforms | **Complexity:** advanced
**Tags:** 3d, color, linear_algebra, transforms | **Themes:** geometry, math, transforms

Wall panel explaining why 4x4 matrices are used in 3D. Shows a color-coded 4x4 homogeneous transformation matrix: rotation/scale (blue), translation (green), projection (red), homogeneous row (gray). Demonstrates [x,y,z,1] → [x',y',z',1] via matrix multiplication with a 3D coordinate frame.

## Design

- **Visual:** material: vertex_color, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/homogeneous_coordinates/homogeneous_coordinates.gd` (475 lines) |
| **Scene** | `res://commons/artifacts/homogeneous_coordinates/homogeneous_coordinates.tscn` |
| **Registry** | `transforms.json` |
| **Class** | `HomogeneousCoordinates` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `color_panel` | Color | Color(0.06, 0.06, 0.1) |
| `color_rotation` | Color | Color(0.3, 0.5, 1.0) |
| `color_translation` | Color | Color(0.3, 1.0, 0.5) |
| `color_projection` | Color | Color(1.0, 0.35, 0.35) |
| `color_homogeneous` | Color | Color(0.55, 0.55, 0.6) |
| `color_title` | Color | Color(0.92, 0.92, 0.97) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_rebuild()`
- `_build_panel()`
- `_build_matrix_display()`
- `_build_brackets()`
- `_build_matrix_cells(values: Array, colors: Array)`
- `_build_point_transform()`
- `_build_coordinate_frame()`
- `_build_axis_lines(axes: Array)`
- `_build_axis_labels(axes: Array)`
- `_build_origin_dot()`
- `_build_translation_arrow()`
- `_build_labels()`
- `_build_title_labels()`
- `_build_legend()`

### Grid Config

Accepts: `color_homogeneous`, `color_panel`, `color_projection`, `color_rotation`, `color_title`, `color_translation`

## Curriculum Position

### Sequences

- transformation

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
