# Artifact: MelodySoundBoard

> Spatial musical instrument where notes are positioned in 3D space and melodies play by moving through them

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** 3d, waves | **Themes:** music, oscillation, physics, waves

Spatial musical instrument where notes are positioned in 3D space and melodies play by moving through them

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/mariocontrol/MelodySoundBoard.gd` (366 lines) |
| **Scene** | `res://algorithms/wavefunctions/mariocontrol/MelodySoundBoard.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `melody_selection` | String | "C Major Scale" |
| `is_ambient_enabled` | bool | true |
| `repeat_interval` | float | 2.0 |
| `tempo_bpm` | float | 120.0 |
| `base_octave` | int | 4 |
| `hardness` | float | 0.3 |
| `ring_time` | float | 0.8 |

### Key Methods

- `_ready()`
- `apply_grid_config(data: Dictionary)`
- `_start_ambient_loop()`
- `play_melody(melody: String)`
- `_play_c_major_scale()`
- `_play_twinkle_twinkle()` -- Twinkle Twinkle Little Star melody
- `_play_ode_to_joy()` -- Beethoven's Ode to Joy (simplified)
- `_play_mario_theme()` -- Super Mario Bros theme (opening)
- `_play_pentatonic_jam()` -- Pentatonic scale improvisation
- `_play_chromatic_run()` -- Chromatic scale run up and down
- `_play_note(note_name: String, octave_offset: int = 0, beat_count: float = 1.0)`
- `_trigger_tone(note_name: String, octave_offset: int, intensity: float)`
- `_semitone_to_freq(semitone: float, octave: int)` -- A4 = 440Hz is semitone 9 in octave 4
- `_generate_sine_bell(freq: float, intensity: float)`
- `_on_mapper_changed(_semitone: float, octave_offset: float, intensity: float)` -- Could trigger notes based on position for interactive play

### Grid Config

Accepts: `melody`, `repeat`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
