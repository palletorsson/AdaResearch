# Artifact: Carpet Cove Gallery

> Grid-based gallery of 24 cyclorama cove displays with wallpaper patterns. Coves in facing rows with 3m aisle, each showing a unique pattern on curved surfaces.

## Context

**Category:** shaders | **Complexity:** intermediate
**Tags:** gallery, cove, cyclorama, wallpaper_groups, installation, grid_aligned | **Themes:** art, shaders, visual

Grid-based gallery of 24 cyclorama cove displays with wallpaper patterns. Coves in facing rows with 3m aisle, each showing a unique pattern on curved surfaces.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/shaders/carpet_cove_gallery/carpet_cove_gallery.gd` (311 lines) |
| **Scene** | `res://algorithms/shaders/carpet_cove_gallery/carpet_cove_gallery.tscn` |
| **Registry** | `shaders.json` |
| **Class** | `CarpetCoveGallery` extends `Node3D` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `corridor_cells_x` | int | 12 |
| `corridor_cells_z` | int | 30 |
| `cove_width_cells` | int | 3 |
| `cove_depth_cells` | int | 2 |
| `aisle_cells` | int | 3 |

### Dependencies

- `res://commons/resourses/shaders/wallpaper_tile.gdshader`
- `res://commons/artifacts/cove_display/cove_display.tscn`

### Key Methods

- `_ready()`
- `_build_floor()`
- `_build_cove_rows()` -- How many coves fit across the width?
- `_place_cove(pos: Vector3, y_rot: float, seed_val: int)`
- `_make_pattern_material(seed_val: int)`
- `_build_lighting()`
- `_generate_domain(seed_val: int)`
- `_domain_blocks(gen: RandomNumberGenerator)`
- `_domain_stripes(gen: RandomNumberGenerator)`
- `_domain_diagonal(gen: RandomNumberGenerator)`
- `_domain_cross(gen: RandomNumberGenerator)`
- `_domain_concentric(_gen: RandomNumberGenerator)`
- `_domain_checker(gen: RandomNumberGenerator)`
- `_domain_scatter(gen: RandomNumberGenerator)`
- `_domain_frame(gen: RandomNumberGenerator)`

### Grid Config

Accepts: `aisle_cells`, `corridor_cells_x`, `corridor_cells_z`, `cove_width_cells`

## Curriculum Position

### Sequences

- patterngeneration

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
