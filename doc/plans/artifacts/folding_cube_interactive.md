# Artifact: folding_cube_interactive

> Interactive folding cube. Grab the handle to manually fold/unfold. Snaps to flat or folded state on release.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** interactive, origami, vr | **Themes:** core, primitives

Interactive folding cube. Grab the handle to manually fold/unfold. Snaps to flat or folded state on release.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/cubes/folding_cube_interactive.gd` (133 lines) |
| **Scene** | `res://commons/primitives/cubes/folding_cube_interactive.tscn` |
| **Registry** | `primitives.json` |
| **Class** | `FoldingCubeInteractive` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `face_size` | float | 0.5 |
| `face_color` | Color | Color(0.9, 0.85, 0.8, 1.0) |
| `handle_color` | Color | Color(0.2, 0.6, 0.9, 1.0) |

### Dependencies

- `res://addons/godot-xr-tools/objects/pickable.tscn`

### Key Methods

- `_ready()`
- `_setup_folding_cube()`
- `_setup_fold_handle()` -- Create a grabbable handle that controls fold progress
- `_process(_delta: float)` -- Map handle Y position to fold progress
- `_on_handle_grabbed(_pickable = null)`
- `_on_handle_released(_pickable = null)` -- Snap to nearest state (flat or folded)
- `_on_handle_clicked(_camera, event, _position, _normal, _shape_idx)`
- `fold()`
- `unfold()`
- `toggle()`
- `get_fold_progress()`
- `set_fold_progress(p: float)`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
