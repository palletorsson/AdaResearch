# Artifact: Newton's Cradle

> The iconic desktop physics demo: 5 pendulum balls suspended from a frame. Pull one back and release — momentum transfers through elastic collisions, ball-by-ball. Shows conservation of momentum and energy, elastic collision mechanics, and the satisfying click-clack of F=ma made tangible.

## Context

**Category:** physics | **Complexity:** beginner
**Tags:** newton, cradle, pendulum, momentum, elastic_collision, energy_conservation, F=ma | **Themes:** classical_mechanics, momentum, collisions, pendulum

The iconic desktop physics demo: 5 pendulum balls suspended from a frame. Pull one back and release — momentum transfers through elastic collisions, ball-by-ball. Shows conservation of momentum and energy, elastic collision mechanics, and the satisfying click-clack of F=ma made tangible.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/physicssimulation/newtoncradle/newtoncradle.gd` (290 lines) |
| **Scene** | `res://algorithms/physicssimulation/newtoncradle/newtoncradle.tscn` |
| **Registry** | `physics_simulation.json` |
| **Class** | `NewtonCradle` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `ball_count` | int | 5 |
| `ball_radius` | float | 0.12 |
| `string_length` | float | 0.6 |
| `ball_mass` | float | 1.0 |
| `gravity` | float | 9.8 |
| `damping` | float | 0.998 |
| `frame_color` | Color | Color(0.3, 0.3, 0.35) |
| `ball_color` | Color | Color(0.85, 0.85, 0.9) |
| `string_color` | Color | Color(0.5, 0.5, 0.55, 0.8) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_frame()` -- Top bar
- `_create_balls()`
- `_update_ball_positions()`
- `_physics_process(delta: float)` -- Pendulum physics for each ball
- `_release_left(count: int = 1)`
- `_release_right(count: int = 1)`
- `_release_both()`
- `_reset_all()`
- `_create_labels()`
- `_create_vr_controls()`
- `_update_info()`
- `_input(event: InputEvent)`
- `reset()`
- `_exit_tree()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- forces

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
