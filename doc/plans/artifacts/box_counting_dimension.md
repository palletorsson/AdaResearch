# Artifact: box_counting_dimension

> D = lim(log N(e) / log(1/e)). Cover the shape with boxes. Count. Shrink. Count again. The slope IS the dimension.

## Context

**Category:** unknown
**Tags:** fractals, dimension, measurement, box-counting

Interactive box-counting dimension measurement on a Sierpinski triangle with log-log plot

*Fractal dimension is not a property of the shape. It's a property of how the shape fills space at every scale.*

## Design

- **Interaction:** To measure. To show that fractals live between integer dimensions. Sierpinski is not 1D or 2D — it's 1.585D.
- **Critical Parameter:** Grid resolution — coarse grids give rough estimates, fine grids converge to true dimension.
- **What Emerges:** Fractal dimension from the scaling relationship. The number 1.585 from pure geometry. Measurement from counting.
- **Triggers:** Grid cycles → progressively finer overlay, log-log plot updates → regression line converges to D, highlight → current scale shown

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/box_counting_dimension/box_counting_dimension.gd` (315 lines) |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural_mesh |

### Key Methods

- `_ready()`
- `_generate_sierpinski_points()` -- Chaos game: Sierpinski triangle in XZ plane
- `_setup_point_cloud()`
- `_setup_grid_display()`
- `_setup_plot()`
- `_setup_labels()`
- `_process(delta: float)`
- `_count_boxes(divisions: int)`
- `_update_grid_display()`
- `_update_plot()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_KochSierpinski | [6,8] | `-` |

### Relationships

- Lives in Fractal_KochSierpinski. Complements the geometric fractals with quantitative measurement. Feeds into the concept that dimension is not always integer.
- Needs: VR shape selector [missing — only Sierpinski]. Manual grid control [missing — auto-cycles]. Different fractal inputs.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
