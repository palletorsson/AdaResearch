# Artifact: Fractal Recursion 1

> circle(center, r, d) = torus(r) + circle(center, r * 0.5, d-1), concentric halving

## Context

**Category:** unknown | **Complexity:** advanced
**Tags:** fractal | **Themes:** generation, procedural

Basic recursion visualization demonstrating fundamental recursive patterns

*Recursion is a function that trusts its smaller self to do the same work — and the concentric circles are the proof that trust was warranted.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 3.0, 3.0] (room_scale)
- **Interaction:** To introduce recursion visually — each ring half the size of the last, rainbow colors marking each depth level
- **Critical Parameter:** max_depth — at 6 the innermost ring is 1/64th of the original, nearly invisible, proving the recursion continues beyond sight
- **What Emerges:** The visual proof that halving never reaches zero — the rings keep going, each smaller but present
- **Triggers:** show_animation → rings appear one depth at a time; animation_speed controls the reveal rhythm

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/example_8_1_recursion_vr.gd` (206 lines) |
| **Scene** | `res://algorithms/fractals/example_8_1_recursion_vr.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_depth` | int | 6 |
| `initial_radius` | float | 0.4 |
| `show_animation` | bool | false |
| `show_frame` | bool | true |
| `frame_thickness` | float | 0.02 |

### Key Methods

- `_ready()` -- Create frame background
- `_process(delta: float)`
- `_create_frame()` -- Create a square frame around the circles
- `_create_frame_bar(pos: Vector3, bar_size: Vector3, mat: StandardMaterial3D)`
- `draw_recursive_circles(center: Vector3, radius: float, depth: int)`
- `create_circle(center: Vector3, radius: float, depth: int)`
- `clear_circles()`
- `increase_depth()`
- `decrease_depth()`
- `reset()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_Synthesis

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_Synthesis | [2,2] | `-` |

### Relationships

- The simplest recursion example; precedes example_8_2 (squares) and example_8_3 (branching circles)
- Needs: VR depth control [missing], animation toggle [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
