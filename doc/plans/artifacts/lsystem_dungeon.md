# Artifact: L-System Dungeon

> F → F+RF-FF-FR+F, R → RFRFRF — corridor grammar with room markers at 90° turns

## Context

**Category:** procedural | **Complexity:** advanced
**Tags:** 2d, l_system, procedural, simulation, string | **Themes:** grammar, l_systems, procedural

Procedural dungeon floor plan generated from L-system grammar rules — corridors and rooms emerge from string rewriting, rendered as a miniature architectural model.

*Two rules and a turtle — the dungeon writes itself.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To generate a dungeon you can look down on — corridors and rooms from two rewriting rules
- **Critical Parameter:** iterations — controls dungeon complexity from a single corridor to a labyrinthine floor plan
- **What Emerges:** Spatial variety from two rules — rooms appear at grammatical intersections, not placed by a designer
- **Triggers:** Iteration 1 → simple L-shaped corridor; iteration 4 → dense maze with many rooms and walled passages

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/lsystem_dungeon/lsystem_dungeon.gd` (284 lines) |
| **Scene** | `res://commons/artifacts/lsystem_dungeon/lsystem_dungeon.tscn` |
| **Registry** | `lsystems.json` |
| **Class** | `LSystemDungeon` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `display_size` | float | 0.7 |
| `iterations` | int | 3 |
| `corridor_color` | Color | Color(0.35, 0.3, 0.28) |
| `room_color` | Color | Color(0.5, 0.4, 0.35) |
| `wall_color` | Color | Color(0.2, 0.18, 0.16) |

### Key Methods

- `_ready()`
- `_create_base()`
- `_create_labels()`
- `_generate_dungeon()` -- Clear previous geometry
- `_produce_string()`
- `_interpret_turtle(lstring: String, segments: Array, rooms: Array)` -- Start facing +X on XZ plane
- `_update_label()`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `iterations`

## Curriculum Position

### Sequences

- **L-Systems: Rules Grow Structure** (lsystems) -- map: LSystems_Architecture

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| LSystems_Architecture | [6,5] | `-` |

### Relationships

- Feeds into LSystems_Architecture. Contrasts with CityGenerator (2D plan vs 3D growth). Bridges to proceduralgeneration.
- Needs: VR iteration control [missing], Label3D [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
