# Artifact: Mesh Morpher

> morph(sphere -> cube -> torus -> cylinder) -- 4 topological shapes, each a different attack

## Context

**Category:** hazards | **Complexity:** expert
**Tags:** mesh, topology, morphing, shape-shifting | **Themes:** gameplay, hazards

Shape-shifting creature morphing between sphere, cube, torus, and cylinder. Each topology has a different attack pattern — roll, slam, spin, sweep. Mesh topology as combat strategy.

*genus is not cosmetic -- sphere, cube, torus, cylinder each encode different threats.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a creature shifting between sphere, cube, torus, cylinder -- genus as combat strategy
- **Critical Parameter:** _current_topo / _morph_timer -- which topology is active determines attack type
- **What Emerges:** topology as identity -- the creature's nature changes with genus, not just appearance
- **Triggers:** morph_timer triggers change; transition_phase interpolates shapes; each has unique attack

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/mesh_morpher/mesh_morpher.gd` (386 lines) |
| **Scene** | `res://commons/hazards/mesh_morpher/mesh_morpher.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `MeshMorpher` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `morph_interval` | float | 5.0 |
| `transition_duration` | float | 0.4 |
| `slam_damage` | float | 25.0 |
| `slam_radius` | float | 1.5 |
| `spin_damage_per_second` | float | 15.0 |
| `sweep_damage` | float | 18.0 |

### Key Methods

- `_on_ready()`
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()`
- `_build_leg(index: int)`
- `_set_body_shape(topo: int)`
- `_get_topo_label_text()`
- `_process_visual(delta: float)`
- `_start_morph_transition()`
- `_update_transition()` -- Shrink phase
- `_finish_morph_transition()`
- `_update_material_for_topo()`
- `_update_speed_for_topo()`
- `_process_chase(delta: float)` -- Topology-specific chase behavior
- `_start_slam()`

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_7

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_7 | [7,5] | `-` |

### Relationships

- embodies meshes sequence; contrasts with bricoleur_golem (fixed topologies vs improvised)
- Needs: HazardCreatureBase [has]; 4 topology meshes [has]; morph interpolation [has]; per-topology attacks [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
