# Artifact: Tentacle Cube

> rigid_cube.unfold(tentacles) -- rigid cell that grew soft appendages when threatened

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** tentacle, procedural, cube, hybrid | **Themes:** biology, gameplay, hazards

A cube with procedural tentacles extending from its faces. Combines rigid geometry with fluid organic motion — the grid cell that grew limbs.

*a rigid cell that grew soft appendages -- the boundary of categories dissolves.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a stationary machine cube unfolding mechanical tentacles when the player approaches
- **Critical Parameter:** _health / tentacle reach -- cube is rigid, tentacles flexible; proximity triggers deploy
- **What Emerges:** the hybrid of rigid and soft -- a cube that forgot which kingdom it belonged to
- **Triggers:** player proximity triggers deployment; tentacles extend, sweep, retract; damage on contact

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/tentacle_cube/tentacle_cube.gd` (494 lines) |
| **Scene** | `res://commons/hazards/tentacle_cube/tentacle_cube.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `TentacleCube` extends `CharacterBody3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cube_size` | float | 0.4 |
| `tentacle_count` | int | 4 |
| `segments_per_tentacle` | int | 4 |
| `segment_length` | float | 0.15 |
| `segment_radius` | float | 0.025 |
| `joint_radius` | float | 0.035 |
| `max_health` | float | 100.0 |
| `detection_radius` | float | 4.0 |
| `retract_radius` | float | 6.0 |
| `strike_damage` | float | 15.0 |
| `strike_range` | float | 1.2 |
| `unfold_duration` | float | 1.2 |
| `retract_duration` | float | 0.8 |
| `idle_sway_speed` | float | 1.5 |
| `track_speed` | float | 4.0 |
| `cube_color` | Color | Color(0.12, 0.12, 0.15, 1.0) |
| `segment_color` | Color | Color(0.2, 0.2, 0.25, 1.0) |
| `joint_color` | Color | Color(0.08, 0.08, 0.1, 1.0) |
| `emission_color` | Color | Color(1.0, 0.4, 0.15, 1.0) |
| `emission_intensity` | float | 2.0 |

### Key Methods

- `_ready()`
- `_physics_process(delta: float)`
- `_process_dormant(_delta: float)`
- `_process_unfolding(_delta: float)`
- `_process_active(delta: float)`
- `_process_retracting(_delta: float)`
- `_process_dead(_delta: float)` -- Collapse animation
- `_set_state(new_state: State)`
- `_create_materials()` -- Cube body - dark metallic
- `_calculate_fold_positions()` -- Each tentacle folds onto a different face of the cube
- `_build_collision()`
- `_build_cube()`
- `_build_skeleton()`
- `_build_tentacle_meshes()`
- `_apply_fold_pose()`

### Signals

- `enemy_destroyed`
- `tentacle_strike`

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_3

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_3 | [7,5] | `-` |

### Relationships

- unique hybrid -- rigid body with soft appendages; contrasts with pure walkers and fields
- Needs: class_name TentacleCube [has]; tentacle deployment [has]; proximity detection [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
