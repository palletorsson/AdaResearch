# Artifact: Omoss

> Interactive Omoss algorithm for procedural organic growth and pattern generation

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** pattern, procedural | **Themes:** probability, procedural, randomness

Interactive Omoss algorithm for procedural organic growth and pattern generation

## Design

- **Visual:** material: standard, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/proceduralrandomness/geometrybased/omoss/omoss.gd` (482 lines) |
| **Scene** | `res://algorithms/randomness/proceduralrandomness/geometrybased/omoss/omoss.tscn` |
| **Registry** | `randomness.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()` -- Set up scene
- `setup_environment()` -- Create environment with soft pink background
- `setup_lighting()` -- Create soft key light
- `create_materials()` -- Initialize noise for materials
- `create_composition()` -- Root node for the composition
- `create_fluid_mass(material, position, scale_vec)`
- `create_fabric_layer(parent, material, position, radius, height)`
- `create_textured_sphere(material, position, radius)`
- `create_drip_elements(parent, count)`
- `create_bubble_extensions(parent, origin_position, count)`
- `_process(delta: float)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- randomness

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| TestMap_Unused_12 | [1,1] | `:0:2` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
