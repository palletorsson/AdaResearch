# Artifact: Critter Placer

> Grid-placeable DNA organism. Bridges the grid system and nature system. Place in interactables layer with config to spawn a specific organism at a grid cell. Supports presets (lsystem_tree, fibonacci_flower, turing_fungus, etc.) and custom DNA gene overrides.

## Context

**Category:** nature_system | **Complexity:** beginner
**Tags:** nature_system, critter_dna, grid_placeable, tree, flower, fungus, creature, hybrid, teaching | **Themes:** biology, nature, teaching, grid

Grid-placeable DNA organism. Bridges the grid system and nature system. Place in interactables layer with config to spawn a specific organism at a grid cell. Supports presets (lsystem_tree, fibonacci_flower, turing_fungus, etc.) and custom DNA gene overrides.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/critter_placer/critter_placer.gd` (217 lines) |
| **Scene** | `res://commons/artifacts/critter_placer/critter_placer.tscn` |
| **Registry** | `nature_system.json` |
| **Class** | `CritterPlacer` extends `Node3D` |
| **Pattern** | procedural |

### Key Methods

- `apply_grid_config(config: Dictionary)` -- No config — spawn a random organism
- `_ready()` -- If no config was applied (standalone use), spawn random
- `_load_preset(preset_name: String)`
- `_build_from_config(config: Dictionary)`
- `_spawn_organism()`
- `get_dna()`
- `get_entity()`
- `get_kingdom_name()`

### Grid Config

Accepts: `preset`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
