# Artifact: SpectrumVisualizer

> wavelength_to_color(lambda) animated between linear layout (physics) and circular layout (perception) — the spectrum closes into a wheel

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** spectrum, color_wheel, magenta, wavelength, perception, animation, color, physics | **Themes:** color, perception

The visible spectrum bends from a line into a circle. Wavelength-to-color animation phases between linear layout (physics) and circular layout (perception). Magenta dots fill the gap that physics leaves empty — magenta is not a spectral color, it is the brain's invention to close the loop. The color wheel is a lie the brain tells to make the spectrum feel complete.

*the color wheel is a lie the brain tells to make the spectrum feel complete — magenta is the seam*

## Design

- **Visual:** material: unlit, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** to watch the visible spectrum bend from a line into a circle, magenta dots filling the gap that physics leaves empty
- **Critical Parameter:** cycle_duration — controls how long you dwell in physics-mode vs perception-mode, pacing the conceptual shift
- **What Emerges:** magenta dots appearing in the gap reveal that magenta is not a spectral color — it is the brain's invention to close the loop
- **Triggers:** time drives a continuous phase animation: 0-40% linear, 40-60% transition, 60-100% circular — no user input needed

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/spectrum_visualizer/SpectrumVisualizer.gd` (160 lines) |
| **Scene** | `res://algorithms/color/spectrum_visualizer/SpectrumVisualizer.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `spectrum_length` | float | 10.0 |
| `dot_count` | int | 60 |
| `dot_size` | float | 0.5 |
| `cycle_duration` | float | 12.0 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_create_spectrum()`
- `_create_dot(color: Color)`
- `_animate()` -- 0.0 - 0.4: Linear Mode (Physics)
- `_wavelength_to_color(lambda: float)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Color: Perception, Not Physics** (color) -- map: Color_Pillar

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Pillar | [20,3] | `-` |
| Color_Spectrum | [6,5] | `-` |

### Relationships

- contrasts with spectrum_forest (spatial vs temporal spectrum); follows dark_side_prism (static split vs animated closure)
- Needs: auto-animation [has]; Label3D [missing]; VR pause/scrub [missing]; wavelength labels [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
