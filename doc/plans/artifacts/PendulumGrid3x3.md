# Artifact: Pendulum Grid 3x3

> Nine pendulums in a grid—each swinging independently but visible together. Watch for emergent patterns: when do they synchronize? When do they diverge? Collective oscillation reveals hidden coupling.

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** 2d, swarm, wave | **Themes:** oscillation, physics, waves

Nine pendulums in a grid—each swinging independently but visible together. Watch for emergent patterns: when do they synchronize? When do they diverge? Collective oscillation reveals hidden coupling.

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [11.0, 9.0, 19.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/oscillation_driver/PendulumGrid3x3.gd` (116 lines) |
| **Scene** | `res://algorithms/wavefunctions/oscillation_driver/PendulumGrid3x3.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | int | 3 |
| `cell_spacing` | float | 5.0 |
| `pendulum_scale` | float | 0.5 |
| `base_theta1` | float | 90.0 |
| `base_theta2` | float | 90.0 |
| `angle_variation` | float | 8.0 |

### Dependencies

- `res://algorithms/wavefunctions/oscillation_driver/DoublePendulum.tscn`

### Key Methods

- `_ready()`
- `_spawn_pendulum_grid()` -- Calculate offset to center the grid
- `_create_pendulum(row: int, col: int)`
- `_disable_camera(pendulum: Node3D)`
- `_setup_camera()`
- `get_pendulum_at(row: int, col: int)`
- `reset_all_pendulums()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
