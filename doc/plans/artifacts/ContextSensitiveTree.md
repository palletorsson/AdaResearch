# Artifact: Context-Sensitive Tree

> if ray_cast(branch_tip, obstacle) → prune_branch(red_stub) else grow_forward

## Context

**Category:** procedural | **Complexity:** advanced
**Tags:** l_system, procedural, simulation, tree | **Themes:** grammar, l_systems, procedural

L-System tree with physics-based environmental awareness and branch pruning

*Context sensitivity is how real trees grow — the environment is part of the grammar.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** To grow around obstacles — a tree that senses its environment and adapts its shape accordingly
- **Critical Parameter:** obstacle positions — the spatial constraints that force the tree to find alternative growth paths
- **What Emerges:** Adaptive morphology from collision detection — the tree's shape encodes the obstacle layout
- **Triggers:** Obstacle near trunk → dramatic pruning, asymmetric crown; no obstacles → normal fractal plant

## Architecture

| | |
|---|---|
| **File** | `algorithms/lsystems/ContextSensitive/ContextSensitiveTree.gd` (188 lines) |
| **Scene** | `res://algorithms/lsystems/ContextSensitive/ContextSensitiveTree.tscn` |
| **Registry** | `lsystems.json` |
| **Class** | `ContextSensitiveLTree` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `iterations` | int | 5 |
| `step_length` | float | 0.12 |
| `angle` | float | 25.0 |

### Key Methods

- `_ready()` -- Setup Turtle
- `_create_obstacles()` -- Obstacles sized to interact with the tree's natural growth space
- `_generate_tree()`
- `_interpret_with_context(instructions: String)`
- `_add_decorations()` -- Info label
- `apply_grid_config(config_data: Dictionary)`
- `_exit_tree()`

### Grid Config

Accepts: `angle`, `iterations`

## Curriculum Position

### Sequences

- **Grammar Systems: Rules Generate Structure** (grammar_systems) -- map: Voxel_Grammar
- **L-Systems: Rules Grow Structure** (lsystems) -- map: LSystems_Growth

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| LSystems_ContextSensitiveTree | [3,5] | `-` |
| LSystems_Growth | [3,6] | `-` |
| Voxel_Grammar | [2,6] | `:0:1:0.65` |

### Relationships

- Feeds into LSystems_Growth. Extends lsystem_tree (blind growth → environmentally aware). Uses physics raycasting.
- Needs: VR obstacle placement [missing], iteration control [missing], Label3D [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
