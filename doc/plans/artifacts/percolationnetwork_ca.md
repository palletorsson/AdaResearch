# Artifact: percolationnetwork_ca

> P(occupied) = threshold -> CA flow propagation from top to bottom — does the random lattice percolate?

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

percolationnetwork_ca

*connectivity is not gradual — below the threshold nothing connects, above it everything does, and at the threshold the system decides*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [18.0, 1.0, 18.0] (world_scale)
- **Interaction:** to stand inside a 3D lattice and watch pink flow seep through white cubes, hoping it reaches the bottom before paths run out
- **Critical Parameter:** PERCOLATION_THRESHOLD (0.4) — the probability that defines whether the system connects or fragments; near p_c the outcome is uncertain
- **What Emerges:** at the critical threshold, the percolating cluster forms a fractal — neither filling the space nor vanishing, but threading through it
- **Triggers:** _process ticks the CA every frame; flow propagates through 6-connected neighbors; collision removed from flowing cubes so player can walk through the network

## Architecture

| | |
|---|---|
| **File** | `algorithms/proceduralgeneration/growth_systems/percolationnetwork_ca/percolationnetwork_ca.gd` (469 lines) |
| **Scene** | `res://algorithms/proceduralgeneration/growth_systems/percolationnetwork_ca/percolationnetwork_ca.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `setup_percolation_system()` -- Initialize 3D arrays
- `initialize_lattice()` -- Create random occupied sites based on percolation threshold
- `create_cube_collision_boxes()` -- Initialize cube nodes array
- `start_percolation()`
- `_process(_delta)`
- `update_percolation_automata()`
- `calculate_neighbor_flow(x: int, y: int, z: int)`
- `propagate_flow_to_neighbors(new_flow: Array, x: int, y: int, z: int, current_flow: float)`
- `is_valid_position(x: int, y: int, z: int)`
- `duplicate_grid()`
- `duplicate_flow_grid()`
- `create_single_cube_collision(x: int, y: int, z: int, state: CellState)` -- Create parent node
- `get_material_for_state(state: CellState)`
- `update_cube_visualization()` -- Update existing cubes and create new ones as needed

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Procedural Generation: Rules Build Worlds** (proceduralgeneration) -- map: PG_Percolation_Network

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| PG_Percolation_Network | [10,4] | `:0:4.2` |

### Relationships

- bridges cellularautomata sequence to proceduralgeneration; contrasts with caverandomwalk (random walk vs random lattice); teaches phase transitions
- Needs: per-cube collision [has]; flow visualization [has]; real-time CA [has]; VR threshold slider [missing]; Label3D [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
