# Artifact: pyramidlong

> tall pyramid(rectangular base, height=2.8) — a stretched variant that teaches aspect ratio

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** primitive | **Themes:** core, primitives

Procedurally models a long rectangular pyramid primitive with computed faces and materials.

*proportion is a dimension of meaning — the same topology at different aspect ratios tells different stories*

## Design

- **Visual:** material: shader, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** learner sees that changing one dimension breaks square symmetry and changes the entire character
- **Critical Parameter:** pyramid_height = 2.8 — taller than wide, making it a spire rather than a mound
- **What Emerges:** the architectural reading — this is a steeple, an obelisk, a monument; proportion carries meaning
- **Triggers:** nothing — static object placed for spatial comparison with regular pyramid

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/pyramid/pyramidlong.gd` (128 lines) |
| **Scene** | `res://commons/primitives/pyramid/pyramidlong.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | shader_demo |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `create_pyramid()`
- `create_pyramid_vertices()`
- `create_pyramid_faces()` -- 6 triangular faces (2 for square base + 4 triangular sides)
- `add_triangle_with_normal(st: SurfaceTool, vertices: Array, face: Array)`
- `apply_queer_material(mesh_instance: MeshInstance3D, color: Color)` -- Create shader material using the solid wireframe shader
- `set_base_color(color: Color)`
- `set_pyramid_size(height: float, width: float, length: float)`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Primitives_Melencolia

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Primitives_Melencolia | [3,3] | `:0:-0.5` |

### Relationships

- sibling to pyramid.gd; demonstrates non-uniform scaling at the geometry level
- Needs: [missing VR controls — static display; pink color chosen for visibility]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
