# Artifact: Edge Detector

> Handheld tool that detects edge of chaos. Beeps faster and glows brighter as lambda approaches 0.4. The antenna vibrates at the sweet spot. A tricorder for QFEP.

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** chaos, graph, qfep | **Themes:** philosophy, qfep, theory

Handheld tool that detects edge of chaos. Beeps faster and glows brighter as lambda approaches 0.4. The antenna vibrates at the sweet spot. A tricorder for QFEP.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1, 1, 1] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/edge_detector.gd` (210 lines) |
| **Scene** | `res://commons/interfaces/qfep/edge_detector.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `EdgeDetector` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `pickable` | - | - |

### Key Methods

- `_ready()`
- `_create_body()`
- `_create_antenna()`
- `_create_indicator()`
- `_create_audio()`
- `_process(delta)`
- `_update_detection()` -- Calculate how close we are to edge of chaos (0.3-0.5)
- `_update_visuals(_delta)`
- `_update_audio()` -- Beep rate depends on proximity to edge
- `_play_beep()` -- In a full implementation, play actual audio
- `on_lambda_changed(lambda: float)`
- `_connect_to_lambda()`

### Signals

- `edge_detected`

## Curriculum Position

### Sequences

- qfeplaboratory

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
