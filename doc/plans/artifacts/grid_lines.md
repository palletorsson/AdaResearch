# Artifact: grid_lines

> grid = {x=i*step, z=j*step : i,j ∈ ℤ} — the XZ plane made legible as a lattice of lines

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** grid | **Themes:** core, primitives

grid_lines.

*a coordinate grid does not exist in space — it is a social agreement we project onto space*

## Design

- **Visual:** material: unlit, animation: vertex
- **Scale:** [5.0, 6.0, 5.0] (environment)
- **Interaction:** learner experiences space as structured and measurable, not infinite and featureless
- **Critical Parameter:** grid_size — how many cells appear; determines resolution of spatial reference
- **What Emerges:** the grid as a display medium, not just reference — traces replay as spatial memory
- **Triggers:** TraceData global signal — saved player traces appear as scaled rotating meshes on grid intersections

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/line/grid_lines.gd` (298 lines) |
| **Scene** | `res://commons/primitives/line/grid_lines.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | int | 5 |
| `cell_spacing` | float | 1.0 |
| `trace_scale` | float | 5.0 |
| `shadow_snap_subdivisions` | int | 4 |
| `trace_height` | float | 0.5 |
| `rotation_speed` | float | 10.0 |
| `show_data_table` | bool | true |
| `data_table_position` | Vector3 | Vector3(0, 2.5, 0) |
| `data_table_color` | Color | Color(0.9, 0.95, 1.0, 1.0) |
| `data_table_font_size` | int | 20 |
| `data_table_page_duration` | float | 2.0 |

### Key Methods

- `_process(delta: float)`
- `_ready()`
- `_on_trace_added(points: Array)`
- `_create_trace_mesh(points: Array)`
- `_create_shadow_trace(points: Array, center: Vector3, scale_factor: float)`
- `setup_grid()`
- `_setup_data_table()`
- `_add_trace_to_data(points: Array)`
- `_update_data_table()`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Point_Line_Grid

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_Line_Context | [11,6] | `-` |
| Point_Line_Grid | [3,4] | `-` |

### Relationships

- subscribes to player_trace data; uses line as its primitive unit
- Needs: [has Label3D [has], missing VR controls — purely informational]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
