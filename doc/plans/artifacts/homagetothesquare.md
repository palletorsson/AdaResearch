# Artifact: homagetothesquare

> for i in 10: draw_quad(center, size[i], color[i]) — ten nested squares from Albers' color theory, each a slightly smaller concentric rectangle rendered front-to-back with vertex colors

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** 3d | **Themes:** algorithms, misc

Procedurally layers Albers-inspired colored squares on a plane mesh.

*Albers proved that color is never seen as it is — it is always relative, always a negotiation between adjacent fields*

## Design

- **Visual:** material: vertex_color, animation: static
- **Scale:** [3.0, 9.0, 4.0] (environment)
- **Interaction:** to stand before a flat painting and feel color relationships shift as squares nest — warm beige yields to mauve yields to coral, each boundary a silent argument about adjacency
- **Critical Parameter:** square_sizes ratio array — the proportional nesting from 1.0 down to 0.16 determines which color dominates peripheral vision vs foveal attention
- **What Emerges:** transparency (alpha 0.8) lets background bleed through, so the painting's appearance depends on the grid cube behind it — context changes the work
- **Triggers:** _ready builds ArrayMesh with vertex colors and a dark wood frame; static display, no animation

## Architecture

| | |
|---|---|
| **File** | `algorithms/primitives/homagetothesquare/homagetothesquare.gd` (175 lines) |
| **Scene** | `res://algorithms/primitives/homagetothesquare/homagetothesquare.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `debug` | bool | false |

### Key Methods

- `_ready()`
- `create_frame()` -- Create a thick frame behind the painting
- `create_albers_plane()` -- Create the base plane geometry
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Dev Examples** (devexamples) -- map: Structure_Examples
- **Structure Grammar: Spatial Formulas** (structure) -- map: F14_Lab_Bench

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| F14_Lab_Bench | [2,5] | `:0:0:0.3` |
| Structure_Examples | [8,29] | `:0:0:0.3` |

### Relationships

- a mathematical art primitive used in F14_Lab_Bench alongside platonicsolids, rainbow, and cube_scene — demonstrates that color theory is computation
- Needs: [missing] no VR interaction — pure contemplative object; no sliders, no buttons

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
