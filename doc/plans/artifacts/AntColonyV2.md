# Artifact: AntColonyV2

> ant.move() = gradient_ascent(pheromone_grid) + wander; pheromone_grid *= decay + diffuse — ant and environment co-evolve

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** swarm | **Themes:** collective, emergence, swarm

AntColonyV2

*the map is written by the travelers — the territory and the route emerge together*

## Design

- **Visual:** material: generated_texture, animation: static
- **Scale:** [11, 21, 6] (world_scale)
- **Interaction:** to walk across the terrain floor and watch ant highways emerge beneath you — paths that existed as pure probability before they were paths
- **Critical Parameter:** grid_resolution — at 64×64 trails look blotchy; at 256×256 the chemical landscape develops fine capillaries and branching structure
- **What Emerges:** trail Y-junctions that route ants to whichever food source is currently less depleted — an emergent load-balancer no one designed
- **Triggers:** two food clusters at asymmetric distances produce unequal trail widths as the colony allocates traffic proportional to yield and proximity

## Architecture

| | |
|---|---|
| **File** | `algorithms/swarmintelligence/ants_v2/AntColonyV2.gd` (138 lines) |
| **Scene** | `res://algorithms/swarmintelligence/ants_v2/AntColonyV2.tscn` |
| **Registry** | `swarmintelligence.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_ants` | int | 100 |
| `terrain_size` | Vector2 | Vector2(50, 50) |
| `grid_resolution` | int | 256 |
| `ant_scene` | PackedScene | - |

### Key Methods

- `_ready()` -- Ensure Home Area detects Ants (Layer 2)
- `_spawn_food_cluster(pos: Vector3, radius: float)` -- Add to emission centers (Type 1 = Food)
- `_on_food_entered(body)` -- print("Food found!")
- `_physics_process(_delta)` -- 1. Source Emission
- `_on_home_area_entered(body)` -- print("Home reached!")
- `_world_to_grid(pos: Vector3)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Swarm Intelligence: No Leader, Yet Coordinated** (swarmintelligence) -- map: SwarmIntelligence_Ant_Colony_Optimization

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| SwarmIntelligence_Ant_Colony_Optimization | [3,5] | `-` |

### Relationships

- simpler predecessor to AntColonyOptimization (no quality weighting, no tandem running, no modes); AntColonyV2 is world-scale vs. ACO's compact heatmap
- Needs: [missing] no VR controls at all; no sliders for ant_count, evaporation, deposit_amount; learner cannot intervene with the colony in real time

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
