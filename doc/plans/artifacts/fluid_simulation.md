# Artifact: Fluid Simulation (SPH)

> Smoothed Particle Hydrodynamics (SPH) fluid simulation — the Navier-Stokes equations approximated by a cloud of particles carrying mass, velocity, and pressure. Kernel functions smooth physical quantities over neighbor particles, producing viscous flow, surface tension, and splashing. The Lagrangian approach to fluid dynamics where the fluid carries its own computational grid.

## Context

**Category:** physics | **Complexity:** advanced
**Tags:** fluid, sph, navier_stokes, pressure, viscosity, surface_tension, splash, lagrangian, particles, continuum | **Themes:** fluid, sph, navier_stokes, continuum

Smoothed Particle Hydrodynamics (SPH) fluid simulation — the Navier-Stokes equations approximated by a cloud of particles carrying mass, velocity, and pressure. Kernel functions smooth physical quantities over neighbor particles, producing viscous flow, surface tension, and splashing. The Lagrangian approach to fluid dynamics where the fluid carries its own computational grid.

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [16.0, 1.0, 16.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/physicssimulation/fluidsimulation/fluidsimulation.gd` (443 lines) |
| **Scene** | `res://algorithms/physicssimulation/fluidsimulation/fluidsimulation.tscn` |
| **Registry** | `physics_simulation.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `particle_count` | int | 150 |
| `smoothing_radius` | float | 0.45 |
| `rest_density` | float | 80.0 |
| `pressure_stiffness` | float | 200.0 |
| `viscosity` | float | 0.8 |
| `gravity_strength` | float | 9.8 |
| `container_half_size` | Vector3 | Vector3(1.0, 1.2, 1.0) |
| `wall_restitution` | float | 0.3 |
| `particle_radius` | float | 0.06 |
| `color_low_speed` | Color | Color(0.15, 0.45, 0.95, 0.85) |
| `color_high_speed` | Color | Color(0.95, 0.35, 0.15, 0.85) |
| `speed_color_range` | float | 3.0 |

### Key Methods

- `_ready()` -- Scale for VR reachability
- `_precompute_kernels()`
- `_allocate_arrays()`
- `_init_particle_positions()` -- Pack particles into a small cube inside the container
- `_create_multimesh()`
- `_create_container()` -- RigidBody3D container — can be grabbed and tilted in VR
- `_setup_vr()`
- `_on_button_pressed(button: String, controller: XRController3D)`
- `_on_button_released(button: String, _controller: XRController3D)`
- `_release_grab()`
- `_physics_process(delta: float)`
- `_build_spatial_hash()`
- `_get_neighbors(idx: int)`
- `_compute_density_pressure()`
- `_compute_forces(delta: float)` -- Gravity in container's local space (allows tilting to slosh!)

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Physics Simulation: The Engine Behind Reality** (physicssimulation) -- map: PhysicsSim_Continuum
- **Physics Simulation: The Engine Behind Reality** (physicssimulation) -- map: PhysicsSimulation_Fluid_Simulation_SPH

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| PhysicsSim_Continuum | [2,2] | `:0:1` |
| PhysicsSimulation_Fluid_Simulation_SPH | [1,0] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
