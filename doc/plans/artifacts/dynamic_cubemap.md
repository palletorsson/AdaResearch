# Artifact: Dynamic Cubemap

> Real-time dynamic cubemap rendering for reflections and environment mapping.

## Context

**Category:** shaders | **Complexity:** beginner
**Tags:** cubemap, reflections, environment, shaders | **Themes:** shaders, visual

Real-time dynamic cubemap rendering for reflections and environment mapping.

## Design

- **Visual:** material: shader, animation: shader_time

## Architecture

| | |
|---|---|
| **File** | `algorithms/shaders/dynamicubemap/dynamiccubemap.gd` (610 lines) |
| **Scene** | `res://algorithms/shaders/dynamicubemap/dynamiccubemap.tscn` |
| **Registry** | `shaders.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `reflective_object_count` | int | 8 |
| `reflection_update_rate` | float | 30.0 |
| `reflection_resolution` | int | 256 |
| `animation_speed` | float | 1.0 |
| `metallic_strength` | float | 0.95 |

### Key Methods

- `_ready()`
- `setup_scene()` -- Create environment optimized for reflections
- `create_dynamic_lighting()` -- Main directional light
- `animate_light(light: OmniLight3D, index: int)`
- `create_environment_objects()` -- Create colorful environment objects to be reflected
- `animate_environment_object(obj: MeshInstance3D, index: int)` -- Gentle rotation for dynamic reflections
- `create_configured_sphere_mesh()` -- Smooth sphere for reflections
- `create_configured_torus_mesh()`
- `create_configured_cylinder_mesh()`
- `create_reflective_objects()` -- Create highly reflective objects that show dynamic reflections
- `create_reflection_material(obj: MeshInstance3D, index: int)` -- Create either standard PBR reflection or custom shader reflection
- `setup_reflection_probes()` -- Create reflection probes for real-time environment capture
- `start_animations()` -- Animate reflective objects for dynamic reflection changes
- `animate_reflective_objects()`
- `start_reflection_updates()` -- Update reflection probes at specified rate for performance

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
