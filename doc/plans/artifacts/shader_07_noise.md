# Artifact: Shader 07 — Noise

> noise(p) = interpolate(random(lattice_points)) — randomness with continuity; value noise interpolates random amplitudes, gradient noise interpolates random slopes, simplex uses triangular lattice

## Context

**Category:** shaders | **Complexity:** advanced
**Tags:** book_of_shaders, noise, perlin, shaders | **Themes:** shaders, visual

Book of Shaders chapter on Perlin noise and gradient noise.

*controlled chaos is not a paradox — it is the only kind that produces worlds*

## Design

- **Visual:** material: shader, animation: transform
- **Interaction:** to slide octaves from 1 to 8 and watch flat static become terrain — to warp the domain and see the landscape hallucinate
- **Critical Parameter:** warp_strength — feeds noise output back as input coordinates, bending space through its own texture
- **What Emerges:** four algorithms answer the same question differently — how do you make chaos coherent? — and domain warping reveals that feeding noise into itself produces geological forms
- **Triggers:** scale slider zooms frequency; octaves adds detail layers; colorize maps noise to hue; domain warp creates self-referential distortion

## Architecture

| | |
|---|---|
| **File** | `algorithms/shaders/book_of_shaders/shader_07_noise.gd` (111 lines) |
| **Scene** | `res://algorithms/shaders/book_of_shaders/shader_07_noise.tscn` |
| **Registry** | `shaders.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_process(_delta: float)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Shaders & Patterns: The Book of Shaders** (patterngeneration) -- map: Shader_07_Noise

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Shader_07_Noise | [5,6] | `-` |

### Relationships

- depends on shader_06_random (hash function); feeds into shader_09_fbm (layered noise) and all procedural terrain/texture generation
- Needs: [has] ShaderRackPanel sliders per display; [missing] no VR push buttons

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
