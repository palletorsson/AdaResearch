# Artifact: Shader Noise Space

> GPU computes noise per-fragment in parallel — the room sphere and walls run independent shader noise loops animated by a time uniform, cycling color and density in real-time

## Context

**Category:** procedural | **Complexity:** advanced
**Tags:** noise, procedural, shader | **Themes:** probability, procedural, randomness

Interactive shader-based noise space exploration with procedural textures and patterns

*GPU noise is not one calculation but millions happening simultaneously — the shader room makes visible that what looks like a continuous field is massively parallel computation*

## Design

- **Visual:** material: shader, animation: transform
- **Scale:** [19, 14, 28] (xlarge)
- **Interaction:** to be inside the noise — to stand inside a sphere where the walls are made of animated GPU hash functions and feel surrounded by the mathematics of structured randomness
- **Critical Parameter:** animation_speed — controls the rate at which the shader's time uniform advances, determining whether the noise feels like slow clouds or fast chaos
- **What Emerges:** the two materials (room sphere vs walls) animate out of phase, creating a sense that the space itself is breathing — an undesigned emergent rhythm from two independent cycles
- **Triggers:** toggling animation_enabled freezes the noise field, revealing its spatial structure without temporal motion; space bar toggles it; escape resets to original parameters

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/shadernoisespace/noiseroom.gd` (260 lines) |
| **Scene** | `res://algorithms/randomness/shadernoisespace/noiseroom.tscn` |
| **Registry** | `randomness.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `animation_enabled` | bool | true |
| `animation_speed` | float | 1.0 |
| `time_scale_variation` | float | 0.5 |
| `color_cycling` | bool | true |
| `color_cycle_speed` | float | 0.3 |
| `cloud_density_animation` | bool | true |
| `density_variation` | float | 0.5 |

### Key Methods

- `_ready()`
- `_setup_materials()` -- Find the main room sphere material
- `_store_original_parameters()`
- `_setup_ui()` -- This could be expanded to create runtime UI controls
- `_process(delta: float)`
- `_update_room_animation()`
- `_update_wall_animation()`
- `_animate_room_colors()`
- `_animate_wall_colors()`
- `_input(event: InputEvent)` -- Space key
- `_reset_to_original_parameters()`
- `set_animation_speed(speed: float)`
- `set_color_cycling(enabled: bool)`
- `set_cloud_density_animation(enabled: bool)`
- `get_animation_info()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Noise: Entropy with Memory** (noise) -- map: Noise_6_Wall

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Noise_6_Wall | [5,5] | `-` |
| Noise_Functions | [6,5] | `-` |
| TestMap_Unused_23 | [3,1] | `:0:2` |

### Relationships

- demonstrates GPU noise as distinct from CPU noise in noiselayers; pairs with shader_arch_gallery; shows what QueerNoiseShader.gdshader produces at room scale
- Needs: animation_enabled toggle via spacebar [has]; no VR slider controls [missing]; color cycling and density animation are coupled to time only [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
