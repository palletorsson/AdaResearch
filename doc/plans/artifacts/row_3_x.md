# Artifact: row_3_x

> for i in range(4): cube.position = Vector3(i × spacing, 0, 0) with label "[i]" — a 1D array of four pickup cubes along the X axis, backed by a 1-row BinaryTableDisplay that turns to 0 when a cube is grabbed

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** array | **Themes:** arrays, data_structures

Places three pickup cubes in a single row along the X axis.

*a 1D array is a line of values — row_3_x makes this literal by arranging physical objects along an axis and labeling them with their distance from the origin*

## Design

- **Scale:** [4, 1, 1] (room_scale)
- **Interaction:** to walk along an array in the X direction and feel that index increases as you move right — to then grab a cube and see the binary table update, closing the loop between physical action and data representation
- **Critical Parameter:** show_binary_table — when true, a 1-row × 4-column "array[i]" table hovers above the row; picking up cube [2] changes the table cell at column 2 from 1 to 0, showing that the cube is the data
- **What Emerges:** the single-row binary table labeled "array[i]" makes explicit that a row of objects IS an array — the visualization and the physical layout are the same data structure rendered in two different media
- **Triggers:** tree_exiting signal on each cube fires _on_cube_removed(index) → sets array_data[0][index] = 0 → calls binary_table.set_cell(0, index, 0) — every grab is a write operation to a 1D array

## Architecture

| | |
|---|---|
| **File** | `algorithms/arrays/row_3_x.gd` (167 lines) |
| **Scene** | `res://algorithms/arrays/row_3_x.tscn` |
| **Registry** | `arrays.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `show_binary_table` | bool | true |
| `debug` | bool | false |

### Dependencies

- `res://commons/scenes/mapobjects/pick_up_cube.tscn`
- `res://algorithms/arrays/binary_table/binary_table_display.tscn`

### Key Methods

- `_ready()`
- `create_row()` -- Load the pickup cube scene
- `_create_binary_table()` -- Load the binary table display scene
- `_on_cube_removed(index: int)` -- Called when a cube is picked up or removed from the row
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Accepts: `count`, `label_color`, `label_size`, `show_binary_table`, `spacing`

## Curriculum Position

### Sequences

- array_tutorial

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Tutorial_2D_Build | [3,2] | `-` |
| Tutorial_Col | [1,2] | `-` |

### Relationships

- same architecture as column_3_z but oriented along X instead of Z, and with binary table companion; both appear in Tutorial_2D_Build to show X and Z axes as independent array dimensions; precedes grid_2d_4x4
- Needs: pickup cubes [has]; Label3D index labels [has]; BinaryTableDisplay [has]; apply_grid_config supports count/spacing/label overrides [has]; no VR controls beyond grabbing [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
