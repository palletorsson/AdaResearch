# Artifact: recursive_boolean_cube

> menger(center, size, d) = {subcube(i) for i in 27 if zeros(i) < 2}, recursive cross-removal

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

recursive_boolean_cube

*The Menger sponge has infinite surface area and zero volume — it is the solid that erased itself.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [3.0, 2.0, 3.0] (room_scale)
- **Interaction:** To be peered through — the holes align on every axis, and through them you see deeper holes, endlessly
- **Critical Parameter:** max_iterations — at 1 it's a cube with a cross cut; at 3 it's a sponge you can barely distinguish from void
- **What Emerges:** Light passing through aligned holes at every scale — the cube becomes a filter, not a solid
- **Triggers:** Each iteration subdivides every surviving cube into 27, removes 7 cross-members; the structure becomes more hole than solid

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/recursive_boolean_cube.gd` (159 lines) |
| **Scene** | `res://algorithms/fractals/recursive_boolean_cube.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_iterations` | int | 3 |
| `cube_size` | float | 2.0 |
| `show_frame` | bool | true |
| `frame_thickness` | float | 0.05 |

### Key Methods

- `_ready()`
- `_create_outer_frame()` -- Light gray plastic frame
- `_create_bar(pos: Vector3, bar_size: Vector3, mat: StandardMaterial3D)`
- `_create_recursive_cubes(center: Vector3, size: float, iteration: int)` -- Base case: create a solid cube at this position
- `_create_cube(center: Vector3, size: float, iteration: int)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_Recursion

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_Recursion | [6,1] | `:0:4` |

### Relationships

- Direct implementation of the Menger sponge rule; precedes the walkable menger_sponge artifact
- Needs: VR walk-through collision [missing], iteration slider [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
