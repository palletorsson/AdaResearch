# Artifact: Kitbashing

> Procedural kitbashing — assembling complex forms from randomized geometric primitives. The digital equivalent of the model-making technique used in Star Wars and Blade Runner.

## Context

**Category:** randomness | **Complexity:** intermediate
**Tags:** kitbashing, procedural, assembly, geometry, randomness, design | **Themes:** algorithms, misc

Procedural kitbashing — assembling complex forms from randomized geometric primitives. The digital equivalent of the model-making technique used in Star Wars and Blade Runner.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/proceduralrandomness/geometrybased/kitbashing/kitbashing.gd` (1283 lines) |
| **Scene** | `res://algorithms/randomness/proceduralrandomness/geometrybased/kitbashing/kitbashing.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `module_count` | int | 30 |
| `space_size` | Vector3 | Vector3(30, 15, 30) |
| `modules_path` | String | "res://modules/" |
| `generation_rules` | String | "surrealist" |
| `color_themes` | - | [ |

### Key Methods

- `_ready()`
- `load_or_create_modules()` -- Try to load existing modules
- `create_procedural_modules()` -- Create module types based on the selected generation style
- `create_surrealist_modules()` -- Create modules inspired by exquisite corpse and surrealist works
- `create_mechanical_modules()` -- Create modules inspired by Vitaly Bulgarov's mechanical kit parts
- `create_organic_modules()` -- Create modules inspired by more organic forms
- `create_mixed_modules()` -- Create some of each type for variety
- `add_stacked_primitives(parent)` -- 2-6 pieces
- `add_floating_parts(parent)` -- 3-7 parts
- `add_nonsensical_machine(parent)` -- Base
- `add_abstract_sculpture(parent)` -- Create a base
- `add_composite_creature(parent)` -- Inspired by exquisite corpse - create a creature with mismatched parts
- `add_mechanical_joint(parent)` -- Base plate
- `add_control_panel(parent)` -- Panel base
- `add_pipe_system(parent)` -- Start and end points

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
