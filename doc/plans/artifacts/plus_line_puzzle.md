# Artifact: Plus Line Puzzle

> ⊥ — two lines whose direction vectors have dot product zero

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

Interactive puzzle: drag line endpoints to form a + (plus) with perpendicular lines.

*perpendicularity is defined by the dot product equaling zero — geometry expressed as algebra*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 2.0, 1.0] (compact)
- **Interaction:** learner feels perpendicularity as a physical arrangement they construct with their hands
- **Critical Parameter:** the intersection constraint — lines must cross at their midpoints to validate
- **What Emerges:** that 90 degrees is not special until you define an inner product — the dot product makes it special
- **Triggers:** snapping all 4 endpoints to the cross targets — success fires only when both conditions met

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/line/puzzles/plus_line_puzzle.gd` (46 lines) |
| **Scene** | `res://commons/primitives/line/puzzles/plus_line_puzzle.tscn` |
| **Registry** | `primitives.json` |
| **Class** | `PlusLinePuzzle` extends `LineSnapPuzzleBase` |
| **Pattern** | procedural |

### Key Methods

- `_init()` -- Define the 4 target vertices for + shape (shared pool)
- `_ready()`
- `_complete_puzzle()`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Point_Lines

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_Lines | [10,2] | `:0:1.2:0#plusfillhole:remove` |

### Relationships

- extends LineSnapPuzzleBase; contrasts with parallel_line_puzzle
- Needs: [missing VR controls — all interaction is line-endpoint snapping]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
