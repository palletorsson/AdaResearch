# Artifact: hallway_scene

> tube_y(x, t) = A * sin(omega * x + phi * t) — sine-displaced cylinders in a tunnel

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** waves | **Themes:** oscillation, physics, waves

Populates a tunnel with sine-driven cylinder tubes and animates them through time.

*A hallway of sine tubes is a frozen moment of wave interference made architectural.*

## Design

- **Visual:** material: standard, animation: particle_lifecycle
- **Scale:** [1.0, 6.0, 26.0] (world_scale)
- **Interaction:** Walk through a hallway where glowing tubes undulate with sine wave displacement
- **Critical Parameter:** tube_count — determines visual density of the oscillating environment
- **What Emerges:** an immersive sine wave environment — architecture that breathes with oscillation
- **Triggers:** time drives phase animation; color_variants assign distinct hues per tube

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/sine_hallway/hallway_scene.gd` (123 lines) |
| **Scene** | `res://algorithms/wavefunctions/sine_hallway/hallway_scene.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `hallway_length` | float | 60.0 |
| `hallway_width` | float | 12.0 |
| `hallway_height` | float | 12.0 |
| `tube_count` | int | 8 |
| `tube_length` | float | 80.0 |
| `wave_amplitude` | float | 2.5 |
| `wave_frequency` | float | 0.3 |
| `tube_radius` | float | 0.4 |
| `segment_spacing` | float | 1.0 |
| `animate_tubes` | bool | true |
| `rotation_speed` | float | 0.35 |
| `color_variants` | Array | [ |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_setup_environment()`
- `_create_sine_tubes()` -- -half_len to half_len-1
- `_update_tubes(time_val: float)`
- `_make_metallic(col: Color)`
- `_make_reflective(col: Color)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Synthesis_Lab

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| TestMap_Unused_7 | [3,3] | `:0:2` |
| Wavefunctions_Colors | [7,2] | `:0:0:0.1` |
| WaveFunctions_Synthesis_Lab | [0,7] | `:0:0:0.1` |

### Relationships

- depends on MultiMesh cylinder instancing; contrasts with coloredlines (sine tubes vs parametric curves); unlocks environmental wave experience
- Needs: VR walkthrough [has], frequency control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
