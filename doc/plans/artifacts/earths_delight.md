# Artifact: earths_delight

> Procedurally sculpts noise terrain and scatters mutated Bosch-inspired plants across it.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** procedural, terrain | **Themes:** philosophy, qfep, theory

Procedurally sculpts noise terrain and scatters mutated Bosch-inspired plants across it.

## Design

- **Visual:** material: vertex_color, animation: static
- **Scale:** [1, 1, 1] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/criticaltheory/earthsdelight/earths_delight.gd` (1456 lines) |
| **Scene** | `res://algorithms/criticaltheory/earthsdelight/earths_delight.tscn` |
| **Registry** | `qfep.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `landscape_size` | - | - |
| `height_scale` | - | - |
| `terrain_octaves` | - | - |
| `terrain_seed` | - | - |
| `terrain_frequency` | - | - |
| `plant_count` | - | - |
| `strange_plant_types` | - | - |
| `mutation_factor` | - | - |
| `plant_scale_min` | - | - |
| `plant_scale_max` | - | - |

### Key Methods

- `_ready()` -- Set up noise generators
- `generate_terrain()` -- Create a gridmesh for the terrain
- `create_basic_plant_meshes()` -- Create various stem types
- `generate_plants()` -- Create a root node for all plants
- `create_strange_plant_template(template_id)`
- `add_decorative_elements(parent_node, rng)` -- Add small decorative elements that reference Bosch's style
- `add_queer_form_elements(plant_node, rng)` -- Add elements that break from traditional plant forms
- `create_spiral_tubes(parent, rng)`
- `create_bubble_cluster(parent, rng)`
- `create_crystal_formation(parent, rng)`
- `create_membrane_sheets(parent, rng)`
- `create_starburst(parent, rng)`
- `create_nested_rings(parent, rng)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
