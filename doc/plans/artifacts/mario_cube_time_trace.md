# Artifact: Mario Cube Time Trace

> trace(t) = (target.x, target.y, z_origin - speed * t) — position history as fading trail

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** wave | **Themes:** commons, mixed

Pick-up cube with time-domain trace in +Z, like pendulum trail.

*Every trajectory is a signal in time; recording it reveals the physics as waveform.*

## Design

- **Visual:** material: unlit, animation: vertex
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** Toss a pickup cube and see its trajectory recorded as a time-domain trace in space
- **Critical Parameter:** time_axis_speed — controls how fast the trace extends along the Z axis
- **What Emerges:** thrown trajectories become visible waveforms — parabolas, oscillations, chaos all recorded
- **Triggers:** continuous position sampling of target node at sample_interval creates the fading trail

## Architecture

| | |
|---|---|
| **File** | `commons/scenes/mapobjects/mario_cube_time_trace.gd` (125 lines) |
| **Scene** | `res://commons/scenes/mapobjects/mario_cube_time_trace.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `target_path` | NodePath | NodePath("PickUpCube") |
| `trail_color` | Color | Color(0.95, 0.7, 0.2, 1.0) |
| `trail_max_points` | int | 1000 |
| `sample_interval` | float | 0.05 |
| `time_axis_speed` | float | 0.6 |
| `fade_trail_over_time` | bool | false |
| `fade_duration` | float | 10.0 |

### Key Methods

- `_ready()`
- `_setup_trail()`
- `_process(delta: float)`
- `_cleanup_old_points()`
- `_rebuild_trail()`

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Intro

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Intro | [13,6] | `:0:1:0.5` |

### Relationships

- depends on target node position sampling; contrasts with draw_dot_time_domain (cube tracking vs hand tracking); unlocks trajectory visualization
- Needs: VR pickup cube to track [has], trail rendering [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
