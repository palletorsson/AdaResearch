# Artifact: 7_8_hexagon_ca_vr

> hex_cell(t+1) = life_rule(6_neighbors) stacked vertically — terrain from time

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** cellular_automata | **Themes:** cellular_automata, computation, emergence

7_8_hexagon_ca_vr

*Six neighbors instead of eight — the shape of the neighborhood reshapes the world.*

## Design

- **Visual:** material: shader, animation: particle_lifecycle
- **Scale:** [32.0, 32.0, 32.0] (world_scale)
- **Interaction:** To grow a hexagonal landscape you can walk on, each generation a new geological layer
- **Critical Parameter:** random_flip — injects noise into deterministic rules, controlling the chaos-order boundary
- **What Emerges:** Walkable terrain with caves and overhangs from stacking hex Life generations with noise
- **Triggers:** High random_flip → turbulent terrain; low → smooth plateaus; changing radius → intimate vs epic scale

## Architecture

| | |
|---|---|
| **File** | `algorithms/cellularautomata/noc_ch07/7_8_hexagon_ca_vr.gd` (238 lines) |
| **Scene** | `res://algorithms/cellularautomata/noc_ch07/7_8_hexagon_ca_vr.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `radius` | int | 20 |
| `update_interval` | float | 0.1 |
| `random_flip` | float | 0.01 |
| `hex_size` | float | 1.0 |
| `max_generations` | int | 40 |
| `layer_height` | float | 0.1 |

### Dependencies

- `res://spatial_ui/parameter_controller_3d.tscn`
- `res://algorithms/cellularautomata/noc_ch07/hex_terrain.gdshader`

### Key Methods

- `_ready()`
- `_setup_environment()`
- `_initialize_grid()`
- `_hex_to_world(coord: Vector2)` -- Pointy topped hexes
- `_process(delta: float)`
- `_step_generation()`
- `_count_neighbors(coord: Vector2)`
- `_update_visuals_layer(layer_index: int)`
- `_add_colliders_for_current_layer()` -- Add colliders for the current layer incrementally
- `_update_status()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Relationships

- Extends CA_BeyondBinary (hex grid vs square). Feeds into CA_ExpandingSpace (3D from 2D).
- Needs: VR random_flip controller [has], Label3D [has], collision [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
