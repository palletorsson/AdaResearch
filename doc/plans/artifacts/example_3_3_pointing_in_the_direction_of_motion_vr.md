# Artifact: Pointing in Direction of Motion

> look_at(position + velocity). A cone that always points where it is going. Orientation derived from velocity, not set independently.

## Context

**Category:** physics | **Complexity:** intermediate
**Tags:** steering, velocity, orientation, heading, lissajous, oscillation, nature_of_code, vr | **Themes:** physics, simulation

A cone that always points where it is going — orientation derived from velocity, not set independently. Sinusoidal thrust drives the mover in Lissajous-like 3D curves. The cone's nose traces the velocity vector, making heading a consequence of movement. A thing that points where it moves has surrendered its identity to its velocity.

*A thing that points where it moves has surrendered its identity to its velocity. Heading is not chosen; it is inherited from motion.*

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [2, 1, 1] (standard)
- **Interaction:** To show that direction of motion IS orientation — the cone's nose traces its velocity vector, making heading a consequence of movement, not a choice.
- **Critical Parameter:** thrust_magnitude (0.01-0.15) — controls how quickly the mover changes direction. Low thrust = gentle curves. High thrust = sharp turns.
- **What Emerges:** The cone's orientation lagging behind direction changes, showing inertia. Smooth curves from continuous thrust. The mover tracing invisible Lissajous figures.
- **Triggers:** Automatic — sinusoidal thrust direction drives the mover in Lissajous-like 3D curves. VR thrust slider → adjusts responsiveness. Mover wraps at boundaries.

## Architecture

| | |
|---|---|
| **File** | `algorithms/oscillation/noc_ch03/example_3_3_pointing_in_the_direction_of_motion_vr.gd` (174 lines) |
| **Scene** | `res://algorithms/oscillation/noc_ch03/example_3_3_pointing_in_the_direction_of_motion_vr.tscn` |
| **Registry** | `physics_simulation.json` |
| **Class** | `PointingInDirectionOfMotionVR` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `thrust_magnitude` | float | 0.05 |

### Dependencies

- `res://commons/artifacts/ArtifactScenePresenter.gd`
- `res://spatial_ui/parameter_controller_3d.tscn`
- `res://commons/resourses/materials/noc_vr/noc_vr_pink_primary.tres`

### Key Methods

- `_ready()`
- `_setup_environment()`
- `_spawn_mover()`
- `_process(delta: float)`
- `_apply_standard_presentation()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: ForcesSystems

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Forces_6 | [4,4] | `-` |
| ForcesSystems | [13,18] | `-` |

### Relationships

- Companion to example_3_2 (arbitrary angular motion vs velocity-aligned orientation). Used by vector_drone (enemy AI points toward player). Core concept for steering behaviors.
- Needs: VR thrust parameter controller [has], cone visual pointing along velocity [has]. Missing: trail visualization, multiple movers.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
