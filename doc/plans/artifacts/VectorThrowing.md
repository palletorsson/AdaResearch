# Artifact: VectorThrowing

> p(t) = p0 + v0*t + 0.5*g*t^2. Projectile motion. Horizontal = constant velocity; vertical = constant acceleration. Your hand writes the initial conditions.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** vector | **Themes:** linear_algebra, math, vectors

VectorThrowing

*A throw is an initial condition. After release, you have no control. Physics decides the rest.*

## Design

- **Visual:** material: unlit, animation: physics
- **Scale:** [1.0, 4.0, 5.0] (environment)
- **Interaction:** To throw. To feel velocity leave your hand, watch gravity bend the arc, and hit something that shatters. Physics as sport.
- **Critical Parameter:** The release velocity from your VR hand. Direction and magnitude together determine the entire trajectory — the parabola is written at release.
- **What Emerges:** Accuracy improving as the learner internalizes the parabola. Leading the drone target. The trajectory preview teaching prediction before the throw.
- **Triggers:** VR grab + release → ball launches with hand velocity, trajectory line predicts path, target hit → score + signal, drone destroyed → points + respawn timer

## Architecture

| | |
|---|---|
| **File** | `algorithms/vectors/08_vector_throwing/VectorThrowing.gd` (425 lines) |
| **Scene** | `res://algorithms/vectors/08_vector_throwing/VectorThrowing.tscn` |
| **Registry** | `vectors.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_balls` | int | 3 |
| `ball_spawn_height` | float | 1.5 |
| `ball_spawn_spacing` | float | 0.3 |
| `target_rows` | int | 3 |
| `target_columns` | int | 4 |
| `target_spacing` | float | 0.6 |
| `target_distance` | float | 3.0 |
| `target_height_offset` | float | 1.0 |
| `enable_drones` | bool | true |
| `drone_count` | int | 1 |
| `drone_spawn_radius` | float | 3.5 |
| `drone_spawn_height` | float | 1.6 |
| `drone_respawn_time` | float | 6.0 |
| `drone_points_value` | int | 40 |
| `drone_base_color` | Color | Color(1.0, 0.45, 0.1, 1.0) |
| `enable_scoring` | bool | true |
| `show_instructions` | bool | true |

### Dependencies

- `res://algorithms/vectors/08_vector_throwing/throw_ball.tscn`
- `res://algorithms/vectors/08_vector_throwing/hit_target_cube.tscn`
- `res://algorithms/vectors/08_vector_throwing/vector_drone.tscn`

### Key Methods

- `_ready()`
- `_setup_trajectory_vis()`
- `_setup_environment()`
- `_spawn_throw_balls()`
- `_spawn_target_grid()`
- `_spawn_drones()`
- `_spawn_drone_at_index(index: int, camera: Node3D = null)`
- `_create_score_display()`
- `_create_instructions()`
- `_create_physics_info_panel()` -- Base class handles pos scale
- `_update_physics_info()`
- `_on_ball_thrown(_pickable: Node3D, ball: Node3D)`
- `_on_target_hit(target: Node3D, impact_velocity: Vector3)`
- `_on_drone_player_hit(_drone: Node3D)`
- `_on_drone_destroyed(drone: Node3D, points_awarded: int)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: VectorAdvanced
- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: ForcesArena

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Forces_Destruct | [1,10] | `-` |
| ForcesArena | [14,20] | `-` |
| VectorAdvanced | [12,17] | `-` |
| VectorThrowing | [3,3] | `-` |

### Relationships

- Culmination of VectorAdvanced. Uses projectile physics from ForceMagnitudeDemo. Drone uses subtraction/normalization from hl_turret_vectors (but as enemy AI).
- Needs: VR throwable balls [has], target grid [has], homing drone [has], trajectory preview [has], score display [has]. Missing: wind force perturbation.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
