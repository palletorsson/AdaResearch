# Artifact: Gradient Descent Landscape

> 3D loss surface with a ball rolling down to the minimum — gradient descent visualization. ImmediateMesh surface colored green at minima, red at peaks. Adjustable learning rate slider and reset button. Trail of breadcrumb spheres shows the optimization path.

## Context

**Category:** machinelearning | **Complexity:** intermediate
**Tags:** gradient_descent, optimization, loss_surface, learning_rate, gradient, machine_learning | **Themes:** ai, education, machine_learning

3D loss surface with a ball rolling down to the minimum — gradient descent visualization. ImmediateMesh surface colored green at minima, red at peaks. Adjustable learning rate slider and reset button. Trail of breadcrumb spheres shows the optimization path.

## Design

- **Visual:** material: vertex_color, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/gradient_descent_landscape/gradient_descent_landscape.gd` (397 lines) |
| **Scene** | `res://commons/artifacts/gradient_descent_landscape/gradient_descent_landscape.tscn` |
| **Registry** | `machinelearning.json` |
| **Class** | `GradientDescentLandscape` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `surface_size` | float | 0.7 |
| `grid_resolution` | int | 32 |
| `learning_rate` | float | 0.08: |
| `step_interval` | float | 0.12 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_loss(x: float, y: float)` -- Local minimum bumps (Gaussian dips)
- `_gradient(x: float, y: float)`
- `_compute_height_range()`
- `_create_surface()`
- `_update_surface()`
- `_height_color(h: float)`
- `_create_ball()`
- `_update_ball_visual()` -- Float above surface
- `_func_to_world(fpos: Vector2)`
- `_create_trail_parent()`
- `_add_trail_point()`
- `_clear_trail()`
- `_create_labels()`
- `_update_info()`

### Grid Config

Accepts: `learning_rate`, `start_x`, `start_y`, `step_interval`

## Curriculum Position

### Sequences

- machinelearning

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
