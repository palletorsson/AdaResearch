# Artifact: Swastika Meander Floor

> a floor carrying the labyrinthine logic of the Greek key meander

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pompeii, meander, greek_key | **Themes:** arrays, art, cultural_computation

Pompeii mosaic floor with swastika meander (Greek key labyrinth) pattern. Continuous dark meander path on light background with terracotta accent squares at cell centers.

*the meander is one of humanity's oldest continuous ornaments*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players trace the continuous path and see it form swastika-like shapes
- **Critical Parameter:** cells_short — number of meander repeat-units on the short axis
- **What Emerges:** the realization that a single L-shaped motif, rotated, fills the plane
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/swastika_meander_floor.gd` (312 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/swastika_meander_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `SwastikaMeanderFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.0, 1.0) |
| `cells_short` | int | 3 |
| `border_widths` | Array[int] | [2, 1, 2] |
| `border_motif` | int | BorderMotifs.Motif.SOLID |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `color_accent` | Color | MosaicPalette.TERRACOTTA |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.02 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`
- `res://commons/artifacts/pompeii_mosaic_floor/border_motifs.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`

### Grid Config

Accepts: `border_widths`, `cells_short`, `floor_size`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- pompeii_mosaic_floor (sibling truchet pattern), mosaic_palette
- Needs: [implemented] procedural mesh, border bands, meander pixel grid

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
