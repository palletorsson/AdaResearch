# Artifact: Static Reference Cube

> Stage 1 of oscillation progression: A motionless cube establishing the baseline for comparison. Without stillness, we cannot perceive motion. The constant against which all oscillation is measured. Position = constant, y = 0.

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** animation, waves | **Themes:** oscillation, physics, waves

Stage 1 of oscillation progression: A motionless cube establishing the baseline for comparison. Without stillness, we cannot perceive motion. The constant against which all oscillation is measured. Position = constant, y = 0.

## Design

- **Visual:** material: shader, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/static_reference_cube/static_reference_cube.gd` (124 lines) |
| **Scene** | `res://commons/artifacts/static_reference_cube/static_reference_cube.tscn` |
| **Registry** | `wavefunctions.json` |
| **Class** | `StaticReferenceCube` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cube_size` | float | 0.3 |
| `cube_color` | Color | Color(0.3, 0.5, 0.8) |
| `show_ground_marker` | bool | true |
| `show_label` | bool | true |

### Dependencies

- `res://commons/primitives/cubes/cube_scene.tscn`

### Key Methods

- `_ready()`
- `_create_cube()`
- `_create_ground_marker()`
- `_create_label()`
- `_apply_cube_color()`
- `get_position_value()`
- `get_cube_instance()`
- `get_cube_mesh()`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config


## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
