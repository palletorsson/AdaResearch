# Artifact: GÃ¶del Statement Plaque

> G: ¬∃p: Proves(p, G) — a sentence that asserts its own unprovability

## Context

**Category:** philosophy | **Complexity:** advanced
**Tags:** godel, self_reference, paradox, incompleteness, interactive | **Themes:** philosophy, qfep

Self-referential plaque cycling through increasingly paradoxical statements, culminating in Gödel's incompleteness

*every sufficiently powerful formal system contains true statements it cannot prove — completeness and consistency are mutually exclusive*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [2, 2, 1] (compact)
- **Interaction:** cycle through nine statements of increasing self-reference, feel the paradox tighten
- **Critical Parameter:** current_index — at index 4+ the plaque pulses gold, the paradox is live
- **What Emerges:** the vertigo of self-reference — each statement is more dangerous than the last
- **Triggers:** click/grab advances to next statement; index >= 4 fires paradox_triggered signal; glow intensifies on hover

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/foundations/godel_statement_plaque.gd` (239 lines) |
| **Scene** | `res://commons/interfaces/foundations/godel_statement_plaque.tscn` |
| **Registry** | `foundations.json` |
| **Class** | `GodelStatementPlaque` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `current_index` | int | 0 |
| `width` | float | 0.6 |
| `height` | float | 0.4 |
| `depth` | float | 0.05 |
| `text_color` | Color | Color(0.9, 0.85, 0.7) |
| `plaque_color` | Color | Color(0.15, 0.12, 0.1) |
| `glow_color` | Color | Color(1.0, 0.9, 0.5) |
| `grabbable` | bool | true |

### Key Methods

- `_ready()`
- `_create_plaque()`
- `_create_text()` -- Main statement
- `_create_glow()`
- `_create_interactable()`
- `_on_input_event(_camera: Node, event: InputEvent, _position: Vector3, _normal: Vector3, _shape_idx: int)`
- `_on_hover_start()`
- `_on_hover_end()`
- `_process(delta: float)`
- `_update_display()`
- `advance_statement()`
- `previous_statement()`
- `set_statement(index: int)`
- `get_current_statement()`
- `_on_grabbed()`

### Signals

- `statement_changed`
- `paradox_triggered`

## Curriculum Position

### Sequences

- **Foundations Crisis: The Limits of Formalization** (foundationscrisis) -- map: Godel_Incompleteness
- **Foundations Crisis: The Limits of Formalization** (foundationscrisis) -- map: Crisis_Synthesis

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Crisis_Synthesis | [3,4] | `:0:0.8` |
| Godel_Incompleteness | [9,3] | `:0:1.2:1` |

### Relationships

- unlocks escher_staircase (visual Godel); contrasts russell_set_box (set-theoretic vs arithmetic self-reference); depends on excluded_middle_demo
- Needs: VR area interaction [has], mouse click [has], XR grab [has conceptual]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
