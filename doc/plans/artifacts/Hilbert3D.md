# Artifact: 3D Hilbert Curve

> A → -BF+AFA+FB-, B → +AF-BFB-FA+ in 3D with pitch/roll — a single path visits every point in a cube

## Context

**Category:** procedural | **Complexity:** advanced
**Tags:** 3d, procedural | **Themes:** grammar, l_systems, procedural

Space-filling curve visualization demonstrating efficient 3D traversal

*A grammar can visit every point in space without crossing itself — infinity threaded through a cube.*

## Design

- **Scale:** [6, 8, 6] (xlarge)
- **Interaction:** To fill space with a single unbroken line — watch a path that never crosses itself touch every corner of a cube
- **Critical Parameter:** generations — each increment multiplies path length by 8, from simple fold to space-saturating curve
- **What Emerges:** A continuous path through 3D space that preserves locality — nearby points on the path are nearby in space
- **Triggers:** Generation 1 → simple bent path; generation 3 → dense space-filling weave; animated drawing → reveals the path order

## Architecture

| | |
|---|---|
| **File** | `algorithms/lsystems/Hilbert3D.gd` (369 lines) |
| **Scene** | `res://algorithms/lsystems/Hilbert3D.tscn` |
| **Registry** | `lsystems.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `generations` | int | 2 |
| `step_length` | float | 2.5 |
| `turn_angle` | float | 90.0 |
| `show_generation_animation` | bool | false |
| `curve_thickness` | float | 0.08 |
| `animate_drawing` | bool | true |
| `drawing_speed` | float | 20.0 |
| `ball_size` | float | 0.15 |

### Dependencies

- `res://utils/lsystem.gd`

### Key Methods

- `_ready()` -- Create turtle
- `_process(delta: float)` -- Handle generation animation
- `create_info_labels()`
- `create_controllers()`
- `_on_generation_changed(new_gen: float)`
- `update_info_label()`
- `count_forward_moves(instructions: String)`
- `draw_lsystem()`
- `increase_generations()` -- Limit to prevent excessive complexity
- `decrease_generations()`
- `reset()`
- `toggle_animation()`
- `compute_segments()`
- `start_animation()`
- `clear_animated_objects()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **L-Systems: Rules Grow Structure** (lsystems) -- map: LSystems_Grammars_And_Curves

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_CrossSequence | [4,5] | `-` |
| LSystems_Grammars_And_Curves | [3,3] | `-` |
| LSystems_Hilbert3D | [3,4] | `-` |

### Relationships

- Feeds into LSystems_Grammars_And_Curves. Pairs with space_filling_curve_gallery (3D vs 2D gallery).
- Needs: VR generation controller [has], Label3D [has], animation toggle [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
