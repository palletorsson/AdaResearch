# Artifact: queer_marching_cave

> Extended cave system — long procedural cave with queer spatial topology themes.

## Context

**Category:** proceduralgeneration | **Complexity:** expert
**Tags:** marching_cubes, cave, long, queer, cpu, working | **Themes:** 3d, isosurfaces, math

Extended cave system — long procedural cave with queer spatial topology themes.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [31, 21, 31] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/spacetopology/marchingcubes/longcave/QueerMarchingCave.gd` (464 lines) |
| **Scene** | `res://algorithms/spacetopology/marchingcubes/longcave/QueerMarchingCave.tscn` |
| **Registry** | `isosurfaces.json` |
| **Class** | `QueerMarchingCave` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cave_size` | Vector3 | Vector3(10, 6, 10) |
| `resolution` | float | 0.5 |
| `iso_level` | float | 0.0 |
| `primary_noise_scale` | float | 0.15 |
| `secondary_noise_scale` | float | 0.4 |
| `bulginess` | float | 1.5 |
| `cave_density` | float | 0.3 |
| `vertical_bias` | float | 0.2 |
| `color_shift_speed` | float | 0.5 |
| `pulse_intensity` | float | 0.3 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `setup_noise_generators()` -- Primary cave structure noise
- `setup_triangle_table()`
- `generate_cave()`
- `clear_previous_data()`
- `generate_density_field()` -- Create 3D density field for marching cubes
- `calculate_density_at_position(pos: Vector3)` -- Primary cave structure
- `generate_mesh_marching_cubes()`
- `process_cube(x: int, y: int, z: int, size_x: int, size_y: int, size_z: int)`
- `interpolate_edge(positions: Array, densities: Array, edge_index: int)` -- Edge vertex mapping
- `world_position_from_grid(grid_pos: Vector3, offset_x: int, offset_y: int, offset_z: int)`
- `add_triangle(v1: Vector3, v2: Vector3, v3: Vector3)`
- `generate_queer_color(pos: Vector3)` -- Base queer color palette
- `create_mesh()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Isosurfaces: Implicit to Explicit** (isosurfaces) -- map: ISO_Caves

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ISO_Caves | [6,6] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
