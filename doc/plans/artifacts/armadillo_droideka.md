# Artifact: Armadillo Droideka

> sphere_roll(t) -> unfold(t) -> attack(t) -- armadillo-inspired droideka rolls compressed, deploys to strike

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** hazard, physics | **Themes:** gameplay, hazards

Foldable machine-creature enemy with rigid scutes, split equator rings, deployable legs, and fire-bolt burst attacks.

*compression and deployment are the same transformation in opposite directions -- the body is its own weapon.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a segmented sphere that trundles toward you as a ball, then blooms into an armored attacker
- **Critical Parameter:** deployment state -- ROLLED vs DEPLOYED determines geometry, speed, and attack capability
- **What Emerges:** the compressed form IS the locomotion -- rolling geometry becomes attack geometry through unfolding
- **Triggers:** player proximity triggers deployment; distance triggers re-rolling; health-based personality progression

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/armadillo_droideka/armadillo_droideka.gd` (689 lines) |
| **Scene** | `res://commons/hazards/armadillo_droideka/armadillo_droideka.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `ArmadilloDroideka` extends `CharacterBody3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_health` | float | 80.0 |
| `roll_speed` | float | 2.8 |
| `strafe_speed` | float | 1.7 |
| `turn_speed` | float | 6.0 |
| `detection_radius` | float | 10.0 |
| `disengage_radius` | float | 16.0 |
| `can_deploy_fire` | bool | true |
| `shots_per_burst` | int | 3 |
| `fire_interval` | float | 0.35 |
| `fire_speed` | float | 14.0 |
| `fire_damage` | float | 14.0 |
| `biped_distance_threshold` | float | 6.0 |
| `contact_damage` | float | 10.0 |
| `contact_damage_cooldown` | float | 0.65 |
| `detect_hold_time` | float | 0.35 |
| `deploy_duration` | float | 0.75 |
| `aim_duration` | float | 0.45 |
| `retract_duration` | float | 0.65 |
| `dead_cleanup_time` | float | 4.0 |
| `scute_count` | int | 12 |
| `shell_radius` | float | 0.62 |
| `shell_height` | float | 1.02 |
| `shell_thickness` | float | 0.075 |
| `scute_open_degrees` | float | 70.0 |
| `scute_wave_degrees` | float | 7.0 |
| `ring_open_degrees` | float | 52.0 |
| `core_raise_height` | float | 0.52 |
| `leg_upper_length` | float | 0.45 |
| `leg_lower_length` | float | 0.38 |
| `leg_folded_hip_degrees` | float | -116.0 |
| `leg_deployed_hip_degrees` | float | 34.0 |
| `leg_folded_knee_degrees` | float | 132.0 |
| `leg_deployed_knee_degrees` | float | -26.0 |

### Dependencies

- `res://commons/hazards/armadillo_droideka/fire_bolt.tscn`

### Key Methods

- `_ready()`
- `_physics_process(delta: float)`
- `_process_roll_state(delta: float)`
- `_process_detect_state(delta: float)`
- `_process_deploy_state(_delta: float)`
- `_process_aim_state(delta: float)`
- `_process_fire_state(delta: float)`
- `_process_retract_state(_delta: float)`
- `_process_dead_state(delta: float)`
- `_update_fire_movement(delta: float)`
- `_handle_contact_damage()`
- `_try_apply_contact_damage(target: Object)`
- `_spawn_fire_bolt(direction: Vector3)`
- `_get_fire_direction()`
- `_aim_core_at_player(blend: float)`

### Signals

- `fired_projectile`
- `enemy_destroyed`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo | [7,1] | `-` |

### Relationships

- parent class for origami_droideka and waterbomb_enemy; contrasts with sphere_droideka
- Needs: segment mesh generation [has]; roll physics [has]; deploy animation [has]; personality system [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
