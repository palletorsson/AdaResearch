# Artifact: Shape Learner (2D)

> Genetic algorithm + cellular automaton hybrid learning target shapes in 2D. Evolution discovers the CA rules that produce desired patterns — emergence guided by selection.

## Context

**Category:** machinelearning | **Complexity:** advanced
**Tags:** genetic_algorithm, cellular_automaton, shape, evolution, 2d, hybrid | **Themes:** ai, biology, education, machine_learning

Genetic algorithm + cellular automaton hybrid learning target shapes in 2D. Evolution discovers the CA rules that produce desired patterns — emergence guided by selection.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/ga_ca_shape_learner/ShapeLearner.gd` (283 lines) |
| **Scene** | `res://algorithms/machinelearning/ga_ca_shape_learner/ShapeLearner.tscn` |
| **Registry** | `machinelearning.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | int | 40 |
| `population_size` | int | 100 |
| `mutation_rate` | float | 0.01 |
| `generations` | int | 100 |

### Key Methods

- `_ready()` -- Calculate cell size so grid fits within 0.8m
- `_start_ga()`
- `_exit_tree()`
- `_setup_multimesh()`
- `_setup_timer()`
- `_setup_label()`
- `run_ga_loop()`
- `evaluate_population_async()`
- `display_ca_sequence(grids: Array)`
- `create_target_shape()`
- `initialize_population()`
- `run_ca(rules: Array)`
- `count_neighbors(grid: Array, x: int, y: int)`
- `calculate_fitness(grid: Array)`
- `select_new_population()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
