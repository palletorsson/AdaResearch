# Artifact: Maze Spinner

> DFS(grid) -> carve(corridors) -> spin(core) -- maze regenerating itself every 8 seconds

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** maze, procedural, dfs, generation, walls | **Themes:** gameplay, hazards

Crystalline core that procedurally generates rotating maze walls. DFS-based maze generation with new random seed every 8 seconds. Navigate the gaps or take wall damage. The algorithm IS the enemy.

*the maze generates itself -- DFS carving corridors that are always new, always temporary, always dangerous.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a crystalline core surrounded by DFS-carved maze that rebuilds constantly
- **Critical Parameter:** _maze_grid / regeneration timer -- DFS carving determines corridors; 8-second rebuild
- **What Emerges:** procedural generation as perpetual threat -- never the same maze, never memorizable
- **Triggers:** timer triggers regeneration; DFS carves new corridors; core spins; walls damage on contact

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/maze_spinner/maze_spinner.gd` (366 lines) |
| **Scene** | `res://commons/hazards/maze_spinner/maze_spinner.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `MazeSpinner` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `maze_size` | int | 5 |
| `cell_size` | float | 0.2 |
| `wall_height` | float | 0.15 |
| `wall_thickness` | float | 0.02 |
| `base_spin_speed` | float | 0.3 |
| `chase_spin_speed` | float | 1.2 |
| `regen_interval` | float | 8.0 |
| `chase_wall_extend` | float | 0.05 |
| `wall_damage` | float | 8.0 |

### Key Methods

- `_on_ready()`
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()`
- `_generate_maze()` -- Clear existing walls
- `_get_unvisited_neighbors(cell: Vector2i)`
- `_create_wall_mesh(row: int, col_idx: int, side: String, half_span: float)`
- `_create_boundary_wall(row: int, col_idx: int, side: String, half_span: float)`
- `_process_visual(delta: float)` -- Spin the maze
- `_on_damaged(_amount: float)` -- Speed up rotation temporarily
- `_on_state_changed(new_state: BaseState)` -- Regenerate sooner

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_6

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_6 | [3,5] | `-` |

### Relationships

- embodies proceduralgeneration sequence; pairs with constraint_web (generation vs satisfaction)
- Needs: HazardCreatureBase [has]; DFS maze generation [has]; wall mesh [has]; core viz [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
