# Artifact: VectorAddition

> Two force arrows, one ball, one truth: nature does not apply forces one at a time. Drag F1 and F2 independently — the yellow net force arrow updates as their vector sum, and the ball accelerates along it. Cancellation when forces oppose. Diagonal trajectories from perpendicular forces. Superposition as felt experience.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** vector_addition, superposition, forces, net_force, interactive, vectors, physics | **Themes:** linear_algebra, math, vectors

Two force arrows, one ball, one truth: nature does not apply forces one at a time. Drag F1 and F2 independently — the yellow net force arrow updates as their vector sum, and the ball accelerates along it. Cancellation when forces oppose. Diagonal trajectories from perpendicular forces. Superposition as felt experience.

## Design

- **Visual:** material: unlit, animation: particle_lifecycle
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/vectors/02_vector_addition/VectorAddition.gd` (197 lines) |
| **Scene** | `res://algorithms/vectors/02_vector_addition/VectorAddition.tscn` |
| **Registry** | `vectors.json` |
| **Pattern** | procedural |

### Dependencies

- `res://algorithms/vectors/shared/gadgets/piston_gadget.gd`

### Key Methods

- `_ready()` -- Half-size for exhibition display
- `_process(delta: float)`
- `_update_info(a: Vector3, b: Vector3, result: Vector3)`
- `_create_dotted_line_multimesh(color: Color = Color(0.7, 0.7, 0.7, 0.5)`
- `_update_dotted_lines(a: Vector3, b: Vector3, result: Vector3)` -- Dotted line from tip of a to result (showing b)
- `_update_single_dotted_line_multimesh(mmi: MultiMeshInstance3D, start: Vector3, end: Vector3)`
- `_create_floating_label(text: String, color: Color)`
- `_cache_vector_nodes(arrow: Node3D, cache_dict: Dictionary)`
- `_get_vector_fast(arrow: Node3D, cache_dict: Dictionary)`
- `_update_vector_fast(_arrow: Node3D, vector: Vector3, cache_dict: Dictionary)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- forces
- transformation

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ForcesArena | [26,4] | `:0:-0.2` |
| Vector_Exhibition | [1,4] | `:0:-0.2` |
| VectorAddition | [1,2] | `-` |
| VectorFoundations | [1,16] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
