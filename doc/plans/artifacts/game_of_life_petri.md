# Artifact: Game of Life Petri Dish

> B3/S23 — three neighbors birth, two or three survive. All complexity from this.

## Context

**Category:** emergence | **Complexity:** beginner
**Tags:** game_of_life, cellular_automata, petri_dish, emergence, conway | **Themes:** biology, commons, emergence, mixed

Conway's Game of Life presented as a laboratory petri dish — cellular automata as biological experiment. Watch gliders, oscillators, and still lifes emerge from simple birth/death rules in a circular containment vessel.

*Simple rules, applied everywhere simultaneously, produce complexity no one designed.*

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To be seeded and watched. To surprise with gliders, oscillators, guns.
- **Critical Parameter:** The rule itself (B3/S23). The initial pattern. Speed of generations.
- **What Emerges:** Gliders from local rules. Oscillators from symmetry. Guns from precise engineering. Computation from life.
- **Triggers:** GLIDER preset → traveling structure, GUN preset → infinite growth, RANDOM → emergent order from noise

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/game_of_life_petri/game_of_life_petri.gd` (369 lines) |
| **Scene** | `res://commons/artifacts/game_of_life_petri/game_of_life_petri.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `GameOfLifePetri` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `dish_size` | float | 0.8 |
| `grid_size` | int | 64 |
| `generations_per_second` | float | 8.0: |
| `auto_run` | bool | true |
| `alive_color` | Color | Color(0.2, 1.0, 0.4) |
| `dead_color` | Color | Color(0.02, 0.05, 0.02) |
| `dish_color` | Color | Color(0.15, 0.15, 0.18) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_dish()` -- Petri dish base
- `_create_multimesh()`
- `_create_labels()`
- `_create_vr_controls()`
- `_sync_speed_slider()`
- `_on_speed_slider_moved(_position)`
- `_on_pattern_button(idx: int)` -- Glider
- `_init_grid()`
- `_clear_grid()`
- `_randomize_grid()`
- `_spawn_pattern(pattern_name: String, cx: int, cy: int)`
- `_count_neighbors(x: int, y: int)`
- `_advance()`
- `_update_display()`

### Grid Config


## Curriculum Position

### Sequences

- **Cellular Automata: Local Rules, Global Patterns** (cellularautomata) -- map: CA_BeyondBinary

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CA_BeyondBinary | [1,3] | `:0:0.5:1` |

### Relationships

- Unlocks cellularautomata sequence. Contrasts with continuous automata (CA_SoftRules). Feeds into Turing completeness.
- Needs: VR speed slider [has], pattern buttons [has], clear/random [has]. Could use: rule editor (B/S variants).

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
