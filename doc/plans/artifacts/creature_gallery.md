# Artifact: Creature Gallery

> DNA organism gallery — 12 preset pedestals in a ring + 5 kingdom clusters. Every organism spawned from CritterDNA at full detail. The proof that the nature system works.

## Context

**Category:** nature_system | **Complexity:** advanced
**Tags:** gallery, creature, dna, showcase, nature_system

DNA organism gallery — 12 preset pedestals in a ring + 5 kingdom clusters. Every organism spawned from CritterDNA at full detail. The proof that the nature system works.

## Design

- **Visual:** material: unlit, animation: transform

## Architecture

| | |
|---|---|
| **File** | `commons/scenes/creature_gallery.gd` (330 lines) |
| **Scene** | `res://commons/scenes/creature_gallery.tscn` |
| **Registry** | `nature_system.json` |
| **Pattern** | procedural |

### Dependencies

- `res://commons/artifacts/critter_placer/critter_placer.gd`

### Key Methods

- `_ready()`
- `_process(delta: float)` -- Slow rotation on preset pedestals
- `_build_title()`
- `_build_floor()`
- `_build_lighting()` -- Ambient
- `_build_preset_ring()`
- `_build_pedestal(pos: Vector3, label_text: String)` -- Cylinder base
- `_build_glow_ring(pos: Vector3, radius: float, color: Color)`
- `_build_kingdom_clusters()`
- `_load_preset_dna(preset_name: String)` -- Use CritterPlacer's preset system

## Curriculum Position

### Sequences

- pokemonstudio
- hazards
- testmaps

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Creature_Gallery | [3,3] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
