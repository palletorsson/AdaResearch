# Artifact: Index Sentinel

> fire(grid[i][j]) -> traverse(row_major) -- walking 4x4 grid firing cells by array index

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** array, indexing, grid, tutorial | **Themes:** biology, education, gameplay, hazards

A walking 4x4 grid that fires cells by [i][j] index, teaching array traversal through attack patterns. Row-major order, visual telegraphing of the next index.

*the array attacks in row-major order -- i and j are not abstract, they are targeting coordinates.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a sentinel attacking in row-major order, each cell lighting before firing -- traversal as combat
- **Critical Parameter:** _current_row / _current_col -- which cell fires next; row-major makes pattern learnable
- **What Emerges:** array indexing as attack pattern -- teaches [i][j] access by making each cell lethal
- **Triggers:** fire_timer advances through [i][j]; cell lights, fires, resets; wraps at grid boundary

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/index_sentinel/index_sentinel.gd` (232 lines) |
| **Scene** | `res://commons/hazards/index_sentinel/index_sentinel.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `IndexSentinel` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_rows` | int | 4 |
| `grid_cols` | int | 4 |
| `cell_size` | float | 0.08 |
| `cell_gap` | float | 0.02 |
| `fire_interval` | float | 1.5 |
| `projectile_speed` | float | 6.0 |
| `body_color` | Color | Color(0.2, 0.25, 0.35) |
| `cell_color` | Color | Color(0.3, 0.8, 0.4) |
| `active_color` | Color | Color(1.0, 0.2, 0.1) |
| `emission_color` | Color | Color(0.3, 0.9, 0.5) |

### Key Methods

- `_on_ready()`
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()` -- Body slab
- `_process_visual(delta: float)` -- Walk animation
- `_highlight_current_cell()` -- Reset all cells
- `_fire_projectile()`
- `_advance_index()`

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_4

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_4 | [7,1] | `-` |

### Relationships

- embodies array_tutorial sequence; pairs with data_tree_walker (linear vs hierarchical)
- Needs: HazardCreatureBase [has]; 4x4 cell grid [has]; row-major traversal [has]; cell highlight [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
