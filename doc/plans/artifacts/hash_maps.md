# Artifact: Hash Maps

> Hash table visualization with buckets, collision chains, and hash function mapping. Watch keys distribute across buckets and see why a good hash function means O(1) lookup while a bad one degrades to O(n).

## Context

**Category:** datastructures | **Complexity:** intermediate
**Tags:** hash_map, hash_table, hashing, collision, bucket, data_structure | **Themes:** algorithms, data_structures

Hash table visualization with buckets, collision chains, and hash function mapping. Watch keys distribute across buckets and see why a good hash function means O(1) lookup while a bad one degrades to O(n).

## Design

- **Visual:** material: vertex_color, animation: vertex
- **Scale:** [17, 16, 11] (world_scale)

## Architecture

| | |
|---|---|
| **File** | [No dedicated GDScript found] |
| **Scene** | `res://algorithms/datastructures/hash_maps/hash_maps.tscn` |
| **Registry** | `datastructures.json` |
| **Pattern** | unknown |

## Curriculum Position

### Sequences

- datastructures

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
