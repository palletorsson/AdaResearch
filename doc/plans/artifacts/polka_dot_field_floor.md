# Artifact: Polka Dot Field Floor

> a floor that maps the Roman practice of scattering light stones on dark ground

## Context

**Category:** primitives | **Complexity:** beginner
**Tags:** 2d, pattern, mosaic, floor, pompeii, dots | **Themes:** arrays, art, cultural_computation

Pompeii mosaic floor with dark field and scattered small light tesserae in a regular grid with organic jitter. Common in vestibules and corridors, bordered by a meander pattern.

*the simplest pattern is still a pattern*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see the simplest possible pattern — dots on a field
- **Critical Parameter:** dot_spacing — tesserae per grid cell side
- **What Emerges:** the insight that randomness constrained to a grid produces organic regularity
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/polka_dot_field_floor.gd` (252 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/polka_dot_field_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `PolkaDotFieldFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.0, 0.75) |
| `tiles_short` | int | 8 |
| `border_width` | int | 2 |
| `border_motif` | int | BorderMotifs.Motif.MEANDER |
| `dot_spacing` | int | 3 |
| `dot_size_fraction` | float | 0.125 |
| `jitter_amount` | float | 0.15 |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.06 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`
- `res://commons/artifacts/pompeii_mosaic_floor/border_motifs.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`
- `_hash_2d(x: int, y: int)`

### Grid Config

Accepts: `border_width`, `dot_size_fraction`, `dot_spacing`, `floor_size`, `jitter_amount`, `tiles_short`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- pompeii_mosaic_floor (sibling pattern), pattern_maker_station (web editor)
- Needs: [implemented] procedural mesh, border bands, dot field, grout

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
