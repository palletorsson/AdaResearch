# Artifact: hilbert_hotel

> Animated Hilbert's Hotel paradox - infinite rooms all occupied, yet a new guest always fits by shifting n to n+1

## Context

**Category:** philosophy | **Complexity:** intermediate
**Tags:** hilbert, infinity, paradox, set_theory, cantor, animated | **Themes:** philosophy, set_theory

Animated Hilbert's Hotel paradox - infinite rooms all occupied, yet a new guest always fits by shifting n to n+1

## Design

- **Visual:** material: standard, animation: transform

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/hilbert_hotel/hilbert_hotel.gd` (353 lines) |
| **Scene** | `res://commons/artifacts/hilbert_hotel/hilbert_hotel.tscn` |
| **Registry** | `foundations.json` |
| **Class** | `HilbertHotel` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_rooms` | int | 10 |
| `room_width` | float | 0.06 |
| `room_height` | float | 0.08 |
| `room_depth` | float | 0.05 |
| `room_gap` | float | 0.015 |
| `guest_radius` | float | 0.015 |
| `shift_duration` | float | 0.8 |
| `pause_between_shifts` | float | 1.2 |

### Key Methods

- `_ready()`
- `_build_base()` -- Wooden platform beneath the hotel
- `_build_rooms()`
- `_build_guests()` -- A sphere guest inside each room
- `_build_new_guest()` -- The incoming guest — starts outside the hotel to the left
- `_build_labels()` -- Title
- `_build_infinity_sign()` -- Infinity symbol "..." and ∞ at the right end of the row
- `_process(delta: float)`
- `_set_phase(p: int)`
- `_animate_shift(t: float)` -- Ease function for smooth motion
- `_finalize_shift()` -- Snap guests to shifted positions
- `_animate_new_guest(t: float)`
- `_finalize_new_guest()`
- `_reset_hotel()` -- Return all guests to their original rooms
- `_ease_in_out(t: float)` -- Smooth cubic ease in-out

### Grid Config

Accepts: `num_rooms`, `pause_between_shifts`, `shift_duration`

## Curriculum Position

### Sequences

- foundationscrisis

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
