# Artifact: Chain Link Floor

> a floor carrying the geometry of Roman interlocking circles

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, roman, circles, chain, vesica_piscis, guilloche | **Themes:** arrays, art, cultural_computation

Pompeii mosaic floor with interlocking circles arranged so each passes through its neighbors centers, creating a continuous chain. Classic Roman guilloche derivative. Overlapping areas form pointed vesica piscis shapes filled in terracotta.

*interlocking circles decorated Roman floors from the Republic onward*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see chain links — the earliest topology
- **Critical Parameter:** tiles_short — number of circle rows on the short axis
- **What Emerges:** recognition that overlapping circles create vesica piscis lenses
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/chain_link_floor.gd` (399 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/chain_link_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `ChainLinkFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.2, 1.0) |
| `tiles_short` | int | 6 |
| `border_widths` | Array[int] | [2, 1, 2] |
| `border_motif` | int | BorderMotifs.Motif.GUILLOCHE |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `color_terracotta` | Color | MosaicPalette.TERRACOTTA |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.04 |
| `wear_level` | float | 0.3 |
| `circle_segments` | int | 48 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`
- `res://commons/artifacts/pompeii_mosaic_floor/border_motifs.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`

### Grid Config

Accepts: `border_widths`, `floor_size`, `tiles_short`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- flower_of_life_floor (sibling), pompeii_mosaic_floor (parent pattern)
- Needs: [implemented] procedural mesh, border bands, circle grid with overlap

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
