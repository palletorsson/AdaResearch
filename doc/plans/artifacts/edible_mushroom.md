# Artifact: Edible Mushroom

> eat(mushroom) -> alter_perception() -- pickable mushroom that rewrites sensory state

## Context

**Category:** items | **Complexity:** intermediate
**Tags:** mushroom, pickup, effect, randomness, perception | **Themes:** gameplay, hazards

Pickable mushroom with transformative effects. RAND Corporation 1955 meets fungal spores — historical randomness made edible. Consume to trigger perceptual shifts.

*the mushroom is the same form whether it heals or harms -- context determines outcome.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** a small organic form that dissolves when consumed, triggering perceptual transformation
- **Critical Parameter:** _dissolve_timer -- how quickly it vanishes after eating; effects depend on type
- **What Emerges:** the boundary between pickup and hazard dissolves -- medicine and poison share the same form
- **Triggers:** XRToolsPickable grab -> bring to face -> consume; dissolve animation; perception shift

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/mushroom/edible_mushroom.gd` (263 lines) |
| **Scene** | `res://commons/hazards/mushroom/edible_mushroom.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `EdibleMushroom` extends `XRToolsPickable` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `eat_distance` | float | 0.25 |
| `mushroom_type` | int | 0 |
| `mushroom_scale` | float | 1.0 |
| `cap_color` | Color | Color(0.8, 0.2, 0.2) |
| `stem_color` | Color | Color(0.9, 0.85, 0.75) |
| `glow_color` | Color | Color(0.0, 0.0, 0.0) |
| `glow_energy` | float | 0.8 |
| `effect_shader_path` | String | "res://commons/resourses/shaders/mushroom_trip.gdshader" |
| `effect_duration` | float | 10.0 |

### Key Methods

- `_ready()`
- `_process(delta: float)` -- Dissolve animation
- `_on_picked_up(_pickable)`
- `_on_dropped(_pickable)`
- `_eat()`
- `_find_or_create_effect()` -- Look for existing MushroomEffect in the scene
- `_build_mushroom()`
- `_add_spots()`
- `_setup_collision()` -- Capsule collision for grabbing

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_4

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_4 | [3,1] | `-` |
| Lab_Death | [0,8] | `-` |

### Relationships

- paired with stick_tool and becoming_catalyst as inventory items; Q-FEP dual-nature
- Needs: XRToolsPickable [has]; dissolve animation [has]; mesh scaling [has]; VR grab interaction [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
