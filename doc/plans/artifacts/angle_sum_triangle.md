# Artifact: angle_sum_triangle

> α + β + γ = 180° (Euclidean angle sum theorem)

## Context

**Category:** philosophy | **Complexity:** beginner
**Tags:** euclidean, geometry, angles, triangle, foundations | **Themes:** geometry, philosophy

Demonstrates that triangle angles sum to 180° in Euclidean space — a fact that fails in non-Euclidean geometries

*the angle sum of a triangle is not a fact about triangles but a fact about the space they inhabit*

## Design

- **Visual:** material: unlit, animation: static
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** see the number 180 emerge from three vertices — the axiom made spatial
- **Critical Parameter:** curvature — at K=0 the sum is exactly 180°; any deviation breaks it
- **What Emerges:** the realization that this "obvious" fact depends on the parallel postulate
- **Triggers:** static display; presence alone teaches

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/angle_sum_triangle/angle_sum_triangle.gd` (71 lines) |
| **Scene** | `res://commons/artifacts/angle_sum_triangle/angle_sum_triangle.tscn` |
| **Registry** | `foundations.json` |
| **Class** | `AngleSumTriangle` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `size` | float | 0.4 |

### Key Methods

- `_ready()`
- `_create_triangle()`
- `_create_labels()` -- Equilateral for simplicity
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Foundations Crisis: The Limits of Formalization** (foundationscrisis) -- map: Euclid_Parallel
- **Unused Artifacts Test** (testmaps) -- map: test_batch_1

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Euclid_Parallel | [5,7] | `:0:0.5` |
| test_batch_1 | [2,4] | `-` |

### Relationships

- contrasts hyperbolic_surface (angle sum < 180°) and elliptic_surface (angle sum > 180°); depends on euclid_postulates_plaque
- Needs: VR controls [missing] — could add draggable vertices to explore angle sum changes

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
