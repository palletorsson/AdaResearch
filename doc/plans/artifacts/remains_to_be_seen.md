# Artifact: Remains To Be Seen

> Critical theory artifact exploring visibility, erasure, and what computation makes invisible. What the algorithm chooses not to show is as important as what it reveals.

## Context

**Category:** critical | **Complexity:** intermediate
**Tags:** critical_theory, visibility, erasure, representation, queer_theory | **Themes:** algorithms, art, misc

Critical theory artifact exploring visibility, erasure, and what computation makes invisible. What the algorithm chooses not to show is as important as what it reveals.

## Design

- **Visual:** material: standard, animation: particle_lifecycle

## Architecture

| | |
|---|---|
| **File** | `algorithms/criticaltheory/remainstobeseen/remains_to_be_seen.gd` (398 lines) |
| **Scene** | `res://algorithms/criticaltheory/remainstobeseen/remains_to_be_seen.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()` -- Create a dark environment with appropriate lighting
- `setup_environment()` -- Create environment
- `create_suspended_frames()` -- Parent node for all frames
- `create_random_frame()`
- `create_bed_frame(parent)`
- `create_chair_frame(parent)` -- Chair seat
- `create_table_frame(parent)` -- Table top
- `create_crib_frame(parent)` -- Crib base
- `convert_to_wireframe(mesh_instance)` -- This is a simplified approach to create a wireframe effect
- `add_suspension_wires(frames_parent)` -- Add thin suspension wires that go up to ceiling
- `create_swing_animation(_animation_player, frame)` -- Create animation for subtle swinging motion
- `_process(_delta)` -- Add any global animations or interactions here
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
