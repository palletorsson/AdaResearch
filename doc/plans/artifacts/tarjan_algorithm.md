# Artifact: Tarjan's Algorithm

> single-pass DFS with a stack — each vertex gets discovery_time and low_link; low_link[v] = min(low_link[v], discovery_time[w]) for back edges; when low_link[v] == discovery_time[v], pop the stack to extract an SCC

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** graph_theory, strongly_connected_components, depth_first_search, low_link, directed_graph, topology | **Themes:** algorithms, misc

Follow a single depth-first pass as it discovers strongly connected components. Discovery times, low-link values, and the stack make cycles visible as pockets of mutual reachability that pop out in reverse traversal order. Back edges show exactly where a directed graph folds back on itself, and each component flashes into view when its root condition is met. It teaches SCCs as something you can watch emerge, not just compute.

*a strongly connected component is a pocket of mutual reachability — every vertex can reach every other, and Tarjan's genius was finding them all in a single depth-first pass*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [7.0, 6.0, 7.0] (xlarge)
- **Interaction:** to watch a DFS drill through a directed graph, painting vertices yellow as it visits them, then suddenly flash an entire SCC in color when a root vertex is found
- **Critical Parameter:** edge_density — controls how many SCCs form; sparse graphs have many trivial SCCs (single vertices), dense graphs collapse into fewer large SCCs
- **What Emerges:** the stack-based SCC extraction means you see the components pop out in reverse DFS order — deeper components emerge first, creating a bottom-up revelation of graph structure
- **Triggers:** auto_start begins the non-blocking step-by-step animation; each _process frame advances one DFS step; back edges (colored red) indicate the low_link updates that close cycles

## Architecture

| | |
|---|---|
| **File** | `algorithms/graphtheory/tarjan_algorithm/tarjan_algorithm.gd` (437 lines) |
| **Scene** | `res://algorithms/graphtheory/tarjan_algorithm/tarjan_algorithm.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Class** | `TarjanAlgorithm` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `graph_size` | int | 12 |
| `edge_density` | float | 0.3 |
| `auto_start` | bool | true |
| `animation_speed` | float | 0.3 |
| `show_discovery_time` | bool | true |
| `show_low_link` | bool | true |
| `highlight_current_vertex` | bool | true |
| `vertex_color` | Color | Color(0.4, 0.6, 0.9, 1.0) |
| `visited_color` | Color | Color(0.9, 0.6, 0.2, 1.0) |
| `current_color` | Color | Color(0.9, 0.9, 0.2, 1.0) |
| `scc_color_1` | Color | Color(0.9, 0.2, 0.2, 1.0) |
| `scc_color_2` | Color | Color(0.2, 0.9, 0.2, 1.0) |
| `scc_color_3` | Color | Color(0.2, 0.2, 0.9, 1.0) |
| `scc_color_4` | Color | Color(0.9, 0.2, 0.9, 1.0) |
| `scc_color_5` | Color | Color(0.2, 0.9, 0.9, 1.0) |
| `edge_color` | Color | Color(0.6, 0.6, 0.6, 0.8) |
| `back_edge_color` | Color | Color(0.9, 0.3, 0.3, 1.0) |
| `cross_edge_color` | Color | Color(0.3, 0.9, 0.3, 1.0) |

### Key Methods

- `_ready()`
- `setup_environment()`
- `initialize_graph()`
- `generate_random_graph()` -- Create vertices
- `has_edge(from: String, to: String)`
- `create_visual_elements()`
- `create_edge_visual(edge: Dictionary)`
- `start_algorithm()`
- `_process(delta: float)`
- `process_tarjan_step()` -- If DFS stack is empty, try to start from next unvisited root
- `start_dfs(vertex: String)`
- `get_scc_color(index: int)`
- `update_vertex_color(vertex: String, color: Color)`
- `update_edge_color(from: String, to: String, color: Color)`
- `update_info_text(text: String)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Graph Theory: Connections Define Structure** (graphtheory) -- map: GT_Connectivity

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| GraphTheory_Tarjan_s_Algorithm | [6,5] | `-` |
| GT_Connectivity | [2,2] | `:0:0:0.6` |

### Relationships

- paired with kosaraju_algorithm in GT_Connectivity — both find SCCs but through different strategies (one-pass vs two-pass); contrasts with topological_sort which requires a DAG (no SCCs)
- Needs: slider_horizontal [missing]; push_button [missing]; Label3D [has] (info_label, vertex labels)

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
