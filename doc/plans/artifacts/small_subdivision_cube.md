# Artifact: small_subdivision_cube

> touch(cube) → cube.scale *= 0.5; spawn(cube_left, cube_right); queue_free(self)

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

small_subdivision_cube

*Division is not destruction — it is multiplication, and every touch proves that one contains many.*

## Design

- **Visual:** material: shader, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To be touched and shattered — each touch halves the cube and doubles the population, mitosis in geometry
- **Critical Parameter:** max_divisions — the generation limit; at 3 you get 8 tiny cubes from one, each a different hue
- **What Emerges:** Cubes flying apart with physics impulse and torque — a controlled explosion that looks like cell division
- **Triggers:** VR hand touch or grab → immediate division with impulse; color hue-shifts per generation

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/cubes/subdivision_cube.gd` (204 lines) |
| **Scene** | `res://commons/primitives/cubes/subdivision_cube.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_divisions` | int | 3 |
| `division_color_shift` | float | 0.15 |
| `divide_on_touch` | bool | true |
| `touch_cooldown` | float | 0.5 |
| `impulse_strength` | float | 1.0 |

### Dependencies

- `res://commons/primitives/cubes/subdivision_cube.tscn`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_on_body_entered(body: Node)`
- `pick_up(by: Node3D)`
- `_do_division_from_pickup()` -- Reset has_divided so divide_cube can set it properly
- `divide_cube_delayed()`
- `divide_cube()`
- `create_child_cube(pos: Vector3, new_scale: Vector3, rot: Vector3, division_level: int)`
- `set_division_level(level: int)`
- `update_color_by_division()` -- Wait for scene to be ready

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_RecursiveTrees

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_RecursiveTrees | [3,1] | `-` |
| Fractal_RecursiveTrees | [3,2] | `-` |

### Relationships

- The only interactive fractal primitive — used in Fractal_RecursiveTrees as tactile recursion lesson
- Needs: VR touch [has], grab [has], physics impulse [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
