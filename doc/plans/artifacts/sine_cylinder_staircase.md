# Artifact: sine_cylinder_staircase

> r(theta) = base_radius + sine_mod * sin(mod_freq * theta), y = step_rise * i

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** wave | **Themes:** oscillation, physics, waves

Builds a helical staircase around a central column, modulating tread radius with a sine wave.

*A spiral staircase with sine-modulated radius is a helix that breathes.*

## Design

- **Visual:** material: unlit, animation: static
- **Scale:** [3.0, 9.0, 6.0] (environment)
- **Interaction:** Climb a helical staircase whose radius breathes with sine modulation as you ascend
- **Critical Parameter:** SINE_MODULATION — amplitude of the radial sine wave that makes the staircase breathe
- **What Emerges:** a walkable sine function wrapped into architecture — your body traces the wave by climbing
- **Triggers:** _compute_samples() calculates each step position from modulated polar coordinates

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/sine_cylinder_staircase/sine_cylinder_staircase.gd` (233 lines) |
| **Scene** | `res://algorithms/wavefunctions/sine_cylinder_staircase/sine_cylinder_staircase.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `STEP_COUNT` | int | 80 |
| `TOTAL_TURNS` | float | 2.0 |
| `BASE_RADIUS` | float | 1.6 |
| `WALKWAY_OFFSET` | float | 0.9 |
| `STEP_RISE` | float | 0.10 |
| `STEP_THICKNESS` | float | 0.22 |
| `STEP_WIDTH` | float | 2.0 |
| `MIN_STEP_DEPTH` | float | 0.4 |
| `WAVE_AMPLITUDE` | float | 0.0 |
| `WAVE_FREQUENCY` | float | 2.0 |
| `COLLIDER_HEIGHT_OFFSET` | float | 0.3 |
| `show_planes` | bool | true |
| `show_center_pole` | bool | false |
| `debug` | bool | false |

### Key Methods

- `_ready()`
- `_build()` -- clear
- `_add_plane_bridge(a_root: Node3D, a_span: float, b_root: Node3D, b_span: float)` -- --- compute facing edges in WORLD space (unchanged logic) ---
- `_compute_samples()`
- `_add_center_pole(h: float)`
- `_make_step_mat()`
- `_make_plane_mat()` -- pink, semi-transparent
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: Wavefunctions_Sky_Stairs

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| TestMap_Unused_24 | [1,3] | `:0:2` |
| Wavefunctions_Sky_Stairs | [6,5] | `:0:-2.5` |

### Relationships

- depends on polar-to-Cartesian conversion with sine modulation; contrasts with bernini_columns (staircase vs column spirals); unlocks wave-as-architecture
- Needs: VR locomotion and collision [has], staircase physics [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
