# Artifact: Graph Coloring

> Greedy chromatic number exploration on a Petersen graph — 10 nodes colored step-by-step with the lowest available color not used by any neighbor. The four color theorem made visible.

## Context

**Category:** algorithms | **Complexity:** intermediate
**Tags:** graph_coloring, chromatic_number, greedy, petersen_graph, graph_theory, four_color | **Themes:** algorithms, misc

Greedy chromatic number exploration on a Petersen graph — 10 nodes colored step-by-step with the lowest available color not used by any neighbor. The four color theorem made visible.

## Design

- **Visual:** material: vertex_color, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/graph_coloring/graph_coloring.gd` (300 lines) |
| **Scene** | `res://commons/artifacts/graph_coloring/graph_coloring.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Class** | `GraphColoring` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `node_radius` | float | 0.018 |
| `edge_width` | float | 0.003 |
| `graph_radius_outer` | float | 0.28 |
| `graph_radius_inner` | float | 0.12 |
| `color_step_delay` | float | 0.35 |

### Key Methods

- `_ready()`
- `_build_petersen_graph()` -- 10 nodes: 0-4 outer pentagon, 5-9 inner pentagram
- `_add_edge(a: int, b: int)`
- `_compute_positions()`
- `_create_node_visuals()`
- `_create_edge_visual()`
- `_rebuild_edges()`
- `_create_labels()` -- Title
- `_process(delta: float)`
- `_color_node(idx: int)` -- Greedy: assign lowest color index not used by any neighbor
- `_compute_chromatic_number()`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `color_step_delay`, `graph_radius`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
