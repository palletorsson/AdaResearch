# Artifact: Cave System Generator

> Procedural cave generation using noise-based geometry. Random seeds produce unique underground networks — exploration of how controlled randomness creates believable natural spaces.

## Context

**Category:** randomness | **Complexity:** intermediate
**Tags:** cave, procedural, noise, geometry, generation, randomness | **Themes:** algorithms, misc

Procedural cave generation using noise-based geometry. Random seeds produce unique underground networks — exploration of how controlled randomness creates believable natural spaces.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/proceduralrandomness/geometrybased/cavesystem/cave_system.gd` (488 lines) |
| **Scene** | `res://algorithms/randomness/proceduralrandomness/geometrybased/cavesystem/caveSystem.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cave_size` | Vector3 | Vector3(20, 15, 20) |
| `num_primary_caverns` | int | 5 |
| `tunnel_complexity` | int | 10 |
| `cave_seed` | int | 42 |
| `texture_scale` | float | 0.1 |
| `color_variation` | float | 0.2 |
| `ensure_full_connectivity` | bool | true |
| `extra_tunnels_ratio` | float | 0.3 |

### Key Methods

- `_ready()` -- Seed the random number generator
- `generate_cave_system()` -- Create primary caverns
- `generate_primary_caverns()`
- `deform_cavern(cavern: CSGSphere3D)` -- Add multiple deformation spheres to create irregular shapes
- `connect_caverns(caverns: Array)` -- Build a minimum spanning tree to ensure all caverns are connected
- `create_tunnel(start_cavern: CSGSphere3D, end_cavern: CSGSphere3D)` -- Create a tunnel between two caverns
- `add_tunnel_variations(tunnel: CSGCylinder3D)` -- Add some organic variation to the tunnel
- `add_cave_details()` -- Add some additional details to make the cave more interesting
- `add_stalactites_stalagmites()` -- Number of formations to create
- `generate_cave_texture()` -- Create a procedural texture with noise
- `apply_cave_textures()` -- Generate a unique texture
- `generate_normal_map(base_texture: ImageTexture)` -- Get image from texture
- `apply_material_recursive(node: Node, material: Material)`
- `is_cave_system_connected()`
- `regenerate_cave()` -- Clear existing children

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
