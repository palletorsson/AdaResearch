# Artifact: Dissolving Form

> noise_threshold(world_pos * scale) > dissolve_amount -> discard pixel; edge_glow at boundary

## Context

**Category:** qfep | **Complexity:** intermediate
**Tags:** 3d, qfep | **Themes:** philosophy, qfep, theory

A mesh that appears to dissolve/disintegrate. High λ territory.

*form is not permanent — every structure carries its own dissolution encoded in the noise of its material*

## Design

- **Visual:** material: shader, animation: transform
- **Interaction:** watch a solid form erode into nothing — and then watch it return
- **Critical Parameter:** dissolve_amount (0=solid, 1=gone) — the threshold that eats the mesh
- **What Emerges:** the glowing edge where solid meets void — a moving frontier of destruction/creation
- **Triggers:** auto_cycle oscillates dissolve via sin(t); dissolve()/reform() methods for scripted transitions

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/dissolving_form.gd` (159 lines) |
| **Scene** | `res://commons/interfaces/qfep/dissolving_form.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `QFEPDissolvingForm` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `mesh_type` | int | 0 |
| `mesh_size` | float | 0.3 |
| `dissolve_speed` | float | 0.2 |
| `auto_cycle` | bool | true |
| `cycle_duration` | float | 5.0 |
| `solid_color` | Color | Color(0.4, 0.5, 0.9, 1.0) |
| `dissolve_color` | Color | Color(0.9, 0.3, 0.1, 1.0) |

### Key Methods

- `_ready()`
- `_build_mesh()`
- `_process(delta: float)`
- `set_dissolve(amount: float)`
- `dissolve()`
- `reform()`

## Curriculum Position

### Sequences

- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Lambda_Spectrum

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| QFEP_Lambda_Spectrum | [12,2] | `:0:0.5` |

### Relationships

- contrasts rigid_sculpture (permanence vs impermanence); paired with chaos_particles; depends on noise functions
- Needs: VR dissolve slider [missing], mesh type selector [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
