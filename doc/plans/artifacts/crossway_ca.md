# Artifact: Crossway CA

> per_row_rule[y] = random(0..255); cell = wolfram(rule[y], neighbors)

## Context

**Category:** emergence | **Complexity:** intermediate
**Tags:** cellular_automata, emergence | **Themes:** biology, cellular_automata, computation, emergence

Crossway Cellular Automata.

*When every row speaks a different rule language, the boundaries become the most interesting places.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** To be a crossroads of rules — each row obeys a different Wolfram number, creating layered conflict
- **Critical Parameter:** rule_change_interval — how often rules re-randomize, controlling stability vs perpetual novelty
- **What Emerges:** Interference patterns where different rule regimes collide at row boundaries — moiré-like zones of order
- **Triggers:** Every 10 turns all rules re-randomize → the entire pattern language shifts; collision toggles walkability

## Architecture

| | |
|---|---|
| **File** | `algorithms/cellularautomata/crossway_ca/CrosswayCA.gd` (132 lines) |
| **Scene** | `res://algorithms/cellularautomata/crossway_ca/CrosswayCA.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | procedural |

### Dependencies

- `res://commons/primitives/cubes/cube_scene.tscn`

### Key Methods

- `_ready()` -- Initialize the grid
- `_process(delta: float)`
- `update_bridge()`
- `animate_cells(_delta)`
- `count_neighbors(x, y)`
- `initialize_rules()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Cellular Automata: Local Rules, Global Patterns** (cellularautomata) -- map: CA_ExpandingSpace

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CA_ExpandingSpace | [7,2] | `-` |

### Relationships

- Extends ca_bridge (single rule → many rules). Feeds into CA_ExpandingSpace.
- Needs: VR rule display [missing], speed control [missing], collision [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
