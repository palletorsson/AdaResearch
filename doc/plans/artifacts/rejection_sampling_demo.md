# Artifact: Rejection Sampling Demo

> Visual scatter plot of the accept/reject method for sampling arbitrary distributions. 2000 uniform random points plotted against a Gaussian target curve — green dots accepted (below curve), red dots rejected (above). Shows acceptance ratio.

## Context

**Category:** randomness | **Complexity:** beginner
**Tags:** rejection_sampling, accept_reject, gaussian, distribution, monte_carlo, scatter_plot, floor_display | **Themes:** probability, randomness

Visual scatter plot of the accept/reject method for sampling arbitrary distributions. 2000 uniform random points plotted against a Gaussian target curve — green dots accepted (below curve), red dots rejected (above). Shows acceptance ratio.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/rejection_sampling_demo/rejection_sampling_demo.gd` (214 lines) |
| **Scene** | `res://commons/artifacts/rejection_sampling_demo/rejection_sampling_demo.tscn` |
| **Registry** | `randomness.json` |
| **Class** | `RejectionSamplingDemo` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `plot_size` | Vector2 | Vector2(0.8, 0.8) |
| `plot_resolution` | int | 256 |
| `num_samples` | int | 2000 |
| `seed_value` | int | 42 |

### Key Methods

- `_ready()`
- `_build_floor_quad()`
- `_build_labels()`
- `_target_pdf(x: float)`
- `_generate_plot()`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `amplitude`, `num_samples`, `seed`, `sigma`

## Curriculum Position

### Sequences

- randomness

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
