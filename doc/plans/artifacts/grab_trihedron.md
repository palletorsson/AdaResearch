# Artifact: grab_trihedron

> trihedron — 4 vertices, 3 triangular faces and 1 quad base: the wedge as 3D primitive

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

grab_trihedron

*a solid is defined by its topology — how faces connect — not by any particular material or color*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** learner holds a 3D solid, turns it in their hands, and counts faces, edges, vertices
- **Critical Parameter:** button material toggle — pressing reveals the internal structure through a different material
- **What Emerges:** the Euler relation (V-E+F=2) as something you count by holding; topology felt through handling
- **Triggers:** pick up → snaps to shelf; button press → alternates material showing surface vs wireframe quality

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/trihedron/grab_trihedron.gd` (212 lines) |
| **Scene** | `res://commons/primitives/trihedron/grab_trihedron.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `alternate_material` | Material | - |
| `snap_to_shelf` | bool | true |
| `snap_max_distance` | float | 0.08 |
| `snap_match_rotation` | bool | false |
| `snap_falloff_distance` | float | 1.0 |
| `base_color` | Color | Color(0.2, 0.8, 1.0) |
| `trihedron_size` | float | 0.6: |

### Dependencies

- `res://commons/primitives/shared/grid_material_factory.gd`
- `res://commons/primitives/shared/primitive_mesh_builder.gd`

### Key Methods

- `_ready()` -- Call the super
- `_rebuild_trihedron()`
- `_trihedron_geometry()`
- `_create_trihedron_vertices()`
- `_create_trihedron_faces()` -- Trihedron has exactly 3 triangular faces, all sharing the apex vertex
- `_process(_delta: float)`
- `_on_picked_up(_pickable)` -- Listen for button events on the associated controller
- `_on_dropped(_pickable)` -- Unsubscribe to controller button events when dropped
- `_on_controller_button_pressed(button : String)` -- Handle controller button presses
- `_on_controller_button_released(button : String)` -- Handle controller button releases
- `_snap_to_nearest_shelf_point(force: bool = false)`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Primitives_Polythedra

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Primitives_Polythedra | [2,2] | `:90:0:0.1` |

### Relationships

- sibling to grab_octahedron; both teach held polyhedra with button interaction
- Needs: [has VR button (material toggle) [has], has shelf snap [has], missing edge/vertex count label]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
