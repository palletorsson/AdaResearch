# Artifact: Chaos Particles

> GPU_particles(spread=180, velocity=random, angular_velocity=+-720) -> dissolution

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** chaos, compression, qfep | **Themes:** philosophy, qfep, theory

λ=1 dissolution zone. Particles exploding outward, no coherence, pure entropy.

*at lambda=1, information has no structure — dissolution is not destruction but the liberation of possibility*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** feel structure explode outward — every particle fleeing every other
- **Critical Parameter:** speed_max — controls how violently particles escape coherence
- **What Emerges:** brief accidental alignments in the noise — ghosts of pattern in pure entropy
- **Triggers:** burst() creates explosive pulse; set_intensity() scales velocity and count

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/chaos_particles.gd` (129 lines) |
| **Scene** | `res://commons/interfaces/qfep/chaos_particles.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `QFEPChaosParticles` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `particle_count` | int | 300 |
| `emission_radius` | float | 0.3 |
| `speed_min` | float | 1.0 |
| `speed_max` | float | 3.0 |
| `lifetime` | float | 2.0 |
| `dissolution_color` | Color | Color(1.0, 0.3, 0.1, 1.0) |

### Key Methods

- `_ready()`
- `_build_particles()`
- `burst()`
- `set_intensity(value: float)`

## Curriculum Position

### Sequences

- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Lambda_Spectrum

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| QFEP_Lambda_Spectrum | [15,2] | `:0:0.8` |

### Relationships

- contrasts ordered_grid (lambda=0 vs lambda=1); paired with dissolving_form; unlocks understanding of entropy term E(S)
- Needs: VR intensity control [missing], burst trigger [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
