# Artifact: Y-Oscillation Cube

> y(t) = A * sin(2*PI*f*t) — simple harmonic motion along the vertical axis

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** animation, visualization, waves | **Themes:** oscillation, physics, waves

Stage 2 of oscillation progression: Core concept of y = A·sin(ωt). A cube oscillates vertically along a rail, demonstrating how sine function maps time to position. Trail ghosts show motion path, real-time labels display t, sin(t), and y values. The fundamental oscillation.

*y = A*sin(2*pi*f*t) is the simplest sentence in the language of oscillation.*

## Design

- **Visual:** material: shader, animation: transform
- **Interaction:** Watch a cube bounce on a vertical rail, feeling the simplest possible oscillation
- **Critical Parameter:** frequency — controls how fast the cube oscillates up and down
- **What Emerges:** the most elemental wave visualization — one axis, one sine, one cube
- **Triggers:** time drives continuous sine evaluation; amplitude and frequency adjustable

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/y_oscillation_cube/y_oscillation_cube.gd` (295 lines) |
| **Scene** | `res://commons/artifacts/y_oscillation_cube/y_oscillation_cube.tscn` |
| **Registry** | `wavefunctions.json` |
| **Class** | `YOscillationCube` extends `Node3D` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cube_size` | float | 0.15 |
| `amplitude` | float | 0.2 |
| `frequency` | float | 1.0 |
| `cube_color` | Color | Color(0.2, 0.8, 0.5) |
| `show_rail` | bool | true |
| `show_trail` | bool | true |
| `trail_count` | int | 5 |

### Dependencies

- `res://commons/resourses/shaders/Grid.gdshader`

### Key Methods

- `_ready()` -- Debug
- `_create_cube()` -- Create MeshInstance3D with BoxMesh directly
- `_create_rail()`
- `_create_range_marker(color: Color)`
- `_create_labels()` -- Real-time values label
- `_create_trail_ghosts()`
- `_process(delta)`
- `_update_trail(current_y_offset: float)`
- `_update_labels(sin_value: float, y_offset: float)`
- `_update_color_feedback(sin_value: float)`
- `set_amplitude(a: float)` -- Rebuild rail if needed
- `set_frequency(f: float)`
- `get_current_y()`
- `get_cube_instance()`
- `get_sin_value()`

### Grid Config


## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Intro

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Intro | [5,3] | `:180:0.5` |

### Relationships

- depends on basic sine function; contrasts with oscillation_controlled_cube (single vs multi-axis oscillation); unlocks y=sin(t) intuition
- Needs: VR observation [has], amplitude/frequency sliders [missing], ghost trail [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
