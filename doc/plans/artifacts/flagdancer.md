# Artifact: Flag Dancer

> Skeleton3D with per-bone sine wave displacement — each flag_bone gets xform.origin.z = base_wave + wind_noise, creating cloth-like ripple across a SurfaceTool-built pride flag mesh

## Context

**Category:** physics | **Complexity:** advanced
**Tags:** physics | **Themes:** physics, soft_body

Animated soft body flag that waves and dances using cloth physics simulation with wind forces.

*a flag is not a symbol displayed — it is a material surface negotiating wind, and the pride it carries lives in the physics of its motion*

## Design

- **Visual:** material: standard, animation: particle_lifecycle
- **Scale:** [1, 1, 2] (compact)
- **Interaction:** to wave a pride flag in VR that responds to wind_strength and celebration_intensity — a flag you can make dance harder by pressing SPACE
- **Critical Parameter:** wind_strength — controls the amplitude of the sine wave displacement; below 0.5 the flag barely stirs, above 1.5 it whips violently
- **What Emerges:** bone phase offsets create a travelling wave across the flag surface that looks like real wind even though it is just sin() with different offsets per bone
- **Triggers:** celebration_mode toggle (ui_accept) multiplies wave amplitude by 1.5 and adds bone rotation and scale stretching; flag_type enum switches the color stripe palette

## Architecture

| | |
|---|---|
| **File** | `algorithms/physicssimulation/softbodies/flagdancer/flagdancer.gd` (270 lines) |
| **Scene** | `res://algorithms/physicssimulation/softbodies/flagdancer/flagdancer.tscn` |
| **Registry** | `soft_bodies.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `flag_type` | FlagType | FlagType.RAINBOW |
| `wind_strength` | float | 1.0 |
| `celebration_intensity` | float | 1.0 |

### Dependencies

- `res://algorithms/physicssimulation/softbodies/flagdancer/flagdancer.gd`

### Key Methods

- `_ready()`
- `create_pride_flag_with_bones()`
- `setup_joyful_animation()`
- `_process(_delta: float)`
- `_input(event: InputEvent)`
- `create_themed_particles()`
- `create_flag_parade()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Soft Bodies & Morphogenesis: Matter That Finds Its Shape** (softbodies) -- map: SoftBodies_Obsticals

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| SoftBodies_Obsticals | [10,4] | `-` |

### Relationships

- extends cloth_straps from static draping to animated wind; bridges soft body physics to affect theory in SoftBodies_Affect_Theory_Visualization
- Needs: slider_horizontal [missing]; push_button [missing]; Label3D [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
