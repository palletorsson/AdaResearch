# Artifact: Bayesian Inference

> Bayesian inference visualization showing prior, likelihood, and posterior updating.

## Context

**Category:** statistics | **Complexity:** intermediate
**Tags:** bayesian, prior, posterior, inference, statistics | **Themes:** math, probability, statistics

Bayesian inference visualization showing prior, likelihood, and posterior updating.

## Design

- **Visual:** material: unlit, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/statistics/07_bayesian_inference/BayesianInferenceVR.gd` (491 lines) |
| **Scene** | `res://algorithms/statistics/07_bayesian_inference/BayesianInferenceVR.tscn` |
| **Registry** | `statistics.json` |
| **Class** | `BayesianInferenceVR` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `enable_vr` | bool | true |
| `true_coin_bias` | float | 0.7 |
| `prior_alpha` | float | 1.0 |
| `prior_beta` | float | 1.0 |
| `max_observations` | int | 100 |
| `curve_resolution` | int | 200 |
| `show_true_value` | bool | false |
| `animate_updates` | bool | true |

### Key Methods

- `_ready()`
- `setup_vr()`
- `initialize_bayesian_params()`
- `setup_visualization()` -- Prior distribution display
- `create_distribution_curve(parent: Node3D, alpha: float, beta: float, color: Color, label: String)` -- Clear existing curve
- `create_line_mesh(mesh_instance: MeshInstance3D, points: Array[Vector3], color: Color)`
- `create_axes(parent: Node3D)` -- X-axis (probability)
- `create_true_value_line(parent: Node3D, value: float, color: Color)`
- `create_coin_visual()`
- `setup_info_display()`
- `_on_controller_button(button_name: String)`
- `_input(event: InputEvent)`
- `flip_coin()`
- `update_posterior(is_heads: bool)` -- Beta-Binomial conjugate prior
- `animate_coin_flip(is_heads: bool)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
