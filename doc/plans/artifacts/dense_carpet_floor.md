# Artifact: Dense Carpet Floor

> a floor that maps the Roman practice of tessellated textile imitation

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, roman, carpet, cross, textile | **Themes:** arrays, art, cultural_computation

Pompeii mosaic floor imitating woven textile: a dark field covered in hundreds of tiny light crosses arranged in a dense offset grid, creating a carpet/tweed effect.

*stone can weave a carpet if the pattern is dense enough*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see a carpet woven from stone — dense, regular, hypnotic
- **Critical Parameter:** tiles_short — pixel density of the cross grid
- **What Emerges:** the insight that repetition at small scale creates textile-like visual texture
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/dense_carpet_floor.gd` (267 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/dense_carpet_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `DenseCarpetFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.0, 0.75) |
| `tiles_short` | int | 60 |
| `cross_size` | int | 3 |
| `border_dark` | int | 3 |
| `border_light` | int | 2 |
| `border_inner_dark` | int | 1 |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `color_terracotta` | Color | MosaicPalette.TERRACOTTA |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.02 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`
- `_draw_border_ring(verts: PackedVector3Array, add_fn: Callable, inset_val: int, grid_w: int, grid_h: int, tile_s: float)` -- Top row

### Grid Config

Accepts: `cross_size`, `floor_size`, `tiles_short`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- pompeii_mosaic_floor (sibling pattern), polka_dot_field_floor (simpler scatter)
- Needs: [implemented] procedural mesh, border bands, cross pixel grid

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
