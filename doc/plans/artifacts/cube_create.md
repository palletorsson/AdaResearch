# Artifact: Cube Creator

> Tool for creating and manipulating cubes in 3D space

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** 3d | **Themes:** commons, mixed

Tool for creating and manipulating cubes in 3D space

## Design

- **Visual:** material: shader, animation: static
- **Scale:** [2.0, 2.0, 2.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/context/cubecreate/cubecreate.gd` (594 lines) |
| **Scene** | `res://commons/context/cubecreate/cubecreate.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `MeshTutorial` extends `Node3D` |
| **Pattern** | shader_demo |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `setup_scene()` -- Create camera positioned to see all shapes
- `setup_materials()` -- Load simple double-sided grid shader
- `start_tutorial()`
- `create_triangle()`
- `create_mesh_from_vertices(vertices_array: Array, mesh_name: String)`
- `create_star_mesh(vertices_array: Array, mesh_name: String)`
- `create_fish_mesh(vertices_array: Array, mesh_name: String)`
- `advance_tutorial()`
- `create_plane()`
- `create_pyramid()`
- `create_cube()` -- Use BoxMesh for simplicity, but show it's built from vertices
- `create_star()`
- `create_cylinder()`
- `create_sphere()`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
