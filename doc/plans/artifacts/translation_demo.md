# Artifact: Translation Demo

> Two blocking obstacles demonstrating X and Y axis translation. Red obstacle slides horizontally, blue obstacle slides vertically. Obstacles turn green when path is cleared.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

Two blocking obstacles demonstrating X and Y axis translation. Red obstacle slides horizontally, blue obstacle slides vertically. Obstacles turn green when path is cleared.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/translation/translation_demo.gd` (169 lines) |
| **Scene** | `res://commons/primitives/translation/translation_demo.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | vr_interactable |

### Exports

| Name | Type | Default |
|------|------|---------|
| `obstacle_material_blocked` | Color | Color(1.0, 0.3, 0.3, 0.8) |
| `obstacle_material_clear` | Color | Color(0.3, 1.0, 0.3, 0.8) |
| `check_clearance` | bool | true |
| `clearance_threshold` | float | 0.8 |

### Dependencies

- `res://commons/primitives/translation/axis_slider.tscn`

### Key Methods

- `_ready()`
- `_setup_demo()` -- Create two blocking obstacles
- `_create_blocking_obstacle(start_pos: Vector3,
	size: Vector3,
	axis: String,
	slide_min: float,
	slide_max: float,
	color: Color)` -- Instantiate the axis_slider scene (includes XRToolsPickable setup)
- `_create_path_indicator()` -- Create a glowing sphere behind the obstacles as a "goal"
- `_process(_delta: float)`
- `_on_path_cleared()` -- Change obstacle colors to green
- `print_help()`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
