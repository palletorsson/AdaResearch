# Artifact: Control Board

> Black Mesa-style mission control console with interactive CRT monitors, VU meters, push buttons, toggle switches, faders, rotary knobs, speaker and LED indicators. Screens flicker with phosphor glow, needles bounce to simulated signal, LEDs blink with activity.

## Context

**Category:** laboratory | **Complexity:** advanced
**Tags:** lab, control-panel, interactive, vr, monitors, buttons, retro | **Themes:** environment, immersion, retro-tech

Black Mesa-style mission control console with interactive CRT monitors, VU meters, push buttons, toggle switches, faders, rotary knobs, speaker and LED indicators. Screens flicker with phosphor glow, needles bounce to simulated signal, LEDs blink with activity.

## Design

- **Visual:** material: standard, animation: transform

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/control_board/control_board.gd` (493 lines) |
| **Scene** | `res://commons/artifacts/control_board/control_board.tscn` |
| **Registry** | `lab.json` |
| **Class** | `ControlBoard` extends `Node3D` |
| **Pattern** | vr_interactable |

### Exports

| Name | Type | Default |
|------|------|---------|
| `panel_width` | float | 2.4 |
| `panel_height` | float | 1.4 |
| `panel_depth` | float | 0.06 |

### Dependencies

- `res://commons/interactables/slider_horizontal.tscn`
- `res://commons/interactables/push_button.tscn`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_exit_tree()`
- `apply_grid_config(config_data: Dictionary)`
- `_xy(x: float, y: float, z: float = 0.0)` -- positive z = forward (in front of panel)
- `_build_back_plate()`
- `_build_monitors()` -- Large CRT left — grid ~(1,0) to (6,4)
- `_add_crt(pos: Vector3, size: Vector3)` -- Housing bezel
- `_build_meters()`
- `_add_vu_meter(pos: Vector3)`
- `_build_fader_bank()`
- `_on_fader_moved(_pos: Variant, index: int)`
- `_build_button_array()`
- `_on_board_button(index: int)`
- `_build_switch_row()`

### Grid Config

Accepts: `panel_height`, `panel_width`

## Curriculum Position

### Sequences

- **Unused Artifacts Test** (testmaps) -- map: Control_Board_Demo

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Control_Board_Demo | [5,2] | `:0:1.0` |
| Control_Board_Demo | [5,10] | `:0:1.0` |
| Control_Board_Demo | [7,7] | `:0:0.7` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
