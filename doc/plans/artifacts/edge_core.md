# Artifact: Edge Core

> pulsing_sphere(frequency=f(lambda)) + ring_particles(orbit) -> living core at lambda~0.4

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** chaos, graph, qfep | **Themes:** philosophy, qfep, theory

Central orb for edge of chaos. Pulsing, alive, responsive to λ.

*life does not exist at order or chaos but at the narrow boundary between them — the edge is where complexity breathes*

## Design

- **Visual:** material: standard, animation: particle_lifecycle
- **Interaction:** stand before the heart of the edge of chaos and feel it breathe
- **Critical Parameter:** lambda distance from 0.4 — the closer to edge, the more alive the pulse
- **What Emerges:** the core appears to breathe — counter-pulsing inner/outer spheres create an organic heartbeat
- **Triggers:** set_lambda() shifts pulse speed, color (blue->green->red), and amplitude; pulse() creates burst

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/edge_core.gd` (178 lines) |
| **Scene** | `res://commons/interfaces/qfep/edge_core.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `QFEPEdgeCore` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `core_radius` | float | 0.4 |
| `pulse_speed` | float | 1.0 |
| `pulse_amount` | float | 0.1 |
| `rotation_speed` | float | 0.3 |
| `ring_particles` | int | 100 |
| `edge_color` | Color | Color(0.2, 0.9, 0.4, 1.0) |

### Key Methods

- `_ready()`
- `_build_core()` -- Outer translucent sphere
- `_build_inner()` -- Inner bright core
- `_build_ring_particles()`
- `_process(delta: float)`
- `set_lambda(value: float)` -- Color shifts based on how close to edge (0.4)
- `pulse()`

## Curriculum Position

### Sequences

- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Edge_Of_Chaos

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| QFEP_Edge_Of_Chaos | [6,6] | `:0:1.5` |

### Relationships

- central to QFEP_Edge_Of_Chaos map; depends on lambda_slider; contrasts ordered_grid (dead) and chaos_particles (dissolved)
- Needs: VR lambda connection [missing], touch-to-pulse [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
