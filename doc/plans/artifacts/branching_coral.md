# Artifact: Branching Coral

> F → FF[+F][-F][>F][<F] from multiple shoot origins with 3D pitch/roll branching

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** color, l_system, procedural, simulation, swarm | **Themes:** grammar, l_systems, procedural

Marine L-system coral colony — multiple shoots with wide branching angles produce organic fan and staghorn forms. Color gradient from deep purple-brown base to bright pink coral tips on a rock base.

*A coral is not one tree — it is many grammars growing from the same rock.*

## Design

- **Visual:** material: vertex_color, animation: static
- **Interaction:** To grow a coral colony — multiple shoots from a shared base, each branching in all directions with organic randomness
- **Critical Parameter:** num_shoots — the number of independent growth starts that together form the colony's bushy silhouette
- **What Emerges:** Colony morphology from independent shoots sharing a base — the whole is bushier than any individual shoot
- **Triggers:** More shoots → denser colony; higher angle_variation → wilder, more organic forms; fewer iterations → sparse polyps

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/branching_coral/branching_coral.gd` (316 lines) |
| **Scene** | `res://commons/artifacts/branching_coral/branching_coral.tscn` |
| **Registry** | `lsystems.json` |
| **Class** | `BranchingCoral` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `display_size` | float | 0.7 |
| `base_color` | Color | Color(0.35, 0.15, 0.25) |
| `tip_color` | Color | Color(0.95, 0.45, 0.55) |
| `iterations` | int | 4 |
| `base_length` | float | 0.08 |
| `base_angle` | float | 35.0 |
| `length_decay` | float | 0.7 |
| `angle_variation` | float | 12.0 |
| `num_shoots` | int | 5 |

### Key Methods

- `_ready()`
- `_create_display()`
- `_create_rock_base()`
- `_create_label()`
- `_generate()` -- Collect segments from multiple shoot starts
- `_grow_shoot(shoot_idx: int)` -- Rewrite L-system string
- `_draw_coral(segments: Array, max_depth: int)`
- `_update_label()`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `base_angle`, `display_size`, `iterations`, `length_decay`, `num_shoots`, `seed`

## Curriculum Position

### Sequences

- **L-Systems: Rules Grow Structure** (lsystems) -- map: LSystems_Competition
- **L-Systems: Rules Grow Structure** (lsystems) -- map: LSystems_Living

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| LSystems_Competition | [4,6] | `-` |
| LSystems_Living | [6,2] | `-` |

### Relationships

- Appears in LSystems_Competition and LSystems_Living. Contrasts with lsystem_tree (single trunk vs multi-shoot colony).
- Needs: VR shoot/angle/iteration controls [missing], Label3D [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
