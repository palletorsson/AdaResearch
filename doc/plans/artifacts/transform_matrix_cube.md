# Artifact: Transform Matrix Cube

> Interactive 3D cube with its transformation matrix displayed in real-time. Rotate, scale, and translate the cube while watching the 4x4 matrix update — making the abstract algebra of linear transformations concrete and visual.

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** transform, matrix, linear_algebra, rotation, scale, translation | **Themes:** commons, mixed

Interactive 3D cube with its transformation matrix displayed in real-time. Rotate, scale, and translate the cube while watching the 4x4 matrix update — making the abstract algebra of linear transformations concrete and visual.

## Design

- **Visual:** material: unlit, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/transform_matrix_cube/transform_matrix_cube.gd` (441 lines) |
| **Scene** | `res://commons/artifacts/transform_matrix_cube/transform_matrix_cube.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `TransformMatrixCube` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cube_size` | float | 0.4 |
| `matrix` | Basis | Basis.IDENTITY: |
| `color_original` | Color | Color(0.3, 0.3, 0.4, 0.3) |
| `color_transformed` | Color | Color(0.3, 1.0, 0.5, 0.7) |
| `color_i` | Color | Color(1.0, 0.3, 0.3) |
| `color_j` | Color | Color(0.3, 1.0, 0.3) |
| `color_k` | Color | Color(0.3, 0.5, 1.0) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_init_shared_materials()`
- `_create_original_cube()`
- `_create_transformed_cube()` -- Vertex markers via MultiMesh (8 spheres, one draw call)
- `_create_axis_arrows()`
- `_create_arrow(arrow_name: String, color: Color, label_text: String)`
- `_create_labels()`
- `_create_vr_controls()`
- `_apply_matrix(m: Basis)`
- `_add_node(node: Node)`
- `_update_visualization()`
- `_set_edge_transform(idx: int, start: Vector3, end: Vector3)`
- `_update_faces(positions: Array[Vector3])`
- `_update_axis_arrow(arrow: Node3D, direction: Vector3)`
- `_update_labels()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- transformation

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
