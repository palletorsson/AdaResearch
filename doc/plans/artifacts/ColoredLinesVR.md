# Artifact: ColoredLinesVR

> Builds a VR hallway filled with glowing sine-wave tubes rendered from procedural meshes.

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** procedural, vr, wave | **Themes:** oscillation, physics, waves

Builds a VR hallway filled with glowing sine-wave tubes rendered from procedural meshes.

## Design

- **Visual:** material: shader, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/hallway_lines/ColoredLinesVR.gd` (378 lines) |
| **Scene** | `res://algorithms/wavefunctions/hallway_lines/ColoredLinesVR.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `points_per_line` | int | 120 |
| `animation_speed` | float | 0.0 |
| `flow_speed` | float | 1.0 |
| `hallway_width` | float | 12.0 |
| `hallway_length` | float | 60.0 |
| `hallway_height` | float | 8.0 |
| `row_spacing` | float | 6.0 |
| `span_wave_amp` | float | 2.2 |
| `span_twist_amp` | float | 1.1 |
| `span_frequency` | float | 1.5 |
| `line_radius` | float | 0.3 |
| `ring_segments` | int | 16 |
| `arch_mode` | bool | false |
| `arch_base_height` | float | 0.5 |
| `arch_peak_height` | float | 6.0 |

### Key Methods

- `_ready()`
- `_setup_environment()`
- `_create_hallway_box()` -- Walls removed - just open space with the colored lines
- `_make_metal(color: Color)`
- `generate_line_paths()`
- `_create_span_path(z_pos: float, y_mid: float, phase: float)`
- `_create_arch_path(z_pos: float, x_offset: float, phase: float)` -- Create an arch that rises from floor, goes up and over, back to floor
- `create_line_meshes()`
- `_create_line_mesh_from_path(path: PackedVector3Array)`
- `start_line_animations()`
- `_update_line_deformation(time: float)`
- `_animate_shader_properties()`
- `_set_shader_param(material: ShaderMaterial, param: StringName, value: float)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Wavefunctions_ColoredLinesVRCorridor | [8,5] | `:0:-1.4` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
