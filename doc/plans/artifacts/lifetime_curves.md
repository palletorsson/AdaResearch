# Artifact: lifetime_curves

> Property = f(normalized_age). Every particle traces an arc through parameter space as it ages.

## Context

**Category:** unknown
**Tags:** particles, lifetime, curves

Three emitters demonstrating color, size, and opacity curves over particle lifetime

*How a particle dies defines what it looks like alive.*

## Design

- **Interaction:** To show that aging is not linear. Three personalities of death: flash, fade, pulse.
- **Critical Parameter:** The curve shape — sharp sine peak (flash), gentle arc (fade), damped oscillation (pulse).
- **What Emerges:** Visual personality from mathematical curves. The curve graph IS the soul of each emitter.
- **Triggers:** Spawn → particle enters curve, age crosses 0.5 → midlife behavior diverges between profiles

## Architecture

| | |
|---|---|
| **File** | `algorithms/particles/lifetime_curves/lifetime_curves.gd` (205 lines) |
| **Registry** | `physics_simulation.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_setup_multimesh()`
- `_setup_labels()`
- `_setup_curve_display()` -- Visual curve graphs behind each emitter
- `_process(delta: float)`
- `_spawn_particle(emitter_id: int)`
- `_size_curve(t: float, emitter_id: int)` -- Quick flash: sharp peak then collapse
- `_opacity_curve(t: float, emitter_id: int)` -- Quick flash: instant on, fast fade
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Particle Systems** (particles) -- map: Particles_LifetimeAndTypes

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Particles_LifetimeAndTypes | [4,5] | `-` |

### Relationships

- Fills the gap in Particles_LifetimeAndTypes. Complements particle_campfire (synthesis uses these curves). Contrasts with sub_emitters (death as birth vs death as fade).
- Needs: VR curve editor or profile switcher [missing]. Lifetime duration slider [missing]. Spawn rate slider [missing].

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
