# Artifact: Turtle Meander Floor

> a floor that proves the Greek key IS a turtle program

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, turtle_graphics, meander, hilbert, algorithms | **Themes:** arrays, art, cultural_computation, algorithms

Procedural mosaic floor whose Greek key border is traced by a turtle graphics engine. The same turtle machine generates the ancient meander, the Hilbert curve, and the Peano curve — different programs, same machine.

*the same machine that draws a Greek key draws a Hilbert curve*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players see that ancient ornament and modern algorithms share a machine
- **Critical Parameter:** border_mode — switches between meander, Hilbert, Peano
- **What Emerges:** the insight that L-system / turtle graphics unify ancient and modern pattern
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/turtle_meander_floor.gd` (538 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/turtle_meander_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `TurtleMeanderFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `border_mode` | int | 0 |
| `meander_depth` | int | 3 |
| `floor_size` | Vector2 | Vector2(1.2, 0.9) |
| `tiles_short` | int | 40 |
| `outer_band` | int | 1 |
| `inner_band` | int | 1 |
| `path_width` | int | 1 |
| `dot_spacing` | int | 3 |
| `dot_size_fraction` | float | 0.12 |
| `jitter_amount` | float | 0.15 |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.06 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`
- `_trace_hilbert_border(turtle: MeanderTurtle, zone_w: int, zone_h: int, depth: int)` -- For the Hilbert border, we trace a Hilbert curve that fits within the
- `_trace_peano_border(turtle: MeanderTurtle, zone_w: int, zone_h: int, depth: int)`

### Grid Config

Accepts: `border_mode`, `dot_size_fraction`, `dot_spacing`, `floor_size`, `inner_band`, `jitter_amount`, `meander_depth`, `outer_band`, `path_width`, `tiles_short`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- pompeii_mosaic_floor (sibling), meander_threshold_floor (related motif)
- Needs: [implemented] turtle engine, meander program, Hilbert program, polka-dot field

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
