# Artifact: Spline Cables

> Smooth curves connecting points—cables that hang in catenary arcs, the natural shape a flexible line takes under gravity. Infrastructure as frozen wave.

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** physics, wave | **Themes:** oscillation, physics, waves

Smooth curves connecting points—cables that hang in catenary arcs, the natural shape a flexible line takes under gravity. Infrastructure as frozen wave.

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [9.0, 6.0, 16.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/static_cables/SplineCables.gd` (278 lines) |
| **Scene** | `res://algorithms/wavefunctions/static_cables/SplineCables.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `points_per_cable` | int | 80 |
| `cable_radius` | float | 0.12 |
| `ring_segments` | int | 12 |
| `color_start` | Color | Color(0.9, 0.5, 0.6) |
| `color_end` | Color | Color(0.5, 0.7, 1.0) |
| `auto_generate_on_ready` | bool | true |
| `use_position_markers` | bool | true |
| `sine_amplitude` | float | 0.5 |
| `sine_frequency` | float | 2.0 |
| `sine_vertical` | bool | true |
| `auto_switch_oscillation` | bool | true |
| `floor_height_threshold` | float | 0.5 |

### Key Methods

- `_ready()`
- `_create_environment()`
- `_generate_cables_from_paths()` -- Clear existing cables
- `_sample_curve(curve: Curve3D, num_points: int)`
- `_create_tube_mesh(path: PackedVector3Array)`
- `_generate_cables_from_positions()` -- Clear existing cables
- `_create_sine_wave_path(positions: Array[Node3D])`
- `regenerate_cables()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| TestMap_Unused_3 | [1,1] | `:0:2` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
