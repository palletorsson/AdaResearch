# Artifact: evolvingflowers

> genome = {petals, color_hue, symmetry, L-system_rules, branch_angle}; fitness = symmetry + vibrancy + complexity

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** machine_learning | **Themes:** ai, machine_learning

evolvingflowers

*beauty is what survives selection — aesthetics can be an emergent property of optimization*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [20, 3, 20] (xlarge)
- **Interaction:** watch a garden of flowers evolve toward beauty — petals deepen in color, branching patterns elaborate
- **Critical Parameter:** mutation_rate — shapes how quickly the aesthetic drifts; too high and beauty dissolves
- **What Emerges:** L-system branching patterns, color preferences, symmetry — an aesthetic nobody programmed
- **Triggers:** generation_time timer breeds new population; elite flowers survive; growth animation springs flowers up

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/evolvingflowers/evolvingflowers.gd` (594 lines) |
| **Scene** | `res://algorithms/machinelearning/evolvingflowers/evolvingflowers.tscn` |
| **Registry** | `machinelearning.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `generation_time` | float | 8.0 |
| `mutation_rate_export` | float | 0.15: |

### Key Methods

- `_ready()`
- `setup_environment()`
- `initialize_population()`
- `spawn_flower(genome: FlowerGenome, pos: Vector3)`
- `create_petal_mesh(genome: FlowerGenome)`
- `create_leaf_mesh(size: float)`
- `generate_lsystem_string(genome: FlowerGenome)`
- `create_lsystem_branches(genome: FlowerGenome, parent: Node3D, petal_color: Color)`
- `create_branch_segment(start: Vector3, end: Vector3, thickness: float, color: Color)`
- `_process(delta: float)`
- `evolve_population()`
- `calculate_fitness()`
- `select_parent(genomes_with_fitness: Array)` -- Tournament selection
- `update_generation_label()`
- `_exit_tree()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| MachineLearning_Evolving_Flowers | [6,5] | `-` |
| ML_Evolution | [2,10] | `:0:2:0.6` |

### Relationships

- depends on 9_3_smart_rockets_vr (shared GA pattern); unlocks evolved_creatures (from flowers to creatures)
- Needs: VR controls [missing] — generation_time and mutation_rate exported but no spatial sliders

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
