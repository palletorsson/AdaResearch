# Artifact: Fluid Form

> vertex_shader(noise_layers * flow_intensity) + fresnel_iridescence -> morphing sphere

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** qfep | **Themes:** philosophy, qfep, theory

Morphing iridescent form for φ>0 zone. Embraces becoming.

*positive phi means embracing transformation — the fluid form is not broken, it is becoming*

## Design

- **Visual:** material: shader, animation: shader_time
- **Interaction:** watch a form that never holds still — always becoming, never arriving
- **Critical Parameter:** flow_intensity — how much the vertex shader deforms the mesh each frame
- **What Emerges:** iridescent color shifts at glancing angles — the form appears to have an inner light
- **Triggers:** _process updates shader time continuously; pulse() temporarily triples flow intensity

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/fluid_form.gd` (146 lines) |
| **Scene** | `res://commons/interfaces/qfep/fluid_form.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `QFEPFluidForm` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `size` | float | 0.4 |
| `morph_speed` | float | 0.5 |
| `flow_intensity` | float | 0.3 |
| `fluid_color` | Color | Color(1.0, 0.8, 0.2, 1.0) |

### Key Methods

- `_ready()`
- `_build_form()`
- `_process(delta: float)`
- `set_flow(intensity: float)`
- `pulse()`

## Curriculum Position

### Sequences

- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Phi_Term

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| QFEP_Phi_Term | [2,7] | `:0:0.5` |

### Relationships

- contrasts rigid_sculpture (phi<0 frozen vs phi>0 flowing); paired with transforming_pattern; depends on phi concept
- Needs: VR flow intensity control [missing], pulse trigger [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
