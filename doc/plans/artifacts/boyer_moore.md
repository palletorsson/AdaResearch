# Artifact: Boyer-Moore Pattern Matching

> Boyer-Moore string search — the algorithm that searches backwards, skipping ahead when mismatches reveal the pattern can't possibly match. Sublinear average time for text search.

## Context

**Category:** algorithms | **Complexity:** intermediate
**Tags:** boyer_moore, pattern_matching, string_search, skip, text | **Themes:** algorithms, misc

Boyer-Moore string search — the algorithm that searches backwards, skipping ahead when mismatches reveal the pattern can't possibly match. Sublinear average time for text search.

## Design

- **Visual:** material: standard, animation: transform

## Architecture

| | |
|---|---|
| **File** | `algorithms/stringalgorithms/patternmatching/boyer_moore_visualization.gd` (476 lines) |
| **Scene** | `res://algorithms/stringalgorithms/patternmatching/boyer_moore_visualization.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `text_corpus` | String | "In a world where queer stories matter, every word carries weight. Trans voices echo through digital spaces, searching for recognition and belonging. Non-binary identities resist categorization while seeking authentic representation." |
| `search_pattern` | String | "queer" |
| `case_sensitive` | bool | false |
| `highlight_partial_matches` | bool | true |
| `animation_speed` | float | 0.8 |
| `show_bad_character_table` | bool | true |
| `auto_animate` | bool | true |
| `text_size` | float | 0.8 |
| `character_spacing` | float | 1.0 |
| `pattern_color` | Color | Color(0.9, 0.3, 0.9) |
| `match_color` | Color | Color(0.2, 0.9, 0.3) |
| `mismatch_color` | Color | Color(0.9, 0.3, 0.3) |
| `text_color` | Color | Color(0.9, 0.9, 0.9) |
| `identity_preset` | String | "Trans_Visibility" |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_input(event: InputEvent)`
- `setup_environment()` -- Dramatic lighting for text focus
- `setup_camera()`
- `load_identity_preset(preset: String = "")`
- `preprocess_pattern()`
- `build_bad_character_table()`
- `create_text_visualization()`
- `create_text_meshes()`
- `create_pattern_overlay()`
- `create_character_mesh(character: String, position: Vector3, color: Color)`
- `start_boyer_moore_search()`
- `perform_search_step()`
- `calculate_boyer_moore_skip(mismatched_char: String)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
