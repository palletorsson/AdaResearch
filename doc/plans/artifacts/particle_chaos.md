# Artifact: Particle Chaos

> GPU_particles(spread=180, gravity=0, angular_velocity=+-360) -> pure entropy cloud

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** chaos, compression, particle, probability, procedural, qfep, simulation | **Themes:** philosophy, qfep, theory

High-entropy particle system. Maximum spread, random velocities, dissolution.

*entropy is not disorder but possibility — maximum spread means maximum freedom from constraint*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** be surrounded by particles that obey no pattern — feel the freedom of maximum entropy
- **Critical Parameter:** spread (180 = full sphere) — the absence of preferred direction that defines chaos
- **What Emerges:** transient clusters and voids in the noise — the eye finds patterns even where none exist
- **Triggers:** set_intensity() scales velocity and count; pulse() creates momentary burst

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/particle_chaos.gd` (117 lines) |
| **Scene** | `res://commons/interfaces/qfep/particle_chaos.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `QFEPParticleChaos` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `particle_count` | int | 200 |
| `emission_radius` | float | 0.5 |
| `speed_min` | float | 0.5 |
| `speed_max` | float | 2.0 |
| `lifetime` | float | 3.0 |
| `chaos_color` | Color | Color(0.9, 0.2, 0.2, 1.0) |

### Key Methods

- `_ready()`
- `_build_particles()`
- `set_intensity(value: float)`
- `pulse()`

## Curriculum Position

### Sequences

- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_E_Term

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| QFEP_E_Term | [5,5] | `:0:1` |

### Relationships

- represents E(S) term; contrasts ordered_grid (F term); paired with random_cubes; feeds into lambda spectrum
- Needs: VR intensity control [missing], pulse trigger [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
