# Artifact: Helix Cylinder Creature

> a flat triangle grid folds into a twisted faceted cylinder — pineapple topology as a living creature

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** fold_system, helix, cylinder, pineapple, creature, runtime | **Themes:** fold_system

Runtime fold creature — flat triangle grid folds into twisted faceted cylinder. Pineapple topology. Same vertices, same faces, only positions change. Driven by fold_amount.

*a cylinder is a flat grid with its edges identified. twist makes it alive.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a tube that unfurls flat when dormant and coils shut when alert — the fold IS the body
- **Critical Parameter:** fold_amount — 0 is flat grid, 1 is closed helix cylinder
- **Triggers:** same vertices, same faces, only positions change — folding preserves topology

## Architecture

| | |
|---|---|
| **File** | `commons/fold_system/helix_cylinder_creature.gd` (173 lines) |
| **Scene** | `res://commons/fold_system/helix_cylinder_creature.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `HelixCylinderCreature` extends `FoldCritter` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `rings` | int | 12 |
| `segments_per_ring` | int | 8 |
| `cyl_radius` | float | 0.06 |
| `cyl_height` | float | 0.25 |
| `twist_amount` | float | 0.5 |
| `flat_width` | float | 0.35 |
| `flat_height` | float | 0.25 |
| `base_color` | Color | Color(0.88, 0.82, 0.72) |

### Dependencies

- `res://commons/primitives/shared/primitive_mesh_builder.gd`

### Key Methods

- `_ready()`
- `_generate_geometry()`
- `_create_material()`
- `_create_fold_rig()`
- `_build_fold_mesh()`
- `_create_collision()`
- `_process_visual(delta: float)`
- `_build_mesh_at_fold(t: float)`
- `configure(config: Dictionary)`
- `apply_grid_config(config: Dictionary)`

### Grid Config


## Curriculum Position

### Sequences

- **Folding: Same Faces, Only Positions Change** (fold_system) -- map: Folding_Zoo

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
