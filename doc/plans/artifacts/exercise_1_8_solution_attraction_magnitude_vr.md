# Artifact: Attraction and Orbit

> F = strength * dir / r^2. Inverse-square attraction. Closer = stronger. Orbit emerges from falling and missing.

## Context

**Category:** physics | **Complexity:** beginner
**Tags:** attraction, orbit, inverse_square, gravity, grabbable, interactive, nature_of_code, vr | **Themes:** linear_algebra, math, vectors

Inverse-square attraction. Closer means stronger. A ball orbits a grabbable attractor — drag it in VR and the orbit reshapes in real time. Elliptical paths emerge from the interplay of tangential velocity and radial pull. F = strength * dir / r-squared. An orbit is a perpetual fall that always misses the ground.

*An orbit is a perpetual fall that always misses the ground. Attraction creates structure from nothing but distance and direction.*

## Design

- **Visual:** material: unlit, animation: vertex
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** To let the learner grab the attractor in VR and drag it — watching the orbiting ball reshape its path in real time.
- **Critical Parameter:** attraction_strength — controls the force constant. Too weak → ball escapes. Too strong → ball spirals inward. The sweet spot produces stable orbits.
- **What Emerges:** Elliptical orbits from the interplay of tangential velocity and radial attraction. Moving the attractor mid-orbit creates chaotic transitions between orbit shapes.
- **Triggers:** VR grab attractor sphere → ball orbit changes shape, strength slider → orbits tighten or loosen, velocity clamped at 0.15 to prevent escape

## Architecture

| | |
|---|---|
| **File** | `algorithms/vectors/noc_ch01/exercise_1_8_solution_attraction_magnitude_vr.gd` (160 lines) |
| **Scene** | `res://algorithms/vectors/noc_ch01/exercise_1_8_solution_attraction_magnitude_vr.tscn` |
| **Registry** | `vectors.json` |
| **Class** | `AttractionMagnitudeVR` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `attraction_strength` | float | 0.5 |

### Dependencies

- `res://spatial_ui/parameter_controller_3d.tscn`
- `res://commons/primitives/math_gallery/GrabSphere.tscn`
- `res://commons/resourses/materials/noc_vr/noc_vr_pink_primary.tres`
- `res://commons/resourses/materials/noc_vr/noc_vr_pink_accent.tres`
- `res://commons/resourses/materials/noc_vr/noc_vr_pink_secondary.tres`

### Key Methods

- `_ready()`
- `_setup_environment()`
- `_spawn_objects()`
- `_setup_line()`
- `_process(_delta: float)` -- Track attractor position from the grabbable sphere (local to sim_root)
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: VectorAdvanced

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Vector_Exhibition | [16,3] | `-` |
| VectorAdvanced | [12,4] | `-` |
| Vectors_6 | [3,4] | `-` |

### Relationships

- Extends bouncing_ball (adds attraction). Feeds into three_body_problem and nbody_simulation (many attractors). Gateway to gravitational dynamics.
- Needs: VR grabbable attractor [has], strength parameter controller [has], force line visualization [has]. Missing: trail showing orbit history.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
