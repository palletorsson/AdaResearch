# Artifact: Surreal Kinetic Sculpture

> An art installation that weaves together multiple physics systems — springs, rigid bodies, constraints, particles, and forces — into a mesmerizing, perpetually-moving kinetic sculpture. Gears drive pendulums that scatter particles through force fields while cloth banners ripple in simulated wind. Not a tutorial but a celebration: physics as aesthetic experience, computation as art.

## Context

**Category:** physics | **Complexity:** advanced
**Tags:** kinetic_sculpture, art, combined, springs, rigid_body, constraints, particles, forces, cloth, aesthetic, surreal, perpetual_motion | **Themes:** art, kinetic, combined_systems, aesthetic

An art installation that weaves together multiple physics systems — springs, rigid bodies, constraints, particles, and forces — into a mesmerizing, perpetually-moving kinetic sculpture. Gears drive pendulums that scatter particles through force fields while cloth banners ripple in simulated wind. Not a tutorial but a celebration: physics as aesthetic experience, computation as art.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [13.0, 10.0, 18.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/physicssimulation/surrealkineticsculpture/SurrealKineticSculpture.gd` (196 lines) |
| **Scene** | `res://algorithms/physicssimulation/surrealkineticsculpture/SurrealKineticSculpture.tscn` |
| **Registry** | `physics_simulation.json` |
| **Class** | `SurrealKineticSculpture` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `sculpture_scale` | float | 1.0 |
| `piston_speed_multiplier` | float | 1.0 |
| `color_intensity` | float | 1.2 |
| `mechanical_precision` | float | 0.95 |
| `organic_distortion` | float | 0.8 |
| `rotation_speeds` | Array[float] | [0.5, 0.7, 0.3] |
| `piston_amplitudes` | Array[float] | [2.0, 1.5, 2.5] |
| `phase_offsets` | Array[float] | [0.0, 120.0, 240.0] |

### Key Methods

- `_ready()`
- `setup_sculpture_base()`
- `create_base_platform()` -- Main platform
- `create_material_system()`
- `build_tanguy_mechanisms()`
- `create_niki_extensions()`
- `setup_kinetic_lighting()`
- `start_kinetic_animation()`
- `_process(delta: float)` -- Update each mechanism
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Physics Simulation: The Engine Behind Reality** (physicssimulation) -- map: PhysicsSim_Continuum

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| PhysicsSim_Continuum | [5,4] | `:0:1` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
