# Artifact: Lenia Continuous

> Continuous cellular automaton (Lenia) — smooth lifelike organisms emerge from a ring-shaped kernel, bell-curve growth function, and continuous [0,1] states. The 'wow' artifact for cellular automata: watch amorphous blobs self-organize into gliding, pulsing creatures on a glowing floor display.

## Context

**Category:** emergence | **Complexity:** advanced
**Tags:** 2d, cellular_automata, emergence, visualization | **Themes:** art, biology, cellular_automata, computation

Continuous cellular automaton (Lenia) — smooth lifelike organisms emerge from a ring-shaped kernel, bell-curve growth function, and continuous [0,1] states. The 'wow' artifact for cellular automata: watch amorphous blobs self-organize into gliding, pulsing creatures on a glowing floor display.

## Design

- **Visual:** material: generated_texture, animation: transform

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/lenia_continuous/lenia_continuous.gd` (222 lines) |
| **Scene** | `res://commons/artifacts/lenia_continuous/lenia_continuous.tscn` |
| **Registry** | `cellular_automata.json` |
| **Class** | `LeniaContinuous` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `display_size` | float | 0.8 |
| `grid_size` | int | 64 |
| `step_interval` | float | 0.05 |
| `dt` | float | 0.1 |
| `kernel_radius` | int | 13 |
| `ring_mu` | float | 0.5 |
| `ring_sigma` | float | 0.15 |
| `growth_mu` | float | 0.135 |
| `growth_sigma` | float | 0.015 |

### Key Methods

- `_ready()`
- `_precompute_kernel()` -- Build ring-shaped kernel: K(r) = bell(r/R, ring_mu, ring_sigma)
- `_bell(x: float, mu: float, sigma: float)` -- Gaussian bell curve: exp(-((x - mu)^2) / (2 * sigma^2))
- `_create_display()`
- `_seed_organisms()` -- Clear grid
- `_process(delta: float)`
- `_simulation_step()`
- `_update_texture()`
- `_state_to_color(val: float)` -- Color map: 0=black, 0.5=deep blue, 1.0=bright cyan
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `dt`, `growth_mu`, `growth_sigma`, `step_interval`

## Curriculum Position

### Sequences

- cellularautomata

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
