# Artifact: A* Pathfinding

> Advanced pathfinding comparison — Jump Point Search (pruned A* with symmetry breaking), Theta* (any-angle line-of-sight paths), and side-by-side algorithm comparison on the same randomized grid. ImmediateMesh visualization of open/closed sets, jump points, line-of-sight checks, and final paths with per-algorithm statistics.

## Context

**Category:** algorithms | **Complexity:** intermediate
**Tags:** astar, pathfinding, heuristic, search, shortest_path, grid, jps, theta_star, jump_point_search, any_angle | **Themes:** algorithms, misc

Advanced pathfinding comparison — Jump Point Search (pruned A* with symmetry breaking), Theta* (any-angle line-of-sight paths), and side-by-side algorithm comparison on the same randomized grid. ImmediateMesh visualization of open/closed sets, jump points, line-of-sight checks, and final paths with per-algorithm statistics.

## Design

- **Visual:** material: vertex_color, animation: vertex
- **Scale:** [1.0, 6.0, 21.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/searchpathfinding/astar/astar.gd` (867 lines) |
| **Scene** | `res://algorithms/searchpathfinding/astar/astar.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | int | 20 |
| `obstacle_density` | float | 0.25 |
| `step_speed` | float | 3.0 |
| `cell_size` | float | 0.3 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_create_materials()`
- `_create_mesh_instances()`
- `_create_labels()`
- `_create_vr_controls()`
- `_on_mode_pressed()`
- `_on_reset_pressed()`
- `_on_density_changed(_v: float)`
- `_on_speed_changed(_v: float)`
- `_on_size_changed(_v: float)`
- `_generate_grid()`
- `_in_bounds(p: Vector2i)`
- `_walkable(p: Vector2i)`
- `_make_alg_state(alg_name: String)`

### Grid Config

Accepts: `cell_size`, `grid_size`, `mode`, `obstacle_density`, `step_speed`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
