# Artifact: This is just a test

> tree(pos, dir, len, d) = branch + tree(left, angle+noise) + tree(right, angle+noise) + maybe tree(mid)

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

this is the test

*The stochastic tree proves that randomness is not noise — it is the difference between a diagram and a tree.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 4.0, 2.0] (room_scale)
- **Interaction:** To look natural — identical algorithm to a deterministic tree, but angle_variance and length_variance inject life
- **Critical Parameter:** angle_variance (15.0) — the random range added to each branch angle; at 0 the tree is symmetric, at 15 it is organic
- **What Emerges:** The 30% chance third branch (randf() < 0.3) — occasional triple branching creates asymmetric fullness, like a real tree
- **Triggers:** Randomize seed at ready → unique tree every instantiation; depth-based color gradient from dark stem to bright tips

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/example_8_7_stochastic_tree_vr.gd` (142 lines) |
| **Scene** | `res://algorithms/fractals/example_8_7_stochastic_tree_vr.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `recursion_depth` | int | 5 |
| `base_angle` | float | 25.0 |
| `angle_variance` | float | 15.0 |
| `length_reduction` | float | 0.67 |
| `length_variance` | float | 0.1 |

### Dependencies

- `res://commons/resourses/materials/noc_vr/noc_vr_pink_primary.tres`

### Key Methods

- `_ready()`
- `_setup_environment()`
- `_grow_tree(start_pos: Vector3, direction: Vector3, length: float, thickness: float, depth: int)`
- `_create_branch(start: Vector3, end: Vector3, thickness: float, depth: int)`
- `_rotate_vector(vec: Vector3, axis: Vector3, angle: float)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Relationships

- Stochastic variant of recursive_tree; parent to stochastic_tree_separated (adds post-growth separation physics)
- Needs: VR regenerate button [missing], variance control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
