# Artifact: Persian Rug

> cell(t+1) = born/survive(neighbors) with four-fold symmetry enforcement

## Context

**Category:** emergence | **Complexity:** intermediate
**Tags:** 2d, cellular_automata, emergence | **Themes:** biology, cellular_automata, computation, emergence

Persian Rug Cellular Automata.

*Symmetry imposed on local rules transforms noise into ornament — the rug designs itself.*

## Design

- **Visual:** material: generated_texture, animation: static
- **Scale:** [3, 1, 4] (xlarge)
- **Interaction:** To weave itself — a living carpet whose patterns breathe and shift like textile memory
- **Critical Parameter:** rule_border_born/survive vs rule_inner_born/survive — two rulesets create the border-interior tension
- **What Emerges:** Intricate symmetric textile patterns from the interplay of two CA rulesets and forced mirror symmetry
- **Triggers:** Switching random_seed reshuffles initial conditions; changing border vs inner rules shifts the visual dialect entirely

## Architecture

| | |
|---|---|
| **File** | `algorithms/cellularautomata/persian_rug/persian_rug.gd` (302 lines) |
| **Scene** | `res://algorithms/cellularautomata/persian_rug/persian_rug.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `width` | int | 256 |
| `height` | int | 384 |
| `border_size` | int | 32 |
| `iterations` | int | 100 |
| `auto_step` | bool | true |
| `step_interval` | float | 0.05 |
| `random_seed` | bool | false |
| `performance_mode` | bool | false |
| `play_duration` | float | 10.0 |
| `pause_duration` | float | 10.0 |
| `rule_border_born` | Array[int] | [2] |
| `rule_border_survive` | Array[int] | [3, 4, 5] |
| `rule_border_states` | int | 4 |
| `rule_inner_born` | Array[int] | [3] |
| `rule_inner_survive` | Array[int] | [1, 2, 3, 4, 5] |
| `rule_inner_states` | int | 5 |
| `color_background` | Color | Color("5e1026") |
| `color_border_active` | Color | Color("d6336c") |
| `color_inner_active` | Color | Color("ffc966") |
| `color_decay` | Gradient | - |
| `rows_per_frame` | int | 40 |

### Key Methods

- `_ready()`
- `_process(delta: float)` -- Ensure variables are initialized for @tool hot-reloading
- `_setup_default_gradient()`
- `_initialize_grid()` -- Force type reset in case of hot-reload artifact
- `_is_border(x, y)`
- `_enforce_symmetry()` -- Mirror Top-Left to Top-Right, Bottom-Left, Bottom-Right
- `step()` -- Pre-calculate constants to avoid lookups in loop
- `_setup_texture()`
- `_update_texture_slice(rows: int)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Cellular Automata: Local Rules, Global Patterns** (cellularautomata) -- map: CA_Introduction

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CA_Introduction | [3,3] | `:0:-0.49` |
| Disposition_Test_Hub | [4,9] | `-` |

### Relationships

- Contrasts with mirrored_cellular_automata (stochastic symmetry vs deterministic). Feeds into CA_Introduction.
- Needs: VR parameter controls [missing], auto-play toggle [has via auto_step]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
