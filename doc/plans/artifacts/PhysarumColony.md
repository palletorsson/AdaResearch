# Artifact: PhysarumColony

> trail(x,y) = deposit(agent_positions) * diffuse - evaporate — no neurons, no memory, just chemistry

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** swarm | **Themes:** collective, emergence, swarm

PhysarumColony

*intelligence is a property of the medium, not the agent — the slime mold's network is smarter than any individual cell*

## Design

- **Visual:** material: generated_texture, animation: static
- **Scale:** [11, 31, 1] (world_scale)
- **Interaction:** to feel what it's like to be a network that designs itself — the moment a random scatter becomes a vascular web
- **Critical Parameter:** emission_centers layout — the attractor geometry determines every possible network the colony can grow
- **What Emerges:** shortest-path trees, Steiner networks, and load-balanced branching — solutions that take mathematicians centuries to formalize
- **Triggers:** adding a new attractor causes the colony to re-route all trails within seconds, like rewiring a circuit live

## Architecture

| | |
|---|---|
| **File** | `algorithms/swarmintelligence/physarum/PhysarumColony.gd` (136 lines) |
| **Scene** | `res://algorithms/swarmintelligence/physarum/PhysarumColony.tscn` |
| **Registry** | `swarmintelligence.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_agents` | int | 500 |
| `grid_resolution` | int | 256 |
| `terrain_size` | Vector2 | Vector2(50, 50) |
| `agent_scene` | PackedScene | - |

### Key Methods

- `_ready()` -- 1. Setup Grid
- `_physics_process(delta: float)` -- Sequence Logic
- `set_distribution(mode: int)` -- Clear old
- `add_attractor(pos_world: Vector3, radius: float)`
- `_spawn_marker(pos: Vector3, radius: float)`
- `_world_to_grid(pos: Vector3)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Swarm Intelligence: No Leader, Yet Coordinated** (swarmintelligence) -- map: SwarmIntelligence_PhysarumColony

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| SwarmIntelligence_PhysarumColony | [4,4] | `-` |

### Relationships

- unlocks AntColonyV2 and ant_colony_optimization; contrasts with FlowFieldMain (external field vs. self-created field); depends on PhysarumGrid and PhysarumAgent
- Needs: [missing] no VR sliders; attractor positions could be grabbed and moved; deposit/evaporation rate sliders would let learner push colony to collapse or overgrowth

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
