# Artifact: Triangle Line Puzzle

> triangle = 3 edges connecting 3 vertices — the minimal closed polygon

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

Interactive puzzle: drag 3 line endpoints to form an equilateral triangle.

*three points that are not collinear always define a triangle — and a unique plane*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 2.0, 1.0] (compact)
- **Interaction:** learner discovers that three lines can close into the simplest possible bounded shape
- **Critical Parameter:** closure — all 3 snap targets must be occupied with shared endpoints to complete
- **What Emerges:** the triangle as the atomic unit of all mesh geometry — every surface is triangles beneath
- **Triggers:** snapping all 3 line endpoints into the 3 triangle vertex targets

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/line/puzzles/triangle_line_puzzle.gd` (51 lines) |
| **Scene** | `res://commons/primitives/line/puzzles/triangle_line_puzzle.tscn` |
| **Registry** | `primitives.json` |
| **Class** | `TriangleLinePuzzle` extends `LineSnapPuzzleBase` |
| **Pattern** | procedural |

### Key Methods

- `_init()` -- Define the 3 target vertices of the triangle (shared pool)
- `_ready()`
- `_complete_puzzle()` -- Hide logic display if needed

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Point_Triangle

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_Triangle | [3,3] | `:0:1.2:0#fillhole:remove` |

### Relationships

- extends LineSnapPuzzleBase; logical predecessor to snap_tetrahedron_puzzle
- Needs: [missing VR controls — all interaction is line-endpoint snapping]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
