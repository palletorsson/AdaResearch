# Artifact: Plasma Critter

> form(contact) -> transmute(effect) -- raw energy that takes shape from what touches it

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** hazards, interactive | **Themes:** gameplay, hazards, qfep

Shapeshifting energy substance — a critter that takes form based on relational context. Raw form shocks on contact. Touch with a stick → fire torch. Touch water → healing aura. Touch crystal → power orb. Q-FEP: same potential, different restraint.

*the plasma is the same substance whether it burns or heals -- the mediator determines manifestation.*

## Design

- **Visual:** material: standard, animation: particle_lifecycle
- **Interaction:** plasma that shocks raw, becomes fire via stick, becomes healing via water -- dual-nature
- **Critical Parameter:** current_form -- raw/fire/healing determined by what mediator contacts the plasma
- **What Emerges:** Q-FEP dual-nature at creature scale -- same energy, different relation, different outcome
- **Triggers:** stick_tool contact -> fire form; water -> healing; no mediator -> raw damage

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/plasma/plasma_critter.gd` (586 lines) |
| **Scene** | `res://commons/hazards/plasma/plasma_critter.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `PlasmaCritter` extends `Area3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `starting_form` | Form | Form.RAW |
| `interaction_radius` | float | 0.6 |
| `raw_damage` | float | 5.0 |
| `fire_damage` | float | 15.0 |
| `burn_duration` | float | 60.0 |
| `respawn_delay` | float | 5.0 |
| `heal_per_second` | float | 8.0 |
| `damage_cooldown` | float | 0.8 |
| `contact_damage_enabled` | bool | true |
| `core_radius` | float | 0.18 |
| `particle_count` | int | 50 |
| `light_range` | float | 6.0 |
| `light_energy` | float | 2.5 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `set_form(new_form: Form)`
- `_apply_form_visuals()`
- `attach_to(target: Node3D)` -- Already attached
- `detach()`
- `_update_fuel_visuals()`
- `_deplete()`
- `_process_depleted(delta: float)`
- `_respawn()`
- `_on_body_entered(body: Node3D)`
- `_on_body_exited(_body: Node3D)`
- `_on_area_entered(area: Area3D)`
- `_handle_player_contact(player: Node3D)`
- `_handle_stick_contact(stick: Node3D)` -- Stick contact → become fire form and attach

### Signals

- `form_changed`
- `plasma_attached`
- `plasma_detached`
- `plasma_depleted`
- `plasma_respawned`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_2

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_2 | [3,5] | `-` |

### Relationships

- paired with stick_tool and force_field; central Q-FEP dual-nature demonstration
- Needs: Area3D [has]; form state machine [has]; mediator detection [has]; multiple effects [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
