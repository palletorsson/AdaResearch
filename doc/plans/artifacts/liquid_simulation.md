# Artifact: Liquid Simulation

> Particle-based liquid simulation and fluid dynamics

## Context

**Category:** unknown | **Complexity:** advanced
**Tags:** procedural | **Themes:** generation, procedural

Particle-based liquid simulation and fluid dynamics

## Design

- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/proceduralgeneration/hybrid_complex/particlebasedsimulation/liquidsimulation/liquid_simulation.gd` (453 lines) |
| **Scene** | `res://algorithms/proceduralgeneration/hybrid_complex/particlebasedsimulation/liquidsimulation/liquid_simulation.tscn` |
| **Registry** | `procgen_extra.json` |
| **Class** | `LiquidSimulation` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `container_size` | Vector3 | Vector3(2.0, 1.0, 2.0) |
| `particle_count` | int | 100 |
| `simulation_steps_per_frame` | int | 5 |
| `gravity` | float | 9.8 |
| `viscosity_multiplier` | float | 1.0 |
| `diffusion_rate` | float | 0.05 |
| `show_entropy` | bool | true |

### Key Methods

- `_ready()` -- Set up the container
- `_create_container()` -- Create a transparent container
- `_initialize_particles()` -- Set up grid cell size based on typical interaction distance
- `_setup_entropy_display()`
- `_setup_material()`
- `_process(delta: float)` -- Update simulation at a smaller time step for stability
- `_update_simulation(delta)` -- Update grid for spatial partitioning
- `_update_spatial_grid()` -- Clear the previous grid
- `_find_neighbors()` -- For each particle, find its neighbors
- `_calculate_forces(delta)` -- Apply forces for each particle
- `_update_positions(delta)` -- Update position for each particle using velocity verlet integration
- `_apply_boundary_conditions()` -- Keep particles inside the container with soft boundaries
- `_handle_mixing(delta)` -- Handle diffusion between particles
- `_update_visuals()` -- Update particle visual representation
- `_calculate_entropy()` -- Calculate system entropy based on mixing state

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
