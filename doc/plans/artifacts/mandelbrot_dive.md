# Artifact: Mandelbrot Dive

> z = z² + c, iterated. Escape or stay forever. The boundary between these is infinitely complex.

## Context

**Category:** fractals | **Complexity:** intermediate
**Tags:** mandelbrot, fractal, self_similarity, infinite_detail, shader, interactive, zoom | **Themes:** chaos, dynamical_systems, fractals

GPU-accelerated Mandelbrot set on a 1x1m table. Infinite zoom into self-similar fractal structure. 5 color schemes, auto-dive mode to famous coordinates. The boundary between order and chaos, infinitely complex.

*The simplest equation in complex dynamics produces the most complex object in mathematics.*

## Design

- **Visual:** material: shader, animation: transform
- **Scale:** [2, 1, 2] (standard)
- **Interaction:** To be zoomed into. To reveal that every magnification contains the whole. To never end.
- **Critical Parameter:** Zoom depth. At 10x you see the cardioid. At 10000x you see seahorses. At 10^15 you see copies of yourself.
- **What Emerges:** Self-similarity from iteration. The Mandelbrot set from the simplest possible complex map. Beauty from z²+c.
- **Triggers:** Zoom slider → deeper structure, palette switch → different beauty from same math, auto-dive → the infinite descent

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/mandelbrot_dive/mandelbrot_dive.gd` (411 lines) |
| **Scene** | `res://commons/artifacts/mandelbrot_dive/mandelbrot_dive.tscn` |
| **Registry** | `chaos.json` |
| **Class** | `MandelbrotDive` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `table_size` | float | 1.0 |
| `max_iterations` | int | DEFAULT_ITERATIONS: |
| `zoom` | float | 1.0: |
| `center_x` | float | -0.5: |
| `center_y` | float | 0.0: |
| `color_scheme` | int | 0: |
| `auto_zoom` | bool | false |
| `zoom_speed` | float | 0.5 |
| `zoom_target_x` | float | DEFAULT_TARGET_X |
| `zoom_target_y` | float | DEFAULT_TARGET_Y |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_table()`
- `_create_display()`
- `_create_info_label()`
- `_create_vr_controls()`
- `_connect_button(btn: Node, callback: Callable)`
- `_sync_sliders_deferred()`
- `_sync_zoom_slider()`
- `_sync_palette_slider()`
- `_on_zoom_slider_moved(_position)`
- `_on_palette_slider_moved(_position)`
- `_update_shader_params()`
- `_update_info_label()`
- `_process(delta: float)`
- `_reset()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_MandelbrotSet

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_MandelbrotSet | [5,4] | `:0:0.5:1` |

### Relationships

- Contains all Julia sets as slices. Connects to fractals sequence. Demonstrates escape-time algorithms.
- Needs: VR zoom slider [has], palette selector [has], auto-dive [has]. Could use: Julia set toggle (show connected set for current c).

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
