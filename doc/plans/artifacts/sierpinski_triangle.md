# Artifact: sierpinski_triangle

> sierpinski(v1,v2,v3,d) = 3 * sierpinski(corners, midpoints, d+1), skip center triangle. D = log(3)/log(2) ~ 1.585.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

sierpinski_triangle

*The Sierpinski triangle has zero area and infinite perimeter — it is the shape that taught mathematics that removal creates structure.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To be walked through — 10m triangle rotated vertical, extruded per iteration, creating a fractal wall of triangular portals
- **Critical Parameter:** extrusion_height — each iteration extrudes deeper, turning the 2D fractal into a 3D walkable relief
- **What Emerges:** The central voids become corridors — the removed triangles are the spaces you walk through
- **Triggers:** subdivision_interval tick → all current triangles split into 3 → center removed → depth hue-shifts rainbow

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/sierpinskitriangle/sierpinski_triangle.gd` (349 lines) |
| **Scene** | `res://algorithms/fractals/sierpinskitriangle/sierpinski_triangle.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `subdivision_interval` | float | 1.0 |
| `max_iterations` | int | 6 |
| `auto_start` | bool | true |
| `triangle_size` | float | 10.0 |
| `triangle_thickness` | float | 0.5 |
| `extrude_on_subdivision` | bool | true |
| `extrusion_height` | float | 1.5 |
| `colorize_by_depth` | bool | true |
| `initial_rotation_degrees` | float | 90.0 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `create_initial_triangle()` -- Create equilateral triangle vertices (1 meter)
- `perform_subdivision()`
- `subdivide_triangle(triangle: Dictionary)`
- `create_triangle_mesh(triangle: Dictionary)`
- `create_extruded_triangle_mesh(triangle: Dictionary)`
- `start_subdivision()`
- `stop_subdivision()`
- `reset()` -- Clear all meshes
- `step()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_KochSierpinski
- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_Synthesis

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_KochSierpinski | [5,9] | `:180:2` |
| Fractal_Synthesis | [14,5] | `-` |

### Relationships

- 2D cousin of sierpinski_pyramid and menger_sponge; the canonical D ~ 1.585 fractal dimension example
- Needs: VR walking [has via geometry], iteration control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
