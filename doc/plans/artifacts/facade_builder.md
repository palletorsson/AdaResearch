# Artifact: Facade Builder

> Preset-driven or plan-driven facade composer — zones, bays, symmetry, and classical orders as architectural grammar

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** facade, architecture, builder, classical, gothic, palazzo | **Themes:** arrays, data_structures

Interactive architectural facade composer. Five presets (classical, gothic, palazzo, arcade, minimal) with configurable bays, stories, symmetry, and colors. Uses SpatialComposition zones and SurfaceTool geometry.

*Every facade is a sentence in an architectural grammar — the preset is the dialect, the parameters are the words.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** To let players compose their own facades from learned vocabulary: base, columns, openings, cornice, crown
- **Critical Parameter:** preset — which architectural grammar (classical, gothic, palazzo, arcade, minimal) governs zone proportions
- **What Emerges:** Complete architectural facades from zone-based grammar rules
- **Triggers:** Switching presets transforms the entire facade; changing bay_count alters rhythm; symmetry mode reshapes hierarchy

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/facade_builder/facade_builder.gd` (873 lines) |
| **Scene** | `res://commons/artifacts/facade_builder/facade_builder.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `FacadeBuilder` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `preset` | String | "classical" |
| `plan_path` | String | "" |
| `bay_count` | int | 5 |
| `story_count` | int | 3 |
| `facade_width` | float | 15.0 |
| `facade_height` | float | 10.0 |
| `primary_color` | Color | Color(0.82, 0.78, 0.70) |
| `secondary_color` | Color | Color(0.65, 0.60, 0.52) |

### Dependencies

- `res://commons/composition/spatial_composition.gd`
- `res://commons/facade_parts/facade_composer.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config_data: Dictionary)`
- `build_facade()`
- `_build_composition()` -- Create composition sized to bay_count x story_count
- `_build_zone_geometry(zone_defs: Array)`
- `_compute_zone_extents(zone_defs: Array)` -- Normalize height fractions and compute world-space extents (bottom-up)
- `_add_zone_wall(zone_id: String, bottom: float, top: float, depth: float, zone_idx: int)`
- `_build_element(element_name: String, cx: float, bay_w: float, z_bottom: float, z_top: float, z_height: float, depth: float, bay_idx: int, zone_idx: int)`
- `_build_column(x: float, bottom: float, top: float, depth: float, bay_idx: int, zone_idx: int)`
- `_build_rustication(cx: float, bay_w: float, bottom: float, top: float, depth: float, bay_idx: int, zone_idx: int)` -- Horizontal groove lines to simulate rusticated stonework
- `_build_window_rect(cx: float, win_w: float, win_bottom: float, win_h: float, depth: float, bay_idx: int, zone_idx: int)`
- `_build_arched_opening(cx: float, arch_w: float, bottom: float, height: float, depth: float, bay_idx: int, zone_idx: int, full_arch: bool)`
- `_build_pointed_arch(cx: float, arch_w: float, bottom: float, top: float, depth: float, bay_idx: int, zone_idx: int)`
- `_build_horizontal_band(cx: float, bay_w: float, bottom: float, top: float, depth: float, bay_idx: int, zone_idx: int)`
- `_build_pediment(bottom: float, top: float, depth: float)` -- Single triangular pediment spanning full width (only built once)

### Grid Config


## Curriculum Position

### Sequences

- **Facades: Grammar Builds Architecture** (facades) -- map: Facade_Custom_Composition

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Facade_Custom_Composition | [2,2] | `:0:-0.6` |

### Relationships

- Synthesis artifact for facades sequence alongside facade_grammar_demo. Uses FacadeComposer part library.
- Needs: apply_grid_config [has], plan JSON support [has], FacadeComposer integration [has], VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
