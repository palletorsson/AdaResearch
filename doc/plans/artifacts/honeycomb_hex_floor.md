# Artifact: Honeycomb Hex Floor

> a floor that carries the geometry of nature's most efficient tiling

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pompeii, hexagonal, honeycomb | **Themes:** arrays, art, cultural_computation

Procedural Pompeii mosaic floor with honeycomb hexagonal tessellation. Regular hexagons in alternating dark/light with dark grout outlines. Every 3rd hexagon features a small terracotta cross motif at its center.

*the hexagonal lattice is the most efficient partition of the plane*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see the same pattern bees discovered millennia ago
- **Critical Parameter:** hex_radius — size of each hexagon
- **What Emerges:** the understanding that hexagons tile the plane with minimal perimeter
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/honeycomb_hex_floor.gd` (290 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/honeycomb_hex_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `HoneycombHexFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.2, 0.9) |
| `hex_radius` | float | 0.04 |
| `border_widths` | Array[int] | [2, 1, 1] |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `color_terracotta` | Color | MosaicPalette.TERRACOTTA |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.06 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`
- `_add_rect(verts: PackedVector3Array, x: float, z: float, w: float, h: float)`
- `_add_line_quad(verts: PackedVector3Array, a: Vector2, b: Vector2, width: float)` -- Create a thin quad along the line from a to b

### Grid Config

Accepts: `border_widths`, `floor_size`, `hex_radius`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- pattern_maker_station (web editor), mosaic_editor (composition)
- Needs: [implemented] procedural mesh, border bands, hex grid, decorative crosses

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
