# Artifact: brick_wall_factory

> grid(width, height) of BoxMesh bricks colored sequentially from a named palette — running bond pattern

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** color | **Themes:** color, perception

brick_wall_factory

*a brick wall is a color sequence made solid — the mortar is the silence between notes*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** to see a color palette become architecture, each brick a swatch in a wall you can walk alongside
- **Critical Parameter:** palette — selects from 20 named palettes (starry_night to industrial_brutalism), redefining the wall's emotional register
- **What Emerges:** the running bond offset (half-brick shift on odd rows) creates diagonal color rivers through the palette sequence
- **Triggers:** apply_grid_config can change dimensions, brick size, mortar, and palette at runtime — wall regenerates entirely

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/brick_wall_factory/brick_wall_factory.gd` (92 lines) |
| **Scene** | `res://algorithms/color/brick_wall_factory/brick_wall_factory.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `wall_width` | int | 10 |
| `wall_height` | int | 5 |
| `brick_size` | Vector3 | Vector3(0.2, 0.1, 0.1) |
| `mortar_thickness` | float | 0.01 |
| `palette` | String | "starry_night" |
| `color_palette_resource` | Resource | - |
| `palette_resource_path` | String | "res://algorithms/color/color_palettes.tres" |

### Key Methods

- `_ready()`
- `generate_wall()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Accepts: `brick_depth`, `brick_height`, `brick_width`, `mortar_thickness`, `palette`, `wall_height`, `wall_width`

## Curriculum Position

### Sequences

- **Color: Perception, Not Physics** (color) -- map: Color_Nails

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Nails | [9,0] | `:90` |

### Relationships

- depends on color_palettes.tres; contrasts with pillarcolorcollection (vertical vs horizontal color display)
- Needs: palette selection [has via config]; VR interaction [missing]; mortar color control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
