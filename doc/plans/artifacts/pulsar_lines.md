# Artifact: Pulsar Lines

> for each (row, col) in pulsar signal data: CylinderMesh(height = intensity × max_height, radius = tube_radius) positioned at (col×spacing_x, height/2, row×spacing_z) — every pulse's amplitude mapped to a physical glass tube height

## Context

**Category:** primitives | **Complexity:** beginner
**Tags:** primitives | **Themes:** arrays, data_structures

Pulsar visualization using lines instead of tubes. Better performance.

*scientific visualization is always a mapping from array index to physical property — tube height is signal intensity, and standing beside this mapping makes data science a spatial act*

## Design

- **Visual:** material: unlit, animation: vertex
- **Interaction:** to stand beside a radio signal from 2300 light-years away and see it as physical columns of glass — to understand that scientific data is an array and that arrays can be beautiful, spatial, and historically specific
- **Critical Parameter:** max_height — scales all tube heights relative to the strongest signal pulse; at 0.25m the tallest tube reaches eye-level for seated VR; changing it transforms the data from a chart into an architectural landscape
- **What Emerges:** the sparse pattern of tall tubes versus short tubes reveals the pulsar's actual pulse shape — students encounter the mathematical structure of a neutron star's spin and atmosphere encoded in glass column heights
- **Triggers:** visualization_mode switches between tubes (CylinderMesh), lines (ImmediateMesh), and points, showing that the same data array can take different physical forms depending only on the rendering choice

## Architecture

| | |
|---|---|
| **File** | `algorithms/arrays/pulsar/pulsar_glass_tubes.gd` (256 lines) |
| **Scene** | `res://algorithms/arrays/pulsar/pulsar_glass_tubes.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `PulsarGlassTubes` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `tube_spacing_x` | float | 0.015 |
| `tube_spacing_z` | float | 0.025 |
| `max_height` | float | 0.25 |
| `tube_radius` | float | 0.004 |
| `rows_to_show` | int | 40 |
| `cols_per_row` | int | 50 |
| `min_intensity` | float | 0.05 |
| `glass_color` | Color | Color(0.85, 0.92, 1.0, 0.25) |
| `glow_color` | Color | Color(1.0, 1.0, 1.0, 0.8) |
| `use_emission` | bool | true |
| `visualization_mode` | int | 0 |
| `animate` | bool | false |
| `animation_speed` | float | 1.0 |

### Key Methods

- `_ready()`
- `_setup_materials()` -- Glass tube material (outer)
- `_load_data()`
- `build_visualization()` -- Clear existing children (except non-tube nodes)
- `_create_tube(height: float, intensity: float)`
- `_add_cylinder_tube(group: Node3D, height: float, intensity: float)` -- Glass tube (outer)
- `_add_line_tube(group: Node3D, height: float, intensity: float)` -- Simple line visualization (much faster for large datasets)
- `_add_point_tube(group: Node3D, height: float, intensity: float)` -- Point at the top of where tube would be
- `apply_grid_config(config: Dictionary)`
- `_process(delta: float)`
- `_exit_tree()`

### Grid Config

Accepts: `cols`, `glow_color`, `max_height`, `rows`, `tube_spacing`, `visualization_mode`

## Curriculum Position

### Relationships

- compact form of pulsar_visualizer (which also shows the binary table); shares PulsarData static class for signal generation; appears in Tutorial_Pattern as the compact version; connects to grid_2d_4x4 (same data, different display)
- Needs: glass material [has]; glow material [has]; all three visualization modes [has]; no VR controls for changing visualization_mode [missing]; apply_grid_config not implemented [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
