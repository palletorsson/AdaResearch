# Artifact: Origami Bird

> fold_amount maps a continuous origami crane — egg(0.0) → standing(0.4) → walking(0.6) → flying(1.0)

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** fold_system, procedural, folding, origami, bird, locomotion, bone_solver, flying, walking | **Themes:** fold_system, gameplay

Origami crane that progresses through four fold stages: egg seed (0.0) → standing crane (0.4) → walking with alternating gait (0.6) → full wingspan flight (1.0). Twelve segments, paper-white materials, spring-damped fold dynamics. One fold_amount variable drives the entire lifecycle.

*One fold_amount variable, twelve segments, four locomotion modes. Origami is compressed potential.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a paper bird that hatches from a folded seed, stands on two legs, walks with a gait, then unfolds into soaring flight
- **Critical Parameter:** fold_amount — 0.0 is tight origami egg, 0.4 is standing crane, 0.6 is walking, 1.0 is full wingspan flight
- **What Emerges:** walking gait from sinusoidal leg alternation during WALKING state; flight from wing flap cycle during DEPLOYED
- **Triggers:** FSM drives fold_amount through spring dynamics; proximity triggers hatch; energy gates flight

## Architecture

| | |
|---|---|
| **File** | `commons/fold_system/origami_bird.gd` (696 lines) |
| **Scene** | `res://commons/fold_system/origami_bird.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `OrigamiBird` extends `FoldCritter` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `move_speed` | float | 1.5 |
| `fly_height` | float | 1.5 |

### Key Methods

- `_ready()`
- `_create_fold_rig()`
- `_create_materials()`
- `_paper_mat(albedo: Color, emission: Color, emission_energy: float)`
- `_build_fold_mesh()`
- `_add_wing_mesh(parent: Node3D, side: float, mat: StandardMaterial3D)` -- Origami wing — two triangular panels with a central crease ridge
- `_add_tri(st: SurfaceTool, a: Vector3, b: Vector3, c: Vector3)`
- `_make_diamond_mesh(half_width: float, length: float, mat: StandardMaterial3D)`
- `_make_strip_mesh(half_width: float, length: float, mat: StandardMaterial3D)`
- `_make_pointed_mesh(half_base: float, length: float, mat: StandardMaterial3D)`
- `_make_tail_fan_mesh(half_width: float, length: float, mat: StandardMaterial3D)`
- `_create_collision()`
- `_physics_process(delta: float)`
- `_update_bird_mode()`
- `_update_egg_visual(delta: float)`

### Grid Config


## Curriculum Position

### Sequences

- **Folding: Same Faces, Only Positions Change** (fold_system) -- map: Folding_Zoo

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Folding_Zoo | [8,2] | `:0:0.5` |
| Folding_Zoo | [8,4] | `:90:0.5` |
| Folding_Zoo | [8,6] | `:180:0.5` |
| Folding_Zoo | [8,8] | `:270:0.5` |

### Relationships

- first creature to use full fold progression (egg→stand→walk→fly); extends fold_demo_critter pattern
- Needs: FoldCritter base [has]; BoneFoldSolver [has]; procedural mesh [built here]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
