# Artifact: Fractal Recursion 2

> square(center, size, d) = box(size) + square(center, size * 0.5, d-1)

## Context

**Category:** unknown | **Complexity:** advanced
**Tags:** fractal | **Themes:** generation, procedural

Advanced recursion patterns with multiple levels of depth

*A square inside a square is not decoration — it is the first evidence that a function can call itself.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 3.0, 3.0] (room_scale)
- **Interaction:** To be stared into — concentric squares receding in Z, each layer a different color, pulling the eye inward
- **Critical Parameter:** size_reduction (0.5) — the ratio between parent and child determines whether the nesting feels dense or sparse
- **What Emerges:** A tunnel effect from flat geometry — depth perception from color gradient and z-stacking alone
- **Triggers:** max_depth increase → deeper nesting; z-offset per layer prevents z-fighting and creates parallax depth

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/example_8_2_recursion_vr.gd` (144 lines) |
| **Scene** | `res://algorithms/fractals/example_8_2_recursion_vr.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_depth` | int | 6 |
| `size_reduction` | float | 0.5 |
| `initial_size` | float | 1.0 |
| `show_frame` | bool | true |
| `frame_thickness` | float | 0.02 |

### Key Methods

- `_ready()`
- `_create_frame(center: Vector3, size: float)` -- Create 4 bars forming a frame around the pattern
- `_create_frame_bar(pos: Vector3, bar_size: Vector3, mat: StandardMaterial3D)`
- `_draw_recursive_pattern(center: Vector3, size: float, depth: int)`
- `_create_square(center: Vector3, size: float, depth: int)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_Recursion

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_Recursion | [2,8] | `:-90:2` |
| Fractal_Recursion | [3,8] | `:-90:2` |

### Relationships

- Simplest 2D recursion before Koch and Sierpinski; sibling to example_8_1 (concentric circles)
- Needs: VR depth control [missing], animation toggle [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
