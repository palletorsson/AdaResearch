# Artifact: Loss Function Comparator

> three landscapes from one dataset — the loss function is the author

## Context

**Category:** machinelearning | **Complexity:** intermediate
**Tags:** loss_function, gradient_descent, optimization, comparison, mse, huber, critical_theory | **Themes:** ai, education, machine_learning

Same dataset, three different loss functions applied simultaneously. MSE (smooth bowl), Huber (outlier-robust), and adversarial (deceptive valleys). Gradient descent balls roll on each surface, arriving at different answers. The landscape is not given — it is authored.

*the landscape is not given — it is authored*

## Design

- **Visual:** material: vertex_color, animation: vertex
- **Scale:** [2, 1, 1] (standard)
- **Interaction:** players see that optimization answers depend on the question you formalize
- **Critical Parameter:** outlier_fraction — how many data points are outliers
- **What Emerges:** the insight that loss functions are not discovered but designed
- **Triggers:** instantiation, dataset perturbation slider

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/loss_function_comparator/loss_function_comparator.gd` (424 lines) |
| **Scene** | `res://algorithms/machinelearning/loss_function_comparator/loss_function_comparator.tscn` |
| **Registry** | `machinelearning.json` |
| **Class** | `LossFunctionComparator` extends `Node3D` |
| **Pattern** | vr_interactable |

### Exports

| Name | Type | Default |
|------|------|---------|
| `terrain_resolution` | int | 32 |
| `terrain_size` | float | 0.5 |
| `terrain_height_scale` | float | 0.15 |
| `ball_radius` | float | 0.012 |
| `learning_rate` | float | 0.02 |
| `spacing` | float | 0.6 |
| `outlier_fraction` | float | 0.15 |

### Dependencies

- `res://commons/interactables/slider_horizontal.tscn`
- `res://commons/interactables/push_button.tscn`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_generate_dataset()`
- `_mse_loss(w: float, b: float)`
- `_huber_loss(w: float, b: float)`
- `_adversarial_loss(w: float, b: float)`
- `_eval_loss(loss_idx: int, w: float, b: float)`
- `_create_terrains()`
- `_build_terrain_mesh(loss_idx: int)`
- `_create_balls()`
- `_update_ball_visuals()`
- `_gradient_step(loss_idx: int)`
- `_create_labels()`
- `_create_title()`
- `_create_controls()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ML_Gradient_Landscape | [7,5] | `:0:0:0.6` |

### Relationships

- gradient_descent_landscape (single loss), optimization_algorithms (optimizer comparison)
- Needs: [implemented] 3 terrain meshes, gradient descent balls, Label3D equations, controls

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
