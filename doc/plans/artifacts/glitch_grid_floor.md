# Artifact: Glitch Grid Floor

> a floor encoding the visual language of digital corruption

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pixel_art, glitch, neon | **Themes:** arrays, art, cultural_computation

Procedural pixel art floor with digital noise aesthetic. Mostly black grid with ~18% of cells lit in hot pink, cyan, lime, red, or white using deterministic hash for reproducible glitch pattern.

*a deterministic hash applied to a grid is indistinguishable from randomness*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see scattered bright pixels like a glitched screen
- **Critical Parameter:** tiles_short — pixel resolution on the short axis
- **What Emerges:** the insight that a hash function deterministically maps coordinates to noise
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/glitch_grid_floor.gd` (169 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/glitch_grid_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `GlitchGridFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.2, 0.9) |
| `tiles_short` | int | 40 |
| `border_width` | int | 1 |
| `glitch_density` | float | 0.18 |

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_create_neon_material(color: Color, emission: bool = false)`
- `_hash2d(x: int, y: int)`
- `_build()`

### Grid Config

Accepts: `floor_size`, `glitch_density`, `tiles_short`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- pixel_invader_floor (aesthetic sibling), memphis_floor (scattered shapes cousin)
- Needs: [implemented] procedural mesh, hash-based coloring, neon palette

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
