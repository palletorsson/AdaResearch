# Artifact: Shape Learner (3D)

> 3D version of the GA+CA shape learner — evolving cellular automaton rules to produce target 3D structures. Emergence in three dimensions guided by evolutionary pressure.

## Context

**Category:** machinelearning | **Complexity:** advanced
**Tags:** genetic_algorithm, cellular_automaton, shape, 3d, evolution, hybrid | **Themes:** ai, biology, education, machine_learning

3D version of the GA+CA shape learner — evolving cellular automaton rules to produce target 3D structures. Emergence in three dimensions guided by evolutionary pressure.

## Design

- **Visual:** material: standard, animation: particle_lifecycle

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/shape_learner_3d/ShapeLearner3D.gd` (303 lines) |
| **Scene** | `res://algorithms/machinelearning/shape_learner_3d/ShapeLearner3D.tscn` |
| **Registry** | `machinelearning.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | - | 10 |
| `population_size` | - | 20 |
| `mutation_rate` | - | 0.01 |
| `generations` | - | 50 |
| `ca_generations` | - | 5 |
| `individuals_per_frame` | - | 5 |
| `target_shape_enum` | TargetShape | TargetShape.CUBE |

### Dependencies

- `res://commons/primitives/cubes/cube_scene.tscn`

### Key Methods

- `_ready()`
- `_process(_delta)` -- Process evaluation asynchronously
- `run_ga_generation()` -- Start async evaluation
- `finish_ga_generation()`
- `stamp_ca_generation()`
- `create_target_shape()`
- `initialize_population()`
- `evaluate_population_async()` -- Evaluate a few individuals per frame to avoid freezing
- `run_ca(rules)`
- `count_neighbors(grid, x, y, z)` -- Pre-calculate bounds to avoid repeated checks
- `calculate_fitness(grid)`
- `select_new_population()`
- `tournament_selection()`
- `crossover(parent1, parent2)`
- `mutate(child)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
