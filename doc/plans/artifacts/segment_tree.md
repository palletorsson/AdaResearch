# Artifact: Segment Tree

> Segment tree for range queries (min, max, sum) with lazy propagation. Each node stores an aggregate of its interval — answer any range query in O(log n) after O(n) construction.

## Context

**Category:** datastructures | **Complexity:** advanced
**Tags:** segment_tree, range_query, lazy_propagation, interval, data_structure | **Themes:** algorithms, data_structures

Segment tree for range queries (min, max, sum) with lazy propagation. Each node stores an aggregate of its interval — answer any range query in O(log n) after O(n) construction.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [9, 24, 21] (world_scale)

## Architecture

| | |
|---|---|
| **File** | [No dedicated GDScript found] |
| **Scene** | `res://algorithms/datastructures/segment_tree/segment_tree.tscn` |
| **Registry** | `datastructures.json` |
| **Pattern** | unknown |

## Curriculum Position

### Sequences

- datastructures

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
