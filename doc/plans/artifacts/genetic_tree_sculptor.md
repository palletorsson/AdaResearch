# Artifact: Genetic Tree Sculptor

> 8 sliders → CritterDNA → TreeMorphology.build() — design a tree's genome, watch it grow live

## Context

**Category:** procedural | **Complexity:** advanced
**Tags:** l_system, procedural, simulation, sorting, tree | **Themes:** biology, grammar, l_systems, procedural

Evolutionary L-system tree that uses genetic algorithm selection to sculpt tree forms — combines grammar-based growth with fitness-driven optimization.

*A tree is eight numbers — change them and you are doing what evolution does, but with intention.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** To sculpt a tree by tweaking its DNA — depth, forks, angle, taper, leaves, tropism, twist, arrangement
- **Critical Parameter:** branch_angle (15-90°) — the single gene that most visibly transforms the tree's character
- **What Emerges:** The player becomes a breeder — intuition about which genes produce which forms develops through play
- **Triggers:** Any slider move → debounced rebuild in 0.3s; Randomize → new genome; Plant → exports DNA for world placement

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/genetic_tree_sculptor/genetic_tree_sculptor.gd` (203 lines) |
| **Scene** | `res://commons/artifacts/genetic_tree_sculptor/genetic_tree_sculptor.tscn` |
| **Registry** | `lsystems.json` |
| **Class** | `GeneticTreeSculptor` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()` -- kingdom 0 = tree
- `_process(delta: float)`
- `_build_ui()`
- `_on_slider_moved(_value: float, gene_name: String)`
- `_apply_slider_to_dna(gene_name: String)`
- `_sync_sliders_from_dna()`
- `_rebuild_tree()`
- `_on_randomize()`
- `_on_export_dna()` -- Store designed DNA globally so the branching catalyst can read it
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `dna_seed`

## Curriculum Position

### Sequences

- **L-Systems: Rules Grow Structure** (lsystems) -- map: LSystems_Living

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| AutoGenTest | [3,10] | `-` |
| AutoGenTest2 | [8,8] | `-` |
| LSystems_Living | [4,4] | `-` |

### Relationships

- Central to LSystems_Living. Uses CritterDNA and TreeMorphology systems. Bridges to evolution concepts.
- Needs: VR sliders [has, 8 sliders], push buttons [has, Random + Plant], Label3D [has via slider labels]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
