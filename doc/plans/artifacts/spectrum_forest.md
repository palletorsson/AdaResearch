# Artifact: spectrum_forest

> hue(x) = remap(x, bounds) — 5000 vertical lines colored by x-position across the visible spectrum

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** color | **Themes:** color, perception

spectrum_forest.

*the spectrum is continuous but we discretize it into lines — the gaps between lines are the same lie as RGB*

## Design

- **Visual:** material: vertex_color, animation: static
- **Scale:** [1.0, 9.0, 6.0] (environment)
- **Interaction:** to walk through a rain of color, each step shifting the hue around you from violet to red
- **Critical Parameter:** line_count — at low counts the spectrum is sparse and abstract, at high counts it becomes an immersive curtain
- **What Emerges:** the density of lines creates an accidental fog effect where distant colors blend perceptually, demonstrating optical mixing
- **Triggers:** none at runtime — static procedural sculpture generated once from deterministic seed

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/spectrumforest/spectrum_forest.gd` (132 lines) |
| **Scene** | `res://algorithms/color/spectrumforest/spectrum_forest.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `forest_size` | Vector2 | Vector2(5.0, 5.0): |
| `line_count` | int | 5000: |
| `line_length` | float | 3.0: |
| `ceiling_height` | float | 4.0: |

### Key Methods

- `_ready()`
- `_setup_mesh_instance()`
- `generate_forest()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Accepts: `ceiling_height`, `forest_depth`, `forest_width`, `line_count`, `line_length`

## Curriculum Position

### Sequences

- **Color: Perception, Not Physics** (color) -- map: Color_Grid_Pallet

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Grid_Pallet | [6,5] | `:0:2` |

### Relationships

- contrasts with dark_side_prism (linear spectrum vs spatial spectrum); feeds into SpectrumVisualizer (animated spectrum vs static)
- Needs: VR walkthrough [has]; line_count slider [missing]; spectrum range control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
