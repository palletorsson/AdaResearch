# Artifact: Fibonacci Terrain

> terrain(faces) = select_non_edge(faces) → lift(golden_ratio_scaled) → subdivide_golden(lifted) → repeat

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** graph, probability, procedural, recursion, terrain | **Themes:** generation, procedural

Terrain generated through golden ratio subdivision - plane subdivides, random non-edge faces lift and subdivide. Presets: gentle_hills, mountain_peak, plateau, archipelago, fibonacci_spiral

*Terrain is not random — it is a surface that remembers the golden ratio in every fold and ridge.*

## Design

- **Visual:** material: vertex_color, animation: vertex
- **Scale:** [20.0, 5.0, 20.0] (world_scale)
- **Interaction:** To grow mountains — the terrain heaves upward iteration by iteration, golden spiral overlay tracing the growth pattern
- **Critical Parameter:** prefer_center_lift — when true, mountains grow from the center outward; when false, peaks scatter randomly
- **What Emerges:** The golden spiral overlay tracks the terrain — a line of gold traces phi's fingerprint across the landscape
- **Triggers:** growth_delay tick → select faces → lift by phi-scaled height → subdivide along golden ratio → rebuild mesh

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/fibonacci_terrain/fibonacci_terrain.gd` (557 lines) |
| **Scene** | `res://algorithms/fractals/fibonacci_terrain/fibonacci_terrain.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `initial_size` | - | - |
| `max_iterations` | - | - |
| `initial_subdivisions` | - | - |
| `max_faces` | - | - |
| `lift_height_base` | - | - |
| `lift_height_variance` | - | - |
| `subdivision_depth_per_lift` | - | - |
| `faces_to_lift_per_iteration` | - | - |
| `prefer_center_lift` | - | - |
| `animate_growth` | - | - |
| `growth_delay` | - | - |
| `zoom_out_per_iteration` | - | - |
| `base_color` | - | - |
| `peak_color` | - | - |
| `use_golden_spiral_overlay` | - | - |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_initialize_terrain()`
- `_perform_growth_iteration()`
- `_lift_and_subdivide_face(face_idx: int)`
- `_subdivide_face_golden(face_idx: int)`
- `_create_face_dict(corners: Array, height: float, subdivisions: int)`
- `_subdivide_all_faces()` -- IMPORTANT: Capture the count ONCE before subdividing
- `_recalculate_edges()` -- Find bounding box
- `_rebuild_mesh()` -- Remove old mesh
- `_add_face_sides(surface_tool: SurfaceTool, face: Dictionary, top_color: Color)`
- `_update_spiral_overlay()`
- `_get_terrain_height_at(pos: Vector2)`
- `_point_in_face(point: Vector2, face: Dictionary)`
- `_generate_full_terrain()`

### Signals

- `iteration_complete`
- `terrain_complete`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_GoldenSpiral
- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_JuliaSet

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_GoldenSpiral | [9,2] | `#preset:deep` |

### Relationships

- Terrain counterpart to golden_rectangle (2D phi subdivision); uses the same 1/phi split ratio
- Needs: VR walking on terrain [missing], preset selection [has], spiral toggle [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
