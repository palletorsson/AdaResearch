# Artifact: Ecosystem Simulation

> population(t+1) = reproduce(survive(eat(move(population(t))))) with DNA crossover and mutation at each birth — evolution in a closed box

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** algorithm | **Themes:** algorithms, misc

Predator-prey ecosystem dynamics with emergent population behaviors

*evolution has no goal — only the dead are wrong, and only the living get to try again*

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** to watch a predator-prey cycle spike and crash and recover — to feel the ecosystem breathe and realize no agent understands the cycle, only you can see it
- **Critical Parameter:** mutation_rate — at 0 the ecosystem either freezes in its starting configuration or collapses; at 0.1 adaptation keeps prey one step ahead of predators indefinitely
- **What Emerges:** prey develop faster flee_weight and higher perception_radius over generations without explicit selection pressure — predation IS the selection pressure
- **Triggers:** balance_ecosystem() acts as a hidden stabilizer that respawns prey below 10 and weakens predators when they outnumber prey by 2:1 — remove it and the system collapses in minutes

## Architecture

| | |
|---|---|
| **File** | `algorithms/emergentsystems/ecosystemsimulation/ecosystem.gd` (332 lines) |
| **Scene** | `res://algorithms/emergentsystems/ecosystemsimulation/ecosystem.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_exit_tree()`
- `_setup_multimeshes()` -- Prey — blue spheres
- `_setup_label()`
- `_sync_multimeshes()` -- Prey
- `_update_label()`
- `update_prey(delta: float)`
- `update_predators(delta: float)`
- `check_for_mate(creature, is_predator: bool = false)`
- `crossover_dna(dna1: Dictionary, dna2: Dictionary)`
- `mutate_dna(dna: Dictionary)`
- `spawn_prey(pos: Vector3, dna_values: Dictionary = {})`
- `spawn_predator(pos: Vector3, dna_values: Dictionary = {})`
- `spawn_food(amount: int)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Swarm Intelligence: No Leader, Yet Coordinated** (swarmintelligence) -- map: SwarmIntelligence_Swarm_Intelligence_Algorithms

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| AdvancedLaboratory_Systems_Lab | [4,1] | `-` |
| AutoGenTest2 | [12,12] | `-` |
| SwarmIntelligence_Swarm_Intelligence_Algorithms | [4,4] | `-` |

### Relationships

- synthesis artifact for the swarmintelligence sequence; combines local movement rules from boids with genetic memory from neuroevolution; previews machinelearning sequence's evolutionary algorithms
- Needs: [missing] no VR controls at all; prey_population, predator_population, and mutation_rate are hardcoded; learner cannot experiment with initial conditions

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
