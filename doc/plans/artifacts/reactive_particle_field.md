# Artifact: Reactive Particle Field

> A field of 100 particles that respond to lambda and phi. Low λ: ordered grid. Edge λ: swirling dance. High λ: chaotic swarm. Phi affects dynamics. The formula made physical.

## Context

**Category:** qfep | **Complexity:** intermediate
**Tags:** 2d, graph, math, particle, qfep, swarm | **Themes:** philosophy, qfep, theory

A field of 100 particles that respond to lambda and phi. Low λ: ordered grid. Edge λ: swirling dance. High λ: chaotic swarm. Phi affects dynamics. The formula made physical.

## Design

- **Visual:** material: standard, animation: physics

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/reactive_particle_field.gd` (192 lines) |
| **Scene** | `res://commons/interfaces/qfep/reactive_particle_field.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `ReactiveParticleField` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `particle_count` | - | - |
| `field_size` | - | - |

### Key Methods

- `_ready()`
- `_create_particles()`
- `_create_single_particle(index: int)`
- `_calculate_grid_positions()` -- Calculate ordered grid positions for when lambda is low
- `_physics_process(delta)`
- `_update_particles(_delta)`
- `_get_current_color()`
- `on_lambda_changed(lambda: float)`
- `on_phi_changed(phi: float)`
- `_connect_to_sliders()`

## Curriculum Position

### Sequences

- qfeplaboratory

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
