# Artifact: Wireworld Circuit

> Wireworld cellular automaton — 4-state CA for logic gates and signal routing. Displays an OR gate with clock inputs on a floor display, demonstrating how simple state-transition rules produce computational behavior.

## Context

**Category:** emergence | **Complexity:** advanced
**Tags:** 2d, cellular_automata, emergence, visualization | **Themes:** biology, cellular_automata, computation, emergence

Wireworld cellular automaton — 4-state CA for logic gates and signal routing. Displays an OR gate with clock inputs on a floor display, demonstrating how simple state-transition rules produce computational behavior.

## Design

- **Visual:** material: generated_texture, animation: transform

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/wireworld_circuit/wireworld_circuit.gd` (223 lines) |
| **Scene** | `res://commons/artifacts/wireworld_circuit/wireworld_circuit.tscn` |
| **Registry** | `cellular_automata.json` |
| **Class** | `WireworldCircuit` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `display_size` | float | 0.8 |
| `grid_resolution` | int | 32 |
| `steps_per_frame` | int | 1 |
| `step_interval` | float | 0.15 |

### Key Methods

- `_ready()`
- `_create_display()`
- `_build_circuit()` -- Clear grid
- `_set_cell(x: int, y: int, state: int)`
- `_set_wire_line(x0: int, y0: int, x1: int, y1: int)` -- Draw a horizontal or vertical wire line (inclusive endpoints)
- `_process(delta: float)`
- `_simulation_step()`
- `_count_head_neighbors(x: int, y: int)`
- `_update_texture()`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `step_interval`, `steps_per_frame`

## Curriculum Position

### Sequences

- cellularautomata

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
