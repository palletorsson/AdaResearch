# Artifact: Rotation Oscillation Cube

> Stage 3 of oscillation progression: θ = A·sin(ωt) applied to rotation instead of position. Same mathematics, different output mapping. Arc indicator shows rotation range, direction arrow on cube face. Toggle between oscillating mode (pendulum-like) and continuous rotation (θ += ω·dt).

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** interactive, wave | **Themes:** oscillation, physics, waves

Stage 3 of oscillation progression: θ = A·sin(ωt) applied to rotation instead of position. Same mathematics, different output mapping. Arc indicator shows rotation range, direction arrow on cube face. Toggle between oscillating mode (pendulum-like) and continuous rotation (θ += ω·dt).

## Design

- **Visual:** material: shader, animation: vertex
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/rotation_oscillation_cube/rotation_oscillation_cube.gd` (376 lines) |
| **Scene** | `res://commons/artifacts/rotation_oscillation_cube/rotation_oscillation_cube.tscn` |
| **Registry** | `wavefunctions.json` |
| **Class** | `RotationOscillationCube` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cube_size` | float | 0.3 |
| `amplitude_degrees` | float | 45.0 |
| `frequency` | float | 0.8 |
| `cube_color` | Color | Color(0.8, 0.5, 0.2) |
| `continuous_mode` | bool | false |
| `show_arc` | bool | true |
| `show_direction_arrow` | bool | true |

### Dependencies

- `res://commons/primitives/cubes/cube_scene.tscn`
- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_cube()`
- `_apply_cube_color()`
- `_create_arc_indicator()`
- `_update_arc_mesh()`
- `_create_direction_arrow()`
- `_create_labels()`
- `_create_center_marker()`
- `_process(delta)`
- `_update_labels()`
- `_update_color_feedback()`
- `set_mode(continuous: bool)`
- `set_amplitude(degrees: float)`
- `set_frequency(f: float)`
- `get_current_angle_degrees()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Tutorial_Start | [2,2] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
