# Artifact: Traffic Cone

> Orange traffic cone with white stripes and square base. Construction site safety marker.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

Orange traffic cone with white stripes and square base. Construction site safety marker.

## Design

- **Visual:** material: vertex_color, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/trafficcone/trafficcone.gd` (243 lines) |
| **Scene** | `res://commons/primitives/trafficcone/trafficcone.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `orange_color` | Color | Color(1.0, 0.4, 0.0) |
| `white_color` | Color | Color(1.0, 1.0, 1.0) |
| `height` | float | 0.7 |
| `base_radius` | float | 0.15 |
| `top_radius` | float | 0.02 |
| `radial_segments` | int | 12 |
| `stripe_count` | int | 2 |

### Dependencies

- `res://commons/primitives/shared/grid_material_factory.gd`

### Key Methods

- `_ready()`
- `_build_traffic_cone()` -- Clean up existing
- `_build_cone_with_stripes()` -- We'll create multiple ring sections with alternating colors
- `_add_cone_section(st: SurfaceTool, start_t: float, end_t: float, color: Color)` -- Calculate radii at start and end of section
- `_build_base()` -- Square base plate
- `_create_collision()`
- `set_orange_color(color: Color)`
- `_init()` -- Apply vertex color material to cone when ready
- `_enter_tree()` -- Apply proper material after build
- `_apply_materials()`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
