# Artifact: Instancing Scatter

> High-performance instance scattering algorithm for procedural object placement and distribution

## Context

**Category:** procedural | **Complexity:** advanced
**Tags:** procedural | **Themes:** probability, procedural, randomness

High-performance instance scattering algorithm for procedural object placement and distribution

## Design

- **Visual:** material: unlit, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/noise/scatter/instancingscatter.gd` (477 lines) |
| **Scene** | `res://algorithms/randomness/noise/scatter/instancingscatter.tscn` |
| **Registry** | `randomness.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `instance_count` | int | 5000 |
| `scatter_radius` | float | 20.0 |
| `animation_speed` | float | 1.0 |
| `object_scale` | float | 0.3 |

### Key Methods

- `_ready()`
- `setup_scene()` -- Create environment
- `create_ambient_lights()` -- Main directional light
- `create_surface_mesh()` -- Create a complex surface for scattering - like terrain or organic shapes
- `create_terrain_collision()` -- Create collision shape from terrain mesh
- `generate_scatter_points()` -- Sample points across the surface mesh
- `create_instanced_objects()` -- Create crystals
- `create_crystal_instances()`
- `create_flower_instances()`
- `create_particle_instances()`
- `create_crystal_mesh()`
- `create_flower_mesh()` -- Simple flower made of quads
- `start_animations()` -- Gentle swaying animation for crystals
- `animate_crystals(time: float)`
- `animate_particles(time: float)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Unused** (unused) -- map: One_Adapt_6

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| One_Adapt_6 | [2,3] | `-` |
| TestMap_Unused_8 | [1,3] | `:0:2` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
