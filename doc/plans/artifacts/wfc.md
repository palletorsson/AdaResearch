# Artifact: Wave Function Collapse

> Wave Function Collapse algorithm â€” generating complex patterns from local adjacency constraints. Start with superposition of all possibilities, observe one tile, propagate constraints until the world resolves.

## Context

**Category:** proceduralgeneration | **Complexity:** advanced
**Tags:** wfc, wave_function_collapse, constraint, procedural, generation, tile | **Themes:** generation, procedural

Wave Function Collapse algorithm â€” generating complex patterns from local adjacency constraints. Start with superposition of all possibilities, observe one tile, propagate constraints until the world resolves.

## Design


## Architecture

| | |
|---|---|
| **File** | `algorithms/proceduralgeneration/constraint_solvers/wave_function_collapse/WFC.gd` (436 lines) |
| **Scene** | `res://algorithms/proceduralgeneration/constraint_solvers/wave_function_collapse/WFC.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | Vector3i | Vector3i(10, 5, 10) |
| `cell_size` | float | 2.0 |
| `generation_speed` | float | 0.1 |
| `auto_generate` | bool | true |
| `show_debug_info` | bool | true |
| `tile_textures` | Array[Texture2D] | [] |
| `use_default_edges` | bool | true |

### Key Methods

- `_ready()`
- `setup_tiles()`
- `_should_generate_rotations(tile_index: int)` -- Don't rotate symmetric tiles (0, 1, 7, 10)
- `_generate_rotations(base_tile: WFCBasicTile)` -- Generate Y-axis rotations (90°, 180°, 270°)
- `_rotate_edges_y(edges: Array[String], rotations: int)` -- Rotate horizontal edges (keep top/bottom, rotate north/south/east/west)
- `_is_duplicate_tile(new_tile: WFCBasicTile)`
- `_create_default_textures()` -- Create simple colored textures if none provided
- `initialize_grid()`
- `start_generation()`
- `_process(delta: float)`
- `wfc_step()` -- Get uncollapsed cells
- `_update_cell_visual(cell: WFCCell)`
- `_propagate_constraints(changed_cell: WFCCell)`
- `_get_neighbors(pos: Vector3i)`
- `_get_valid_options_for_direction(cell: WFCCell, direction: String)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
