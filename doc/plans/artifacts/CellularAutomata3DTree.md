# Artifact: CellularAutomata3DTree

> square_layer(n) → ca_prune(edge, isolation, random) → tree silhouette

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** cellular_automata | **Themes:** biology, cellular_automata, computation, emergence

CellularAutomata3DTree

*A tree is what remains when you remove everything that isn't tree.*

## Design

- **Visual:** material: standard, animation: particle_lifecycle
- **Scale:** [1.0, 6.0, 11.0] (world_scale)
- **Interaction:** To grow upward layer by layer, pruning itself into a tree shape through local elimination rules
- **Critical Parameter:** edge_pruning_chance — controls how aggressively corners are carved, shaping the crown profile
- **What Emerges:** Tree-like silhouettes from rectangular layers and three simple removal rules — no tree model was coded
- **Triggers:** High isolation_pruning → skeletal branches; low → dense canopy; changing base_size → bonsai vs oak

## Architecture

| | |
|---|---|
| **File** | `algorithms/cellularautomata/cellular_automata_3d_tree/CellularAutomata3DTree.gd` (225 lines) |
| **Scene** | `res://algorithms/cellularautomata/cellular_automata_3d_tree/CellularAutomata3DTree.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `base_size` | int | 4 |
| `max_height` | int | 20 |
| `generation_interval` | float | 0.1 |
| `auto_play` | bool | true |
| `gradient` | Gradient | - |
| `edge_pruning_chance` | float | 0.4 |
| `isolation_pruning_chance` | float | 0.5 |
| `random_pruning_base` | float | 0.1 |

### Key Methods

- `_ready()` -- Try to get existing MultiMeshInstance3D node, or create one
- `_process(delta: float)`
- `_setup_multimesh()`
- `_setup_default_gradient()` -- Default gradient has 2 points at 0.0 and 1.0
- `start_growth()`
- `grow_next_level()` -- Expansion phase: levels 8-10
- `create_square_layer(level: int, size: int)`
- `apply_ca_pruning(level: int, size: int)`
- `count_neighbors_2d(x: int, level: int, z: int, size: int)`
- `_update_multimesh()`
- `_get_size_for_level(level: int)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Relationships

- Feeds into CA_ExpandingSpace. Contrasts with lsystem_tree (subtractive CA vs additive grammar).
- Needs: VR pruning sliders [missing], growth speed control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
