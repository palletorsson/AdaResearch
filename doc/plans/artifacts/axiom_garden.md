# Artifact: Axiom Garden

> An L-System garden game — grow plants from axiom strings, navigate a turtle through branching structures, overcome obstacles with evolved grammar rules. Formal languages become living landscapes.

## Context

**Category:** machinelearning | **Complexity:** advanced
**Tags:** axiom, garden, l_system, game, turtle, grammar, evolution | **Themes:** ai, machine_learning

An L-System garden game — grow plants from axiom strings, navigate a turtle through branching structures, overcome obstacles with evolved grammar rules. Formal languages become living landscapes.

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [3.0, 9.0, 9.0] (environment)

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/thegame_a/AxiomGarden.gd` (183 lines) |
| **Scene** | `res://algorithms/machinelearning/thegame_a/AxiomGarden.tscn` |
| **Registry** | `machinelearning.json` |
| **Class** | `AxiomGarden` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `generations` | int | 4 |
| `step_length` | float | 0.5 |
| `angle` | float | 25.0 |
| `max_segments` | int | 2000 |

### Key Methods

- `_ready()` -- Initialize components
- `load_solution(index: int)`
- `_on_next_pressed()`
- `_on_prev_pressed()`
- `grow_garden()` -- 1. Generate String
- `check_collisions(lines: Array)`
- `check_goals(lines: Array)`
- `_on_grow_pressed()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
