# Artifact: Transform Composition

> Transform composition demonstrator showing that matrix multiplication order matters. A simple house shape undergoes Rotate-then-Translate (blue) vs Translate-then-Rotate (red) — landing in different places. Displays R·T ≠ T·R with live 4x4 matrices. VR sliders control rotation angle and translation distance. Buttons toggle order A, order B, or both. Animated step-through shows intermediate states.

## Context

**Category:** transforms | **Complexity:** advanced
**Tags:** interactive, linear_algebra, transforms, vr | **Themes:** geometry, math, transforms

Transform composition demonstrator showing that matrix multiplication order matters. A simple house shape undergoes Rotate-then-Translate (blue) vs Translate-then-Rotate (red) — landing in different places. Displays R·T ≠ T·R with live 4x4 matrices. VR sliders control rotation angle and translation distance. Buttons toggle order A, order B, or both. Animated step-through shows intermediate states.

## Design

- **Visual:** material: vertex_color, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/transform_composition/transform_composition.gd` (482 lines) |
| **Scene** | `res://commons/artifacts/transform_composition/transform_composition.tscn` |
| **Registry** | `transforms.json` |
| **Class** | `TransformComposition` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `rotation_angle` | float | 45.0 |
| `translate_dist` | float | 0.3 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_build_axes()`
- `_build_shapes_mesh()`
- `_redraw()`
- `_draw_house(im: ImmediateMesh, verts: Array, col: Color, alpha: float)`
- `_make_rotate_y(rad: float)`
- `_make_translate(offset: Vector3)`
- `_transform_verts(verts: Array, xform: Transform3D)`
- `_compose_matrix(first: Transform3D, second: Transform3D)`
- `_format_matrix(xf: Transform3D)` -- Display as 4x4 homogeneous matrix
- `_build_labels()` -- Title
- `_update_matrix_labels()`
- `_update_info()`
- `_build_vr_controls()`
- `_sync_slider(slider: Node, normalized: float)`

### Grid Config

Accepts: `rotation_angle`, `translate_dist`

## Curriculum Position

### Sequences

- transformation

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
