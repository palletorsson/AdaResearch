# Artifact: Barnsley Fern

> Classic Barnsley fern rendered via iterated function system — four affine transforms with weighted probabilities produce the iconic fractal fern shape as a point cloud on a floor display. Density-based coloring reveals the self-similar structure.

## Context

**Category:** procedural | **Complexity:** beginner
**Tags:** barnsley, fern, fractal, ifs, iterated_function_system, point_cloud | **Themes:** commons, mixed, procedural

Classic Barnsley fern rendered via iterated function system — four affine transforms with weighted probabilities produce the iconic fractal fern shape as a point cloud on a floor display. Density-based coloring reveals the self-similar structure.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/barnsley_fern/barnsley_fern.gd` (151 lines) |
| **Scene** | `res://commons/artifacts/barnsley_fern/barnsley_fern.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `BarnsleyFern` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `quad_size` | Vector2 | Vector2(0.6, 0.8) |
| `num_points` | int | 50000 |

### Key Methods

- `_ready()`
- `_build_floor_quad()`
- `_generate_texture()` -- Run the IFS to collect all points
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `num_points`, `seed`

## Curriculum Position

### Sequences

- fractals

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
