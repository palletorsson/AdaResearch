# Artifact: Bucket of Tulips

> A charming metal bucket filled with colorful tulips, bringing natural beauty and algorithmic growth patterns to the laboratory space

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** primitive | **Themes:** core, primitives

A charming metal bucket filled with colorful tulips, bringing natural beauty and algorithmic growth patterns to the laboratory space

## Design

- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/computationalbiology/bucketoftulips/bucket_of_tulips.gd` (769 lines) |
| **Scene** | `res://commons/primitives/plants/bucket_of_tulips.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `tulip_count` | int | 21 |
| `bucket_radius` | float | 0.35 |
| `bucket_height` | float | 0.28 |
| `max_stem_height` | float | 0.45 |
| `step_speed` | float | 1.0 |
| `golden_angle_deg` | float | 137.508 |
| `radial_packing` | float | 0.06 |
| `growth_duration` | float | 12.0 |
| `heat_level` | float | 0.0 |
| `drought_level` | float | 0.0 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_create_materials()`
- `_create_mesh_instances()`
- `_create_bucket()`
- `_init_tulip_data()`
- `_init_mode()`
- `_cycle_mode()`
- `_draw_tulips()`
- `_draw_single_tulip(d: Dictionary)`
- `_draw_flower_head(d: Dictionary, tip: Vector3, stem_dir: Vector3, bloom: float, wilt: float)` -- Wilting desaturates and browns the petals
- `_draw_leaf(leaf_base: Vector3, angle: float, col: Color, growth: float, wilt: float)`
- `_draw_phyllotaxis_spiral()`
- `_advance_growth(delta: float)`
- `_update_environment(delta: float)` -- Read slider values

### Grid Config

Accepts: `bucket_height`, `bucket_radius`, `drought_level`, `golden_angle_deg`, `growth_duration`, `heat_level`, `max_stem_height`, `mode`, `radial_packing`, `step_speed`, `tulip_count`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
