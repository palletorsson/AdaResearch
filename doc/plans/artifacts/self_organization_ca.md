# Artifact: Self Organization CA

> cell(t+1) = cell(t) * (1 - k) + avg(neighbors) * k + noise — local averaging with perturbation

## Context

**Category:** emergence | **Complexity:** advanced
**Tags:** emergence | **Themes:** cellular_automata, computation, emergence

Self-organizing patterns simulation.

*Average with your neighbors and add noise — domains form, boundaries emerge, organization happens without an organizer.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** To settle into consensus regions, then be disturbed — watch opinion clusters form and dissolve
- **Critical Parameter:** INTERACTION_STRENGTH (0.1) — how fast cells converge toward their neighbors' average
- **What Emerges:** Domain boundaries and cluster formation from simple averaging — phase separation without physics
- **Triggers:** High interaction → rapid homogenization into uniform zones; randomness prevents total consensus

## Architecture

| | |
|---|---|
| **File** | `algorithms/cellularautomata/ca_showcase/self_organization_ca.gd` (135 lines) |
| **Scene** | `res://algorithms/cellularautomata/ca_showcase/self_organization_ca.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | procedural |

### Key Methods

- `initialize_grid()` -- Configure MultiMesh
- `update_simulation(_delta)` -- Evolve self-organizing patterns
- `evolve_self_organization()`
- `update_visualization()`
- `get_state_color(state: int)` -- Color-code different states
- `get_pattern_diversity()`
- `reset_simulation()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Cellular Automata: Local Rules, Global Patterns** (cellularautomata) -- map: CA_EdgeOfChaos

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CA_EdgeOfChaos | [2,4] | `-` |

### Relationships

- Feeds into CA_EdgeOfChaos. Demonstrates self-organization as the CA analogue of social consensus.
- Needs: VR interaction/randomness sliders [missing], diversity readout [has via get_pattern_diversity]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
