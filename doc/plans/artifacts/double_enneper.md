# Artifact: Double Enneper Surface

> Extended Enneper minimal surface with self-intersecting folds. A higher-order minimal surface where mean curvature vanishes everywhere despite complex geometry.

## Context

**Category:** geometry | **Complexity:** advanced
**Tags:** parametric, minimal_surface, enneper, self_intersection, pickable | **Themes:** curves, math, parametric

Extended Enneper minimal surface with self-intersecting folds. A higher-order minimal surface where mean curvature vanishes everywhere despite complex geometry.

## Design

- **Visual:** material: shader, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/parametric/double_enneper.gd` (120 lines) |
| **Scene** | `res://commons/primitives/parametric/double_enneper.tscn` |
| **Registry** | `parametric.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `u_min` | float | -2.0 |
| `u_max` | float | 2.0 |
| `u_steps` | int | 64 |
| `v_min` | float | -2.0 |
| `v_max` | float | 2.0 |
| `v_steps` | int | 64 |
| `scale_factor` | float | 0.15 |
| `base_color` | Color | Color(0.8, 0.4, 0.9) |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `create_parametric_surface()`
- `apply_material()`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
