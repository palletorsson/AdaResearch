# Artifact: Sierpinski Pyramid

> pyramid(depth) = 5 * pyramid(depth-1) — top + four corners, recursively

## Context

**Category:** emergence | **Complexity:** advanced
**Tags:** emergence | **Themes:** cellular_automata, computation, emergence

Recursive Sierpinski Pyramid construction using cubes.

*Five copies of yourself, arranged just so, and you contain yourself forever.*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To be climbed and understood — a fractal mountain where every face is itself
- **Critical Parameter:** depth — each increment multiplies complexity by 5, from 5 cubes to 3125
- **What Emerges:** Gaps and voids at every scale — the pyramid is more hole than solid, yet structurally coherent
- **Triggers:** Depth 1 → five cubes; depth 4 → a mountain of self-similar structure; @tool → live preview in editor

## Architecture

| | |
|---|---|
| **File** | `algorithms/cellularautomata/sierpinski_pyramid/SierpinskiPyramid.gd` (153 lines) |
| **Scene** | `res://algorithms/cellularautomata/sierpinski_pyramid/SierpinskiPyramid.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `depth` | int | 4: |
| `size` | float | 1.0: |
| `generate_on_ready` | bool | true |

### Dependencies

- `res://commons/primitives/cubes/cube_scene.tscn`

### Key Methods

- `_ready()`
- `generate()` -- Clear existing children
- `_recursive_build(pos: Vector3, current_size: float, current_depth: int)`
- `_spawn_cube(pos: Vector3)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Cellular Automata: Local Rules, Global Patterns** (cellularautomata) -- map: CA_AgentsCircuits
- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_KochSierpinski
- **Recursive Emergence** (recursiveemergence) -- map: RecursiveEmergence_Julia_Set

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CA_AgentsCircuits | [5,3] | `:0:6` |
| Fractal_KochSierpinski | [8,5] | `-` |
| RecursiveEmergence_Julia_Set | [2,2] | `-` |

### Relationships

- Feeds into CA_AgentsCircuits. Bridges fractals sequence to cellularautomata — Sierpinski via recursion, not CA rules.
- Needs: VR depth control [missing], scale adjustment [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
