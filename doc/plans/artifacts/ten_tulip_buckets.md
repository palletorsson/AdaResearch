# Artifact: Ten Tulip Buckets

> Array of procedurally generated tulip bouquets — each bucket grows tulips from parametric curves with randomized variations. Botanical algorithms producing natural beauty.

## Context

**Category:** biology | **Complexity:** beginner
**Tags:** tulip, plant, procedural, botanical, biology, array | **Themes:** biology, simulation

Array of procedurally generated tulip bouquets — each bucket grows tulips from parametric curves with randomized variations. Botanical algorithms producing natural beauty.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/computationalbiology/bucketoftulips/ten_tulip_buckets.gd` (251 lines) |
| **Scene** | `res://algorithms/computationalbiology/bucketoftulips/ten_tulip_buckets.tscn` |
| **Registry** | `computational_biology.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `bucket_spacing` | float | 3.0 |
| `grid_size` | int | 5 |
| `randomize_colors` | bool | true |
| `randomize_positions` | bool | true |

### Dependencies

- `res://algorithms/computationalbiology/bucketoftulips/bucket_of_tulips.tscn`

### Key Methods

- `_ready()`
- `setup_environment()` -- Create enhanced lighting
- `create_sky()`
- `create_ten_buckets()` -- Load the bucket of tulips scene
- `randomize_bucket_properties(bucket_instance, _index)` -- Randomize scale slightly
- `randomize_bucket_colors(bucket_instance)` -- Find and randomize bucket material
- `setup_camera_controls()` -- Create camera controller
- `setup_ui()` -- Create UI canvas
- `_on_regenerate_pressed()` -- Clear existing buckets
- `_on_toggle_randomization_pressed()`
- `_input(event: InputEvent)` -- Handle keyboard input
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
