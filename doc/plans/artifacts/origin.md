# Artifact: Point Zero

> (0, 0, 0) — the reference from which all coordinates are measured

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

origin

*the origin is a convention, not a place — but every space must agree on one*

## Design

- **Visual:** material: standard, animation: vertex
- **Scale:** [2.0, 2.0, 2.0] (compact)
- **Interaction:** learner viscerally locates themselves relative to the world's fixed anchor
- **Critical Parameter:** spinning octahedron at origin, cycling alias labels (origin / home / zero / (0,0,0))
- **What Emerges:** understanding that "zero" is a choice, not a given — every coordinate system has an origin
- **Triggers:** proximity — labels cycle on a timer to reinforce multiple names for the same concept

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/origin/origin.gd` (143 lines) |
| **Scene** | `res://commons/primitives/origin/origin.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `octahedron_size` | float | 0.05 |
| `rotation_speed` | float | 0.5 |
| `octahedron_color` | Color | Color(0.0, 1.0, 1.0) |

### Key Methods

- `_ready()`
- `_process(delta: float)` -- Rotate the octahedron
- `create_origin_marker()`
- `_create_octahedron_mesh(size: float)`
- `_add_triangle(st: SurfaceTool, v0: Vector3, v1: Vector3, v2: Vector3)`
- `_start_origin_alias_cycle()`
- `_on_origin_alias_timeout()`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Point_One
- **Unused Artifacts Test** (testmaps) -- map: Point_Zero
- **Unused Artifacts Test** (testmaps) -- map: test_cctv

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| _Example_Island | [2,2] | `:180:-0.5` |
| _Example_Linear | [2,2] | `:180:-0.5` |
| Point_One | [0,0] | `:180:-0.5` |
| Point_Zero | [0,0] | `:180:0.5` |
| Point_Zero_ | [0,0] | `:180:-1` |
| test_cctv | [0,0] | `-` |

### Relationships

- prerequisite for all spatial reasoning; every other artifact measures itself from here
- Needs: [missing VR controls — atmospheric/reference object only]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
