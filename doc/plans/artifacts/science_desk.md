# Artifact: science_desk

> a lab workstation with cabinets, countertop equipment, and built-in lighting.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** qfep | **Themes:** philosophy, qfep, theory

a lab workstation with cabinets, countertop equipment, and built-in lighting.

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [2, 1, 1] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/criticaltheory/sciencedesk/science_desk.gd` (387 lines) |
| **Scene** | `res://algorithms/criticaltheory/sciencedesk/science_desk.tscn` |
| **Registry** | `qfep.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `create_science_desk()` -- Create the main desk structure
- `create_desk_base()`
- `add_desk_legs(desk_base, desk_size)` -- Slightly darker than the desk
- `create_cabinets()`
- `add_cabinet_handle(cabinet, is_drawer)` -- Light gray handles
- `create_countertop()`
- `add_lab_equipment(countertop)` -- Add a sink
- `add_beakers_and_bottles(countertop)` -- Create a few beakers
- `create_beaker(liquid_color)`
- `create_lab_bottle()`
- `create_microscope()`
- `add_lighting(desk_base)` -- Add subtle lighting to illuminate the desk
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
