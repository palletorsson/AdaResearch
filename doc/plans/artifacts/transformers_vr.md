# Artifact: Transformers_VR

> Attention(Q,K,V) = softmax(QK^T / √d_k)V — tokens attend to all other tokens via learned queries and keys

## Context

**Category:** unknown | **Complexity:** advanced
**Tags:** machine_learning | **Themes:** ai, machine_learning

Interactive transformer with attention beam visualization

*attention is all you need — the transformer proves that explicit memory gates are unnecessary when every token can directly query every other token*

## Design

- **Visual:** material: unlit, animation: physics
- **Scale:** [9, 10, 11] (xlarge)
- **Interaction:** walk through a token sequence, see attention beams connect distant tokens, switch between attention heads
- **Critical Parameter:** num_attention_heads — each head learns to attend to different relationship types
- **What Emerges:** multi-head attention patterns — some heads attend locally, others capture long-range dependencies, nobody assigns these roles
- **Triggers:** auto_train drives loss descent; head switching reveals different attention patterns; positional encoding visualization shows how order is encoded

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/transformers/Transformers_VR.gd` (632 lines) |
| **Scene** | `res://algorithms/machinelearning/transformers/Transformers_VR.tscn` |
| **Registry** | `machinelearning.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `token_spacing` | float | 1.2 |
| `token_size` | float | 0.3 |
| `encoder_decoder_gap` | float | 8.0 |
| `attention_beam_thickness` | float | 0.03 |
| `sequence_length` | int | 8 |
| `num_attention_heads` | int | 4 |
| `num_encoder_layers` | int | 3 |
| `num_decoder_layers` | int | 3 |
| `auto_train` | bool | true |
| `training_speed` | float | 0.15 |
| `show_attention_beams` | bool | true |
| `show_positional_encoding` | bool | true |
| `enable_token_grabbing` | bool | true |
| `enable_head_switching` | bool | true |
| `enable_query_manipulation` | bool | true |
| `animate_attention_flow` | bool | true |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_initialize_attention()`
- `_create_encoder_sequence()`
- `_create_decoder_sequence()`
- `_create_token(index: int, is_encoder: bool)`
- `_create_sequence_platform(container: Node3D, color: Color)`
- `_create_attention_mechanism()`
- `_create_positional_encoding_viz()`
- `_create_qkv_visualization()`
- `_create_attention_head_selectors()`
- `_create_control_panel()`
- `_create_info_panels()` -- Self-attention explanation
- `_create_info_panel(pos: Vector3, text: String, color: Color)`
- `_update_attention_weights(delta)` -- Slowly shift attention weights during training

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| MachineLearning_Neural_Networks | [10,9] | `-` |
| ML_Sequence_Memory | [6,1] | `:0:0:0.6` |

### Relationships

- supersedes lstms_vr (attention replaces recurrence for long-range dependencies); depends on neural_networks_vr (transformer IS a neural network)
- Needs: grabbable tokens [has], switchable attention heads [has], attention beam visualization [has], positional encoding display [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
