# Artifact: Vector Field Visualizer

> 3D arrow field showing flow — vortex, source, sink, or dipole fields. An 8x8x4 grid of arrow glyphs colored by magnitude (blue=slow, red=fast). Each arrow drawn with ImmediateMesh lines for shaft and arrowhead.

## Context

**Category:** mathematics | **Complexity:** intermediate
**Tags:** vectors, vector_field, flow, vortex, dipole, source, sink, arrows, 3d, magnitude, direction | **Themes:** vectors, fields, visualization

3D arrow field showing flow — vortex, source, sink, or dipole fields. An 8x8x4 grid of arrow glyphs colored by magnitude (blue=slow, red=fast). Each arrow drawn with ImmediateMesh lines for shaft and arrowhead.

## Design

- **Visual:** material: vertex_color, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/vector_field_visualizer/vector_field_visualizer.gd` (252 lines) |
| **Scene** | `res://commons/artifacts/vector_field_visualizer/vector_field_visualizer.tscn` |
| **Registry** | `vectors.json` |
| **Class** | `VectorFieldVisualizer` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_x` | int | 8 |
| `grid_y` | int | 4 |
| `grid_z` | int | 8 |
| `grid_spacing` | float | 0.12 |
| `arrow_scale` | float | 0.04 |
| `field_mode` | int | 0 |

### Key Methods

- `_ready()`
- `_build_field()`
- `_rebuild_arrows()`
- `_evaluate_field(pos: Vector3)` -- Vortex — F(x,z) = (-z, 0, x) normalized, with falloff
- `_field_vortex(pos: Vector3)` -- Vortex around Y axis: tangential flow
- `_field_source(pos: Vector3)` -- Radial outward from origin
- `_field_sink(pos: Vector3)` -- Radial inward toward origin
- `_field_dipole(pos: Vector3)` -- Two charges: +1 at (0.2, 0.3, 0), -1 at (-0.2, 0.3, 0)
- `_magnitude_to_color(mag: float)` -- Blue (slow) -> cyan -> green -> yellow -> red (fast)
- `_draw_arrow(mesh: ImmediateMesh, origin: Vector3, dir: Vector3, length: float, color: Color)`
- `_build_labels()`
- `_get_field_equation()`
- `set_field_mode(mode: int)`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `arrow_scale`, `field_mode`, `grid_spacing`

## Curriculum Position

### Sequences

- forces

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ForcesChaos | [5,18] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
