# Artifact: Decaying Bridge

> health(cell) -= decay_if_near_player; health += regrow_rate * healthy_neighbors

## Context

**Category:** emergence | **Complexity:** beginner
**Tags:** emergence | **Themes:** cellular_automata, computation, emergence

A bridge that decays when stepped on.

*Architecture that reacts to presence is architecture that is alive.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** To crumble under your feet and heal behind you — architecture that remembers your weight
- **Critical Parameter:** decay_speed vs regrow_speed — the ratio determines whether the bridge survives your crossing
- **What Emerges:** Path memory — the bridge records where you walked as a fading scar of transparency
- **Triggers:** Standing still → hole opens beneath you; moving fast → bridge barely notices; no player → full restoration

## Architecture

| | |
|---|---|
| **File** | `algorithms/cellularautomata/living_architecture/decaying_bridge.gd` (171 lines) |
| **Scene** | `res://algorithms/cellularautomata/living_architecture/decaying_bridge.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `length` | int | 20 |
| `width` | int | 4 |
| `cell_size` | float | 1.0 |
| `decay_speed` | float | 0.1 |
| `regrow_speed` | float | 2.0 |
| `decay_radius` | float | 2.0 |
| `color_healthy` | Color | Color(0.2, 0.8, 0.2) |
| `color_decayed` | Color | Color(0.2, 0.2, 0.2) |
| `color_dead` | Color | Color(0.0, 0.0, 0.0, 0.0) |

### Key Methods

- `_ready()` -- Try to get existing StaticBody3D node, or create one
- `_process(delta: float)`
- `_initialize_bridge()` -- Clear old
- `_update_simulation(dt)`
- `_count_healthy_neighbors(coord: Vector2i)`
- `_update_visuals()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Cellular Automata: Local Rules, Global Patterns** (cellularautomata) -- map: CA_ExpandingSpace

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CA_ExpandingSpace | [7,5] | `-` |

### Relationships

- Feeds into CA_ExpandingSpace. Extends ca_bridge (static structure → responsive architecture).
- Needs: VR parameter controls [missing], player tracking [has via group lookup], collision [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
