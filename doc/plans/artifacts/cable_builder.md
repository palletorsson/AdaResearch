# Artifact: Cable Builder

> catenary(x) approximated by cubic spline through grabbable control points

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** physics, wave | **Themes:** oscillation, physics, waves

Construct hanging cables between anchor points—watch them sag into catenaries, the natural curve of flexible lines under gravity. Engineering meets wave mathematics.

*A hanging cable finds the shape that minimizes potential energy — nature solves calculus of variations.*

## Design

- **Visual:** material: shader, animation: vertex
- **Interaction:** Grab control points in VR and sculpt hanging cable curves in real-time
- **Critical Parameter:** control_point_count — determines degrees of freedom for curve shaping
- **What Emerges:** natural catenary sag from gravity acting on flexible curves between fixed points
- **Triggers:** moving any control point recomputes the spline and regenerates the cable mesh

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/line_builder_3d/cable_builder.gd` (163 lines) |
| **Scene** | `res://algorithms/wavefunctions/line_builder_3d/cable_builder.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `point_scene` | PackedScene | preload("res://commons/primitives/point/grab_sphere_point.tscn") |
| `line_material` | ShaderMaterial | preload("res://commons/resourses/shaders/line_shader.tres") |
| `control_point_count` | int | 4 |
| `cable_length` | float | 4.0 |
| `cable_thickness` | float | 0.02 |
| `tube_sides` | int | 8 |
| `spline_resolution` | int | 20 |
| `gravity_sag` | float | 0.5 |

### Dependencies

- `res://commons/primitives/point/grab_sphere_point.tscn`
- `res://commons/resourses/shaders/line_shader.tres`

### Key Methods

- `_ready()`
- `_process(_delta)`
- `_spawn_control_points()`
- `_update_cable()`
- `_generate_tube_from_curve()`
- `_create_cable_material()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Effect_Sound

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Wavefunctions_Cables | [10,4] | `-` |
| WaveFunctions_Effect_Sound | [2,9] | `-` |

### Relationships

- depends on Curve3D spline interpolation; contrasts with BigPipeSystem (flexible vs rigid segments); unlocks curve intuition
- Needs: VR grab on control points [has], cable thickness control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
