# Artifact: fouriertransformshape

> Shows nested rotating circles that trace a Fourier drawing with emissive connections.

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** waves | **Themes:** oscillation, physics, waves

Shows nested rotating circles that trace a Fourier drawing with emissive connections.

## Design

- **Visual:** material: vertex_color, animation: shader_time
- **Scale:** [1.0, 3.0, 9.0] (environment)

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/fouriertransformshape/fouriertransformshape.gd` (386 lines) |
| **Scene** | `res://algorithms/wavefunctions/fouriertransformshape/fouriertransformshape.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural_mesh |

### Key Methods

- `_ready()`
- `setup_audio()`
- `setup_theremin()`
- `setup_camera()`
- `setup_wheels()` -- Create materials
- `setup_trace()`
- `_process(delta: float)` -- Control animation speed
- `update_wheels()`
- `update_trace()`
- `ensure_trace_lights()`
- `_generate_audio_samples()` -- MATHEMATICAL BEAUTY - Pure, clean harmonic tones
- `_generate_theremin_audio()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| TestMap_Unused_7 | [3,1] | `:0:2` |
| WaveFunctions_Fourier_Form | [5,5] | `:0:3` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
