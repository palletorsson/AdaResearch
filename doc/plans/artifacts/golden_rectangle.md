# Artifact: Golden Rectangle

> golden_rect → square(min_side) + golden_rect(remainder), spiral arc per square. phi = (1+sqrt(5))/2.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** recursion | **Themes:** generation, procedural

Classic golden ratio rectangle subdivision with Fibonacci spiral. Shows numbers 1,1,2,3,5,8,13,21... Presets: classic, minimal, rainbow, deep, fast

*The golden rectangle is the only rectangle that can lose a square and remain itself — self-similarity without fractals.*

## Design

- **Visual:** material: unlit, animation: vertex
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To spiral inward forever — each subdivision peels off a square and the remainder is always another golden rectangle
- **Critical Parameter:** max_subdivisions — each step cuts a square and rotates direction (right→up→left→down); the spiral tightens with each
- **What Emerges:** The golden spiral from quarter-circle arcs in successive squares — no spiral was drawn, only arcs in boxes, yet the spiral appears
- **Triggers:** subdivision_delay tick → peel square → create remainder → draw quarter-circle arc → Fibonacci numbers label each square

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/golden_rectangle/golden_rectangle.gd` (527 lines) |
| **Scene** | `res://algorithms/fractals/golden_rectangle/golden_rectangle.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `initial_width` | - | - |
| `max_subdivisions` | - | - |
| `extrusion_height` | - | - |
| `animate` | - | - |
| `subdivision_delay` | - | - |
| `spiral_draw_speed` | - | - |
| `use_fibonacci_colors` | - | - |
| `base_hue` | - | - |
| `hue_shift_per_level` | - | - |
| `show_spiral` | - | - |
| `show_numbers` | - | - |
| `spiral_color` | - | - |
| `outline_color` | - | - |

### Key Methods

- `_ready()`
- `_setup_containers()`
- `_process(delta: float)`
- `_initialize_rectangle()`
- `_perform_subdivision()`
- `_create_rectangle_visual(rect: Dictionary, is_golden: bool)`
- `_add_rectangle_outline(rect: Dictionary, height: float)`
- `_add_number_label(rect: Dictionary, height: float)`
- `_update_spiral()`
- `_generate_all_subdivisions()`
- `regenerate()` -- Clear visuals
- `set_subdivisions(count: int)`
- `toggle_spiral()`
- `toggle_numbers()`
- `configure(data: Dictionary)` -- preset:rainbow)"""

### Signals

- `subdivision_complete`
- `all_subdivisions_complete`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_GoldenSpiral

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_GoldenSpiral | [5,1] | `#preset:rainbow` |

### Relationships

- The geometric proof that phi = 1 + 1/phi; feeds visual understanding into fibonacci_sequences and fibonacci_terrain
- Needs: VR subdivision control [missing], spiral toggle [has], number toggle [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
