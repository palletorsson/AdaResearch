# Artifact: Artifact Workstation

> VR Artifact Workstation — browse and test any of 1692 artifacts from inside VR. Navigate by sequence, type, or all. Arrow keys for desktop, VR buttons for headset.

## Context

**Category:** tools | **Complexity:** beginner
**Tags:** tools, workstation, browser, quality, testing | **Themes:** tools, quality

VR Artifact Workstation — browse and test any of 1692 artifacts from inside VR. Navigate by sequence, type, or all. Arrow keys for desktop, VR buttons for headset.

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [4.0, 3.0, 4.0] (large)

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/workstation/ArtifactWorkstationVR.gd` (246 lines) |
| **Scene** | `res://commons/artifacts/workstation/ArtifactWorkstationVR.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `ArtifactWorkstationVR` extends `Node3D` |
| **Pattern** | procedural |

### Dependencies

- `res://addons/godot-xr-tools/objects/viewport_2d_in_3d.tscn`
- `res://commons/artifacts/workstation/workstation_ui.tscn`
- `res://addons/godot-xr-tools/objects/hand_pose_area.tscn`
- `res://addons/godot-xr-tools/hands/poses/pose_point_left.tres`
- `res://addons/godot-xr-tools/hands/poses/pose_point_right.tres`

### Key Methods

- `_ready()`
- `_build_kiosk()` -- Kiosk positioned in front of the artifact — no stand, just a floating panel at waist height
- `_connect_ui(viewport: Node)` -- Wait for viewport to create the scene instance
- `_build_presentation_area()`
- `_on_artifact_changed(lookup_name: String)`
- `_load_artifact(lookup_name: String)` -- Clear current
- `_safe_load(path: String)`
- `_safe_instantiate(packed: PackedScene)` -- Check the scene state for script/type conflicts before instantiating
- `_fit_artifact()`
- `_disable_cameras_recursive(node: Node)`
- `_get_aabb(node: Node3D)`
- `_unhandled_input(event: InputEvent)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Lab | [11,5] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
