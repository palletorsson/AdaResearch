# Artifact: NeuralNetworks_VR

> layers of neurons connected by weighted edges; forward pass propagates signal, backward pass propagates error

## Context

**Category:** unknown | **Complexity:** advanced
**Tags:** graph | **Themes:** ai, machine_learning

Walkable neural network with visible weights and backpropagation

*a neural network is not a brain — it is a differentiable function that learns by following its own error gradient*

## Design

- **Visual:** material: unlit, animation: physics
- **Scale:** [23, 10, 8] (xlarge)
- **Interaction:** walk through each layer as a physical room, throw data into the input, watch activations cascade and gradients flow back
- **Critical Parameter:** learning_rate — controls how aggressively weights change during backpropagation
- **What Emerges:** the spatial intuition that depth creates abstraction — each room transforms the signal further
- **Triggers:** auto_train drives forward/backward passes; backprop particles flow in reverse; data particles enter the network

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/neural_networks/NeuralNetworks_VR.gd` (718 lines) |
| **Scene** | `res://algorithms/machinelearning/neural_networks/NeuralNetworks_VR.tscn` |
| **Registry** | `machinelearning.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `layer_spacing` | float | 6.0 |
| `neuron_size` | float | 0.25 |
| `room_height` | float | 8.0 |
| `layer_sizes` | Array[int] | [5, 4, 3, 2] |
| `enable_bias_neurons` | bool | true |
| `activation_function` | String | "ReLU" |
| `auto_train` | bool | true |
| `learning_rate` | float | 0.05 |
| `training_speed` | float | 0.2 |
| `show_backprop` | bool | true |
| `enable_neuron_grabbing` | bool | true |
| `enable_data_throwing` | bool | true |
| `show_weights` | bool | true |
| `show_activations` | bool | true |
| `weight_line_thickness` | float | 0.02 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_initialize_network()` -- Initialize activations for each layer
- `_create_network_layers()`
- `_create_neuron(layer_idx: int, neuron_idx: int)`
- `_create_layer_room(layer_container: Node3D, layer_idx: int)` -- Floor
- `_create_layer_label(layer_container: Node3D, layer_idx: int)`
- `_create_weight_connections()`
- `_create_data_input_area()`
- `_create_data_particle(pos: Vector3)`
- `_create_activation_flow()`
- `_create_gradient_flow()`
- `_create_control_panel()`
- `_create_info_panels()` -- Forward propagation
- `_create_info_panel(pos: Vector3, text: String, color: Color)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| MachineLearning_Neural_Networks | [2,1] | `-` |
| ML_Neural_Networks | [1,1] | `:0:0:0.6` |

### Relationships

- depends on gradient_descent_visualization (training IS optimization); unlocks lstms_vr (adding memory) and convolutional_neural_networks_cnns_vr (adding spatial structure)
- Needs: grabbable neurons [has], throwable data [has], visible weight connections [has], backprop particle flow [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
