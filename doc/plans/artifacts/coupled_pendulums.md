# Artifact: Coupled Pendulums

> Two or more pendulums connected by springs or shared supports, demonstrating energy transfer, normal modes, and synchronization. Watch energy slosh back and forth between pendulums — the physical basis of beat frequencies and resonance coupling.

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** pendulum, coupled, synchronization, normal_modes, energy_transfer | **Themes:** commons, mixed, waves

Two or more pendulums connected by springs or shared supports, demonstrating energy transfer, normal modes, and synchronization. Watch energy slosh back and forth between pendulums — the physical basis of beat frequencies and resonance coupling.

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/coupled_pendulums/coupled_pendulums.gd` (343 lines) |
| **Scene** | `res://commons/artifacts/coupled_pendulums/coupled_pendulums.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `CoupledPendulums` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `pendulum_length` | float | 0.5 |
| `bob_radius` | float | 0.04 |
| `rod_thickness` | float | 0.008 |
| `gravity` | float | 9.8 |
| `coupling_strength` | float | 2.0: |
| `damping` | float | 0.02 |
| `initial_angle_1` | float | 30.0 |
| `initial_angle_2` | float | 0.0 |
| `color_pendulum_1` | Color | Color(1.0, 0.3, 0.3) |
| `color_pendulum_2` | Color | Color(0.3, 0.5, 1.0) |
| `color_spring` | Color | Color(0.8, 0.8, 0.3) |
| `color_energy` | Color | Color(0.3, 1.0, 0.5) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_frame()` -- Support structure
- `_create_pendulums()` -- Pendulum 1 (left)
- `_create_rod(color: Color)`
- `_create_bob(color: Color)`
- `_create_spring()`
- `_update_spring()` -- Get bob world positions
- `_create_energy_bars()` -- Energy bar for pendulum 1
- `_create_energy_bar(color: Color)`
- `_create_labels()`
- `_create_vr_controls()`
- `_sync_coupling_slider()`
- `_on_coupling_changed(_position)`
- `_apply_preset(a1: float, a2: float, k: float)`
- `_reset_simulation()`

### Grid Config


## Curriculum Position

### Sequences

- wavefunctions
- forces

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
