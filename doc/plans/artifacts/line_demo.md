# Artifact: line_demo

> snap_connection(pointA, pointB) → line — demonstrate the SnapConnectionManager wiring

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

line_demo.

*a connection is not just spatial proximity — it carries categorical meaning about what fits with what*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** learner sees the snap system itself as a subject — connections have categories and validation logic
- **Critical Parameter:** SnapConnectionManager — the node that tracks which snap points are linked
- **What Emerges:** that snap networks can encode semantic rules — not just any point connects to any other
- **Triggers:** any snap or unsnap event — CategoryLogicDisplay shows/hides to reflect current connection state

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/snappoint/demos/line_demo.gd` (30 lines) |
| **Scene** | `res://commons/primitives/snappoint/demos/line_demo.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_on_connection_created(_point_a, _point_b, _line)` -- Hide instruction display when a connection is made
- `_on_connection_broken(_point_a, _point_b)` -- Show instruction display again if connection is broken

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Point_Lines

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_Lines | [3,3] | `:0` |

### Relationships

- depends on SnapConnectionManager; shows the engine that snap_tetrahedron_puzzle uses
- Needs: [missing VR controls — purely demonstrative, driven by scene-level snap events]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
