# Artifact: Juno Pads Sequencer

> 2D boolean array[track][step] where each 1 triggers a sound at BPM-derived intervals — a physical 16-step × 4-track drum machine rendered in 3D, where toggling a cell means "play this sound at this beat"

## Context

**Category:** primitives | **Complexity:** beginner
**Tags:** primitives, wave | **Themes:** arrays, data_structures

Lush analog pads. Juno-106, Jupiter-8, PPG Wave. Slow atmospheric builds.

*a step sequencer is a for-loop over time — the array of steps is executed rhythmically, and music is what happens when a boolean array runs at 120 BPM*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** to feel rhythm as a 2D array — to toggle cells on a physical 16-step grid and hear your pattern play back, discovering that a drum pattern is just a boolean matrix traversed column by column at a fixed tempo
- **Critical Parameter:** sound_preset — determines which 4 sounds fill the tracks; "house" gives TR909 kick + M1 piano + hi-hat + TB303 acid bass, turning the array into a culturally specific musical grammar
- **What Emerges:** students discover that musical style is a distribution over the boolean array — house music has kick on beats 1&3, and this pattern emerges from which cells are toggled; the array encodes cultural knowledge
- **Triggers:** BPM controls the column-advance rate; step_triggered(track, step) fires on every active cell; the "house" preset carries the cultural memory of 1990s house music into the abstract array demonstration

## Architecture

| | |
|---|---|
| **File** | `commons/audio/sequencer/step_sequencer.gd` (552 lines) |
| **Scene** | `res://commons/audio/sequencer/step_sequencer.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `StepSequencer` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_tracks` | int | 4 |
| `num_steps` | int | 16 |
| `bpm` | float | 120.0 |
| `sound_preset` | String | "808_kit" |
| `auto_play` | bool | false |

### Dependencies

- `res://commons/audio/generators/AudioSynthesizer.gd`
- `res://commons/audio/generators/CustomSoundGenerator.gd`

### Key Methods

- `_ready()`
- `_find_sequencer_ui()` -- Look for Viewport2Din3D child and get its scene
- `_connect_ui_signals()`
- `_on_cell_toggled(track: int, step: int, active: bool)` -- Play preview sound when activating
- `_on_playback_toggled(playing: bool)`
- `_on_bpm_changed(new_bpm: float)`
- `_on_preset_changed(preset_name: String)`
- `_on_pattern_preset_changed(preset_name: String)`
- `_process(delta: float)`
- `_resolve_sound_type(sound_name: String)`
- `_load_sound_preset(preset_name: String)`
- `_create_audio_players()`
- `_update_step_interval()` -- 16th notes
- `_advance_playhead()` -- Trigger sounds for current step
- `_play_sound(track: int)`

### Signals

- `step_triggered`
- `playback_started`
- `playback_stopped`

### Grid Config

Accepts: `num_tracks`

## Curriculum Position

### Relationships

- paired with StandaloneDiscoFloor in Tutorial_Disco for the full arrays-as-music synthesis; house preset references Gypsy Woman/Crystal Waters production style; connects to step sequencer in audio rack
- Needs: SequencerUI [has via SubViewport2Din3D]; AudioSynthesizer [has]; CustomSoundGenerator [has]; VR interaction for toggling cells [has]; BPM control [has]; no VR slider_horizontal [missing — uses internal UI]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
