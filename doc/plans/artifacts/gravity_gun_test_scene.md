# Artifact: gravity_gun_test_scene

> gravity_gun_test_scene

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** vector | **Themes:** linear_algebra, math, vectors

gravity_gun_test_scene

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 1] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/vectors/08_vector_throwing/destructibles/gravity_gun_test_scene.gd` (149 lines) |
| **Scene** | `res://algorithms/vectors/08_vector_throwing/destructibles/gravity_gun_test_scene.tscn` |
| **Registry** | `vectors.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `spawn_count` | int | 10 |
| `arrangement` | String (enum) | "grid" |
| `spawn_spacing` | float | 0.3 |
| `spawn_height` | float | 0.0 |
| `spawn_gravity_scale` | float | 1.0 |
| `randomize_colors` | bool | true |
| `color_seed` | int | 0 |
| `color_palette` | Array[Color] | [ |

### Dependencies

- `res://algorithms/vectors/08_vector_throwing/destructibles/shootable_sphere.tscn`

### Key Methods

- `_ready()`
- `spawn_spheres()`
- `spawn_grid_pattern()`
- `spawn_ramp_pattern()`
- `spawn_ring_pattern()`
- `spawn_stack_pattern()`
- `spawn_sphere_at(position: Vector3)`
- `clear_spheres()`
- `reset_spheres()`
- `set_pattern(pattern: String)`
- `get_sphere_count()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

`arrangement` only, gated on the key and on a change (promoted 2026-08-05; it was
`pass`). `gravity_gun_test_scene#arrangement:ring` respawns the collection as a ring;
a token carrying any other key returns on the first line and respawns nothing.

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: ForcesArena

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Forces_Destruct | [11,6] | `-` |
| Forces_Drone_Game | [11,6] | `-` |
| ForcesArena | [5,6] | `-` |
| ForcesArena | [20,20] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
