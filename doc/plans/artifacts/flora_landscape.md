# Artifact: Flora Landscape

> Renders the Folding Zoo nature_layer.json as a 3D landscape. Vertex-colored ground mesh with MultiMesh flora on top.

## Context

**Category:** hazards | **Complexity:** exhibition
**Tags:** fold_system, nature, landscape, flora, ground_mesh | **Themes:** fold_system

Renders the Folding Zoo nature_layer.json as a 3D landscape. Vertex-colored ground mesh with MultiMesh flora on top.

## Design

- **Visual:** material: vertex_color, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/fold_system/flora_landscape.gd` (246 lines) |
| **Scene** | `res://commons/fold_system/flora_landscape.tscn` |
| **Registry** | `hazards.json` |
| **Pattern** | procedural |

### Dependencies

- `res://commons/primitives/shared/primitive_mesh_builder.gd`
- `res://commons/fold_system/synthetic_foliage.gd`

### Key Methods

- `_ready()`
- `_build_ground(layers: Dictionary, w: int, d: int)`
- `_place_real_scenes(layers: Dictionary, w: int, d: int)` -- Place up to 3 full artifact scenes per type at the strongest-painted cells.
- `_build_flora(layers: Dictionary, w: int, d: int)`
- `_make_mesh(flora_type: String)`
- `apply_grid_config(_config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- hazards

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
