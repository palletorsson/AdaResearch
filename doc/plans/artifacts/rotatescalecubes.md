# Artifact: Rotate Scale Cubes

> cube[i].transform(t) = rotate(speed_i * t) * scale(base + variation * sin(t))

## Context

**Category:** waves | **Complexity:** beginner
**Tags:** rotation, scaling, transformation, cubes | **Themes:** oscillation, physics, transformation

Interactive transformation algorithms demonstrating rotation and scaling operations on geometric primitives. Shows how transform matrices compose.

*A field of rotating objects is a field of phases — each cube is a clock at its own tempo.*

## Design

- **Interaction:** Float among hundreds of cubes all rotating and pulsing at different rates
- **Critical Parameter:** cube_count — determines the density of the transformation field
- **What Emerges:** a galaxy of independent oscillators — collective visual rhythm from individual rotation rates
- **Triggers:** per-frame MultiMesh transform update applies unique rotation speeds to each instance

## Architecture

| | |
|---|---|
| **File** | `algorithms/transformation/rotatescalecubes/rotatescalecubes.gd` (158 lines) |
| **Scene** | `res://algorithms/transformation/rotatescalecubes/rotatescalecubes.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cube_count` | int | 500 |
| `spread_radius` | float | 10.0 |
| `animation_speed` | float | 0.5 |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `setup_lighting()` -- Create a simple environment for basic lighting and a dark background.
- `create_grid_shader_material()`
- `setup_cubes()` -- Use a MultiMesh for performance, which is ideal for drawing
- `_process(delta: float)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Intro

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Oscillation_Animated_Cube | [2,5] | `:0:15` |
| WaveFunctions_Intro | [1,4] | `:0:15` |

### Relationships

- depends on MultiMesh instancing; contrasts with rotating_cube_demo (mass vs individual cube); unlocks transformation-as-field
- Needs: VR spatial navigation [has], parameter control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
