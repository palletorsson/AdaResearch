# Artifact: destructible_marching_cube

> destructible_marching_cube

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** vector | **Themes:** linear_algebra, math, vectors

destructible_marching_cube

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/vectors/08_vector_throwing/destructibles/destructible_marching_cube.gd` (264 lines) |
| **Scene** | `res://algorithms/vectors/08_vector_throwing/destructibles/organic_voronoi_cube_test.tscn` |
| **Registry** | `vectors.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `terrain_generator_path` | NodePath | - |
| `fragment_count` | int | 12 |
| `explosion_strength` | float | 5.0 |
| `health` | int | 1 |

### Key Methods

- `_ready()` -- Find the terrain generator
- `_initialize()` -- Wait for terrain to generate if needed
- `_setup_base_mesh()` -- Find the MeshInstance3D from terrain generator
- `_setup_collision()` -- Create static body for collision
- `_setup_hit_detection()` -- Create area to detect projectile hits
- `_on_body_entered(body: Node3D)`
- `_hit_feedback()` -- Visual feedback for hit but not destroyed
- `_destroy(impact_point: Vector3, impact_velocity: Vector3)`
- `_create_fragments(_impact_point: Vector3, impact_velocity: Vector3)`
- `_calculate_mesh_center(vertices: PackedVector3Array)`
- `_generate_voronoi_centers(mesh_center: Vector3, count: int)`
- `_create_fragment(center: Vector3, mesh_center: Vector3, impact_velocity: Vector3, index: int)` -- Create a simple box fragment at the voronoi center
- `_fade_fragment(mesh_instance: MeshInstance3D)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Forces_Destruct | [4,2] | `:0:1` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
