# Artifact: Evolving Creatures (EA)

> Evolutionary algorithms demo — creatures evolving body plans and behaviors through selection, crossover, and mutation. A hands-on look at how evolution works as an optimization process.

## Context

**Category:** machinelearning | **Complexity:** intermediate
**Tags:** evolution, creature, evolutionary_algorithm, morphology | **Themes:** ai, biology, machine_learning

Evolutionary algorithms demo — creatures evolving body plans and behaviors through selection, crossover, and mutation. A hands-on look at how evolution works as an optimization process.

## Design

- **Visual:** material: unlit, animation: physics

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/evolutionaryalgorithms/evolving_creatures.gd` (868 lines) |
| **Scene** | `res://algorithms/machinelearning/evolutionaryalgorithms/evolving_creatures.tscn` |
| **Registry** | `machinelearning.json` |
| **Class** | `SimplifiedCreature` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `limb_length` | float | 0.8: |
| `limb_radius` | float | 0.15: |
| `torque_strength` | float | 5.0 |
| `creature_type` | int | 0: |
| `learning_rate` | float | 0.1 |
| `discount_factor` | float | 0.95 |
| `exploration_rate` | float | 0.4 |
| `exploration_decay` | float | 0.995 |
| `min_exploration_rate` | float | 0.05 |
| `update_frequency` | float | 0.1 |

### Key Methods

- `_ready()`
- `_process(delta: float)` -- Update timer
- `create_environment()` -- Create a floor
- `create_grid()` -- Create a grid on the floor for distance reference
- `create_debug_visualization()` -- Create a node for path visualization
- `add_path_point(point)` -- Keep y slightly above floor
- `update_path_visualization()` -- Set start marker position
- `create_ui()`
- `update_ui()`
- `create_quadruped()` -- Create core body
- `create_biped()` -- Create core body
- `create_snake()` -- Create head segment
- `create_core()` -- Create the core body — the main torso of the creature
- `create_leg(parent_body, offset, leg_index)` -- Calculate the attachment point on the parent
- `create_segment(parent_segment, position, segment_index)` -- Create a segment for snake-like creature

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
