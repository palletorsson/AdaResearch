# Artifact: Pompeii Mosaic Floor

> a floor that carries the geometry of Roman domestic space

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pompeii | **Themes:** arrays, art, cultural_computation

Multi-zone Roman mosaic floor from Pompeii. Composes field, border, corner, and threshold patterns using wallpaper group symmetries with opus tessellatum aging effects.

*constraint applied to two colors on a grid produced all of Pompeii's floors*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see 2000-year-old pattern logic still tiling
- **Critical Parameter:** tiles_short — number of truchet tiles on the short axis
- **What Emerges:** the understanding that symmetry groups predate their mathematics
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/pompeii_mosaic_floor.gd` (272 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/pompeii_mosaic_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `PompeiiMosaicFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.2, 0.9) |
| `tiles_short` | int | 10 |
| `border_widths` | Array[int] | [2, 1, 2] |
| `border_motif` | int | BorderMotifs.Motif.SAWTOOTH |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.06 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`
- `res://commons/artifacts/pompeii_mosaic_floor/border_motifs.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`

### Grid Config

Accepts: `border_widths`, `floor_size`, `tiles_short`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| AutoGenTest2 | [4,4] | `-` |
| Mosaic_Pompeii_Floors | [1,1] | `-` |

### Relationships

- pattern_maker_station (web editor), mosaic_editor (composition)
- Needs: [implemented] procedural mesh, border bands, truchet pinwheel

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
