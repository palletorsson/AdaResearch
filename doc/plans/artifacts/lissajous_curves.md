# Artifact: LissajousCurves

> (x,y,z) = (A_x*sin(a*t+delta), A_y*sin(b*t), A_z*sin(c*t)) — parametric Lissajous

## Context

**Category:** waves | **Complexity:** advanced
**Tags:** waves | **Themes:** oscillation, physics, waves

Parametric curves with particle tracer

*The ratio of two frequencies determines whether a curve closes or wanders forever.*

## Design

- **Visual:** material: vertex_color, animation: vertex
- **Interaction:** Watch a glowing particle trace beautiful closed curves defined by frequency ratios
- **Critical Parameter:** freq_ratio_x / freq_ratio_y — rational ratios close the curve, irrational ratios fill space
- **What Emerges:** complex geometric figures from the interference of perpendicular oscillations
- **Triggers:** time drives the parametric position; preset selection switches between classic ratio combinations

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/lissajous_curves/lissajous_curves.gd` (232 lines) |
| **Scene** | `res://algorithms/wavefunctions/lissajous_curves/lissajous_curves.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `freq_ratio_x` | float | 3.0 |
| `freq_ratio_y` | float | 2.0 |
| `freq_ratio_z` | float | 1.0 |
| `amplitude_x` | float | 1.0 |
| `amplitude_y` | float | 1.0 |
| `amplitude_z` | float | 0.5 |
| `phase_shift` | float | 0.0 |
| `animate_phase` | bool | true |
| `phase_speed` | float | 0.5 |
| `num_points` | int | 500 |
| `line_width` | float | 0.02 |
| `trail_length` | int | 200 |
| `show_particle` | bool | true |
| `particle_size` | float | 0.08 |
| `rainbow_gradient` | bool | true |
| `animation_speed` | float | 1.0 |
| `auto_rotate` | bool | false |
| `rotation_speed` | float | 0.2 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_setup_visualization()` -- Clear existing
- `_generate_full_curve()`
- `_calculate_point(t: float)`
- `_update_curve_mesh()`
- `_update_particle()`
- `get_current_position()`
- `get_velocity()`
- `set_preset(preset_name: String)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Synthesis_Lab

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| TestMap_Unused_9 | [1,3] | `:0:2` |
| Wavefunctions_Form_Slide | [2,3] | `:0:2` |
| WaveFunctions_Synthesis_Lab | [5,7] | `:0:1:0.1` |

### Relationships

- depends on ImmediateMesh trail rendering; contrasts with SphericalHarmonics (planar vs spherical oscillation); unlocks frequency ratio visualization
- Needs: VR frequency ratio control [missing], 3D rotation [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
