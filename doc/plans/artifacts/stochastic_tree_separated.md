# Artifact: stochastic_tree_separated

> tree + separation_force(branches_at_level) + upward_alignment(blend), post-growth branch repulsion

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** tree | **Themes:** generation, procedural

stochastic_tree_separated

*A tree's canopy is not designed — it is negotiated, branch by branch, through local repulsion toward global form.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 4.0, 2.0] (room_scale)
- **Interaction:** To watch a tree argue with itself — branches push apart level by level, finding space, then aligning upward
- **Critical Parameter:** separation_strength (0.05) — the repulsion force between same-level branches; too high and the tree explodes, too low and branches overlap
- **What Emerges:** Natural-looking canopy spacing from simple repulsion — branches discover their territory without planning
- **Triggers:** Growth completes → separation phase activates level by level (deepest first); upward_alignment smooths the result

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/example_8_7_stochastic_tree_separated_vr.gd` (384 lines) |
| **Scene** | `res://algorithms/fractals/example_8_7_stochastic_tree_separated_vr.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `recursion_depth` | int | 5 |
| `base_angle` | float | 25.0 |
| `angle_variance` | float | 15.0 |
| `length_reduction` | float | 0.67 |
| `length_variance` | float | 0.1 |
| `separation_duration` | float | 3.0 |
| `separation_strength` | float | 0.05 |
| `upward_bias` | float | 0.01 |
| `upward_alignment_strength` | float | 0.25 |

### Dependencies

- `res://commons/resourses/materials/noc_vr/noc_vr_pink_primary.tres`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_setup_environment()`
- `_grow_tree(start_pos: Vector3, direction: Vector3, length: float, thickness: float, depth: int, parent_data: BranchData = null)`
- `_update_branch_mesh(branch: MeshInstance3D, start: Vector3, end: Vector3, thickness: float)`
- `_create_branch(start: Vector3, end: Vector3, thickness: float, depth: int, level: int, parent_branch: BranchData = null)`
- `_apply_separation(delta: float)`
- `_apply_upward_alignment(level: int)`
- `_compute_rotation(from_vec: Vector3, to_vec: Vector3)`
- `_propagate_child_transform(parent_branch: BranchData, rotation: Quaternion)`
- `_reset_level_velocities(level: int)`
- `_rotate_vector(vec: Vector3, axis: Vector3, angle: float)`
- `_update_status()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_CantorSet

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_CantorSet | [17,3] | `:0:-0.5` |
| Fractal_CantorSet | [17,8] | `:0:-0.5` |
| Fractal_CantorSet | [17,12] | `:0:-0.5` |

### Relationships

- Extends fractal_stochastic_tree with flocking-like separation; bridges fractals and emergence sequences
- Needs: VR separation speed control [missing], force visualization [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
