# Artifact: Slingshot Launcher

> Step onto the platform and get catapulted! A charging catapult mechanism detects the player, builds power, then fires you (and demo balls) along a parabolic trajectory. Adjustable launch angle and power with a visible trajectory preview. Shows impulse, projectile motion, and the visceral thrill of Newton's third law.

## Context

**Category:** physics | **Complexity:** intermediate
**Tags:** slingshot, catapult, launch, projectile, impulse, trajectory, parabola, player_interaction, VR | **Themes:** impulse, projectile_motion, interaction, catapult

Step onto the platform and get catapulted! A charging catapult mechanism detects the player, builds power, then fires you (and demo balls) along a parabolic trajectory. Adjustable launch angle and power with a visible trajectory preview. Shows impulse, projectile motion, and the visceral thrill of Newton's third law.

## Design

- **Visual:** material: vertex_color, animation: physics

## Architecture

| | |
|---|---|
| **File** | `algorithms/physicssimulation/slingshotlauncher/slingshotlauncher.gd` (457 lines) |
| **Scene** | `res://algorithms/physicssimulation/slingshotlauncher/slingshotlauncher.tscn` |
| **Registry** | `physics_simulation.json` |
| **Class** | `SlingshotLauncher` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `launch_force` | float | 18.0: |
| `launch_angle_deg` | float | 60.0: |
| `charge_time` | float | 2.0 |
| `platform_radius` | float | 0.5 |
| `platform_height` | float | 0.08 |
| `arm_length` | float | 1.2 |
| `base_color` | Color | Color(0.2, 0.2, 0.25) |
| `platform_color` | Color | Color(0.4, 0.6, 1.0) |
| `charged_color` | Color | Color(1.0, 0.4, 0.2) |
| `arm_color` | Color | Color(0.6, 0.6, 0.65) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_base()` -- Heavy base block
- `_create_arm_mechanism()` -- Pivot point for the arm
- `_create_platform_with_detection()` -- Platform at the end of the arm
- `_create_charge_indicator()` -- Ring around platform that fills as it charges
- `_create_launch_arrow()`
- `_update_launch_arrow()`
- `_create_trajectory_preview()`
- `_update_trajectory()` -- Show predicted parabolic arc
- `_on_body_entered(body: Node3D)`
- `_on_body_exited(body: Node3D)`
- `_is_player(body: Node3D)`
- `_physics_process(delta: float)`
- `_launch()`
- `_launch_demo_balls()` -- Fire 3 colored balls along the trajectory so the physics is visible

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- forces

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
