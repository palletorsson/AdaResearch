# Artifact: Pattern Compositor

> Zone-based pattern composition using the SpatialComposition library. Presets: carpet (border/field/medallion), facade (base/columns/windows/cornice), mosaic, quilt, tunnel. Each zone gets a different wallpaper group shader.

## Context

**Category:** shaders | **Complexity:** advanced
**Tags:** compositor, pattern, zones, carpet, facade, wallpaper_groups, composition, reusable | **Themes:** shaders, visual

Zone-based pattern composition using the SpatialComposition library. Presets: carpet (border/field/medallion), facade (base/columns/windows/cornice), mosaic, quilt, tunnel. Each zone gets a different wallpaper group shader.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pattern_compositor/pattern_compositor.gd` (503 lines) |
| **Scene** | `res://commons/artifacts/pattern_compositor/pattern_compositor.tscn` |
| **Registry** | `shaders.json` |
| **Class** | `PatternCompositor` extends `Node3D` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `preset_name` | String | "carpet" |
| `grid_width` | int | 20 |
| `grid_height` | int | 20 |
| `tile_size` | float | 0.5 |
| `surface` | String | "floor" |
| `emission_strength` | float | 0.8 |
| `tiling` | String | "rect" |

### Dependencies

- `res://commons/composition/spatial_composition.gd`
- `res://commons/resourses/shaders/wallpaper_tile.gdshader`
- `res://commons/composition/art_history_presets.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config_data: Dictionary)`
- `_build_composition()` -- Try art history presets first, then basic presets
- `_render()`
- `_render_rectangular()`
- `_render_with_coord_system()`
- `_get_zone_pattern(zone)` -- Start with zone's own properties, fall back to defaults by zone ID
- `_hex_to_color(hex: String)` -- ", "")
- `_create_zone_material(props: Dictionary)`
- `_generate_domain(seed_val: int)`
- `_grid8(fill: int = 0)`
- `_set8(arr: Array, x: int, y: int, val: int)`
- `_domain_blocks(gen: RandomNumberGenerator)`
- `_domain_stripes(gen: RandomNumberGenerator)`
- `_domain_diagonal(gen: RandomNumberGenerator)`

### Grid Config

Accepts: `emission_strength`, `height`, `preset`, `surface`, `tile_size`, `tiling`, `width`

## Curriculum Position

### Sequences

- patterngeneration

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
