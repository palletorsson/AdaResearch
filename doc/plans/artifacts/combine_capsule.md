# Artifact: combine_capsule

> Arranges capsule meshes in a matrix while iterating through height and radial segment combinations.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** linear_algebra | **Themes:** core, primitives

Arranges capsule meshes in a matrix while iterating through height and radial segment combinations.

## Design

- **Visual:** material: shader, animation: static
- **Scale:** [2.0, 2.0, 2.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/combines/combine_capsule.gd` (165 lines) |
| **Scene** | `res://commons/primitives/combines/combine_capsule.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `radius` | float | 0.3 |
| `height_values` | Array[float] | [0.6, 0.9, 1.2, 1.5] |
| `radial_segments_values` | Array[int] | [8, 12, 16, 20] |
| `rings` | int | 6 |
| `spacing` | float | 3.0 |
| `use_wireframe` | bool | false |
| `metallic` | float | 0.15 |
| `roughness` | float | 0.35 |
| `color_gradient` | Gradient | - |
| `wireframe_width` | float | 0.1 |
| `wireframe_brightness` | float | 2.0 |

### Key Methods

- `_ready()` -- Load the grid shader
- `generate_capsule_grid()`
- `create_capsule_at_position(pos: Vector3, height_value: float, segments: int, gradient_ratio: float)`
- `create_label_for_capsule(pos: Vector3, height_value: float, segments: int)`
- `clear_existing_capsules()`
- `regenerate_grid()`
- `set_wireframe_mode(enabled: bool)`
- `set_color_gradient(gradient: Gradient)`
- `update_materials()`
- `update_colors()`
- `get_capsule_count()`

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_Tests | [1,1] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
