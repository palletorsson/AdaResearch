# Artifact: Helix Cylinder

> vertices on a helix form triangles into a faceted cylinder — pineapple topology

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** fold_system, helix, cylinder, pineapple, kresling, triangle_grid | **Themes:** fold_system

Vertices on a helix form triangles into a closed faceted cylinder. Pineapple diamond topology. Flat triangle grid unfurls into a twisted tube. Kresling-inspired.

*a cylinder is just a flat grid with its edges identified. the helix adds twist.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a closed spiral surface that unfurls from flat triangle grid into a twisted tube
- **Critical Parameter:** fold_amount — 0.0 is flat grid, 1.0 is closed helix cylinder
- **What Emerges:** the spiral seam, the diamond facets, the pineapple pattern — all from sin/cos on a grid
- **Triggers:** each vertex lerps between grid position and helix position; triangles connect neighbors

## Architecture

| | |
|---|---|
| **File** | `commons/fold_system/helix_cylinder.gd` (134 lines) |
| **Scene** | `res://commons/fold_system/helix_cylinder.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `HelixCylinder` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `rings` | int | 12 |
| `segments_per_ring` | int | 8 |
| `radius` | float | 0.06 |
| `height` | float | 0.25 |
| `twist` | float | 0.5 |
| `flat_width` | float | 0.35 |
| `flat_height` | float | 0.25 |

### Dependencies

- `res://commons/primitives/shared/primitive_mesh_builder.gd`

### Key Methods

- `_ready()`
- `_generate_geometry()`
- `_build_stages()`
- `apply_grid_config(_config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Folding: Same Faces, Only Positions Change** (fold_system) -- map: Folding_Zoo

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Folding_Zoo | [4,7] | `:0:0.5` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
