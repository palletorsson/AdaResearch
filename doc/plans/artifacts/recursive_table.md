# Artifact: recursive_table

> table = subdivide_3x2(cube) -> keep(top_layer, corner_legs) -> flatten(top) -> stretch(legs)

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

recursive_table

*A table is a cube that kept its top layer and four corners — everything else was excess.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [6.0, 3.0, 4.0] (environment)
- **Interaction:** To stand beside in VR — a table that assembled itself from a single cube through pure subtraction
- **Critical Parameter:** the top-layer vs bottom-layer role split — one layer becomes surface, four corners become legs
- **What Emerges:** Structural apron supports arise from the need to connect legs to tabletop — engineering from aesthetics
- **Triggers:** Each step refines: merge tabletop into single slab, thin the legs, add apron supports

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/cubesubdivision/recursive_table.gd` (303 lines) |
| **Scene** | `res://algorithms/fractals/cubesubdivision/recursive_table.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `table_size` | float | 2.5 |
| `show_animation` | bool | true |
| `step_delay` | float | 0.4 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_execute_step(s: int)`
- `_build_instant()`
- `_step_0_initial_cube()` -- Create the initial cube that will become the table
- `_step_1_first_subdivision()` -- Subdivide into 3x3x2 grid (3 wide, 3 deep, 2 tall)
- `_step_2_shape_tabletop()` -- Remove the 9 individual tabletop cubes and replace with single solid piece
- `_step_3_create_legs()` -- Extend the leg cubes to meet tabletop - thinner plastic legs
- `_step_4_add_supports()` -- Add cross supports between legs (apron) - thinner plastic
- `_create_cube(pos: Vector3, size: Vector3, generation: int, role: String)`
- `_apply_color(mesh: MeshInstance3D, color: Color)`
- `_find_cubes_by_role(role: String)`
- `_remove_cube(cube_data: Dictionary)`
- `reset()`
- `_exit_tree()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_Recursion

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_Recursion | [3,2] | `:0:-0.5:0.2` |

### Relationships

- Sibling to recursive_chair; together they prove furniture is a family of subdivision strategies
- Needs: VR place-objects-on-top [missing], construction replay [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
