# Artifact: editable_curve

> Interactive curve with grabbable control points. Supports Bezier, Catmull-Rom, and linear modes. Extends the trace concept: deliberate construction over recorded gesture.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** linear_algebra | **Themes:** core, primitives

Interactive curve with grabbable control points. Supports Bezier, Catmull-Rom, and linear modes. Extends the trace concept: deliberate construction over recorded gesture.

## Design

- **Visual:** material: unlit, animation: vertex
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/curves/editable_curve.gd` (394 lines) |
| **Scene** | `res://commons/primitives/curves/editable_curve.tscn` |
| **Registry** | `primitives.json` |
| **Class** | `EditableCurve` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `curve_type` | CurveType | CurveType.BEZIER: |
| `curve_color` | Color | Color(0.2, 0.8, 0.9, 1.0): |
| `control_point_color` | Color | Color(1.0, 0.4, 0.2, 1.0): |
| `tangent_color` | Color | Color(0.5, 0.5, 0.5, 0.6): |
| `curve_segments` | int | 32: |
| `control_point_size` | float | 0.08: |
| `show_tangents` | bool | true: |
| `closed_curve` | bool | false: |
| `initial_points` | Array[Vector3] | [ |

### Dependencies

- `res://addons/godot-xr-tools/objects/pickable.tscn`
- `res://commons/primitives/curves/curve_control_point.gd`

### Key Methods

- `_ready()`
- `_setup_curve_rendering()` -- Curve line
- `_initialize_control_points()` -- Clear existing
- `_create_control_point(index: int, pos: Vector3)`
- `_create_grabbable_point()` -- Try to load XR pickable, fallback to simple sphere
- `_on_control_point_moved(_index: int)`
- `_process(_delta: float)` -- Check if any control point moved (for non-signal-based movement)
- `_rebuild_curve()`
- `_calculate_curve_points(control_positions: Array[Vector3])`
- `_calculate_bezier(points: Array[Vector3])`
- `_cubic_bezier(p0: Vector3, p1: Vector3, p2: Vector3, p3: Vector3, t: float)`
- `_calculate_catmull_rom(points: Array[Vector3])`
- `_catmull_rom_point(p0: Vector3, p1: Vector3, p2: Vector3, p3: Vector3, t: float)`
- `_update_materials()`
- `_resize_control_points()`

### Signals

- `curve_changed`
- `control_point_grabbed`
- `control_point_released`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
