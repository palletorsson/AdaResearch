# Artifact: roughrock

> cube_vertices + deterministic_noise(seed) → irregular polyhedron — controlled randomness

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** procedural | **Themes:** core, primitives

Irregular rock polyhedron created from noise-perturbed vertices.

*nature's irregularity is not random — it is deterministic noise that we lack the seed to predict*

## Design

- **Visual:** material: shader, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** learner sees a non-uniform organic shape and understands it as seeded perturbation of regular geometry
- **Critical Parameter:** noise_seed — the same seed always produces the same rock; change it, get a different rock
- **What Emerges:** the concept of procedural generation from seed — infinite variation, deterministic replay
- **Triggers:** nothing at runtime — generated once at _ready() with a fixed seed for repeatability

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/roughrock/roughrock.gd` (111 lines) |
| **Scene** | `res://commons/primitives/roughrock/roughrock.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | shader_demo |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `create_rough_rock()`
- `add_triangle_with_normal(st: SurfaceTool, vertices: Array, face: Array)`
- `apply_queer_material(mesh_instance: MeshInstance3D, color: Color)` -- Create shader material using the solid wireframe shader
- `set_base_color(color: Color)`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Primitives_Ignorance
- **Unused** (unused) -- map: Primitives_2

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Primitives_2 | [8,1] | `-` |
| Primitives_Ignorance | [15,1] | `-` |

### Relationships

- sibling to all other static primitives; conceptual bridge to fractal/procgen sequences
- Needs: [missing VR controls — static; would benefit from live noise_seed slider]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
