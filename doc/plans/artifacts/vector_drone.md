# Artifact: vector_drone

> desired = (target - position).normalized() * speed. Chase, prepare, shoot, cooldown. A state machine driven by vector subtraction.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** vector | **Themes:** linear_algebra, math, vectors

vector_drone

*The drone is a mirror of the turret. Same math, opposite role. Subtraction works for both hunter and hunted.*

## Design

- **Visual:** material: shader, animation: physics
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To hunt. To orbit at ideal_distance, charge projectiles, fire in bursts, and kamikaze when dying — an adversary that teaches vectors by trying to kill you.
- **Critical Parameter:** ideal_distance (4.0 units) — the range the drone wants to maintain. Too close = backs up. Too far = approaches. In range = switches to attack states.
- **What Emerges:** The drone's strafing pattern from sinusoidal offsets. Leading shots via look_at. Kamikaze desperation creating final-moment tension.
- **Triggers:** Player position → chase/orbit, prepare timer → spawn held projectiles, shoot timer → fire burst, health reaches 0 → kamikaze mode (speed boost, explosion, damage)

## Architecture

| | |
|---|---|
| **File** | `algorithms/vectors/08_vector_throwing/vector_drone.gd` (611 lines) |
| **Scene** | `res://algorithms/vectors/08_vector_throwing/vector_drone.tscn` |
| **Registry** | `vectors.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `player_path` | NodePath | - |
| `fallback_player_names` | PackedStringArray | PackedStringArray(["XROrigin3D", "XRPlayer", "Player", "Camera3D", "PlayerBody", "FirstPersonPlayer"]) |
| `move_speed` | float | 3.2 |
| `acceleration` | float | 6.0 |
| `hover_height` | float | 1.6 |
| `strafe_amplitude` | float | 0.6 |
| `strafe_frequency` | float | 1.0 |
| `max_health` | int | 2 |
| `ball_damage` | int | 1 |
| `stun_duration` | float | 0.35 |
| `player_hit_radius` | float | 0.45 |
| `player_hit_cooldown` | float | 2.0 |
| `knockback_force` | float | 5.0 |
| `points_value` | int | 25 |
| `destruction_fade_time` | float | 0.6 |
| `enable_kamikaze` | bool | true |
| `kamikaze_speed_boost` | float | 2.0 |
| `explosion_radius` | float | 0.8 |
| `min_damage_percent` | float | 20.0 |
| `max_damage_percent` | float | 40.0 |
| `kamikaze_duration` | float | 3.0 |
| `base_color` | Color | Color(1.0, 0.45, 0.1, 1.0) |
| `spin_speed_deg` | float | 65.0 |
| `bob_amplitude` | float | 0.15 |
| `bob_frequency` | float | 1.4 |
| `projectile_scene` | PackedScene | preload("res://algorithms/vectors/08_vector_throwing/drone_projectile.tscn") |
| `ideal_distance` | float | 4.0 |
| `shoot_interval` | float | 0.5 |
| `prepare_time` | float | 1.0 |
| `cooldown_time` | float | 2.0 |
| `burst_count` | int | 3 |

### Dependencies

- `res://algorithms/vectors/08_vector_throwing/drone_projectile.tscn`

### Key Methods

- `_ready()`
- `_process(delta: float)` -- Spin faster in kamikaze mode
- `_physics_process(delta: float)`
- `_process_chase(target_pos: Vector3, delta: float)`
- `_process_prepare(_delta: float)`
- `_process_shoot(_delta: float)` -- Stop to shoot
- `_process_cooldown(target_pos: Vector3, delta: float)` -- Strafe while cooling down
- `_switch_state(new_state: State)`
- `_spawn_held_projectiles()`
- `_fire_projectile()`
- `_clear_held_projectiles()`
- `_kamikaze_physics(delta: float)`
- `_configure_body()`
- `_configure_detector()`
- `_ensure_visual()`

### Signals

- `player_hit`
- `drone_destroyed`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: ForcesArena

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Forces_Drone_Game | [17,10] | `-` |
| ForcesArena | [9,10] | `-` |

### Relationships

- Enemy counterpart to hl_turret_vectors (player-controlled targeting vs AI targeting). Sub-component of VectorThrowing. Uses subtraction and normalization as core AI.
- Needs: State machine AI [has], burst-fire system [has], kamikaze mode [has], VR player tracking [has]. Missing: difficulty scaling, formation flying with multiple drones.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
