# Artifact: Cube Cabin

> cabin = foundation + walls + subtract(door, windows) + add(roof, chimney, porch), subtractive architecture

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

Rustic cabin created through architectural cube subdivision - walls, door, windows, gabled roof, chimney and porch

*Architecture is not the boxes — it is the holes cut in them for doors and windows, the subtractions that let light and people through.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To be approached — door, windows, porch, chimney, each step makes it more inviting, more inhabitable
- **Critical Parameter:** cabin_size — scales the entire structure proportionally; the ratios (wall height 0.6, door 0.45) are the architectural DNA
- **What Emerges:** A sense of home from geometric primitives — the welcome mat and lantern are the final touches that transform boxes into architecture
- **Triggers:** 8-step construction: foundation → walls → door → windows → roof → chimney → porch → details (lantern, welcome mat)

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/cubesubdivision/cube_cabin.gd` (385 lines) |
| **Scene** | `res://algorithms/fractals/cubesubdivision/cube_cabin.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cabin_size` | float | 3.0 |
| `show_construction` | bool | true |
| `step_delay` | float | 0.35 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_execute_step(s: int)`
- `_build_instant()`
- `_step_foundation()` -- Raised foundation/floor
- `_step_walls()`
- `_step_door_opening()` -- Door frame and door
- `_step_windows()`
- `_step_roof()`
- `_step_chimney()`
- `_step_porch()`
- `_step_details()`
- `_create_part(pos: Vector3, size: Vector3, color: Color, part_name: String)`
- `reset()`
- `_exit_tree()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_KochSierpinski

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_KochSierpinski | [2,0] | `-` |

### Relationships

- Most architecturally complete subdivision artifact; contrasts pure-math subdivisions (bookshelf, chair) with lived space
- Needs: VR walk-inside [missing], door-open interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
