# Artifact: Random Plants

> Interactive procedural plant generation with random growth patterns and organic forms

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** probability, procedural | **Themes:** probability, procedural, randomness

Interactive procedural plant generation with random growth patterns and organic forms

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/proceduralrandomness/geometrybased/randomplants/random_plants.gd` (592 lines) |
| **Scene** | `res://algorithms/randomness/proceduralrandomness/geometrybased/randomplants/random_plants.tscn` |
| **Registry** | `randomness.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `number_of_plants` | int | 100 |
| `area_size` | float | 20.0 |
| `plant_density` | float | 0.8 |
| `tree_probability` | float | 0.2 |
| `bush_probability` | float | 0.3 |
| `flower_probability` | float | 0.3 |
| `grass_probability` | float | 0.2 |
| `size_variation` | float | 0.5 |
| `color_variation` | float | 0.6 |
| `rotation_variation` | float | 0.2 |
| `leaf_density_variation` | float | 0.4 |

### Key Methods

- `_ready()`
- `create_materials()` -- Trunk material
- `create_terrain()`
- `generate_plants()` -- Calculate actual number of plants based on density
- `determine_plant_type()` -- Normalize probabilities
- `create_tree(parent, position)`
- `create_bush(parent, position)`
- `add_flowers_to_bush(bush)` -- Approximate radius
- `create_flower(parent, position)`
- `create_grass(parent, position)`
- `setup_environment()` -- Create a camera for better viewing
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Unused Artifacts Test** (testmaps) -- map: Test_7
- **Unused Artifacts Test** (testmaps) -- map: Test_8

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Test_7 | [7,3] | `:0:-1:` |
| Test_8 | [7,3] | `:0:-1:` |
| TestMap_Unused_18 | [3,3] | `:0:2` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
