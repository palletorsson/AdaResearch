# Artifact: pythagorean_triangle

> pythagorean_triangle.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

pythagorean_triangle.

## Design

- **Visual:** material: shader, animation: static
- **Scale:** [2.0, 2.0, 6.0] (environment)

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/triangle/pythagorean_triangle.gd` (454 lines) |
| **Scene** | `res://commons/primitives/triangle/pythagorean_triangle.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `sphere_size_multiplier` | float | 0.25 |
| `sphere_y_offset` | float | 0.5 |
| `alter_freeze` | bool | false |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `create_meshes()` -- Main Triangle
- `_setup_drag_points()`
- `create_labels()` -- Create labels for the three sides
- `update_visuals()`
- `update_labels()` -- Vertices: 0=C (Corner), 1=B (Right), 2=A (Top)
- `update_triangle_mesh()`
- `update_squares()` -- Generate squares extending OUTWARDS from the sides
- `update_square_mesh(mesh_inst: MeshInstance3D, p1: Vector3, p2: Vector3)`
- `add_triangle_face(st: SurfaceTool, v1, v2, v3, n)`
- `apply_material(mesh_instance: MeshInstance3D, color: Color)`
- `reset_to_right_triangle()`
- `_on_point_moved(index: int, position: Vector3, _meta: Dictionary)`
- `_get_snapped_position(index: int, pos: Vector3)` -- Indices: 0=Corner(C), 1=Base(B), 2=Top(A)
- `update_angles()` -- 0 is C (Right Angle), 1 is B, 2 is A

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
