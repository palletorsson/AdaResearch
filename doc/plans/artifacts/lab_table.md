# Artifact: Lab Table

> table = top_surface + legs[4] + frame, all from rounded box primitives

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** 3d, procedural, waves | **Themes:** music, oscillation, physics, waves

Procedural wooden lab table with legs and support frame. Surface for placing instruments and apparatus. The workbench where oscillation becomes experiment.

*The table is the zero plane from which all measurement begins.*

## Design

- **Visual:** material: shader, animation: static
- **Interaction:** Provide a stable horizontal surface for laboratory instruments in VR
- **Critical Parameter:** base_color — sets the wood tone that grounds the lab aesthetic
- **What Emerges:** the functional foundation — every experiment needs a table
- **Triggers:** create_table() builds the full furniture piece procedurally from box vertices

## Architecture

| | |
|---|---|
| **File** | `commons/lab/table/table.gd` (203 lines) |
| **Scene** | `res://commons/lab/table/table.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | shader_demo |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `create_table()`
- `create_tabletop(parent: Node3D)`
- `create_table_legs(parent: Node3D)` -- Front left
- `create_table_frame(parent: Node3D)` -- Create support beams between legs
- `create_rounded_box_vertices(center: Vector3, size: Vector3, bevel: float)` -- For simplicity, just return regular box vertices
- `create_box_vertices(center: Vector3, size: Vector3)`
- `create_box_faces()` -- Front
- `add_triangle_with_normal(st: SurfaceTool, vertices: Array, face: Array)`
- `apply_furniture_material(mesh_instance: MeshInstance3D, color: Color)` -- Create shader material using the same approach as dodecahedron
- `set_base_color(color: Color)` -- Update all child materials
- `update_child_materials(node: Node, color: Color)`

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Pendulum
- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Effect_Sound
- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_AirMusic

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_AirMusic | [2,1] | `-` |
| WaveFunctions_AirMusic | [2,9] | `-` |
| WaveFunctions_Effect_Sound | [11,3] | `-` |
| WaveFunctions_Pendulum | [10,1] | `-` |
| WaveFunctions_Pendulum | [10,9] | `-` |

### Relationships

- depends on SurfaceTool mesh generation; supports all lab instruments (microscope, multimeter, scales); unlocks lab spatial organization
- Needs: VR object placement surface [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
