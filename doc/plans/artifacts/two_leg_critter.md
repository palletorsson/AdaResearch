# Artifact: Two Leg Critter

> bipedal_balance(t) = alternate(foot_L, foot_R) -- constant negotiation with falling

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** procedural, ik, legs, locomotion, bipedal | **Themes:** gameplay, hazards

Two-legged procedural walker. Bipedal gait emerges from alternating step cycles. The simplest creature that must solve balance.

*two legs means constant falling -- every step is a crisis of balance resolved just in time.*

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [2.0, 1.0, 2.0] (compact)
- **Interaction:** two legs in perpetual controlled falling -- the human gait's instability made visible
- **Critical Parameter:** _foot_L / _foot_R IK targets -- alternating support creates the bipedal problem
- **What Emerges:** bipedal locomotion as managed instability -- walking is falling and catching yourself
- **Triggers:** gait cycle alternates feet; patrol_timer drives direction; each step is a caught fall

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/octapod_crawler/two_leg_critter.gd` (314 lines) |
| **Scene** | `res://commons/hazards/octapod_crawler/two_leg_critter.tscn` |
| **Registry** | `hazards.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `move_speed` | float | 2.0 |
| `turn_speed` | float | 40.0 |
| `patrol_speed` | float | 1.0 |
| `step_threshold` | float | 1.5 |
| `step_height` | float | 1.0 |
| `step_duration` | float | 0.25 |
| `step_overshoot` | float | 0.5 |

### Key Methods

- `_ready()`
- `_process(delta: float)` -- ── Movement ──────────────────────────────────────────────────────────
- `_update_gait(delta: float)` -- Compute where each foot's "home" position is (in world space)
- `_begin_step(leg_index: int, home: Vector3)`
- `_position_foot(leg_index: int, foot_target: Marker3D, _home: Vector3)`
- `_add_skinned_mesh(skeleton: Skeleton3D, mesh_name: String)`
- `_make_tapered_tube(bone_count: int, spacing: float, base_r: float, tip_r: float, segs: int)`
- `_add_foot_vis(foot: Marker3D)`
- `_build_scene()`

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_3

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_3 | [3,3] | `-` |

### Relationships

- second in leg progression; introduces bilateral symmetry
- Needs: MeshInstance3D [has]; dual IK chains [has]; alternating gait [has]; patrol [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
