# Artifact: K-Means Simple

> Simplified K-means clustering demonstration — a minimal version showing the core iterative assignment-and-update loop.

## Context

**Category:** machinelearning | **Complexity:** beginner
**Tags:** k_means, clustering, unsupervised, simple | **Themes:** ai, machine_learning

Simplified K-means clustering demonstration — a minimal version showing the core iterative assignment-and-update loop.

## Design

- **Scale:** [20.0, 15.0, 25.0] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/kmeansclustering/kmeans_simple.gd` (233 lines) |
| **Scene** | `res://algorithms/machinelearning/kmeansclustering/kmeans_simple.tscn` |
| **Registry** | `machinelearning.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `data_point_count` | int | 50 |
| `cluster_count` | int | 3 |
| `iteration_speed` | float | 2.0 |
| `data_space_size` | float | 20.0 |
| `cluster_spread` | float | 3.0 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `generate_data()`
- `initialize_centroids()`
- `create_visuals()` -- Create data point visuals
- `start_clustering()`
- `perform_clustering_step()`
- `update_visuals()` -- Update point colors
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
