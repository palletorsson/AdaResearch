# Artifact: Elliptic Surface

> K > 0 — positive Gaussian curvature; parallel lines converge, triangles have angle sum > 180°

## Context

**Category:** geometry | **Complexity:** intermediate
**Tags:** elliptic, positive_curvature, non_euclidean, surface, alternative_geometry | **Themes:** geometry, non_euclidean

Dome/sphere-like surface demonstrating positive Gaussian curvature (K > 0) — where parallel lines converge and triangle angles sum to more than 180 degrees.

*on a positively curved surface, there is no room for parallel lines — all paths eventually meet*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** touch a dome and feel space curve inward — lines that start parallel must eventually meet
- **Critical Parameter:** radius — controls how tightly space curves
- **What Emerges:** the realization that on a sphere there are NO parallel lines at all
- **Triggers:** static geometry; presence alone teaches that parallel lines can converge

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/elliptic_surface/elliptic_surface.gd` (59 lines) |
| **Scene** | `res://commons/artifacts/elliptic_surface/elliptic_surface.tscn` |
| **Registry** | `alternative_geometries.json` |
| **Class** | `EllipticSurface` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `radius` | float | 0.4 |
| `resolution` | int | 24 |

### Key Methods

- `_ready()`
- `_create_surface()`
- `_create_label()`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config


## Curriculum Position

### Sequences

- **Foundations Crisis: The Limits of Formalization** (foundationscrisis) -- map: NonEuclidean_Spaces

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| NonEuclidean_Spaces | [4,10] | `:0:0.5:1` |

### Relationships

- contrasts hyperbolic_surface (K<0, lines diverge); depends on curvature_slider; paired with angle_sum_triangle
- Needs: VR controls [missing] — could add curvature_slider connection

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
