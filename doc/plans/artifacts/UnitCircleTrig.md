# Artifact: UnitCircleTrig

> (cos(theta), sin(theta)) on circle -> sin projected as wave, cos projected as wave

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** waves | **Themes:** oscillation, physics, waves

Unit circle generating Sine and Cosine

*Rotation is the generator of all oscillation; sine and cosine are its shadows.*

## Design

- **Visual:** material: unlit, animation: vertex
- **Scale:** [6.0, 6.0, 8.0] (environment)
- **Interaction:** See rotation on the unit circle continuously generate sine and cosine wave trails
- **Critical Parameter:** rotation_speed — controls how fast the angle sweeps and waves extend
- **What Emerges:** visual proof that sine and cosine are projections of circular motion
- **Triggers:** continuous angle increment projects position onto two perpendicular wave axes

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/oscillation_driver/UnitCircleTrig.gd` (150 lines) |
| **Scene** | `res://algorithms/wavefunctions/oscillation_driver/UnitCircleTrig.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `radius` | float | 1.5 |
| `rotation_speed` | float | 1.5 |
| `time_speed` | float | 2.0 |
| `max_trail_length` | int | 400 |
| `projection_offset` | float | 2.5 |

### Key Methods

- `_ready()`
- `_setup_visuals()` -- Sine Trail (Red)
- `_process(delta: float)`
- `_update_trail_points(points: Array[Vector3], delta: float)` -- Move points along Z (Time)
- `_draw_trail(mesh: ImmediateMesh, points: Array[Vector3])`
- `_draw_connections()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Synthesis_Lab

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_Synthesis_Lab | [8,2] | `:-90:0:0.1` |

### Relationships

- depends on ImmediateMesh trail drawing; contrasts with OscillationCurve (generation vs observation); unlocks unit circle intuition
- Needs: VR angle control [missing], projection distance slider [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
