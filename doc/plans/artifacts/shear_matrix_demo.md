# Artifact: Shear Matrix Demo

> Wall panel showing shearing transformation with the visible 2x2 matrix. A unit square (blue) gets sheared into a parallelogram (orange) with fixed shear factor k=0.5. Shows [[1,k],[0,1]] matrix and transformation arrow between original and sheared shapes.

## Context

**Category:** transforms | **Complexity:** advanced
**Tags:** linear_algebra, matrix, transforms | **Themes:** geometry, math, transforms

Wall panel showing shearing transformation with the visible 2x2 matrix. A unit square (blue) gets sheared into a parallelogram (orange) with fixed shear factor k=0.5. Shows [[1,k],[0,1]] matrix and transformation arrow between original and sheared shapes.

## Design

- **Visual:** material: vertex_color, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/shear_matrix_demo/shear_matrix_demo.gd` (238 lines) |
| **Scene** | `res://commons/artifacts/shear_matrix_demo/shear_matrix_demo.tscn` |
| **Registry** | `transforms.json` |
| **Class** | `ShearMatrixDemo` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `shear_factor` | float | 0.5 |
| `shape_size` | float | 0.25 |
| `color_original` | Color | Color(0.3, 0.5, 1.0) |
| `color_sheared` | Color | Color(1.0, 0.6, 0.2) |
| `color_panel` | Color | Color(0.08, 0.08, 0.12) |
| `color_arrow` | Color | Color(0.7, 0.7, 0.8) |

### Key Methods

- `_ready()`
- `_build_panel()`
- `_build_original_square()`
- `_build_sheared_parallelogram()`
- `_build_arrow()`
- `_build_labels()` -- Title
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `shape_size`, `shear_factor`

## Curriculum Position

### Sequences

- transformation

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
