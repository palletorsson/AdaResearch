# Artifact: Monte Carlo Estimator

> Estimate mathematical quantities through random sampling — watch as dart throws approximate pi, random walks estimate integrals, and stochastic processes converge to deterministic values. The power of randomness as a computational tool.

## Context

**Category:** emergence | **Complexity:** intermediate
**Tags:** monte_carlo, estimation, random_sampling, pi, stochastic | **Themes:** commons, emergence, mixed

Estimate mathematical quantities through random sampling — watch as dart throws approximate pi, random walks estimate integrals, and stochastic processes converge to deterministic values. The power of randomness as a computational tool.

## Design

- **Visual:** material: shader, animation: particle_lifecycle
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/monte_carlo_estimator/monte_carlo_estimator.gd` (333 lines) |
| **Scene** | `res://commons/artifacts/monte_carlo_estimator/monte_carlo_estimator.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `MonteCarloEstimator` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `board_size` | float | 0.5 |
| `darts_per_second` | float | 20.0: |
| `max_darts` | int | 2000 |
| `auto_throw` | bool | true |
| `color_inside` | Color | Color(0.3, 0.8, 1.0) |
| `color_outside` | Color | Color(1.0, 0.4, 0.3) |
| `color_circle` | Color | Color(0.8, 0.8, 0.8, 0.3) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_board()`
- `_create_circle()`
- `_create_dart_multimesh()`
- `_create_labels()`
- `_create_vr_controls()`
- `_process(delta: float)`
- `_throw_dart()`
- `_clear_darts()`
- `_update_pi_display()`
- `_input(event: InputEvent)`
- `throw()`
- `clear()`
- `get_estimate()`
- `_exit_tree()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- randomness

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
