# Artifact: Sphere Droideka

> sphere_roll(v) -> deploy(turret) -- spherical droideka with shield and beam weapon

## Context

**Category:** hazards | **Complexity:** beginner
**Tags:** geometry, hazards | **Themes:** gameplay, hazards

Armadillidiidae-style spherical droideka. Rolls as compact ball of overlapping shell plates, unfolds to tripod stance with shield dome and twin blasters. Star Wars droideka inspired.

*the sphere is the most efficient armor -- minimum surface for maximum volume, weaponized by deployment.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** smooth sphere rolling at speed, deploying into turreted form with shield and beam
- **Critical Parameter:** deployment state -- ROLLED (fast, invulnerable) vs DEPLOYED (stationary, armed, shielded)
- **What Emerges:** the sphere IS the armor -- rolling geometry provides speed, deployment trades mobility for firepower
- **Triggers:** player distance triggers deploy/retract; shield absorbs when deployed; beam targets player

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/sphere_droideka/sphere_droideka.gd` (745 lines) |
| **Scene** | `res://commons/hazards/sphere_droideka/sphere_droideka.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `SphereDroideka` extends `CharacterBody3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `shell_segments` | int | 8 |
| `shell_slices` | int | 12 |
| `ball_radius` | float | 0.35 |
| `deployed_height` | float | 1.2 |
| `leg_count` | int | 3 |
| `max_health` | float | 120.0 |
| `roll_speed` | float | 4.5 |
| `detection_radius` | float | 12.0 |
| `attack_radius` | float | 10.0 |
| `fire_speed` | float | 18.0 |
| `fire_damage` | float | 14.0 |
| `shots_per_burst` | int | 6 |
| `fire_interval` | float | 0.15 |
| `shield_strength` | float | 50.0 |
| `unroll_duration` | float | 4.0 |
| `deploy_duration` | float | 3.0 |
| `retract_duration` | float | 2.5 |
| `rollup_duration` | float | 3.0 |
| `shell_color` | Color | Color(0.25, 0.28, 0.32, 1.0) |
| `inner_color` | Color | Color(0.15, 0.15, 0.18, 1.0) |
| `accent_color` | Color | Color(0.8, 0.3, 0.1, 1.0) |
| `shield_color` | Color | Color(0.3, 0.5, 1.0, 0.3) |

### Dependencies

- `res://commons/hazards/armadillo_droideka/fire_bolt.tscn`

### Key Methods

- `_ready()`
- `_physics_process(delta: float)`
- `_process_ball(delta: float)`
- `_process_unroll(_delta: float)`
- `_process_deploy(_delta: float)`
- `_process_aim(delta: float)`
- `_process_fire(delta: float)`
- `_process_retract(_delta: float)`
- `_process_rollup(_delta: float)`
- `_process_dead(delta: float)`
- `_set_state(new_state: State)`
- `_create_materials()`
- `_build_shell()`
- `_create_shell_plate(lat_top: float, lat_bottom: float, lon_left: float, lon_right: float)`
- `_build_legs()`

### Signals

- `fired_projectile`
- `enemy_destroyed`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo | [7,9] | `-` |

### Relationships

- contrasts with armadillo_droideka (biological vs mechanical); sphere roller family
- Needs: class_name SphereDroideka [has]; roll physics [has]; shield system [has]; beam weapon [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
