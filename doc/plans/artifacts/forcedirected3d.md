# Artifact: forcedirected3d

> F_repel = k/r^2 (all pairs), F_spring = -k*(L-L0) (edges only). Repulsion spreads nodes apart; springs hold connected nodes together. Layout is equilibrium.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** algorithm | **Themes:** algorithms, misc

forcedirected3d

*A good layout is a force equilibrium. The graph does not know its shape — physics finds it by minimizing energy.*

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle
- **Scale:** [21.0, 24.0, 23.0] (xlarge)
- **Interaction:** To watch a graph untangle itself — nodes pushing apart, edges pulling together, the layout emerging from physics rather than being designed.
- **Critical Parameter:** temperature (simulated annealing). It starts at 1.0 and cools by 0.995 per step. High temperature = large movements, exploration. Low temperature = fine adjustment, convergence.
- **What Emerges:** Clusters forming for densely connected subgraphs. Simulated annealing cooling the system into a stable layout. Stress coloring revealing which nodes are under the most force.
- **Triggers:** Auto-run on ready → simulation iterates, SPACE → pause/resume, R → regenerate graph, F → toggle force arrows, E → toggle edges, S → toggle stress coloring

## Architecture

| | |
|---|---|
| **File** | `algorithms/graphtheory/forcedirected3d/forcedirected3d.gd` (863 lines) |
| **Scene** | `res://algorithms/graphtheory/forcedirected3d/forcedirected3d.tscn` |
| **Registry** | `algorithms_misc.json` |
| **Class** | `ForceDirected3D` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `node_count` | int | 20 |
| `edge_probability` | float | 0.15 |
| `layout_bounds` | Vector3 | Vector3(50, 30, 50) |
| `initial_distribution` | String | "random" |
| `repulsion_strength` | float | 200.0 |
| `spring_constant` | float | 0.08 |
| `rest_length` | float | 8.0 |
| `damping_factor` | float | 0.92 |
| `gravity_strength` | float | 0.01 |
| `min_distance` | float | 0.5 |
| `max_force` | float | 50.0 |
| `max_iterations` | int | 2000 |
| `convergence_threshold` | float | 0.05 |
| `auto_run` | bool | true |
| `time_step` | float | 0.016 |
| `adaptive_timestep` | bool | true |
| `temperature_cooling` | float | 0.995 |
| `pause_on_convergence` | bool | true |
| `show_forces` | bool | true |
| `show_edges` | bool | true |
| `show_energy_indicators` | bool | true |
| `show_node_trails` | bool | false |
| `node_trail_length` | int | 30 |
| `force_visualization_scale` | float | 0.1 |
| `stress_color_mapping` | bool | true |
| `enable_vr_manipulation` | bool | true |
| `manipulation_sphere_radius` | float | 1.0 |
| `grabbed_node_mass_multiplier` | float | 10.0 |
| `comfort_movement_speed` | float | 0.5 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `setup_environment()` -- Main directional light
- `setup_containers()`
- `setup_ui()`
- `initialize_graph()`
- `get_initial_position(node_id: int)`
- `create_edge(from_id: int, to_id: int)`
- `ensure_connectivity()` -- Start with node 0
- `calculate_node_degrees()`
- `create_visualization()`
- `create_node_visuals()`
- `create_edge_visuals()`
- `_compute_edge_transform(from_pos: Vector3, to_pos: Vector3)`
- `create_force_visuals()` -- Shaft — unit cylinder

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: ForcesChaos

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Forces_8 | [4,4] | `-` |
| ForcesChaos | [2,18] | `-` |
| GraphTheory_Force_Directed_3d | [6,5] | `-` |

### Relationships

- Applies force physics to graph theory (forces as layout algorithm, not simulation). Contrasts with nbody_simulation (all-pairs gravity vs spring+repulsion). Lives in ForcesChaos.
- Needs: MultiMesh node/edge/force rendering [has], VR grab nodes [has], spatial hashing [has], convergence detection [has]. Missing: community detection coloring, edge bundling.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
