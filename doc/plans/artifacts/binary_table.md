# Artifact: Binary Table Display

> Show any 2D array as a grid of values. See the data structure made visible.

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, primitives, visualization | **Themes:** arrays, data_structures

Show any 2D array as a grid of values. See the data structure made visible.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `algorithms/arrays/binary_table/binary_table_display.gd` (216 lines) |
| **Scene** | `res://algorithms/arrays/binary_table/binary_table_display.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `BinaryTableDisplay` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `rows` | int | 4 |
| `cols` | int | 4 |
| `cell_size` | float | 0.08 |
| `font_size` | int | 24 |
| `show_indices` | bool | true |
| `title` | String | "" |

### Key Methods

- `_ready()` -- Default: create empty grid based on rows/cols
- `set_data(new_data: Array)`
- `set_cell(row: int, col: int, value)`
- `_create_display()` -- Create background panel
- `_rebuild_display()` -- Remove old children
- `_update_cell_label(row: int, col: int)`
- `_get_value(row: int, col: int)`
- `_format_value(val)`
- `_get_color(val)` -- Color coding for different values - high visibility
- `apply_grid_config(config: Dictionary)`
- `_exit_tree()`

### Grid Config

Accepts: `cell_size`, `cols`, `data`, `rows`, `show_indices`, `title`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
