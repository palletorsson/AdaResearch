# Artifact: Lambda Slider

> slider_position / rail_length -> lambda in [0,1]; color_gradient(blue->green->red)

## Context

**Category:** qfep | **Complexity:** intermediate
**Tags:** interactive, slider, parameter_control, entropy | **Themes:** qfep, tools

QFEP λ parameter controller. The entropy drive dial: λ=0 (pure order) → λ=0.4 (edge of chaos) → λ=1 (dissolution). Grab and slide to tune how much a system values disorder vs order. Life exists at λ ≈ 0.3-0.5.

*lambda is the entropy drive — the dial between crystallization and dissolution that every living system must negotiate*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** grab the entropy drive and feel the system shift from crystal to chaos under your hand
- **Critical Parameter:** lambda — the single number that controls order/chaos balance across the entire QFEP system
- **What Emerges:** the green zone at 0.3-0.5 where particles and color converge — the sweet spot finds you
- **Triggers:** XR grab moves handle; slider_moved signal propagates to all connected visualizations; broadcasts globally

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/lambda_slider.gd` (391 lines) |
| **Scene** | `res://commons/interfaces/qfep/lambda_slider.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `LambdaSlider` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `lambda` | float | 0.4: |
| `rail_length` | float | 0.5 |
| `rail_height` | float | 0.02 |
| `rail_depth` | float | 0.05 |
| `handle_radius` | float | 0.03 |
| `show_particles` | bool | true |
| `show_label` | bool | true |
| `broadcast_globally` | bool | true |

### Key Methods

- `_ready()`
- `_build_slider()` -- Create the XR Tools slider - horizontal along X axis
- `_build_visuals()` -- Build the rail
- `_build_rail_gradient()` -- Create gradient segments on the rail
- `_build_scale_labels()` -- Order label (λ=0)
- `_build_particles()`
- `_on_slider_moved(position: float)` -- Convert slider position to lambda value
- `_on_slider_grabbed(_interactable)`
- `_on_slider_released(_interactable)`
- `_update_visuals()` -- Update handle position
- `_get_lambda_color(value: float)` -- Gradient: Blue (order) → Green (edge) → Red (chaos)
- `set_lambda(value: float)`
- `get_lambda()`
- `move_to(value: float)`
- `get_state_description()`

### Signals

- `lambda_changed`
- `slider_grabbed`
- `slider_released`

## Curriculum Position

### Sequences

- **Foundations Crisis: The Limits of Formalization** (foundationscrisis) -- map: Crisis_Synthesis
- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Introduction
- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_F_Term
- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_E_Term
- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Edge_Of_Chaos
- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Sandbox

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Crisis_Synthesis | [11,6] | `:0:0.5` |
| QFEP_E_Term | [9,1] | `:0:0.5#value:1.0` |
| QFEP_Edge_Of_Chaos | [11,2] | `:0:0.5#locked:0.4` |
| QFEP_F_Term | [7,1] | `:0:0.5#locked:0` |
| QFEP_Introduction | [4,2] | `:0:0.5` |
| QFEP_Sandbox | [2,2] | `:0:0.5` |

### Relationships

- controls edge_core, qfep_reactor, queer_morphology_specimen; paired with phi_slider; central to every QFEP map
- Needs: VR XR slider grab [has], visual gradient rail [has], particle feedback [has], global broadcast [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
