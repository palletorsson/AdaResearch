# Artifact: Worley Noise

> Voronoi/cellular noise texture on a floor quad. Scatters random seed points and computes F2-F1 (second-nearest minus nearest distance) for the classic cell boundary look. Complement to Perlin/Simplex noise.

## Context

**Category:** procedural | **Complexity:** beginner
**Tags:** worley, voronoi, cellular, noise, distance_field, floor_display | **Themes:** biology, probability, procedural, randomness

Voronoi/cellular noise texture on a floor quad. Scatters random seed points and computes F2-F1 (second-nearest minus nearest distance) for the classic cell boundary look. Complement to Perlin/Simplex noise.

## Design

- **Visual:** material: generated_texture, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/worley_noise/worley_noise.gd` (116 lines) |
| **Scene** | `res://commons/artifacts/worley_noise/worley_noise.tscn` |
| **Registry** | `randomness.json` |
| **Class** | `WorleyNoise` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `quad_size` | Vector2 | Vector2(0.8, 0.8) |
| `num_seeds` | int | 30 |
| `seed_value` | int | 42 |

### Key Methods

- `_ready()`
- `_build_floor_quad()`
- `_generate_texture()` -- Scatter seed points in [0, IMAGE_SIZE) space
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `num_seeds`, `seed`

## Curriculum Position

### Sequences

- noise
- randomness

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
