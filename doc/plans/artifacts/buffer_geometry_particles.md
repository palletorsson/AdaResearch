# Artifact: Buffer Geometry Particles

> Particle system using buffer geometry for GPU-driven particle effects.

## Context

**Category:** shaders | **Complexity:** advanced
**Tags:** particles, buffer, gpu, shaders | **Themes:** shaders, visual

Particle system using buffer geometry for GPU-driven particle effects.

## Design

- **Visual:** material: shader, animation: shader_time

## Architecture

| | |
|---|---|
| **File** | `algorithms/shaders/buffer/buffergeometryparticles.gd` (440 lines) |
| **Scene** | `res://algorithms/shaders/buffer/buffergeometryparticles.tscn` |
| **Registry** | `shaders.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `particle_count` | int | 10000 |
| `animation_speed` | float | 1.0 |
| `spread_radius` | float | 25.0 |
| `color_variation` | float | 1.0 |

### Key Methods

- `_ready()`
- `setup_scene()` -- Create space-like environment
- `generate_particle_data()` -- Initialize arrays
- `create_custom_particle_mesh()`
- `start_particle_animation()` -- The particles animate automatically via the shader
- `animate_shader_parameters()` -- Animate size multiplier
- `_process(_delta)` -- Optional: Update particle data in real-time
- `regenerate_particles()`
- `create_spiral_pattern()`
- `create_explosion_pattern()`
- `create_galaxy_pattern()`
- `_ready_with_pattern(pattern: String)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
