# Artifact: Noir Sequencer

> Film noir style algorithmic music sequencer

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** sound, waves | **Themes:** music, oscillation, physics, waves

Film noir style algorithmic music sequencer

## Design

- **Visual:** material: standard, animation: transform

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/noirsequencer/noir_sequencer.gd` (746 lines) |
| **Scene** | `res://algorithms/wavefunctions/noirsequencer/noir_sequencer.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()` -- Initialize the grid
- `create_tone_generator()` -- Create a simple AudioStreamPlayer
- `initialize_grid()`
- `create_grid_ui()` -- Create note labels
- `create_cell_style(active, hover)`
- `initialize_audio()` -- Print audio diagnostic information
- `setup_fallback_audio()` -- Create fallback audio players using AudioStreamGeneratorPlayback
- `play_note(note_name)`
- `try_play_note_primary(_note_name, freq)`
- `play_note_fallback(note_name, freq)` -- Use the fallback system
- `_process(delta: float)`
- `update_step_indicator()` -- Update visual indicator for current step
- `create_active_cell_style(active)`
- `update_bpm_label()` -- Time per 16th note
- `set_status(message)`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
