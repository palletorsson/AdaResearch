# Artifact: Cube Desk

> desk = table_form + grid_subdivide(drawer_unit, rows * cols), nested subdivision hierarchy

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

Modern desk with drawer unit created through cube subdivision

*A desk is a table that subdivided one of its own volumes into a grid — recursion within recursion.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** To be used — drawers that emerged from subdividing a box within a box, functional complexity from recursive cuts
- **Critical Parameter:** drawer_rows * drawer_cols — the grid resolution of the drawer unit determines storage granularity
- **What Emerges:** Cable management holes and accent strips appear as final-step details — the desk develops personality
- **Triggers:** Construction sequence: desktop -> legs -> drawer frame -> individual drawers with handles -> details

## Architecture

| | |
|---|---|
| **File** | `algorithms/fractals/cubesubdivision/cube_desk.gd` (274 lines) |
| **Scene** | `res://algorithms/fractals/cubesubdivision/cube_desk.tscn` |
| **Registry** | `procgen_extra.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `desk_size` | float | 2.0 |
| `drawer_rows` | int | 2 |
| `drawer_cols` | int | 3 |
| `show_construction` | bool | true |
| `step_delay` | float | 0.25 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_execute_step(s: int)`
- `_build_instant()`
- `_step_desktop()` -- Main work surface
- `_step_legs()`
- `_step_drawer_frame()` -- Frame that holds the drawers (left side of desk)
- `_step_back_panel()`
- `_step_single_drawer(row: int, col: int)`
- `_step_details()`
- `_create_part(pos: Vector3, size: Vector3, color: Color, part_name: String)`
- `reset()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Fractals: Infinite Within Finite** (fractals) -- map: Fractal_RecursiveTrees

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Fractal_RecursiveTrees | [6,1] | `-` |

### Relationships

- Most complex cube-subdivision furniture; builds on recursive_table by adding nested second-level subdivision
- Needs: VR drawer-open interaction [missing], construction speed control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
