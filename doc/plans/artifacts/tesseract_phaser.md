# Artifact: Tesseract Phaser

> project_4D(vertices, angle_xw, angle_yw) -> 3D -- wireframe tesseract rotating through 4th dimension

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** 4d, hypercube, tesseract, projection, rotation | **Themes:** gameplay, hazards

Wireframe hypercube with 16 vertices and 32 edges. 4D rotation phases parts in and out of hittability — vertex alpha based on W-depth. The fourth dimension as combat mechanic.

*the tesseract rotates through dimensions you cannot see -- vertices phase in and out by W-depth.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a hypercube whose vertices phase in/out of hittability based on W-depth coordinate
- **Critical Parameter:** _angle_xw / _angle_yw -- 4D rotation angles determine which vertices reach 3D space
- **What Emerges:** the fourth dimension as combat mechanic -- the creature is partly elsewhere, and elsewhere rotates
- **Triggers:** continuous 4D rotation; vertices phase by W-depth; only vertices near W=0 can be hit

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/tesseract_phaser/tesseract_phaser.gd` (195 lines) |
| **Scene** | `res://commons/hazards/tesseract_phaser/tesseract_phaser.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `TesseractPhaser` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `edge_length` | float | 0.3 |
| `rotation_speed_4d` | float | 0.5 |
| `vertex_radius` | float | 0.03 |
| `edge_thickness` | float | 0.01 |
| `inner_color` | Color | Color(0.3, 0.5, 1.0, 0.7) |
| `outer_color` | Color | Color(1.0, 0.7, 0.2, 0.9) |
| `edge_color_val` | Color | Color(0.5, 0.6, 0.8, 0.6) |
| `emission_val` | Color | Color(0.3, 0.4, 1.0) |

### Key Methods

- `_on_ready()`
- `_generate_tesseract()` -- 16 vertices of a tesseract: all combinations of ±1 in 4D
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()` -- Create vertex meshes
- `_process_visual(delta: float)` -- 4D rotation

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_7

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_7 | [7,1] | `-` |

### Relationships

- embodies higher_dimensions sequence; the only creature partly outside 3D space
- Needs: HazardCreatureBase [has]; 4D vertex/edge system [has]; projection [has]; W-depth hitbox [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
