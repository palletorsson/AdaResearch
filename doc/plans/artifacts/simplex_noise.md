# Artifact: Simplex Noise

> simplex(p) = Σ contribution(simplex_vertex) over the n+1 vertices of the simplex enclosing p, where contributions use (r² - dist²)⁴ falloff — no grid artifacts because simplexes have no axis-aligned edges

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** noise, procedural | **Themes:** probability, procedural, randomness

Interactive Simplex noise generation and visualization for high-quality procedural content

*Simplex noise solved Perlin's axis-alignment problem by changing the grid — the simplex lattice has no preferred directions, so randomness becomes genuinely isotropic*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [10, 3, 10] (xlarge)
- **Interaction:** to compare two landscapes side-by-side and feel the difference between Perlin's rectangular grid bias and Simplex's isotropic smoothness — to see algorithm aesthetics with your body
- **Critical Parameter:** persistence — controls how much each successive octave contributes relative to the previous; high persistence makes noise spiky and self-similar, low persistence makes it smooth with barely-visible detail
- **What Emerges:** the learner notices that Simplex noise has no preferred directions — mountain ridges don't tend to align with axes the way Perlin's do, which becomes perceptible when generating large terrains
- **Triggers:** compare_button is designed to show Perlin vs Simplex simultaneously — currently logs to console but demonstrates the design intent of architectural comparison

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/simplexnoise/simplexnoise.gd` (62 lines) |
| **Scene** | `res://algorithms/randomness/simplexnoise/simplexnoise.tscn` |
| **Registry** | `randomness.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()` -- Connect UI signals
- `_on_frequency_changed(value)`
- `_on_amplitude_changed(value)`
- `_on_persistence_changed(value)`
- `_on_regenerate_pressed()`
- `_on_compare_pressed()` -- This could open a comparison scene or overlay
- `_update_noise_parameters()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Noise: Entropy with Memory** (noise) -- map: Noise_Perlin_Simplex

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Noise_Perlin_Simplex | [3,2] | `-` |
| TestMap_Unused_23 | [3,3] | `:0:2` |

### Relationships

- contrasts with PerlinNoise.gd (same scene architecture, different algorithm); shares the Noise_Perlin_Simplex map for direct comparison; Simplex is Perlin's successor — fewer visual artifacts, better performance in higher dimensions
- Needs: frequency slider [has] (2D UI); amplitude slider [has]; persistence slider [has]; compare button [has] (non-functional); regenerate button [has]; no VR slider_horizontal controls [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
