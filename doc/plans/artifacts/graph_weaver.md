# Artifact: Graph Weaver

> G(V,E).traverse(algorithm) -> attack(path) -- 8 sphere nodes reconfiguring edges via BFS/DFS

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** graph, bfs, dfs, shortest-path, network | **Themes:** gameplay, hazards

Network of 8 sphere-nodes connected by cylinder-edges. Reconfigures edges to create shortest path toward player with visible BFS/DFS traversal. Can cast edges as projectile attacks.

*the graph hunts through its own edges -- connectivity is not abstract, it is the weapon.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a network creature rewiring its graph to hunt -- edges appear and vanish as algorithms execute
- **Critical Parameter:** _edges / traversal algorithm -- active edges determine attack reach and path
- **What Emerges:** graph theory as predation -- connectivity IS the attack surface, rewiring IS the strategy
- **Triggers:** player proximity triggers algorithm selection; BFS/DFS/shortest-path each hunt differently

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/graph_weaver/graph_weaver.gd` (486 lines) |
| **Scene** | `res://commons/hazards/graph_weaver/graph_weaver.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `GraphWeaver` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_nodes` | int | 8 |
| `node_radius` | float | 0.05 |
| `edge_radius` | float | 0.01 |
| `arrangement_radius` | float | 0.5 |
| `orbit_speed` | float | 0.15 |
| `reconfigure_interval` | float | 4.0 |
| `traversal_speed` | float | 0.3 |
| `edge_fire_damage` | float | 10.0 |
| `projectile_speed` | float | 5.0 |

### Key Methods

- `_on_ready()`
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()`
- `_generate_spanning_tree()` -- Prim's algorithm for random spanning tree
- `_rebuild_edge_meshes()` -- Clear old edge meshes
- `_bfs_from(start: int)` -- Build adjacency list
- `_find_shortest_path_to_closest()` -- Find the node closest to the player
- `_process_visual(delta: float)`
- `_process_chase(delta: float)`
- `_reconfigure_edges()` -- Reset all node colors
- `_fire_edge_projectile()`
- `_on_damaged(_amount: float)`
- `_on_state_changed(new_state: BaseState)` -- Trigger soon

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_4

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_4 | [7,5] | `-` |

### Relationships

- embodies graphtheory sequence; pairs with data_tree_walker (graph vs tree)
- Needs: HazardCreatureBase [has]; 8-node graph [has]; BFS/DFS/shortest-path [has]; edge viz [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
