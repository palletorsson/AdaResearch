# Artifact: Folding Zoo

> six zones demonstrate the fold system's full range — bone, chain, shell, states, misfold, tension

## Context

**Category:** hazards | **Complexity:** exhibition
**Tags:** fold_system, zoo, showcase, demo, bone_solver, chain_solver, shell_solver | **Themes:** fold_system

Six-zone interactive showcase of the fold system — bone fold, chain fold, shell fold, state parade, misfold, and spring tension. All three solvers demonstrated with debug overlays.

*If you can see all fold families breathing in one room, the system is real.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** a living laboratory where every fold family breathes, oscillates, and responds to inspection
- **Critical Parameter:** _global_fold — sinusoidal oscillation that drives all non-frozen creatures
- **What Emerges:** side-by-side comparison reveals how one fold variable (fold_amount) creates radically different motion across solvers
- **Triggers:** placed as artifact in map; self-contained environment with floor, lighting, labels

## Architecture

| | |
|---|---|
| **File** | `commons/fold_system/zoo/folding_zoo.gd` (447 lines) |
| **Scene** | `res://commons/fold_system/zoo/folding_zoo.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `FoldingZoo` extends `Node3D` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_process(delta: float)` -- Global oscillation: sinusoidal 0..1 with period OSCILLATION_PERIOD seconds.
- `_build_environment()`
- `_build_floor()`
- `_build_zone_label(text: String, pos: Vector3)`
- `_build_demo_critter(solver: FoldSolver, color: Color)`
- `_build_chain_demo()`
- `_update_chain_demo()`
- `_build_shell_demo()`
- `_update_shell_demo()`
- `_make_emissive_material(color: Color, emission_strength: float)`
- `_make_sphere_mesh(radius: float, material: StandardMaterial3D)`
- `_create_debug_label(parent: Node3D, offset: Vector3)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Folding: Same Faces, Only Positions Change** (fold_system) -- map: Folding_Zoo

### Relationships

- test harness for the entire fold system; visual proof that the architecture works
- Needs: FoldCritter [has]; all three solvers [has]; FoldDemoCritter [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
