# Artifact: Heap Operations

> Min-heap and max-heap with sift-up and sift-down operations animated. The structure behind priority queues — always know your most important element in O(1), insert and extract in O(log n).

## Context

**Category:** datastructures | **Complexity:** intermediate
**Tags:** heap, priority_queue, sift, min_heap, max_heap, data_structure | **Themes:** algorithms, data_structures

Min-heap and max-heap with sift-up and sift-down operations animated. The structure behind priority queues — always know your most important element in O(1), insert and extract in O(log n).

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [17, 20, 9] (world_scale)

## Architecture

| | |
|---|---|
| **File** | [No dedicated GDScript found] |
| **Scene** | `res://algorithms/datastructures/heap_operations/heap_operations.tscn` |
| **Registry** | `datastructures.json` |
| **Pattern** | unknown |

## Curriculum Position

### Sequences

- datastructures

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
