# Artifact: Meander Threshold Floor

> a floor that marks the liminal space of doorways with meander geometry

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pompeii, meander, threshold | **Themes:** arrays, art, cultural_computation

Pompeii mosaic threshold floor with bold Greek key meander border framing a dark field with scattered white tesserae. Common Roman doorway pattern placed at transitions between rooms.

*the meander has guarded doorways for three thousand years*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players step across a threshold and recognise the oldest border ornament
- **Critical Parameter:** meander_width — pixel rows consumed by the meander band
- **What Emerges:** the understanding that a threshold is a frame, and a frame is a pattern
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/meander_threshold_floor.gd` (348 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/meander_threshold_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `MeanderThresholdFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.2, 0.8) |
| `meander_width` | int | 10 |
| `dot_spacing` | int | 4 |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.02 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`
- `_build_meander_grid(gw: int, gh: int, border: int)`
- `_fill_rect(grid: Array, gw: int, gh: int, r0: int, c0: int, h: int, w: int)`
- `_hash_2d(x: int, y: int)`

### Grid Config

Accepts: `dot_spacing`, `floor_size`, `meander_width`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- swastika_meander_floor (sibling meander), polka_dot_field_floor (dot logic)
- Needs: [implemented] procedural mesh, meander pixel grid, polka dot field

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
