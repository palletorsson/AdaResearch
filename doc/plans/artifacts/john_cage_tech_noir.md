# Artifact: john_cage_tech_noir

> ambient(t) = drone(t) + sum(random_effect_i(t)) with stochastic scheduling

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** probability, procedural, waves | **Themes:** oscillation, physics, waves

Procedurally composes endless techno-noir ambience with city drones, random effects, and a loading-bar visualizer.

*Silence is not the absence of sound — it is the space between algorithmically chosen events.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** Stand in a space where algorithmic composition endlessly generates cyberpunk ambience
- **Critical Parameter:** num_effect_players — determines the density of the random sound event texture
- **What Emerges:** endless non-repeating ambient music from a small vocabulary of synthesis rules
- **Triggers:** stochastic timers trigger random effect generation; drone layer provides continuous foundation

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/technoirgameaudio/john_cage_tech_noir.gd` (1281 lines) |
| **Scene** | `res://algorithms/wavefunctions/technoirgameaudio/john_cage_tech_noir.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural_mesh |

### Key Methods

- `_ready()`
- `_ensure_rng()`
- `_randf_range(min_value: float, max_value: float)`
- `_randf()`
- `_randi()`
- `_ensure_thread_rng()`
- `_t_randf_range(min_value: float, max_value: float)`
- `_t_randf()`
- `_t_randi()`
- `_process(delta: float)`
- `start_sound_generation()`
- `_thread_generate_sounds()`
- `_emit_sound_created(sound_name: String)`
- `_emit_progress_updated()`
- `_emit_generation_complete()`

### Signals

- `sound_generation_started`
- `sound_created`
- `generation_progress_updated`
- `sound_generation_complete`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_John_Cage

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| WaveFunctions_John_Cage | [12,5] | `-` |

### Relationships

- depends on threaded audio generation; contrasts with SoundscapeRadioRack (generative vs preset ambience); unlocks algorithmic composition
- Needs: VR presence [has], 3D loading bar visualization [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
