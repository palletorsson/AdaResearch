# Artifact: Spiral Mosaic Floor

> a floor that traces the oldest curve known to human hands

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, roman, spiral, archimedean, ancient | **Themes:** arrays, art, cultural_computation

Procedural Pompeii mosaic floor with Archimedean spiral pattern. A single continuous dark spiral band winds from a central rosette outward on a light background, widening as it goes. Ancient symbol found across cultures from Newgrange to Roman mosaics.

*the spiral is the shape of becoming — always the same, always further from where it began*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see the spiral path that cultures worldwide discovered independently
- **Critical Parameter:** num_rotations — number of full spiral turns
- **What Emerges:** the understanding that growth and motion share one equation: r = a + b*theta
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/spiral_mosaic_floor.gd` (354 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/spiral_mosaic_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `SpiralMosaicFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_radius` | float | 0.55 |
| `num_rotations` | float | 4.5 |
| `spiral_band_fraction` | float | 0.35 |
| `border_bands` | Array[int] | [2, 1, 1] |
| `border_tile_size` | float | 0.025 |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `color_accent` | Color | MosaicPalette.TERRACOTTA |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.04 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`
- `_add_ring_grout(verts: PackedVector3Array, radius: float, half_w: float, segments: int)`
- `_add_spiral_grout_segment(verts: PackedVector3Array, theta0: float, theta1: float, r0: float, r1: float, half_w: float)` -- Grout quad strip following the spiral edge

### Grid Config

Accepts: `border_bands`, `floor_radius`, `num_rotations`, `spiral_band_fraction`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- pompeii_mosaic_floor (sibling pattern), mosaic_palette (shared colors)
- Needs: [implemented] procedural mesh, border bands, Archimedean spiral, center rosette

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
