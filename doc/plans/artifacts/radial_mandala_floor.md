# Artifact: Radial Mandala Floor

> a floor that radiates sacred geometry from a single center point

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pompeii, mandala, radial | **Themes:** arrays, art, cultural_computation

Procedural Pompeii mosaic floor with concentric radial mandala pattern. A terracotta 6-pointed star rosette at center, surrounded by rings of alternating dark/light triangles radiating outward with interlocking offset.

*every mandala begins with a point and the courage to draw a circle around it*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and see the mandala logic of Roman cosmological floors
- **Critical Parameter:** num_rings — number of concentric triangle rings
- **What Emerges:** the understanding that radial symmetry is tessellation freed from the grid
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/radial_mandala_floor.gd` (342 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/radial_mandala_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `RadialMandalaFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_radius` | float | 0.55 |
| `num_rings` | int | 5 |
| `base_segments` | int | 6 |
| `segments_per_ring_mult` | int | 2 |
| `border_bands` | Array[int] | [2, 1, 1] |
| `border_tile_size` | float | 0.025 |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `color_accent` | Color | MosaicPalette.TERRACOTTA |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.04 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`
- `_add_ring_grout(verts: PackedVector3Array, radius: float, half_w: float, segments: int)`
- `_add_radial_grout(verts: PackedVector3Array, angle: float, r_inner: float, r_outer: float, half_w: float)` -- Thin quad along a radial line

### Grid Config

Accepts: `base_segments`, `border_bands`, `floor_radius`, `num_rings`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- pompeii_mosaic_floor (sibling pattern), mosaic_palette (shared colors)
- Needs: [implemented] procedural mesh, border bands, radial triangle rings, center star

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
