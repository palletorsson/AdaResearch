# Artifact: Spring Mass Bouncer

> F = -k*x -- 3x3 grid of mass nodes connected by springs obeying Hooke's law

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** spring, mass, hooke, soft-body, deformation | **Themes:** gameplay, hazards

3x3 grid of sphere mass-nodes connected by springs with Hooke's law physics. Deforms on contact; spring tension equals damage. Visible soft-body simulation as creature.

*F = -k*x -- the spring knows only displacement and stiffness, yet soft body dynamics emerge.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** nine masses connected by springs deforming under stress, tension converted to damage
- **Critical Parameter:** spring constants / mass positions -- k determines stiffness; deformation generates damage
- **What Emerges:** soft body physics as threat -- the jiggling mass-spring system damages through deformation
- **Triggers:** continuous spring simulation; masses oscillate under Hooke's law; contact triggers damage

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/spring_mass_bouncer/spring_mass_bouncer.gd` (233 lines) |
| **Scene** | `res://commons/hazards/spring_mass_bouncer/spring_mass_bouncer.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `SpringMassBouncer` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `spring_k` | float | 40.0 |
| `damping` | float | 2.0 |
| `node_mass` | float | 1.0 |
| `rest_length` | float | 0.18 |
| `node_radius` | float | 0.05 |
| `base_damage` | float | 10.0 |
| `tension_damage_multiplier` | float | 3.0 |

### Key Methods

- `_on_ready()`
- `_create_materials()` -- Materials created per-node in _build_mesh
- `_build_collision()`
- `_build_mesh()`
- `_process_visual(delta: float)`
- `_process_chase(delta: float)`
- `_on_damaged(_amount: float)` -- Excite springs — add random velocity to all nodes
- `_on_state_changed(new_state: BaseState)` -- Slightly excite springs on chase start

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_5

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_5 | [3,3] | `-` |

### Relationships

- embodies softbodies sequence; pairs with collision_crasher (elasticity vs impulse)
- Needs: HazardCreatureBase [has]; 3x3 mass-spring grid [has]; Hooke's law [has]; tension viz [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
