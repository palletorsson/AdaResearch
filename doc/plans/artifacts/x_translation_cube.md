# Artifact: X-Translation Cube

> An animated cube that moves only along the X axis. Ghosts and labels keep the motion explicit, so the learner can see that one coordinate changes while the others stay fixed. That isolation makes translation feel elementary instead of abstract. It is the cleanest single-axis version of state change in the sequence.

## Context

**Category:** transforms | **Complexity:** beginner
**Tags:** transformation, translation, x_axis, coordinates, ghost_trail, invariant, transforms, visualization | **Themes:** geometry, math, transforms

An animated cube that moves only along the X axis. Ghosts and labels keep the motion explicit, so the learner can see that one coordinate changes while the others stay fixed. That isolation makes translation feel elementary instead of abstract. It is the cleanest single-axis version of state change in the sequence.

## Design

- **Visual:** material: shader, animation: particle_lifecycle

## Architecture

| | |
|---|---|
| **File** | [No dedicated GDScript found] |
| **Scene** | `res://commons/artifacts/axis_translation_cube/x_translation_cube.tscn` |
| **Registry** | `transforms.json` |
| **Pattern** | unknown |

## Curriculum Position

### Sequences

- **Transformation: What Stays the Same When Everything Changes** (transformation) -- map: Trans_AxisDecomposition

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Trans_AxisDecomposition | [8,2] | `:180:1:2` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
