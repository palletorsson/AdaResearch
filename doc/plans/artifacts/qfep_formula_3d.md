# Artifact: QFEP Formula 3D

> TextMesh("QFE = F - lambda*E(S) + phi*delta_E(S,t)") with per-term color and pulse animation

## Context

**Category:** qfep | **Complexity:** intermediate
**Tags:** visualization, formula, theory, 3d | **Themes:** qfep, philosophy

3D visualization of the complete QFEP formula: QFE = F − λE(S) + φΔE(S,t). Each term glows with its signature color. Responds to lambda and phi slider changes. The theoretical anchor.

*QFE = F - lambda*E(S) + phi*delta_E(S,t) is not an equation to solve but a lens through which to see every system*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** see the formula floating in space — each symbol glowing with its own color, alive with meaning
- **Critical Parameter:** highlighted_term — which term pulses brightest, drawing attention to one piece of the whole
- **What Emerges:** the formula reads differently depending on which term is highlighted — the whole changes meaning with focus
- **Triggers:** highlight_term() intensifies one symbol; on_lambda_changed/on_phi_changed shift term colors to match system state

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/formula_display/qfep_formula_3d.gd` (233 lines) |
| **Scene** | `res://commons/interfaces/qfep/formula_display/qfep_formula_3d.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `QFEPFormula3D` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `animate` | - | - |
| `pulse_speed` | - | - |
| `glow_intensity` | - | - |
| `font_size` | - | - |
| `depth` | - | - |

### Key Methods

- `_ready()`
- `_create_formula()` -- Create the formula: QFE = F âˆ’ Î»E(S) + Ï†Î”E(S,t)
- `_add_term(term_id: String, text: String, x_pos: float, color: Color)`
- `_setup_materials()` -- Materials are created in _add_term
- `_process(_delta)`
- `highlight_term(term_name: String)`
- `clear_highlight()`
- `get_term_info(term_name: String)`
- `on_lambda_changed(value: float)` -- Adjust lambda term color based on value
- `on_phi_changed(value: float)`

### Signals

- `term_highlighted`
- `term_selected`

## Curriculum Position

### Sequences

- **Foundations Crisis: The Limits of Formalization** (foundationscrisis) -- map: Crisis_Synthesis
- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Introduction
- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Sandbox
- **QFEP Laboratory: The Formula Made Interactive** (qfeplaboratory) -- map: QFEP_Synthesis

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Crisis_Synthesis | [8,8] | `:0:1.5:1` |
| QFEP_Introduction | [2,4] | `:0:1.5` |
| QFEP_Sandbox | [12,5] | `:0:1` |
| QFEP_Synthesis | [5,5] | `:0:2` |

### Relationships

- central display in QFEP_Introduction and QFEP_Sandbox; depends on lambda_slider, phi_slider; references all QFEP terms
- Needs: VR term touch selection [missing], slider connections [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
