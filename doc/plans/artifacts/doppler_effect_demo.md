# Artifact: Doppler Effect Demo

> Moving source emits concentric wave rings — compression ahead (blue shift) and stretching behind (red shift). Rings expand from emission positions, naturally demonstrating how relative motion changes observed frequency.

## Context

**Category:** wavefunctions | **Complexity:** intermediate
**Tags:** doppler, wave, frequency, shift, red_shift, blue_shift, motion | **Themes:** oscillation, physics, waves

Moving source emits concentric wave rings — compression ahead (blue shift) and stretching behind (red shift). Rings expand from emission positions, naturally demonstrating how relative motion changes observed frequency.

## Design

- **Visual:** material: vertex_color, animation: vertex

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/doppler_effect_demo/doppler_effect_demo.gd` (297 lines) |
| **Scene** | `res://commons/artifacts/doppler_effect_demo/doppler_effect_demo.tscn` |
| **Registry** | `wavefunctions_extra.json` |
| **Class** | `DopplerEffectDemo` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `arena_size` | float | 0.8 |
| `source_speed` | float | 0.15 |
| `emit_interval` | float | 0.18 |
| `wave_expand_speed` | float | 0.25 |
| `max_rings` | int | 30 |
| `circle_segments` | int | 48 |
| `ring_max_radius` | float | 0.6 |
| `color_blue_shift` | Color | Color(0.3, 0.5, 1.0) |
| `color_neutral` | Color | Color(0.8, 0.8, 0.8) |
| `color_red_shift` | Color | Color(1.0, 0.3, 0.2) |
| `color_source` | Color | Color(1.0, 0.8, 0.2) |

### Key Methods

- `_ready()`
- `_build_base()`
- `_build_source()`
- `_build_observer()`
- `_build_ring_display()`
- `_build_labels()`
- `_process(delta: float)`
- `_emit_ring(source_x: float)` -- Store: (emit_x, emit_z, birth_time)
- `_cull_old_rings()`
- `_rebuild_rings()`
- `_draw_circle(im: ImmediateMesh, center: Vector3, radius: float, alpha: float)`
- `_update_freq_label(source_x: float)` -- Calculate apparent frequency at observer based on Doppler formula
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `emit_interval`, `source_speed`, `wave_expand_speed`

## Curriculum Position

### Sequences

- wavefunctions

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
