# Artifact: Isosurface Trap

> f(x) = sum(charge_i / |x - p_i|^2) -- metaball charges creating implicit surface danger zones

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** isosurface, metaball, scalar-field, implicit | **Themes:** gameplay, hazards

Blobby metaball surface from point charges. Scalar field bulges toward player; contact equals damage proportional to field strength. The implicit surface made dangerous.

*the surface is not a boundary you can see -- it is an equation you are inside or outside of.*

## Design

- **Visual:** material: standard, animation: transform
- **Interaction:** blobby charges bulging toward the player, implicit surfaces merging and splitting
- **Critical Parameter:** _charges array -- position and strength of each metaball; iso-threshold defines boundary
- **What Emerges:** implicit surfaces as threat -- danger zone has no fixed shape, only a field equation
- **Triggers:** player enters Area3D; charges drift toward player; damage inside iso-surface boundary

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/isosurface_trap/isosurface_trap.gd` (330 lines) |
| **Scene** | `res://commons/hazards/isosurface_trap/isosurface_trap.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `IsosurfaceTrap` extends `Area3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_charges` | int | 6 |
| `field_radius` | float | 2.0 |
| `base_strength` | float | 1.0 |
| `threshold` | float | 1.5 |
| `drift_speed` | float | 0.4 |
| `charge_spread` | float | 1.2 |
| `damage_per_second` | float | 12.0 |
| `ring_samples` | int | 24 |
| `ring_radius` | float | 1.5 |
| `indicator_radius` | float | 0.02 |
| `max_health` | float | 100.0 |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_compute_field(pos: Vector3)`
- `_build_charges()`
- `_build_boundary_ring()`
- `_update_boundary_ring()` -- Place indicator spheres at approximate isosurface boundary
- `_build_label()`
- `_on_body_entered(body: Node3D)`
- `_on_body_exited(body: Node3D)`
- `_find_player()`
- `take_damage(amount: float)`
- `apply_damage(amount: float)`
- `damage(amount: float)`
- `_apply_damage(amount: float)`
- `configure(config: Dictionary)`

### Signals

- `enemy_destroyed`

### Grid Config


## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_6

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_6 | [7,7] | `-` |

### Relationships

- embodies isosurfaces sequence; pairs with noise_field and spatial_voronoi as field hazards
- Needs: Area3D [has]; charge tracking [has]; field evaluation [has]; damage accumulation [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
