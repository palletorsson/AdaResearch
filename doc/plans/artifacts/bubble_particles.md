# Artifact: Bubble Particles

> Movement-based procedural bubble system — bubbles spawn in a configurable area, rise with randomized speeds and sizes, fade over time, and pop with synthesized audio. Concurrent sound limiting and pitch variation create an organic underwater soundscape.

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** animation, procedural, simulation, sorting, sound | **Themes:** music, probability, procedural, randomness

Movement-based procedural bubble system — bubbles spawn in a configurable area, rise with randomized speeds and sizes, fade over time, and pop with synthesized audio. Concurrent sound limiting and pitch variation create an organic underwater soundscape.

## Design

- **Visual:** material: standard, animation: transform

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/proceduralrandomness/movementbased/scenes/bubbleparticles/bubble_particles.gd` (226 lines) |
| **Scene** | `res://algorithms/randomness/proceduralrandomness/movementbased/scenes/bubbleparticles/bubble_particles.tscn` |
| **Registry** | `randomness.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `spawn_area_size` | Vector3 | Vector3(10, 1.0, 10) |
| `min_bubble_size` | float | 0.2 |
| `max_bubble_size` | float | 0.8 |
| `min_rise_speed` | float | 0.5 |
| `max_rise_speed` | float | 2.0 |
| `max_bubble_count` | int | 200 |
| `spawn_rate` | float | 2.0 |
| `bubble_lifetime` | float | 10.0 |
| `bubble_fade_time` | float | 2.0 |
| `bubble_scale_down_rate` | float | 0.05 |
| `use_synthesized_sounds` | bool | true |
| `sound_variations` | int | 5 |
| `min_pitch_scale` | float | 0.8 |
| `max_pitch_scale` | float | 1.5 |
| `min_volume_db` | float | -15.0 |
| `max_volume_db` | float | -5.0 |
| `max_concurrent_sounds` | int | 4 |
| `sound_play_chance` | float | 0.3 |
| `bubble_sounds` | Array[AudioStream] | [] |

### Key Methods

- `_ready()` -- Create the sound synthesizer
- `_process(delta: float)` -- Spawn new bubbles
- `spawn_bubble()` -- Create a random position within the spawn area
- `play_bubble_sound(position: Vector3, bubble_size: float)` -- Determine normalized bubble size (0-1)
- `_on_audio_finished(audio_player)` -- Return the audio player to the pool when finished
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- randomness

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
