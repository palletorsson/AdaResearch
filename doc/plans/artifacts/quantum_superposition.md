# Artifact: Quantum Superposition

> Visualization of quantum superposition showing qubits in simultaneous states before measurement collapse.

## Context

**Category:** quantumalgorithms | **Complexity:** expert
**Tags:** quantum, superposition, qubit, measurement | **Themes:** algorithms, quantum

Visualization of quantum superposition showing qubits in simultaneous states before measurement collapse.

## Design

- **Visual:** material: vertex_color, animation: vertex
- **Scale:** [1, 4, 13] (world_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/quantumalgorithms/superposition/quantum_superposition.gd` (876 lines) |
| **Scene** | `res://algorithms/quantumalgorithms/superposition/quantum_superposition.tscn` |
| **Registry** | `quantumalgorithms.json` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_qubits` | int | 2 |
| `bloch_radius` | float | 0.9 |
| `gate_speed` | float | 1.5 |
| `step_speed` | float | 1.0 |
| `trail_length` | int | 120 |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_create_materials()`
- `_create_mesh_instances()`
- `_create_labels()`
- `_create_vr_controls()`
- `_cycle_mode()`
- `_on_gate_pressed()`
- `_on_reset()`
- `_on_theta_changed(_value: float)`
- `_on_phi_changed(_value: float)`
- `_init_mode()`
- `_init_state()` -- 2-qubit register: |00⟩ = [1, 0, 0, 0]
- `_complex_mul(a: Vector2, b: Vector2)`
- `_complex_add(a: Vector2, b: Vector2)`

### Grid Config

Accepts: `alice_phi`, `alice_theta`, `bloch_phi`, `bloch_radius`, `bloch_theta`, `gate_speed`, `mode`, `num_qubits`, `step_speed`, `trail_length`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
