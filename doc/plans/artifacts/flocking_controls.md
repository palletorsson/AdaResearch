# Artifact: Flocking Controls

> Interactive boid flocking with VR sliders for separation, alignment, cohesion, speed, and perception radius. Highlights one boid with visible force vectors.

## Context

**Category:** steering | **Complexity:** advanced
**Tags:** steering, flocking, boids, sliders, interactive, emergence | **Themes:** autonomous_agents, steering

Interactive boid flocking with VR sliders for separation, alignment, cohesion, speed, and perception radius. Highlights one boid with visible force vectors.

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/flocking_controls/flocking_controls.gd` (561 lines) |
| **Scene** | `res://commons/artifacts/flocking_controls/flocking_controls.tscn` |
| **Registry** | `steering.json` |
| **Class** | `FlockingControls` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `boid_count` | int | 30 |
| `bounds` | Vector3 | Vector3(0.8, 0.6, 0.8) |
| `separation_weight` | float | 1.5: |
| `alignment_weight` | float | 1.0: |
| `cohesion_weight` | float | 1.5: |
| `max_speed` | float | 0.5: |
| `perception_radius` | float | 0.2: |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_create_bounds_wireframe()`
- `_create_multimesh()`
- `_create_highlight_ring()`
- `_create_force_mesh()`
- `_update_force_arrows()`
- `_draw_arrow(im: ImmediateMesh, origin: Vector3, vec: Vector3, col: Color)`
- `_create_labels()`
- `_update_info()`
- `_create_vr_controls()` -- Use RackTemplates for Rams-styled parameter panel
- `_sync_slider(slider: Node, normalized: float)`
- `_sync_all_sliders()`
- `_on_sep_slider(_pos)`
- `_on_align_slider(_pos)`
- `_on_coh_slider(_pos)`

### Grid Config

Accepts: `alignment`, `boid_count`, `cohesion`, `perception_radius`, `separation`, `speed`

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| AutoGenTest | [8,9] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
