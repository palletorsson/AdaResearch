# Artifact: Circle Spiral

> p(t) = (r*cos(omega*t), r*sin(omega*t), v_z*t) — helix as circle extruded through time

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** animation, physics, waves | **Themes:** biology, oscillation, physics, waves

Rotation extended through time becomes spiral—a circle that doesn't close but climbs. This is what happens when circular motion gains a third dimension: the helix, the spring, the DNA strand.

*A helix is what happens when rotation refuses to stay in one plane.*

## Design

- **Visual:** material: unlit, animation: vertex
- **Scale:** [5.0, 3.0, 5.0] (environment)
- **Interaction:** Watch circular motion leave a helical trail as time flows along the Z axis
- **Critical Parameter:** radius — determines the helix width, making rotation visible as spatial coil
- **What Emerges:** visual proof that a helix is circular motion plus linear translation
- **Triggers:** continuous time progression extends the helix; rotation_speed sets coil tightness

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/oscillation_driver/CircleSpiral.gd` (158 lines) |
| **Scene** | `res://algorithms/wavefunctions/oscillation_driver/CircleSpiral.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `radius` | float | 0.5 |
| `rotation_speed` | float | 2.0 |
| `time_speed` | float | 2.0 |
| `max_trail_length` | int | 400 |

### Key Methods

- `_ready()`
- `_create_ring_assembly(z_pos: float, assembly_name: String)` -- Root for the end assembly
- `_setup_visuals()`
- `_process(delta: float)`
- `_draw_trail()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Relationships

- depends on ImmediateMesh trail; contrasts with UnitCircleTrig (projection vs extrusion); unlocks helix intuition
- Needs: VR viewpoint control [has], speed adjustment [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
