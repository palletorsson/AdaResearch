# Artifact: Data Tree Walker

> BST.traverse(node) -> walk(node.position) -- binary search tree that walks on its own leaves

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** binary-tree, bst, data-structure, traversal | **Themes:** gameplay, hazards

Binary search tree of sphere nodes connected by cylinder edges. BST insert with values 1-7; visible tree rotations on damage. Walks on its leaf nodes. Data structures made physical.

*a binary search tree balancing on its own leaves -- structure is not abstract, it walks.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** sphere nodes connected by edges forming a walking tree -- data structure IS the skeleton
- **Critical Parameter:** tree balance / traversal order -- BST rebalancing determines the creature's gait
- **What Emerges:** the data structure literally walks -- binary search becomes bipedal locomotion
- **Triggers:** player proximity triggers traversal; in-order/pre-order/post-order determines attack pattern

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/data_tree_walker/data_tree_walker.gd` (255 lines) |
| **Scene** | `res://commons/hazards/data_tree_walker/data_tree_walker.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `DataTreeWalker` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `node_radius` | float | 0.06 |
| `edge_radius` | float | 0.015 |
| `level_height` | float | 0.2 |
| `level_spread` | float | 0.15 |
| `max_depth` | int | 3 |
| `node_color` | Color | Color(0.3, 0.7, 1.0) |
| `root_color` | Color | Color(1.0, 0.8, 0.2) |
| `edge_color` | Color | Color(0.4, 0.5, 0.6) |
| `emission` | Color | Color(0.2, 0.5, 1.0) |

### Key Methods

- `_on_ready()`
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()`
- `_build_initial_tree()` -- Build a balanced BST with values 1-7
- `_insert_value(val: int)`
- `_get_node_position(idx: int)`
- `_find_path(current: int, target: int, path: Array[int])`
- `_rebuild_tree_meshes()` -- Clear existing
- `_find_parent(child_idx: int)`
- `_create_edge(from_pos: Vector3, to_pos: Vector3)`
- `_process_visual(delta: float)` -- Walk on leaf nodes
- `_on_damaged(_amount: float)` -- Visual "rotation" effect — shuffle a random subtree

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_4

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_4 | [7,3] | `-` |

### Relationships

- embodies datastructures sequence; pairs with index_sentinel (linear vs tree traversal)
- Needs: HazardCreatureBase [has]; BST node/edge visualization [has]; traversal animation [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
