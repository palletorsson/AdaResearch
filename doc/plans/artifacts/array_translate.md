# Artifact: array_translate

> Array + Translate. position[i] = i Ã— offset. The staircase: cumulative translation.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** array | **Themes:** core, primitives

Array + Translate. position[i] = i Ã— offset. The staircase: cumulative translation.

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/arrays/array_transform_staircase.gd` (148 lines) |
| **Scene** | `res://commons/primitives/arrays/array_transform_staircase.tscn` |
| **Registry** | `primitives.json` |
| **Class** | `ArrayTransformStaircase` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `mode` | TransformMode | TransformMode.TRANSLATE: |
| `count` | int | 8: |
| `element_size` | Vector3 | Vector3(0.5, 0.1, 0.3): |
| `translate_offset` | Vector3 | Vector3(0.4, 0.15, 0.0): |
| `rotate_axis` | Vector3 | Vector3(0, 1, 0): |
| `rotate_angle_degrees` | float | 30.0: |
| `rotate_radius` | float | 0.8: |
| `scale_factor` | float | 0.85: |
| `scale_center_offset` | float | 0.0: |
| `element_color` | Color | Color(0.7, 0.7, 0.75, 1.0): |

### Key Methods

- `_ready()`
- `_create_material()`
- `_update_material()`
- `_rebuild()` -- Clear
- `_apply_translate(el: MeshInstance3D, i: int)` -- position[i] = i × offset
- `_apply_rotate(el: MeshInstance3D, i: int)` -- rotation[i] = i × angle, placed on circle
- `_apply_scale(el: MeshInstance3D, i: int)` -- scale[i] = factor^i (telescoping/nested)
- `get_formula()`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
