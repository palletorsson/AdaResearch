# Artifact: draw_dot

> sample(hand_position) → ImmediateMesh trail — the hand writes points in 3D space

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** primitive | **Themes:** core, primitives

draw_dot.

*a curve has no intrinsic form — it is just the limit of increasingly dense point samples*

## Design

- **Visual:** material: shader, animation: vertex
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** learner experiences themselves as a drawing instrument — hand motion becomes visible line
- **Critical Parameter:** trail_length — how many hand positions are stored before the oldest disappears
- **What Emerges:** the understanding that a continuous curve is just many sampled points
- **Triggers:** moving the grabbed sphere — total_distance accumulates and triggers unlock tags

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/point/draw_dot.gd` (455 lines) |
| **Scene** | `res://commons/primitives/point/draw_dot.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grab_point_path` | NodePath | NodePath("GrabPoint") |
| `draw_sphere_path` | NodePath | NodePath("GrabPoint/DrawSphere") |
| `trail_color` | Color | Color(1.0, 0.4, 0.9, 1.0) |
| `trail_max_points` | int | 1024 |
| `min_segment_distance` | float | 0.01 |
| `record_only_when_grabbed` | bool | true |
| `auto_clear_on_drop` | bool | false |
| `reference_frame_position` | Vector3 | Vector3(0, 0, 0.2) |
| `reference_frame_size` | float | 0.5 |
| `show_reference_frame` | bool | true |
| `fade_trail` | bool | false |
| `fade_duration` | float | 2.0 |
| `progress_bar_height_offset` | float | 0.15 |
| `show_data_table` | bool | true |
| `data_table_height_offset` | float | -0.35 |
| `data_table_color` | Color | Color(0.9, 0.95, 1.0, 1.0) |
| `data_table_font_size` | int | 20 |
| `data_table_update_interval` | float | 0.15 |
| `trigger_tag` | String | "" |
| `trigger_action` | String | "shrink_and_remove" |
| `movement_threshold` | float | 6.0 |
| `unlock_progress_color` | Color | Color(0.2, 1.0, 0.4) |
| `unlock_sound` | AudioStream | - |

### Dependencies

- `res://commons/scenes/main_menu/components/linear_progress.gdshader`

### Key Methods

- `_setup_trail()`
- `_setup_reference_frame()`
- `_setup_progress_indicator()` -- Create progress indicator as world-space element (not child of grab point)
- `_update_progress_indicator_position()`
- `_setup_data_table()`
- `_update_data_table()`
- `_ready()`
- `_process(delta: float)`
- `_check_unlock_progress()`
- `_trigger_unlock()`
- `_setup_success_audio()`
- `_build_default_success_stream()`
- `_cleanup_old_points()`
- `_rebuild_trail()`
- `clear_trail()`

## Curriculum Position

### Sequences

- **Primitives: Points Build Worlds** (primitives) -- map: Point_Trace

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Point_Context | [4,4] | `:180` |
| Point_Trace | [3,3] | `:0:1:0#fillhole:remove` |

### Relationships

- contrasts with static_point (fixed vs dynamic); pairs with player_trace
- Needs: [has Label3D [has], grabbable sphere [has], missing speed/density slider]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
