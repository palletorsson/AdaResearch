# Artifact: Singing Board

> Interactive singing surface — touch points map to pitch and timbre, creating a spatial instrument where position becomes sound.

## Context

**Category:** speech | **Complexity:** beginner
**Tags:** singing, instrument, spatial, sound, interaction, speech | **Themes:** audio, language, music, speech

Interactive singing surface — touch points map to pitch and timbre, creating a spatial instrument where position becomes sound.

## Design

- **Visual:** material: standard, animation: transform

## Architecture

| | |
|---|---|
| **File** | `algorithms/speech/phoneme_cloud/singing_board.gd` (367 lines) |
| **Scene** | `res://algorithms/speech/phoneme_cloud/singing_board.tscn` |
| **Registry** | `speech.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `melody_selection` | String | "Scale" |
| `base_pitch` | float | 220.0 |
| `tempo_bpm` | float | 120.0 |
| `vibrato_enabled` | bool | true |
| `vibrato_rate` | float | 5.5 |
| `vibrato_depth` | float | 0.015 |
| `is_looping` | bool | true |

### Key Methods

- `_ready()` -- Configure Mapper Range
- `_process(delta: float)` -- Apply vibrato to pitch
- `_start_melody_loop()`
- `sing_melody(melody: Array)`
- `_sing_note(freq: float, vowel: String, consonant: String, duration: float, intensity: float = 1.0)`
- `_sustain_with_vibrato(base_freq: float, duration: float)`
- `_on_mapper_changed(delta_val: float, f1_val: float, intensity_val: float)`
- `_set_mapper_pos(target_field: Vector2, target_intensity: float)`
- `_tween_mapper(target_field: Vector2, target_intensity: float, duration: float)`
- `sing_custom(notes: Array)`
- `sing_text(text: String, pitches: Array = [])`
- `_text_to_syllables(text: String)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
