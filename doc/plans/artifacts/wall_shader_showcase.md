# Artifact: Wall Shader Showcase

> Replaces GridMultiMesh material with GPU shaders from the shader library. Cycles through different shaders on wall cubes — voronoi, tartan, rothko, truchet, etc.

## Context

**Category:** shaders | **Complexity:** advanced
**Tags:** shader, showcase, multimesh, wall, configurable, cycling | **Themes:** shaders, visual

Replaces GridMultiMesh material with GPU shaders from the shader library. Cycles through different shaders on wall cubes — voronoi, tartan, rothko, truchet, etc.

## Design

- **Visual:** material: shader, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/wall_shader_showcase/wall_shader_showcase.gd` (121 lines) |
| **Scene** | `res://commons/artifacts/wall_shader_showcase/wall_shader_showcase.tscn` |
| **Registry** | `shaders.json` |
| **Class** | `WallShaderShowcase` extends `Node3D` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `shader_name` | String | "voronoiShader" |
| `cycle_interval` | float | 6.0 |
| `emission_strength` | float | 0.8 |

### Key Methods

- `_ready()`
- `apply_grid_config(config_data: Dictionary)`
- `_find_multimesh()`
- `_search_multimesh(node: Node)`
- `_apply_shader(name: String)`
- `_try_set_param(mat: ShaderMaterial, param_name: String, value: Variant)` -- Only set if the shader actually has this uniform
- `_start_cycling()`
- `_exit_tree()` -- Restore original material

### Grid Config

Accepts: `cycle_interval`, `emission_strength`, `shader`, `shader_list`

## Curriculum Position

### Sequences

- patterngeneration

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
