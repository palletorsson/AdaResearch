# Artifact: Shader 03 — Shapes

> d = length(uv) - radius — signed distance fields reduce geometry to a single number per pixel: negative inside, zero on boundary, positive outside

## Context

**Category:** shaders | **Complexity:** advanced
**Tags:** book_of_shaders, shapes, distance_fields, shaders | **Themes:** shaders, visual

Book of Shaders chapter on drawing shapes with distance fields.

*the shape is not stored — it is perpetually computed; distance is the oldest geometric concept, and the fragment shader asks it millions of times per frame*

## Design

- **Visual:** material: shader, animation: transform
- **Interaction:** to slide polygon sides from 3 to 12 and watch a triangle become a circle — to boolean-combine shapes with min/max and see geometry as arithmetic
- **Critical Parameter:** radius/sides — the number that separates inside from outside, or the symmetry order of the polygon
- **What Emerges:** the show_distance toggle reveals the full SDF field — smooth hills radiating from every boundary, making the implicit geometry explicit
- **Triggers:** operation slider switches between union(min), intersection(max), and subtraction(max(a,-b)) — three words, three operators, infinite compositions

## Architecture

| | |
|---|---|
| **File** | `algorithms/shaders/book_of_shaders/shader_03_shapes.gd` (111 lines) |
| **Scene** | `res://algorithms/shaders/book_of_shaders/shader_03_shapes.tscn` |
| **Registry** | `shaders.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_process(_delta: float)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Shaders & Patterns: The Book of Shaders** (patterngeneration) -- map: Shader_03_Shapes

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Shader_03_Shapes | [5,6] | `-` |

### Relationships

- depends on shader_01_shaping (smoothstep for anti-aliasing); feeds into shader_05_patterns (tiled SDF shapes) and all procedural geometry
- Needs: [has] ShaderRackPanel sliders per display; [missing] no VR push buttons

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
