# Artifact: Wedge Skill Pickup

> wireframe_cube(1m) -> skill_unlock(wedge_placer) -- a cage holding the wedge power

## Context

**Category:** tools | **Complexity:** simple
**Tags:** tool, catalyst, skill_pickup, wedge, prism, unlock | **Themes:** gameplay, hazards

A one-meter wireframe cube containing a floating orange prism. When grabbed, unlocks the wedge_placer mode on the catalyst bracelet. Place in the primitives sequence for the player to discover.

*the wedge teaches that not everything fits in a box.*

## Design

- **Visual:** material: unlit, animation: physics
- **Interaction:** to be discovered mid-sequence, expanding what the player can build
- **Critical Parameter:** unlock triggers only once — the wedge mode persists for the session
- **What Emerges:** the second tool in the builder's kit — slopes, ramps, angles
- **Triggers:** player enters primitives sequence; walks to the pickup; grabs the prism

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/becoming_catalyst/wedge_skill_pickup.gd` (229 lines) |
| **Scene** | `res://commons/hazards/becoming_catalyst/wedge_skill_pickup.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `WedgeSkillPickup` extends `Node3D` |
| **Pattern** | procedural |

### Dependencies

- `res://addons/godot-xr-tools/objects/pickable.tscn`

### Key Methods

- `_ready()`
- `_process(delta: float)` -- Prism hover and spin
- `_build_cage()`
- `_build_edge(start: Vector3, end_pos: Vector3, idx: int)`
- `_build_light()`
- `_build_prism_pickable()` -- Use XRToolsPickable for VR grabbing
- `_on_prism_grabbed(_pickable)`
- `apply_grid_config(config_data: Dictionary)` -- No config needed

### Signals

- `skill_collected`

### Grid Config


## Curriculum Position

### Sequences

- primitives
- hazards
- testmaps

### Relationships

- sibling to catalyst_pedestal; found in primitives sequence; second bracelet stone
- Needs: CapacityBracelet [adds gem]; BecomingCatalyst [adds mode]; wireframe cage [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
