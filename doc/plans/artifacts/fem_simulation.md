# Artifact: Finite Element Method

> Finite Element Method (FEM) for deformable solid mechanics. A continuous body is discretized into tetrahedral or hexahedral elements, each computing stress and strain from local deformation gradients. Assemble the global stiffness matrix, solve the linear system, and watch beams bend, bridges sag, and materials yield under load — engineering simulation made visible.

## Context

**Category:** physics | **Complexity:** advanced
**Tags:** fem, finite_element, deformable, stress, strain, stiffness_matrix, tetrahedral, engineering, solid_mechanics | **Themes:** fem, deformation, stress, strain, engineering

Finite Element Method (FEM) for deformable solid mechanics. A continuous body is discretized into tetrahedral or hexahedral elements, each computing stress and strain from local deformation gradients. Assemble the global stiffness matrix, solve the linear system, and watch beams bend, bridges sag, and materials yield under load — engineering simulation made visible.

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [6.0, 1.0, 6.0] (environment)

## Architecture

| | |
|---|---|
| **File** | `algorithms/physicssimulation/fem/fem.gd` (113 lines) |
| **Scene** | `res://algorithms/physicssimulation/fem/fem.tscn` |
| **Registry** | `physics_simulation.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `mesh_size` | float | 2.0 |
| `stiffness` | float | 0.8 |
| `precision` | int | 8 |

### Key Methods

- `_ready()`
- `_create_deformable_body()`
- `_pin_edges()` -- Pin some edge vertices to simulate fixed boundary conditions
- `_create_poke_sphere()` -- Interactive sphere that deforms the mesh when moved into it
- `_create_supports()` -- Floor support
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Physics Simulation: The Engine Behind Reality** (physicssimulation) -- map: PhysicsSim_Continuum
- **Physics Simulation: The Engine Behind Reality** (physicssimulation) -- map: PhysicsSimulation_Finite_Element_Method_FEM

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| PhysicsSim_Continuum | [2,6] | `:0:1` |
| PhysicsSimulation_Finite_Element_Method_FEM | [3,3] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
