# Artifact: Elphaba Demo

> Character animation driven by oscillation—the Wicked witch moves through wave functions. Procedural performance where sine waves become gesture, mathematics becomes drama.

## Context

**Category:** waves | **Complexity:** intermediate
**Tags:** procedural, string, wave | **Themes:** oscillation, physics, waves

Character animation driven by oscillation—the Wicked witch moves through wave functions. Procedural performance where sine waves become gesture, mathematics becomes drama.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [1.0, 1.0, 4.0] (room_scale)

## Architecture

| | |
|---|---|
| **File** | `algorithms/wavefunctions/dancing_body/ElphabaDemo.gd` (206 lines) |
| **Scene** | `res://algorithms/wavefunctions/dancing_body/ElphabaDemo.tscn` |
| **Registry** | `wavefunctions.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `show_body` | bool | true |
| `animate_bones` | bool | true |
| `animation_speed` | float | 1.0 |
| `arm_amplitude` | float | 0.3 |
| `body_sway` | float | 0.1 |
| `frequency` | float | 1.0 |

### Dependencies

- `res://algorithms/wavefunctions/dancing_body/player.glb`

### Key Methods

- `_ready()`
- `_load_player_model()`
- `_setup_dress()` -- Find the dress node that was instanced in the scene
- `_find_hip_bone()` -- Try various common hip bone names
- `_find_skeleton(node: Node)`
- `_update_body_visibility()` -- Find mesh instances and toggle visibility
- `_set_mesh_visibility(node: Node, visible: bool)`
- `_process(delta: float)`
- `_apply_dance_animation()` -- Animate arms in a flowing dance motion
- `_find_bone_by_names(names: Array)`
- `_update_dress_position()` -- Dress is now parented to skeleton, so we just update local position to follow hip bone
- `set_body_visible(visible: bool)`
- `set_animation_speed(speed: float)`
- `set_arm_amplitude(amplitude: float)`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
