# Artifact: MeshGrammar — Petal Flower

> Petal splay on upward faces — radiating curved petals with tilt and curl. Organic decoration operation.

## Context

**Category:** substrate | **Complexity:** intermediate
**Tags:** substrate, mesh_grammar, petal, flower, decoration, lsystems | **Themes:** grammar, mesh, procedural

Petal splay on upward faces — radiating curved petals with tilt and curl. Organic decoration operation.

## Design

- **Visual:** material: standard, animation: transform

## Architecture

| | |
|---|---|
| **File** | `commons/mesh_grammar/mesh_grammar_node.gd` (160 lines) |
| **Scene** | `res://commons/mesh_grammar/mesh_grammar_node.tscn` |
| **Registry** | `mesh_grammar.json` |
| **Class** | `MeshGrammarNode` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `seed_type` | String | "icosahedron" |
| `seed_scale` | float | 0.5 |
| `generations` | int | 2 |
| `max_faces` | int | 10000 |
| `auto_generate` | bool | true |
| `animate` | bool | false |
| `animation_speed` | float | 0.5 |
| `animation_parameter` | float | 0.0 |
| `base_color` | Color | Color(0.6, 0.75, 0.85) |
| `use_grid_material` | bool | true |
| `double_sided` | bool | true |

### Dependencies

- `res://commons/primitives/shared/grid_material_factory.gd`

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `generate()`
- `apply_step()`
- `reset()`
- `set_seed_mesh(mesh_data: MeshData)`
- `add_rule(rule: MeshRule)`
- `clear_rules()`
- `configure(config: Dictionary)`
- `apply_grid_config(config_data: Dictionary)`
- `_ensure_grammar()`
- `_create_seed()`
- `_update_display(mesh_data: MeshData)`
- `_setup_material()`

### Grid Config


## Curriculum Position

### Sequences

- lsystems

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
