# Artifact: Terrain Erosion

> Hydraulic erosion on a procedural heightmap â€” layered sin-wave terrain eroded by 800 virtual water droplets flowing downhill, carrying and depositing sediment. ImmediateMesh surface colored by altitude: water, beach, grass, rock, snow.

## Context

**Category:** proceduralgeneration | **Complexity:** advanced
**Tags:** erosion, hydraulic, heightmap, terrain, procedural, landscape, sediment | **Themes:** generation, procedural

Hydraulic erosion on a procedural heightmap â€” layered sin-wave terrain eroded by 800 virtual water droplets flowing downhill, carrying and depositing sediment. ImmediateMesh surface colored by altitude: water, beach, grass, rock, snow.

## Design

- **Visual:** material: vertex_color, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/terrain_erosion/terrain_erosion.gd` (361 lines) |
| **Scene** | `res://commons/artifacts/terrain_erosion/terrain_erosion.tscn` |
| **Registry** | `procgen_extra.json` |
| **Class** | `TerrainErosion` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `terrain_size` | float | 0.8 |
| `grid_res` | int | 48 |
| `height_scale` | float | 0.25 |
| `droplet_count` | int | 800 |
| `droplet_lifetime` | int | 64 |
| `erosion_rate` | float | 0.3 |
| `deposition_rate` | float | 0.3 |
| `evaporation_rate` | float | 0.02 |
| `min_sediment_capacity` | float | 0.01 |
| `sediment_capacity_factor` | float | 4.0 |
| `inertia` | float | 0.3 |
| `gravity` | float | 4.0 |

### Key Methods

- `_ready()`
- `_generate_heightmap()`
- `_erode()`
- `_simulate_droplet()` -- Random starting position
- `_deposit_at(ix: int, iy: int, fx: float, fy: float, amount: float)` -- Bilinear deposit
- `_erode_at(ix: int, iy: int, fx: float, fy: float, amount: float)` -- Bilinear erosion
- `_compute_height_range()`
- `_create_surface()`
- `_build_surface()`
- `_altitude_color(h: float)`
- `_create_labels()`
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `droplet_count`, `erosion_rate`, `height_scale`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
