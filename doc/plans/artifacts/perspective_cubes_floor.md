# Artifact: Perspective Cubes Floor

> a floor of stacked cubes that trick the eye with three flat shades

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pompeii, isometric | **Themes:** arrays, art, cultural_computation

Procedural Pompeii mosaic floor with perspective cubes pattern — three rhombus shades on a regular rectangular grid create a strong 3D stacked-cube illusion.

*alignment in a grid makes each cube reinforce its neighbours' illusion*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and perceive depth in a perfectly flat surface
- **Critical Parameter:** cubes_short — number of cube columns on the short axis
- **What Emerges:** the realization that regular grids amplify the isometric illusion
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/perspective_cubes_floor.gd` (337 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/perspective_cubes_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `PerspectiveCubesFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.2, 0.9) |
| `cubes_short` | int | 6 |
| `border_width` | int | 2 |
| `border_motif` | int | BorderMotifs.Motif.SOLID |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_medium` | Color | MosaicPalette.MEDIUM |
| `color_light` | Color | MosaicPalette.LIGHT |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.04 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`
- `res://commons/artifacts/pompeii_mosaic_floor/border_motifs.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`

### Grid Config

Accepts: `border_width`, `cubes_short`, `floor_size`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- tumbling_blocks_floor (sibling — hex offset), pompeii_mosaic_floor (family)
- Needs: [implemented] procedural mesh, border bands, rectangular rhombus tiling

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
