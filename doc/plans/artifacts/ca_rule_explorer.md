# Artifact: Cellular Automata Rule Explorer

> next_cell = (rule >> (left<<2 | center<<1 | right)) & 1 — all 256 Wolfram rules on one board

## Context

**Category:** emergence | **Complexity:** intermediate
**Tags:** cellular_automata, rules, wolfram, exploration, edge_of_chaos | **Themes:** biology, commons, emergence, mixed

Browse and experiment with different cellular automata rule sets in real-time. Dial through Wolfram's elementary rules or configure custom neighborhood and transition tables to discover the full spectrum from dead stillness to chaotic noise, with complexity emerging at the edge.

*An 8-bit number is a universe — Rule 110 proves that three neighbors and one bit can compute anything.*

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle
- **Scale:** [2.0, 1.0, 2.0] (standard)
- **Interaction:** To be dialed — slide through all 256 rules and watch chaos, order, and Turing completeness appear
- **Critical Parameter:** rule (0-255) — a single 8-bit number that determines the entire visual universe
- **What Emerges:** The full spectrum from death (Rule 0) to chaos (Rule 30) to computation (Rule 110) from one integer
- **Triggers:** Rule 30 → chaos from a single seed; Rule 110 → Turing complete computation; Rule 90 → Sierpinski triangle

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/ca_rule_explorer/ca_rule_explorer.gd` (421 lines) |
| **Scene** | `res://commons/artifacts/ca_rule_explorer/ca_rule_explorer.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `CARuleExplorer` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `board_size` | float | 1.0 |
| `cells_x` | int | 64 |
| `rows_visible` | int | 50 |
| `rule` | int | 110: |
| `generations_per_second` | float | 10.0: |
| `auto_run` | bool | true |
| `alive_color` | Color | Color(0.2, 0.9, 0.4) |
| `dead_color` | Color | Color(0.05, 0.08, 0.05) |
| `board_color` | Color | Color(0.1, 0.12, 0.1) |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_exit_tree()`
- `_create_board()`
- `_create_frame()`
- `_create_multimesh()`
- `_create_rule_display()`
- `_create_vr_controls()`
- `_sync_rule_slider()`
- `_sync_speed_slider()`
- `_on_rule_slider_moved(_position)`
- `_on_speed_slider_moved(_position)`
- `_on_preset_pressed(preset_rule: int)`
- `_on_reset_pressed()`
- `_update_rule_display()`
- `_reset()`

### Grid Config

Accepts: `rule`, `speed`

## Curriculum Position

### Sequences

- **Cellular Automata: Local Rules, Global Patterns** (cellularautomata) -- map: CA_AgentsCircuits

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CA_AgentsCircuits | [1,3] | `:0:0.5:1#mount:wall` |

### Relationships

- Central to CA_AgentsCircuits. Extends ca_bridge (single rule, no interaction vs full exploration).
- Needs: VR rule slider [has], speed slider [has], preset buttons [has], reset button [has], Label3D [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
