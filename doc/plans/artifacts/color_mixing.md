# Artifact: Color Mixing

> Interactive color mixing — combine colors and see the result in different color spaces. Additive (light) vs subtractive (pigment) mixing made visible.

## Context

**Category:** color | **Complexity:** beginner
**Tags:** color, mixing, additive, subtractive, rgb, beginner | **Themes:** color, perception

Interactive color mixing — combine colors and see the result in different color spaces. Additive (light) vs subtractive (pigment) mixing made visible.

## Design

- **Visual:** material: unlit, animation: static
- **Scale:** [2.0, 2.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/color_mixing/color_mixing.gd` (106 lines) |
| **Scene** | `res://algorithms/color/color_mixing/color_mixing.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `plane_size` | float | 2.0 |
| `plane_thickness` | float | 0.15 |
| `plane_opacity` | float | 0.5 |
| `separation` | float | 0.5 |

### Key Methods

- `_ready()`
- `_create_subtractive_mix(position: Vector3)` -- Subtractive Mixing (CMY) - Like Ink/Paint
- `_create_additive_mix(position: Vector3)` -- Additive Mixing (RGB) - Like Light/Screens
- `_create_plane(parent: Node, name: String, color: Color, pos: Vector3, additive: bool)`
- `_create_labels()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- color

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Overlay | [5,4] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
