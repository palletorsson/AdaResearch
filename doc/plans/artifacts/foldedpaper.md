# Artifact: 1.2: A foldedpaper

> Accordion-style folded paper mesh with editable top/base control points and multiple fold reset modes.

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** 3d, origami | **Themes:** core, primitives

Accordion-style folded paper mesh with editable top/base control points and multiple fold reset modes.

## Design

- **Visual:** material: shader, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/primitives/foldedpaper/foldedpaper.gd` (315 lines) |
| **Scene** | `res://commons/primitives/foldedpaper/foldedpaper.tscn` |
| **Registry** | `primitives.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `sphere_size_multiplier` | float | 0.3 |
| `sphere_y_offset` | float | 0.0 |
| `segment_width` | float | 0.4 |
| `fold_height` | float | 0.6 |
| `segment_count` | int | 5 |
| `double_sided` | bool | true |
| `alter_freeze` | bool | false |

### Dependencies

- `res://commons/resourses/shaders/SimpleGrid.gdshader`

### Key Methods

- `_ready()`
- `create_paper_mesh()` -- Create paper mesh
- `create_folded_vertices()` -- Create folded paper vertices with alternating fold directions (70% folded)
- `_setup_drag_points()`
- `update_paper_mesh()` -- Update the paper mesh from current vertices
- `add_triangle_with_normal(st: SurfaceTool, vertices: Array)`
- `_on_point_picked_up(_index: int, _pickable, _meta: Dictionary)`
- `_on_point_dropped(index: int, _pickable, meta: Dictionary)`
- `_on_point_moved(index: int, position: Vector3, meta: Dictionary)`
- `apply_paper_material(mesh_instance: MeshInstance3D, color: Color)`
- `reset_to_accordion()` -- Reset to accordion fold pattern (70% compressed)
- `reset_to_zigzag_fold()` -- Reset to zigzag fold pattern
- `reset_to_wave_fold()` -- Reset to wave fold pattern
- `reset_to_flat()` -- Reset to flat paper
- `update_sphere_positions()`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
