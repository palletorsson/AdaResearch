# Artifact: Numerical Integration Methods

> Side-by-side comparison of numerical integration schemes: forward Euler (simple but drifty), symplectic Euler (energy-preserving), and RK4 (fourth-order accuracy). Watch identical initial conditions diverge as each method accumulates different error profiles over time. The gap between the curves is the price of computational shortcuts.

## Context

**Category:** physics | **Complexity:** intermediate
**Tags:** euler, rk4, symplectic, integration, numerical_methods, time_stepping, error, stability, comparison | **Themes:** numerical_methods, integration, accuracy, stability

Side-by-side comparison of numerical integration schemes: forward Euler (simple but drifty), symplectic Euler (energy-preserving), and RK4 (fourth-order accuracy). Watch identical initial conditions diverge as each method accumulates different error profiles over time. The gap between the curves is the price of computational shortcuts.

## Design

- **Visual:** material: unlit, animation: physics
- **Scale:** [10.0, 1.0, 6.0] (environment)

## Architecture

| | |
|---|---|
| **File** | `algorithms/physicssimulation/numericalintegration/numericalintegration.gd` (191 lines) |
| **Scene** | `res://algorithms/physicssimulation/numericalintegration/numericalintegration.tscn` |
| **Registry** | `physics_simulation.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `time_step` | float | 0.016 |
| `trail_length` | int | 200 |

### Key Methods

- `_ready()`
- `_create_godot_ball()`
- `_create_trail_renderers()` -- Euler trail (red)
- `_create_containment()`
- `_reset_manual_particles()`
- `_physics_process(delta: float)` -- Euler integration (manual)
- `_draw_trails()`
- `_draw_trail(mesh: ImmediateMesh, trail: Array[Vector3])`
- `reset()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Physics Simulation: The Engine Behind Reality** (physicssimulation) -- map: PhysicsSim_Foundations
- **Physics Simulation: The Engine Behind Reality** (physicssimulation) -- map: PhysicsSimulation_Numerical_Integration

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| PhysicsSim_Foundations | [5,5] | `:0:1` |
| PhysicsSimulation_Numerical_Integration | [3,5] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
