# Artifact: Flow Field Painter

> Paint with vector fields — particles follow the flow you create, tracing streamlines that reveal the hidden topology of your brushstrokes. Combines art and physics as your gestures define the force landscape that particles navigate.

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** flow_field, painting, particles, streamlines, interactive_art | **Themes:** art, commons, mixed, procedural

Paint with vector fields — particles follow the flow you create, tracing streamlines that reveal the hidden topology of your brushstrokes. Combines art and physics as your gestures define the force landscape that particles navigate.

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle
- **Scale:** [1.0, 1.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/flow_field_painter/flow_field_painter.gd` (361 lines) |
| **Scene** | `res://commons/artifacts/flow_field_painter/flow_field_painter.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Class** | `FlowFieldPainter` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `canvas_size` | Vector2 | Vector2(0.6, 0.4) |
| `num_particles` | int | 500 |
| `particle_speed` | float | 0.1 |
| `trail_length` | int | 50 |
| `trail_fade` | bool | true |
| `noise_scale` | float | 3.0: |
| `noise_speed` | float | 0.1 |
| `palette` | Array[Color] | [ |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_setup_noise()`
- `_update_noise()`
- `_create_canvas()`
- `_create_particles()`
- `_create_labels()`
- `_create_vr_controls()`
- `_on_scale_slider_moved(_pos)`
- `_on_speed_slider_moved(_pos)`
- `_reset_particles(_button = null)` -- Accept optional button arg
- `_new_seed(_button = null)` -- Accept optional button arg
- `_process(delta)`
- `_update_trail_mesh()`
- `_exit_tree()`
- `_input(event)`

### Grid Config

Accepts: `noise_scale`, `noise_speed`, `num_particles`, `particle_speed`, `trail_fade`, `trail_length`

## Curriculum Position

### Sequences

- **Unused Artifacts Test** (testmaps) -- map: test_batch_1

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| test_batch_1 | [6,4] | `#mount:shelf` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
