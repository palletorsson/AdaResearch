# Artifact: Flower Lab

> Interactive botanical laboratory â€” VR sliders control petal count, length, width, curve, stem height, color hue, leaf count, and symmetry of a procedural BotanicalFlower. Randomize to discover unexpected morphologies, save presets to JSON. Teaches how parametric variation drives flower form.

## Context

**Category:** proceduralgeneration | **Complexity:** beginner
**Tags:** flower, procedural, interactive, botanical, morphology, vr, sliders | **Themes:** education, generation, procedural

Interactive botanical laboratory â€” VR sliders control petal count, length, width, curve, stem height, color hue, leaf count, and symmetry of a procedural BotanicalFlower. Randomize to discover unexpected morphologies, save presets to JSON. Teaches how parametric variation drives flower form.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/flower_lab/flower_lab.gd` (369 lines) |
| **Scene** | `res://commons/artifacts/flower_lab/flower_lab.tscn` |
| **Registry** | `procgen_extra.json` |
| **Class** | `FlowerLab` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Dependencies

- `res://commons/flora/botanical_flower.tscn`
- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_init_params()` -- Start with defaults from PARAM_DEFS
- `_build_flower()` -- Remove old flower if rebuilding
- `_build_flower_config()` -- Convert hue to a vivid petal color (high saturation, medium-high value)
- `_create_info_label()`
- `_update_info()`
- `_create_vr_controls()`
- `_sync_sliders()`
- `_make_slider_callback(param_name: String)`
- `_on_symmetry_toggle(_b = null)`
- `_on_randomize(_b = null)`
- `_on_save_preset(_b = null)` -- Convert Color to hex string for JSON serialization
- `_rebuild_flower()`
- `apply_grid_config(config_data: Dictionary)` -- Apply a named preset first if specified (e.g. "bluebell", "iris")

### Grid Config

Accepts: `preset`

## Curriculum Position

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Morphogenesis_Intro | [3,3] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
