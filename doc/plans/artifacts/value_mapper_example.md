# Artifact: value_mapper_example

> Demonstrates 1D, 2D, and 3D value mappers driving sphere scale, position, color, and audio hooks.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** 2d, 3d, color, geometry, sound | **Themes:** commons, mixed, music

Demonstrates 1D, 2D, and 3D value mappers driving sphere scale, position, color, and audio hooks.

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [5.0, 3.0, 4.0] (environment)

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/value_mapper_example.gd` (83 lines) |
| **Scene** | `res://commons/interfaces/value_mapper_example.tscn` |
| **Registry** | `commons_artifacts.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()` -- Connect signals from mappers
- `_process(delta: float)`
- `_on_1d_value_changed(value: float)` -- Map 1D value to sphere scale
- `_on_2d_values_changed(x_value: float, y_value: float)` -- Map 2D values to sphere position (XY only, keep Z constant)
- `_on_3d_values_changed(x_value: float, y_value: float, z_value: float)` -- Map 3D values to RGB color
- `map_to_audio(frequency: float, amplitude: float)` -- frequency could control pitch
- `map_to_hand_nails(r: float, g: float, b: float)` -- This would connect to your VR hand material
- `map_to_sine_wave(amplitude: float, frequency: float, phase: float)`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
