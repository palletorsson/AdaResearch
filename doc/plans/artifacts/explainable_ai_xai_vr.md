# Artifact: ExplainableAIXAI_VR

> explanation(model, input) → feature importance; three methods: SHAP (game theory), LIME (local surrogate), Grad-CAM (gradient attention)

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** machine_learning | **Themes:** ai, machine_learning

Interactive SHAP, LIME, and Grad-CAM explanations

*a model that cannot explain itself is not understood — it is merely obeyed*

## Design

- **Visual:** material: standard, animation: physics
- **Scale:** [6, 16, 7] (xlarge)
- **Interaction:** see WHY a model decided — grab feature bars, walk through perturbation clouds, read attention heatmaps
- **Critical Parameter:** feature_importance — the SHAP values that reveal which inputs the model actually used
- **What Emerges:** the realization that different explanation methods disagree — there is no single "true" explanation
- **Triggers:** switching XAI methods recolors the entire scene; manipulating features changes the explanation

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/explainable_AI_XAI/ExplainableAIXAI_VR.gd` (613 lines) |
| **Scene** | `res://algorithms/machinelearning/explainable_AI_XAI/ExplainableAIXAI_VR.tscn` |
| **Registry** | `machinelearning.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `feature_spacing` | float | 1.5 |
| `importance_scale` | float | 2.0 |
| `zone_spacing` | float | 10.0 |
| `num_features` | int | 10 |
| `num_perturbations` | int | 20 |
| `num_attention_points` | int | 16 |
| `show_shap` | bool | true |
| `show_lime` | bool | true |
| `show_grad_cam` | bool | true |
| `enable_counterfactuals` | bool | true |
| `enable_feature_manipulation` | bool | true |
| `show_confidence` | bool | true |
| `show_decision_boundary` | bool | true |
| `animate_explanations` | bool | true |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `_initialize_model_data()`
- `_create_prediction_display()`
- `_create_shap_zone()`
- `_create_importance_bar(index: int, feature_name: String, importance: float)`
- `_create_lime_zone()`
- `_create_perturbation_point()`
- `_create_decision_boundary(parent: Node3D)`
- `_create_grad_cam_zone()`
- `_create_heatmap_point(x: int, y: int)`
- `_create_counterfactual_explorer()`
- `_create_control_panel()`
- `_create_info_panels()` -- SHAP explanation
- `_create_info_panel(pos: Vector3, text: String, color: Color)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| MachineLearning_Explainable_AI_XAI | [2,1] | `-` |
| ML_Synthesis | [5,5] | `:0:2:0.6` |

### Relationships

- depends on neural_networks_vr (must have a model to explain); contrasts anomaly_detection (detecting vs explaining)
- Needs: grabbable SHAP bars [has], perturbation cloud [has], heatmap visualization [has], counterfactual path [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
