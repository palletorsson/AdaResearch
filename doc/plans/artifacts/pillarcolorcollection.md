# Artifact: pillarcolorcollection

> pillar_grid(rows, cols) colored from palette[i % palette.size()] — vertical color swatches as architectural columns

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** 2d, color | **Themes:** color, perception

Spawns a grid of pillar prefabs and cycles palette-driven materials from the shared color catalog.

*a column of color has gravity that a flat swatch does not — verticality makes color feel like it stands for something*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** to walk a colonnade where each pillar is a different color, feeling palette relationships through spatial rhythm
- **Critical Parameter:** current_palette_index — cycling palettes transforms the colonnade's mood while the geometry stays fixed
- **What Emerges:** the grid layout creates sight-lines through columns where distant pillars optically mix with near ones
- **Triggers:** cycle_to_next_palette() regenerates all pillars with the next palette; GameManager provides gradient palettes

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/pillarcolorcollection/pillarcolorcollection.gd` (205 lines) |
| **Scene** | `res://algorithms/color/pillarcolorcollection/pillarcolorcollection.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `color_palette_resource` | Resource | - |
| `columns` | int | 3 |
| `rows` | int | 8 |
| `spacing` | Vector3 | Vector3(2.0, 0.0, 2.0) |
| `origin_offset` | Vector3 | Vector3.ZERO |
| `default_palette` | String | "rainbow_gradient" |

### Dependencies

- `res://commons/primitives/pillar/pillar.tscn`

### Key Methods

- `_ready()`
- `_ensure_palette_resource()`
- `_collect_palette_keys()`
- `_get_palettes_dict()`
- `_get_palette_entry(palette_name: String)`
- `_get_palette_colors(palette_name: String)` -- 1. Check Resource
- `_get_palette_title(palette_name: String)`
- `_spawn_pillars()`
- `_clear_existing_pillars()`
- `_apply_color_to_pillar(pillar_instance: Node3D, color: Color)` -- Keep the footer/base black and color only the column
- `_find_mesh_instance(node: Node)`
- `_find_named_mesh_instance(node: Node, target_name: String)`
- `regenerate_pillars()`
- `cycle_to_next_palette()`
- `get_current_palette_name()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Color: Perception, Not Physics** (color) -- map: Color_Pillar

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Pillar | [3,1] | `:0:-0.5` |

### Relationships

- depends on color_palettes.tres and pillar.tscn; contrasts with brick_wall_factory (horizontal vs vertical); feeds into color_sets_overview (structured vs grabbable)
- Needs: palette cycling [has]; pillar.tscn prefab [has]; VR palette selector [missing]; Label3D [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
