# Artifact: Phase Cube

> A cube that shows phase transitions. SOLID (crystal, λ<0.25) → COMPLEX (edge, 0.25<λ<0.55) → GAS (dissolution, λ>0.55). Visual metaphor for matter states as QFEP states.

## Context

**Category:** qfep | **Complexity:** advanced
**Tags:** graph, qfep, visualization | **Themes:** philosophy, qfep, theory

A cube that shows phase transitions. SOLID (crystal, λ<0.25) → COMPLEX (edge, 0.25<λ<0.55) → GAS (dissolution, λ>0.55). Visual metaphor for matter states as QFEP states.

## Design

- **Visual:** material: standard, animation: particle_lifecycle
- **Scale:** [1, 1, 1] (compact)

## Architecture

| | |
|---|---|
| **File** | `commons/interfaces/qfep/phase_cube.gd` (239 lines) |
| **Scene** | `res://commons/interfaces/qfep/phase_cube.tscn` |
| **Registry** | `qfep.json` |
| **Class** | `PhaseCube` extends `Node3D` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `_create_cube()`
- `_create_inner_particles()`
- `_create_surface_particles()`
- `_create_label()`
- `_process(delta)`
- `_update_phase()`
- `_update_visuals(delta)`
- `on_lambda_changed(lambda: float)`
- `_connect_to_lambda()`

### Signals

- `phase_changed`

## Curriculum Position

### Sequences

- qfeplaboratory

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
