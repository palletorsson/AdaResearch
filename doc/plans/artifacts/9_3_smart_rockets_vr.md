# Artifact: Smart Rockets (Genetic Algorithm)

> fitness(x) = 1/d(x, target)^2; DNA = [thrust_vectors]; select → crossover → mutate

## Context

**Category:** machinelearning | **Complexity:** beginner
**Tags:** genetic_algorithm, rockets, evolution, fitness, nature_of_code | **Themes:** ai, biology, machine_learning

Rockets evolving to reach a target through genetic algorithm — selection, crossover, mutation visible in real-time. Based on Nature of Code Ch.9.

*selection pressure alone, without any gradient, discovers solutions in spaces too vast to search*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** watch a swarm of rockets evolve from chaos to precision across generations
- **Critical Parameter:** mutation_rate — too low freezes, too high forgets
- **What Emerges:** coordinated flight paths through gaps nobody designed
- **Triggers:** obstacle collisions kill fitness; target proximity rewards; generation boundary resets

## Architecture

| | |
|---|---|
| **File** | `algorithms/machinelearning/noc_ch09/9_3_smart_rockets_vr.gd` (410 lines) |
| **Scene** | `res://algorithms/machinelearning/noc_ch09/9_3_smart_rockets_vr.tscn` |
| **Registry** | `machinelearning.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `population_size` | int | 120 |
| `mutation_rate` | float | 0.01 |
| `lifespan` | int | 300 |
| `max_force` | float | 0.07 |
| `obstacle_gap_height` | float | 0.18 |

### Dependencies

- `res://commons/artifacts/ArtifactScenePresenter.gd`
- `res://spatial_ui/parameter_controller_3d.tscn`
- `res://commons/resourses/materials/noc_vr/noc_vr_pink_primary.tres`
- `res://commons/resourses/materials/noc_vr/noc_vr_pink_accent.tres`
- `res://commons/resourses/materials/noc_vr/noc_vr_pink_secondary.tres`

### Key Methods

- `_ready()`
- `_setup_environment()`
- `_create_controllers()`
- `_spawn_target()`
- `_spawn_obstacles()`
- `_update_obstacles()`
- `_spawn_population()`
- `_physics_process(delta: float)`
- `rocket_position_nearest()`
- `_update_status(best_fit: float)`
- `_next_generation()`
- `_apply_standard_presentation()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- machinelearning

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| ML_Evolution | [4,1] | `:180:2:0.6` |

### Relationships

- unlocks gradient_descent_visualization (optimization WITH gradient); depends on evolvingflowers (shared GA pattern)
- Needs: VR slider for mutation_rate [has], slider for obstacle gap [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
