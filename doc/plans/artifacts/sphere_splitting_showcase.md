# Artifact: sphere_splitting_showcase

> sphere → fragments(method). Five fracture algorithms: planar cut, octree division, orange-peel sectors, CSG boolean, pre-segmented mesh. One sphere, five deaths.

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** vector | **Themes:** linear_algebra, math, vectors

sphere_splitting_showcase.

*There is no single way to break a sphere. Each fracture method is a decomposition basis — a different answer to what "parts" means.*

## Design

- **Visual:** material: standard, animation: transform
- **Scale:** [18.0, 18.0, 18.0] (world_scale)
- **Interaction:** To compare how the same sphere breaks under five different mathematical decompositions — each one a different answer to "how do you divide a sphere?"
- **Critical Parameter:** The impact point and velocity of the thrown ball. For planar cuts, the impact plane determines the split. For Voronoi, the crack propagates from impact. Geometry meets ballistics.
- **What Emerges:** Planar cuts producing clean halves. Octree producing eight cubic chunks. Sectors peeling like an orange. CSG producing irregular but realistic cuts. Each algorithm's personality visible in its fragments.
- **Triggers:** VR grab + throw → ball hits sphere, each sphere type fractures by its algorithm, stats track throws and total splits, per-sphere piece counts update live

## Architecture

| | |
|---|---|
| **File** | `algorithms/vectors/08_vector_throwing/sphere_splitting_showcase.gd` (286 lines) |
| **Scene** | `res://algorithms/vectors/08_vector_throwing/sphere_splitting_showcase.tscn` |
| **Registry** | `vectors.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `num_balls` | int | 6 |
| `ball_spawn_height` | float | 1.5 |
| `ball_spawn_spacing` | float | 0.3 |

### Dependencies

- `res://algorithms/vectors/08_vector_throwing/throw_ball.tscn`
- `res://algorithms/vectors/08_vector_throwing/destructibles/planar_cut_sphere.tscn`
- `res://algorithms/vectors/08_vector_throwing/destructibles/octree_sphere.tscn`
- `res://algorithms/vectors/08_vector_throwing/destructibles/sectored_sphere.tscn`
- `res://algorithms/vectors/08_vector_throwing/destructibles/csg_cut_sphere.tscn`
- `res://algorithms/vectors/08_vector_throwing/destructibles/segmented_sphere.tscn`

### Key Methods

- `_ready()`
- `_spawn_throw_balls()`
- `_spawn_spheres()`
- `_add_info_label(sphere: Node3D, text: String)`
- `_create_main_info_panel()`
- `_create_instructions()`
- `_on_ball_thrown(_pickable: Node3D, _ball: Node3D)`
- `_on_sphere_split(_parent: Node3D, children: Array, sphere_name: String)`
- `_on_sector_destroyed(_sector: Node3D, _velocity: Vector3, sphere_name: String)`
- `_on_segment_destroyed(_segment: Node3D, _velocity: Vector3, sphere_name: String)`
- `_update_sphere_label(sphere_name: String)`
- `_update_main_info(label: Label3D)`
- `_process(_delta: float)` -- Dynamically update piece counts for active splitting spheres
- `_update_dynamic_piece_counts()` -- This runs every frame to show real-time piece counts
- `_exit_tree()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Vectors & Forces: From Direction to Dynamics** (forces) -- map: ForcesArena

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Forces_Destruct | [11,1] | `-` |
| ForcesArena | [20,15] | `-` |

### Relationships

- Companion to destructibles_test_scene (cubes + planes vs spheres). Lives in ForcesArena. Showcases computational geometry algorithms through destruction.
- Needs: Throwable balls [has], five sphere types [has], per-sphere info labels [has], live piece counters [has]. Missing: slow-motion mode, side-by-side comparison replay.

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
