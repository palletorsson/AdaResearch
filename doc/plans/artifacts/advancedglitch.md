# Artifact: advancedglitch

> Interactive colored line visualization and manipulation algorithms

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** color | **Themes:** color, perception

Interactive colored line visualization and manipulation algorithms

## Design

- **Visual:** material: unlit, animation: transform
- **Scale:** [2.0, 2.0, 1.0] (compact)

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/advancedglitch/advancedglitch.gd` (566 lines) |
| **Scene** | `res://algorithms/color/advancedglitch/advancedglitch.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `auto_animate` | bool | true |
| `glitch_strength` | float | 0.5 : set = set_glitch_strength |
| `corruption_rate` | float | 0.1 : set = set_corruption_rate |
| `temporal_speed` | float | 1.0 : set = set_temporal_speed |

### Key Methods

- `_ready()`
- `_process(delta: float)`
- `setup_demo_scene()` -- Create demo objects in a grid
- `create_demo_object(pos: Vector3, name: String, index: int)` -- Create base geometry
- `initialize_glitch_systems()` -- Initialize datamosh vectors
- `update_all_glitch_effects(delta)`
- `update_datamosh_effect(delta)` -- Update motion vectors
- `apply_datamosh_transform(obj: Node3D, delta)` -- Apply motion vector corruption to geometry
- `update_pixel_sort_effect(_delta)` -- Update pixel sort simulation
- `channel_corruption_effect(t)` -- Advanced RGB channel corruption
- `memory_leak_effect(t, delta)` -- Simulate memory leak accumulation
- `buffer_overflow_effect(t)` -- Simulate buffer overflow conditions
- `apply_overflow_geometry(obj: Node3D, t)` -- Apply geometric corruption during overflow
- `bit_crushing_effect(t)` -- Advanced bit crushing with temporal variation
- `xor_pattern_effect(t, pos: Vector3)` -- Advanced XOR patterns with position and time

### Signals

- `glitch_event_triggered`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- color

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Context | [13,2] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
