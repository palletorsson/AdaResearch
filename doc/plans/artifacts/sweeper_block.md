# Artifact: Sweeper Block

> rotation(beam) -> sweep(player) -> pit(death) — a spinning arm that sweeps you off the edge

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** hazard, transformation, rotation, sweep, windmill, pit | **Themes:** gameplay, hazards

A blue beam that rotates around a central pivot, sweeping players off edges into pits. Don Quixote's windmill — the world spins and you must time your crossing. Uses AnimatableBody3D.

*rotation is the transformation you feel in your inner ear — the one that makes VR real.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** to make rotation felt as a force — not an angle but a sweep that catches your body
- **Critical Parameter:** rotation_speed / arm_length — the windmill must be readable: see it coming, time your move
- **What Emerges:** Don Quixote's windmill — the world rotates and you must dance with it or be swept away
- **Triggers:** constant rotation around Y axis; AnimatableBody3D collision pushes player on contact

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/transformation_blocks/sweeper_block.gd` (111 lines) |
| **Scene** | `res://commons/hazards/transformation_blocks/sweeper_block.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `SweeperBlock` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `arm_length` | float | 3.0 |
| `arm_height` | float | 0.8 |
| `arm_thickness` | float | 0.3 |
| `rotation_speed` | float | 0.8 |
| `block_size` | float | 1.0 |

### Key Methods

- `_ready()`
- `_physics_process(delta: float)`
- `_build_pivot()`
- `_build_beam()` -- AnimatableBody3D: rotates via code, pushes player on contact
- `apply_grid_config(config_data: Dictionary)`

### Grid Config

Accepts: `height`, `length`, `speed`

## Curriculum Position

### Sequences

- transformation
- hazards

### Relationships

- sibling to pusher_block (translation) and grower_block (scale); the rotation lesson
- Needs: AnimatableBody3D [has]; pivot rotation [has]; DangerZone below [external]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
