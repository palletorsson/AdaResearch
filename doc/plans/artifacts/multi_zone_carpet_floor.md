# Artifact: Multi-Zone Carpet Floor

> a floor that demonstrates how Roman mosaicists composed spatial hierarchy

## Context

**Category:** primitives | **Complexity:** intermediate
**Tags:** 2d, pattern, mosaic, floor, pompeii, carpet, composition | **Themes:** arrays, art, cultural_computation

Pompeii mosaic floor demonstrating carpet composition — nested rectangular bands of distinct motifs (dark border, sawtooth chevrons, scallop semicircles) surrounding a central checkerboard field. Demonstrates the Roman principle of hierarchical spatial framing.

*Pompeii's richest floors layer meander, sawtooth, scallop, and field into a single carpet*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 0] (compact)
- **Interaction:** players look down and read nested borders as a lesson in compositional framing
- **Critical Parameter:** cells_short — controls resolution of the central checkerboard field
- **What Emerges:** understanding that carpet composition = nested constraint boundaries
- **Triggers:** instantiation or apply_grid_config

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/pompeii_mosaic_floor/multi_zone_carpet_floor.gd` (397 lines) |
| **Scene** | `res://commons/artifacts/pompeii_mosaic_floor/multi_zone_carpet_floor.tscn` |
| **Registry** | `arrays.json` |
| **Class** | `MultiZoneCarpetFloor` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `floor_size` | Vector2 | Vector2(1.4, 1.0) |
| `cells_short` | int | 6 |
| `color_dark` | Color | MosaicPalette.DARK |
| `color_light` | Color | MosaicPalette.LIGHT |
| `color_terra` | Color | MosaicPalette.TERRACOTTA |
| `grout_color` | Color | MosaicPalette.GROUT |
| `grout_width_fraction` | float | 0.03 |
| `wear_level` | float | 0.3 |

### Dependencies

- `res://commons/artifacts/pompeii_mosaic_floor/mosaic_palette.gd`
- `res://commons/artifacts/pompeii_mosaic_floor/border_motifs.gd`

### Key Methods

- `_ready()`
- `apply_grid_config(config: Dictionary)`
- `_build()`
- `_draw_scallop_frame(dark_v: PackedVector3Array,
	light_v: PackedVector3Array,
	_terra_v: PackedVector3Array,
	x0: float, z0: float,
	frame_w: float, frame_h: float,
	band_w: float, tile_size: float,
	add_tri: Callable)` -- arc segments per semicircle
- `_draw_scallop_strip(dark_v: PackedVector3Array,
	light_v: PackedVector3Array,
	ox: float, oz: float,
	strip_length: float, band_w: float,
	scallop_w: float, segments: int,
	direction: int,
	add_tri: Callable)`

### Grid Config

Accepts: `cells_short`, `floor_size`, `wear_level`

## Curriculum Position

### Sequences

- mosaicanalysis

### Relationships

- pompeii_mosaic_floor (parent pattern), border_motifs (shared library)
- Needs: [implemented] procedural mesh, multi-zone bands, checkerboard field

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
