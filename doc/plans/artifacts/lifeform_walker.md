# Artifact: Lifeform Walker

> cell(t+1) = GoL(neighbors(t)) -- walking creature with 6x6 Game of Life grid as armor

## Context

**Category:** hazards | **Complexity:** intermediate
**Tags:** cellular-automata, conway, game-of-life, armor | **Themes:** biology, gameplay, hazards

Walking creature with a 6x6 Game of Life grid on its torso. Living cells provide armor; Conway's rules run each tick. Dead automaton reinitializes. The cellular automaton made corporeal.

*armor that evolves by its own rules -- the creature does not choose defenses, emergence does.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** Conway's automaton tiling a walker's torso, alive cells as armor, dead cells as vulnerability
- **Critical Parameter:** _grid state / _generation -- GoL rules determine which body cells are armored
- **What Emerges:** emergent armor patterns -- gliders, oscillators, still lifes have tactical meaning as coverage
- **Triggers:** _ca_timer advances generations; alive cells block damage; dead cells expose creature

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/lifeform_walker/lifeform_walker.gd` (223 lines) |
| **Scene** | `res://commons/hazards/lifeform_walker/lifeform_walker.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `LifeformWalker` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `ca_rows` | int | 6 |
| `ca_cols` | int | 6 |
| `ca_tick_rate` | float | 0.5 |
| `ca_tick_rate_chase` | float | 0.2 |
| `cell_size` | float | 0.05 |
| `alive_color` | Color | Color(0.2, 1.0, 0.3) |
| `dead_color` | Color | Color(0.1, 0.1, 0.12) |
| `body_color` | Color | Color(0.25, 0.3, 0.28) |
| `emission_alive` | Color | Color(0.1, 0.8, 0.2) |

### Key Methods

- `_on_ready()`
- `_init_grid()`
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()` -- Torso
- `_process_visual(delta: float)` -- Walk animation
- `_step_ca()`
- `_count_neighbors(row: int, col: int)`
- `_count_alive()`
- `_update_cell_visuals()`
- `_on_damaged(amount: float)`

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_5

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_5 | [7,1] | `-` |

### Relationships

- embodies cellularautomata sequence; GoL rules create unpredictable defense patterns
- Needs: HazardCreatureBase [has]; 6x6 GoL grid [has]; cell mesh [has]; armor logic [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
