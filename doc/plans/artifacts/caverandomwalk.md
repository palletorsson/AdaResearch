# Artifact: caverandomwalk

> N walkers carve air from solid voxel grid — biased random walk inward, then upward level by level

## Context

**Category:** unknown | **Complexity:** beginner
**Tags:** procedural | **Themes:** generation, procedural

caverandomwalk

*a cave is not empty space — it is the record of everything that was removed, and the walker's path is the cave's memory*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1.0, 1.0, 1.0] (compact)
- **Interaction:** to spelunk through a cave that no one designed, knowing every tunnel exists because a random walker happened to step there
- **Critical Parameter:** walkers — more walkers create more interconnected passages; fewer create lonely corridors with dead ends
- **What Emerges:** the biased-toward-center walk in Phase A creates a natural hub without explicitly coding one; vertical climbing creates multi-story caves
- **Triggers:** regenerate() rebuilds from seed; walkers start from four sides, converge at center, then climb — three distinct phases

## Architecture

| | |
|---|---|
| **File** | `algorithms/proceduralgeneration/growth_systems/caverandomwalk/caverandomwalk.gd` (229 lines) |
| **Scene** | `res://algorithms/proceduralgeneration/growth_systems/caverandomwalk/caverandomwalk.tscn` |
| **Registry** | `procgen_extra.json` |
| **Class** | `CaveGenerator` extends `Node3D` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | Vector3i | Vector3i(10, 10, 10) |
| `cube_size` | float | 1.0 |
| `walkers` | int | 4 |
| `steps_per_level` | int | 8 |
| `seed` | int | 1337 |
| `show_gizmos` | bool | false |
| `make_collision` | bool | true |

### Key Methods

- `_ready()`
- `regenerate()` -- Clear existing meshes
- `_init_grid()`
- `_idx(p: Vector3i)`
- `_in_bounds(p: Vector3i)`
- `_set_air(p: Vector3i)`
- `_is_solid(p: Vector3i)`
- `_carve_ball(center: Vector3i, radius: int = CARVE_RADIUS, headroom: int = HEADROOM)` -- Carve a small sphere/cylinder + headroom vertically.
- `_biased_step_towards(current: Vector3i, target_xz: Vector2i, bias: float = 0.65)` -- Lateral step with drift toward target in XZ (y unchanged).
- `_random_lateral_step(cur: Vector3i)`
- `_carve_cave()` -- Start walkers from four sides working inwards on y=0
- `_build_meshes()`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Procedural Generation: Rules Build Worlds** (proceduralgeneration) -- map: PG_Caves_Mazes
- **Procedural Generation (All Maps)** (proceduralgeneration_all) -- map: ProceduralGeneration_CaveRandomWalk

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| PG_Caves_Mazes | [4,2] | `:0:2:0.5` |
| ProceduralGeneration_CaveRandomWalk | [8,7] | `:0:2:0.5` |

### Relationships

- paired with maze_generation in PG_Caves_Mazes; contrasts random walk (organic) with recursive backtracking (structured)
- Needs: voxel collision [has]; spawn gizmo [has]; VR exploration [has]; seed control slider [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
