# Artifact: Pollock 3D

> Interactive 3D Jackson Pollock-inspired art using movement-based procedural randomness

## Context

**Category:** procedural | **Complexity:** intermediate
**Tags:** 3d, animation, procedural | **Themes:** art, probability, procedural, randomness

Interactive 3D Jackson Pollock-inspired art using movement-based procedural randomness

## Design

- **Visual:** material: standard, animation: transform

## Architecture

| | |
|---|---|
| **File** | `algorithms/randomness/proceduralrandomness/movementbased/scenes/pollock3d/pollock_3d.gd` (351 lines) |
| **Scene** | `res://algorithms/randomness/proceduralrandomness/movementbased/scenes/pollock3d/pollock_3d.tscn` |
| **Registry** | `randomness.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `max_splats` | int | 5000 |

### Key Methods

- `_ready()`
- `setup_camera()` -- Create camera if not exists
- `create_canvas()` -- Create a plane mesh for the canvas
- `setup_timer()` -- Create timer for generating new strokes
- `_on_stroke_timer_timeout()` -- Start a new stroke if we haven't reached max active strokes
- `_process(delta: float)`
- `start_new_stroke()` -- Generate random start and end points within canvas bounds
- `animate_stroke(stroke, progress)` -- How far along the curve we should be
- `add_random_splatters(parent, pos, width, color, viscosity)` -- Number of splatters to add - affected by viscosity
- `add_paint_drip(parent, pos, width, color, _direction, viscosity)` -- Create a drip that goes downward from the position
- `get_curve_tangent_at(curve, t)`
- `add_splat(parent: Node3D, position: Vector3, size: float, color: Color)` -- Create sphere for splatter
- `random_point_on_canvas()` -- Generate a random point within canvas bounds
- `_input(event: InputEvent)` -- Space key to clear canvas
- `clear_canvas()` -- Remove all paint nodes and active strokes

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- randomness

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| TestMap_Unused_16 | [1,1] | `:0:2` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
