# Artifact: Perception Altering Environment

> Environmental perception distortion through shader manipulation and spatial warping. Experience how altered sensory parameters change the felt quality of space — perception as active construction, not passive recording.

## Context

**Category:** neuroscience | **Complexity:** intermediate
**Tags:** perception, shader, distortion, sensory, embodiment, neuroscience | **Themes:** biology, neuroscience

Environmental perception distortion through shader manipulation and spatial warping. Experience how altered sensory parameters change the felt quality of space — perception as active construction, not passive recording.

## Design

- **Visual:** material: shader, animation: shader_time

## Architecture

| | |
|---|---|
| **File** | `algorithms/neuroscience/perceptionaltering/perception_altering.gd` (508 lines) |
| **Scene** | `res://algorithms/neuroscience/perceptionaltering/perception_altering.tscn` |
| **Registry** | `neuroscience.json` |
| **Class** | `PerceptionAlteringEnvironment` extends `Node3D` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `environment_size` | Vector3 | Vector3(30, 8, 30) |
| `grid_size` | float | 1.0 |
| `effect_intensity` | float | 1.0 |
| `speed_multiplier` | float | 1.0 |
| `color_shift_speed` | float | 0.2 |
| `wobble_frequency` | float | 0.5 |
| `distortion_regions` | Array[NodePath] | [] |
| `max_color_shift` | float | 0.3 |
| `max_wobble_amount` | float | 0.1 |
| `max_spatial_distortion` | float | 0.2 |
| `max_direction_shift` | float | 45.0 |
| `direction_shift_smoothness` | float | 0.5 |
| `enable_gravity_distortion` | bool | true |
| `max_gravity_angle` | float | 15.0 |

### Key Methods

- `_ready()` -- Find the player
- `_setup_environment()` -- Create a world environment for global visual effects
- `_create_grid()` -- Create a grid mesh
- `_setup_distortion_regions()` -- Process predefined distortion regions
- `_create_default_regions()` -- Create several distortion regions with different effects
- `_create_distortion_region(position: Vector3, size: Vector3, color: Color, type: String, intensity: float)`
- `_on_region_entered(body: Node3D, region: Node3D, type: String, intensity: float)`
- `_on_region_exited(body: Node3D, region: Node3D, type: String)`
- `_process(delta: float)` -- Update perception phases
- `_apply_movement_distortion(_delta: float, direction_effect: float, gravity_effect: float)` -- Get the player's XR camera
- `_add_instructions()`
- `reset_player()`
- `_load_or_create_grid_shader()` -- Try to load existing shader
- `_ready()` -- Store original controller path for reference
- `set_perception_distortion(angle: float)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
