# Artifact: Minimal Glitch

> Minimal glitch aesthetics — subtle data corruption effects that reveal the digital substrate beneath visual representation.

## Context

**Category:** color | **Complexity:** beginner
**Tags:** color, glitch, minimal, digital, aesthetics | **Themes:** art, color, perception

Minimal glitch aesthetics — subtle data corruption effects that reveal the digital substrate beneath visual representation.

## Design

- **Visual:** material: unlit, animation: transform

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/minimalglitch/minimalglitch.gd` (216 lines) |
| **Scene** | `res://algorithms/color/minimalglitch/minimalglitch.tscn` |
| **Registry** | `color.json` |
| **Pattern** | procedural |

### Key Methods

- `_ready()`
- `setup_basic_scene()` -- Camera
- `create_glitch_cubes()` -- Create a grid of cubes with different glitch effects
- `_process(delta: float)`
- `bit_crush_color(t: float)` -- 3-5 bit depth
- `xor_pattern_color(t: float, pos: Vector3)`
- `rgb_channel_shift(t: float)`
- `digital_corruption_color(t: float)`
- `binary_noise_color(t: float)`
- `chromatic_glitch_color(t: float)`
- `_exit_tree()`
- `apply_grid_config(config: Dictionary)`

### Grid Config

Accepts: `cube_spacing`, `grid_cols`, `grid_rows`

## Curriculum Position

### Sequences

- color

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Context | [15,4] | `-` |

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
