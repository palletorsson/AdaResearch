# Artifact: Jelly Cube

> SoftBody3D with spring-mass physics — stiffness, damping, and pressure_coefficient govern deformation response

## Context

**Category:** physics | **Complexity:** beginner
**Tags:** softbody, physics, deformable, jelly, queer_morphology, interactive | **Themes:** physics, soft_body

A soft, deformable cube that jiggles when touched. VR-enabled with physics parameter controls for stiffness, damping, and pressure.

*elasticity is not a property of the object — it is a negotiation between the object's internal forces and whatever touches it*

## Design

- **Visual:** material: standard, animation: static
- **Scale:** [1, 1, 1] (compact)
- **Interaction:** to let you poke a translucent cube and feel its resistance change under your hands as you drag sliders between rigid and liquid
- **Critical Parameter:** stiffness — at 0 the cube collapses into a puddle, at 1 it barely yields, the sweet spot between is where jelly lives
- **What Emerges:** at low stiffness and high pressure the cube becomes a breathing balloon; at low pressure it drapes over the pedestal like cloth
- **Triggers:** hand collision with the SoftBody3D surface causes real-time vertex displacement; pressure slider inflates or deflates the volume

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/jelly_cube/jelly_cube.gd` (309 lines) |
| **Scene** | `res://commons/artifacts/jelly_cube/jelly_cube.tscn` |
| **Registry** | `soft_bodies.json` |
| **Class** | `JellyCube` extends `Node3D` |
| **Pattern** | rack_templates_panel |

### Exports

| Name | Type | Default |
|------|------|---------|
| `cube_size` | float | 0.3 |
| `subdivisions` | int | 4 |
| `jelly_color` | Color | Color(0.2, 0.9, 0.5, 0.8): |
| `stiffness` | float | 0.5: |
| `damping` | float | 0.01: |
| `pressure` | float | 1.0: |
| `mass` | float | 1.0 |
| `emission_strength` | float | 0.2 |
| `use_transparency` | bool | true |

### Dependencies

- `res://commons/audio/rack_templates/RackTemplates.gd`

### Key Methods

- `_ready()`
- `_exit_tree()`
- `_track_node(node: Node)`
- `_track_signal(obj: Object, sig_name: String, callable: Callable)`
- `_create_soft_body()`
- `_create_pedestal()`
- `_create_labels()`
- `_create_vr_controls()`
- `_sync_stiffness_slider()`
- `_sync_damping_slider()`
- `_sync_pressure_slider()`
- `_on_stiffness_slider_moved(_position)`
- `_on_damping_slider_moved(_position)`
- `_on_pressure_slider_moved(_position)`
- `_on_color_pressed(_button = null)`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Soft Bodies & Morphogenesis: Matter That Finds Its Shape** (softbodies) -- map: SoftBodies_Soft_Body_Deformation

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| SoftBodies_Soft_Body_Deformation | [3,3] | `:0:0.5:1` |

### Relationships

- introduces spring-mass foundation before cloth_straps and soft_mushroom; contrasts with rounded_softbody_test which adds strain visualization
- Needs: slider_horizontal [has] (stiffness, damping, pressure); push_button [has] (color cycle, reset); Label3D [has]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
