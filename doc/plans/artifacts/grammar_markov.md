# Artifact: Grammar Markov

> state(t+1) = P(state(t)) -- floating tetrahedron with 4 face-states governed by probabilities

## Context

**Category:** hazards | **Complexity:** advanced
**Tags:** markov-chain, grammar, stochastic, state-machine | **Themes:** gameplay, hazards

Tetrahedron with 4 colored state-faces. Markov chain transition matrix determines attack patterns — visible transition probabilities. Matrix mutates over time. Stochastic grammar made combative.

*every stochastic process is a bet on what comes next -- and the probabilities are always drifting.*

## Design

- **Visual:** material: standard, animation: static
- **Interaction:** a Markov chain attacking in grammar -- lunge, sweep, projectile, shield -- transitions visible
- **Critical Parameter:** _markov_state / transition probabilities -- which attack follows which, mutating over time
- **What Emerges:** unpredictability with structure -- behavior is random but statistically learnable
- **Triggers:** transition_timer fires state changes; probabilities shift with mutations; attack follows state

## Architecture

| | |
|---|---|
| **File** | `commons/hazards/grammar_markov/grammar_markov.gd` (334 lines) |
| **Scene** | `res://commons/hazards/grammar_markov/grammar_markov.tscn` |
| **Registry** | `hazards.json` |
| **Class** | `GrammarMarkov` extends `HazardCreatureBase` |
| **Pattern** | procedural_mesh |

### Exports

| Name | Type | Default |
|------|------|---------|
| `transition_interval` | float | 2.0 |
| `lunge_speed` | float | 6.0 |
| `sweep_speed` | float | 4.0 |
| `projectile_speed` | float | 8.0 |
| `shield_heal` | float | 5.0 |
| `cycles_before_mutation` | int | 3 |

### Key Methods

- `_on_ready()` -- Float above ground
- `_create_materials()`
- `_build_collision()`
- `_build_mesh()` -- Central torus body
- `_process_visual(delta: float)` -- Gentle float bob
- `_process_chase(delta: float)`
- `_do_markov_transition()`
- `_execute_current_attack()`
- `_start_lunge()`
- `_start_sweep()`
- `_fire_projectile()`
- `_activate_shield()` -- Pause and heal
- `_update_face_glow()`
- `_mutate_matrix()`
- `_on_damaged(_amount: float)` -- Flash all faces white briefly then return

## Curriculum Position

### Sequences

- **Hazards: The Bestiary** (hazards) -- map: Hazards_Zoo_6

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Hazards_Zoo_6 | [3,3] | `-` |

### Relationships

- embodies grammar_systems sequence; Markov concept shared with generative_music
- Needs: HazardCreatureBase [has]; state machine [has]; visible probabilities [has]; mutation system [has]; VR interaction [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
