# Artifact: Artifact Catalog Kiosk

> VR kiosk for browsing and spawning artifacts. Filter by theme and complexity, search by tags. 318+ artifacts organized into 10 thematic categories. Dev tool that becomes player inventory through progression.

## Context

**Category:** navigation | **Complexity:** intermediate
**Tags:** navigation, searching, vr | **Themes:** art, decoration, environment

VR kiosk for browsing and spawning artifacts. Filter by theme and complexity, search by tags. 318+ artifacts organized into 10 thematic categories. Dev tool that becomes player inventory through progression.

## Design

- **Visual:** material: standard, animation: static

## Architecture

| | |
|---|---|
| **File** | `commons/artifacts/catalog/ArtifactCatalogTablet3D.gd` (109 lines) |
| **Scene** | `res://commons/artifacts/catalog/ArtifactCatalogTablet3D.tscn` |
| **Registry** | `furniture.json` |
| **Class** | `ArtifactCatalogTablet3D` extends `Node3D` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `screen_width` | float | 1.6 |
| `screen_height` | float | 1.2 |

### Key Methods

- `_ready()` -- Connect to UI signals after scene is ready
- `_connect_ui_signals()` -- Get the catalog UI instance from the Viewport2Din3D node
- `_find_catalog_ui_recursive(node: Node)`
- `_ensure_viewport_updates()` -- Force the SubViewport to use UPDATE_ALWAYS mode for continuous rendering
- `_find_spawn_manager()` -- Try to find spawn manager in the scene
- `_on_spawn_requested(lookup_name: String)`
- `refresh_catalog()`

### Signals

- `artifact_spawn_requested`

## Curriculum Position

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
