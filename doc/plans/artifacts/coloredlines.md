# Artifact: Colored Lines

> path_i(t) = f_type(t, params) where type in {spiral, wave, random, orbit, dna}

## Context

**Category:** unknown | **Complexity:** intermediate
**Tags:** color | **Themes:** color, perception

Interactive colored line visualization and manipulation algorithms

*Every flowing line is a parametric curve — a point moving through space according to equations.*

## Design

- **Visual:** material: shader, animation: shader_time
- **Scale:** [25, 20, 28] (xlarge)
- **Interaction:** Walk through a hallway of flowing colored light tubes that follow mathematical paths
- **Critical Parameter:** flow_speed — controls how fast color flows along the lines
- **What Emerges:** an immersive tunnel of living light from five basic curve equations
- **Triggers:** animation_speed modulates the parametric path evolution over time

## Architecture

| | |
|---|---|
| **File** | `algorithms/color/colorlines/coloredlines.gd` (478 lines) |
| **Scene** | `res://algorithms/color/colorlines/coloredlines.tscn` |
| **Registry** | `color.json` |
| **Pattern** | shader_demo |

### Exports

| Name | Type | Default |
|------|------|---------|
| `line_count` | int | 25 |
| `points_per_line` | int | 100 |
| `animation_speed` | float | 1.0 |
| `line_length` | float | 15.0 |
| `flow_speed` | float | 2.0 |

### Key Methods

- `_ready()`
- `setup_scene()` -- Create atmospheric environment
- `generate_line_paths()` -- Create various path types for visual variety
- `create_spiral_path(index: int)`
- `create_wave_path(index: int)`
- `create_random_path(index: int)`
- `create_orbit_path(index: int)`
- `create_dna_path(index: int)`
- `create_line_meshes()`
- `create_line_mesh_from_path(path: Array[Vector3])`
- `start_line_animations()` -- Animate the line paths themselves
- `animate_line_paths()` -- Deform the line paths over time for organic movement
- `update_line_deformation(time: float)` -- Gently deform line paths over time
- `animate_shader_properties()` -- Animate shader uniforms for color flow effects
- `create_lissajous_curve(a: int, b: int, delta: float, scale: float = 5.0)` -- Create beautiful Lissajous curves (mathematical art)

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Unused** (unused) -- map: Directionality_Examples
- **Wavefunctions: Everything Oscillates** (wavefunctions) -- map: WaveFunctions_Synthesis_Lab

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| Color_Context | [5,12] | `-` |
| Directionality_Examples | [18,19] | `:0` |
| Wavefunctions_Colors | [5,4] | `:0:1:0.05` |
| WaveFunctions_Synthesis_Lab | [0,6] | `:0:1:0.05` |

### Relationships

- depends on ImmediateMesh line rendering; contrasts with hallway_scene (color tubes vs sine tubes); unlocks parametric curve visualization
- Needs: VR walkthrough [has], path type selection [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
