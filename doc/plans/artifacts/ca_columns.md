# Artifact: CA Columns

> 2D Game of Life stacked as 3D history — each generation becomes a new vertical layer

## Context

**Category:** emergence | **Complexity:** intermediate
**Tags:** cellular_automata, emergence | **Themes:** art, biology, cellular_automata, computation

Cellular Automata Columns Sculpture.

*Stack time as space and a flat automaton becomes architecture.*

## Design

- **Visual:** material: vertex_color, animation: particle_lifecycle
- **Scale:** [9, 1, 9] (xlarge)
- **Interaction:** To grow a city of columns from a flat seed, then release walkers to erode and reshape it
- **Critical Parameter:** rule_born/rule_survive — the Life variant determines whether columns are sparse towers or dense walls
- **What Emerges:** Architectural forms from Life rules; walker erosion carves arches and bridges no one planned
- **Triggers:** Generation completes → walkers spawn on top and begin stochastic erosion/deposition cycle

## Architecture

| | |
|---|---|
| **File** | `algorithms/cellularautomata/ca_columns/ca_columns.gd` (309 lines) |
| **Scene** | `res://algorithms/cellularautomata/ca_columns/ca_columns.tscn` |
| **Registry** | `cellular_automata.json` |
| **Pattern** | procedural |

### Exports

| Name | Type | Default |
|------|------|---------|
| `grid_size` | int | 64 |
| `max_height` | int | 128 |
| `cell_size` | float | 0.1 |
| `gap` | float | 0.0 |
| `rule_born` | Array[int] | [3] |
| `rule_survive` | Array[int] | [2, 3] |
| `random_seed` | int | 0 |
| `initial_density` | float | 0.1 |
| `auto_generate` | bool | true |
| `generation_speed` | float | 0.05 |
| `continuous_mode` | bool | false |
| `use_gradient` | bool | true |
| `color_start` | Color | Color(0.1, 0.3, 0.8, 1) |
| `color_end` | Color | Color(0.8, 0.2, 0.8, 1) |
| `gradient` | Gradient | - |
| `enable_walkers` | bool | true |
| `walker_count` | int | 10 |
| `walker_speed` | float | 0.1 |
| `erosion_chance` | float | 0.2 |
| `deposition_chance` | float | 0.05 |

### Key Methods

- `_ready()`
- `_setup_gradient_from_colors()`
- `_process(delta: float)`
- `start_generation()`
- `stop_generation()`
- `clear()`
- `step()`
- `_initialize_grid()`
- `_evolve_grid()`
- `_count_neighbors(x, y)`
- `_setup_multimesh()`
- `_update_multimesh()`
- `_spawn_walkers()`
- `_process_walkers(delta)`
- `_step_walkers()`

### Grid Config

Stub only (`pass`)

## Curriculum Position

### Sequences

- **Cellular Automata: Local Rules, Global Patterns** (cellularautomata) -- map: CA_GameOfLife

### Map Placements

| Map | Cell | Config |
|-----|------|--------|
| CA_GameOfLife | [6,4] | `:0:-0.5:0.2` |

### Relationships

- Feeds into CA_GameOfLife. Contrasts with ca_bridge (1D rule → bridge vs 2D rule → columns). Unlocks CellularAutomata3DStacked.
- Needs: VR rule controls [missing], walker parameter sliders [missing], density control [missing]

## Verification

- [ ] Run scene directly
- [ ] Place in map, check interaction
- [ ] Capture screenshot

---
*Generated by generate_artifact_plans.py on 2026-04-15*
