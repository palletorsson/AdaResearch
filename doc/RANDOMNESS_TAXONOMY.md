# Randomness, taught in the order the engine needs it

> Eighth sequence through the recipe (2026-08-27). Cheat-code:
> **`RandomNumberGenerator.seed` — chance the engine can replay.**

June's 21-concept canon was the best inherited one in the corpus — real concepts, real
truths. Its fault was ORDER: the seed sat 14th when it is the foundation. No generator
owns the file (it is de-facto hand-truth), so the reorder was authored in place:
**the seed first**, then flat chance → dice → the walk → distributions → the bell →
sampling → blue noise → Monte Carlo → Markov → entropy → the noise family → the
applications → and the close, *Seeded growth / DNA → form* — a seed that becomes a
body. Live at **localhost:3003/randomness-concepts** — 199 tiles, 21 sections.

## The super: replay_casino

A casino where the house always replays. Every accident drawn from seed 4, returning
identically forever — and **every number on every placard computed from the draws it
describes**:
- twin dice piles thrown from the SAME seed, identical to the die, beside a seed+1
  stranger that landed its own way — determinism wearing chance as a costume;
- a Galton board whose bins hold the bell 200 seeded balls actually made;
- a random walk of footprints, each step a fresh seeded coin;
- a dartboard quarter-circle announcing the π its 60 darts measured (Monte Carlo);
- a Markov weather wheel, transition ribbons thick by probability;
- two urns wearing their Shannon entropy in bits (sorted 0.00, mixed computed);
- white-noise vs blue-noise star ceilings — clumps against evenly-spaced chance;
- a 10 PRINT runner at the entrance; and the deep-end vitrine, a genome ribbon
  beside the small seeded body it becomes.

Probe: 353 meshes, 0 broken, first try. Seated at Seeds / PRNG vs TRNG.
