# Eye shot — Assemblage_Same_Desire

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **1 overlaps, 4 tight** — the law wants ≥1.2m to walk between.
- `[OVERLAP] panel_bridge_loom          <-> ten_print_textile          gap -0.17m (centers 1.05m)`
- `[tight  ] pattern_atlas_gallery      <-> mamma_monster_gallery      gap +0.05m (centers 1.05m)`
- `[tight  ] pattern_atlas_gallery      <-> incompleteness_scale       gap +1.10m (centers 2.10m)`
- `[tight  ] mamma_monster_gallery      <-> incompleteness_scale       gap +0.05m (centers 1.05m)`
- `[tight  ] incompleteness_scale       <-> godel_statement_plaque     gap +0.23m (centers 1.05m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 0/1  mean Δ=+0.000
      walkability improved: 0/1  mean Δ=+0.000

no sibling kept — the move did not beat the ride (overlaps 1→1, tight 4→4). Note-only.

## The voice (qfep)
6 of 6 cast members carry a theory-claim; 0 mute.
- **godel_statement_plaque** — Proves pure F-minimization is impossible - every formal system has an outside
- **incompleteness_scale** — A balance that cannot weigh itself, tilting forever — Gödel as furniture. In the grammar chapter it stands as 
- **mamma_monster_gallery** — g_dream (each shader) — the Mothers Dream gene: 0 = the weave as she drew it, 1 = the dream the machine breath
- **panel_bridge_loom** — The oldest L-system in human hands: threading, tie-up, treadling — a weaving draft is a grammar, and the drawd

## The text vs the space
walked.md exists — the writing names 6/6 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: godel_statement_plaque, incompleteness_scale, mamma_monster_gallery, panel_bridge_loom, pattern_atlas_gallery, ten_print_textile sit in clearance violations — the text promises what the floor obstructs.

## The heuristic understanding
The violations are real but mechanical moving does not fix them — they are placement DECISIONS (which body yields?), not placement errors. This is verdict material, not tooling material.
