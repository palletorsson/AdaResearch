# Eye shot — Primitives_Melencolia

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **0 overlaps, 17 tight** — the law wants ≥1.2m to walk between.
- `[tight  ] prism_block:90             <-> durer_scene:180:0:0.5      gap +0.00m (centers 1.00m)`
- `[tight  ] prism_block:90             <-> snap_pyramid_puzzle:180    gap +0.00m (centers 1.00m)`
- `[tight  ] prism_block:90             <-> code_display:180:4#tutorial:melencolia_axioms gap +1.00m (centers 2.00m)`
- `[tight  ] durer_scene:180:0:0.5      <-> snap_pyramid_puzzle:180    gap +0.41m (centers 1.41m)`
- `[tight  ] snap_pyramid_puzzle:180    <-> code_display:180:4#tutorial:melencolia_axioms gap +0.00m (centers 1.00m)`
- `[tight  ] snap_pyramid_puzzle:180    <-> dark_sphere:0:-0.5         gap +1.00m (centers 2.00m)`
- `[tight  ] code_display:180:4#tutorial:melencolia_axioms <-> dark_sphere:0:-0.5         gap +0.00m (centers 1.00m)`
- `[tight  ] code_display:180:4#tutorial:melencolia_axioms <-> pyramid:0:-0.5             gap +0.41m (centers 1.41m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 0/1  mean Δ=-0.001
      walkability improved: 0/1  mean Δ=-0.017

no sibling kept — the move did not beat the ride (overlaps 0→0, tight 17→17). Note-only.

## The voice (qfep)
11 of 11 cast members carry a theory-claim; 0 mute.
- **bigframe** — A hollow rectangular beam — the quad given thickness and a window. The primitive becoming architecture: a fram
- **code_display** — a code and text pane — the room's marginal note; it shows the notation beside the demonstration, never dominan
- **cube_scene** — Transformation changes S while the cube serves as the invariant reference body. Because the base form is so le
- **dark_sphere** — Transformation often needs an invariant reference to be legible. The sphere stays simple so surrounding change

## The text vs the space
walked.md exists — the writing names 10/11 of the cast; dwells declared for 2.
- **the writing's subjects are blocked in space**: code_display, dark_sphere, durer_scene, prism_block, pyramid, snap_pyramid_puzzle sit in clearance violations — the text promises what the floor obstructs.
- space without text: floating_sphere_field — standing in the room, absent from the walk.

## The heuristic understanding
The violations are real but mechanical moving does not fix them — they are placement DECISIONS (which body yields?), not placement errors. This is verdict material, not tooling material.
