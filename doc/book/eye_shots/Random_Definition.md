# Eye shot — Random_Definition

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **1 overlaps, 7 tight** — the law wants ≥1.2m to walk between.
- `[tight  ] entropy_axiom:0:1.5:0.9    <-> 2d_in_3d_randomness_vis:0:-0.5 gap +1.00m (centers 2.00m)`
- `[tight  ] entropy_jar                <-> random_butterflies         gap +0.07m (centers 1.00m)`
- `[OVERLAP] random_butterflies         <-> seed_replay_demo:0:-0.5    gap -0.23m (centers 1.00m)`
- `[tight  ] random_butterflies         <-> dark_sphere                gap +0.85m (centers 2.00m)`
- `[tight  ] seed_replay_demo:0:-0.5    <-> dark_sphere                gap +0.08m (centers 1.00m)`
- `[tight  ] clipboard#prng_axioms:-94  <-> trng_vs_prng:-90:-0.2:0.1  gap +1.00m (centers 2.00m)`
- `[tight  ] digital_materiality_glitch:90:-0.4:0.1 <-> catalyst_pickup#sequence_name:randomness#label_text:CHAOS CATALYST#orb_color:0.95,0.55,0.20#accent_color:0.95,0.55,0.20 gap +1.00m (centers 2.00m)`
- `[tight  ] catalyst_pickup#sequence_name:randomness#label_text:CHAOS CATALYST#orb_color:0.95,0.55,0.20#accent_color:0.95,0.55,0.20 <-> trng_vs_prng:-90:-0.2:0.1  gap +1.00m (centers 2.00m)`

## The move
        w, d = self.artifact.footprint_dim()
      File "C:\Users\palle\Documents\GitHub\AdaResearch_46\tools\placement_research.py", line 62, in footprint_dim
        s = max(1, int(round(math.sqrt(self.footprint_cells))))
    TypeError: must be real number, not list

no sibling kept — the move did not beat the ride (overlaps 1→1, tight 7→7). Note-only.

## The voice (qfep)
14 of 14 cast members carry a theory-claim; 0 mute.
- **2d_in_3d_randomness_vis** — The flat diagrams of chance — scatter plots, walks, histograms — stood upright in the room. The board bridges 
- **catalyst_pickup** — sequence_name — IS the catalyst-bracelet mode this the chamber teaches the algorithm; the catalyst is the a pe
- **clipboard** — Transformation changes the clipboard's state, page, and content while preserving a continuous reading object. 
- **dark_sphere** — Transformation often needs an invariant reference to be legible. The sphere stays simple so surrounding change

## The text vs the space
walked.md exists — the writing names 14/14 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: 2d_in_3d_randomness_vis, catalyst_pickup, clipboard, dark_sphere, digital_materiality_glitch, entropy_axiom, entropy_jar, random_butterflies, seed_replay_demo, trng_vs_prng sit in clearance violations — the text promises what the floor obstructs.

## The heuristic understanding
The violations are real but mechanical moving does not fix them — they are placement DECISIONS (which body yields?), not placement errors. This is verdict material, not tooling material.
