# Eye shot — Fractal_Recursion

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **8 overlaps, 14 tight** — the law wants ≥1.2m to walk between.
- `[OVERLAP] fibonacci_pagoda           <-> recursive_chair:0:-0.5:0.2 gap -1.44m (centers 1.00m)`
- `[OVERLAP] fibonacci_pagoda           <-> recursive_table:0:-0.5:0.2 gap -0.44m (centers 2.00m)`
- `[tight  ] fibonacci_pagoda           <-> dark_sphere                gap +0.47m (centers 2.83m)`
- `[OVERLAP] fibonacci_pagoda           <-> example_8_3_recursion_circles_vr gap -0.33m (centers 3.61m)`
- `[tight  ] recursive_chair:0:-0.5:0.2 <-> recursive_table:0:-0.5:0.2 gap +0.00m (centers 1.00m)`
- `[tight  ] recursive_chair:0:-0.5:0.2 <-> example_8_3_recursion_circles_vr gap +0.33m (centers 2.83m)`
- `[tight  ] fractal_recursion_2:-90:2  <-> fractal_recursion_2:-90:2  gap +0.00m (centers 1.00m)`
- `[tight  ] recursive_table:0:-0.5:0.2 <-> dark_sphere                gap +1.08m (centers 2.00m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 1/1  mean Δ=+0.007
      walkability improved: 1/1  mean Δ=+0.075

sibling **Trial_eye_Fractal_Recursion** kept: overlaps 8→2, tight 14→11, pathfinder OK.

## The voice (qfep)
9 of 10 cast members carry a theory-claim; 1 mute.
- **cube_staircase** — step_count — determines granularity of the subdivision, how many slices the cube yields A staircase is a cube 
- **cube_subdivision** — random_corner — when true, every subdivision picks a different child, creating unpredictable growth; when fals
- **dark_sphere** — Transformation often needs an invariant reference to be legible. The sphere stays simple so surrounding change
- **example_8_3_recursion_circles_vr** — max_depth — at 3 it's a simple pattern, at 5 the circles overlap and interfere, creating moire-like density Re
- mute: fractal_recursion_2

## The text vs the space
walked.md exists — the writing names 5/10 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: fibonacci_pagoda, recursive_chair, recursive_table sit in clearance violations — the text promises what the floor obstructs.
- space without text: dark_sphere, example_8_3_recursion_circles_vr, fractal_recursion_2, recursive_boolean_cube, science_screen — standing in the room, absent from the walk.

## The heuristic understanding
The floor was fighting the walk — bodies inside each other's clearance. The mover found a better seating; the ride confirms it in text. The voice column above says what the room is FOR; the next writing pass should say it in the walked page.
