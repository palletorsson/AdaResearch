# Eye shot — Tutorial_Row

> one pass: ride (gaze), move (place --only-improve), gate (pathfinder), voice (qfep). Field note, not a ruling.

## The ride (before)
clearance violations: **1 overlaps, 2 tight** — the law wants ≥1.2m to walk between.
- `[tight  ] xyz_coordinates            <-> dark_sphere                gap +0.97m (centers 2.00m)`
- `[tight  ] xyz_coordinates            <-> column_3_z                 gap +0.64m (centers 3.00m)`
- `[OVERLAP] dark_sphere                <-> column_3_z                 gap -1.17m (centers 1.00m)`

## The move
    logged 1 entries to ada_run\placement_log.json
    summary: 1 maps processed
      constraint improved: 0/1  mean Δ=+0.000
      walkability improved: 0/1  mean Δ=-0.117

no sibling kept — the move did not beat the ride (overlaps 1→1, tight 2→2). Note-only.

## The voice (qfep)
3 of 3 cast members carry a theory-claim; 0 mute.
- **column_3_z** — spacing — at 1.0 the cubes are adjacent and the array feels compact; at 2.0 they feel like separate objects; t
- **dark_sphere** — Transformation often needs an invariant reference to be legible. The sphere stays simple so surrounding change
- **xyz_coordinates** — The addressing scheme laid bare — x, y, z, the three numbers that turn empty space into an array of positions.

## The text vs the space
walked.md exists — the writing names 2/3 of the cast; dwells declared for 0.
- **the writing's subjects are blocked in space**: column_3_z, xyz_coordinates sit in clearance violations — the text promises what the floor obstructs.
- space without text: dark_sphere — standing in the room, absent from the walk.

## The heuristic understanding
The violations are real but mechanical moving does not fix them — they are placement DECISIONS (which body yields?), not placement errors. This is verdict material, not tooling material.
