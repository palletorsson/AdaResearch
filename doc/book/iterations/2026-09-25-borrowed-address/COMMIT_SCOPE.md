# Commit scope

This commit contains the enclosing vertex shader, matching collision, selected-direction witness, aligned Noise_Inside_Noise prose, native images and review evidence. It also includes the existing, previously uncommitted noisesphere stand:warp hookup and this hall's map configuration, which are needed to reach the study.

The shared production files commons/data/museum_core_encounters.json and doc/book/iterations/WORKING.md are intentionally left unstaged: both contain wider uncommitted work. The hall's updated encounter record is preserved in the review snapshots and scoped changes.diff. Those snapshots record the reviewed working state; they are not production-wide replacements.

Validation before commit: 65 native assertions, twelve sampled body routes, two ground rays, six doorway rays, 15 synthetic desktop-pointer clicks, 34 preservation/publication checks and 16 HTTP checks passed. Tracked-headset and Quest acceptance remain open.
