# From a surrounding field to supporting ground

Build on Noise_Inside_Noise: the enclosing sphere retained fixed collision; here the existing heightfield sends its reconstructed mesh to collision too. The learner chooses a route, tests it, changes height from a stable margin and tries again. Preserve the original terrain resource and recovered map platforms.

The body is part of the comparison. A line on the ground and its section can expose possible difficulties without certifying passage. Keep seed, generator and field scale fixed; vary the height contribution. Preserve boundary access explicitly through an edge envelope. Keep noise_space and dark_sphere primary, and use the orb's independent clock to delimit what RESET resets.

Carry the one-height-per-horizontal-position limitation towards later representations. Bring an honest comparison practice into Noise_Perlin_Simplex. Further parameter controls, route finding, line-of-sight and alternative body sizes remain experiments to build, not existing capabilities.
