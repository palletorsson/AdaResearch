# Random_Entropy — decisions

Sequence `randomness`, hall 2 of 14. 24 September 2026. Verdict: **Targeted revision.**

Original at `before.md` (identical to `commons/maps/Random_Entropy/final.md` when work began); candidate at `after.md`. The repo's `final.md` was not touched, and no other repo file was changed. Godot was not run.

## Preserved

The question, the voice, all four code excerpts and `height = max(0.002, height)`, every existing `<!-- @token -->` anchor, and the ledger movement, which matches `shannon_entropy_meter.gd` line by line: tiles, SORT, the two loops, the ceiling, CONTRAST, the 2 mm floor, relative scaling, DISCLOSE. The same goes for the axiom and ruin paragraphs apart from the sentences below, and for the closing paragraph apart from its last sentence.

## Changes (audit and re-check agree; re-checker's wording used where it offered one)

1. **"A gauge hangs on a post in the middle of the hall"** became **"just inside the entrance"**. The hall is 17 x 31 and the spawn is at row 0 (`commons/maps/Random_Entropy/map_data.json:10-11`). The meter token sits at row 5, col 8 (`map_data.json:1379`). The middle of the hall holds the glass field (`:1569`). The chapter itself later says "The ledger at the entrance". This is the re-checker's wording.
2. **"Now press SORT, on the desk's front"** became **"on the console on its post in front of the desk"**. The token carries `#controls:compact#control_front:1.7` (`map_data.json:1379`). The museum calls `Stage.install` on every stamped node (`commons/scenes/endless_museum.gd:13271`). `_compact_controls` reparents every node carrying `panel_w`/`panel_h` into a ReachConsole at local z `control_front`, 1.10 m high, on its own post (`commons/artifacts/randomness_space/museum_exhibit_stage.gd:7-23, 89-114`). The LEDGER panel is a `RackTemplates.create_panel` module (`shannon_entropy_meter.gd:1107`), and those modules get that meta (`commons/audio/rack_templates/RackTemplates.gd:97-98`). It is the meter's only such panel. The readout plate has no such meta and stays on the desk front (`shannon_entropy_meter.gd:1138`), so "The plate on the desk's front…" was left alone. This is the re-checker's wording.
3. **"The same counts entered the same loop."** became **"…so the gauge had nothing to recount. The plate runs the sorted copy's counts through a copy of the same loop, and gets the same H."** `set_sorted` only sets the flag, tweens the tiles and calls `_update_readout` (`shannon_entropy_meter.gd:1259-1266`). It never re-enters `_measure`. The plate's figure comes from `entropy_of(counts_of(sorted_copy()))` (`:1238-1239`), a static twin of the loop (`:1183-1197`). This is the re-checker's wording.
4. **"This work moves bodies away from lattice addresses."** became **"sets points off their lattice addresses, once, when it is built."** Positions are written once into a MultiMesh in `_ready`/`_rebuild` as lattice base plus offset (`algorithms/randomness/entropy_axiom/entropy_axiom_multimesh.gd:70-75, 131-172, 331-341`). There is no `_process`, no body and no collider. This is the re-checker's wording.
5. **"A brick wall fills the space behind them."** became **"A brick wall about half their height runs behind them."** The wall is 12 stacks x 6 courses with its top at about 1.72 m. The columns and lintel top out at about 3.53 m (`commons/artifacts/randomness_space/entropy_ruin.gd:29-35, 37-49`). This is the re-checker's wording.
6. **Before "Choose a joint and wait", added two sentences:** the ruin starts on its own and is down within about a minute, so the visitor should press RESTORE and then RUN / PAUSE if they find it down. The script starts with `running = true` and `clock = -5.0` (`entropy_ruin.gd:12-13`) and removes one stone every 0.65 s (`:7, 87-93`). 84 of its 116 pieces are eligible (12 x 4 + 4 x 9, `:111-114`), so it finishes about 60 s after it starts processing and then stops itself (`:115-121`). RESTORE sets `running = false` (`:153`). The museum suspends a body only beyond 38 m (`commons/data/em_layout.json:133-136`), and the ruin stands at row 26 (`map_data.json:1778`). The museum's own weathering wall starts paused for exactly this reason (`commons/scenes/em/em_weathering_wall.gd:11-14`). The auditor flagged this, and the re-checker confirmed it independently in the brick-wall re-check.
7. **The handover's last sentence:** "the eligible set becomes spatial" became "is laid out where you can count it", and one sentence was added before it: "The ruin never drew among all 116 stones, only among the exposed tops." The ruin already applies Remove's procedure (`entropy_ruin.gd:111-122`: eligible stacks, then `rng.randi_range` over them). So "becomes spatial" in the next room contradicted this chapter's own ruin, and the sequence reader noted that the eligible set was never named here. Remove opens by having you count the amber cubes (`commons/maps/Random_Remove/final.md:5-9`).

## Added (a placed work doing real work, previously ignored)

- **`<!-- @entropy_jar -->` and four sentences.** The jar sits in the meter's row, at (3,5) (`map_data.json:1374`; museum plan row `ada_run/em_plan.json` plans[63] lists it at [3,5]). Its `_measure_entropy` computes −p log₂ p of red against blue inside six height bands, weighted by occupancy (`algorithms/randomness/entropy_jar/entropy_jar.gd:422-465`), and shows `S = %.2f` (`:538, 542`). The shipped state is red below and blue above (`:626-629`). The band boundary falls at y = 0, so the reading starts at 0.00. The jar holds 40 of each colour (`:31`), so the marginal entropy is 1 bit throughout. Picking it up unfreezes the beads (`:646-654`), and while it is held, shaking applies impulses (`:155-164, 407-415`). The measure runs every physics frame (`:168`). Bins run along y only (`:436-446`; the unplaced `shelled` variant's comment `:618-622` states the consequence). This is the arrangement-sensitive counterpoint to "never looked at two tiles at once", in the same row as the desk.

## Left alone, and why

- The auditor's clause "from an unseeded draw: unlike the ledger's sample, this scatter cannot be called back" is true (`entropy_axiom_multimesh.gd:58, 124-129`; the token sets no seed). But it is an addition, not a correction, and the brief prefers the re-checker's shorter wording.
- `hardware_entropy_decay` (desktop fallback is sin/cos of the clock) and `random_butterflies` (whose landing targets use the non-square formula under `#square:true`) are not added. The first is a side note. Describing the second would describe a staging fault, not a work.
- The console standing in front of the desk may partly hide the readout plate in the 09-16 capture. The prose still directs you to the plate on the desk's front, which is true.

## Limits

- **No live walk.** The ruin's timing is arithmetic from the script and the cull distances, not a measured run. Whether a visitor reaches it before the minute is up depends on how long they stay at the meter.
- **If Astra takes the audit's staging fix** (an `autostart:false` option on `entropy_ruin`, mirroring the weathering wall), remove the two sentences added in change 6. They exist only because the ruin does not wait.
- **Jar reach is unverified in the museum.** Shaking needs the XR `picked_up` signal (`entropy_jar.gd:318-321, 646-654`). Whether the desktop carry emits it was not checked, and a project note says it does not. "Lift it and shake it" is written for the headset visitor, who is the chapter's reader throughout.
- The working-tree plan row stamps colonnade piers into the glass field and the ruin's wall line (`dressing: piers: template colonnade x27`). This is uncommitted and derived, and the prose does not depend on it.

## Installed

`after.md` was installed to `commons/maps/Random_Entropy/final.md` on 24 September 2026, after an independent second agent checked every changed or added sentence against the code and its fixes were applied. Line endings: CRLF, as in the baseline. Where this record says `final.md` was not touched, it describes the candidate stage. The diff is in `text.diff`, and the hashes are in `HASHES.txt`: before `c9616c6da7d9…`, after `7ee078e1a76f…`. No runtime or learner status changes, because the text changed and nothing new was walked.
