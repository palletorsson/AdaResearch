# Noise Voxel — what does the threshold let have a body?

Sequence role: after Noise_One's different readings of a field, carry the array into a volume of occupied/empty decisions. Continue into Noise_6_Wall with another possible receiver for the values classification leaves out.

Encounter: keep the seed while moving the threshold; inspect one coordinate; distinguish a change to occupancy from a cut in the model's visible instances. Read the actual score slice beside the stored decisions, then approach the same 24³ arrangement at five times the cell spacing.

Four primary works: perlin_terrain_sculptor, science_screen, voxelnoise, dark_sphere. The source model, section screen and large receiver compare one field; the orb retains independent sine-driven material variation. It adds an earlier operation to the comparison without being retuned as a noise artifact.

Critical opening: occupied cells, face-connected components, visible instances and body-specific routes answer different questions. A threshold is useful and situated. The selected values can also feed another operation. The height bias gives the array a downstairs before gravity enters the account.

Current evidence: 61 rendered museum checks pass; exact sample/occupancy equality at five thresholds; live section, boundary collision, local controls and connected surrounding route. All authored artifacts and recovered structure/utilities remain. Headset reach, body traversal through the volume and Quest performance remain pending.

Return later: reverse the vertical bias; compare adjacency conventions; let a rejected scalar dress a surface; test different body sizes. These are proposed extensions, not existing controls. Earlier intent is preserved in doc/space/voxel-review-2026-09-13/before/.
