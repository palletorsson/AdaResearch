# The line that decides

A green model and a large voxel body share one field. Change the threshold, inspect a live section, and discover how a value becomes occupied or empty. CUT opens the model's view while leaving its stored cells intact. Nearby, the dark orb gives a changing value another life as light.
