# Commit scope

This commit contains this review's optional entropy-ruin start mode, its opt-in and piers:false declaration in Random_Entropy, the revised arrival paragraph and original museum figure, nine scoped review-record updates, the refreshed Randomness reading capture, and this pass's review page, source snapshots, scripts, diffs, native reports and photographs.

Only this pass's two map edits are staged. The weathering-wall declaration was already present in the working map before this pass and remains separate. The shared production files commons/data/museum_core_encounters.json, doc/book/figures/README.md and doc/book/iterations/WORKING.md contain wider uncommitted work; they remain unstaged. This pass's changes to them are preserved in the review's before/after snapshots and diffs. Those snapshots are historical review evidence, not replacements for the shared production files.

The native runner reused output directories. Unrelated older floor screenshots (full, room, first-draw, four-draws and eight-draws) and the stale entropy/ruin-unattended copy are excluded. The pre-change unattended ruin photograph is retained under entropy-before. Current before-entry/after-entry floor photographs and both runs' reports remain.

Validation: 195 publication/preservation assertions and 29 review-link checks passed. The native reports contain 42 completed encounter assertions: entropy 16, removal floor 13, printed maze 13. Entropy and printed-maze processes exited 0. Both removal-floor processes subsequently failed during shutdown with 0xC0000005; that unresolved limit is preserved explicitly. No headset acceptance is claimed.

Only the before-snapshots needed by this pass's chapters, diffs and publication scripts are committed. Unrelated sequence queues, companion files and complete museum-plan backups stay local; their baseline hashes remain in baseline.json.
