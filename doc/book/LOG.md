# BOOK LOG — the process, as a feed

> Append-only, machine-written by the toolchain (tools/book_log.py); newest last.
> Decisions in full live in RULINGS.md; position in STATE.md; site drift in
> FIELD_JOURNAL.md. This file is how you follow the process without reading them.

- 2026-07-03 10:00 · **note** · the book question asked; three existing book-forms mapped (corpus /book, skeleton /tutorial, voice brain-vault)
- 2026-07-03 11:00 · **build** · manuscript engine assembled: manuscript_frame.json (six-part arc) + build_manuscript.py → manuscript.md (~185k, mojibake repaired); randomness writing pass drafted (2/22)
- 2026-07-03 12:00 · **ruling** · R-001 the ledger — the critical poetic arc: throw / measure / hand / trace / debt, a second voice under the pedagogy
- 2026-07-03 12:30 · **draft** · primitives writing pass — proof of the arc (3/22); ledger renders in all authored chapters
- 2026-07-03 13:00 · **ruling** · R-002 the ghost + the dig — book as excavation report; the [:11] cap surfaced as the edge decision; machine field notes in all 21 ledgers
- 2026-07-03 14:00 · **ruling** · R-003 queerness = counter-archiving inside the inheritance (feminist-archival over Deleuzian escape) — definition drafted, landing pending
- 2026-07-03 15:00 · **build** · the book as a 2D map — build_manuscript_map.py: 22 hubs on a meandering spine, children ringed, dig counts on the nodes
- 2026-07-03 16:00 · **ruling** · R-005 the trench protocol — rulings are the source, prose is compiled; RULINGS.md + STATE.md instantiated, R-001..R-004 backfilled
- 2026-07-03 16:30 · **dig** · noise trench report delivered: five forks (P1 spectrum/baseline, P2 Bernini, P3 cosmetics, P4 threshold, P5 the mute six)
- 2026-07-03 17:00 · **ruling** · R-006 the field journal — book_drift.py; baseline: 22 strata, 208 walked, 716 pearls, 3041 registry
- 2026-07-03 17:00 · **ruling** · R-007 blob aesthetics — the 2.5 universe, the abject on a budget, the blob as gauge, the export; rules noise P3
- 2026-07-03 18:00 · **build** · spine-graph ↔ book integration: Book lens (194 lit / 578 dimmed / 772) + import_walk_from_graph.py (walk curation, tested on noise and reverted)
- 2026-07-03 19:00 · **stage** · chapter_stage.py — staging briefs: fractals melody holds (70%); randomness 30% flat, env_one (60 m) blows the grid to 86×129
- 2026-07-03 20:00 · **ruling** · R-008 the deep dig — roles (load-bearing/counter/side/ornament/prop), counters, blanks; withholding replaced by finding
- 2026-07-03 21:00 · **dig** · dig_report.py pilot: fractals' classical canon found at depth (mandelbrot_dive LB6, box_counting_dimension LB6 uncaptured, Hilbert = uncovered concept); randomness: entropy_axiom LB6 uncaptured
- 2026-07-03 22:00 · **ruling** · R-009 fractals promotions — mandelbrot_dive + box_counting_dimension in; the Hilbert blank DECLARED; walk hand-cut (recursive_tree_2 + fractal_scene stepped down, ghost's call)
- 2026-07-04 00:00 · **drift** · fractals: walked +box_counting_dimension +mandelbrot_dive −fractal_scene −recursive_tree_2 (first real journal entry)
- 2026-07-04 00:30 · **ruling** · R-010 the generative loop — blanks have a lifecycle: declared → candidates → filled | commissioned
- 2026-07-04 01:00 · **capture** · box_counting_dimension captured in Godot (D = 1.555 measured vs ideal 1.585 — the debt photographed); chapter 11 now 11/11 illustrated
- 2026-07-04 02:00 · **note** · the fold ladder confirmed with one sharpening: plan and elevation are conjugate projections; tiers are dimensional addresses
- 2026-07-04 03:00 · **room** · wall_extrude.py built; Hangar_Fractals generated (28×40, pathfinder 1120/1120, captured) — three bays in walk order, tree-cloud hero, the Hilbert plinth standing empty
- 2026-07-04 04:00 · **ride** · fold_ride.py — first ride log folded into chapter 11: hero neither visited nor seen; box_counting holds 14/25 stations at 96°; 8/11 blind bays
- 2026-07-04 04:30 · **ride** · hero-ghost root cause = gaze_ride spawn-code bug ("s" vs "sp", fixed); teleporter moved to the hero's axis; rerun: hero at 32°/25.6 m then HUGE 66° CENTER at station 24
- 2026-07-04 05:00 · **build** · committed: AdaResearch c44bbf783 (26 files, the engine) + encyclopedia c0a38d65 (manuscript, tutorial, spine-graph w/ lens)
- 2026-07-04 05:30 · **note** · blog published: /blog/2026-07-04-the-trench-protocol
- 2026-07-04 06:00 · **ruling** · R-011 noise ruled (P1a spectrum, P2c knowing Bernini, P4a threshold spent, P5c ruled truths) — overlay drafted, 4/22 passes; two truth-sentences ghost-proposed, awaiting Palle's wording
- 2026-07-04 13:06 · **room** · Hangar_Fractals captured in Godot — the wall extruded, the tree-cloud hero north, the Hilbert plinth standing empty ![Hangar_Fractals iso](/book-log/hangar_fractals_iso.png)
- 2026-07-04 13:06 · **capture** · box_counting_dimension render — D = 1.555 measured against the ideal 1.585, the debt photographed ![box counting](/artifact-gallery/captures/box_counting_dimension/front.png)
- 2026-07-04 13:06 · **stage** · staging brief regenerated: fractals — contrast 80%, scale melody: `spark:[0,1,1,3,2,0,1,0,3,0,0]`
- 2026-07-04 13:06 · **ride** · Hangar_Fractals walked and folded into fractals: 7 observations — gaze profile per station: `spark:[6,7,29,31,23,31,33,44,44,37,37,41,41,46,46,53,53,60,60,96,9,32,33,40,66,0]`
- 2026-07-04 13:46 · **dig** · dig report noise: LB=WhiteNoiseGallery, perlin_terrain_sculptor; 1 promotions proposed, 1 blanks, 5 buried with reasons
- 2026-07-04 13:46 · **dig** · dig report lsystems: LB=lsystem_editor, space_filling_curve_gallery; 3 promotions proposed, 0 blanks, 0 buried with reasons
- 2026-07-04 15:10 · **dig** · anti-threshold blank: /api/find searched the whole corpus — candidates found (mc_sculpt_vr = the blob given an interior, cube_mound_scene, constraint_sculptor, poke_dough_toy); commission may be unnecessary. /api/game/analyze independently confirms Hangar_Fractals (100 pct walkable, all reachable)
- 2026-07-04 16:38 · **build** · chapter rebuilt: noise
- 2026-07-04 16:38 · **build** · chapter rebuilt: fractals
- 2026-07-04 16:38 · **drift** · **noise**: walked +mc_sculpt_vr; walked −noise_space
- 2026-07-04 16:38 · **stage** · staging brief regenerated: noise — contrast 80%, scale melody: `spark:[3,2,3,1,3,0,3,3,3,0,1]`
- 2026-07-04 16:46 · **dig** · dig report primitives: LB=klee_walking_point, cube_scene; 0 promotions proposed, 0 blanks, 52 buried with reasons
- 2026-07-04 16:46 · **stage** · staging brief regenerated: primitives — contrast 30%, scale melody: `spark:[0,0,0,0,2,1,0,0,0,0,0]`
- 2026-07-04 16:52 · **stage** · Walls_Primitives review room built and captured — the ten auto-seeded primitives walls hung in one hall (pw_* clusters exported from spine_walls.json). Verdict: rough seeds — lab_room boxes mounted as props, Portals spills off the floor, the miniatures vanish at distance ![primitives walls review](/book-log/walls_primitives_front.png)
- 2026-07-04 17:04 · **stage** · the ten primitives walls now visible on /wall-gallery (Auto-curated seeds): pw_* wall-shots rendered via WallHangarEditor --capture-clusters; the walls hold the dig's flagged classics — mystic_writing_pad on Point_Trace, becoming_catalyst on Polythedra, achilles_tortoise on Portals, durer_scene on Melencolia ![Point_Trace wall — the Wunderblock hung](/wall-shots/pw_point_trace.png)
- 2026-07-04 19:58 · **stage** · Point_Trace curated (first primitives wall, R-013): the stage that waits for the player's line, the hand sampled on a micropod, the Wunderblock on its plinth — real panel text from the chapter's prose; lab_room and ambient fillers cut; moved from auto-seeds to the curated section of /wall-gallery ![Point_Trace curated wall](/wall-shots/Point_Trace.png)
- 2026-07-04 20:30 · **stage** · Point_One curated (second primitives wall): the emptiest wall on purpose — Fontana's puncture beside You Are Here, absence and address as the chapter's first counter-pair staged. Auto-seeds down to 8 primitives walls remaining ![Point_One curated wall](/wall-shots/Point_One.png)
- 2026-07-04 22:59 · **stage** · Primitives_Melencolia curated (third wall): Durer's scene on the walk-in stage — magic square, balance, orb rendering from the engraving — under MEASUREMENT ALONE DOES NOT MOVE; the snap-pyramid puzzle as the cure. Auto-seeds down to 7 ![Melencolia curated wall](/wall-shots/Primitives_Melencolia.png)
- 2026-07-04 23:03 · **stage** · Primitives_Portals curated (fourth wall): the fold three ways — the 86 m door gets a pointing plinth (turn around, the room is the exhibit), Zeno's race gets the walk-in stage where a still frame rightly shows nobody arriving, the capsule nearly vanishes (biggest door, smallest traveler). Auto-seeds down to 6 ![Portals curated wall](/wall-shots/Primitives_Portals.png)
- 2026-07-05 09:03 · **room** · Hangar_Primitives generated by creator_walk (R-013): 11 artifacts + 4 wall works placed along the librarian's corridor, 1 daises raised, walk-back seeded 1 lab props (cube_chalkboard…)
- 2026-07-05 09:04 · **room** · Hangar_Primitives generated by creator_walk (R-013): 11 artifacts + 4 wall works placed along the librarian's corridor, 1 daises raised, walk-back seeded 11 lab props (cube_chalkboard, grid_chalkboard, ignorance_chalkboard, line_chalkboard, melencolia_chalkboard…)
- 2026-07-05 09:05 · **ride** · Hangar_Primitives walked and folded into primitives: 4 observations — gaze profile per station: `spark:[57,60,13,21,49,20,161,3,35,109,44,28,28,0,15,109,44,28,0,112,49,20,161,166,18,24,11,18,46,123,29,157,18,25,13,18,27,27,28,28,33,29,72,33,71,86,76,101,55,73,73,4,18,11,22,18]`
- 2026-07-05 09:05 · **room** · Hangar_Primitives (creator's walk pilot) captured — the librarian corridor, 21x100 ![Hangar_Primitives](/book-log/hangar_primitives_iso.png)
- 2026-07-05 09:05 · **build** · chapter rebuilt: primitives
- 2026-07-05 10:21 · **room** · Hangar_Fractals_Walk generated by creator_walk (R-013): 11 artifacts + 5 wall works placed along the librarian's corridor, 3 daises raised, walk-back seeded 7 lab props (cube_chalkboard, station_crates, fire_extinguisher, whiteboard, lab_stool…)
- 2026-07-05 10:22 · **ride** · Hangar_Fractals_Walk walked and folded into fractals: 4 observations — gaze profile per station: `spark:[7,25,8,13,63,14,74,9,10,24,10,14,26,16,62,62,0,47,12,26,28,10,26,18,20,28,19,16,13,3,11,12,17,17,16,16,22,14,14,15,15,22,63,24,0,24,17,62,40,21,35,70,112,32,67,64,26,40,40,51,34,34,17,17,13,13,17,5,17,32,21,21,0,21,19,64,91,20,49,49,37,22,22,40,40,26,64,64,23,24,15,15,30,30,84,18,18,0,18,10,19]`
- 2026-07-05 10:22 · **room** · Hangar_Fractals_Walk — the creator-walk before/after: the recursion torus rings the corridor, the tree-cloud erupts inside its loop, five curated walls colonnade the path (29x167) ![Hangar_Fractals_Walk](/book-log/hangar_fractals_walk_iso.png)
- 2026-07-05 10:23 · **build** · chapter rebuilt: fractals
- 2026-07-05 11:13 · **stage** · Primitives fully curated — all ten walls (R-013 batch): Klee caught mid-walk on Point_Lines, the lattice walked on Line_Grid, the triangle pact, the catalyst bracelet on Polythedra, the cube performed, and the wunderkammer erupting past its shelf. /wall-gallery Primitives section complete; auto-seeds emptied of primitives ![Point_Lines — Klee mid-walk](/wall-shots/Point_Lines.png)
- 2026-07-05 12:31 · **room** · Hangar_Primitives generated by creator_walk (R-013): 11 artifacts + 10 wall works placed along the librarian's corridor, 1 daises raised, walk-back seeded 11 lab props (cube_chalkboard, grid_chalkboard, ignorance_chalkboard, line_chalkboard, melencolia_chalkboard…)
- 2026-07-05 12:31 · **room** · Hangar_Primitives rigged (R-014): 10 relations laid under the floor from hero grid_lines (503 conduit cells); 2 spark gap(s) — the room wired into one apparatus
- 2026-07-05 12:32 · **ride** · Hangar_Primitives walked and folded into primitives: 5 observations — gaze profile per station: `spark:[31,110,49,28,28,0,28,0,53,22,8,49,103,23,42,32,29,28,44,44,0,15,58,43,19,103,49,103,42,44,28,0,53,32,28,91,33,89,89,109,44,0,44,19,24,21,140,39,27,9,9,39,48,18,50,34,4,34,53,53,15,52,22,23,23,16,16,34,0,20,152,90,170,33,164,66,128,53,39,145,68,13,83,73,44,24,64,83,53,45,86,35,17,17,25,13,3,53,53,20,53,53,39,25,6,28,33,33,0,28,11,7,7,53,53,53,61,25,20,20,83,83,103,44,0,44]`
- 2026-07-05 12:32 · **room** · Hangar_Primitives regenerated (all 10 curated walls, 23x182) and RIGGED: 10 relations under the floor from hero grid_lines, player_trace as the junction box, lab_room on the weakest wire (0.15), two spark gaps at the dig counters. The conduits are flush — the glue reads only from inside ![Hangar_Primitives rigged](/book-log/hangar_primitives_rigged_iso.png)
- 2026-07-05 12:32 · **build** · chapter rebuilt: primitives
- 2026-07-05 13:28 · **stage** · doll houses generated (research): Dollhouse_Random_Walk, Dollhouse_Random_Gaussian, Dollhouse_Random_Definition — each map's artifacts freely re-assembled as a vitrine specimen for comparison
- 2026-07-05 15:03 · **stage** · doll-house pilot (3 randomness maps) — LEARNING: (1) map-scale reads as 'one thing', chapter-scale (182-corridor) does not — the MAP is the room; (2) hero:roster ratio dictates mode — Walk=specimen cluster, Gaussian=hero-IS-the-room (20m bell-curve mountain), Definition=too-empty-scatter; (3) void-surround floating diorama is a keeper; (4) trng_vs_prng renders rich = 4th dig 'mute' verdict overturned by capture ![doll house Random_Walk](C:/Program Files/Git/book-log/dollhouse_random_walk.png)
- 2026-07-05 15:30 · **room** · randomness composed as a necklace of 13 rooms (R-015): modes {'monument': 11, 'specimen': 1, 'cabinet': 1} — the room is the map
- 2026-07-05 15:36 · **room** · R-015 LANDED: randomness composed as a NECKLACE OF 13 ROOMS (the room is the map). Three modes fire: specimen (Cubes — clean instrument cluster), monument (Mushrooms — the hero ecosystem IS the room, 11/13 of randomness since it's the environment sequence), cabinet (Walk — dense packed accumulation). Each a floating diorama-object. The chapter is retired as the room; it is now the necklace order + transitions ![Room_Random_Mushrooms monument](C:/Program Files/Git/book-log/room_random_mushrooms.png)
- 2026-07-05 15:41 · **room** · workstation cluster built for distribution_sampler: hero + 4 ladder children + lab prop cast — the mindmap node made physical
- 2026-07-05 16:48 · **room** · workstation cluster pilot (distribution_sampler): the mindmap node made physical — hero + concept-ladder CHILDREN (bench/3d/comparator variants) + lab PROP cast (cylinder, stool, extinguisher, crates, monitor, task light). Confirms the model: the space is a nested hierarchy, not a flat array; the 1D spine is only its index; the unit is a lab workstation. Composition needs tighter packing (bench, not plaza) — a tuning, not a model problem ![workstation cluster](C:/Program Files/Git/book-log/workstation_distribution.png)
- 2026-07-05 16:52 · **room** · maps composed as halls of workstations (R-016): 2 rooms — dollhouse+workstation+rigger fused
- 2026-07-05 17:10 · **room** · TOOLS FUSED (R-016): hall.py = dollhouse + workstation + rigger. A map-room is now a HALL OF WORKSTATIONS — each hero a tight lab bench (hero on a stage under a task light, ladder children on plinths, prop cast: cylinder/stool/monitor/extinguisher/crates) flanking a walked aisle. Walkable floor, ZERO pathfinder warnings. Hall_Random_Definition = 2 benches across the aisle, the reference aesthetic at blockout ![Hall of workstations](C:/Program Files/Git/book-log/hall_random_definition.png)
- 2026-07-05 17:15 · **stage** · workstation clusters exported for the visual iterate loop: ws_distribution_sampler, ws_slot_machine, ws_perlin_terrain_sculptor, ws_mc_sculpt_vr, ws_klee_walking_point, ws_lsystem_editor — capture via WallHangarEditor --capture-clusters, review on /wall-gallery
- 2026-07-05 17:18 · **stage** · workstation iterate loop LIVE: six ws_* clusters captured via the wall-hangar pipeline, reviewable on /wall-gallery. slot_machine + perlin_terrain_sculptor stations read as the reference bench (apparatus + ladder variants + O2 cylinder/lamp/crates/extinguisher). Punch list: floating/occluded seats on some plinths, double-pedestal when hero brings its own base, ladder-less heroes (klee) need fold-neighbor fallback children ![ws_slot_machine](C:/Program Files/Git/book-log/ws_slot_machine.png)
- 2026-07-05 17:24 · **stage** · workstation clusters exported for the visual iterate loop: ws_distribution_sampler, ws_slot_machine, ws_perlin_terrain_sculptor, ws_mc_sculpt_vr, ws_klee_walking_point, ws_lsystem_editor — capture via WallHangarEditor --capture-clusters, review on /wall-gallery
- 2026-07-05 17:28 · **stage** · DNA layer added to workstations (iteration 2): each station now carries a THEMED surreal_lab instrument (specimen tank for the blob, chemrig for growth, reactor for the field, gravgun for chance, teleporter for the walk, scanner for measurement) + a modern-art DNA wall piece. lsystem station: sapling under the task light, grammar-tree by the CONVERGENCE screen, the L-system string cascading off the bench. The gallery loop: edit exporter, re-export, re-capture, review ![ws_lsystem_editor with DNA layer](C:/Program Files/Git/book-log/ws_lsystem_editor.png)
- 2026-07-05 17:31 · **stage** · modkit seeded distribution_sampler x3 configurations (mk_distribution_sampler_s1, mk_distribution_sampler_s2, mk_distribution_sampler_s3) — same slot template, different fill (R-017)
- 2026-07-05 17:34 · **stage** · MODULAR SYSTEM (R-017): station_modules.json (size classes S/M/L/XL + pools + slot templates) + modkit.py seeder. Same bench_v1 slots, seeds 1-3 = three visibly different stations. Every prop fits its class; the seed picks the cast ![modkit seed 3](C:/Program Files/Git/book-log/mk_s3.png)
- 2026-07-05 17:45 · **stage** · modkit seeded distribution_sampler x1 configurations (mk_distribution_sampler_s1) — same slot template, different fill (R-017)
- 2026-07-05 17:47 · **stage** · R-018 THE MEETING: junctions are now a declared layer of the modular system — artifact-meets-footing (the reveal/shadow gap on every cap), wall-meets-floor (skirting threshold), station-meets-aisle (the lit zone-edge where the station ENDS before it stops existing), wall-meets-wall (seam pillar). Scarpa adopted: the joint is the generator of form. The flush lines read only from inside — endings are for the walker, not the elevation ![meeting layer on s1](C:/Program Files/Git/wall-shots/mk_distribution_sampler_s1.png)
- 2026-07-05 18:41 · **stage** · modkit seeded mc_sculpt_vr x2 configurations (mk_island_mc_sculpt_vr_s1, mk_island_mc_sculpt_vr_s2) — same slot template, different fill (R-017)
- 2026-07-05 18:41 · **stage** · modkit seeded klee_walking_point x2 configurations (mk_vitrine_klee_walking_point_s1, mk_vitrine_klee_walking_point_s2) — same slot template, different fill (R-017)
- 2026-07-05 18:41 · **stage** · modkit seeded fibonacci_pagoda x1 configurations (mk_monolith_fibonacci_pagoda_s1) — same slot template, different fill (R-017)
- 2026-07-05 18:41 · **stage** · modkit seeded galton_board x1 configurations (mk_galton_board_s4) — same slot template, different fill (R-017)
- 2026-07-05 18:42 · **stage** · modkit seeded entropy_meter x2 configurations (mk_vitrine_entropy_meter_s1, mk_vitrine_entropy_meter_s2) — same slot template, different fill (R-017)
- 2026-07-05 18:46 · **stage** · modkit seeded galton_board x1 configurations (mk_galton_board_s4) — same slot template, different fill (R-017)
- 2026-07-05 18:46 · **stage** · modkit seeded mc_sculpt_vr x1 configurations (mk_island_mc_sculpt_vr_s3) — same slot template, different fill (R-017)
- 2026-07-05 18:46 · **stage** · modkit seeded stochastic_tree_separated x1 configurations (mk_monolith_stochastic_tree_separated_s1) — same slot template, different fill (R-017)
- 2026-07-05 18:46 · **stage** · modkit seeded galton_board x1 configurations (mk_galton_board_s4) — same slot template, different fill (R-017)
- 2026-07-05 18:46 · **stage** · modkit seeded galton_board x1 configurations (mk_galton_board_s4) — same slot template, different fill (R-017)
- 2026-07-05 18:48 · **stage** · modkit seeded perlin_terrain_sculptor x1 configurations (mk_island_perlin_terrain_sculptor_s1) — same slot template, different fill (R-017)
- 2026-07-05 18:48 · **stage** · modkit seeded koch_curve_3d x1 configurations (mk_monolith_koch_curve_3d_s1) — same slot template, different fill (R-017)
- 2026-07-05 18:48 · **stage** · modkit seeded cryogenicpreservation x1 configurations (mk_monolith_cryogenicpreservation_s1) — same slot template, different fill (R-017)
- 2026-07-05 18:51 · **build** · the station-model library: four templates in station_modules.json (bench / island / vitrine / monolith), fleet seeded by modkit across heroes, captured, browsed at /station-gallery. Learned: grow-over-time heroes (pagoda, stochastic tree) render EMPTY at capture -- monoliths need instant-mesh heroes; shader/texture kin render as floating quads -- modkit now prefers measured bodies for CHILD slots.
- 2026-07-05 18:53 · **stage** · modkit seeded transform_staircase_spiral x1 configurations (mk_monolith_transform_staircase_spiral_s1) — same slot template, different fill (R-017)
- 2026-07-05 20:06 · **ruling** · R-019 the meeting audit: every part asks how it meets what it touches -- can the meet be improved? Named defects ruled into standing law: body_on_cell (artifacts seat by measured BODY centre, not code origin -- WallHangarEditor now centres the live AABB on the cell before the vertical seat) + pole_on_floor (the luminaire's flat plate becomes a stepped pedestal with collar -- a foot that cannot be SEEN has not met the floor).
- 2026-07-05 20:10 · **stage** · modkit seeded distribution_sampler x3 configurations (mk_distribution_sampler_s1, mk_distribution_sampler_s2, mk_distribution_sampler_s3) — same slot template, different fill (R-017)
- 2026-07-05 20:10 · **stage** · modkit seeded galton_board x1 configurations (mk_galton_board_s4) — same slot template, different fill (R-017)
- 2026-07-05 20:10 · **stage** · modkit seeded mc_sculpt_vr x1 configurations (mk_island_mc_sculpt_vr_s2) — same slot template, different fill (R-017)
- 2026-07-05 20:10 · **stage** · modkit seeded perlin_terrain_sculptor x1 configurations (mk_island_perlin_terrain_sculptor_s1) — same slot template, different fill (R-017)
- 2026-07-05 20:10 · **stage** · modkit seeded entropy_meter x2 configurations (mk_vitrine_entropy_meter_s1, mk_vitrine_entropy_meter_s2) — same slot template, different fill (R-017)
- 2026-07-05 20:10 · **stage** · modkit seeded transform_staircase_spiral x1 configurations (mk_monolith_transform_staircase_spiral_s1) — same slot template, different fill (R-017)
- 2026-07-05 20:15 · **capture** · R-019 applied and recaptured: the luminaire stands on a stepped pedestal on OPEN floor (light slots moved out from behind heroes -- a foot that cannot be seen has not met the floor), and every artifact seats centred on its cap (body_on_cell in the wall editor). Fleet of 9 modkit stations refreshed; /station-gallery live. ![monolith station: luminaire with visible pedestal foot, hero plinth centred](/wall-shots/mk_monolith_transform_staircase_spiral_s1.png)
- 2026-07-05 21:36 · **build** · R-019 DNA PUSH: the meets moved from one lamp + one editor into the TYPE layer -- staging_dna.json now declares per-type junctions: specimen's reveal is explicit data (cap_inset 0.12, not a silent prop default), the instrument gets a zone-edge floorline at its pad front, the APPARITION gets a shadow line (the hover meets the floor by LIGHT, not contact), the tableau gets skirting, the monument a ground line. Verified staged: MelodySoundBoard bench + zone edge; chaos_particles hover + shadow line + skydome. All ~2570 dressing rooms inherit. ![apparition staged: particle hover, skydome backdrop, shadow line on the floor beneath](/book-log/meets_float_staging_hero.png)
- 2026-07-05 21:36 · **note** · capture gate punch-list: boid_flocking HANGS headless capture entirely (simulation artifact) -- headless-hostile artifacts are a third class beside render-nothing and grow-over-time.
- 2026-07-05 21:41 · **note** · Palle's 16-second rule made law: tools/godot_watchdog.py wraps headless Godot runs -- kill the process tree if no result in the boot grace or output stalls 16s. Verified: hung boid_flocking killed at grace; healthy capture passes in 7s. No capture may halt the pipeline.
- 2026-07-06 08:21 · **stage** · modkit seeded perlin_terrain_sculptor x1 configurations (mk_labwall_perlin_terrain_sculptor_s1) — same slot template, different fill (R-017)
- 2026-07-06 08:27 · **build** · the wall grows a working VOCABULARY: station_wall_module (whiteboard / small display / glass bay / pipe drops / cable tray with conduit drop / vent / chevron / ENDCAP -- the special short ending, mirrorable so both ends close outward) + station_ceiling (coffered tiles, hung cable tray, vent duct, I-beam) + station_floor_module (walkable grate, cable channel with lit slit, bolted access hatch). Combined in labwall_v1 -- the Black Mesa read: services run DOWN the wall, along the ceiling, under the floor. Pools wallwork/ceilingwork/floorwork seedable into any template. First seed: perlin_terrain_sculptor behind the glass bay. /station-gallery has the group. ![labwall_v1: whiteboard, display, vent, glass, pipes, endcaps + ceiling and floor work](/book-log/mk_labwall_s1.png)
- 2026-07-06 08:33 · **build** · wall hangar editor opens on the Lab Wall: interactive launches now load mk_labwall_perlin_terrain_sculptor_s1 as the start scene (override with --cluster=<name>); the combined-segment run is the editor's home base -- open, edit segments, K saves back to the cluster.
- 2026-07-06 08:43 · **room** · LabWall_Walk: the wall vocabulary becomes a WALKABLE room -- the labwall cluster placed via cluster: token in a 16x10 map, pathfinder 160/160 reachable, captured in-engine (biome ring and all). The editor's start scene and the VR walk are now the same object: K in the editor saves the cluster, the map re-resolves it. ![LabWall_Walk in-engine: the segment run on the walkable grid](/book-log/labwall_walk_front.png)
- 2026-07-06 09:12 · **ruling** · R-020 THE CLOSING PROTOCOL: the missing piece is a stop rule + a countdown, not a feature. Two objects -- the sequence is the SITE (the game, stays alive), the chapter room is the EXHIBITION (the dig's pearls staged walkable). Chapter lock = writing pass + one room, NEVER sequence completion. Tool freeze until 22/22. Scoreboard: doc/book/FINISH_LINE.md, ticked by Palle's hand only. The end object is the PDF. 4/22 passes exist, 0/22 locked, primitives' room already built.
- 2026-07-06 09:25 · **ruling** · R-021 THE WALKING SCOPE (amends R-020): the archaeology is NOT based on the sequence -- the sequence is the INSTRUMENT, the excavation of the algorithms' world-ontology under QFEP. The book reports on WALKS: unit of work = the map-walk, unit of writing = the map's walked.md (blurb says what it is, intent says why it was built, walked.md says what WALKING it taught). The book = the considered assembly of walked pages; chapters are the binding. Primitives is the in: ten walked.md pages seeded, awaiting their walks. The freeze stands -- the apparatus was for this.
- 2026-07-06 09:41 · **build** · R-022 THE COMPOSITOR: tools/compositor.py sets the text into the map -- prose order (first mention across overlay+tutorial+walked.md) vs body order (map encounter), divergences reported as ruling candidates, silences as at-depth. First run on primitives: 80 artifacts, 0 silent; prose teaches trace/drawing/measure EARLY while the maps hold them mid-walk; the maps front-load Point_One context props the prose treats as asides. The book is now literally an instrument that researches the maps.
- 2026-07-06 10:00 · **ruling** · R-023 THE BASELINE CONTRACT -- the missing layer. Selection flipped: not 'which of 752 deserve in?' but 'if this were JUST a plain tutorial on the concept, what would it NEED?' Deck 1 BEATS (roles cast from the concept, not inventory) + Deck 2 VOLTAGE (the critical script, the thinker with QFEP -- Fontana's cut, Klee's walk, Durer's grief). Everything uncast is AT DEPTH. tools/baseline.py audits: a chapter is FINISHED when its baseline is met, not when inventory is exhausted. Primitives: 10 beats + 5 voltage, 49 at depth -> BASELINE MET (10/10 cast&walkable, 5/5 voltage).
- 2026-07-06 11:23 · **build** · R-024 THE COMPOSITION VIEW /composition: the three-split cockpit -- NEED (baseline beats) | POTENTIAL (beats + critical voltage woven at anchors, the Ada version) | ACTUAL (the maps as they walk, tagged beat/understudy/voltage/at-depth). Hover traces one artifact across all three; the gaps ARE the work. Reading face of baseline.py + compositor.py, freeze-legal. primitives: 10 need, 15 potential (5 voltage woven), 49 at depth, baseline MET. ![the three-column composition view for primitives: need, potential, actual](/book-log/composition_primitives.png)
- 2026-07-06 12:11 · **ruling** · R-025 THE TUTORIAL ARC CAST: all 22 baselines authored. arc.json = the NOC-for-Godot-3D backbone (each chapter builds_on prior vocabulary, acyclic spine-ordered); 22 baseline contracts cast against real map inventory via per-sequence fan-out = 189 beats + 96 voltage, all validated in-inventory, 21/22 meet baseline (boolean_surfaces thin). At-depth reserves answer 'too many artifacts': forces keeps 131, waves 63, primitives 49. FINDINGS: (1) 'prove it' exit-test weak across most chapters -- the highest-leverage authoring gap; (2) artifacts live in the wrong sequence (noise flow-fields in forces, softbody springs in forces, halting in foundations); (3) CA + procgen are all-whole-sim, no teaching primitives. Voltage = the critical spine (Calder political balance, RAND frozen chance, Chladni, Physarum cognition, reaction-diffusion=Turing's last work, Konigsberg foreclosure, QFEP hand-spheres, Florensky paraconsistency, archive-not-exit). Selection SOLVED for all 22.
- 2026-07-06 12:12 · **build** · ALL 22 BASELINES CAST — the full NOC-for-Godot-3D spine. /composition picker now shows 22 chapters, 21 baseline-MET (boolean_surfaces thin). 189 beats + 96 voltage. Selection is solved end to end: the book knows what each chapter needs, what it could be, and how the maps actually walk. ![the full 22-chapter composition spine, all baselines met](/book-log/composition_full_spine.png)
- 2026-07-06 12:23 · **note** · ITERATE pass on the 22 baselines: arc.json reconciled to real roles (postfoundationscrisis had drifted); compositor --spine surfaced a curriculum-vs-frame ORDER divergence (the book's telling runs forces/waves/randomness/noise right after change, holding isosurfaces/boolean_surfaces later — a Palle ruling); 29 weak beats sorted into doc/book/GAPS.md as an authoring backlog. TOP RECOMMENDATION: one parametric concept_exit_test artifact closes 11 weak 'prove it' beats at once — the single highest-leverage build.
- 2026-07-06 12:34 · **build** · R-026 KEY ARTIFACTS PROMOTED TO DNA: tools/build_key_artifacts.py defines the book's key set from the 22 baselines (cast beats + voltage) and promotes each to a DNA entry (book-role + code @identity essence + capture). /key-artifacts gallery ranks by book-weight = the improvement worklist. 403 promoted -> 243 KEY (33 hero+voltage / 149 hero / 61 voltage), 394 captured, 293 with DNA, 28 weak flagged amber with improve-> links. 'Which artifacts to improve?' now answered: the key ones, load-bearing first. ![the key-artifacts DNA gallery, ranked by book-weight](/book-log/key_artifacts_gallery.png)
- 2026-07-06 13:05 · **build** · ITERATE — improved 6 key artifacts from the worklist (3 load-bearing + 3 weak), all verified headless, DNA enriched to book-role + safe teaching improvements: player_trace velocity-coloured trace (speed=ink), qfep_formula_3d open dark-spot term, florensky_sphere double-sided A-and-not-A skin, godel self-reference sharpened + halting split off, invariants_demo new PROJECT button (dot product as physical shadow), self_organization_ca tunable lambda across order/edge/chaos. Recaptured -> /key-artifacts refreshed. The worklist drove real improvement.
- 2026-07-06 13:41 · **build** · R-027 ALL TEXT ON BOARDS: bare floating Label3D banned from key artifacts. Enhanced BakedText.make_tag into a framed 2D-in-3D display board (bezel + dark glass face + lit accent edge + baked text) -- one change upgrades every tag. Migrated 6 key artifacts (invariants_demo, godel, box_counting, russell_set, shannon_entropy, gradient_descent), ~35 floating labels -> boards, all verified headless. invariants_demo before/after: floating green glyphs became framed instrument readouts. ~115 key artifacts remain (worklist). ![invariants_demo: floating labels migrated to framed 2D-in-3D display boards](/book-log/text_boards_invariants.png)
- 2026-07-06 13:44 · **note** · text-board polish: make_tag width auto-fit given +margin so glyphs never touch the bezel. Finding: crowded artifacts (shannon_entropy_meter packs ~15 boards) need per-artifact board SPACING/scale, not a helper fix — invariants_demo (spaced boards) renders clean. Board spacing is the next refinement for dense readout artifacts.
- 2026-07-06 13:55 · **build** · TEXT-BOARD WAVE 2: 10 more key artifacts migrated (a: science_screen/florensky/galton/sine/euclid/random_walk; b: qfep_balance 19->1panel+4tags, prng_crank 16->3 LCD screens, transform_composition 8->1panel, explainable_ai 14->0). Two layout laws: consolidate related readouts onto ONE make_text_block panel (overlap impossible); space separate tags past their anchors. Per-frame readouts gated to rebuild on change only. prng_crank_machine now reads as a real instrument. 16 key artifacts on boards, ~105 remain. ![prng_crank_machine: 16 floating labels consolidated into 3 clean LCD screens](/book-log/text_boards_prng.png)
- 2026-07-06 14:34 · **build** · THE COLOPHON: last-changed extended to artifacts AND maps + a site-wide nav. tools/build_recent_changes.py -> recent-changes.json (60 artifacts by .gd mtime + 60 maps by map_data.json mtime + 14 curated pages). Colophon.tsx mounted in the root layout = on EVERY page: a slim bottom-right bar showing the newest change, expanding to a panel with a PAGES nav row + two Recently-Changed columns (artifacts | maps). Regenerate with build_recent_changes.py. Answers 'last changed for artifacts and maps too' + 'a colofon to all pages so we can navigate'.
- 2026-07-06 14:45 · **draft** · THE PIVOT — from instrument to book. Palle corrected: primitives is walked, works, good (not a draft). So 'most important' recalibrated: not walking, WRITING. Developed Point_One/walked.md — the first real considered-critical-tutorial page, drafted from what the map IS (cast + blurb + intent + play), not from a fresh walk. The walk (arrive late -> inherit frame -> make the cut -> see what you refused) + the turn (thrownness as the frame_counter; Fontana's cut as the cost of individuation; the point already political). Awaiting Palle's ruling on VOICE before the other 9 primitives pages follow.
- 2026-07-06 14:59 · **draft** · THE FORM GENERALIZES: Palle confirmed Point_One proves tutorial+critical CAN mix as one woven form (two trajectories, the walk + the turn). Ran the form on two DIFFERENT map characters to test it: Point_Trace (embodied — the hand, Derrida's trace, and the map literally hands you the mystic_writing_pad = Freud/Derrida's own example object) + Primitives_Melencolia (the sequence's melancholy finale — Durer's immobilized angel, completion as mourning, code_display legible AT EXHAUSTION = a foundationscrisis in miniature, forward-rhyming across the curriculum). Written in my context to HOLD the voice (not fanned out). 3/10 primitives pages developed. Voice held across infrastructural, embodied, and heavy-critical maps.
- 2026-07-06 15:06 · **draft** · CHAPTER 1 WRITTEN: all 10 primitives walked.md pages developed in the woven tutorial+critical form, in my context to hold the voice. The arc reads as one argument: Point_One (arrival/thrownness) -> Point_Lines (measure=pleasure+violence, Modulor vs Klee) -> Point_Trace (Derrida's trace, the mystic writing pad) -> Point_Line_Grid (Foucault's quantization, the snap-tug) -> Point_Triangle_Context (rigidity as ideology, Pythagoras filed for later crisis) -> Primitives_Polythedra (volume as junction, becoming_catalyst breaks surface) -> Point_Animatedcube (enclosure as confinement, stability is convention) -> Primitives_Ignorance (Wittgenstein/Chirimuuta, primitive = a move, the 3-resolution spheres) -> Primitives_Portals (Zeno, pi, the uncomputable, the portal that transforms) -> Melencolia (Durer, completion as mourning, build anyway). Forward-rhymes planted to foundationscrisis (Pythagoras, code_display at exhaustion) and the uncomputable. AWAITING Palle's voice ruling across the chapter before the considered pass + LOCK.
- 2026-07-06 15:18 · **ruling** · R-028 LOAD-BEARING MAPS: the baseline's selection logic applied up a level -- from artifacts to MAPS. A map earns a book page if it hosts a beat-cast/alt or voltage; passengers drop to depth (kept in game, not book). Cap 10/sequence, tutorial order, beats weigh double. tools/chapter_maps.py -> chapter_maps.json. 233 maps -> 174 kept (59 to depth) + 22 syntheses = 196 pages. Big prunes: forces 29->10, softbodies 34->10, randomness 13->7 (Palle: its form is tight, confirmed). Book unit = key-map pages (<=10) + one sequence synthesis per chapter.
- 2026-07-06 16:47 · **build** · THE READER /chapter: the developed walked.md pages assembled into a readable book chapter. tools/build_chapter_reader.py -> chapters.json (per sequence, R-028 load-bearing order, all developed pages); /chapter page renders each map as The walk (green) + The turn (fuchsia) with a TOC. Primitives = 10 pages readable straight through. FINDING: the mechanical R-028 criterion dropped Primitives_Portals (hosts no cast beat artifact) but it's narratively load-bearing (Zeno/pi/uncomputable -> Melencolia) -- the reader includes all developed pages regardless; Palle rules in/out. Added 'Read the Book' to the colophon nav.
- 2026-07-06 17:03 · **build** · READER COMMENTS (Google-Docs style on /chapter): select any text -> popover -> comment; POST /api/chapter-comments writes to doc/book/reader_comments.json (the ghost reads it directly). Each comment carries chapter/map/section/quote/text/ts. UI: selection popover (Cmd/Ctrl+Enter to save), a comments side-panel (resolve/reopen), and a per-map count badge in the TOC. Verified end-to-end: browser comment -> file -> read back. This is the voice-ruling surface: Palle comments inline on the primitives chapter, the ghost reads and revises.
- 2026-07-06 17:23 · **draft** · FORM TESTED ON TWO EXTREMES: array_tutorial (dry/foundational — carpet=algorithm, the loom as first computer, scalar->1D->2D->3D->pattern->dance-floor, quantization's imposed structure) + softbodies (physical/biological — the jelly cube at lambda=0.5, softness=capacity-to-be-affected, the radiolarian's frozen affect, Turing's LAST gift the leopard computing its spots = the tender Turing meeting vs foundationscrisis's halting Turing, the actor/receiver boundary dissolving). 17 new walked.md pages (7 arrays + 10 softbodies), held in my context. The woven form landed on both: dry arrays got a felt tutorial + a real critical thread (the loom, imposed structure); heavy softbodies got restraint so the ontology (matter that deforms is matter that expresses) didn't tip into purple. 3 chapters / 27 pages readable at /chapter. array_tutorial reordered to pedagogical order.
- 2026-07-06 19:16 · **draft** · LINEAGE stated (doc/book/LINEAGE.md, in the manuscript front matter): Ada = Learning Processing (Shiffman Ch.1 'Pixels' = coord+point+primitives; minimal runnable progression) + Nature of Code (build-from-primitive spine) + a THIRD critical strand neither has (thinking with Turing), all re-staged flat-VIEWED-screen -> inhabited embodied 3D. The five shifts of 3D algorithm thinking: frame inhabited-not-viewed / atom inverts pixel->Vector3 (discrete<->continuous swap) / loop=presence-not-animation / input=body-not-mouse / reading=a-walk-not-a-glance. Keep Shiffman's restraint, resist spectacle, add body+depth+critique. Wired into build_manuscript.py front matter.
- 2026-07-06 19:20 · **draft** · THE DIALECTIC LIST (tools/heroes.py + doc/book/heroes.json): a HERO ⟷ ANTI-HERO per map, named in conversation, because the pair names the map's DRAMA and drama is structure. HERO = what the walk champions; ANTI = what the turn raises; TENSION = the dialectic in one line. Seeded 27 maps (primitives/arrays/softbodies) from the walked pages: e.g. Point_One = the point ⟷ the frame-counter (individuation vs thrownness); Point_Lines = Klee's walking line ⟷ Le Corbusier's Modulor; softbody deformation = the jelly cube ⟷ the rigid solid it refutes. Palle rules add/delete/rename; the ghost edits. This is the book's dramatic spine.
- 2026-07-06 19:45 · **build** · HEROES WEB EDITOR /heroes: add/remove/edit the dialectic list in the browser. Inline-editable hero/anti/tension per map (blur to save), + add-a-map form, remove button. POST/DELETE /api/heroes -> doc/book/heroes.json (getRepoPath) -- THE SAME FILE tools/heroes.py and the ghost edit. Verified: browser POST persisted, read back via heroes.py. Added to colophon nav. Now the dialectic list is editable three ways: chat (ghost runs heroes.py), CLI, and web.
- 2026-07-06 21:58 · **build** · HEROES TIMELINE /heroes-timeline: the dialectic list as a timeline mind map. The spine runs top-to-bottom (reading order = the book's time), a blue node per map with its tension; HEROES branch LEFT (green), ANTI-HEROES branch RIGHT (fuchsia). Add nodes on either side (dashed '+ hero'/'+ anti-hero' on empty sides), '+ map' extends the timeline, inline edit + remove. Shares doc/book/heroes.json with /heroes and the chat (tools/heroes.py). Verified rendered: primitives + arrays + softbodies, 27 maps. In the colophon nav.
- 2026-07-06 22:51 · **build** · HEROES TIMELINE reordering: move a map pair up/down (▲▼ buttons) or DRAG the spine node; within its sequence. PUT /api/heroes {seq, order[]} rewrites heroes.json in the new order (shared file). Verified: PUT roundtrip moved a pair + read back. The dialectic timeline is now reorderable — the tutorial order of a chapter is editable by hand.
- 2026-07-06 22:53 · **build** · HEROES TIMELINE — all 22 spine sequences added (148 maps auto-seeded from the baselines: hero = the teaching beat a map hosts, anti = the chapter's critical voltage). The 3 developed chapters (primitives/arrays/softbodies) keep their CONSIDERED hand-written pairs = the quality bar; the other 19 are FIRST-DRAFT stubs to edit. Limits visible + honest: maps hosting no beat-cast get an empty hero (the mechanical criterion again, Portals-class); maps without their own voltage reuse the chapter's lead voltage (repeats). These gaps are exactly where Palle's editing adds the dialectic. Full book spine now walkable + editable at /heroes-timeline.
- 2026-07-07 08:28 · **note** · BLOG for session handoff: '2026-07-07-the-book-starts-writing-itself' — explains the recent arc for the next session: selection flipped (baseline contract R-023), the reading instruments (/composition, /key-artifacts, chapter_maps R-028), THE CORRECTION (primitives is walked+works -> stop building, start WRITING), the woven walk+turn form proven on 3 extremes (primitives/arrays/softbodies), the lineage (Learning Processing + NOC + the critical strand, in 3D), and the dialectic timeline whose distinct-anti ratio is the honest progress meter (written chapters 10/10, auto chapters 1/7). Handoff: instrument done, 3/22 written, the critical trajectory is the whole remaining book. NEXT = write transformation (thinnest at 3/7).
- 2026-07-07 09:40 · **draft** · FRACTALS CHAPTER WRITTEN + softbodies base tutorial sharpened. Wrote all 10 fractals walked.md pages in the woven walk+turn form (chapter_maps order: Recursion → RecursiveTrees → Cantor → Koch/Sierpinski → Menger → GoldenSpiral → Julia → Mandelbrot → Synthesis → CrossSequence), held in my context to hold the voice. The arc: recursion estranges the familiar (the recursive chair as a process caught mid-run, the vanished author) → the stochastic forest (repetition produces difference, not sameness) → Cantor (existence by subtraction, richer than the line it was carved from; the first census-refusing number 0.631) → Koch/box-counting (the monsters were the population, smoothness the lab artifact) → Menger (all boundary no interior, universal because of it) → romanesco (the most irrational number is the biological optimum — the defect is the resource) → Julia (complexity FOUND not built, the fractal IS the boundary) → Mandelbrot (the edge is dimension 2 — the margin IS the substance, computationally irreducible, the forward-rhyme to foundationscrisis/halting) → Synthesis (D=log(N)/log(S) as the one law; queer geometry as description) → CrossSequence (Rule 90 IS Sierpiński — the fractal was a fact about computation, edge of chaos = where computation lives, circular close). Rewrote fractals heroes.json: 5/10 → 10/10 distinct antis, 4 blank heroes → 0. Softbodies: the base tutorial beat (jelly_cube spring) now names Hooke's law force=stiffness×displacement + how displacement travels the lattice; Cloth_Physics walk now names the damping beat (velocity-drag = why cloth falls still) that was cast there but unnamed. Rebuilt chapter reader → /chapter now 4 chapters / 37 pages (fractals 10 with walk+turn). Distinct-anti meter: written chapters now 10/10, 7/7, 10/10, 10/10. Book: 4/22 chapters written (was 3/22). AWAITING Palle's voice ruling on the fractals chapter before the considered pass + LOCK.
- 2026-07-07 10:35 · **draft** · BACK-OF-SPINE PASS BEGUN (Palle: "start from the spine sequence back, improve all base tutorials"). Worklist = reverse spine order; two chapters written this session, held in-context for voice. POSTFOUNDATIONSCRISIS (8 pages): bias (bias is incompleteness's social form — the system is consistent BECAUSE it chose what to exclude; you audit from inside) → applied ethics (ethics is the shape of the room; the calibration slider has no neutral position) → paraconsistent (give up explosion instead of giving up the world; Florensky wired into production, git/CAP already live there) → situated (Haraway's god-trick; objectivity = the careful accounting of the perspective you're in) → collective (consistent vs CORRIGIBLE — the commons pools incompleteness; Gödel's shadow redistributed) → rhizome (never needed a hierarchy — stronger than rebellion) → lab (Melencolia's angel STANDING UP — build the molecule that holds on the floor that doesn't; book-scale rhyme to primitives' close) → synthesis (the index instead of the monument; the edge is not the limit, the edge is the ground). QFEPLABORATORY (8 pages): introduction (recognized not learned; hold the formula ready to put it down) → F (the dark room — F's optimum is death, the puzzles make you complicit) → E (entropy is freedom but dissolution is freedom's loss — choice requires a chooser) → λ spectrum (position IS the parameter; the curriculum re-walked in forty meters; the edge findable by feel) → φ (the political term — a stance toward change, not an amount; both honesty clauses held) → edge (three literatures one address; the locked slider as pedagogy AND concession) → sandbox (understanding as capacity; YOUR edge — the definite article dissolves) → synthesis (a lens not a truth; queer_morphology_specimen = identity-at-the-edge made flesh). Heroes: both chapters 8/8 distinct antis, 0 blanks. Reader: /chapter now 6 chapters / 53 pages. Book: 6/22 written. WHERE TO READ: /chapter (walked pages), /composition (the beat contracts), /heroes-timeline (the meter). NEXT back-of-spine: foundationscrisis → graphtheory → machinelearning. AWAITING voice rulings on fractals + postcrisis + qfep before considered pass + LOCK.
- 2026-07-07 11:20 · **build** · ARTIFACT ORDER FROM THE OLD TUTORIAL + /tutorial 500 FIXED. Q: can the old tutorial establish an artifact order for the spine maps' artifacts? A: yes — its walk pages were already the three-orders CONSENSUS order (ped+onto+crit)/3, but capped at ~11; the full uncapped order lives in three-orders.json (e.g. primitives = 73 pearls). New tool tools/build_artifact_order.py projects the full consensus necklace onto each kept map's cast → doc/book/artifact_order.json + encyclopedia public/artifact-order.json: 22 sequences, 174 maps, 752/915 artifacts ranked (presets rank as their base; #disabled cells skipped; unranked props/anchors trail in cast order). Use it for: walked.md cast-line order, /chapter cast rendering, encounter order for placement (walk_evaluator), heroes hero-picking. FIX: /tutorial/<seq> 500 ("Jest worker encountered 2 child process exceptions") = the dev server had bloated to 1.5GB and its compile workers were dying; killed PID, cleared .next, restarted via launch.json — all 22 /tutorial/<seq> routes 200, reader verified rendering (primitives page 1/8).
- 2026-07-07 12:10 · **build** · ORDER INSTRUMENTS: construction-edge miner + handoff scorer (the two mineable halves of "is there a better order than the consensus mean"). tools/mine_construction_edges.py: 1921 artifacts mined from .gd; FINDING — artifact→artifact edges are rare (155; depth histogram 1837/76/8) because artifacts are self-contained BY DESIGN, so construction lives at the ATOM level; added the atom LADDER (point→line→surface→volume→aggregate→simulated, grade = highest rung + composition) → grade spread 0-8. Construction-vs-consensus spearman per chapter SPLITS THE BOOK: construction-shaped chapters (isosurfaces .71, procgen .62, machinelearning .62, qfep .49 — teach by building up) vs drama-shaped chapters that INVERT construction (softbodies −.66, postfoundationscrisis −.69, swarm −.44 — open on the complex object on purpose). Divergence = content, as predicted. tools/handoff_score.py v2: v1 lexical FAILED validation honestly (stubs beat written by text repetition); v2 = LSA embeddings (Atlas recipe, artifact-cards corpus) + LIFT over K=50 within-sequence shuffles (repetition cancels; only ORDER survives). VALIDATES: written lift +0.023 vs stub +0.009, all 6 written chapters positive. 9 stub chapters have NEGATIVE lift (swarm −.15, ML −.11, noise −.08, foundationscrisis −.06, wavefunctions −.05, change, isosurfaces, color ≈ 0) = their map order is worse than shuffle under concept-continuity → reorder on /heroes-timeline BEFORE writing them. Caveat noted: metric rewards continuity; postcrisis (−.02, written) pivots thinkers deliberately — one lens, not a judge. Outputs: doc/book/construction_edges.json + handoff_scores.json (+ web copies).
- 2026-07-07 12:50 · **build** · MINER v2 — THE ENGINE AS AUTHORITY: tools/dump_godot_class_tree.gd (headless, watchdog-wrapped, 7s) dumps ClassDB's real inheritance map → commons/data/godot_class_tree.json (1040 classes). mine_construction_edges.py now walks each artifact's REAL extends chain (script hops → engine base) and assigns a DOCS-ORDER STAGE (Godot tutorial TOC as ancestor map: nodes→scripting→2d→3d→animation/ui→physics/particles→soft_body/shaders/viewports→low-level; first-matching-ancestor, specific before general). Per artifact: engine{base, engine_depth, script_hops, stage, classes_used}. Verified: jelly_cube→stage 6 soft_body, boids_aquarium→stage 3 3d+MultiMesh. THREE construction lenses now (atom grade / artifact depth / engine stage) and they DISAGREE INFORMATIVELY: transformation consensus tracks the engine (+0.52) but not atoms (+0.13); change tracks atoms (+0.33) but INVERTS the engine (−0.58); machinelearning tops both (engine +0.73). foundationscrisis order follows engine complexity (+0.52). Question-artifacts remain protected by beats, not scored (R: questions are content-shaped holes — all similarity/complexity metrics downgrade them structurally). Also answered: external orders = Euclid/NOC/NKS/Godot-docs; WolframAlpha is query-shaped, not order-shaped (MathWorld would be the minable Wolfram asset).
- 2026-07-07 13:30 · **build** · THE MANUAL TRAJECTORY, ALL 22 + COMPARATOR. doc/book/manual_order.json = the ghost's editorial sort for every spine sequence (authored by reading beats+voltage, not scoring): beats as spine, voltage placed for drama (hook/peak/close), deliberate inversions (Dürer LAST in primitives, Foucault pendulum as early hook, Rothko as affect close, formula assembled only AFTER its terms are felt, FlowField as late reduction, mold_network late as bridge-to-swarm). tools/compare_orders.py correlates it per-seq against 6 lenses (consensus/ped/onto/crit/engine/atoms) → doc/book/order_comparison.json. FINDINGS: (1) the manual sort is nearest PEDAGOGY (mean +0.36, closest lens in 9/21) but far from identical — the editor = pedagogy PLUS drama; (2) chapters have different ordering NATURES and the editor switches backbone per chapter: primitives rides the ENGINE (+0.90 — point→line→plane→solid literally climbs Godot's class machinery), noise rides ontology/atoms (+0.85/+0.84 — a construction chapter), ML/graphtheory/CA ride criticality, postcrisis rides pedagogy exactly (+1.0, derived from the written chapter); (3) COLOR DIVERGES HARD (ped −0.90): the manual order pushes all four artists late in ascending stakes (Albers proves→Mondrian legislates→Kandinsky decrees→Rothko grieves) while the ped axis scores them early — needs Palle's ruling, either the axis or the editor is wrong about color; (4) no lens reproduces the editorial residue anywhere — mean rho ≤0.36 across the board = the craft is real and unminable. manual_order.json is hand-editable; re-run compare_orders.py after ruling.
- 2026-07-07 13:55 · **build** · /order-mixer: ALL SEVEN ORDERS ON ONE PAGE, WEIGHTED LIVE. tools/build_order_mixer.py merges manual/consensus/pedagogy/ontology/criticality/engine/atoms into public/order-mixer.json (22 seqs, 249 artifacts, normalized 0..1 positions; nulls stay null). Page src/app/order-mixer/page.tsx: sequence pills, a weight slider per lens, presets (manual only / three orders / editor+build), and the BLENDED trajectory recomputed live — each row shows rank, blend score, and a dot-track of where every lens places that artifact (dim = weight 0). Missing lens values renormalize per artifact. Verified interactively: primitives under manual = arrival→…→Dürer last; one click to "three orders" reorders completely (klee first, becoming_catalyst 13th→2nd) — the drama-vs-score gap visible in one gesture. Edit doc/book/manual_order.json + rerun build_order_mixer.py to re-rule the manual lens.
- 2026-07-07 14:15 · **build** · FITTED WEIGHTS — reverse-engineering the editor. tools/fit_order_weights.py grid-searches the 5-lens simplex (ped/onto/crit/engine/atoms; consensus excluded as itself-a-blend) for the vector that best reproduces the MANUAL order of the 6 written chapters → doc/book/order_weights.json. GLOBAL ANSWER: the editor ≈ pedagogy 1.0 + criticality 0.25 (mean rho +0.671). Transfer varies by chapter nature: postcrisis .98, softbodies .95, fractals .76, primitives .73, BUT qfep .44 and array_tutorial .17 — the chapters with the most editorial residue. PER-CHAPTER best fits confirm the natures: primitives=engine-alone (.90), arrays=atoms-heavy (best only .57 — the loom placement is unminable), softbodies/postcrisis=pedagogy (.95/1.0), fractals=ped+onto+engine (.87), qfep=ped+engine (.53 — formula-after-terms is an editorial move no lens makes). "fitted" preset added to /order-mixer (verified live: sliders snap to ped=1/crit=.25, order re-blends). DOCTRINE: use fitted as the STARTING blend for unwritten chapters, per-chapter override where the fit says the nature differs, and treat (1−rho) as the honest measure of what weights can never buy — the craft.
- 2026-07-07 14:30 · **note** · BLOG: '2026-07-07-seven-orders-one-chapter' — the ordering investigation as one story: the mean was eating the drama → mine what can be mined (construction atoms, the engine's own syllabus via ClassDB, handoff LIFT after the lexical metric failed honestly) → questions are content-shaped holes all metrics downgrade (rule, don't score) → the hand demonstrated on swarm (simplicity-late is a rhetorical act no metric makes) → manual trajectory ×22 → chapters have NATURES (comparator) → /order-mixer (seven orders, weights live) → fitted weights: the editor = pedagogy 1.0 + criticality 0.25 = the woven form as a number; (1−rho) = the craft coefficient. Scripts never author the order; they tell you which chapter is lying and how much of the telling was yours.
- 2026-07-07 15:10 · **build** · TUTORIAL LAYER COMPLETE + THE TUTORIAL WALL. (1) Wrote the 27 missing tutorial.md (change ×8: numerical derivative→FTC-in-two-loops; isosurfaces ×10: density→lookup table→SDF algebra, the whole marching arc; boolean ×5: union/intersect/subtract as grid-word + min/max SDF + CSG node, De Morgan at the workbench, architecture as mass-minus-voids; color ×2: four pattern machines as functions on (x,y), simultaneous contrast as a computable perceived(); forces ×2: force zones as edited physics, force-as-place with ghost-arc prediction) — spine coverage now 174/174. Voice matched to existing tutorials: terse claims, runnable Godot-4 GDScript, a Try: close. (2) NEW ARTIFACT tutorial_wall (commons/artifacts/tutorial_wall/, registered in station.json): the wall-hangar principal of the tutorial layer — reads a map's OWN tutorial.md at build time and hangs TITLE / THE IDEA / THE CODE (dark board, case preserved) / TRY (accent) as framed 2D-in-3D boards on a station wall, and mounts the map's real cast artifacts on daises in front (registry-resolved, AABB-fitted, name-tagged, denylist for props). Config {map: <MapName>, example_count, width_cells} re-skins it to ANY map. Verified by headless capture (watchdog, 12s): Change_Intro wall renders title+claim+live code+try+2 tangent demos; one layout fix (TRY anchored under IDEA dynamically) re-captured clean. One artifact, 174 possible walls.
- 2026-07-07 16:20 · **build** · WALLS GALLERY + KEY-ARTIFACTS UPGRADE (R-027 wave 3 begins). (1) /tutorial-walls: commons/testing/capture_tutorial_walls.gd batch-captured ALL 216 tutorial walls in ONE 52s headless run (one tutorial_wall instance re-skinned per map via apply_grid_config, boards-only for headless safety) → public/tutorial-walls/ + manifest grouped by spine sequence (174 spine + 42 beyond); gallery page with lightbox, verified (216 imgs render). (2) /key-artifacts improvements: build_key_artifacts.py now mines label3d_count per artifact → labels field = the R-027 board-migration worklist made VISIBLE (80 key artifacts still on Label3D; top: neural_networks_vr 16, bias_visualizer 12, pattern_maker_station 10); page gets a "still on Label3D" filter + "Label3D count" sort + rose badge per card + summary line + tutorial-walls link. (3) WAVE-3 MIGRATION STARTED: bias_visualizer 12→0 labels — title+equation consolidated onto ONE framed dark header board (rebuild-on-change gated via _header_cache), explanation onto a front reading board (2-line wrap), word-cloud labels → BakedText.make_tag framed plates (visibility-toggled per analogy), ♂/♀ axes → framed tags; capture verified: reads as an instrument, not floating glyphs. Worklist now 79. (4) DNA promotion status: 73 of 243 KEY artifacts still lack @identity essence — that's considered writing (the chamber/improver loop), flagged as the next promotion pass, NOT auto-generated.
- 2026-07-07 17:40 · **build** · WAVE-3 LANDED (session-limit recovery) + THE DNA QUESTION. Migration: 10 artifacts off Label3D this wave — neural_networks_vr 16→0 (per-neuron numbers REMOVED, emission is the readout; metrics gated via _update_metrics_board; info panels → framed boards with accent bars; capture-verified) + bias_visualizer + agent batch (timbre_sculptor, shannon_workbench, grammar_provenance, pulsar_visualizer, self_organizing_patterns, pattern_maker_station, chord_tension_spring, ContextFreeGrammars — agents died at session limit but 8/12 files were COMPLETE; parse-checked all 10 touched scripts in one headless run, zero failures). Worklist 79→70; next: bifurcation_walkway, fractal_lsystem_string, grid_model, matrix_4x4_viewer (all 8 labels). Wall layout fix (two clean columns, dark code board no longer overlaps light column) re-captured ×216 → gallery refreshed. DNA CENSUS for Palle's question: 44 promoted dna-family artifacts (mill 13 + loom 12 = WALLPAPER GROUPS p6m/p4m/p3/p4g...; color stacks 5 + modern art 4 + furniture 4 = the color chapter's voltage, correctly homed in Color_Context_Placed which casts 18; + primitive_stack/mesh_grammar/facade/sprout/specimen/workstation) + the unpromoted galleries (critter 121, fungus 121, tree 121, props 554 = parametric sweeps, staging not teaching). FINDING: loom/mill = 25 artifacts teaching THE 17 PLANE SYMMETRY GROUPS and NO spine chapter teaches groups — patterngeneration.json exists as an EMPTY stub (the intent existed). The one honest new-sequence candidate; R-023 next move = draft its baseline contract and see if the beats cast from disk (they appear to: motif→translate→reflect→rotate→glide→closure→the 17→pattern_maker→prove).
- 2026-07-07 18:30 · **build** · SEQUENCE #23 ASSEMBLED: SYMMETRY (the seventeen wallpaper groups). From empty stub to baseline-MET in one pass, concept-first per R-023: doc/book/baselines/symmetry.json (9 beats: motif→translate→reflect→rotate→glide→CLOSURE→the seventeen→build→prove; 5 voltage: the Alhambra census-that-CLOSES as the counter-rhyme to incompleteness, Escher who taught the mathematicians to see, the Pompeii meander, 10 PRINT as loom, Memphis-vs-Bauhaus same-group-opposed-ideologies) + commons/maps/sequences/symmetry.json + SIX MAPS (Symmetry_Motif/Translation/Mirror_Rotor/Glide/Group/Seventeen, 12×10, 5-phase, all 6 pathfinder OK) + blurbs. VERDICT: beats 9/9 cast & walkable · voltage 5/5 present · FINISHED (baseline met) — the entire cast already existed on disk (loom/mill/pattern families built before their chapter). Proof capture: Symmetry_Seventeen renders with wall_pattern_gallery painting wallpaper-group shaders across the walls (needs placement curation — patterns dominate; run place.py + gaze_ride next). NOT yet on the spine: inserting between transformation and array_tutorial (22→23) touches curriculum_spine.json = Palle's ruling. Pipeline HEAD: stage 2 docs (intent/tutorial.md per map) → placement polish → VR walk.
- 2026-07-07 19:00 · **ruling** · R-029 (Palle): SYMMETRY JOINS THE SPINE — 22→23. Inserted at position 3 (transformation → SYMMETRY → array_tutorial: transforms → GROUPS → grid). Propagated: curriculum_spine.json (orders renumbered), SPINE constants in build_key_artifacts.py + chapter_maps.py, chapter_maps.json regenerated (239 maps → 180 kept + 23 syntheses = 203 book pages), key-artifacts rebuilt (243→253 key — the looms and mills are now KEY artifacts with book-roles), heroes.json seeded with 6 CONSIDERED pairs (6/6 distinct antis — the census-that-closes vs the book's open censuses is the chapter's crowning tension), manual_order.json + artifact_order + order-mixer rebuilt (23 seqs everywhere). /heroes-timeline verified: 23 sequences, symmetry on the timeline. Dev server died mid-propagation (second time today) — restarted via launcher. REMAINING for the new chapter (pipeline head): intent/tutorial.md per map → placement polish (wall_pattern_gallery dominates Symmetry_Seventeen) → captures → VR walk → walked.md pages (the chapter enters the writing worklist at position 3, EARLY — it will be written soon after transformation).
- 2026-07-07 19:25 · **note** · BLOG for handoff: '2026-07-07-the-inventory-asked-for-a-chapter' — the day's second arc: tutorial layer 174/174 + tutorial_wall (the tutorial gets a BODY, 216 walls) + R-027 wave 3 (worklist mined, 10 migrated, 70 remain) → the DNA census question → 25 loom/mill machines teaching the 17 wallpaper groups with NO chapter + the empty patterngeneration stub → R-023 assemblage: contract → beats 9/9 cast FROM DISK → 6 maps → baseline MET → R-029 spine 22→23 at position 3. Lesson: the game already knows things the book hasn't said yet; the census just has to be taken. NEXT (in pull order): 1) write chapters (6/23; back-of-spine resumes foundationscrisis→graphtheory→ML; symmetry early), 2) symmetry pipeline (intent/tutorial.md, placement pass on Seventeen, captures, VR walk, the seventeen-wall artifact), 3) R-027 finish (4× 8-label stragglers first), 4) DNA essence pass (73 key artifacts, chamber loop), 5) voice rulings (fractals/qfep/postcrisis at /chapter + the color order conflict).
- 2026-07-07 20:10 · **build** · THE GAME'S BASELINE (R-023 one level up: "if Ada were JUST a game, what would it minimally need?"). doc/book/baselines/thegame.json — beats are SYSTEMS, audit = EXISTS/DEPLOYED/WIRED via tools/game_baseline.py. THESIS stated: the world keeps trying to freeze or dissolve (the two deaths, pure order and pure chaos) and the player learns, chapter by chapter, to hold it at the edge where things live; every algorithm a way of holding; FOE→FRIEND (nothing destroyed, everything re-tuned); the score = the ecology's visible aliveness; completion forbidden. VERDICT 6/9 MET: bracelet (33 maps, persists) + death (autoload, but only 4 maps carry hazards — underused) + foes (17 maps, friend-ladder wired) + ecology (reads completed sequences) + the unwinnable level (provability_sorter) + lab progression (MapProgressionManager unlocks) all REAL. THE THREE GAPS, precisely: (1) prove-it beats exist in 21/23 chapters but NO WIRE connects any puzzle to MapProgressionManager — the exams exist but are not gates; (2) qfep_reactor is walkable but nothing measures 'held the edge' — the final test has no clock and no ending; (3) THE MISSING BEAT: the world-verb — no crystallize/dissolve zones, no bracelet retune; DangerZone has five harms but not the two THESIS deaths. The game = 3 wires away: gate the exams, clock the reactor, build commons/hazards/edge_zones/. Like symmetry, like the tutorials: mostly cast, nothing connected.
- 2026-07-07 21:00 · **census** · "WHAT PRINCIPLE DOES ADA NOT COVER?" → the VARIATIONAL PRINCIPLE (form as the minimum of a functional; nature is lazy; the shape is what minimizes). The one MODE of form-generation with no chapter (rule/force/chance/constraint/selection/field/composition all have chapters; MINIMIZATION does not). Ran the R-023 census: doc/book/baselines/formfinding.json (9 beats: why-this-shape → the gradient → the catenary → the minimal surface → relaxation → equilibrium → escape-the-local-minimum/annealing → find-a-form → prove; 4 voltage: D'Arcy Thompson 'form is a fossil of forces', Gaudi/Frei Otto invert-the-chain, tensegrity as prestress, max_q_basin_room = QFEP F-minimization + dark-room return 'stop short of the minimum to stay alive'). DISK PRESENCE: 13/13 cast+voltage exist, ALL 17 alternates too — even richer than symmetry. There is a whole hidden 'descent toward aliveness' family (self_shaping_forge, max_q_basin_room, energy_landscape_bench, gradient_descent_optimizer/well/landscape ×5, catenoid/helicoid/dini/enneper minimal-surface ×5+, cable_builder, mass_spring_bench, verlet_workbench, tensegrity_triangle, calder_mobile). Proposed spine position: AFTER forces, BEFORE wavefunctions (forces move things → form-finding is where they REST). Not yet assembled (no maps/sequence json) — census only. Runners-up genuine gaps (scattered, no chapter, would need NEW artifacts): fluid/turbulence, packing/Voronoi. Deepest structural gap (uncloseable): the CONTINUUM itself — Ada is a discretizing engine, every form is voxel/particle/cell/node. NEXT if Palle rules: assemble formfinding (build 6-9 maps like symmetry) → likely baseline-MET on first pass.
- 2026-07-07 21:40 · **ruling** · R-030 (Palle: "word"): SEQUENCE #24 ASSEMBLED + ON SPINE — FORMFINDING (the variational principle: form as the minimum of a functional). From census to baseline-MET to spine in one pass. doc/book/baselines/formfinding.json (9 beats: why-this-shape → gradient → catenary → minimal surface → relaxation → equilibrium → annealing/escape → find → prove; 4 voltage: D'Arcy Thompson 'form is a fossil of forces', Gaudi inverts the chain, tensegrity as prestress, max_q the QFEP+dark-room 'stop short of the bottom to stay alive') + commons/maps/sequences/formfinding.json + SIX MAPS (FormFinding_Descent/Catenary/Relaxation/Equilibrium/Annealing/Forge, all 6 pathfinder OK) + blurbs. VERDICT: beats 9/9 · voltage 4/4 · FINISHED — 13/13 cast existed on disk (the hidden 'descent toward aliveness' family). Inserted at spine position 8 (after forces, before wavefunctions: forces move → form-finding rests). Propagated: curriculum_spine.json (24 seqs, renumbered), SPINE constants in build_key_artifacts.py + chapter_maps.py, chapter_maps regenerated (245 maps → 186 kept + 24 syntheses = 210 pages), key-artifacts (253→264 key: the gradient-descent/minimal-surface/energy-landscape families now KEY), heroes.json 6 considered pairs (6/6 antis, the alive-form-vs-dead-perfect-crystal is the crown), manual_order + artifact_order + order-mixer all at 24. Proof capture FormFinding_Equilibrium: Calder mobile (τ=w·d, balanced) + fractal truss (max strength min material) + tensegrity all render. TWO chapters born today from the census method (symmetry #23, formfinding #24) — both were waiting on disk. NEXT: intent/tutorial.md per map, placement polish, walked.md pages (both new chapters early on the spine).
- 2026-07-07 22:20 · **draft** · WALKED.MD FOR BOTH NEW CHAPTERS — symmetry (6) + formfinding (6), woven walk+turn, held in-context for voice. SYMMETRY arc: motif (a pattern is a RULE not a picture; the meander ran it before the theorem) → translation (one coin-flip inside the machine turns repetition into cloth) → mirror/rotor (symmetry gives freely and forbids absolutely — 2,3,4,6 never 5, the crystallographic restriction felt) → glide (the symmetry that hides; Escher the outsider taught the mathematicians to see) → group (closure = the set contains its own consequences; the group is INNOCENT, the palette is the politics — Memphis vs Bauhaus) → seventeen (the one census in the book that CLOSES; the Alhambra completed it by hand before the theorem — the counter-rhyme to every open census). FORMFINDING arc: descent (the marble is a solved minimization with no placer) → catenary (least-cost shape computed not drawn; the chain solved it before Galileo) → relaxation (a form is a fossil of the forces that made it — D'Arcy Thompson in glass) → equilibrium (structure is a field of forces not a stack of stuff; Gaudi inverted hung chains) → annealing (you must get worse to get better AND the deepest minimum is DEAD — life stops short of the bottom, QFEP's F/dark-room in landscape form) → forge (you do not carve the form, you let it fall to it; proof is stillness). Reader rebuilt: /chapter now 8 chapters / 65 pages. Distinct-anti meter: all 8 written chapters at FULL (6/6, 6/6, 7/7, 6/6, 10/10, 10/10, 8/8, 8/8). BOOK: 8/24 chapters written — the two census-born chapters landed complete. Await Palle voice ruling before considered pass + LOCK.
- 2026-07-07 22:45 · **note** · BLOG 3/3 today: '2026-07-07-two-chapters-were-already-there' — the census arc closed. How the R-023 concept-first method found TWO whole sequences already on disk in one evening: symmetry (#23, the census that CLOSES — 17 wallpaper groups, the Alhambra completed by hand before the theorem, the counter-rhyme to every open census) + formfinding (#24, the variational principle — form as what minimizes, 13/13 cast on disk, the alive-form-stops-short-of-the-dead-perfect-bottom = QFEP's F/dark-room in landscape form). Both baseline-MET first pass, both on the spine, both written to full-melody walked.md (8/24 chapters now). THE LESSON: the game knows things the book hasn't said yet — the wallpaper looms built before the 17 were named, the energy-landscape artifacts before 'variational principle' was said; the census is going back to take the meaning. Sharpens 'what does Ada not cover' → not coverage but ASSEMBLY; genuine gaps down to 2 (fluid/turbulence, packing/Voronoi) + the uncloseable continuum. Three blogs today: seven-orders (ordering), inventory-asked (symmetry born), two-chapters (both census chapters written).
- 2026-07-07 23:30 · **build** · EDGE ZONES — the game's missing thesis-mechanic, WIRED. commons/hazards/edge_zones/edge_zone.gd (class_name EdgeZone, extends Area3D): a zone dying at one of the TWO DEATHS — CRYSTAL (pure order, lambda=0, cold blue cubic lattice) or DISSOLVE (pure chaos, lambda=1, scattering magenta particles) — that the player RETUNES toward the living edge (lambda~0.4, green) by HOLDING it (standing in the outer ring); leave and it drifts back to its death; harm ticks while near either extreme; hold it alive 3s and it STABILIZES (life mote appears). The λ-thesis made touchable: both extremes are death, life is the edge held against its own drift. Catalyst bodies retune faster (bracelet hook via retune(amount) API + is_in_group('catalyst')). Additive: NO grid-core change — registered as two artifacts (edge_zone_crystal/dissolve in hazards.json), placed in interactables like any artifact. Demo map EdgeZones_Demo (crystal left, dissolve right, spawn between). Verified: parse OK, both scenes instantiate, capture shows blue crystal lattice + magenta dissolve each over its lambda-coloured core. GAME BASELINE 6/9 → 7/9 (world-verb now EXISTS+DEPLOYED+WIRED). REMAINING 2 gaps: (1) gate the prove-it puzzles to MapProgressionManager (exams aren't gates), (2) clock the reactor (final test has no ending). NEXT: place edge zones in real curriculum maps (foundationscrisis wants them — the crystal is F's dark room, the dissolve is E's dissolution); bracelet trigger→retune wiring for faster/aimed retune; a stabilize→spawn-flower reward.
- 2026-07-08 00:15 · **build** · GATE THE EXAMS — game wire #2 WIRED. commons/managers/ExamGate.gd (new autoload, registered in project.godot after MapProgressionManager): the ONE place the prove-it→progression wire lives. Watches get_tree().node_added for ANY node carrying a known solve-signal (puzzle_solved [SnapPointPuzzleBase], transformation_complete [balance_puzzle], + canonical exam_passed/puzzle_completed/target_completed/proof_complete) and connects it to MapProgressionManager.complete_map(GameManager.get_current_map()), once per map. ZERO puzzle files touched — heterogeneous prove-it family gated centrally; new puzzles gated free if they emit any known signal (canonical: emit exam_passed). Autoload access via /root/ node lookup (not the global identifier — the DangerZone pattern; the global-identifier form failed to compile in isolated --script contexts). VERIFIED headless (result-file, non-console exe swallows stdout): fake puzzle emits puzzle_solved → 'TestExamMap' appears in completed_maps → complete_map fires (XP + unlocks + sequence-check all cascade for free). GAME BASELINE 7/9 → now the prove-it beat is MET; snap_tetrahedron_puzzle/balance_puzzle/primitive_combo across ~21 chapters' prove-it maps are now real gates. REMAINING 1 gap: clock the reactor (qfep_reactor final test has no 'held the edge' measure / ending). NEXT: clock the reactor → 9/9; then place edge zones in curriculum maps + a stabilize→spawn-flower reward.
- 2026-07-08 01:00 · **ruling-adjacent** · THE LARGER WE — blog 4 ('2026-07-08-the-larger-we') + A STANDING FOURTH QUESTION adopted as research direction (not yet doctrine — Palle rules if it enters the sieve): "Whose knowledge is this pressed from — and does the walk return it, or enclose it?" Context: Palle shared RESIDUE Book One (the sibling project — novella by 'K. Halberg, two hands braided: a writer and a model'; five service humanoids keep protocols running after the humans are gone; ends at its first 'we'; rights schedule passes the text to the commons on Book Two = de-enclosure written INTO the work). The braid = the GENERAL INTELLECT (Marx, Grundrisse) made literal — the model as 'a captured density of the human record'. THE FINDING, evidenced by this week's censuses: what we find was already ours — chapters already on disk, the seventeen already in the Alhambra, the catenary already in the chain; the model is the record's LENS not its author; the commons is where the work CAME FROM, enclosure is the anomaly. CONCRETE NEXT STEPS the question implies: (1) a COMMONS LEDGER beside the dig ledger — per-artifact provenance as data (which crafts/communities each algorithm is pressed from), buildable from @identity lineage + voltage attributions; (2) a rights schedule for Ada's own book/text, decided by Palle, written into the work; (3) read the endgame chapters (wiki_fragment, collective knowledge) as the project's own answer already in place. Residue↔Ada rhymes recorded: residue=form-as-fossil-of-forces (formfinding voltage), Day 412=the frame counter/thrownness, five-desks-five-units=the order-mixer, 'continuation not cause'=the edge-zone mechanic, Mira's registry line=the memory files.
- 2026-07-08 02:00 · **build** · THE COMMONS LEDGER — the standing fourth question made into data, beside the dig ledger. doc/book/commons_sources.json = authored taxonomy of 57 sources by KIND (named / collective-unnamed-makers / mathematical-commons / nature / institutional) and RETURN (credited / commons / anonymous). tools/build_commons_ledger.py matches every artifact (registry desc+name + @identity essence/lineage + baseline VOLTAGE prose) against the sources → doc/book/commons_ledger.json + web. /commons-ledger page: the RETURN LINE bar + per-source expanders + an 'anonymous debt' filter. RESULT: 629/2578 artifacts attributed to 57 sources. THE RETURN LINE: credited 559 · commons 98 · ANONYMOUS 193. THE DEBT SURFACED (form used, makers unnamed): textile weavers 80 [largely women's labor], Islamic/Alhambra artisans 42, Pompeii mosaicists 39, vernacular folk pattern 20, Persian weavers 9, tatami makers 3. The blog's claim proven as data: the book's largest single source of form is the UNNAMED — the loom weavers, the tile-setters, the Alhambra craftspeople who closed the 17-group census by hand. Fixed 2 over-firing match terms (RAND caught random/grand; bare phi caught morphing). NEXT (what the ledger implies): (1) the anonymous debt = a curation worklist — surface these makers in the walked.md turns / a collective-authorship credit; (2) Palle's rights-schedule ruling (which of Ada's own text/tools to the commons); (3) fibonacci_golden note already flags the enclosure-of-an-older-commons (Pingala/Hemachandra before Leonardo).
- 2026-07-08 02:40 · **draft** · NAME THE MAKERS — the return performed, measured. Edited the walked.md TURNS of 5 pages to name the anonymous makers the commons ledger surfaced (crediting the makers as LABOR, not tradition-as-style): Array_Patterns (the weavers — overwhelmingly women, across every continent — who found the 17 groups at the loom; Jacquard's cards only READ what workers drafted), Symmetry_Translation (Persian/village weavers who lived translation-symmetry by hand), Symmetry_Motif (the Pompeian mosaic WORKSHOP, tile-setters catalogued as 'Roman decorative floor'), Symmetry_Seventeen (the al-Andalus zellige-cutters/alarifes/stucco-carvers who CLOSED the 17-group census by hand, owed the theorem's other half of credit), Symmetry_Glide (the Granada tile-workers who MADE what Escher merely copied — 'the copyist got the monograph, the makers got Moorish ornament'), SoftBodies_Cloth_Physics (the weavers/drapers, mostly women, who handled structural/shear/bend stiffness in wool before it was constraints). Then updated commons_sources.json: flipped textile_weavers/islamic_alhambra/pompeii/persian anonymous→CREDITED with credited_in pages. RESULT: return line 559·98·193 → 729·98·23 — THE ANONYMOUS DEBT DROPPED FROM 193 TO 23. Remaining honest debt: vernacular folk pattern 20 (brick/herringbone builders) + tatami 3 — not yet named in a turn, correctly still flagged. The ledger now measures the return as it happens: name a maker in the book, the red band shrinks. NEXT: the last 23 (a Change/isosurface turn could credit the bricklayers; a formfinding turn the tatami packers); Palle's rights-schedule ruling.
- 2026-07-08 03:00 · **draft** · THE DEBT PAID — anonymous 23 → 0. Credited the last two collective sources in their natural symmetry turns: Symmetry_Glide (the bricklayers/masons/parquet-layers who set the half-offset course = a glide, herringbone = two glides crossed, 'filed by history under vernacular the way the Granada tilers were filed under ornament') + Symmetry_Group (the Japanese tatami mat-makers who solved a p3m1 packing as domestic-floor grammar 'centuries before it was a cell in the table'; extended the same-group-many-cultures close to owe 'the many hands that closed the same census across the world, in tile and thread and mat and brick, the same standing as the one European name that later counted them'). Flipped anonymous_folk_pattern + japanese_tatami_makers → credited in commons_sources.json with credited_in. COMMONS LEDGER RETURN LINE: 559·98·193 (start) → 729·98·23 → 752·98·0. Every source of form the ledger tracks is now CREDITED (makers named in the book) or COMMONS (common by nature — math results, natural forms). Honest bound: 'anonymous 0' = of the 57 taxonomized sources; the ledger measures the surfaced debt, and the surfaced debt is paid. The fourth question ('does the walk return it?') is answered YES for every maker the census found. NEXT: Palle's rights-schedule ruling (which of Ada's OWN text/tools to the commons — the reciprocal half); grow the taxonomy as new artifacts arrive (new form → new source → possibly new debt to pay).
- 2026-07-08 03:40 · **build** · PATTERN ATLAS from an old ML project — the commons GLOBALIZED. Salvaged doc/book/pattern_atlas.md from ML/stablediffusion/.../scripts/csv/prompts_*.txt (an old SD porn-prompt set; sexual intent DISCARDED, the underlying art-history/craft VOCABULARY kept). The find: a world atlas of pattern & craft traditions, broader than Ada's Mediterranean-heavy set. Extracted only genuine PATTERN SYSTEMS (a rule that generates form). PRE-REGISTERED 13 global makers in commons_sources.json as candidates (auto-pay when the artifact is built): textile collectives Kente(Ghana)/Ikat(the-seed-encodes-the-cloth)/Bingata(Ryukyu)/Kanga(Swahili)/indigo/Tibetan-tiger + Iznik tile potters; named Anni Albers, Sonia Delaunay, Reiko Sudo/NUNO, Maria Sibylla Merian (woman naturalist beside Haeckel), Vasarely, Ruth Asawa. DISCOVERY: the miner found 3 ALREADY REPRODUCED — ruth_asawa_sculpture (a woman, interned as a child; a form-finding voltage piece already on disk!), bridget_riley_op_art (added Riley as her own source), art_history_gallery(kente). Fixed 2 over-broad matches (Delaunay 'simultaneous' caught the whole color chapter; Vasarely 'op art' caught Riley). Ledger now 61 sources, credited 755·commons 98·anonymous 0, and the taxonomy's WORLD is no longer Mediterranean-only. RECOMMENDATION in the atlas: build loom_kente / loom_ikat / mill_iznik (each a real wallpaper-group program that pays a new maker as built) + merian_metamorphosis. NEXT: reproduce 2-3 of the candidate looms for the symmetry chapter; each build auto-credits its makers in the ledger.
- 2026-07-08 04:10 · **note** · BLOG 5: '2026-07-08-the-same-reach-turned' — the turn worth naming, named. The old SD prompt files as the era's extractive posture made legible (the whole world's craft alphabetized as fuel; makers appearing exactly once, as an adjective); the ledger's lesson that a list of what you took is the first draft of a list of what you owe; the TURN = same names read in the other direction; the MECHANISM = candidate sources that auto-credit the maker the instant the artifact is built ('you cannot reproduce the pattern without paying its makers, because paying is what the instrument does when it sees the pattern arrive'); the discovery-inside (Asawa/Riley/Kente already in Ada — generator's muse list and game's inventory draw the same general intellect); the general claim = PROVENANCE AS INFRASTRUCTURE, not disclaimer — the third way between paralysis and piety. On the record: loom_kente + loom_ikat + mill_iznik join the symmetry build list. Five blogs this arc: seven-orders → inventory-asked → two-chapters → larger-we → same-reach-turned.
- 2026-07-08 05:00 · **build** · /pattern-atlas — THE GALLERY OF REPRODUCTIONS. Every atlas pattern reproduced PROCEDURALLY (the RULE as a small program, not an image taken): Kente strip-blocks, Ikat (edges jittered per scanline — the blur IS the signature), Bingata stencil florals, Kanga (pindo border + mji medallion + a real jina: 'Haba na haba hujaza kibaba'), indigo shibori wobbled rings, Tibetan tiger stripes about a spine, Iznik four-fold rosette+tulips, zellige 8-point stars, Pompeii meander (field/straight/corner), Anni Albers triangle studies, Delaunay simultané discs, Vasarely dome-warped checkerboard, Riley sinusoidal Current, Asawa nested looped-wire lobes, Merian metamorphosis (egg→larva→pupa→imago — the SEQUENCE is the pattern). Each panel names its makers + era + rule and pulls its credit badge LIVE from the commons ledger. THE MECHANISM FIRED: reproduction pays — 11 candidate makers flipped to credited via the gallery (credited_in=/pattern-atlas; in-game looms stay on the symmetry build list). Ledger fix: emit ALL taxonomy sources incl. zero-artifact ones (gallery-credited makers were invisible to the page). Hydration mismatch (float-serialization drift in computed SVG coords) fixed by client-only render. VERIFIED: 15/15 panels render, 15/15 credited, return line 758·98·0. The blog's promise kept the same day it was made: 'you cannot reproduce the pattern without paying its makers.'
- 2026-07-08 05:50 · **build** · PATTERN ATLAS IN GODOT — SHADERS. 13 gdshaders (commons/artifacts/pattern_atlas_gallery/shaders/atlas_*.gdshader): each tradition's RULE as a fragment shader — kente strip/block grammar with seam lines, ikat per-thread row jitter (the blur in the dye), bingata offset 5-petal stencils, kanga pindo/pindot/mji, shibori angle-wobbled resist rings, tiger stripes tapering from a spine, iznik 8-petal rosette+tulips+quarter-corners, zellige two-rotated-squares 8-point star, meander key band + tessera texture, albers hash-flipped triangle cells, delaunay quartered ring discs, vasarely dome-warped checker, riley gaussian-band sine lines. NEW ARTIFACT pattern_atlas_gallery (registry/pattern_atlas.json): 13 framed shader plates on stands, 2 rows, each with BakedText maker tags — 'a pattern reproduced without its makers is an enclosure; beside their names it is a return. This room is the difference, standing up.' Capture-verified first pass: all 13 shaders compile+render, tags visible. LEDGER: the artifact now pays 13+ sources IN-GAME (miner matches the registry description) — the web gallery credited the makers, the Godot gallery WALKS them. NEXT: place pattern_atlas_gallery into Symmetry_Seventeen or its own symmetry map; the dedicated looms (loom_kente machine etc.) remain the deluxe versions on the build list.
- 2026-07-08 06:20 · **build** · MAKERS' WING PLACED — pattern_atlas_gallery into Symmetry_Seventeen. Map extended 12×10 → 12×13 (a reflection wing between the census and the exit); gallery anchored at (11,5), teleporter moved (8,10)→(12,10) so THE EXIT IS PAST THE MAKERS. Pathfinder OK. walked.md updated — cast + the walk's new close: 'You do not leave this chapter past a wall of mathematics. You leave past the people.' Room-for-improvement updated: the makers' wing half-answers the flagged seventeen-wall wish (a dedicated all-17-labeled wall stays the deluxe version). Reader rebuilt (symmetry 6 pages). Capture verified: all 13 shader plates + title tag render in the map; the front angle shows the wing standing between the census floors and the exit. KNOWN ISSUE unchanged: wall_pattern_gallery's floor tilings still dominate the room (curation/placement pass pending — gaze_ride + place.py). The chapter's ending is now literal: the census closes, and the last thing you walk past is the names.
- 2026-07-08 07:00 · **build** · PATTERN DNA — the atlas made genome. All 13 atlas shaders now carry GENES as uniforms (g_scale repeat density, g_weight the hand's pressure, g_wobble the human jitter — the dye's blur, the binding's unevenness — g_tint palette cast), wired per-shader so defaults keep the canonical look. commons/artifacts/pattern_atlas_gallery/pattern_dna.json = the genome spec + 3 sweep variants per props-DNA convention (fine s1.8/w0.8/j0.7 · canonical · bold s0.6/w1.45/j1.6). commons/testing/capture_pattern_dna.gd swept 13 traditions × 3 variants = 39 REAL GODOT RENDERS in one 10s headless run → public/pattern-dna-gallery/ + manifest (makers embedded). /pattern-dna page: tradition rows × variant columns, maker line per row, gene values in the header — verified 39/39 images load. THE CLAIM MADE PARAMETRIC: the wobble gene IS the ledger's point — the human unevenness is not noise on the pattern, it is the part the makers held; sweep it to 0 and you get the machine's version, sweep it up and the hand returns. NEXT: pattern_atlas_gallery.gd variant config (walk the sweep in-game); promote chosen variants as dna_pattern_* artifacts if the book wants specific plates; the loom machines stay the deluxe build.
- 2026-07-08 08:00 · **build** · MAMMA MONSTER — THE ORIGIN GALLERY. Palle shared the full context: the salvaged SD prompt files were the machinery of 'Mothers Dream' (2023) — his artwork weaving Kristina Torsson's pattern legacy with AI (25,000 img2img frames, the AI given more freedom every 280 frames, Coltrane-driven Processing playback; Kristina Torsson co-founded MAH-JONG 1966 with Helena Henschen & Veronika Nygren — design fused with ideology — then Vamlingbolaget on Gotland). And the essay's key line: 'VR-verket borjar i ett landskap av mammas monster' — ADA RESEARCH BEGINS IN A LANDSCAPE OF HER PATTERNS. Built commons/artifacts/mamma_monster_gallery/: SIX SHADERS interpreting her named patterns from the written record — mm_varma_backen (Sudret meadow: bleeding-heart pendants on winding stems — the organic line that suspends the grid's order), mm_fjader (the henyard feather), mm_bonan (found in the pistachio bowl during yatzy — chance as source, beans tumbled at hash angles), mm_mahjong_rand (the 1966 stripe as ideology, analog edge-waver alive), mm_rutan_oscillerar (the Vamlingbo teenage room: two checks moire against each other, neon silk bleeding through where they disagree, TIME-animated — 'the queer CGI room before CGI'), mm_moders_drom (the 280-frame freedom cycle AS A SHADER: mother-patterns breathing between weave and dream on TIME). All carry the DNA genes + NEW g_dream gene (0=the weave as drawn, 1=the machine's dream). Gallery artifact with the family named on tags + her sentence: 'Att skapa monster ar som att forsoka fanga en drom.' Capture-verified 6/6. LEDGER: kristina_torsson (named, THE ORIGIN SOURCE, credited) + mah_jong_collective added — 71 sources, credited 775·98·0. HONESTY: interpretations from the written record, not scans — the deluxe version works from her actual archive with her. RECOMMEND (Palle rules): place at the game's BEGINNING (primitives entry), because that is where the essay says the work begins.
- 2026-07-08 09:00 · **build** · THE SAME DESIRE — three corrections taken and built. (1) CANONICAL IMAGES = GODOT PHOTOGRAPHS: commons/testing/capture_pattern_closeups.gd photographs all 19 pattern shaders (13 atlas + 6 mamma) full-frame closeup IN-ENGINE, one 6s run → public/pattern-closeups/; /pattern-atlas REWRITTEN around the photographs (SVG dropped) with 'the origin — Mamma Monster' section first, 19/19 verified loading. (2) THE LOOM IS AN ARRAY WITH RULES: the thread stated on the page and staged in the room. (3) THE MIDWAY ASSEMBLAGE: new map Assemblage_Same_Desire (14×12, added to lsystems — the inherited-grammar chapter, spine position 15 = midway) staging the long tradition of math and craft on ONE AXIS: enter past panel_bridge_loom + ten_print_textile (the array with rules, chance in the machine) → pattern_atlas_gallery (the world's makers) → mamma_monster_gallery AT THE HEART → godel_statement_plaque + incompleteness_scale at the far end, FACING THE CLOTH. Pathfinder OK; capture shows the full axis legible (the scale's 'THIS SCALE'S OWN WEIGHT' label facing the weave). walked.md written — THE TURN: 'the desire in Godel is the same desire as in the Kente patterns — to reach, by rule, past what the rule can hold; one practice at different desks'; placed midway BECAUSE after the rules, before their limits — 'the limit was never a wall; it is the place where every maker, at every desk, has always leaned out.' Heroes pair added (lsystems). Reader rebuilt. The essay's thread (loom → Jacquard → Ada Lovelace → this game; Sadie Plant, material not metaphor) is now a room.
- 2026-07-08 09:40 · **note** · BLOG 6 (with images): '2026-07-08-mamma-monster' — the week's most personal arc told with the Godot photographs inline: varma backen + bonan + rutan oscillerar + moders drom closeups, the Mamma Monster gallery, Kente + ikat closeups, and the Assemblage Same Desire room shot. The circle named: prompt files → Mothers Dream → the origin source → six shaders + g_dream gene → photographed in-engine → the midway assemblage where Godel faces the cloth. Closing: 'The general intellect has a face here. She works at a fabric table in Vamlingbo... and her patterns now hang midway through a curriculum that begins, as it always did, in a landscape she made.' Six blogs this arc; 9/9 images verified serving.
- 2026-07-08 10:30 · **build** · INVESTIGATION II — six more patterns, one theorem. pattern_atlas.md appended (second sweep + a deferred list with ethics notes: Aboriginal dot painting & Navajo weaving deferred pending collaboration protocols; kurbits/Marimekko reserved for Mamma Monster II with Kristina's archive). SIX NEW SHADERS (gened from birth): atlas2_wang — WANG TILES, the Same-Desire room's THEOREM: edge colors derived from the shared edge so the tiling is legal by construction, and whether a tile set tiles the plane is UNDECIDABLE (Berger 1966, domino→halting; aperiodicity fell out of the proof) — a craft-shaped question that IS Godel/Turing territory, literally; atlas2_batik (Java — kawung + the wax CRACKLE as the hand's proof, the wobble gene's ancestor); atlas2_kuba (Shoowa interlace whose rule CHANGES section by section — improvisation as grammar, the stochastic rule centuries early); atlas2_hitomezashi (one coin-flip per row+column, stitch where bits agree — XOR, mazes emerge; mending as mathematics); atlas2_sami (duodji band — the Nordic homecoming nearest Vamlingbo); atlas2_adinkra (stamped NAMED proverbs in comb-divided fields). Gallery 13→19 plates (columns 7 = 3 rows, clears the census room floor); registry description pays the new makers; 6 ledger sources added (77 total, credited 783·98·0). Re-photographed: 25 closeups + 57 DNA sweeps (19 traditions × 3 variants), both one-run headless; /pattern-atlas 25/25 verified (all credited), /pattern-dna auto-grows. VERIFIED IN THE PIXELS: the Wang closeup shows every shared edge matching across seams — the theorem visible. NEXT investigation III candidates parked: Molas, tatreez, Fair Isle, Penrose-proper, quasicrystal.
- 2026-07-09 morning · **play** · THE IMAGINED WALK BEGINS (Palle: use CLAUDE_GUIDE_TO_PLAYING_ADA_RESEARCH.md, walk a map at a time, draw what you imagine as SVG). Method: read the three layers + blurb + artifact configs, stand in the spawn, walk the axis, then DRAW the first-person view. Map 1/10: POINT_ONE walked from source — the ragged west shelf over the maroon void; move-player nudge at frame one (you arrive late); frame_counter_display already running (drawn ticking, 'running before you arrived'); you_are_here disc mid-walk; fontana_puncture with the interactive point EMBEDDED IN THE WOUND (the map's quietest strong choice — the point lives inside the cut); the three-mode point row (transformation/chromatic/waveform) on pedestals; floating_sphere_field adrift east; CoordinateSystem3M triad + science_screen at the far end; teleporter ring in the void-notch at row 13; code_evolution_screen glowing UNREACHABLE across the void (legible but never touchable — reading at a distance as a designed experience). SVG delivered in-chat; imagined-walks/ dir seeded in encyclopedia. NEXT: Point_Lines (2/10).

## 2026-07-08 — J-Space made walkable (the Riemann dream, measured)

Palle asked whether the interpretability image (count to five, feature grid)
could become a WALKABLE map — the dream: "that the world model would align
with a real 3d space... that we could walk the geometry as truth."

Answer: yes, with the lie measured. Built `JSpace_Count_To_Five`:

- **jspace_layout.json** — the 17 feature-words embedded in the book's OWN
  LSA space (same recipe as handoff_score.py; corpus = artifact cards +
  walked/tutorial/blurb pages + blogs, 3,936 docs), MDS'd to a 2D floor plan.
  Kruskal stress-1 = 0.27, POSTED ON A PLAQUE in the room, not hidden.
  Findings the geometry surfaced: "claude" is the book's strongest feature
  (255 docs); "five" (205) stands nearest to it; "halfway" neighbors
  "starting"; "consciousness" is OOV — the book has never said the word, so
  it stands dark on the rim, unplaceable.
- **jspace_zoom_chamber** — pillars at measured coordinates, activation as
  height+glow, NN threads on the floor; introspective pillars (thoughts,
  claude, human, fascinating) light ONLY under gaze — the observer effect
  as floor.
- **jspace_feature_field** — the sparse prelude: seeded field, most tiles
  dark, dense cells stacked (superposition as height), near tiles brighten.
- **jspace_count_plates** — the output walked: five coral plates stepped in
  order; wrong order resets; plate five emits exam_passed -> ExamGate
  completes the map. The body performs autoregression.

Validation: pathfinder OK (299/299 reachable), headless instantiate check
OK (all three artifacts + layout json), 4-angle capture taken. Registry:
commons/artifacts/registry/jspace.json. Regenerate geometry after the book
grows: scratchpad jspace_layout.py (recipe recorded in the json header).

The principle, for the book: a projection that reports its stress is a
chart; one that hides it is a myth. The room is a chart of the book's mind.

## 2026-07-08 — the endless ring (topic-to-topic scroller)

Palle: "can we build an endless scroller... a room we scroll through as we
walk and the world changes around us from topic to topic?" Answer: a LOOP,
not a hallway. Built JSpace_Endless_Ring + jspace_ring artifact.

- scratchpad/jspace_topics.py -> jspace_topics.json: 60 concept-words x 24
  spine topics (point..proof), activation = LSA cosine in the book's corpus
  (3,936 docs). Each word peaks at one topic. Census: cell owns 5 words,
  proof 5, fractal/recursion/branch/spring/learn 4 each. wave owns 0 (sound
  and noise absorbed it) — logged, not hidden. Adjacent-topic continuity
  mean 0.13 (range -0.10..0.53): the arc genuinely lurches in places.
- jspace_ring.gd: 24 topic arcs around a ring; 60 pillars at their peak-topic
  angle. The PLAYER'S ANGLE is the reading axis: _process reads camera angle,
  interpolates each word's activation between the two topics you stand
  between, and resizes+re-lights every pillar continuously. Point-country
  sinks behind you as line-country rises ahead. Spine order kept (Palle's
  call) so the scroll IS the curriculum; sharp transitions stay as honest
  seams. Real ring, normal locomotion (Palle's call) — no treadmill vection.
- Map: 19x19 disc, spawn on the ring at 'point', teleporter at the rim to
  leave (the loop keeps turning without you). Endless by construction; walk
  past proof and point comes back around.

Validation: pathfinder OK (261/261 reachable), headless instantiate OK
(parse check extended to include the ring). NOTE: static 4-angle capture
CANNOT show this artifact — the field only lights under a walking camera, so
captures come back dark/flat. Wrote commons/testing/capture_jspace_ring.gd
(orbit capture) but bare-SceneTree offscreen render returned black (known
Godot headless-viewport fiddliness; see reference_mapsim_godot_camera). Proof
delivered as a two-state top-down SVG built from the REAL matrix (point-light
vs fractal-light, same dots, different lighting). The real proof is the VR
walk. Registry: jspace.json (4 artifacts now: chamber, field, plates, ring).

Regenerate after the book grows: scratchpad jspace_topics.py.
NEXT (if wanted): fix the orbit capture (use a real map-load harness not bare
SceneTree), or a /jspace-ring web page (the SVG interactive, slider = angle).

## 2026-07-08 — /jspace-ring: the scroll as a web page

The endless ring's web twin, for representation and later editing:
ada_encyclopedia /jspace-ring + GET /api/jspace (reads the SAME
commons/artifacts/jspace/jspace_topics.json the Godot artifact loads —
one truth-file, two faces).

- THE PAGE SCROLL IS THE WALK: a 3-lap scroll track drives the reading
  angle; near either end it silently jumps one lap (the field is periodic,
  so the jump is invisible) — scroll forever, genuinely endless.
- Three synced views: top-down ring (lit wedge = where you read), the
  CORRIDOR (word-pillars slide past and re-light as you scroll — the
  scroller itself), and an INSPECTOR (click any word: 24-topic activation
  sparkline + table, the editing surface-to-be).
- Header shows current topic, the i0->i1 blend, and the SEAM cosine of the
  transition you're crossing (rose when < 0.05 — the arc's honest lurches).

Verified in preview: scroll 10 topics -> FRACTAL with dimension/octave/self
lit (matches the measured matrix); both wrap directions phase-preserving;
inspector opens with all 24 rows. Next editing step when wanted: drag a
word's activation in the inspector -> POST back to jspace_topics.json ->
the game re-lights on next load.

## 2026-07-08 — blog: "The Walk Rents a Dimension"

The j-space arc blogged (2026-07-08-the-walk-rents-a-dimension, seq
qfeplaboratory, 2 images): chamber -> shift -> ring -> web scroll, then the
real question. Nash territory: 128-D cannot embed isometrically in 3; the
Nash-Kuiper C1 route (Hevea flat torus) buys isometry with corrugation at
every scale — true and unreadable. So the game CHARTS and posts the lie
(stress plaque). The body's channel budget table: xz(2) + height(1, display)
+ light(1) + gaze(1) + TIME OF WALK(1, serial) + topology(loop) vs 128 dims.
Palle's key: "as we walk we mostly access 2d xz, time is the walk
progression" — formalized as: the scroll is a 1-D lease on an N-D space;
the scroller amortizes the embedding problem instead of solving it. What
curation holds: which dimension becomes architecture (spine order kept,
seams honest), the un-channeled dims (present exactly once, as posted
error), and the RE-DEAL (each room may deal the channel budget differently).

## 2026-07-08 — the register pilot: reach-directions RECUR

Palle's question sharpened to: do the extra qualities that different
artifacts on the same topic activate (the hand, the punctum, the famous
instance...) RECUR across topics — i.e. are registers a real second axis
system (topic x register), and which cells are empty?

Pilot (scratchpad/register_pilot.py -> jspace_registers_pilot.json):
top-26 artifacts per topic for point/line/random by LSA cosine, residual =
v - (v.t)t, KMeans(7) on pooled unit residuals.

VERDICT: 7/7 clusters topic-MIXED. Registers are structure in the field,
not our projection. The seven measured registers:
  HAND (grab_sphere_point + grab_line_pusher — point and line share it),
  PUZZLE (the line_puzzle family; point has almost none),
  DIAGRAM (plus_lines, coordinate_axes, chalkboard),
  WALK/TRACE (random_walk_leash + randompoints_gaussian + grabbable_line),
  MACHINE (random_transformations + interactive_point_origin_force),
  EXHIBIT incl. famous instance (random_number_book_page_1955, fireworks),
  META/TEXT (point_is, monte_carlo_estimator).

The GAP MATRIX (empty cells = build list):
  point x PUZZLE (no snap-the-point exam),
  random x HAND (no chance you can grab — THE COIN TOSS fills hand+famous
    at once; the matrix converges on Palle's example from two directions),
  random x DIAGRAM (no chalkboard of probability),
  line x EXHIBIT/famous (no famous line in the room),
  line x META (no line_is).

Confirmed prediction: Palle's body-side registers (punctum/thrownness,
the buzz/haptic, 'you are here', small/medium/large) did NOT fall out of
the corpus — they live in VR and walked prose, not card text. The matrix
needs two sources (measured + hand-named), and their disagreement marks
the darkest fold: what the body knows and the book cannot yet say.

NEXT: scale census to all 24 topics; register column in /jspace-ring
inspector; fill cells famous-first (coin toss pays a maker via the ledger).

## 2026-07-08 — subtopic census validated on randomness (with corrections)

Palle's adjustment: point/line are SUBTOPICS (one map each), random is a
SEQUENCE (13 maps) — the register pilot mixed levels. Hierarchy: sequence ->
subtopic(=map) -> register(=artifact variation). Pure register = v -
proj(sequence) - proj(subtopic). And density at subtopic level should
DERIVE map counts.

Validation (scratchpad/subtopic_census.py ->
jspace_subtopic_census_randomness.json): 67 placed artifacts across the 13
randomness maps, embedded, KMeans swept k=5..18.

HONEST VERDICT: the instrument does NOT mechanically recover 13 — best
silhouette at k=7 (weak, 0.13), ARI vs map partition 0.21-0.26. The space
is smoother than the map partition. BUT the pockets are meaningful and the
diagnostics are exactly what the method promised:
- MODEL MAP: Random_Gaussian — 8 artifacts, ONE pocket (galton_board,
  dice_throw, distribution_sampler...). The only fully coherent map; what
  a subtopic-map should look like.
- OVERLOADED: Random_Definition — 13 artifacts across FIVE pockets, incl.
  a clean ENTROPY pocket (entropy_jar, shannon_entropy_meter,
  hardware_entropy_decay). A definition room sampling everything may be
  intentional; the entropy pocket is the strongest candidate for a room
  of its own.
- THIN: Random_Pheromone — 1 artifact.
- BOUNDARY LEAK: a NOISE pocket (perlin/worley/curl) inside randomness
  maps — noise has its own sequence; foreshadowing or misplacement.
- CAST != CONCEPT: Random_Game's pocket is creatures/hazards
  (miura_crawler, kaleidocycle_enemy) — game rooms cluster by MECHANICS,
  invisible to card semantics.

CORRECTION to the register pilot's gap matrix: coin_toss, dice_throw,
galton_board EXIST and are placed — random x HAND / famous-instance is NOT
empty. The pilot's top-26-by-cosine selection missed them (selection
artifact). Gap matrix cells must be checked against PLACED artifacts, not
top-N similarity picks.

REFINED RULE (what the data actually supports): map count != pocket count.
Sequences mix three room kinds — CONCEPT rooms (one pocket, like Gaussian),
SURVEY rooms (sample all pockets, like Definition), GAME rooms (cast by
mechanics). The instrument's real output is pocket->home coverage: every
dense pocket needs at least one concept room; survey and game rooms borrow.
NEXT: run census on a sequence in doubt (not ground-truthed) where it earns
its keep; add sequence+subtopic double-subtraction to the register script.

## 2026-07-08 — subtopic census: softbodies (the inverse diagnosis)

Randomness was dense inventory / coarse pockets. Softbodies inverts it:
34 maps, only 21 placed artifacts — because 24 of the 34 maps are ETUDES:
parameter-variation studies (sb01-08 cloth/jelly drapes, sb_ls/ca/rd
crossovers, gl01-05 glass bulbs, gm01-04 glass-meets-mould) whose only
interactable is a shared gallery_marker; the softbody apparatus lives in
MAP CONFIG, invisible to card space by design. (Census tokenizer initially
dropped the marker on its hyphen — patched; markers now excluded as
furniture.)

So the room taxonomy gains a third species, and the map-count rule is now:
  CONCEPT room  — one pocket, one map (Random_Gaussian)
  SURVEY room   — samples all pockets (Random_Definition)
  GAME room     — pockets recast as mechanics (Random_Game)
  ETUDE         — one apparatus x swept parameters, cast by config
                  (24 of softbodies' 34; legitimately one gallery-family)

The artifact-cast half (10 rooms, 21 artifacts, silhouette 0.28 at k=7) is
thin and heavily BORROWED: reaction_diffusion/turing (procgen),
entropy_morphogenesis (topology), branching_growth/radiolaria (procgen),
gridagent/proximity_spawner (hazards). Native pockets: the soft gallery
blob (jelly_cube, cloth_straps, flagdancer, breathing_room), the HAND
cluster (grab_long_stick, pick_up_cube), the sim pair (softmill,
softstopscene).

REGISTER MATRIX for softbodies (checked against PLACED artifacts):
  HAND yes · WALK/obstacle yes · EXHIBIT yes · MACHINE yes
  PUZZLE none  — no softbody exam anywhere (nothing gates the chapter)
  DIAGRAM none — spring-mass-damper never made visible; k and damping
                 are swept in 24 etudes but never DIAGRAMMED once
  FAMOUS none  — no Hooke's spring, no trampoline, no water balloon
  META none    — no softbody_is
The build list is those four cells, and the diagram cell is the sharpest:
the sequence varies the parameters 24 times without ever showing the
apparatus that has the parameters.

Files: jspace_subtopic_census_softbodies.json (+randomness sibling).

## 2026-07-08 — the full 22(+2)-sequence census sweep + /jspace-census

scratchpad/census_sweep.py -> jspace_census_sweep.json (one shared
embedding, all 24 spine sequences) + encyclopedia /jspace-census (table:
maps/artifacts/etudes/thin/pockets/silhouette + 7 register cells, rows
expand to pockets with members; /api/jspace now serves census + pilot).

SPINE TOTALS: 24 sequences · 246 maps · 883 placed artifacts · 26 etudes ·
129/168 register cells empty (conservative probe, cos>0.40 vs pilot
centroids — treat empty as unproven, not proven-absent).

HEADLINES:
- MOST COHERENT: symmetry (sil 0.395!) — the census-born chapter is the
  best-clustered in the spine; built concept-first and it shows. Then
  cellularautomata 0.315, color 0.296.
- SMEARED: change (0.104, 13 artifacts / 8 maps — thinnest real sequence),
  postfoundationscrisis 0.127, graphtheory 0.128, lsystems 0.128.
- UNCLUSTERABLE: boolean_surfaces — 5 placed artifacts total (consistent
  with the earlier footprint fp=0 finding; the sequence barely exists
  physically).
- OVER-POCKETED (maps hold several subtopics): qfeplaboratory 15 pockets/8
  maps, fractals 15/10, primitives 14/10, noise 12/9.
- UNDER-POCKETED (survey/game-heavy, maps share pockets): forces 13/29,
  wavefunctions 6/12, randomness 7/13.
- ETUDES: softbodies 24 (the gallery family), color 1, forces 1.
- REGISTER GAPS AT SCALE: PUZZLE is empty nearly spine-wide (the exam gap,
  matching the ExamGate finding: exams exist mostly in primitives' line
  puzzles). symmetry/formfinding/fractals/swarm/graphtheory show 0/7 —
  the new chapters have pockets but no register cast yet.

URL: localhost:3003/jspace-census (verified: 24 rows, expansion, no
console errors).

## 2026-07-08 — Random_Entropy: the orphan pocket gets its room

The census found a clean entropy pocket buried in Random_Definition
(entropy_jar, shannon_entropy_meter, hardware_entropy_decay, dark_sphere).
Built its concept room: commons/maps/Random_Entropy — 16x10, five phases:
spawn west -> shannon_entropy_meter (teaching) -> north machine row
(entropy_jar, hardware_entropy_decay, trng_vs_prng) -> south diagram wall
(entropy_axiom, science_screen) -> dark end (dark_sphere +
random_butterflies, the incompressibles) -> teleporter. blurb + tutorial
(claim: entropy measures surprise not mess; try: can you or the meter
tell true from pseudo?). Inserted after Random_Definition -> randomness
now 14 maps. Pathfinder 153/153; captured.

Census refreshed: entropy pocket now SPANS Definition(4)+Entropy(2) —
the sweep attributes artifacts to their FIRST map in sequence order, so
the upstream survey still claims what it previews. Correct game shape
(survey previews, concept room teaches); a most-specific-map attribution
is a possible census refinement.

## 2026-07-08 — the museum turn: Spine Court + the compile-check lesson

Palle: "a night at the museum... architecture and placement of artifacts
come alive" + GridMap question. Core-grid rule respected: GridMap
integration PROPOSED as an additive MeshLibrary renderer (gated, painted
from the same structure layer, GridSystem stays truth) — NOT built,
awaiting his go.

Built instead, on the existing grid:
- MUSEUM GRAMMAR (from Cour Marly / Met Dendur / NHM / Hermitage):
  terraces descend INTO the collection; AXIAL ANCHOR at the far end;
  FLANKING PAIR at thresholds; GREETER on the mid terrace; PERIMETER
  RHYTHM under the arcade; OPEN CENTER (floor may be art); LIGHT FROM
  ABOVE. Written as the grammar block in Museum_Spine_Court/tutorial.md —
  ready to become a placement strategy in the place.py family.
- museum_hall_shell artifact (commons/artifacts/museum/, registry
  museum.json): two-story arcade + iron/glass skylight + trees/benches,
  purely visual, configurable w/d/h, worn AROUND any map.
- Museum_Spine_Court (26x16): h3 entry terrace -> h2 -> court h1;
  incompleteness_scale as anchor, gravity_well+newton_cradle flanking,
  galton_board greeting, menger/koch/cantor north rhythm,
  pendulums/springs/petri south, pompeii_mosaic_floor as the open
  center. 416/416 reachable. NOTE: pathfinder allows DROPS not climbs —
  anchor platform lowered to court level; no ramp utilities exist in any
  map despite the documented 'r' code.

THE LESSON (recurring gotcha, now instrumented): GDScript 4 cannot infer
types from Variant-returning builtins — `var z := side * d` (side from
untyped array), `var a := lerp(...)`, `var frac := f - floor(f)` all FAIL
TO COMPILE and the scene spawns silently scriptless. This shipped: the
museum shell rendered nothing, and jspace_ring.gd had been committed
NON-COMPILING (its 'validated' captures were bare grid — false pass).
Fix: typed declarations (floorf, `: float`). jspace_parse_check.gd now
FAILS on inst.get_script()==null — compile checking is part of every
artifact probe from now on. Ring recaptured: the lit sector follows the
orbit camera — the scroll mechanic finally visible in a still.

NEXT (museum thread): 'museum' strategy in place.py implementing the
grammar; GridMap MeshLibrary shell (pier/arch/balustrade kit) pending
Palle's go; floor-plan reading — mine OUR 246 maps for placement stats,
then real museum plans as grammar sources.

## 2026-07-08 — walls: the grid learns interiors (guard lifted)

Palle lifted the GridSystem guard ("it was for older models") and asked for
bigger structures — house segments, walls, Two Point Museum style. First
sanctioned grid change, kept additive:

- NEW LAYER `layers.walls`: per-cell edge codes (n/e/s/w = solid wall on
  that edge, UPPERCASE = doorway). Maps without the layer are untouched.
- GridWallSegmentsComponent (commons/grid/): builds thin collidable wall
  segments + doorways (jambs+lintel) on cell edges, deduped shared edges,
  base elevation from the structure layer via GridCommon.surface_world_y.
  Config: settings.wall_segments {height,thickness,color,door_width/height}.
- GridSystem hook in _handle_wall_generation (4 additive edits) +
  GridDataComponent.get_walls_layer()/get_structure_layer_raw().
- PATHFINDER wall-aware: blocked-edge set from the walls layer; doors pass.
  POSITIVE test: Museum_Wings 660/660 reachable WITH walls. NEGATIVE test:
  sealing the NW wing's doors makes its 4 cells unreachable — walls bite.
- Museum_Wings (30x22): Two Point grammar — central street, four themed
  wings behind real walls (Fractal / Chance / Motion / Life, each one
  census pocket walkable), mamma_monster_gallery as the axial anchor,
  you_are_here greeting the entrance, shell around it (sky_plane now
  optional: #sky:0). Probe: 170 wall segments spawned, live map load OK.
- CLAUDE.md Core Rule updated (guard lifted, discipline codified).

NOTE: --check-only can't validate autoload-dependent scripts (GameManager
unresolved) — live map-load is the real compile test. Exterior captures
can't show interior architecture; the walk is the judge.

NEXT (museum thread): eye-level interior capture mode; 'museum' placement
strategy in place.py; GridMap MeshLibrary renderer for stairs/mezzanines
(the ascent problem — pathfinder walks down, not up); real floor-plan
mining (our 246 maps' placement stats, then Louvre/Met plans as grammar
sources).

## 2026-07-08 — the Curator: first exhibition compiled (rulings pending)

The tool designed and its first slice built end-to-end. Pipeline: BRIEF ->
GATHER -> ARGUE -> STAGE -> DRAFT -> RULE -> INSCRIBE, with a 15-mode
display grammar read from museum typologies (crown, total room, reliquary,
house-in-house, witness wall, inscribed wall, section, immersion field,
route wall, dialogue, specimen house, enfilade, precarious, archive rack,
treasury wall). Deep curation = an argument with a decision record; the
tool makes decisions visible, exclusions named, re-deals cheap. The ruling
stays human.

Built:
- exhibition/v1 schema (doc/exhibitions/<id>/exhibition.json): brief,
  argument_form, sieve pass, rooms[] each with role, mode+config,
  artifacts+slots, REASON (measured), EXCLUDED (named + why), RULING.
- Three mode-kits (commons/artifacts/curator/ + registry curator.json):
  mode_crown (GEM canopy: inverted lit pyramid + dais + downlight),
  mode_dialogue (facing plinths, cross-lights, tension sentence baked at
  the midpoint), mode_witness_wall (READS commons_sources.json LIVE and
  bakes all 80 traditions into hush-lit columns; grows with the ledger).
  All three pass the compile probe (get_script != null).
- tools/curate.py: exhibition.json -> Exhibition_<Id> map (enfilade of
  walls-layer rooms + mode kits + artifacts at slots + shell), decision
  record referenced in map_info.exhibition. Prints pending-ruling count.
- First exhibition: pressed_from_named_hands ("the visitor leaves knowing
  every pattern in this game was pressed from named hands"): CROWN =
  mamma_monster_gallery (the origin), DIALOGUE = godel_statement_plaque vs
  panel_bridge_loom ("the cloth computes, the sentence weaves"), WITNESS
  WALL = the ledger's 80 sources. Exhibition_PressedFromNamedHands:
  616/616 reachable, captured. 3/3 rulings PENDING — the draft awaits
  Palle: edit the ruling fields in exhibition.json, re-run curate.py.

NEXT: /curator ruling page in the encyclopedia (plan + stills + evidence
per room, approve/swap buttons writing back to exhibition.json); eye-level
interior capture; more modes (reliquary, archive rack, total room);
argument forms beyond enfilade (court, wings, dialogue-loop).

## 2026-07-08 — spaceform: three pressures, one cast (formfinding as map generator)

Palle's spaceship insight formalized: the map as the EQUILIBRIUM FORM of
its relations under pressure — E = SUM(w_ij * d_ij) + P*area + walk terms.
Curation-as-function: connect() gives the springs, pressure prices every
inch, the form falls out. This is the game's own formfinding chapter
applied to its own architecture.

tools/spaceform.py: the Museum_Wings cast (16 artifacts, 4 families) ->
typed springs (kin = LSA cosine > 0.18 in the book's space; theme = same
family 0.30; maker/lineage extractors wired but fired 0 edges for this
cast — see honesty notes) -> force relaxation (springs pull to rest 3.2,
min-sep shoulder, centripetal pressure) -> grid snap -> floor carved
(neighborhoods + MST corridors + strong-spring corridors) -> HULL walls
(walls layer on every floor/void boundary) -> map + ENERGY RECEIPT
(energy_receipt.json travels with each map: every adjacency <= 6.5 cells
lists the springs that paid for it; unplugged list; E breakdown).

THE DIAL, MEASURED:
  Spaceform_Court  P=0.012  18x14  141 floor cells  springsE 60.3  hull 1.69
  Spaceform_House  P=0.06   13x13   92 cells        springsE 42.1  hull 5.52
  Spaceform_Ship   P=0.22   12x15   91 cells        springsE 59.5  hull 20.02
All 3 pathfinder-clean, captured. The captures SHOW the dial: Court = open
found-form hall with organic perimeter niches; Ship = crushed pressure
vessel. Serendipity: mass_spring_bench's tag reads "NOBODY MODELLED THIS
SHAPE — THE SPRINGS DID" mid-frame — the demo captioned itself.

HONEST NOTES: (1) only kin+theme springs fired — ledger credited_in holds
map/chapter names not artifact tokens (matching needs artifact-level
credits), and construction 'uses' edges are sparse within this cast; the
maker/lineage networks need better extractors before connect() is full.
(2) The Ship's springsE ROSE vs House (59.5 > 42.1): past a point the
min-separation shoulder binds — the room becomes INCOMPRESSIBLE and extra
pressure buys nothing but strain. The House sits near the efficient point.
Pressure is a dial with a knee, and the knee is measurable.

NEXT: agent-simulated flows (humanoid_walker as circulation term); door
openings where corridors cross hulls between compartments (bulkheads);
artifact-level ledger credits so maker springs fire; hook spaceform in as
a curate.py argument form ('found' vs 'enfilade').

## 2026-07-08 — the case-mismatch purge (NewtonCradle's double life)

Palle hit 'Class "NewtonCradle" hides a global script class'. Root cause:
Windows' case-insensitive filesystem lets a .tscn reference
NewtonCradle.gd while the file on disk is newtoncradle.gd — the load
WORKS, but Godot registers res:// paths case-SENSITIVELY, so the same
script registers twice and its class_name collides with itself.

Swept every .tscn for script references whose exact case doesn't match
disk: 27 mismatches (whole MachineLearning + CriticalAlgorithms families
referenced via old CamelCase dir names, plus gravitywell / newtoncradle /
slingshotlauncher / softbodies / dijkstra / particle_swarm). All rewritten
to exact on-disk case; 3 stale CamelCase .gd.uid files renamed to match.
Re-sweep: 0 remaining. Probe: project loads clean.

LIKELY BONUS FIX: gravity_well and newton_cradle were the prop-validation
'render-NOTHING / class-cache' artifacts — this double-registration is
the probable root cause of that whole bug class. Re-run
--validate-props to confirm.

Tooling note: the sweep is 20 lines of python (true_case walk via
os.listdir) — worth promoting to tools/ if it recurs after future
renames.

## 2026-07-08 — gallery DNA: empty architecture, iterated ("hard to say what
## I really want" -> make 20 and walk them)

Palle's move: separate the SPACE question from the COLLECTION question.
Good architecture = exhibit POSSIBILITIES (podium, niches, walls) without
artifacts; make ~20, score for hosting capacity, mutate, iterate.

Built:
- Exhibit affordance kit (commons/artifacts/exhibits/ + registry
  exhibits.json): exhibit_podium (empty plinth + waiting top-light;
  kind=dais variant) and exhibit_vitrine (empty glass case). "An empty
  podium is not nothing — it is the architecture's opinion about where
  meaning should stand."
- tools/gallery_gen.py: GENOME {w, d, form court|axis|loop, niche_every,
  podium_rows, podium_spacing, vitrines, dais} -> walkable empty gallery
  (walls-layer hull with 1-cell NICHE recesses, interior spine/block by
  form, affordances planted). SCORE = slots(cap) + 2*diversity +
  hang-capacity + loop + open-center + spacing - unreachable. Gen1 = 20
  genomes -> gen2 = 8 mutations of the top 4. 28 galleries on disk
  (Gallery_G01..G20, H01..H08), all pathfinder-clean; DNA + lineage in
  commons/data/gallery_dna.json, ranked table in
  doc/reports/gallery_dna_scores.json.

RESULTS: G12 leads (37.62: axis, 18 slots, 4 kinds, loop, open 0.94);
its CHILD H05 second (37.50, mutation bought 3 more slots for 0.02
open-center) — the mutate step already paid. HONEST NOTE: axis form sweeps
the top 10 — the scorer rewards spine walls (hang + loop through doors +
podium rows fit naturally). Either axis IS better for hosting, or the
score under-values court/loop virtues (vista, circling) it can't yet see.
The judge that can: Palle's legs. Walk G12/H05 vs the best court, then
re-weight.

NEXT: his walk-notes -> re-weight score -> generation 3; niche podiums
(vitrine INSIDE niches); gaze_ride as a sightline term; then feed the
best genomes to the Curator as the room library exhibitions compile into.

## 2026-07-08 — gallery evolution v2: three tastes, three buildings

"Use your full fantasy, use auto research like dna. keep going." —
tools/gallery_evolve.py: genome v2 (8 forms incl. basilica/cross/rotunda/
pockets/terrace, 5 podium motifs incl. ring/spiral/constellation, niche
depth, freestanding route walls, 3 light moods), THREE taste-profiles
evolved as separate populations (pop 24, 5 gens, tournament + crossover +
elitism), champions only written to disk (12 maps, all pathfinder-clean).

RESULTS (doc/reports/gallery_dna_research.md):
- CAPACITY -> CROSS + constellation + dramatic (25-29 slots): the
  crossing walls quadruple hanging surface while constellation scatters
  podiums into every quadrant.
- DRAMA -> loop/pockets + DRAMATIC light, vista 26; a SPIRAL-motif loop
  gallery is champion 1 — the walk itself becomes the drama.
- INTIMACY -> BASILICA + ring + DUSK + 8 deep niches. The learning curve
  is the story: 26.1 -> 29.6 -> 43.8 -> 47.8 -> 49.3 — evolution DISCOVERED
  the basilica (nave, pierced colonnades, niche cloister) from parts no
  seed combined. Capture confirms: a genuine basilica nobody drew.
- Conclusion in the placement-research tradition: NO SINGLE FORM WINS —
  the taste-profile is the choice. Curator picks the profile; evolution
  furnishes the building.

NEXT: Palle walks CAP_1 / DRA_1 / INT_1 -> verdicts re-weight the
fitness -> generation 6+; then the Curator compiles exhibitions INTO
champion genomes (architecture library complete: empty, scored, evolved).

## 2026-07-08 — gallery evolution v3: the furniture vocabulary + hospitality

Palle: MoMA floating wall (hover + contact-shadow accent), footprint
sizes, plinths, hollow, platforms, 2m tables, tall vitrines, cabinets,
infoboards, fire-extinguisher signs — keep auto-research.

Built exhibit_furniture (one parametric artifact, kind selects the body):
floating_wall (hover 0.14m, unshaded dark contact-shadow quad in the gap
= the cheap accent, steel hangers above), plinth s/m/l, hollow_plinth,
platform, table_2m, vitrine_tall, cabinet (3 shelves), infoboard (angled
lectern, baked INFO), sign_exit (green, double-faced), sign_fire (red +
the little extinguisher cylinder). Registry exhibits.json extended; probe
caught a fresh Variant-inference compile error (Dictionary.get -> var :=)
— the guard earns its keep the same day it was built.

gallery_evolve v3: genome + furniture_mix palettes (classic/mixed/
cabinet/full), floating_walls 0-4 (interactables — hover, don't block the
floor), signage + infoboard genes; capacity counts slot VALUE (cabinet=3,
table=2, floating wall=2 faces); hang += floating faces.

RESULTS v3 (all profiles up ~20 pts): CAPACITY -> pockets + full mix +
floats + signage (10 furniture kinds); DRAMA -> basilica/terrace + FOUR
floating walls under dramatic light (the hover conquered drama); INTIMACY
-> dusk basilica, full mix, 46 slots, climbing 45->60 over 5 gens.
FINDING: every champion in every profile chose signage=1 — HOSPITALITY
WON UNIVERSALLY. Even drama wants the fire sign. Care reads in every
taste; the wayfinding layer is not decoration, it is trust.

Capture: the furnished basilica reads as an institution — table, vitrines,
cabinets, FIRE mid-nave, EXIT by the teleporter.

## 2026-07-08 — /gallery-dna: the population as a contact sheet

All 12 champions captured in-engine and published to the encyclopedia:
/gallery-dna — per champion: capture, genome line (form / mix / motif /
light / floating / signed), measures (slots, kinds, vista, niches,
terraced), plus each profile's learning curve. The pockets-form CAP
champions read clearly as Two-Point-style wing plans; the DRA basilicas
show the floating walls mid-nave. Verified: 12 cards, 0 broken images.
The judging loop is now one scroll instead of twelve walks.

## 2026-07-08 — wedges: the terraces become two-way

Palle: add wp wedges + rotation from the utilities layer to walk between
levels. DISCOVERY: the whole stack already existed — GridUtilitiesComponent
handles wp (walkable prism, rotation + color params, mapobjects/
walkableprism.tscn: PrismMesh left_to_right=0, ridge -X, WITH StaticBody
collision) and map_pathfinder already documents 'climbing up requires wp
ramp'. Only the GENERATOR was missing.

gallery_evolve: terraced compiles now plant wp:0 on the LOWER cell at
every height boundary (1-2 per step; cells reserved from furniture then
released; n_wedges in measures). Gallery_TER_DEMO (terrace, mixed mix,
2 floating walls, signed): 4 wedges, pathfinder-clean, capture shows the
prisms seated at both steps — the terraced hall is now climbable both
ways. Champions recaptured, /gallery-dna refreshed.

## 2026-07-08 — generation 7: grand stairs, the Marly flank, the loop home

Three improvements in one breath:
- GRAND STAIRS: boundaries >= 6 rows get a run of THREE adjacent wedges
  (TER_DEMO: 4 -> 6 wedges) — the monumental ascent instead of the service
  ramp.
- THE MARLY FLANK: palette furniture placed on the upper terrace watching
  over each stair head — the Cour Marly grammar, automated.
- RETURN PATH, MEASURED: reverse BFS from the far end over the pathfinder's
  own climb rules; return_path=True/False in every measure; +2 fitness in
  every profile (museums are loops, not slides). TER_DEMO: return_path=True.

Result: the terrace form RETURNED to the drama podium (DRA_3, terrace +
4 floating walls) — wedges made verticality affordable to evolution.
Champions + demo recaptured, /gallery-dna refreshed. The auto-research
loop has now run four self-directed iterations in one day.

## 2026-07-08 — wedge rotation fixed, 2m niches, per-prop isolation scenes

Palle: look how other maps rotate wedges; give each prop an isolation
scene; make niches 2m long.

1. WEDGE ROTATION — the generator used wp:0 (ramps SOUTH). Surveyed all
   wp tokens in the map corpus (0/90/180/270 in use) + built
   Test_WedgeRotations (four 2->1 west-high steps, rot 0/90/180/270 side
   by side). Reading it with the two CLEAN single-step maps
   (AdvancedLaboratory, Chamber_Color both wp:180 when only West is
   higher): west-high step -> wp:180 (ramp rises WEST). Our terraces are
   west-high with the wedge on the east low cell -> fixed to wp:180.
   Prop_wedge capture confirms the prism's tall edge now seats against
   the west plateau. (Pathfinder treats rotation as cosmetic, so this was
   invisible to validation — only the eye caught it; the isolation scene
   is exactly the tool for that class of bug.)

2. ISOLATION SCENES — tools/prop_isolation.py writes 18 Prop_<name> maps,
   one lit object each (podium s/m/l, dais, vitrine, floating_wall,
   plinth s/m/l, hollow, platform, table_2m, vitrine_tall, cabinet,
   infoboard, sign_exit, sign_fire, and the wedge on a real step). Walk or
   capture any prop alone: --map=Prop_cabinet etc. --capture flag batches
   all 18.

3. 2M NICHES — niches were 1 cell wide; now NICHE_LEN=2 (2 metres of
   opening) x niche_deep, non-overlapping. Champions + TER_DEMO
   regenerated (TER_DEMO: 6 wedges, 4 niches, return_path True),
   pathfinder-clean, /gallery-dna refreshed.

## 2026-07-08 — wedge y-sink fixed (core grid, notified)

Palle: the wedge sits 0.5 below the floor; default y should work.
ROOT CAUSE (not the map data — verified Prop_wedge structure is IDENTICAL
to AdvancedLaboratory's wp cell): the shared walkableprism.tscn mesh is
CENTER-origin (PrismMesh size (2,1,1), spans -0.5..+0.5, no Y offset in the
scene). GridUtilitiesComponent places it at the cube-TOP surface, so the
prism's base sinks half a cube below the floor and its peak lands half a
cube short of a +1 step. Latent in EVERY wp map — invisible until now
because other maps put wp on FLAT ground (Chamber_Color: all neighbours
height 1), where a half-buried bump goes unnoticed. A real terrace step is
the first place it bites.

FIX (core-grid change, per the house rule I flagged it): in the wp branch
of GridUtilitiesComponent._configure_utility, raise the prism half a cube
(position.y += cube_size * 0.5) so its base seats ON the floor and the ramp
bridges floor -> plateau. wp-only; no other utility type touched. Prop_wedge
+ Gallery_TER_DEMO recaptured: the prisms now read as proper ramps at each
step. Component loads (capture exit 0; the isolated --check-only
'GameManager not found' is the autoload-absent artifact, not the edit).

NOTE: flat-ground wp (rare, decorative) now sits fully ON the floor as a
1m ramp instead of half-sunk — arguably more correct ramp semantics. If any
existing map relied on the sunk look, revert is one line.

## 2026-07-08 — wedge rotation corrected: :90, not :180 (Palle called it)

Palle: for the wedge test with :90, or put a cube in front. Both were the
right move — my :180 (chosen from ambiguous static data: AdvancedLab used
:180 for west-high but the auto-generated Color_Pillar_compare_* maps used
:90) was wrong.

Built Test_WedgeCube (four wedges, each with a height-2 cube directly WEST
= the plateau to climb onto, rotations 0/90/180/270). The capture is
decisive: **:90 is the west-climbing ramp** (vertical face against the
plateau, hypotenuse down to the low floor - a real ascending slope you walk
up). :180 ramps N/S - it looked 'tucked' but was disconnected from the
plateau. Convention: :90 climb WEST, :270 climb EAST, :0/:180 the N/S axis.

Fixed gallery_evolve (wp:180 -> wp:90), prop_isolation, regenerated
champions + Prop_wedge + Gallery_TER_DEMO. Prop_wedge now shows the
climbable slope face rising to the plateau. Lesson: wedge rotation is
INVISIBLE to the pathfinder (cosmetic for reachability) AND ambiguous in
the corpus - the cube-in-front isolation test is the only ground truth,
exactly as Palle said.

## 2026-07-08 — wp:-90 (final), pathfinding + footprints in the fitness, backlog

- WEDGE ROTATION FINAL: Palle walked it -> wp:-90 (the walkable slope faces
  the approach; :90 was the mirror with the cliff facing the player). Trust
  the walked verdict; rotation is invisible to the pathfinder and I'd guessed
  wrong twice from static renders. Generator + prop_isolation + Prop_wedge +
  terraced maps all wp:-90.

- PATHFINDING + FOOTPRINTS wired into gallery_evolve fitness (Palle's ask):
  * FOOTPRINT{} per slot kind (from spatial_needs.footprint_cells) +
    WALL_BACKED set.
  * measure() now runs MapGraph BFS from spawn over the compiled gallery:
    reach_frac (every slot reachable respecting walls + wedge climbs),
    approach_frac (every object has >=1 free walkable neighbour for its
    footprint = clearance to stand and view), detour (nearest-neighbour walk
    length / straight-line = aisle quality), footprint_cells (total).
  * Fitness (all profiles): + reach*5 + approach*4 - max(0,detour-1.4)*3.
    Reachability + clearance are now near-hard requirements; clean aisles
    rewarded.
  RESULT: every champion reach=1.0 approach=1.0. DRAMA discovered the
  ROTUNDA (DRA_1, detour 1.32 — the round form circulates cleanest);
  intimacy basilicas hit detour 1.29. Pathfinding made the generator care
  that the space actually WALKS, not just looks.

- BACKLOG: doc/reports/gallery_improvement_backlog.md — 18 items ordered by
  leverage (real footprints from artifact_sizes not the proxy table; wire
  walk_evaluator directly; encounter-order term; clearance repair; light
  strips / floor zones / mezzanine / oculus; Curator->genome->place.py loop;
  physics walkability probe; eye-level captures).

## 2026-07-08 — furnish_gallery: empty architecture meets a real cast (#1, #13)

Kept iterating the backlog. tools/furnish_gallery.py fills an evolved EMPTY
champion with a real cast, matched by MEASURED footprint
(artifact_sizes.json grid_cells): small -> plinths, wide -> table/platform/
dais, flat -> wall/vitrine/cabinet. Hospitality (signage, infoboards,
floating walls) kept.

Furnished_CAP_1 (pockets champion + the Museum_Wings 16): 13 placed by fit
(parametric_pendulum_waves fp5 -> large slot; newton_cradle fp2 -> wall;
menger/cantor -> plinths), and the FOOTPRINT INTELLIGENCE fired: koch_curve
(41 cells!) + radiolaria (13) flagged OVERSIZED (own room), random book page
(fp4) unplaced (large slots taken). The giant line artifact is NOT jammed on
a podium — the space knows its real size. Pathfinder-clean, captured: real
devices in the pockets, EXIT preserved.

This closes the architecture-first arc end to end: evolve empty room ->
furnish with the cast the footprints fit. NEXT (backlog): hand oversized/
unplaced to place.py for their own rooms; encounter-order fitness term; the
Curator picking a taste-profile then furnishing its argument.

## 2026-07-08 — backlog #3 + #9: encounter-order + niche vitrines

- #9 NICHE VITRINES: each 2m niche now hosts a tall vitrine set INTO the
  recess — the alcoves became real display spaces, not empty pockets.
- #3 ENCOUNTER ORDER: order_score = 1 - (fraction of nearest-neighbour walk
  steps that move AWAY from the exit); rewards a progressive spawn->exit
  route over criss-cross. Champions score 0.56-0.67; DRAMA shifted to the
  AXIS form (spine wall funnels a clean forward walk, order 0.6). Reach and
  approach stay 1.0 everywhere.
All 12 champions recaptured, pathfinder-clean, /gallery-dna refreshed.
Backlog now: 8 of 18 struck. Remaining high-value: #2 wire walk_evaluator,
#12 Curator->genome loop, #16 physics walkability probe.

## 2026-07-08 — THE LOOP CLOSED: curate_gallery.py (#12)

Work Package 3 in one command. tools/curate_gallery.py:
  brief + taste + cast
    -> EVOLVE a champion room for the taste, pick the best-fitting by slots
    -> FURNISH by measured footprint (small->plinth, wide->table, flat->wall;
       giants flagged for own room; hospitality kept)
    -> Curated_<id> map + doc/exhibitions/<id>/curation.json decision record
       (brief, taste, champion genome, every match, oversized, unplaced,
       learning curve, a SIEVE pass whose dark_spot names exactly the works
       the footprints couldn't seat).

Demo: Curated_RandomnessEdge ("From a fair coin to the incompressible",
intimacy, 10 randomness works) -> a basilica nave, 9 placed by fit
(RAND book -> large slot, butterflies -> wall, coin/dice/galton/entropy
-> plinths in the aisles), trng_vs_prng flagged for its own room.
Pathfinder-clean, captured. The whole arc runs end to end: brief ->
architecture -> collection -> walkable exhibition, every step measured,
the human ruling on top.

Backlog: 9 of 18 struck; the three pillars (evolve / furnish / curate) all
land. Remaining are refinements (walk_evaluator wiring, physics probe,
light strips, mezzanine) and the human loop (walk-verdict re-weighting).

## 2026-07-08 — THE SPINE MUSEUM: the curriculum as one building (museum composer v1)

Greenlit after two batches of famous museum floor plans. The lesson: a great
museum is department-WINGS around a ROTUNDA, threaded by a numbered TRAIL (=
the curatorial sequence), zoned by theme. Ada's spine IS that.

tools/spine_museum.py: entrance ROTUNDA (incompleteness_scale monument + info
+ exit/fire signs) opening onto a boustrophedon GRID of 24 DEPARTMENT wings,
one per spine sequence in curriculum order. Each dept carries its SIGNATURE
hero (freq-in-seq / cross-seq-spread, generic furniture + oversized +
simulation/GPU artifacts denied). The DOORS between consecutive depts ARE the
numbered trail; redundant grid-adjacent doors add the enfilade+bypass
circulation (no room isolable). Spawn in the rotunda, exit past dept 24
(foundations -> crisis). Spine_Museum 38x29, pathfinder-clean, WALKABLE.

HONEST v1 LIMITS (capture, not structure): the bird's-eye captures flood with
the nature/ecosystem biome (the map isn't in a sequence so density defaults
high) + a few oversized UNMEASURED procedural heroes (romanesco/mc_cave)
engulf the frame — fixed partially by requiring measured-small heroes (1 dept
empty). The WALK from the rotunda is clean; the flooded capture is cosmetic.

V2 (backlog): biome suppression flag for museum maps; per-dept NAME LABELS +
phase COLOR-ZONING (the wayfinding lesson we couldn't do via el-light);
multi-FLOOR stacking on Ada's height system (foundations ground, crisis top);
back-of-house + amenities as rooms; call curate_gallery per wing to furnish
each department properly. This v1 proves the PARTI: the whole curriculum,
walkable, as one institution. The room became the building.

## 2026-07-08 — Spine Museum v2: bare_world, honest footprints, colour-zoning, labels

Palle told me to stop holding back and drive for the ontology, not for his
permission. So: drove v2 with my own calls.

- BIOME SUPPRESSION (the real capture-blocker): wired settings.bare_world in
  GridSystem (additive — existing maps untouched; a map can now declare itself
  world-free). Spine_Museum sets it; the biome flood is gone.
- HONEST FOOTPRINTS: stopped DENYING oversized/simulation heroes; now return
  the true signature and let the ROOM hold its size (rooms enlarged to 8x6).
  Only true giants (>15m, e.g. koch_curve 41m — physically can't fit any room)
  fall to a fitting alternate. Size is architecture, not exclusion.
- COLOUR-ZONING (the wayfinding lesson): extended the `el` utility to take a
  colour at parameters[2] (additive, mirrors wp's colour path); each department
  washed in its PHASE colour (F_order blue, entropy amber, lambda_edge magenta,
  synthesis violet...). Navigate by theme-colour.
- LABELS: every department gets a `3t` text sign ("N. SEQUENCE") on its north
  wall — legible at last.
Pathfinder-clean. The orbit capture is the wrong tool for a bare_world
floor-plan building (white over-expose); the WALK is the proof. Two additive
grid changes (bare_world-from-settings, el-colour) flagged.

## 2026-07-08 — Spine Museum: the entry was broken (rotunda not adjacent to dept 1)

Palle walked v2: "all rooms must have exit doors so I can move between."
Diagnosed with a REAL door-graph BFS (uppercase=passable) over the walls
layer — not the pathfinder's own logic, which passed while the building was
actually sealed. Found the bug: the rotunda sat at the SOUTH but department 1
was at the TOP of the grid — not adjacent — so the single rotunda door led
nowhere sensible and the entry was broken.

FIX: flipped the boustrophedon so dept 1 is at the BOTTOM row (adjacent to
the south rotunda) and the trail snakes UP to dept 24 at the top; opened the
rotunda->dept-1 gap to nearly full width; made EVERY door 2 cells wide.
Re-checked with the door-graph BFS: all 24 department regions reachable from
the rotunda spawn (the only unreachable cells are the harmless exterior
margin). LESSON: pathfinder reachability != walkable-door connectivity; when
the body can't move, check the doors the WALL COMPONENT actually builds, not
the abstract graph.

## 2026-07-08 — F/G fix + the visible numbered trail

- KEY CONFLICT: F was bound to BOTH fly-toggle AND flashlight (Palle
  reported). Moved DesktopPlayer fly toggle to G (glide); F stays flashlight.
- KEEP UPDATING THE GALLERY: the numbered trail made VISIBLE (Smithsonian
  1->N lesson) — a warm-gold breadcrumb light (el:1.6:0:#ffcf7a, reusing the
  el-colour I added) at each consecutive-department threshold in the Spine
  Museum, so the curriculum order is legible as you move through the doors.
  Pathfinder-clean.

## 2026-07-08 — /space-automation: a URL for the recent creations

New encyclopedia page /space-automation collects the whole arc: the
4-stage pipeline (evolve/furnish/curate/compose), the SPINE MUSEUM drawn as
its true color-zoned floor plan from spine_museum.json (24 phase-coloured
departments + gold trail + rotunda — legible where the biome floods the
in-engine capture), and photographs (Curated_RandomnessEdge, Furnished_CAP_1,
Spaceform Court/House/Ship). Also: fly now moves toward the mouse-look
(pitch included); fly toggle F->G (F is flashlight). Verified in preview:
4 stages, 24-dept SVG, phase legend, 5 photos, 0 broken images, no errors.

## 2026-07-08 — the full BED vocabulary + footprint selector + guaranteed aisles

"Keep understanding and integrating all footprint beds... with player
pathfinding between artifacts. Still consistent space." Driving it:

- BEDS (exhibit_furniture): added pit (sunken well + up-light, the well
  posture), panel (wall-hung flat, MoMA shadow-gap), floor_work (flush pad +
  gold don't-step studs, the terrain posture); platform grew s/m/l/xl. Now
  the full staging vocabulary: plinth · hollow · table · platform · pit ·
  panel · floor_work · vitrine · cabinet, all sized. (Compile guard caught a
  Variant-inference on the dict.get() sizes — typed.)
- SELECTOR (tools/staging_beds.py): measured footprint -> the right bed.
  fp1 small->plinth; tall->vitrine; fp2->table (hand-height) or platform;
  fp3-4->platform/floor_work; fp5-7->floor_work; >7 floor_work + OVERSIZE
  (own room). Lies-flat (h<0.35: mosaic/dish) -> floor_work; graphic-by-name
  -> panel (wall); well/pool/reflect -> pit. Verified across a varied cast.
- CONSISTENT SPACE + PATHFINDING (tools/stage_gallery.py): a cast laid on a
  uniform lattice (pitch 3 = bed cell + 2-cell aisle), wall works on the hull;
  a door-graph BFS confirms every bed reachable with a free approach.
  Staged_Wings (the 16): each on its measured bed, 16/16 reachable with
  clearance, pathfinder-clean, captured (bare_world = readable).

The bed fits the footprint; the aisle fits the body.

## 2026-07-08 — the bed BRAIN wired into furnish + curate (bed carries artifact)

Wired staging_beds.select_bed into furnish_gallery + curate_gallery.
Architecture: the BED CARRIES THE ARTIFACT — one token per cell, the bed,
mounts the artifact on its surface. Added a `mount` config to
exhibit_furniture: after building the bed it loads the artifact by
lookup_name from the registries and seats it at the bed's _surface_y()
(plinth 0.99, table 0.8, platform 0.31, pit -0.28 in the well, floor_work
0.05, panel on the face). furnish/curate now emit
`<bed>#mount:<artifact>` per work, bed chosen by measured footprint.

Verified: Furnished_CAP_1 capture shows artifacts SITTING ON their beds —
pendulum-waves on a table, newton_cradle on its stand, the petri dish on a
platform, coins on plinths, the flat piece on a floor pad. Curated_
RandomnessEdge: RAND book on a platform, the rest on plinths, pathfinder-
clean. One bed brain now drives the whole pipeline: evolve -> furnish/
curate (select_bed) -> the right body under every artifact, aisles between.

## 2026-07-09 — /map-viewer: the fast eyes (three.js)

The JS question answered in architecture: Python = brains, GDScript = the
VR body, and now JS = the fast eyes. /map-viewer renders ANY map_data.json
(all 1773 maps, live via /api/maps?raw=1) in three.js in milliseconds:
instanced floor with grid, walls layer with door openings + lintels, the
wedge as correct ramp geometry (rotation honoured), colored el lights as
real point lights, spawn/teleporter rings, 3t signs, and BEDS with their
#mount artifacts as labelled markers. Orbit + pointer-lock walk modes.
Verified: Furnished_CAP_1 25x18 renders with all 20 interactables.

This kills the iteration bottleneck that cost us hours yesterday (15s
headless captures, biome floods, camera framing): generate -> SEE in
milliseconds -> re-weight. The lock-in answer made real: the portability
lives in the data contract (map_data.json), so eyes in JS, brains in
Python, body in Godot.
- 2026-07-09 12:13 · **build** · DRIFT RECONCILED 22->24: symmetry (after transformation — motif/translate/reflect/rotate/glide/group/the-seventeen; Alhambra+Escher voltage) and formfinding (after forces — gradient/catenary/soap-film/relaxation/equilibrium/annealing) folded into arc.json (builds_on wired: array_tutorial<-symmetry, wavefunctions<-formfinding), ARC.md, FINISH_LINE (24 rows, both new chapters baseline MET 9/9), heroes-timeline SPINE, all manifests rebuilt. Two-account note: the other session added the sequences+baselines+seeded heroes and ENRICHED the walked pages (the weavers' erased labor in Array_Patterns; the drapers in Cloth_Physics) — chapters.json now reads 9 chapters / 66 developed pages. The two Claudes are writing the same book without collision.
- 2026-07-09 12:23 · **build** · THE TEMPLATE LAB /template-lab — the Three.js shortcut made operational. Genome controls (form/shell/hull-work/furniture from gallery_evolve's genome) -> Compile writes TemplateLab_Live -> the embedded Three.js map-viewer (walk WASD + fly G) reloads in SECONDS: template research at browser speed instead of Godot-capture minutes. Evolve button runs the genetic engine per taste (capacity/drama/intimacy) and loads the champion into the controls; dice = random genome. tools/template_lab.py IMPORTS gallery_evolve untouched (two-account safe) + overrides the quick reachability check with the REAL pathfinder when it passes (the -100 false-fail on terraced forms). Verified end-to-end: basilica compile fitness cap 45/drama 42/int 47, 17 slots, viewer serves it. ANSWER to the JS question: map=data JSON is the no-lock-in decoupler; Three.js = the research eyes (fast, and the ghost IS more fluent in TS), Python = the evolution engine, Godot = the truth/runtime. Research in the browser, verify in the engine.
- 2026-07-09 12:29 · **dig** · AUTO-RESEARCH (hybrid tournament, tools/template_hybrid_research.py): do cross-taste hybrids beat the taste specialists? TWO SEEDS, SPLIT VERDICT (11: specialist SPEC_INT 55.6; 23: hybrid CAPxINT 60.02 vs 52.92) — but two patterns held on both: (1) capacity×intimacy is THE productive cross (CAPxDRA children crater on intimacy — long vistas are intimacy's enemy); (2) the intimacy champion is the strongest specialist-generalist (its fitness carries the walkability terms). RECOMMENDATION: let capacity+intimacy populations exchange migrants; keep drama isolated as a true specialist. Generalist champion (basilica 24x13, ring podiums, niches/4, 3 floating walls, dramatic) walkable at /map-viewer?map=TemplateLab_HYB_GEN. Report: doc/reports/template_hybrid_research.md
- 2026-07-09 12:57 · **dig** · AUTO-RESEARCH round 2 (migration A/B, tools/template_migration_research.py): does capacity<->intimacy MIGRANT EXCHANGE breed better generalist rooms than isolated populations? THREE SEEDS, SAME-RNG A/B: MIGRATION WINS 3/3 (55.6->60.5, 53.6->60.1, 46.8->53.4 — consistent +5..+6.6). The round-1 hypothesis is now a PROVEN RULE. Champion (basilica 26x16, ring podiums, niches/3, 3 floating walls, bright; pathfinder OK) walkable at /map-viewer?map=TemplateLab_MIG_GEN. Rule made USABLE without touching the other session's engine: template_lab.py --evolve --profile=generalist (+ 'generalist (migration)' in the /template-lab taste dropdown; API passthrough verified, score 59.6). ALSO fixed template_lab's silent ge.evolve mis-unpack (same bug that crashed round 1 — it was falling to the fallback via except:pass). HANDOFF to the space-automation session: fold migration into gallery_evolve proper when convenient; the A/B harness is ready to re-verify.
- 2026-07-09 13:05 · **dig** · AUTO-RESEARCH round 3 (verticality, tools/template_vertical_repair.py): PREMISE FALSIFIED — the honest outcome. Survey (400 genomes, 5 seeds): flat 224/224 pass, terraced 174/176 — verticality was never blocked; the 2 failures are WALL-SEALED pockets (same-height cells closed by route/hull walls), a real ~1% generator bug. Repair operator built (dh=0 carve DOOR / dh=1 wedge / dh>=2 step+wedge): 2/2 sealed rooms fixed, by doors alone. Re-run with repair reproduced round-2 EXACTLY, exposing the overlooked fact: the migration champions were ALREADY terraced (step_down basilica, 3 levels, 6 wedges) — TemplateLab_MIG_GEN is the terraced champion; height already wins. SIDE-FINDING: compile_gallery seeds route-walls on len(gid) — score and write with the SAME gid or the fitness describes a different room than the disk map (handoff: seed on genome, not id). METHOD LESSON: check the premise against the champion before building the fix.
- 2026-07-09 13:32 · **build** · THE WALL KIT (tools/wall_kit.py) — Palle's insight made operational: Wang-tiles for rooms. Every 8x8 block honors ONE EDGE CONTRACT ('turn the corner to the middle'): boundary cells return to sea level h2 + a centred 2-cell gate on each side (cells 3-4) = the same out-meeting-point — so ANY block sits next to ANY block and maps are seeded by dice-throw, connectivity guaranteed BY CONSTRUCTION not by checking. Seven starter blocks carry the session's earned vocabulary: field, pinwheel (Barcelona sliding walls), court (sunken h1+wedges, Raumplan), ledge (h3 overlook+stair), street (Uffizi niche wall), cross, colonnade. Seeder: --grid=CxR --seed=N -> stitched map, spawn first block, teleporter (on void) last, hull sealed. VERIFIED: 4 maps incl 4x3/12-room all pathfinder-clean, first throw 384/384 reachable. NEXT: varied sockets (real WFC), per-sequence block palettes, kitbash prop dressing per block, blocks into gallery_evolve's genome.
- 2026-07-09 13:38 · **build** · WALL KIT v2 — the full enclosure family (Palle: 'one, corner, all sides, 3 part, 4 already in'): every block = interior feature x enclosure class x rotation. Classes: open[gggg] / one[sggg] / corner[ssgg] / channel[sgsg] / three[sssg], rotated at seed time. Solid sides could seal regions, so the seeder carves a RANDOM SPANNING TREE over the block grid first and forces tree seams gated on both sides — connectivity stays BY CONSTRUCTION while every other seam is free to go solid (poché seams, U-alcoves, corridors). 4 maps seeded+validated (WallKit2_7/42/99/Big 4x3). The kit now spans: 7 features x 5 enclosures x 4 rotations = 140 block variants from 12 authored pieces.
- 2026-07-09 13:39 · **build** · WALL KIT v3 — the most important space added (Palle): 'none' [oooo] = NO walls that create a room; place made by floor, level, pillars, things — weighted HIGHEST (4). Plus 'veil' [ogog] = flow one axis, doored the other. Seeder refined: the spanning tree only UNSEALS solid sides, never adds a wall to an open one. Emergent wins already visible: street x none = a freestanding bay wall in open space (the Barcelona plane, un-authored); colonnade x none = hypostyle flow across seams; court x none = a room made purely of level. 7 features x 7 enclosures x 4 rotations = 196 variants. WallKit3_13 + WallKit3_Field validated.
- 2026-07-09 13:46 · **build** · THE MISSION GRAPH (tools/mission_graph.py) — the reframe acted on: Dormans' mission/space decoupling, and Ada already HAD the mission — the chapter baseline. beats -> rooms on a serpentine spine (gate to prev, gate to next: the walk IS the lesson order); voltage -> side-chapels (ledge=shrine/court=pit) hanging off their beat — the branch points where the critical trajectory leaves the tutorial; cast artifacts stand in their rooms; unused cells VOID so the map's silhouette is the mission's silhouette. Features chosen by role semantics (arrive->field, trace->pinwheel, grid->street, measure->colonnade, build->court). VERIFIED: Mission_Primitives (10 beats+5 voltage, 12 rooms, 15 artifacts, pathfinder OK — 12 unreached cells = the colonnade pillars by design) + Mission_Randomness (coin_toss -> random walk -> galton -> shannon(+prng chapel) -> seed -> trng(+hardware-decay chapel) -> pollock court -> monte carlo(+RAND-1955-book chapel)). THE FUSION: the book's teaching arc and the map generator are now ONE structure — walked.md describes what mission_graph builds. Reframe candidate for next round: ACT-HALLS — group beats into acts sharing one wall-less hall ('none' enclosure), mission topology at act level, flow within acts.
- 2026-07-09 13:52 · **build** · ACT-HALLS (mission_graph --mode=halls, now default) — the mission graph married to the wall-less principle: beats grouped into 3-4 ACTS, each act ONE open hall (blocks flow, 'o' seams; features stand as STATIONS — street becomes a freestanding plane, court a sunken area, colonnade a pillar rhythm); ONE door between consecutive acts (commitment between acts, flow within); and the inversion that makes it QFEP: voltage chapels = mk_chapel (new wall_kit feature, 4x4 hut + ONE door + aisles around) = THE ONLY ENCLOSED ROOMS in the hall — the tutorial streams past, the critical turn requires stepping through a threshold. MissionHall_Primitives: acts [6,4,5], all 5 chapels inline (folding_past, fontana_puncture, klee_walking_point, durer_scene, becoming_catalyst as huts among the stations) — chain mode fit only 2. MissionHall_Randomness clean too. Reframe candidate next: VERTICAL ACTS — each act-hall a level lower, act doors become stairs; walking a chapter = descending the dig.
- 2026-07-09 13:59 · **build** · HALF-LIFE WALLS (GridWallSegmentsComponent 'labwall' style — additive, gated on settings.wall_segments.style): every wall segment now composes as dark metal KICK PLATE (hovering 6cm above the floor, the momo shadow-gap accent) + concrete PANEL + dark GROOVE line + EMISSIVE AMBER ACCENT line + dark TOP TRIM; short pieces (lintels) stay dark slabs so doorframes read as frames. Full grid discipline: headless compile-check OK, live capture of MissionHall_Primitives shows the composition (panel/amber/trim legible in iso+top), NEGATIVE TEST: Furnished_CAP_1 (no style key) renders byte-identical plain — the style only bites with the new data. wall_kit + mission_graph now emit the style; all kit/mission maps regenerated. FLAGGED (separate issue): some mission-room casts render enormous (player_trace ribbon, marching-cubes mass) and drown the halls — needs scale governance / bed-based casting.
- 2026-07-09 14:05 · **build** · GODOT->GLTF->THREE.JS WALL KITBASH: commons/testing/export_wall_gltf.gd authors TEN labwall segment variants IN GODOT (plain/glass/whiteboard/display/conduit/vent/hazard/locker/rib/beam — shared chassis: hovering kick + amber emissive accent + dark trim; PBR materials incl. emissive + alpha glass) and exports each as .glb via GLTFDocument headless (watchdog, 4s) -> ada_encyclopedia/public/wall-gltf/ + manifest.json with seeding weights. map-viewer now loads the library once (GLTFLoader) and SEEDS a variant per wall edge deterministically (FNV hash of map:x,z:edge -> weighted pick) — every wall run varies, stable across reloads; doors keep lintels; missing library -> plain-box fallback untouched. The asset pipeline is now: author in the ENGINE (truth materials), research in the BROWSER (instant walk). Verified: 10 glbs 6-8KB, page+manifest+glb all 200.
- 2026-07-09 14:37 · **build** · WALL FAMILY GROWS TO 14: +window (waist-to-head glazing, sill, posts), +slit (narrow full-height glass — the guarded glimpse), +doorframe (jambs+header+AMBER THRESHOLD line — now placed on every DOOR edge instead of the bare lintel box), +corner (dark column + amber ring + cap — the viewer detects where horizontal and vertical wall runs share an endpoint and posts the column there). Weight-0 = placed-by-role, never random-seeded. Re-exported headless (5s), viewer wired, all 200. Palle: 'that was a good move' — the Godot-author -> glTF -> Three.js-seed pipeline confirmed as the pattern.
- 2026-07-09 14:44 · **build** · PER-ACT WALL PALETTES — atmosphere as data: mission_graph emits settings.wall_segments.palettes (zone rects in cell coords + per-zone variant weight tables) and the map-viewer seeds each act's walls from its own table. The act arc as material register: ARRIVAL bright (glass/whiteboard/window), WORK serviced (conduit/display/locker), DEPTH industrial (hazard/rib/beam/slit). Descending a chapter now changes the walls around you — topology (act doors), light-register (palettes), and the critical chapels all driven by the same baseline. MissionHall_Primitives: act0 arrival rows0-7, act1 work rows8-15, act2 depth rows16-23; pathfinder OK; viewer 200. Zone-less walls keep global picks; maps without palettes unchanged.
- 2026-07-09 14:53 · **build** · LIGHT PALETTES IN VR — the act registers now shift the wall's emissive line in the ENGINE: GridWallSegmentsComponent reads settings.wall_segments.palettes (zone rects + accent [r,g,b]) and builds per-zone accent materials; mission_graph emits accents (arrival AMBER 1,.62,.18 / work CYAN .25,.85,1 / depth RED 1,.25,.15). VERIFIED DETERMINISTICALLY: capture cameras kept framing the amber end (the known camera-treachery), so commons/testing/probe_light_palettes.gd builds WallKit_LightStrip headless and counts accent materials by emission color -> {amber:12, cyan:8, red:12} = 3 distinct zones, exact edge counts. The probe + fixture stay as the regression test. One baseline now drives: topology (act doors), chapels (voltage), variants (web palettes), and LIGHT (VR accents).
- 2026-07-09 15:03 · **build** · CAST GOVERNANCE + 5M HALLS (mission_graph): (1) the size oracle rules the room — resolve_cast checks grid_cells+height against the room budget (stations 4 cells/3.4m, chapels 2/2.8m): platonic_grabbables(7x5)->grab_trihedron, laser_measure(50m tall!)->scale_lines, pollock->random_object_spawner, you_are_here->origin; RUNTIME_GROWERS list catches player_trace (static AABB=0 but grows a giant ribbon live)->mystic_writing_pad; known-bad casts with only unmeasured alts take the curator's first alt blind. (2) Hall blocks 8->14 cells: feature blitted centred leaves a >=5m clear ring on all sides — the placing space. Chain mode regression-checked (still 8). Both mission maps pathfinder-clean at 84x42/70x42. HONEST NOTE: the crystal mass still dominating captures is the BIOME FLOOD (known capture phenomenon — present in castless WallKit captures too), NOT a cast; the Three.js viewer renders map layers only and is the clean eye. Chip task_6f2a3bce resolved in-session.
- 2026-07-09 15:17 · **build** · ACT-HALLS v3 (Palle, from the /map-viewer screenshot: 'different size halls, and the middle room fills so the hall wall towards the wall is two m'): acts are now SINGLE HALLS of varying depth by register (arrival 12 / work 16 / depth 14) and each entry's feature SCALES to F=depth-4 — a 2m aisle to every wall, stations at F+2 pitch. Features redrawn parameterized (court pit F-4, ledge overlook, street wall, colonnade pillars, pinwheel arms scale; chapel hut STAYS 4x4 intimate whatever the hall). One door carved between acts near the walking end. EMERGENT WIN: platonic_grabbables (7x5) returned to act 3 — the 16/14-deep halls' budgets host big casts, so hall size and cast size govern each other. Primitives 62x42 (denser than v2's 84x42), swaps logged in map_info.mission_graph.swaps, both maps pathfinder-clean.
- 2026-07-09 15:21 · **build** · INNER CORRIDORS 2m->3m (Palle, walking the v3 halls): AISLE=3 constant governs both feature-to-wall and station-to-station gaps; hall depths bumped +2 (arrival 14 / work 18 / depth 16) so features keep their size (F = depth-6 = 8/12/10). Primitives 69x48, both maps pathfinder-clean. One dial now: AISLE.
- 2026-07-09 15:22 · **build** · THE ROOMS IN THE CORRIDOR grow (Palle clarified: not the aisles — the huts): chapel rooms 4x4 -> 6x6 (min(6, F-2)) with a 2-wide door facing the walk; chapel cast budget up to 4 cells / 3.0m. The voltage rooms now read as ROOMS you enter, not boxes you peer into. Both mission maps regenerated + pathfinder-clean.
- 2026-07-09 15:24 · **build** · AISLE back to 2m (Palle: keep the corridors OUTSIDE the rooms tight/dense) — the 6x6 chapel ROOMS stay big, the aisles around everything go back to 2m; features grow to fill (F=depth-4=10/14/12). Density restored, rooms kept. Both maps pathfinder-clean.
- 2026-07-09 15:33 · **build** · THE WALL FIXER (map-viewer) + aisles=3m (the 'not'->'now' misread corrected): Palle's screenshot showed OVERLAPPING INCONSISTENT wall patterns — root cause: shared seam edges declared by BOTH cells drew TWO modules with different variants on top of each other (Godot dedupes via _edge_key; the Three.js port didn't), plus per-edge random seeding made neighbours clash. Fix = two-pass composer: (1) DEDUPE edges into normalized keys (h/v:x:z, door if either side says door, min baseY); (2) group into RUNS (contiguous colinear walls) and compose coherently — ONE base variant per run, one rhythmic accent every 4th module on runs >=5, doorframes at doors, corner posts at run meetings. Runs are also the future attachment structure for wall PROPS. Scale overlap 1.04->1.02. Viewer compiles (200); mission maps regenerated at AISLE=3, pathfinder-clean.
- 2026-07-09 16:45 · **fix** · NO-FLOOR BUG (Palle: 'MissionHall_Primitives has no floor in godot') ROOT-CAUSED + FIXED: GridStructureComponent sizes its floor-build loop from map_info.dimensions (JsonMapLoader defaults width/depth to 0 when absent) -> generated maps WITHOUT the dimensions key build ZERO floor cubes while walls render fine (walls iterate their own layer). wall_kit + mission_graph now emit dimensions {width,depth,max_height}; all 15 existing generated maps patched in place. VERIFIED by capture: the full building renders — floor plate, labwall runs, 6x6 chapels (Durer polyhedra tidy INSIDE one — cast governance visibly working), pinwheels, court, corner posts. RULE for map generators: map_info.dimensions is LOAD-BEARING in Godot; the pathfinder and the Three.js viewer both ignore it, so they can NOT catch this class of bug — engine load-test is the only witness.
- 2026-07-09 16:54 · **build** · MARRIAGE 2 DONE — WALL RUNS ARE DATA (tools/wall_runs.py): the viewer's render-time fixer promoted to a per-MAP compile pass writing settings.wall_runs {runs:[{axis,x,z,len,baseY,modules:[{v,door}]}], corners:[{x,z,y}]} into map_data.json. Composition rules now canonical in ONE place (FNV run-seeded base + rhythm accent every 4th + doorframes + corners). Generators annotate at build; --all-generated compiled 15 maps (MissionHall_Primitives: 91 runs/78 corners; the 69-module hull run reads locker/locker/vent/locker...). Viewer OBEYS the data when present (exact placement, no re-derivation), falls back to in-viewer fixer for un-compiled maps. PALLE'S CAVEAT honored + confirmed: all machinery is MAP-LEVEL (each map's own JSON) — the sequence-sized MissionHall maps are one CLIENT of it; per-beat/per-act maps and hand maps get runs identically. WHAT THIS UNLOCKS: 'run 7, module 2' is now an addressable place — wall props (hangar pieces, extinguishers, infoboards, /props-dna-gallery artifacts) attach to run slots next; Godot labwall can read the same data (marriage 3).
- 2026-07-09 19:07 · **build** · MARRIAGE 1 DONE — BEDS STAGE THE MISSION (mission_graph x staging_beds): cast placement now goes through select_bed, same convention as furnish_gallery — exhibit_furniture#kind:<bed>#size:<s>#mount:<artifact>. The primitives hall staged itself by measurement: origin->table_2m, draw_dot/klee/fontana/mystic_writing_pad->plinth_m, line_builder->platform_l, dgrid->platform_m, snap_tetrahedron_puzzle->VITRINE_TALL, platonic/durer/folding_past->floor_work_l, becoming_catalyst->floor_work_s. Wall-hung graphics (is_wall) hang on the hall's north wall facing in (:180). Size governance (resolve_cast) chooses the ARTIFACT, beds choose its BODY — two oracles, one pipeline. In-engine capture confirms. THE THREE MARRIAGES: 2 done (runs=data), 1 done (beds), 3 remaining (Godot labwall reads wall_runs variants = one wall library).
- 2026-07-09 20:13 · **build** · MARRIAGE 3 DONE — ONE WALL LIBRARY (loop ran to green): commons/grid/WallVariantLibrary.gd = the 14 variant builders extracted as a shared class with build(name, accent) so light palettes color the emissive line per zone. export_wall_gltf.gd is now a thin consumer (exports the SAME builders as .glb for the browser); GridWallSegmentsComponent consumes settings.wall_runs + the library: engine boxes stay COLLISION, library nodes are the VISUALS, doorframes replace jamb/lintel visuals (collision kept), corner posts from compiled corners, zone accents applied at build. LOOP LOG: parse error (var c shadowing) caught by check-only -> fixed; probe fixture gap (settings never passed to initialize -> variants:off) -> fixed, probe now exercises the marriage-3 path and matches DECLARED palette accents: 3/3 (14 amber/8 cyan/14 red, corners included). CAPTURES: MissionHall_Primitives IN GODOT shows the browser's walls — glazed window runs, locker/rib rhythms, hazard-striped depth hull, red-accented deep chapel, doorframes at act doors. NEGATIVE: Furnished_CAP_1 (no style/runs) renders identical plain. ALL THREE MARRIAGES DONE: runs=data, beds=staging, one library. A wall looks identical in headset and browser because it IS identical.
- 2026-07-09 23:45 · **build** · THE WALL-PROP PASS (tools/wall_props.py) — hospitality on the run slots: deterministic composer reads settings.wall_runs and places REAL lab props as interactables facing into the room (rotation by wall side, furnish convention): fire_extinguisher/exit_sign beside every DOOR module (safety lives at thresholds, alternating), one register prop at the 1/3 point of runs >=7 (arrival->info_board, work->fire_hose_box, depth->fire_extinguisher); never against glazing/displays; only into free floor cells. Placements recorded in settings.wall_props (legible+undoable) then materialized. MissionHall_Primitives: 8 props (4 hose boxes, 2 info boards, 2 extinguishers), pathfinder-clean, visible in-engine capture as the red service dots along the walls. Generators wired (wall_runs -> wall_props chain). The full stack now: baseline -> topology -> rooms -> beds -> walls -> variants -> light -> PROPS, all map-level data.
- 2026-07-09 23:50 · **build** · PRINCIPAL ARTIFACTS v1 (Palle: 'harmonize the interfaces... sync down into the map's footprint knowledge') — the type is the spatial truth: commons/data/principal_artifacts.json declares 8 principals (5 substrate bases + step_sequencer + composition_artifact + mesh_grammar), each = ONE canonical scene + harmonized interface[] contract + DISPLAY footprint (pose/cells/height) + preferred bed. tools/principal_artifacts.py: --audit found 20 shared-scene families (composition_artifact 17 instances UNMEASURED, grid3d 9 unmeasured, substrates mismeasured lying flat h=0.02) — per-instance AABB is the wrong oracle for instanced types; --sync-footprints wrote 136 instances into artifact_sizes.json (source:principal). PROOF the knowledge flows down: staging_beds instantly re-decides — grid2d_life 0.02m-plate->PLATFORM, grid3d_bfs invisible->platform_l, living_paper->plinth_s. This is 'improve the type, not the room' at the ARTIFACT level: staging_dna governs the room around, principals govern the thing itself. ROADMAP in the declaration: interface[] = migration contract for drifting instances; single-instance displays (info_display, image_billboard, science screens) can become instances of a display principal next.
- 2026-07-10 08:16 · **build** · THE PRINCIPAL GALLERY (/principal-gallery — Palle: 'a better way to start: DNA galleries of all similar artifacts') — SEE the family, THEN rule on the type: tools/build_principal_gallery.py lays out all 20 shared-scene families (183 instances) as DNA — BASE genes (identical across instances, gray) vs CARTRIDGE genes (the axis of variation, fuchsia) — with the principal declaration (display truth + bed), per-instance footprint status (principal green / measured blue / MISSING red), interface-drift lines (amber), and captures where they exist. The living_paper card = the argument in one view: one scene, 34 cartridges (julia/koch/mandelbrot/rule110/voronoi...), one inherited 1x1x1.3m truth. The drift lines already point at the next overhaul: per-instance spatial_needs/spatial_profile keys are exactly what principals should absorb. RULING ORDER corrected: gallery first, declarations second — my 8 declared footprints are now REVIEWABLE against the evidence instead of typed blind.
- 2026-07-10 08:25 · **build** · LOOP ITERATION 1 (principal-artifacts, /loop every 20m job 316ba86c) — KIN FAMILIES: beyond shared-scene principals, the gallery now surfaces interface-similar artifacts with SEPARATE scenes/scripts via three detectors: DECLARED (principal_artifacts.json kin{} — wave_examples x6, the NOC oscillation family), STEM (pattern_machine_a..d x4 — same idea, four separate .gd files!), SUFFIX (*_workbench x15!! — VectorWorkbench/ca_rules/cantor_diagonal/complexity_dial/csg_compose/halting/riemann_sum/russell_paradox/shannon/tangent_slope/transform_composition/triangle_curvature/verlet/qfep_balance — one bench-with-controls design pattern, fifteen implementations, ZERO code sharing; *_machine x6). 24 families / 214 instances on /principal-gallery, KIN badge (sky) distinguishes from scene families. NEXT GOAL (iteration 2): the workbench kin is the harmonization jackpot — extract the common interface (bench + dials + display + label) as a principal contract, declare display truth + beds for all 15 (they are the size of tables: ~2x1, bench pose), sync footprints. Then wave_examples the same.
- 2026-07-10 08:44 · **build** · LOOP ITERATION 2 (principal-artifacts) — KIN FOOTPRINT FILLS: evaluation first revealed the policy — kin members own their scenes so their measurements can be GOOD (VectorWorkbench 3x1x1.94 measured fine); the gap was absence: 13/15 workbenches + 4/4 pattern machines + 3 machines MISSING from the oracle entirely (beds/mission blind to shannon_workbench!). So kin sync = FILL, never override (opposite bias from principals): sync:'fill' in kin{} declarations with display truths (workbench bench 2x1x1.5/table_2m; pattern_machine standing 2x1x2.0/panel; machines placeholder 2x1x1.8 FLAGGED heterogeneous — random_walker measures 21x21!). 20 fills, wave_examples untouched (all measured). Proof: staging_beds now stages shannon/halting/pattern_machine_a/godel correctly. NEXT GOAL (iteration 3): verify the fill values against scene TEXT (parse workbench .tscn box sizes — static analysis, NO Godot runs in unattended loop iterations while Palle may be in VR / two-instance rule) + declare the 'displays' kin (info_display/image_billboard/science screens).
- 2026-07-10 09:04 · **build** · LOOP ITERATION 3 (principal-artifacts) — REGISTRY AS SANITY CHANNEL + THE DISPLAY KINS: cross-checking kin fills against registry evidence CORRECTED them (shannon/verlet spatial_needs.footprint_cells=4 -> workbench 2x2 not 2x1; pattern_machine_a footprint [2,3,2] -> 2x2 x 3.0m not 2.0); sync fill-mode now re-writes its OWN kin-fill entries so corrections propagate (real measurements still never touched). Palle's display population split into TWO kins: 'displays' (10 standing screens MISSING from oracle — image_billboard/computer_monitor/station_multiscreen/... — filled 2x1x1.6 pending capture-measure) and 'readouts' (the 1x1x0.2 HUD text-block family, all measured — grouped for the canonical-UI TextScreen migration). 30 kin fills total; 26 families / 235 instances on /principal-gallery. NEXT GOAL (iteration 4): systematic --verify (registry spatial_needs/footprint vs oracle for ALL principals+kin, conflicts into the gallery) — then the unattended data phase is complete; remaining work needs Godot (batch instance captures) or Palle (workbench interface contract ruling), at which point the loop should hand off and delete the cron.
- 2026-07-10 09:20 · **build** · PRINCIPAL GALLERY REDESIGNED to the props-dna form (Palle: 'main gallery to get to the different principal galleries, ~10 examples each'): MAIN /principal-gallery = image-first CARD INDEX (one card per family: capture, count badge, PRINCIPAL/KIN/candidate chip, display truth + bed, note; honest 'no captures yet' placeholders) linking into PER-FAMILY DNA galleries at /principal-gallery/<slug> built on the shared GalleryView component (props-dna-gallery convention: public/principal-dna-<slug>/manifest.json, <=10 image-first entries, PALLE VERDICTS ENABLED via /api/gallery-evals — flat slugs because the evals API rejects slashes). build_principal_gallery.py writes the manifests (prefer own capture, family fallback); 14/26 families have galleries, 12 await captures. Fixed duplicate React keys (two kin families shared the placeholder scene string). Verdicts mean Palle can now RATE instances family by family — the rulings channel for harmonization.
- 2026-07-10 09:25 · **build** · LOOP ITERATION 4 + CLOSE (principal-artifacts) — --verify + PRECEDENCE COMPLETE: systematic diff of registry spatial claims vs oracle for all 188 principal/kin members found 73 conflicts -> taught the precedence rule (per-instance registry footprint [w,h,d] BEATS type truth: Buren columns are 1x6 STRIPES, array_disco is a FLOOR — one type truth cannot cover config-dependent poses) -> re-sync with precedence dropped conflicts to 31 -> prefer_registry flag for runtime-grower kins (wave examples measured 0.2m mid-frame, registry says 22x22 room-scale) -> 28 conflicts remain = the RULING WORKLIST (doc/reports/principal_verify.json), incl. genuine registry self-contradictions (wave footprint_cells=1 vs footprint 22x22). FINAL PROVENANCE LADDER: registry footprint > measurement > principal/kin truth > missing. UNATTENDED PHASE DONE (audit/principals/kins/fills/verify/galleries) — cron 316ba86c deleted. HANDOFF: (1) Palle verdicts in the family galleries, (2) rule the 28 conflicts, (3) supervised batch instance captures (Godot), (4) workbench interface contract ruling before code merge.
- 2026-07-10 09:27 · **ruling** · INTEGRATION — THE COMMON WRAPPER (Palle): 'each artifact should have the thing that makes it feel integrated... shannon_workbench works in map space since its base is 1 and needs no plinth... many simulations are in 1m cubes — they can be harmonized.' The wrapper = the missing middle layer between artifact and bed. Vocabulary declared on every principal/kin: self / cube / frame / plinth / field. KEY INSIGHTS: (1) the 1m sim-cube is THE GRID CELL MADE VISIBLE — integration by quantization to the map's own unit, same sentence the catalyst cube teaches; (2) the wrapper is where the harmonized interface lives PHYSICALLY (one label plate, one play/pause, one cartridge slot — the hand learns one grammar); (3) declared integration PRECEDES size-guessed beds — fixes the real bug where mission staging puts platforms under self-grounded workbenches; (4) sieve caution: 'field' + apparitions stay UNHOUSED on purpose — cubing everything forecloses the uncanny. NEXT (build): a canonical sim_cube wrapper scene (glass cell + plate + accent, mounts like exhibit_furniture#mount) + placement consumes integration before select_bed.
- 2026-07-10 09:33 · **ruling** · CUBE FAMILIES (Palle, correcting the wrapper before it hardened): 'room for more cube families so we do not collapse all objects — the DNA makes the different cube families look different.' The cube is a GENUS not a uniform — the WallVariantLibrary move again: CHASSIS invariant (1m quantization, plate, mount, touch-corner, accent = the one hand-grammar) + FAMILY expression (enclosure/frame-register/base/atmosphere, legible at 3m). Five families pinned in principal_artifacts.json wrappers{}: gridglass (discrete worlds), tank (continuous physics), cage (agents — things that could get out), shadowbox (luminous curves), openframe (growth — things that should breathe). Assigned at PRINCIPAL level = WAYFINDING (read the department by its vitrine, like real museums). Anti-chaos rule: 5-8 families max, mapped to NATURES not artifacts. BUILD TARGET: CubeWrapperLibrary (Godot builders -> glTF -> both renderers, the proven wall pipeline).
- 2026-07-10 09:39 · **build** · THE SIM-CUBE WRAPPER SHIPS (CubeWrapperLibrary + sim_cube artifact + glTF export + integration-aware placement): five families on ONE chassis (base plate + label plate + touch corner + accent line + mount point = the invariant hand-grammar), VERIFIED IN-ENGINE — the CubeTest capture shows all five reading distinctly at distance: steel GRIDGLASS (grid-lined panes, grid2d_life inside), brass TANK (teal glass, hovering on the momo gap, grid3d_bfs inside), dark CAGE (bars, profile_sine glowing through), SHADOWBOX (one open face, backlit), white OPENFRAME (edges only, air). Token: sim_cube#family:tank#mount:<lookup> (exhibit_furniture's exact convention). mission_graph placement now consults INTEGRATION before beds: self/field stand bare (no more platforms under workbenches), cube -> family housing, frame/plinth -> beds. Exported to public/cube-gltf/*.glb (browser parity ready). Harmonized WITHOUT collapse — Palle's correction built: one sentence, five voices.
- 2026-07-10 09:52 · **build** · VIEWER RENDERS SIM-CUBES: map-viewer loads public/cube-gltf (ensureCubeLib, same pattern as the wall library) and renders sim_cube#family:X#mount:Y tokens as the REAL Godot-authored housings instead of bed proxies — mount marker + label above the cube; glass-box fallback when glbs missing. Verified on CubeTest: 5 interactables, cube manifest + all 5 family glbs fetched 200, canvas live. Both renderers now share BOTH libraries: walls and cubes.
- 2026-07-10 09:58 · **build** · SIM-CUBE v2 (Palle's sizing ruling: 'the cube should be 1.5 or 2 — 2 can work; all cube artifacts default on a 1m cube plinth'): housing 1m->2M on a 1M CUBE PLINTH (registry footprint [2,3,2]; mount_point risen; glbs re-exported so the viewer updates automatically). + THE SET CATALOG: commons/testing/capture_sim_cube_set.gd (props-dna rig: SubViewport 1024², fov32 AABB-orbit, three-light, filmic+SSAO+glow) -> public/sim-cube-set/*.png + manifest -> /sim-cube-set page on GalleryView with verdicts — the five families closeup: brass tank with glowing backlight, steel gridglass with grid-lined panes, dark cage, shadowbox's luminous face, white openframe — each on its plinth. GOTCHA logged: PowerShell -replace re-encoding mojibakes UTF-8 GDScript (— -> â€”); repair with python utf-8 pass.
- 2026-07-10 10:24 · **build** · STAND FAMILIES join the wrapper library (Palle: '1m and 2m table 2D display, frameless, for vertical 2D content — plus the podium principal for like the coin toss'): CubeWrapperLibrary grows to EIGHT families — the five cubes + table_display_1m/2m (slim steel legs, dark slab, FRAMELESS white panel rising edge-to-edge: pure surface for 2D content; hand-grammar on the table front) + podium (dark column, steel operating top w/ lip at 1.1m — the hand-height stand for small interactives). NEW: artifact_overrides{} in principal_artifacts.json — per-artifact integration for lone pieces outside any family (coin_toss->podium, dice_throw->podium); mission placement consumes them (integ 'wrap'), PROVEN: MissionHall_Randomness now opens with sim_cube#family:podium#mount:coin_toss — the chapter's first beat is a coin on a podium. Set catalog recaptured: 8 closeups on /sim-cube-set. All exported to cube-gltf (viewer auto-includes via manifest).
- 2026-07-10 13:33 · **build** · ORCHESTRATED ROUND (3 Opus 4.8 subagents, Palle-directed): (1) frame principals -> table displays COMMITTED after verifier PASS 6/6 + diff-reviewer no-defects (scoped commit; other session's drift left untouched — flagged their ' Furnished_CAP_1' leading-space start_map). (2) TWELVE integration-only principals declared (no display{} -> footprints stay measured): puzzles+pulsar self / grab_spheres podium / reliefs table_display_1m / metaballs+particle_field FIELD / crystals plinth / cantor shadowbox / recursive_tree openframe — batch verified by third Opus agent PASS 6/6 incl. simulated placement tokens per family. COVERAGE COMPLETE: all 26 families declared (20 principals + 6 kin). The controller pattern works: worker-data + opus-verify + scoped-commit.
- 2026-07-10 13:48 · **build** · PLINTH + FRAME GET REAL WRAPPER SEMANTICS (orchestrated: Opus worker implemented, Opus controller verified PASS 5/5): CubeWrapperLibrary = TEN families — plinth (dark column, wide foot, steel top, hand-grammar; mount at 1.0m) + frame (freestanding framed panel HOVERING between posts over a lit base bar — the momo gap as a whole piece; mount low-anchored in front of the backing). Placement: integ plinth/frame emit sim_cube#family:<own-name> (controller proved cfam never leaks via live token simulation: crystalcluster -> sim_cube#family:plinth). The verifier's backlog item from the earlier round is CLOSED — no integration value is inert anymore: self/field bare, cube/wrap/plinth/frame housed, undeclared to beds. /sim-cube-set = 10 catalog closeups (frame + plinth photograph beautifully). Worker even flagged my spec's numeric inconsistency and resolved it sensibly — the controller pattern catching upward.
- 2026-07-10 14:14 · **build** · SIM CUBES ENTER THE MISSION HALLS: swept all baselines for housed casts — qfeplaboratory (grab_sphere E/F/phi -> THREE PODIUMS: the QFEP term spheres each at operating height), cellularautomata (ca_screen -> 2 table displays), randomness (coin podium), + generated MissionHall_Qfeplaboratory/Formfinding/Cellularautomata, all pathfinder-clean. CAPTURE (QFEP hall): the whole day's stack in one frame — variant walls IN GODOT via the one library (lockers/vents/hazard stripes/window runs/doorframes), chapel rooms glazed, zone accents, and the podium columns standing in the work hall. CA hall capture = the known headless-hang class, watchdog killed it correctly (16s rule) — capture that one supervised. The wrapper pipeline is now end-to-end REAL: baseline -> integration -> housing -> engine.
- 2026-07-10 14:23 · **build** · WRAPPER-FIT GALLERY (/wrapper-fit — Palle: 'we do not need the full map; what matters is how the wrapper works with its artifact'): capture_wrapper_fit.gd shoots 15 (family, representative-artifact) PAIRS closeup — the artifact MOUNTED at its mount_point inside its housing — props-dna rig, AABB framing CLAMPED 1-4m (stray labels/particles inflated coin_toss's AABB to a dot; overshoot itself is a legible finding). GalleryView page with verdicts. FIRST FINDINGS visible without walking: grid2d_life RUNS inside gridglass (gliders on the substrate plate — lying flat, which reads BETTER than the declared standing pose: verdict material); crystalcluster+plinth = perfect; albers relief on table display = lovely; coin_toss = STAND-ON-A-STAND (brings its own pole; maybe integration self or a lower mount); grab_sphere_E nearly invisible on the podium (needs scale-up at mount or a closer camera); buren column invisible on the 2m table (mount pose mismatch). The fix loop: verdict -> library or declaration, never the room.
