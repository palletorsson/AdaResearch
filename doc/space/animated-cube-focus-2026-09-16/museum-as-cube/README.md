# The museum is also made from the box — 2026-09-16

Palle's correction: the floor, walls and cube-based prefabs should be recognised in this hall even while composition remains a later investigation. Added a short turn of attention after the folding net and before "A world without composition yet." The main walk now connects the primitive being examined to the structure that carries and encloses the visitor.

The tutorial identifies this as part of the main encounter. The technical text follows the museum's shared unit BoxMesh, instance dimensions and separate BoxShape3D colliders, and the crate's additional geometry for planks and reinforcement. The intent records this distinction without adding an artifact or changing primary roles.

Four pre-edit texts are archived with hashes. Local-source assertions and the live book API agree for all four revised sections; primary tags and footnote definitions remain intact; scoped git diff --check passes. The inspected engine and crate sources are unchanged. No runtime or placement changes were made, so the existing component probe was not rerun for this text-only amendment.
