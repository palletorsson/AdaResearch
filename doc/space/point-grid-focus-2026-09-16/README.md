# Point_Line_Grid — a place we can name again

16 September 2026. After committing the Point_Trace checkpoint, Palle asked to continue with Grid. The revised passage asks what a repeatable address makes possible. Trace already sampled and rounded movement; Grid builds on that capability rather than introducing a second story of an untouched gesture becoming discrete.

The primary route is `grab_sphere_point_snap` → `player_trace` → `grid_lines` → `plan_vitrine`. Their comparisons follow one operation into different uses: locating a representative, recording a return, interpreting a copied gesture at another scale, and arranging a floor plan. The book retains the possibility of pleasure and misuse: tolerances can help pieces meet, a boundary crossing can become a rhythm, an enlarged drawing can invite another body to follow.

All seven physical placements and every floor cell remain as before this editorial pass. The room grammar, drifting spheres and Modulor reference become secondary encounters with notes in `detours.md`. The placed vitrine anchors the book's numbered plan; its internal `simulation_grid` is not another standalone placement. The stale metadata count is corrected from eight to seven.

The earlier tutorial presented first-visit dictionary storage and an axis-by-axis route as though they were the installed walking recorder. Its replacement follows the actual ordered path and direct segments, allowing revisits and diagonals. The technical reference keeps source, display and plan coordinates distinct, and explains the continuous collider beneath the finite cells. The largely useful critical text is retained and extended around repeatable placement and bodily support.

The previous documents are copied byte for byte under `previous/`, with hashes in `previous-sha256.json`. They preserve obsolete examples and wider questions as research material, not as promises about current controls. The prior room role data is saved alongside them.

The next room is Point_Triangle_Context: connecting three positions can close a boundary, but the program must do further work to render a face. Grid prepares that distinction by separating an address, a drawing and a floor that can carry a body.

This pass changes prose, room roles and artifact-count metadata. It adds no interaction, moves no artifact and does not claim headset validation. The floor-plan exercise does not depend on the vitrine's additional VR drawing behavior.

## Verification of the first revision

The revised chapter is 890 words, down from 1,122. Its four artifact markers exactly match the actual necklace resolver's primary cards in order; all seven cards remain. Every placement and structure cell matches the starting map. All twelve archived source documents retain their original SHA-256 hashes, nineteen current local links resolve, and no other room's role data changes. The current blurb, summary and artifact inventory now match the passage; historical field notes and the older walk retain their content with a notice identifying obsolete assumptions.

The existing `probe_grid_lines_discovery.gd` passes all 23 checks in the working project. It exercises museum stamping, the actual dot-release signal and TraceData copy, replay vertices, bounded enlargement, display-space rounding, preservation of source coordinates, paging and the casing's position beside the field. These are supplied-motion headless checks, not a full walk or a headset test. Startup logs the same unresolved UID and certificate-store messages seen in the Trace checks; the probe exits successfully with no failed assertions. Compact editorial results are in `validation.json`.

The preceding Trace checkpoint is committed as `e2303f82f`. This Grid revision is left as the next reviewable working change.

## Selected Grid feedback, 16 September 2026

Palle supplied a further review identifying the snapping sphere as the conceptual centre, the walking record as its extension into ordered visits, the replay as reuse, and the plan as shared placement. The first two sections remain unchanged. The replay is shortened around recognising the bend at another scale; the plan now gives two indices one practical job, specifying a cube's placement. The exit explicitly returns to naming a place and returning together before Triangle.

The chapter is now 852 words, down from 890 in the immediate preceding version. Its two code passages and all four primary markers remain. The preceding chapter and companion note are preserved under `before-selected-feedback/` with their hashes. All twelve earlier archived sources are also intact. The map and role files are byte-for-byte unchanged during this feedback pass, and twenty current local document links resolve. The prior 23-check runtime result is retained as prior evidence; no runtime code changes or fresh headset claims are made here.

Two distinctions in the review are qualified in the companion note: the chosen snapping operation creates the many-to-one equivalence, and transformation of stored positions does not require quantisation or integer indices. The review's older-version warning matches local Git HEAD: it has five Grid artifacts, whereas the working map has seven, including `plan_vitrine` and the Modulor reference. The working intent was already revised. No commit or push is performed as part of selecting this feedback.
