# Portals feedback: convergence and chosen sufficiency

Applied the review supplied in attachment `87a85fdc-b17e-4b4d-9ab0-4d3175e32621` to `commons/maps/Primitives_Portals/final.md` only. The previous text is preserved in `before-final.md`.

The opening now continues Ignorance's question of sufficient representation. Convergence is named after the visitor observes the polygon comparison. A tolerance exercise asks the visitor to choose a positive acceptable gap and check the existing readout; it does not invent an adjustable threshold control. This exercise happens while the visitor is at that panel, avoiding a required return along the long installation after Achilles.

REVERSE, the corner worth keeping, the distinction between polygon error and torus accuracy, and the separation of displayed position from stage target remain. Achilles is tightened around the difference between the last displayed stage and the continuing mathematical sequence. The ending preserves the handover to Melencolia and adds: “The limit gives the sequence a direction. It does not tell us where to stop.”

The short topology footnote preserves the distinction between resolution and an opening, while its Euler-count detour remains in the archived version. The review's phrase “a finite sequence can approach a limit” was not adopted as a definition: the text distinguishes a finite displayed portion from the indefinitely continued rule and makes non-attainment a property of this particular polygon sequence.

Verification: both code excerpts match their current runtime scripts; the live book API serves the revised final; its two artifact markers match the primary roles. ReactMarkdown/remark-gfm renders the two code blocks and four footnotes with resolving backlinks. Hashes establish that the 23 recorded supporting, spatial, role, runtime and Melencolia files remain unchanged. No Godot rerun was needed for this prose-only revision.
