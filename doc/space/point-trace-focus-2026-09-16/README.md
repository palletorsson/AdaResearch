# Point_Trace — selected feedback, 16 September 2026

Palle approved selecting useful parts of the attached review rather than treating every suggestion as a requirement. The revised book follows four decisions through three primary token types: when movement is admitted, where a position may land, which part of the tool is observed, and when contact makes a mark.

The drawing dots, stick and whiteboard remain primary. The telemetry and automatic-writing desk are secondary return visits, alongside the science screen. Their saved card positions move with their roles. All nine physical placements remain; only the stale map metadata count changes from five to nine.

The book preserves the loop/pause experiment, the coarse recorder as a useful drawing instrument, the stick-tip comparison and the whiteboard's lifted stroke. The capacity limit becomes a brief observation. Longer inquiries in timing and interpretation are available in the room's detours.md rather than required by the book.

The Grid ending was checked against TraceData.add_trace() and grid_lines._display_frame(): a released list with at least two positions is copied, then the next display centres and scales it. The source positions remain unchanged. A fivefold enlargement is capped to fit the longest dimension within five metres, so “It may have grown” is supported.

The intent, tutorial, technical reference and critical text now distinguish the selected positions from the event of movement. The older timestamp, disk-storage, fading and simplification sketches are retained under previous/. They include obsolete descriptions and unverified proposals, and are not the current interaction contract. previous-sha256.json records each source file's bytes before editing.

The telemetry source check exposed a further issue: TRACKING is selected by the existence of a controller node, without active-pose or active-rig validation. The optional encounter now states that limitation. Correct source selection is an actionable later implementation task; this pass changes no runtime scripts.

The critical reference connects this room to the project's entropic-morality essay as a method for making reductions answerable. It does not assign an entropy value to a point count or claim that fine/coarse recording determines a political meaning by itself.

Validation: the chapter is reduced from 1,127 to 943 whitespace-separated words, including markers and code. The actual necklace room resolver returns draw_dot, draw_stick and whiteboard in book order: three primary types across six placements. All nine artifact coordinates are unchanged. Map metadata, local document links, code-fence pairing and targeted diff whitespace checks pass. All five previous documents match their archived SHA-256 hashes. Other rooms' role data is unchanged. No runtime or headset test was needed or claimed for this editorial pass.

## Commit checkpoint

The subsequent commit also gathers the previously implemented Trace room layout, cased counter and separate whiteboard tools. The coordinate-preservation statement above compares the editorial pass with its immediate starting state; the commit includes earlier layout changes relative to Git HEAD. Only this room's shared roles and museum-plan row are staged.

Before committing, the real-scene headless probes pass 19 counter/display checks and 59 museum-stamping, pickup, contact, quantisation and release checks. Each probe now creates its own report directory. These runs use supplied motion, with no GPU pixel or headset usability claim. Startup also logs an unresolved UID and a certificate-store error; neither interrupts the probes, whose reports contain no failed checks.
