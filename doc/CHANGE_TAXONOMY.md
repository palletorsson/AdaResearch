# Change, taught in the order the engine needs it

> Sixth sequence through the recipe (2026-08-27). Cheat-code: **the engine does
> calculus numerically, every frame — `_process(delta)` is a Riemann sum you live
> inside.** derivative = (now − before) / delta; integral = x += rate × delta; the
> fundamental theorem is the fact that the engine runs both at once and they agree.

June's four concepts (Rate / Accumulation / Flow / Reconciliation) were already
calculus-true — the thinnest canon in the corpus, but honest. The refine grounds them
in the frame and closes the loop toward wavefunctions. Sequence truth kept: *"Things
change. Change accumulates. Accumulation flows."* Live at
**localhost:3003/change-concepts** — 23 tiles, 8 sections.

## The ladder

1. **The frame** — the world is a flipbook; delta is each page confessing its duration.
2. **State** — a `var` is memory carried across frames.
3. **The rate** — the derivative as the engine knows it: two frames and a division.
   (descent_marble_toy, gradient_descent_well, derivative_pair …)
4. **Accumulation** — the integral as the engine runs it, forever. (integral_area, Riemann)
5. **The reconciliation** — the FTC, proven per-frame by running both at once. (ftc_bridge)
6. **Flow** — a rate at every point: change with an address. (the flow-field family)
7. **The loop** — `fmod(t, T)`: change that comes home; the door to wavefunctions.

## The super: grand_zoetrope

A Victorian zoetrope grown into a calculus engine — the drum remembers twelve
stillnesses of one bounce fast enough to be motion (the engine's whole secret); the
slope batten stands between frames n−1 and n as (now − before) / delta; a sand column
rises as x += rate × delta while its rate and level dials agree on the fundamental
theorem (the level runs the closed-form integral of the same rate, so the agreement
is genuine); two ghost-bead lanes circle the base — the delta lane evenly spaced, the
red lane that forgot delta bunching and stretching, spacing as the confession; a
skirt of vanes leans to a field; and the crown is an arrow eating its own tail.
Probe: 118 meshes, 0 broken. Seated in The frame.

Old sections survive regeneration by the carry-forward rule (the leftover June "Rate"
pair rides along until a chip decides).
