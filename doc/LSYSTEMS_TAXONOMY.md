# L-systems, taught in the order the engine needs it

> Twelfth sequence through the recipe (2026-08-27). Cheat-code: **no primitive at all —
> a string that rewrites itself, and then a turtle that believes it.**

The alias was already correct here (`lsystems` → `lsystem`, the bespoke builder's canon
of 18 real concepts — the fractals bug did not repeat). So this is a pure refine: three
rungs inserted at source in `tools/build_lsystem_concept_map.py`, before the phenomena
the canon already covers well.

## The three inserted rungs

1. **The alphabet** — `F + - [ ]`: characters that mean NOTHING yet. Before a rule and
   long before a plant, the sequence needs marks nobody has interpreted.
2. **The two halves** — the string knows no geometry. Rewriting and DRAWING are separate
   decisions; hand the same sentence to a different turtle and you get a different
   plant, which is exactly why grammar generalises past botany into cities and dungeons.
3. **The stack** — `[` pushes the turtle's state, `]` pops it. Branching is a stack, and
   that one data structure is the whole difference between a curve and a tree.

Then the inherited 18 follow: axiom & productions, parallel rewriting, Chomsky, turtle
interpretation, Koch, dragon, Hilbert, bracketed branching, trees, ferns, coral,
stochastic, parametric, context-sensitive, grammar architecture, CritterDNA → tree,
forest ecology. Live at **localhost:3003/lsystems-concepts** — 99 tiles, 21 sections.
Truth kept: *"A sentence can become a forest. Grammar is generative."*

## The super: the_believing_turtle

A scriptorium making the second rung's argument physical. On the lectern: five brass
glyphs that mean nothing. Beside them the rule board and a generation ladder whose bars
show the string exploding 1 → 5 → 21 → 89 symbols by parallel rewriting.

Then the proof: **three turtles read the SAME final string** and each believes it
differently — 25°, 45°, and one that reads `F` as a step sideways — growing three
unmistakably different plants from one identical sentence. A brass column beside them
rises to the string's true maximum bracket depth, because branching is a stack. And a
stochastic pair closes it: same rules, two seeds, siblings that are family but not twins.

1,087 meshes, every plant rewritten and walked symbol by symbol at build time. Probe
0 broken, first try. Seated at The two halves.
