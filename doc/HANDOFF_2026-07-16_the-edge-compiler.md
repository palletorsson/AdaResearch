# Handoff — the edge compiler days (2026-07-13 → 07-16)

> For the next Claude session. Read this, then `doc/book/LOOK_RULINGS.md` (the
> rulings are the source), then `doc/threads.json` (the tracker, rendered at
> localhost:3003/threads). Everything below is committed on branch
> `palm-scanner-door-entry` (Ada) and `master` (ada_encyclopedia).

## The governing ruling (read before prioritizing anything)

Palle, 2026-07-13: **VR walking is NOT the gate.** He has walked; desktop shows
the gap. The finish line is **the edge between autogenerated and composed** —
"if you find a way to update the look of the organization we are after you
will push the project over the edge and be done. That also proves that
language and observation can do the whole work." Never recommend "walk it in
VR" as the priority action. Tracked as thread `the-edge` (first row).

## What was built: one compiler, authorship at every level

Four days turned separate systems into a single loop. Rulings L-010…L-018
(verbatim in LOOK_RULINGS.md) compiled into properties P-1…P-7:

| register | artifact | enter by | key piece |
|---|---|---|---|
| word | tutorial.md / walked.md | write a sentence | `tools/text_to_target.py` (P-6: text is the clock; prose 220wpm, code 80wpm) |
| time | desire curves + dwells | drag a curve / trim a clip | `tools/desire_timeline.py` (forward transform), `/desire-timeline`, `commons/data/artifact_dwell.json` (L-013: dwell is PER-ARTIFACT; L-015: duration is the SUM) |
| contract | templates / patterns / slots | paint | `/template-maps` (23 principals), `/template-gallery`, `/template-pattern-editor`, `commons/data/template_{subtemplates,patterns}.json` (P-7: the template is the placement contract) |
| space | the composed map | move a dot | `tools/script_compose.py` (registers: promenade/arrival/close; L-010 forward reading: axis discipline, dolly-in, scenes-not-rows; pins never dropped; FIT term chases desire targets in `doc/book/look_scripts/desire_targets/`) |
| observation | gaze ride / captures / live window | look | `tools/map_tournament.py` (14 strategies × experience judge), map-sim bridge (▶ Godot buttons → live window, scratch map `MapSim_Live`) |
| ruling | LOOK_RULINGS.md | one sentence | closes the loop |

`/space-time` = plan⇄filmstrip as ONE object (L-016); bars carry artifact
screenshots, dwell handles, zoom-scrolls; saves `SpaceTime_*` siblings only.

## Numbers that matter

- **Tournament, all 24 spine sequences** (`doc/reports/map_tournament_*.json`):
  336 candidates, 8 different champions (wfc 6, halls 5 — best generalist
  5.98). Thematic kinships emerged unprompted (physarum won swarmintelligence,
  flock won graphtheory). "No base algorithm wins" holds at map scale.
- **Text→target FIT 0.72–0.87** on primitives; the score is smoother than any
  performance — that gap is now measurable per map (the edge, literally).
- **Icons**: 1,981/2,070 artifacts have gallery front.png (found, not
  captured — `tools/find_artifact_icons.py`; gallery dir is GITIGNORED, the
  tool is the artifact). 89-item capture worklist in doc/reports.
- **Dwell register (ruling PENDING)**: 52 dwells on 19 pages — primitives (25)
  + fractals (27) — inset long-takes in walked.md distilled ONLY from each
  map's critical.md/technical.md, provenance lines, computed reading times.
  Walks untouched (verified 0 deletions). `/chapter` has TTS: registers speak
  differently (dwell slower/lower, turn dry), 1.5× default, speed slider.
- **Composed demo maps**: `TemplateMap_Contract`, `TemplateMap_Promenade13`
  (13×47, uses Palle's own `inner_podium` pattern), `TemplateMap_Descent13`
  (stacks/pit/amphitheater, randomness cast). All pathfinder-clean, sidecars
  included (`template_layout.json` round-trips in /template-maps).

## Gotchas the hard way

- Encyclopedia dev server: port 3003, often already running from another
  session — check `curl localhost:3003` before starting; EADDRINUSE is fine.
- Map saves are SIBLINGS only (Script_/Grown_/SpaceTime_/TemplateMap_...).
  Exit convention: teleport cell must be VOID with a floor row past it
  (pathfinder Rule 5).
- gaze_ride's walk is greedy-nearest — a stated proxy, not a player. Desire
  kinds are name-heuristics until interaction DNA is wired (declared dwells in
  artifact_dwell.json always win).
- **book-11**: several fractals critical.md/technical.md files are OFFSET from
  their maps (CantorSet↔radial circles, KochSierpinski↔Cantor, GoldenSpiral↔
  Menger). Fix before the fractals voice pass.
- Book FREEZE (R-020) still nominally in force; the edge work superseded it by
  Palle's direction, but don't spawn unrelated new editors.
- Coverage gate runs on commits touching maps — keep 179/179.
- Blog posts (data-true images, generated from real files): 07-14 desire-
  space-over-a-timeline, 07-14 the-box-meets-the-judge, 07-15 the-text-
  compiles-the-room, 07-15 paint-the-contract, 07-16 the-walk-learns-to-stop.

## Open, in priority order (mirror of doc/threads.json)

1. **Palle's rulings**: the dwell register (pilot on 19 pages, awaiting one
   sentence); L-00x rulings on the /look-pairs three-column captures; the
   look's remaining open items edge-1/3/4/5.
2. **The big connect (proposed, agreed in principle)**: compile TRANSFORMATION
   end-to-end — walked.md pages (trench-style, with rulings) → text_to_target
   → templates/tournament stage its rooms → captures. First chapter whose
   rooms are performances of its writing; serves book-1 and WP3 at once.
3. Wire template-map SLOTS into script_compose as authorship loci (edge-13's
   open half); judge TemplateMap_* with the tournament fitness.
4. R-027 floating text (~70 artifacts — very visible in Descent13's capture),
   book-11 source re-alignment, transformation has NO walked.md yet (that's
   the writing).

Re-entry ritual: `python tools/sequence_pipeline_scorer.py` for pipeline
state, `/threads` for the rows, LOOK_RULINGS.md for the law, this file for
the shape. The voice of the project's method, one line: **rulings are the
source; prose, curves, contracts, and rooms are compiled.**
