---
title: "Coordinate System — The Frame of Reference"
primitive: coordinate-system
sequence: primitives
phase: F_order
lens: Perspective
question: "Whose zero? Whose axes?"
insight: "The coordinate system is a lens, not the space. Same point, different stories."
qfep: "Whose origin? Whose axes? The frame of reference is never neutral — it centers someone."
status: draft
web_editor: /primitives/coordinate-system
tags: [geometry, perspective, convention, basis, reference-frame]
connections: [point, vectors, grid]
---

# Coordinate System — The Frame of Reference

**Lens:** Perspective — *Whose zero? Whose axes?*

**Status:** Ontology pending — needs discussion

## Discussion

The coordinate system is not the space. It's a **lens placed on the space**.
The same point exists regardless of coordinates. But the coordinates
determine what's easy to say and what's hard.

## QFEP Connection

Whose origin? Whose axes? The frame of reference is never neutral — it
centers someone. Right-hand vs left-hand rule is a convention, and
conventions are power.

## Open Questions

- Right-hand vs left-hand rule — a convention, but conventions are power
- Y-up (Godot) vs Z-up (Blender) — same world, different stories
- What does it mean to change basis? Translation of worldview?

## Connections

- **Point** — the coordinate system gives points their numerical identity
- **Vectors** — change of basis transforms how vectors are expressed
- **Grid** — the coordinate system determines the grid's orientation and scale
