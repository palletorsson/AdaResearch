# `draft/`

In-progress chamber proposals. Created by `chamber.py init`, finalized by
`chamber.py finalize`, promoted to `approved/` or `rejected/` by their
respective subcommands.

If you find proposals here older than a few days with no captures, they
were probably abandoned mid-iteration — safe to either revisit or
`chamber.py reject --reason="abandoned"` them.
