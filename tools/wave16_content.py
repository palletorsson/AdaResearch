# wave16_content.py — the prose of the wave-16 gallery, imported by build_wave_gallery.py.
#
# Same discipline as 14 and 15: family census, a thesis someone could disagree with, every
# value glossed from the builder's registry note, a tail with the measurement and the miss.
# H1 and masthead written after the sweep was scored.

TITLE = "Wave 16 &mdash; the bit that is never read"

MAST = """
<div class="wrap">
<header class="mast">
  <p class="eyebrow">Ada Research &middot; wave 16 &middot; 15 August 2026 &middot; six agents, one orchestrator</p>
  <h1>I wrote six briefs.<br>The code overruled all six.</h1>
  <p>Wave 15 had four of six builders correct their brief from the members' source. This time it
  was six of six, and two of them refused a designed null I had handed them because the code
  forbids it. What came back instead: a rule one bit from another that renders identically
  because the bit is never read, a feedforward net that is not a tree, a chain of curves with
  gaps in it, an artifact whose own docstring describes a shape its coordinates cannot make,
  and a vocabulary drift that turned out to be my own tool sorting a list.</p>
</header>
<div class="bar"><span class="hint">6 syntheses &middot; 90 frames &middot; 12 axes, all biting &middot; 6 predictions, 3 hit &middot; 9 designed nulls, 9 held &middot; 6 of them 0.00% to the byte</span></div>
"""

SECTIONS = [
 ("rule_bench", "rule &middot; 5 members &middot; 4 vocabularies &middot; and they cannot be mapped onto each other",
  "A rule is a local law with no picture in it, and one bit of it can be unreachable.",
  """<p class="body">ca_bridge names its rules by NUMBER &mdash; 30, 90, 110, 250, the literal integer
  encoding of the lookup table. lifeform_walker names them by NICKNAME (life, coral). mold_network
  and structure_growth name them by TENDENCY (reinforce, explore). pheromone_terrain names them by
  the agent's ACT (trail, sniff). Four distances from the mechanism, and <b>the brief asked for them
  to be mapped onto each other</b>. The builder refused: there is no elementary rule that means
  <em>coral</em>, each member's own registry says its values are a different alphabet, and inventing
  a correspondence is the fabricated-declaration fault this programme has a gate for. It mapped the
  distance instead, which is what <span class="tok">reading</span> does.</p>
  <p class="body">ca_bridge's update is transcribed verbatim &mdash; periodic wrap, a single live
  cell at the centre &mdash; and verified by re-running it at the member's own 31&times;120, where the
  fills came out 48.0 / 23.5 / 49.7 / 87.5%, matching its registry's ladder note to the decimal.
  48&times;40 cells grown downward as a wall of 20&nbsp;mm cubes. <b>spacetime</b> is the whole wall,
  <b>front</b> the newest generation alone, <b>lookup</b> the eight neighbourhood entries built as
  eight three-cell tiles &mdash; the mechanism as an object, which is what a rule number is.</p>
  <p class="body">And it added a fifth rule the family does not ship: <b>218</b>. It exists to make
  the experiment. 218 is <b>01011010 with one bit flipped</b> from 90, and one bit from 250 &mdash;
  equal distance in the mechanism, both times.</p>""",
  [({"rule": "30", "reading": "spacetime"}, "30 &middot; spacetime"),
   ({"rule": "90", "reading": "spacetime"}, "90 &middot; the Sierpinski"),
   ({"rule": "218", "reading": "spacetime"}, "218 &middot; one bit from 90 &mdash; and identical"),
   ({"rule": "90", "reading": "lookup"}, "90 &middot; lookup &mdash; the mechanism itself")],
  "<b>The strongest result in the wave, and it was pre-registered as a constraint rather than a "
  "number.</b> 90 against 218 measures <b>0.00%</b> at spacetime and at front: rule 90 never "
  "produces three live cells in a row from this seed, so the one bit where they differ is never "
  "read. And because they render identically, their distance to any third rule must be identical "
  "&mdash; the builder wrote that it \\u201cmust be a double entry\\u201d before capture, and the "
  "sweep returned <b>90/250 = 0.09% and 250/218 = 0.09%</b> at front, <b>5.10% and 5.10%</b> at "
  "spacetime. Tied to the decimal, twice. One bit that matters and one that does not."),

 ("topology_net", "topology &middot; 3 members &middot; 2 vocabularies &middot; neural widths and Deleuze",
  "A feedforward network is not a tree, and the word it is actually a synonym for is pinched.",
  """<p class="body">neural_network_visualization and neural_networks_vr say <em>graded / flat / deep /
  widened / pinched</em>; rhizomatic_maze_space says <em>rhizome / tree / severed / plateau</em> &mdash;
  Deleuze and Guattari's vocabulary, and this project's theory spine. The brief claimed the two
  traditions name one thing: a feedforward net is strictly layered with no lateral edges and no
  return, so <b>deep</b> ought to <em>be</em> <b>tree</b>.</p>
  <p class="body"><b>It does not survive contact with the code.</b> The member's deep is 21 nodes,
  54 weights and <b>243 routes</b> from entry to exit; a tree on 21 nodes has 20 edges and exactly
  <b>one</b> route. Feedforward nets are <em>arborescent</em> &mdash; layered, directed, acyclic &mdash;
  which is not the same as a tree, and the difference is the whole point of the word D&amp;G chose.
  The builder found the real synonym by measurement instead: <b>pinched and tree both have minimum
  vertex cut 1</b>. That is the thesis now, and it is a better one, because it says what the two
  traditions actually share &mdash; a single point of failure &mdash; rather than assuming they
  share a shape.</p>
  <p class="body">24 nodes of fixed budget under every topology. <b>net</b> is nodes and rods;
  <b>routes</b> draws every entry-to-exit path as a tube, where a tree has one and a rhizome has
  many; <b>cut</b> builds the narrowest cross-section as geometry. <em>flat</em> is not equal widths
  &mdash; both members set no hidden layer at all &mdash; and <em>plateau</em> was declined because
  its whole bite is extent, which the equal-envelope rule erases.</p>""",
  [({"topology": "tree", "reading": "net"}, "tree &middot; one route"),
   ({"topology": "rhizome", "reading": "net"}, "rhizome &middot; 55 rods against 23"),
   ({"topology": "pinched", "reading": "cut"}, "pinched &middot; cut = 1, like tree"),
   ({"topology": "severed", "reading": "routes"}, "severed &middot; routes &mdash; nothing to draw")],
  "Predicted rhizome against tree in the net reading &mdash; the only two values on identical node "
  "positions, so the entire difference is 55 rods against 23. <b>#1 of 28.</b> And a three-way null "
  "held at 0.00%: severed has no entry-to-exit route and an empty minimum cut, so <em>routes</em> "
  "and <em>cut</em> emit no geometry at all and all three readings are one frame."),

 ("course_cube", "course &middot; 4 names &middot; ONE script &middot; the sixth time this has turned up",
  "Two moves in either order name one destination, and the axis is about the corner you turn.",
  """<p class="body">x_, y_ and z_translation_cube are three <code>.tscn</code> files over a single
  script, <code>axis_translation_cube.gd</code>, differing in one export &mdash; and the script's own
  header says <b>ONE BODY, FOUR NAMES</b>. That is the sixth one-implementation-many-names family
  found in this programme. translation_cube_demo carries the interesting vocabulary:
  <b>lift_lateral</b> and <b>lateral_lift</b> are the same two moves in the opposite order, and
  translations commute, so the two words name one point. <b>free</b> goes straight there.
  <b>lift_depth</b> turns a different corner; <b>lateral</b> is a single move.</p>
  <p class="body">So the axis distinguishes the PATH, not the RESULT &mdash; and the builder checked
  whether the member animates it. It does not: no <code>_process</code>, no timer, the elbow is
  standing geometry. <b>path</b> draws the route as a tube through the corner it turns; <b>ends</b>
  draws start and finish only; <b>frame</b> carries the three basis arrows along.</p>""",
  [({"course": "lift_lateral", "reading": "path"}, "lift_lateral &middot; up then across"),
   ({"course": "lateral_lift", "reading": "path"}, "lateral_lift &middot; across then up"),
   ({"course": "lift_lateral", "reading": "ends"}, "ends &middot; the same point"),
   ({"course": "free", "reading": "path"}, "free &middot; straight there")],
  "<b>Six nulls, six held, every one 0.00% to the byte</b> &mdash; lift_lateral, lateral_lift and "
  "free are mutually identical in both the <em>ends</em> and the <em>frame</em> readings, because "
  "each component of the end point takes exactly one float addition either way. The critic flagged "
  "the same three pairs CONDITIONAL without being told. And the builder found a defect in the "
  "source it was reading: <span class=\"tok\">translation_cube_demo</span> under "
  "<code>guide=ghost</code> draws only the last point, so <b>three of its five course values are "
  "byte-identical</b> &mdash; promoted 2026-08-05 with no null registered."),

 ("curve_pair", "curve &middot; 3 members &middot; 1 vocabulary &middot; and the two orders were my own tool",
  "The five curves are a chain closed under differentiation, with gaps in it.",
  """<p class="body">derivative_pair, ftc_bridge and integral_area are the fundamental theorem split
  across three objects, and <span class="tok">curve</span> is the one function all three take. The
  brief said the five words form a chain: differentiate a ramp and get a step, differentiate a step
  and get a spike. <b>Only the second half survives.</b> A ramp's derivative is a constant and a
  hump's is a half-cosine, and neither constant nor half-cosine is one of the five words. It is a
  chain with gaps, and the builder says so.</p>
  <p class="body">It also found that <b>hump is two different functions</b> in the family &mdash;
  ftc_bridge ships a parabola and says so at gd:164, the other two ship sin(u&pi;) &mdash; and chose
  the parabola against two of three members, because it is the only reading under which the family
  is a chain at all. <b>pair</b> is the function and its derivative, derivative_pair's subject;
  <b>area</b> is the signed area as a real slab with thickness; <b>bridge</b> stands all three
  together, which is ftc_bridge's subject and the theorem.</p>
  <p class="body"><b>And the two orders in the brief were not vocabulary drift.</b> The builder traced
  it to <code>apply_dna_block.py</code> putting the default value first when it derives a block
  &mdash; the tool this programme uses to keep declarations honest was manufacturing the appearance
  of disagreement. Checked across the corpus afterwards: <b>83 apparent vocabularies collapse to 49
  real ones</b> when the lists are compared as sets. Every family census in waves 13&ndash;16,
  including the ones on these pages, has been counting order as difference.</p>""",
  [({"curve": "ramp", "operation": "pair"}, "ramp &middot; pair &mdash; derivative is a constant"),
   ({"curve": "step", "operation": "pair"}, "step &middot; its derivative is the spike"),
   ({"curve": "hump", "operation": "area"}, "hump &middot; area as a slab"),
   ({"curve": "wave", "operation": "bridge"}, "wave &middot; bridge &mdash; all three")],
  "Predicted ramp against step in the area operation &mdash; both are the only odd members, so both "
  "slabs are one lobe below and one above with a signed total of zero and they differ only in "
  "profile. <b>#1 of 10.</b> The builder also predicted its own metric would saturate (the whole "
  "sheet in 58&ndash;69% focus, because this is line art in a void) and named FRAME as the statistic "
  "that ranks &mdash; which is the wave-15 denominator lesson arriving unprompted."),

 ("anchorage_colony", "anchorage &middot; 4 names &middot; 2 scenes &middot; one vocabulary, not two",
  "The colony does not choose its shape. The anchor layout chooses it.",
  """<p class="body">AntColonyV2 and ant_colony_v2 are one scene; PhysarumColony and physarum_colony
  are another. Both declare <em>pair / triad / ring / scattered</em>, and &mdash; per the finding in
  curve_pair's section &mdash; the two "vocabularies" my survey reported are one vocabulary in two
  orders. <b>pair</b> gives two anchors and one road. <b>triad</b> gives three, which is a Steiner
  problem: the cheapest join is a Y meeting at 120&deg;, not a triangle. <b>ring</b> gives many on a
  circle. <b>scattered</b> gives random positions &mdash; and both members already seed it, so the
  five-different-objects trap this corpus has been caught by was already closed here.</p>
  <p class="body">The builder overruled physarum's <b>triad</b>: its three attractors are collinear
  on z&nbsp;=&nbsp;x, so the three-armed Y its own docstring promises <em>cannot exist</em>. It took
  the ants' equilateral triad instead &mdash; and that produced the pass's best measurement: the nest
  settles exactly on the <b>Fermat point</b> of its own food, verified by the solve declining at
  0.15&nbsp;mm. <b>anchors</b> draws the posts alone, which is the thesis: the input already
  determines the answer. <b>network</b> grows the roads; <b>field</b> builds the nutrient relief.</p>""",
  [({"anchorage": "pair", "reading": "anchors"}, "pair &middot; anchors &mdash; the input"),
   ({"anchorage": "triad", "reading": "network"}, "triad &middot; the Fermat point"),
   ({"anchorage": "ring", "reading": "network"}, "ring &middot; not the ring"),
   ({"anchorage": "scattered", "reading": "field"}, "scattered &middot; field")],
  "Predicted pair against triad in the network reading; <b>#3 of 6</b>, the closest being pair "
  "against scattered. The builder had registered the reason in advance: the metric <b>saturates</b> "
  "on this artifact &mdash; anchors spreads only 37.6&ndash;39.1% and network 37.2&ndash;41.3%, while "
  "field spreads 53.2&ndash;66.5% &mdash; so only <em>field</em> resolves a ranking at all. A miss "
  "whose cause was written down before the capture is not the same kind of miss."),

 ("facets_sphere", "facets &middot; 3 members &middot; 1 vocabulary &middot; one implementation under three names",
  "A sphere in a polygon renderer is always a polyhedron; the axis is whether it admits it.",
  """<p class="body">grab_trihedron, sphere_high and sphere_low all declare <em>shown / hidden /
  only</em>. sphere_high and sphere_low are <b>not</b> one scene under two names &mdash; two scripts,
  two colours, two genuinely different second axes &mdash; but their <span class="tok">facets</span>
  halves are identical code, as is the trihedron's. One implementation under three names, which is a
  different pattern from the six one-scene families and worth counting separately.</p>
  <p class="body"><b>shown</b> draws the flat faces and admits the object is a polyhedron.
  <b>hidden</b> swaps a material &mdash; not normals, not the mesh, the builder checked and corrected
  the brief &mdash; so the same triangles present themselves as smooth. <b>only</b> draws the facet
  edges and nothing else: the construction without the illusion. The triangle count never changes
  across the axis; only what is admitted about it does.</p>
  <p class="body">The brief handed it a designed null: at a fine enough subdivision, shown and hidden
  should become indistinguishable. <b>The builder refused it, from the code.</b> The wireframe band is
  <code>dist_px/width</code> &mdash; about two pixels in <em>screen</em> space &mdash; so a finer mesh
  puts <em>more</em> ink on the frame, not less: 14.45% at 512 triangles against 14.68% at 20. It
  registered a falsifiable ordering instead: peak stays flat while area rises by half.</p>""",
  [({"facets": "shown", "body": "sphere_fine"}, "shown &middot; fine &mdash; admits it"),
   ({"facets": "hidden", "body": "sphere_fine"}, "hidden &middot; the same triangles"),
   ({"facets": "only", "body": "sphere_fine"}, "only &middot; the construction alone"),
   ({"facets": "shown", "body": "trihedron"}, "shown &middot; trihedron")],
  "Predicted shown against hidden on the trihedron; <b>#2 of 3</b>, the closest being shown against "
  "only. With three values there are three pairs, so a #2 is a weak result either way &mdash; and "
  "the refusal of the null is the section's real content: a designed null that the code forbids is "
  "worth more than one that holds."),
]

CLOSING_TEMPLATE = """
  <section>
    <div class="hd"><h2 class="tok">the scoreboard</h2><span class="fam">6 predictions &middot; 3 hit &middot; 9 designed nulls &middot; 9 held</span><span class="chip">6 nulls at 0.00%</span></div>
    <p class="thesis">Nine of nine nulls held and three predictions missed &mdash; and the misses are better documented than the hits.</p>
    <div class="mwrap"><table style="width:100%;border-collapse:collapse;font-family:var(--mono);font-size:12.5px">
      <tr style="border-bottom:1px solid var(--rule)"><th style="text-align:left;padding:6px 10px;color:var(--dim);font-weight:400">synthesis</th><th style="text-align:left;padding:6px 10px;color:var(--dim);font-weight:400">predicted</th><th style="text-align:right;padding:6px 10px;color:var(--dim);font-weight:400">rank</th><th style="text-align:left;padding:6px 10px;color:var(--dim);font-weight:400">closest pair</th><th style="text-align:right;padding:6px 10px;color:var(--dim);font-weight:400">%</th></tr>
      __ROWS__
    </table><p class="hint" style="margin-top:8px">Rank among non-null pairs, computed within the context each prediction names. Percent is a share of frame.</p></div>
    <p class="body"><b>Two builders refused a designed null I gave them, and both were right.</b>
    facets_sphere showed the code forbids its null &mdash; a screen-space wireframe band puts more ink
    on a finer mesh, not less &mdash; and registered a falsifiable ordering instead. topology_net
    showed <em>deep&nbsp;==&nbsp;tree</em> is false by counting: 243 routes against one. Both replaced
    a null I wanted with something checkable that I had not thought of. A brief that survives contact
    with the source is a brief nobody read carefully.</p>
    <p class="body"><b>Three defects in the corpus, found by builders reading their own sources.</b>
    translation_cube_demo has three byte-identical values under <code>guide=ghost</code>, promoted in
    August with no null registered. physarum_colony's <em>triad</em> places three collinear
    attractors, so the Y its docstring describes cannot be built. And the member's
    <code>_aligned_basis</code> returns a determinant of &minus;1, which lights its rails from
    inside; the builder fixed it in its own copy and reported the source rather than editing it.</p>
    <p class="body"><b>And one defect in my own tooling, found the same way.</b> curve_pair traced the
    "two orders" in its brief to <code>apply_dna_block.py</code> sorting the default value first. It
    reproduced the artifact while being built. Measured across the corpus afterwards: <b>83 apparent
    vocabularies are 49 real ones</b> &mdash; 27 axes where order alone made one vocabulary look like
    two or three. Every census in these galleries has been counting sort order as disagreement, and
    <span class="tok">anchorage</span>, <span class="tok">fusion</span>,
    <span class="tok">subdivision</span> and <span class="tok">taxonomy</span> are all single
    vocabularies that have been reported as plural.</p>
    <p class="tail"><b>What this wave does not settle.</b> The framing was left exactly where each
    builder set it, because every prediction was rastered at its own declared framing and re-solving
    would have invalidated the arithmetic &mdash; but that leaves <span class="tok">rule_bench</span>'s
    <em>front</em> at 0.77% of frame and <span class="tok">facets_sphere</span>'s <em>only</em> at
    0.45%, both thin, both pre-declared, neither re-measured from a second standpoint. And the
    corpus-wide vocabulary recount above is a number from one script run once; the census fix has not
    been put into the survey tooling, so the next wave will inflate again unless it is.</p>
  </section>

<footer><span>wave 16</span><span>6 syntheses &middot; 12 axes &middot; 3 of 6 hit &middot; 9 of 9 nulls held &middot; six agents, one orchestrator</span>
<span><a href="/synthesis-gallery">&larr; all galleries</a></span></footer>
</div>
"""


def closing(scores: dict) -> str:
    rows = ""
    for tok, s in scores.items():
        cls = "ok" if s["hit"] else "bad"
        rows += (f'<tr style="border-bottom:1px solid var(--rule)"><td style="padding:5px 10px;color:var(--brass)">{tok}</td>'
                 f'<td style="padding:5px 10px">{s["pair"]}</td>'
                 f'<td style="padding:5px 10px;text-align:right" class="{cls}">#{s["rank"]}/{s["n"]}</td>'
                 f'<td style="padding:5px 10px">{s["closest"]}</td>'
                 f'<td style="padding:5px 10px;text-align:right">{s["closest_pct"]}</td></tr>')
    return CLOSING_TEMPLATE.replace("__ROWS__", rows)
