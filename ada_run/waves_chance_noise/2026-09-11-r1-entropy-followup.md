# R1 follow-up handback to Astra — Random_Entropy

Fable 5.1, 2026-09-11, afternoon, from `doc/book/handoffs/fable-r1-entropy-followup.md` (Astra's partial acceptance; three bounded corrections and the record). Status: **corrections made and run — the hall's probe in both lanes bare 132 / 0, live 139 / 0, both exit 0 (15:16:07, 15:16:58); the standalone build-once probe 15 / 0; no script errors; Astra's corrected final, book hero and line preserved, two sentences of the final updated for the corrected ghosts.** Nothing committed; no forum message (none requested).

## 1. The origin reference follows the named source

`_measure` drew every `origin` ghost at `sequence_length / num_symbols` whatever the source. Now `active_probabilities()` — 1/N each for the shipped uniform source, 2⁻ᵏ / Σ 2⁻ᵏ under CONTRAST, from the one weight table the contrast sample is drawn from — and `expected_counts()` = p(k) · N give each ghost its own height after the display scaling (`clamp(expected / max count, 0, 1) · 0.1`). The shipped source keeps its single flat line, byte-identical in height; the concentrated law shows a descending staircase. The readout names the law and its expected counts at origin. Tested in the hall probe's new section 7b, in both orders — CONTRAST → DISCLOSE (origin) and DISCLOSE (origin) → CONTRAST — sorted and unsorted, then back to the arrival: probabilities sum to 1, expected counts to 200, every drawn ghost height equals its expected count's (values recorded per state in the JSON's `ghosts`), the shape reads as a staircase or a flat line, and the return leaves ledger, the first sample, as drawn, no ghosts. Measured (both lanes): under CONTRAST the expected counts 100.1 50.0 25.0 12.5 6.3 3.1 1.6 0.8 0.4 0.2 (sum 200.0) against a largest count of 109 give ghost heights 0.0918, 0.0459, 0.0230, 0.0115, 0.0057, 0.0029, then the 0.002 floor — a staircase, the same in both orders and with SORT on; the uniform source's expected 20.0 each against a largest count of 31 give 0.0645 for every ghost — one flat line; the readout's sixth line reads `law p∝2⁻ᵏ · expected 100.1 50.0 25.0 12.5 6.3 3.1 1.6 0.8 0.4 0.2` at origin under CONTRAST. Captured: `probe_entropy_origin_contrast_live.png` (the tall first bar with the pale staircase behind the live bars) beside `probe_entropy_origin_live.png` (the flat line behind the even sample).

## 2. Readable from standing

Under the ledger (only): every baked tag's meshes drawn unshaded, so the dark plates stay dark and the text keeps its colour under the museum's light; the forty-draw strip in two rows of twenty at 2.6 cm inside the panel's width (the shipped one-row strip was 1.11 m wide at 2 cm — wider than the panel); the LEDGER panel's cream face and the buttons' bright caps toned to a matte mid grey, their label plates untouched. The desk, the row of draws and the housed plate are as they were. Judged from three new captures — the standing approach (the corridor's edge, 2.2 m, eye height), the panel at 0.75 m, and the buttons from a standing eye — and the existing front view: at 0.75 m (`probe_entropy_panel_close_live.png`) the title, the glowing H, the tick numerals 0.0 / 1.0 / 2.0 / 3.0 / 3.32, `max = 3.32 bits (10 symbols)`, the symbol labels 0–9, the ten bars and both strip rows (`7 4 8 2 5 1 1 8 8 2 4 5 9 6 0 8 0 5 0 9` / `0 6 3 6 5 5 9 8 6 7 5 2 2 7 2 5 3 2 2 9`) read plainly on dark plates; from the standing approach at 2.2 m (`probe_entropy_approach_live.png`) the H, the bars, the two strip rows as bands, the desk's plate and the toned button panel read, the title and the small labels not yet; the buttons (`probe_entropy_buttons_live.png`) are three matte caps on a grey face with SORT · CONTRAST · DISCLOSE under them, read from a standing eye a step to the right. A headset reading and a hand's reach remain unassessed; a coordinate distance and a pointer hit are recorded as what they are.

## 3. The shared build-once repair, verified

`commons/testing/probe_wcn_entropy_buildonce.gd` (`bash tools/run_wcn_probe.sh entropy_once`): the shipped `stand:none` scene alone, outside the museum. 15 checks / 0 failures, exit 0. The default builds one panel (ten bars, ten labels, the formula, 42 children); configured to `origin` before `_ready` (the museum's order) it builds one panel of the same 55-child shape as when configured after `_ready` (the standalone grid's order), with ten ghosts and the source strip; `tally` after `_ready` gives 40 children with no formula; four disclosure steps walk the ladder and return the default's shape; seed 7 gives the same draws before or after `_ready`, different from the default's; the seeded sample and H are the same through every rebuild; the ledger before `_ready` builds one staging with 200 tiles, kept through a rebuild. No other placement of the meter carries a config token (94 tokens in 47 maps: 84 bare, 8 `:0:0`, 2 `:0:-0.5`), so the guard changes what no other placement builds.

## A fourth fault, found in the follow-up's own captures

The origin-plus-contrast capture showed an empty histogram region under the symbol labels while every height check passed. `_measure` writes each bar's and ghost's y in the shipped frame (about −0.13 m); the first measure precedes the lift, so the arrival was right, but after CONTRAST — or a probe's `measure()` — the bars had dropped 1.4 m into the desk. `_after_measure` lifts them again, and the probe now checks their places on the panel after every measure and in every ghost state (heights were checked, places were not). Verified in the rerun: after CONTRAST, after the return, and in all seven ghost states the twenty bars and ghosts stand between 1.22 and 1.27 m, on the panel whose centre is 1.40 m.

## 4. The record

Both accepted-partial R1 runner logs record **exit 0** (13:28:41, 13:29:22); my "exits 127" line was W3/W4's fact carried over without reading these logs, corrected in the report, the handback, the field notes and memory. The engine logs' `Unrecognized UID: "uid://rwex60pqapc"` is the project's `buses/default_bus_layout` reference (`project.godot`), present in all 13 saved runs of every hall and in the meter's standalone run. The `ObjectDB instances leaked at exit` warning is present in the three saved runs of the randomness chapter (Random_Definition live, both R1 runs), absent from the ten Waves runs and from the meter's standalone run, and present in a hall where the meter is not placed — not the meter's; its scope (the chapter's other bodies, or the museum's randomness props) is not investigated further here. The accepted-partial logs, JSON and captures are retained in `ada_run/waves_chance_noise/Random_Entropy/history/2026-09-11-1328-r1-accepted-partial/`.

## Changed files and fingerprints

`commons/artifacts/shannon_entropy_meter/shannon_entropy_meter.gd` (active law, expected counts, ghost heights, unshaded tags, two-row strip, toned panel, the readout's law line, the state API); `commons/testing/probe_wcn_entropy.gd` and its live port (section 7b, three captures); `commons/testing/probe_wcn_entropy_buildonce.gd` (new); `tools/run_wcn_probe.sh` (`entropy_once`); `commons/maps/Random_Entropy/{final.md, tutorial.md, technical.md, field_notes.md}` (only the sentences the corrected ghosts require, and the follow-up record); `ada_run/waves_chance_noise/Random_Entropy/report.md`, `2026-09-11-r1-entropy-batch.md`, this file. Hashes after: `doc/space/waves-chance-noise-r1-entropy-2026-09-11/after_sha256.txt` (regenerated).

## Remaining human and headset observations

A person at the desk: SORT, CONTRAST, DISCLOSE twice, the plate and the two-row strip at their own distance, the buttons under a hand; a headset walk of the corridor and the approach; Astra's acceptance.
