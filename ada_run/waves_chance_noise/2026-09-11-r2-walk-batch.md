# Batch handback to Astra — R2: Random_Walk

Fable 5.1, 2026-09-11, evening, the next hall on the route after Random_Remove ("carry the changing set into the next room, where a walker takes its steps from draws like these"). Status: **implemented and run in both lanes — bare 106 / 0, live 112 / 0, both exit 0; the keypad and the wing through the push buttons' own signal path, the desktop rig's pointer pressing 2D, NEW SEED and RESET — awaiting your acceptance, a person at the case and a headset walk.** Nothing committed; forum heads-up 260911-13tnx.

## The room

**Question:** what does a trail remember that the next step does not use? **Encounter:** the shipped cabinet — a glass tank on a dark case at 0.92 m, the keypad 2D · 3D · LEVY / RESET on a wedge at 0.78 m, the seated stats screen — stands on the recovered arena floor beside the north door strip, its front toward the visitor. Under `stand:logbook` a wing hangs on its flank: a plate at 1.03 m with six lines (the rule and its dimension; steps and simulation time at thirty a second; the frame clock, frames and the catch-up cap of five; the trail kept of two hundred and who is followed; the named five-digit seed and RESET's policy; the glass rule), and ONE · ALL / NEW SEED at 0.76 m. One bead is red and followed, four are dimmed. RESET replays the seed cube for cube; NEW SEED names another; the same seed under 2D and 3D spends its first draw as a heading and as an azimuth. The final reads the increment update, the reflection rule and LEVY's cap from the source, asks the visitor to predict from a trail and to find the wall behind a return, and hands the many draws to Random_Gaussian.

**Changes:** the map's arena `0` → `1` (the museum read holes; a starry void with a one-cell strip and an unreachable ring), the terrarium's cell `3` → `1`, `random_walk_128` scaled to a fifth and moved west, the rack off the door strip, the lab room off the door approach with its `dx_i ~ Normal(0, sigma)` annotation replaced, the pixel clouds onto the ring cells, `artifact_placement: map`, one rect. `random_walk_terrarium.gd`: `stand:logbook` (opt-in), `follow_one`, a `seed` alias, and the keypad's RESET relayed through a one-argument handler — the shipped connection had never fired. Texts — final revised (excerpts verified), blurb, intent, summary, critical, technical, tutorial reconciled to the real cabinet and hall (the old ones described a pit viewed from a deck, a 128-step visualisation and Gaussian steps); `field_notes.md` new. Full account: `Random_Walk/report.md` and the map's field notes. The captures, with W4, R1 and R1b's, are on the guide's [runtime captures page](http://localhost:3003/research/waves-chance-noise/runtime-captures.html) (`python tools/build_wcn_captures_page.py`).

## Evidence returned

| item | state |
|---|---|
| 2D / 3D / LEVY and reset through actual controls | done, through the push buttons' own signal path in both lanes and the desktop rig's pointer in the live lane (2D, NEW SEED, RESET); the keypad's RESET had never fired before the relay — measured, then fixed, then measured again (30 → 0) |
| in 2D the constrained coordinate; bounds after long steps | done: 300 2D steps every y 0.20, x and z inside ±0.25; the fold on given points; 3D's floor and lid; LEVY 2000 draws never over 0.15 (max 0.138 / 0.145), 300 LEVY steps all inside — one reflection suffices because the cap is under half the box |
| capped trail and step catch-up, accurate counts; reset separated from deterministic replay | done: the trail 200 of 300 steps; a two-second frame is five steps; the screen, the logbook and the counter agree; RESET under the named seed lands sixty steps bit for bit, under the shipped stream it does not, and the plate says which |
| the seed and boundary treatment explicit; step rule compared apart from dimension | done: the plate names the seed, RESET's policy and the glass rule; the same seed's first draw is the heading in 2D and the azimuth in 3D (equal to 1e-4 rad); 2D against 3D under one fixed step, LEVY against 3D under one dimension |
| vents/creatures away from the observation space or deliberate; Gaussian annotation corrected | done: the vent requires the bracelet and no brood stands in the hall; the lab room, moved off the door approach, no longer claims `dx_i ~ Normal(0, sigma)` |
| museum captures, walks, streaming | done: eight views per lane by their intended cameras plus the rig's front view; the route walked door to door by capsule in five legs; the streamer frees and rebuilds the hall with the wing |
| a person at the case; headset | pending |

## Limitations, stated

The runs are desktop probes: no hand on the keypad, no eye at a person's height. The deterministic sections drive `_step_all_walkers` directly with processing off; the live stream is measured only as a rate. The floor recovery raises the grid lane's arena by half a metre (the ground plane to the strip's level); the ring stays a metre above it and is unreachable on foot in the museum, as the recovery left it. The pixel clouds are towers of one-metre cubes through the roof, as the artifact builds them everywhere. The lab room logs four `SCRIPT ERROR`s per run from its own `bool(String)` on the window key — pre-existing, not touched. The terrarium's other placements are unchanged by construction (`stand` defaults to none; the RESET relay is the only shipped-behaviour change, and it makes a dead button work).

## One reusable improvement, and its conditions

**The named seed on a bench whose rule can be changed, used a second time.** Random_Remove named a seed and replayed it across two eligibility masks; the terrarium names one and replays it across two step rules, and the readout says what RESET does with it. The pattern held in a second, genuinely different room without shared code: a private generator; the seed re-applied at reset before the state is restored; the seed short enough to read; the replay opt-in where the shipped behaviour kept its stream; the readout naming the policy in words, so a visitor is never promised a replay the button cannot give. A third room would justify extracting it.
