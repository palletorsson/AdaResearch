# Bouncing Ball — Deprecated

Legacy scripts from an earlier implementation of the bouncing ball simulation. These were replaced when the algorithm was rewritten as a Node3D VR artifact.

## Contents

- `Ball.gd` — Original ball behavior script (superseded).

The folder is retained for reference. See the parent [Bouncing Ball README](../README.md) for the current implementation.
