# Random Decay — Scripts

GDScript implementations for the random decay simulation.

## Contents

- `random_decay_manger.gd` — Manager controlling decay timing and probability for individual objects.
- `random_decay_multimesh.gd` — MultiMesh-based decay for rendering many decaying elements efficiently.

See the parent [Random Decay README](../README.md) for the decay probability model.
