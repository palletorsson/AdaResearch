The `Anomaly Detection` sandbox is a small theatre of learning. Within this chromatic engine room, the scene `anomaly_detection.tscn` and its companion script `AnomalyDetection.gd` lets hypotheses bloom like constellations.

Delicate gestures around `normal_sample_count`, `anomaly_sample_count`, `dataset_seed`, `z_score_threshold` change the rhythm of the system in an instant. In the control room, calls to `_ready()`, `_generate_samples()`, `_build_points()` keep the pulse steady. The whole scene leans toward discovery and delight.
