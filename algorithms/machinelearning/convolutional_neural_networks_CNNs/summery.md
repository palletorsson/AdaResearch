The `Convolutional Neural Networks (CNNs)` sandbox is a small theatre of learning. Within this luminous laboratory, the scene `convolutional_neural_networks_CNNs.tscn` and its companion script `ConvolutionalNeuralNetworksCNNs.gd` lets the mathematics breathe in color and motion.

Adjustable parameters wait for curious hands to sculpt the flow. Behind the scrim, functions such as `_ready()`, `_process()`, `create_image_grids()` keep the pulse steady. What emerges is a living sketch of learning in motion.
