The `Explainable AI (XAI)` sandbox is a small theatre of learning. Within this chromatic engine room, the scene `explainable_AI_XAI.tscn` and its companion script `ExplainableAIXAI.gd` lets hypotheses bloom like constellations.

Adjustable parameters wait for curious hands to sculpt the flow. In the control room, calls to `_ready()`, `_process()`, `create_input_particles()` keep the pulse steady. The whole scene leans toward discovery and delight.
